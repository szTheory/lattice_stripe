---
phase: 39-credit-note-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/lattice_stripe/credit_note_test.exs
  - test/integration/credit_note_integration_test.exs
  - test/support/fixtures/credit_note.ex
  - lib/lattice_stripe/credit_note.ex
  - guides/credit_notes.md
autonomous: true
requirements:
  - CRDN-01
  - CRDN-02
  - CRDN-03
  - CRDN-04
  - CRDN-05
  - CRDN-06

must_haves:
  truths:
    - "Fresh targeted CreditNote unit and integration evidence exists from the current closure window"
    - "Any code or docs touched in this plan are narrow evidence repairs only, not reopened feature work"
    - "The public CreditNote API surface remains unchanged while evidence is refreshed"
    - "Integration assertions remain honest about `stripe-mock` proving wiring and typed decoding rather than full real-Stripe lifecycle semantics"
  artifacts:
    - path: "test/lattice_stripe/credit_note_test.exs"
      provides: "Current unit-level proof for CreditNote API surface and parser behavior"
      contains: "describe \"create/3\""
    - path: "test/integration/credit_note_integration_test.exs"
      provides: "Current `stripe-mock` integration proof for CreditNote runtime wiring"
      contains: "@moduletag :integration"
    - path: "test/support/fixtures/credit_note.ex"
      provides: "Shared finalized/open invoice setup used by CreditNote integration evidence"
      contains: "create_creditable_invoice!"
  key_links:
    - from: "test/lattice_stripe/credit_note_test.exs"
      to: "lib/lattice_stripe/credit_note.ex"
      via: "request-shape and parser coverage for the public API"
      pattern: "CreditNote\\.(create|retrieve|update|list|stream!|preview|void|list_line_items|list_preview_line_items)"
    - from: "test/integration/credit_note_integration_test.exs"
      to: "test/support/fixtures/credit_note.ex"
      via: "integration proof depends on finalized/open invoice helpers"
      pattern: "create_(creditable_invoice|open_invoice_credit_note)!"
    - from: "guides/credit_notes.md"
      to: "lib/lattice_stripe/credit_note.ex"
      via: "lifecycle caveats and API naming stay aligned if a narrow docs repair is needed"
      pattern: "CreditNote\\."
---

<objective>
Refresh milestone-credible CreditNote runtime proof so Phase 39 can cite current targeted evidence instead of relying only on historical Phase 34 summaries.

Purpose: The milestone audit blocks on missing verification, and the phase context explicitly requires fresh resource-scoped unit and integration commands. This plan produces or re-confirms that proof and allows only the smallest evidence repair needed to keep the proof honest.
Output: Passing targeted CreditNote unit/integration evidence plus any narrow proof repair required to make that evidence stable and auditable.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-v1.3-MILESTONE-AUDIT.md
@.planning/phases/39-credit-note-verification-closure/39-CONTEXT.md
@.planning/phases/39-credit-note-verification-closure/39-RESEARCH.md
@.planning/phases/39-credit-note-verification-closure/39-VALIDATION.md
@.planning/phases/34-creditnote/34-01-SUMMARY.md
@.planning/phases/34-creditnote/34-02-SUMMARY.md
@lib/lattice_stripe/credit_note.ex
@lib/lattice_stripe/credit_note/line_item.ex
@guides/credit_notes.md
@test/lattice_stripe/credit_note_test.exs
@test/integration/credit_note_integration_test.exs
@test/support/fixtures/credit_note.ex

<interfaces>
Required fresh proof commands:
```bash
mix test test/lattice_stripe/credit_note_test.exs
mix test test/integration/credit_note_integration_test.exs --include integration
```

Current verification gap:
```text
No 34-VERIFICATION.md exists.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh CreditNote unit evidence and repair only narrow proof gaps</name>
  <files>
    test/lattice_stripe/credit_note_test.exs
    lib/lattice_stripe/credit_note.ex
    guides/credit_notes.md
  </files>
  <read_first>
    test/lattice_stripe/credit_note_test.exs
    lib/lattice_stripe/credit_note.ex
    lib/lattice_stripe/credit_note/line_item.ex
    guides/credit_notes.md
    .planning/phases/34-creditnote/34-02-SUMMARY.md
  </read_first>
  <behavior>
    - Treat the existing unit suite as the primary proof surface for CRDN-01 through CRDN-06.
    - Preserve all shipped API names and semantics.
    - If a failure or evidence hole appears, fix only the smallest code/test/doc issue needed to restore truthful proof.
  </behavior>
  <action>
Run `mix test test/lattice_stripe/credit_note_test.exs`.

If the suite is green and already proves the full CRDN matrix clearly enough, keep the code unchanged and record the fresh command result for Plan 02.

If the rerun exposes a real evidence gap, make only a narrow repair such as:

- strengthening or correcting a unit assertion
- clarifying a lifecycle caveat in `guides/credit_notes.md` or moduledocs
- fixing a small parser/request-shape regression that blocks current proof

Do not:

- add new public APIs
- broaden the CreditNote feature surface
- refactor unrelated billing modules
- turn this into a general code cleanup pass
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/credit_note_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/credit_note_test.exs` exits 0
    - CRDN-01 through CRDN-06 remain represented by current unit-level evidence
    - no new CreditNote public function or option is introduced
  </acceptance_criteria>
  <done>Fresh unit evidence for the CreditNote surface exists and any narrow proof gap has been repaired without reopening feature scope.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh CreditNote integration proof under stripe-mock and keep claims bounded</name>
  <files>
    test/integration/credit_note_integration_test.exs
    test/support/fixtures/credit_note.ex
    guides/credit_notes.md
  </files>
  <read_first>
    test/integration/credit_note_integration_test.exs
    test/support/fixtures/credit_note.ex
    guides/credit_notes.md
    .planning/phases/34-creditnote/34-02-SUMMARY.md
    .planning/phases/39-credit-note-verification-closure/39-CONTEXT.md
  </read_first>
  <behavior>
    - Keep the integration suite aligned with the phase context's honesty requirements about `stripe-mock`.
    - Prefer shape-first assertions and existing fixture helpers over deeper state assumptions.
    - Allow only evidence repairs that improve verifier credibility.
  </behavior>
  <action>
Run `mix test test/integration/credit_note_integration_test.exs --include integration` with `stripe-mock` running.

If it passes cleanly, capture the exact command result and use it as fresh resource-scoped proof in Plan 02.

If it fails or exposes an evidence-credibility gap, make only the smallest repair needed to restore truthful integration proof, such as:

- correcting a fixture precondition
- tightening a comment or assertion so the suite states exactly what `stripe-mock` does and does not prove
- fixing a narrow request/response wiring regression in existing CreditNote code

Do not:

- broaden into real-Stripe-only lifecycle enforcement
- add unrelated integration helpers
- modify adjacent Quote, Invoice, or Dispute flows
  </action>
  <verify>
    <automated>mix test test/integration/credit_note_integration_test.exs --include integration</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/credit_note_integration_test.exs` remains a dedicated CreditNote integration suite with `@moduletag :integration`
    - `mix test test/integration/credit_note_integration_test.exs --include integration` exits 0 with `stripe-mock` running
    - the suite still covers create/retrieve/update/list/preview/issued lines/preview lines/void at a shape-first level
    - the suite's comments and assertions stay honest about `stripe-mock` limitations
  </acceptance_criteria>
  <done>Fresh integration proof exists for the shipped CreditNote flow, and its confidence boundaries remain explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical Phase 34 summaries -> current verifier | Old completion claims can drift from current repo truth if not refreshed with new commands |
| `stripe-mock` integration run -> milestone acceptance | Over-claiming what integration tests prove can create a false closed state |
| Narrow proof repair -> shipped API surface | Small verification fixes must not smuggle in new feature work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | R | Unit evidence freshness | mitigate | Re-run the focused CreditNote unit suite and fix only the minimal issue needed for truthful proof |
| T-39-02 | R | Integration evidence credibility | mitigate | Re-run the focused integration suite under `stripe-mock` and keep assertions/comments explicit about bounded confidence |
| T-39-03 | T | Scope boundary | mitigate | Forbid new API design, broad cleanup, or adjacent-family changes in this plan |
</threat_model>

<verification>
- `mix test test/lattice_stripe/credit_note_test.exs`
- `mix test test/integration/credit_note_integration_test.exs --include integration`
</verification>

<success_criteria>
- Fresh targeted CreditNote unit and integration proof exists from the current closure window.
- Any edits made in service of that proof are narrow and do not broaden the CreditNote feature surface.
- Plan 02 can cite current evidence without pretending the phase performed a repo-wide re-verification.
</success_criteria>
