---
phase: 10-documentation-guides
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe.ex
  - lib/lattice_stripe/resource.ex
  - lib/lattice_stripe/transport.ex
  - lib/lattice_stripe/transport/finch.ex
  - lib/lattice_stripe/retry_strategy.ex
  - lib/lattice_stripe/json/jason.ex
  - lib/lattice_stripe/request.ex
  - lib/lattice_stripe/webhook/cache_body_reader.ex
  - lib/lattice_stripe/webhook/plug.ex
  - lib/lattice_stripe/webhook/handler.ex
  - lib/lattice_stripe/error.ex
  - lib/lattice_stripe/response.ex
  - lib/lattice_stripe/list.ex
  - lib/lattice_stripe/event.ex
  - lib/lattice_stripe/config.ex
  - lib/lattice_stripe/client.ex
  - lib/lattice_stripe/form_encoder.ex
  - lib/lattice_stripe/testing.ex
  - lib/lattice_stripe/telemetry.ex
  - lib/lattice_stripe/customer.ex
  - lib/lattice_stripe/payment_intent.ex
  - lib/lattice_stripe/setup_intent.ex
  - lib/lattice_stripe/payment_method.ex
  - lib/lattice_stripe/refund.ex
  - lib/lattice_stripe/checkout/session.ex
  - lib/lattice_stripe/checkout/line_item.ex
  - lib/lattice_stripe/webhook.ex
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-06

must_haves:
  truths:
    - "Every public module has @moduledoc with purpose, usage examples, and Stripe API link where applicable"
    - "Every public function has @doc with params, return type, example, and error case"
    - "Non-obvious Stripe-specific code has inline comments with data shape examples"
  artifacts:
    - path: "lib/lattice_stripe/resource.ex"
      provides: "Resource module with real @moduledoc (not false) per D-03"
      contains: "@moduledoc \"\"\""
    - path: "lib/lattice_stripe/transport/finch.ex"
      provides: "Finch transport with @doc on request/1"
      contains: "@doc"
    - path: "lib/lattice_stripe/customer.ex"
      provides: "Customer with @typedoc"
      contains: "@typedoc"
  key_links:
    - from: "lib/lattice_stripe/resource.ex"
      to: "ExDoc Internals group"
      via: "@moduledoc content enables ExDoc rendering"
      pattern: "@moduledoc \"\"\""
---

<objective>
Add comprehensive @moduledoc, @doc, @typedoc, and inline code comments across all public modules.

Purpose: Every module and function is documented per DOCS-01, DOCS-02, DOCS-06 so developers never encounter undocumented API surface.
Output: Enhanced documentation annotations across ~27 source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-guides/10-CONTEXT.md
@.planning/phases/10-documentation-guides/10-RESEARCH.md

<interfaces>
<!-- Current @doc coverage from RESEARCH.md audit -->
<!-- HIGH priority (0 @doc): Resource, Transport.Finch, Json.Jason, Request, RetryStrategy.Default -->
<!-- MEDIUM priority: Client (enhance), Error (@typedoc), Webhook.CacheBodyReader, Webhook.Plug -->
<!-- LOW priority (already good): PaymentIntent, Customer, SetupIntent, PaymentMethod, Refund, Checkout.Session, Event, Webhook, Telemetry, Testing, List, Config -->

From lib/lattice_stripe/resource.ex:
  @moduledoc false  <!-- MUST change to real @moduledoc per D-03 -->
  def unwrap_singular/2, unwrap_list/2, unwrap_bang!/1, require_param!/3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core and internal module @doc/@moduledoc/@typedoc</name>
  <files>
    lib/lattice_stripe.ex,
    lib/lattice_stripe/resource.ex,
    lib/lattice_stripe/transport.ex,
    lib/lattice_stripe/transport/finch.ex,
    lib/lattice_stripe/retry_strategy.ex,
    lib/lattice_stripe/json/jason.ex,
    lib/lattice_stripe/request.ex,
    lib/lattice_stripe/form_encoder.ex,
    lib/lattice_stripe/config.ex,
    lib/lattice_stripe/client.ex,
    lib/lattice_stripe/error.ex,
    lib/lattice_stripe/response.ex,
    lib/lattice_stripe/list.ex,
    lib/lattice_stripe/webhook/cache_body_reader.ex,
    lib/lattice_stripe/webhook/plug.ex,
    lib/lattice_stripe/webhook/handler.ex,
    lib/lattice_stripe/webhook.ex,
    lib/lattice_stripe/event.ex,
    lib/lattice_stripe/telemetry.ex,
    lib/lattice_stripe/testing.ex
  </files>
  <read_first>
    - lib/lattice_stripe.ex
    - lib/lattice_stripe/resource.ex
    - lib/lattice_stripe/transport.ex
    - lib/lattice_stripe/transport/finch.ex
    - lib/lattice_stripe/retry_strategy.ex
    - lib/lattice_stripe/json/jason.ex
    - lib/lattice_stripe/request.ex
    - lib/lattice_stripe/form_encoder.ex
    - lib/lattice_stripe/config.ex
    - lib/lattice_stripe/client.ex
    - lib/lattice_stripe/error.ex
    - lib/lattice_stripe/response.ex
    - lib/lattice_stripe/list.ex
    - lib/lattice_stripe/webhook/cache_body_reader.ex
    - lib/lattice_stripe/webhook/plug.ex
    - lib/lattice_stripe/webhook/handler.ex
    - lib/lattice_stripe/webhook.ex
    - lib/lattice_stripe/event.ex
    - lib/lattice_stripe/telemetry.ex
    - lib/lattice_stripe/testing.ex
    - .planning/phases/10-documentation-guides/10-CONTEXT.md (D-24 through D-29)
    - .planning/phases/10-documentation-guides/10-RESEARCH.md (@doc pattern lines 214-275)
  </read_first>
  <action>
Work through each file applying the documentation patterns from D-24 through D-29. For each file:

**Pattern for @doc on public functions (D-24, D-27):**
```elixir
@doc """
One-line summary.

## Parameters

- `param` - Description

## Returns

`{:ok, result}` on success, `{:error, %LatticeStripe.Error{}}` on failure.

## Examples

    {:ok, result} = LatticeStripe.Module.function(client, params)

    # Error handling
    case LatticeStripe.Module.function(client, params) do
      {:ok, result} -> result
      {:error, %LatticeStripe.Error{type: :invalid_request_error}} -> handle_error()
    end
"""
```

**Pattern for bang @doc (D-29):**
```elixir
@doc """
Same as `function/N` but raises `LatticeStripe.Error` on failure.

See `function/N` for parameter documentation.

## Example

    result = LatticeStripe.Module.function!(client, params)
"""
```

**Pattern for @typedoc on structs (D-28):**
```elixir
@typedoc """
A Stripe [ResourceName] object.

See [Stripe ResourceName API](https://docs.stripe.com/api/resource_name/object) for field definitions.
"""
@type t :: %__MODULE__{...}
```

Use `sk_test_...` in examples per D-14.

**Specific changes per file:**

1. **lib/lattice_stripe.ex** — Enhance @moduledoc to be a landing overview. Add brief description of what LatticeStripe provides, link to Getting Started guide, show quick example. The @doc on api_version/0 is already good.

2. **lib/lattice_stripe/resource.ex** — CRITICAL: Change `@moduledoc false` to a real @moduledoc per D-03. Describe as "Shared helper functions used by all resource modules." Add @doc to unwrap_singular/2, unwrap_list/2, unwrap_bang!/1, require_param!/3 explaining what each does with return types. Mark all functions with `@doc false` is NOT acceptable — they need real @doc since this module is in Internals group.

3. **lib/lattice_stripe/transport.ex** — Enhance @moduledoc explaining the Transport behaviour contract. Add @typedoc for any defined types. If callback docs exist, ensure they follow D-24 pattern.

4. **lib/lattice_stripe/transport/finch.ex** — Add @doc to request/1: describe it as the default Transport implementation using Finch, parameters (transport request map with method, url, headers, body, opts), return type.

5. **lib/lattice_stripe/retry_strategy.ex** — Enhance @moduledoc explaining RetryStrategy behaviour. Ensure callback @doc describes the contract.

6. **lib/lattice_stripe/json/jason.ex** — Add @moduledoc describing as Jason implementation of Json behaviour. Add @doc to each of the 4 callback implementations (encode!, decode!, encode, decode).

7. **lib/lattice_stripe/request.ex** — Add @typedoc to the Request struct. Enhance @moduledoc.

8. **lib/lattice_stripe/form_encoder.ex** — Check existing @doc on encode/1. Enhance @moduledoc if sparse.

9. **lib/lattice_stripe/config.ex** — Check existing @doc on 3 functions. Add @typedoc if struct exists. Enhance if needed.

10. **lib/lattice_stripe/client.ex** — Already has good @moduledoc and 4 @doc. Add @typedoc to Client struct per D-28. Check all public functions have @doc. Ensure examples use `sk_test_...`.

11. **lib/lattice_stripe/error.ex** — Add @typedoc per D-28 listing type, code, message, param, request_id, http_status fields. Add Stripe error codes link.

12. **lib/lattice_stripe/response.ex** — Add @typedoc per D-28. Check @doc completeness.

13. **lib/lattice_stripe/list.ex** — Add @typedoc per D-28. Check 3 existing @doc are complete.

14. **lib/lattice_stripe/webhook/cache_body_reader.ex** — Add @doc to read_body/3 explaining its purpose (caching raw body for webhook signature verification).

15. **lib/lattice_stripe/webhook/plug.ex** — Add @typedoc for options. Ensure init/2 and call/2 have @doc (may already). Add @moduledoc link to Stripe webhook docs.

16. **lib/lattice_stripe/webhook/handler.ex** — Check callback @doc completeness.

17. **lib/lattice_stripe/webhook.ex** — Add @typedoc if any types defined. Check 5 existing @doc follow D-24 pattern. Add Stripe webhook reference link to @moduledoc per D-26.

18. **lib/lattice_stripe/event.ex** — Add @typedoc per D-28. Check 6 existing @doc. Add Stripe events reference link per D-26.

19. **lib/lattice_stripe/telemetry.ex** — Already has 5 @doc and comprehensive @moduledoc. Light touch: verify examples use `sk_test_...`.

20. **lib/lattice_stripe/testing.ex** — Check 2 @doc are complete per D-24 pattern.

**Inline code comments per D-25 and D-06:**

Add short comments with example data shapes on Stripe-specific logic ONLY:
- Retry header parsing in client.ex (if not already commented): `# Stripe-Should-Retry: "true" | "false" | absent`
- Form encoding nested params in form_encoder.ex: `# %{"card" => %{"number" => "4242..."}} => "card[number]=4242..."`
- Webhook HMAC computation in webhook.ex: `# signed_payload = "timestamp.payload" => HMAC-SHA256 with webhook secret`
- Tolerance window check in webhook.ex: `# default 300s (5 min) — reject events older than tolerance`

Do NOT add comments on standard Elixir patterns (pattern matching, pipe operators, GenServer callbacks, etc.) per D-25.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix docs --warnings-as-errors 2>&1 | tail -5 && mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - lib/lattice_stripe/resource.ex contains `@moduledoc """` (NOT `@moduledoc false`)
    - lib/lattice_stripe/resource.ex contains at least 3 `@doc` annotations
    - lib/lattice_stripe/transport/finch.ex contains `@doc`
    - lib/lattice_stripe/json/jason.ex contains at least 2 `@doc`
    - lib/lattice_stripe/request.ex contains `@typedoc`
    - lib/lattice_stripe/error.ex contains `@typedoc`
    - lib/lattice_stripe/response.ex contains `@typedoc`
    - lib/lattice_stripe/list.ex contains `@typedoc`
    - lib/lattice_stripe/customer.ex contains `@typedoc`
    - lib/lattice_stripe/payment_intent.ex contains `@typedoc`
    - lib/lattice_stripe/event.ex contains `@typedoc`
    - lib/lattice_stripe/client.ex contains `@typedoc`
    - lib/lattice_stripe/webhook/cache_body_reader.ex contains at least 1 `@doc`
    - lib/lattice_stripe/form_encoder.ex contains a comment with `card[number]` or similar nested param example
    - `mix compile --warnings-as-errors` exits 0
    - `mix docs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>All core and internal modules have @moduledoc with purpose, all public functions have @doc per D-24 pattern, @typedoc on key structs per D-28, inline comments on Stripe-specific logic per D-25.</done>
</task>

<task type="auto">
  <name>Task 2: Resource module @typedoc + @moduledoc Stripe links + @doc polish</name>
  <files>
    lib/lattice_stripe/customer.ex,
    lib/lattice_stripe/payment_intent.ex,
    lib/lattice_stripe/setup_intent.ex,
    lib/lattice_stripe/payment_method.ex,
    lib/lattice_stripe/refund.ex,
    lib/lattice_stripe/checkout/session.ex,
    lib/lattice_stripe/checkout/line_item.ex
  </files>
  <read_first>
    - lib/lattice_stripe/customer.ex (existing @moduledoc and @doc)
    - lib/lattice_stripe/payment_intent.ex (existing @moduledoc and @doc)
    - lib/lattice_stripe/setup_intent.ex (existing @moduledoc and @doc)
    - lib/lattice_stripe/payment_method.ex (existing @moduledoc and @doc)
    - lib/lattice_stripe/refund.ex (existing @moduledoc and @doc)
    - lib/lattice_stripe/checkout/session.ex (existing @moduledoc and @doc)
    - lib/lattice_stripe/checkout/line_item.ex (existing @moduledoc and @doc)
    - .planning/phases/10-documentation-guides/10-CONTEXT.md (D-26, D-27, D-28, D-29)
  </read_first>
  <action>
These resource modules already have good @doc coverage (12-19 functions each). The work here is:

**1. Add @typedoc per D-28 to each resource struct.**

Pattern for each:
```elixir
@typedoc """
A Stripe [Resource] object.

See [Stripe [Resource] API](https://docs.stripe.com/api/[resource_path]/object) for field definitions.
"""
@type t :: %__MODULE__{...}
```

Stripe API reference paths:
- Customer: `https://docs.stripe.com/api/customers/object`
- PaymentIntent: `https://docs.stripe.com/api/payment_intents/object`
- SetupIntent: `https://docs.stripe.com/api/setup_intents/object`
- PaymentMethod: `https://docs.stripe.com/api/payment_methods/object`
- Refund: `https://docs.stripe.com/api/refunds/object`
- Checkout.Session: `https://docs.stripe.com/api/checkout/sessions/object`
- Checkout.LineItem: `https://docs.stripe.com/api/checkout/sessions/line_items`

**2. Add Stripe API reference link to each @moduledoc per D-26.**

Add to each @moduledoc (if not already present):
```
See the [Stripe {Resource} API](https://docs.stripe.com/api/{path}) for the full object
reference and available parameters.
```

Resource-specific Stripe doc links:
- Customer: `https://docs.stripe.com/api/customers`
- PaymentIntent: `https://docs.stripe.com/api/payment_intents`
- SetupIntent: `https://docs.stripe.com/api/setup_intents`
- PaymentMethod: `https://docs.stripe.com/api/payment_methods`
- Refund: `https://docs.stripe.com/api/refunds`
- Checkout.Session: `https://docs.stripe.com/api/checkout/sessions`
- Checkout.LineItem: (no separate Stripe page — skip link or link to Sessions)

**3. Verify existing @doc follows D-24 and D-27 patterns.**

For each resource module, spot-check:
- Create function @doc has: summary, params, returns, example with {:ok, result}, error pattern matching example
- Retrieve function @doc has: summary, params, returns, example
- List function @doc has: summary, params, returns, example
- Bang variants have one-liner @doc per D-29 referencing the non-bang version

If any @doc is missing the error handling example (D-27), add it:
```elixir
    # Error handling
    case LatticeStripe.Customer.create(client, params) do
      {:ok, customer} -> customer.id
      {:error, %LatticeStripe.Error{type: :card_error} = err} -> handle_card_error(err)
      {:error, %LatticeStripe.Error{} = err} -> handle_error(err)
    end
```

**4. Ensure examples use `sk_test_...` per D-14.** Replace any `"test_key"`, `"sk_test_abc"`, or placeholder strings with `"sk_test_..."` format.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix docs --warnings-as-errors 2>&1 | tail -5 && mix test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - lib/lattice_stripe/customer.ex contains `@typedoc` and `docs.stripe.com/api/customers`
    - lib/lattice_stripe/payment_intent.ex contains `@typedoc` and `docs.stripe.com/api/payment_intents`
    - lib/lattice_stripe/setup_intent.ex contains `@typedoc` and `docs.stripe.com/api/setup_intents`
    - lib/lattice_stripe/payment_method.ex contains `@typedoc` and `docs.stripe.com/api/payment_methods`
    - lib/lattice_stripe/refund.ex contains `@typedoc` and `docs.stripe.com/api/refunds`
    - lib/lattice_stripe/checkout/session.ex contains `@typedoc` and `docs.stripe.com/api/checkout/sessions`
    - lib/lattice_stripe/checkout/line_item.ex contains `@typedoc`
    - `mix docs --warnings-as-errors` exits 0
    - `mix test` exits 0 (no regressions from doc changes)
  </acceptance_criteria>
  <done>All 7 resource modules have @typedoc on struct, Stripe API reference links in @moduledoc, and @doc follows D-24/D-27/D-29 patterns with error handling examples.</done>
</task>

</tasks>

<verification>
- `mix docs --warnings-as-errors` passes
- `mix test` passes (no regressions)
- `mix compile --warnings-as-errors` passes
- Every public module has @moduledoc (grep confirms no @moduledoc false anywhere)
- Resource modules link to Stripe API docs
</verification>

<success_criteria>
- Zero modules with @moduledoc false
- Every public function has @doc
- @typedoc on Error, Response, List, Event, Client, and all 6 resource structs
- Inline comments on Stripe-specific logic (form encoding, retry headers, webhook HMAC)
- mix docs generates clean documentation with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-guides/10-02-SUMMARY.md`
</output>
