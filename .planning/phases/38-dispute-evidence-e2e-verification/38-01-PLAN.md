---
phase: 38-dispute-evidence-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/dispute_integration_test.exs
autonomous: true
requirements:
  - FILE-01
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - DISP-05

must_haves:
  truths:
    - "A dedicated dispute integration suite exists and runs under `@moduletag :integration` against `stripe-mock`"
    - "The suite exercises `File.create/3 -> Dispute.update_evidence/4` using a real uploaded file ID returned by the SDK"
    - "The suite exercises `Dispute.submit_evidence/3` after staging evidence"
    - "The suite includes dispute lifecycle smoke coverage for retrieve/list/update/close so Phase 33 verification can cite integration evidence, not only unit tests"
    - "The tests explicitly document `stripe-mock` statelessness so assertions stay honest about what is and is not proven"
  artifacts:
    - path: "test/integration/dispute_integration_test.exs"
      provides: "End-to-end dispute integration coverage, including uploaded-file evidence flow"
      contains: "Dispute.update_evidence(client,"
  key_links:
    - from: "test/integration/dispute_integration_test.exs"
      to: "lib/lattice_stripe/file.ex"
      via: "uploaded evidence file created with `File.create/3`"
      pattern: "File\\.create\\(client"
    - from: "test/integration/dispute_integration_test.exs"
      to: "lib/lattice_stripe/dispute.ex"
      via: "staged and submitted dispute evidence through real public APIs"
      pattern: "Dispute\\.(update_evidence|submit_evidence|close|update|retrieve|list)"
---

<objective>
Add the missing integration proof for the dispute evidence workflow and enough dispute lifecycle smoke coverage to support a closed Phase 33 verification report.

Purpose: The milestone audit specifically calls out missing `File.create/3 -> Dispute.update_evidence/4` and `Dispute.submit_evidence/3` end-to-end coverage. This plan produces that runtime evidence without reopening the shipped File or Dispute implementations.
Output: One new integration test module that uses the public SDK surface against `stripe-mock`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-v1.3-MILESTONE-AUDIT.md
@.planning/phases/32-file-filelink/32-VERIFICATION.md
@.planning/phases/33-disputes/33-01-SUMMARY.md
@.planning/phases/33-disputes/33-02-SUMMARY.md
@test/integration/file_integration_test.exs
@test/integration/quote_integration_test.exs
@test/lattice_stripe/dispute_test.exs
@lib/lattice_stripe/file.ex
@lib/lattice_stripe/dispute.ex
@test/support/fixtures/dispute.ex

<interfaces>
From `lib/lattice_stripe/file.ex`:
```elixir
@spec create(Client.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
```

From `lib/lattice_stripe/dispute.ex`:
```elixir
@spec retrieve(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec list(Client.t(), map(), keyword()) :: {:ok, Response.t()} | {:error, Error.t()}
@spec update(Client.t(), String.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec update_evidence(Client.t(), String.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec submit_evidence(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec close(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
```

From `test/integration/file_integration_test.exs`:
```elixir
@moduletag :integration
setup_all ... start_supervised!({Finch, name: LatticeStripe.IntegrationFinch})
setup ... %{client: test_integration_client(), upload_client: %{base_client | files_base_url: "http://localhost:12111"}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dispute integration coverage for evidence staging and submission</name>
  <files>
    test/integration/dispute_integration_test.exs
  </files>
  <read_first>
    test/integration/file_integration_test.exs
    test/integration/quote_integration_test.exs
    test/lattice_stripe/dispute_test.exs
    lib/lattice_stripe/file.ex
    lib/lattice_stripe/dispute.ex
  </read_first>
  <behavior>
    - Follow the repo’s existing integration test conventions exactly: `use ExUnit.Case, async: false`, `@moduletag :integration`, `setup_all` TCP guard for `localhost:12111`, and `setup` returning the integration client.
    - Use one upload-capable client variant with `files_base_url: "http://localhost:12111"` for `File.create/3`.
    - Keep assertions honest: shape and routing are authoritative; lifecycle persistence is not, because `stripe-mock` is stateless.
  </behavior>
  <action>
Create `test/integration/dispute_integration_test.exs` as a new dedicated integration module.

Module shape:

- `defmodule LatticeStripe.DisputeIntegrationTest`
- `use ExUnit.Case, async: false`
- `import LatticeStripe.TestHelpers`
- `@moduletag :integration`
- `alias LatticeStripe.{Dispute, File}`
- `setup_all` TCP reachability check and `Finch` startup matching the existing integration suites
- `setup` returning both:
  - `client: test_integration_client()`
  - `upload_client: %{test_integration_client() | files_base_url: "http://localhost:12111"}`

Required scenarios:

1. `retrieve/3` returns `%Dispute{}` for a mock dispute ID
2. `list/3` returns `%LatticeStripe.Response{data: %LatticeStripe.List{}}`
3. `update/4` returns `%Dispute{}` when sending a metadata change
4. `close/3` returns `%Dispute{}` for a mock dispute ID
5. Evidence workflow test:
   - upload a file through `File.create/3` with purpose `dispute_evidence`
   - capture the returned file ID
   - call `Dispute.update_evidence/4` with an evidence map that includes the uploaded file ID in a file-bearing field such as `"uncategorized_file"` or another dispute evidence file slot actually exposed by the SDK
   - assert the response is `{:ok, %Dispute{}}`
   - then call `Dispute.submit_evidence/3` on the same dispute ID
   - assert the response is `{:ok, %Dispute{}}`

Implementation notes:

- Use a fixed mock dispute ID such as `"dp_mock"` unless local inspection shows `stripe-mock` requires a different shape
- Add comments that the test proves request/response wiring and typed decoding, not real dispute-state persistence
- Keep the upload and evidence-submission flow in a single test body so the returned file ID is visibly threaded into the dispute update call
- Do not modify `lib/` code in this plan
  </action>
  <verify>
    <automated>mix test test/integration/dispute_integration_test.exs --include integration</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/dispute_integration_test.exs` contains `defmodule LatticeStripe.DisputeIntegrationTest`
    - the file contains `@moduletag :integration`
    - the file contains `File.create(upload_client` or `File.create(client` with an upload-capable client
    - the file contains `Dispute.update_evidence(`
    - the file contains `Dispute.submit_evidence(`
    - the evidence-flow test threads an uploaded file ID into the dispute evidence params
    - the file contains `Dispute.close(`
    - `mix test test/integration/dispute_integration_test.exs --include integration` exits 0 with `stripe-mock` running
  </acceptance_criteria>
  <done>A dedicated dispute integration suite exists and proves the uploaded-file evidence workflow end-to-end against the public SDK surface.</done>
</task>

<task type="auto">
  <name>Task 2: Keep dispute integration coverage milestone-focused and non-brittle</name>
  <files>
    test/integration/dispute_integration_test.exs
  </files>
  <read_first>
    .planning/v1.3-v1.3-MILESTONE-AUDIT.md
    .planning/phases/32-file-filelink/32-VERIFICATION.md
    test/integration/file_integration_test.exs
  </read_first>
  <behavior>
    - The suite should prove the audit’s missing flow, not attempt full Stripe-state realism that `stripe-mock` cannot guarantee.
    - Comments and assertions must make the test’s confidence boundaries clear to future verifiers.
  </behavior>
  <action>
Review the new module and tighten it around the audit findings:

- retain only the lifecycle smoke tests needed to support verification evidence
- ensure the evidence test clearly names the Phase 38 gap it closes
- prefer shape assertions (`{:ok, %Dispute{}}`, `{:ok, %File{}}`) and specific field spot-checks over fragile assumptions about persisted state transitions
- if `stripe-mock` returns a permissive or non-semantic response for close/submit operations, document that in comments rather than widening the scope into SDK changes

The final file should read like milestone evidence, not a speculative integration DSL.
  </action>
  <verify>
    <automated>mix test test/integration/dispute_integration_test.exs --include integration</automated>
  </verify>
  <acceptance_criteria>
    - the evidence workflow test contains a comment tying it to the milestone audit gap
    - the module does not introduce helper abstractions unrelated to Phase 38
    - the integration assertions are shape-first and compatible with `stripe-mock` statelessness
  </acceptance_criteria>
  <done>The integration suite is narrow, durable, and directly usable as verification evidence for FILE/DISP closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Uploaded file ID -> dispute evidence params | A value returned by one Stripe API surface is threaded into another irreversible workflow |
| Integration suite -> milestone verifier | Tests can create false confidence if they over-claim what `stripe-mock` proves |
| Operator -> `submit_evidence/3` and `close/3` | Irreversible dispute actions are exercised in a test environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | T | Evidence param shape in `Dispute.update_evidence/4` | mitigate | Use the real public API with an actual uploaded file ID and keep assertions on typed `%Dispute{}` responses so malformed nested evidence params fail visibly. |
| T-38-02 | R | `submit_evidence/3` confidence | mitigate | Run submission after staging in the same test and document that the proof is request/response routing plus typed decode, not persistent backend state. |
| T-38-03 | S | `stripe-mock` statelessness | mitigate | Add comments that the suite is shape-based and avoid assertions that require real Stripe dispute state transitions. |
</threat_model>

<verification>
- `mix test test/integration/dispute_integration_test.exs --include integration`
- `mix test test/lattice_stripe/dispute_test.exs test/integration/file_integration_test.exs --include integration`
- confirm the new suite uses `File.create/3`, `Dispute.update_evidence/4`, and `Dispute.submit_evidence/3`
</verification>

<success_criteria>
- The repo contains a dedicated dispute integration suite under `test/integration/`.
- Uploaded file evidence is staged through the real public SDK surface.
- Dispute evidence submission is exercised end-to-end under `stripe-mock`.
- Phase 33 verification can cite integration evidence in addition to existing unit coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/38-dispute-evidence-e2e-verification/38-01-SUMMARY.md`
</output>
