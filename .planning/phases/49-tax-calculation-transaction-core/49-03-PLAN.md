---
phase: 49-tax-calculation-transaction-core
plan: 03
type: execute
wave: 2
depends_on: [49-01, 49-02]
files_modified:
  - test/lattice_stripe/tax/calculation_transaction_test.exs
autonomous: true
requirements: [DX-03]
must_haves:
  truths:
    - "Integration spec under `test/lattice_stripe/tax/` proves calc→txn→reversal via Mox-at-Transport (ROADMAP SC#7)."
  artifacts:
    - path: test/lattice_stripe/tax/calculation_transaction_test.exs
      provides: canonical chained workflow test
      contains: canonical standalone tax flow
  key_links:
    - from: test/lattice_stripe/tax/calculation_transaction_test.exs
      to: lib/lattice_stripe/tax/calculation.ex
      via: Calculation.create/3 in test
      pattern: Tax.Calculation.create
---

<objective>
Ship the load-bearing Mox-at-Transport integration spec `calculation_transaction_test.exs` proving the canonical standalone Tax flow: create calculation → create_from_calculation → retrieve transaction → create_reversal (CONTEXT D-02 Option B).

This is the DX-03 / ROADMAP SC#7 deliverable. No `@moduletag :integration`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-tax-calculation-transaction-core/49-CONTEXT.md
@.planning/phases/49-tax-calculation-transaction-core/49-RESEARCH.md
@test/lattice_stripe/webhook/thin_event_test.exs
@test/lattice_stripe/tax/calculation_test.exs
@test/lattice_stripe/tax/transaction_test.exs
@test/support/fixtures/tax_calculation.ex
@test/support/fixtures/tax_transaction.ex
</context>

<threat_model>
No additional threats — test-only plan using mocked transport.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Chained integration test — canonical standalone tax flow</name>
  <files>test/lattice_stripe/tax/calculation_transaction_test.exs</files>
  <read_first>
    - test/lattice_stripe/webhook/thin_event_test.exs (chained expect pattern)
    - .planning/phases/49-tax-calculation-transaction-core/49-CONTEXT.md (D-02 integration contract)
    - test/support/fixtures/tax_calculation.ex
    - test/support/fixtures/tax_transaction.ex
  </read_first>
  <action>
Create `test/lattice_stripe/tax/calculation_transaction_test.exs`:
- `use ExUnit.Case, async: true`
- `setup :verify_on_exit!`
- import Mox, TestHelpers, TaxCalculation + TaxTransaction fixtures
- **Do NOT** add `@moduletag :integration`

Single test `"canonical standalone tax flow"` with **four ordered** `expect(LatticeStripe.MockTransport, :request, fn req -> ... end)`:

1. **POST** — URL ends with `/v1/tax/calculations`. Return `tax_calculation_json()` but override `"id"` to a variable `calc_id = "taxcalc_#{System.unique_integer([:positive])}"`. Bind `calc_id` for step 2.

2. **POST** — URL ends with `/v1/tax/transactions/create_from_calculation`. Assert `req.body` contains `calc_id` and a dynamically generated `reference = "order_#{System.unique_integer([:positive])}"`. Return transaction JSON with `"id" => txn_id` and same `reference`.

3. **GET** — URL contains `/v1/tax/transactions/#{txn_id}`. Return transaction fixture.

4. **POST** — URL ends with `/v1/tax/transactions/create_reversal`. Assert body contains `mode=full`, `original_transaction=#{txn_id}`, and a **new** unique reversal reference (e.g. `"#{reference}-rev"`). Return reversal transaction JSON.

Test body calls in order:
```elixir
{:ok, calc} = Tax.Calculation.create(client, %{...})
{:ok, txn} = Tax.Transaction.create_from_calculation(client, %{"calculation" => calc.id, "reference" => reference})
{:ok, fetched} = Tax.Transaction.retrieve(client, txn.id)
{:ok, reversal} = Tax.Transaction.create_reversal(client, %{"mode" => "full", "original_transaction" => txn.id, "reference" => reversal_ref})
```

Assert `%Tax.Calculation{}`, `%Tax.Transaction{}` on successes. Minimal create params (currency + customer_details + one line_item) — match fixture shapes.

No duplicate per-verb coverage (unit files own that per D-02).
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax/calculation_transaction_test.exs --no-start 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/tax/calculation_transaction_test.exs --no-start` exits 0
    - `grep "canonical standalone tax flow" test/lattice_stripe/tax/calculation_transaction_test.exs` matches
    - `grep -c "expect(LatticeStripe.MockTransport" test/lattice_stripe/tax/calculation_transaction_test.exs` outputs 4
    - File does NOT contain `@moduletag :integration`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/lattice_stripe/tax/ --no-start`
- Full phase: all 3 plans' tests green
</verification>

<success_criteria>
- DX-03 satisfied: calc→txn chain proven at Transport boundary
- Reversal step included (CONTEXT D-02 Option B)
- Dynamic IDs/references only (PITFALLS #2, #3)
</success_criteria>
