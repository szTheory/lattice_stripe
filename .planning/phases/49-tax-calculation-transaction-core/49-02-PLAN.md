---
phase: 49-tax-calculation-transaction-core
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - lib/lattice_stripe/tax/transaction.ex
  - lib/lattice_stripe/tax/transaction/line_item.ex
  - lib/lattice_stripe/tax/calculation.ex
  - lib/lattice_stripe/object_types.ex
  - test/support/fixtures/tax_transaction.ex
  - test/lattice_stripe/tax/transaction_test.exs
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements: [TXN-01, TXN-02, TXN-03, TXN-04, DX-03]
must_haves:
  truths:
    - "Adopters record tax via `create_from_calculation/3` and reverse via `create_reversal/3` (ROADMAP SC#3–4)."
    - "Adopters inspect transactions via `retrieve/3` and `list_line_items/4` (ROADMAP SC#5)."
    - "Moduledocs document 90-day expiry, globally unique reference, and `Invoice.AutomaticTax` boundary (ROADMAP SC#6)."
    - "`tax.transaction` ObjectTypes entry registered (CONTEXT D-04)."
  artifacts:
    - path: lib/lattice_stripe/tax/transaction.ex
      provides: Tax.Transaction resource + verb functions
      contains: create_from_calculation
    - path: lib/lattice_stripe/tax/calculation.ex
      provides: full Calculation @moduledoc
      contains: Invoice.AutomaticTax
    - path: test/lattice_stripe/tax/transaction_test.exs
      provides: TXN unit coverage
      contains: reference_already_exists
---

<objective>
Ship `LatticeStripe.Tax.Transaction` with verb functions (`create_from_calculation/3`, `create_reversal/3`, `retrieve/3`, `list_line_items/4`), `Transaction.LineItem`, full moduledocs on **both** Calculation and Transaction (ROADMAP SC#6 + grepable DX-03 strings), `tax.transaction` ObjectTypes, fixtures, and unit tests TXN-01..04.

Depends on Plan 01 Calculation modules and shared nested structs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-tax-calculation-transaction-core/49-CONTEXT.md
@.planning/phases/49-tax-calculation-transaction-core/49-RESEARCH.md
@lib/lattice_stripe/tax/calculation.ex
@lib/lattice_stripe/billing/meter.ex
@lib/lattice_stripe/invoice/automatic_tax.ex
@test/lattice_stripe/credit_note_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-49-03 | Reference collision in tests causing flaky `reference_already_exists` | low | Use `System.unique_integer([:positive])` in every create_from_calculation/reversal test |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Transaction.LineItem + Tax.Transaction module</name>
  <files>lib/lattice_stripe/tax/transaction/line_item.ex, lib/lattice_stripe/tax/transaction.ex, lib/lattice_stripe/object_types.ex</files>
  <read_first>
    - lib/lattice_stripe/tax/calculation/line_item.ex
    - lib/lattice_stripe/tax/calculation.ex
    - lib/lattice_stripe/billing/meter.ex (verb POST paths)
    - .planning/phases/49-tax-calculation-transaction-core/49-RESEARCH.md
  </read_first>
  <action>
Create `LatticeStripe.Tax.Transaction.LineItem` — same as Calculation.LineItem plus `@known_fields` entries `reversal`, `type`; default object `"tax.transaction_line_item"`.

Create `LatticeStripe.Tax.Transaction` reusing shared nested decoders from Plan 01. Top-level fields per RESEARCH. `from_map/1` mirrors Calculation patterns.

Verbs:
- `create_from_calculation/3` → POST `/v1/tax/transactions/create_from_calculation` (params pass-through: `calculation`, `reference`, etc.)
- `create_reversal/3` → POST `/v1/tax/transactions/create_reversal`
- `retrieve/3` → GET `/v1/tax/transactions/:id`
- `list_line_items/4` → GET `/v1/tax/transactions/:id/line_items`

Bang variants for all.

Register `"tax.transaction" => LatticeStripe.Tax.Transaction` in object_types.ex.

Fixture `test/support/fixtures/tax_transaction.ex` with `tax_transaction_json/0` and `/1` — include `reference`, `type` => `"transaction"`, line_items with `tax.transaction_line_item`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep create_from_calculation lib/lattice_stripe/tax/transaction.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'create_from_calculation' lib/lattice_stripe/tax/transaction.ex` matches
    - `grep '"tax.transaction"' lib/lattice_stripe/object_types.ex` matches
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Moduledocs (Calculation + Transaction) per CONTEXT D-03</name>
  <files>lib/lattice_stripe/tax/calculation.ex, lib/lattice_stripe/tax/transaction.ex</files>
  <read_first>
    - .planning/phases/49-tax-calculation-transaction-core/49-CONTEXT.md (D-03 sections)
    - lib/lattice_stripe/billing/meter.ex (@moduledoc lifecycle)
    - lib/lattice_stripe/quote.ex (@moduledoc boundary)
  </read_first>
  <action>
Replace minimal Calculation `@moduledoc` with full sections from CONTEXT D-03:
1. Purpose (standalone `/v1/tax/calculations`)
2. Lifecycle (create → retrieve/list_line_items → Transaction handoff; **90-day** expiry via `expires_at`)
3. Relationship paragraph naming `LatticeStripe.Invoice.AutomaticTax` — NOT this API; Checkout/Invoice/Subscription flows use automatic tax instead; filing out of SDK scope
4. Usage example with `customer_details`, `line_items`, `currency`
5. Stripe doc link
6. Placeholder: "A canonical tax guide ships in v1.6 Phase 51."

Transaction `@moduledoc`:
1. Purpose
2. Lifecycle (create_from_calculation + unique reference → retrieve/list_line_items; create_reversal)
3. Operational constraints: **globally unique** `reference` across all tax transactions; example `"order_\#{order_id}"`; calculations expire ~90 days
4. Same AutomaticTax relationship paragraph (trimmed)
5. Examples for create_from_calculation and create_reversal (`mode` => `"full"`)
6. Stripe doc link

**Grep-lock strings (required verbatim somewhere in moduledoc):**
- Calculation: substring `90` co-located with day/expiry language; `Invoice.AutomaticTax`
- Transaction: `reference` near `unique` or `globally`; `Invoice.AutomaticTax`
  </action>
  <verify>
    <automated>grep -E "90|Invoice.AutomaticTax" lib/lattice_stripe/tax/calculation.ex && grep -E "reference|globally|unique" lib/lattice_stripe/tax/transaction.ex && grep Invoice.AutomaticTax lib/lattice_stripe/tax/transaction.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Invoice.AutomaticTax" lib/lattice_stripe/tax/calculation.ex` matches
    - `grep -E "90|ninety" lib/lattice_stripe/tax/calculation.ex` matches (case insensitive via grep -i acceptable)
    - `grep -i "globally" lib/lattice_stripe/tax/transaction.ex` matches OR `grep "unique" lib/lattice_stripe/tax/transaction.ex` matches alongside `reference`
    - `grep "Invoice.AutomaticTax" lib/lattice_stripe/tax/transaction.ex` matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Transaction unit tests + ObjectTypes dispatch test</name>
  <files>test/lattice_stripe/tax/transaction_test.exs, test/lattice_stripe/object_types_test.exs</files>
  <read_first>
    - test/lattice_stripe/tax/calculation_test.exs
    - test/support/fixtures/tax_transaction.ex
  </read_first>
  <action>
Create `transaction_test.exs` with describes:
- `create_from_calculation/3` — POST path ends with `create_from_calculation`, body includes `calculation=` and `reference=`
- `create_reversal/3` — POST `create_reversal`, body includes `mode=full` and `original_transaction=`
- `retrieve/3`, `list_line_items/4`
- Error: 400 `reference_already_exists` on create_from_calculation
- Use `"order_#{System.unique_integer([:positive])}"` for references in every test

Add ObjectTypes test for `tax.transaction` dispatch.

Optional: moduledoc grep test in same file asserting key strings (lightweight DX-03 partial lock before Phase 51 DX-05).
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax/transaction_test.exs test/lattice_stripe/object_types_test.exs --no-start 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/tax/transaction_test.exs --no-start` exits 0
    - File contains `reference_already_exists`
    - File contains `create_reversal`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/lattice_stripe/tax/ --no-start`
- Moduledoc grep strings present (see Task 2 acceptance)
</verification>

<success_criteria>
- TXN-01..04 covered
- Moduledoc operational traps documented (SC#6)
- Both ObjectTypes entries live
</success_criteria>
