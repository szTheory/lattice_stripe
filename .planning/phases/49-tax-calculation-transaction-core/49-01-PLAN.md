---
phase: 49-tax-calculation-transaction-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/tax/customer_details.ex
  - lib/lattice_stripe/tax/shipping_cost.ex
  - lib/lattice_stripe/tax/ship_from_details.ex
  - lib/lattice_stripe/tax/tax_breakdown.ex
  - lib/lattice_stripe/tax/calculation.ex
  - lib/lattice_stripe/tax/calculation/line_item.ex
  - lib/lattice_stripe/object_types.ex
  - test/support/fixtures/tax_calculation.ex
  - test/lattice_stripe/tax/calculation_test.exs
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03]
must_haves:
  truths:
    - "Adopters call `Tax.Calculation.create/3` with string-key params and receive `{:ok, %Tax.Calculation{}}` with typed nested line items when expanded (ROADMAP SC#1)."
    - "Adopters call `retrieve/3` and `list_line_items/4` against calculation IDs (ROADMAP SC#2)."
    - "`tax.calculation` dispatches through ObjectTypes (CONTEXT D-04)."
  artifacts:
    - path: lib/lattice_stripe/tax/calculation.ex
      provides: Tax.Calculation resource module
      contains: def create(
    - path: lib/lattice_stripe/tax/calculation/line_item.ex
      provides: tax.calculation_line_item decoder
      contains: tax.calculation_line_item
    - path: test/lattice_stripe/tax/calculation_test.exs
      provides: per-verb Mox unit tests
      contains: describe "create/3"
---

<objective>
Ship `LatticeStripe.Tax.Calculation` and shared nested structs (`CustomerDetails`, `ShippingCost`, `ShipFromDetails`, `TaxBreakdown`), paginated `list_line_items/4`, `tax.calculation` ObjectTypes registration, canonical fixtures, and unit tests for CALC-01..03.

Moduledocs for Calculation ship in Plan 02 alongside Transaction (both modules must exist before final doc cross-links). Plan 01 adds a minimal `@moduledoc` one-liner only.

Output: `lib/lattice_stripe/tax/*` calculation tree, fixture builder, `calculation_test.exs`, ObjectTypes + test case.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-tax-calculation-transaction-core/49-CONTEXT.md
@.planning/phases/49-tax-calculation-transaction-core/49-RESEARCH.md
@lib/lattice_stripe/credit_note.ex
@lib/lattice_stripe/credit_note/line_item.ex
@lib/lattice_stripe/quote.ex
@lib/lattice_stripe/object_types.ex
@test/lattice_stripe/credit_note_test.exs
@test/support/fixtures/credit_note.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-49-01 | Logging raw API keys in test fixtures | low | Fixtures use `sk_test_` placeholders only via TestHelpers |
| T-49-02 | PII in committed fixture maps | low | Use synthetic addresses (Seattle/CA pattern from Stripe samples) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Shared nested structs + Calculation.LineItem</name>
  <files>lib/lattice_stripe/tax/customer_details.ex, lib/lattice_stripe/tax/shipping_cost.ex, lib/lattice_stripe/tax/ship_from_details.ex, lib/lattice_stripe/tax/tax_breakdown.ex, lib/lattice_stripe/tax/calculation/line_item.ex</files>
  <read_first>
    - lib/lattice_stripe/credit_note/line_item.ex
    - lib/lattice_stripe/invoice/automatic_tax.ex
    - .planning/phases/49-tax-calculation-transaction-core/49-CONTEXT.md (D-01 nested typing)
    - .planning/phases/49-tax-calculation-transaction-core/49-RESEARCH.md (nested param shapes)
  </read_first>
  <action>
Create `LatticeStripe.Tax.CustomerDetails` with `@known_fields ~w[address address_source ip_address tax_ids taxability_override]`, `address` and `tax_ids` stored as map/list (not sub-structs). Pass through `taxability_override` strings; optionally atomize `none`, `customer_exempt`, `reverse_charge`.

Create `LatticeStripe.Tax.ShippingCost` with `@known_fields ~w[amount amount_tax shipping_rate tax_behavior tax_code tax_breakdown]`, `tax_breakdown` as `list() | nil` (maps), atomize `tax_behavior` `:exclusive | :inclusive`.

Create `LatticeStripe.Tax.ShipFromDetails` with `@known_fields ~w[address]`, address as map.

Create `LatticeStripe.Tax.TaxBreakdown` with `@known_fields ~w[amount inclusive tax_rate_details taxability_reason taxable_amount]`, `tax_rate_details` as map.

Create `LatticeStripe.Tax.Calculation.LineItem` at `lib/lattice_stripe/tax/calculation/line_item.ex`:
- `@known_fields ~w[id object amount amount_tax livemode metadata performance_location product quantity reference tax_behavior tax_breakdown tax_code]`
- `object` default `"tax.calculation_line_item"`
- `product`: if map, `ObjectTypes.maybe_deserialize/1`; if string, pass through
- `tax_behavior`: atomize exclusive/inclusive
- `tax_breakdown`: `list() | nil` (do NOT sub-struct jurisdiction trees)
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "defmodule LatticeStripe.Tax.Calculation.LineItem" lib/lattice_stripe/tax/calculation/line_item.ex` succeeds
    - `grep "tax.calculation_line_item" lib/lattice_stripe/tax/calculation/line_item.ex` returns a match
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tax.Calculation verbs + from_map + ObjectTypes</name>
  <files>lib/lattice_stripe/tax/calculation.ex, lib/lattice_stripe/object_types.ex, test/support/fixtures/tax_calculation.ex, test/lattice_stripe/object_types_test.exs</files>
  <read_first>
    - lib/lattice_stripe/quote.ex (parse_line_items, list_line_items, from_map)
    - lib/lattice_stripe/credit_note.ex (create/retrieve)
    - lib/lattice_stripe/object_types.ex
    - test/support/fixtures/credit_note.ex
  </read_first>
  <action>
Create `LatticeStripe.Tax.Calculation` with top-level `@known_fields` from RESEARCH.md. Implement `from_map/1` decoding:
- nested structs via respective `from_map/1`
- `line_items` via `parse_line_items` (Quote pattern: `%List{data: [%LineItem{}]}`)
- `tax_breakdown` as list of `%Tax.TaxBreakdown{}`
- `extra` for unknown keys

Verbs (all with bang variants):
- `create/3` → POST `/v1/tax/calculations`
- `retrieve/3` → GET `/v1/tax/calculations/:id`
- `list_line_items/4` → GET `/v1/tax/calculations/:id/line_items` returning `{:ok, %Response{}}` with LineItem list via `Resource.unwrap_list(&LineItem.from_map/1)`

Add to `object_types.ex` dispatch map: `"tax.calculation" => LatticeStripe.Tax.Calculation`.

Create `LatticeStripe.Test.Fixtures.TaxCalculation` with `tax_calculation_json/0` and `/1` merging overrides — include `id` like `taxcalc_test123`, `object` => `"tax.calculation"`, nested `customer_details`, `line_items` list with one `tax.calculation_line_item`, `expires_at` integer, amounts in cents.

Add ObjectTypes test: `maybe_deserialize` on `%{"object" => "tax.calculation", "id" => "taxcalc_x"}` returns `%Tax.Calculation{}`.

Minimal `@moduledoc`: one line pointing to Phase 51 guide; full moduledoc deferred to Plan 02.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"tax.calculation"' lib/lattice_stripe/object_types.ex` matches
    - `grep 'POST /v1/tax/calculations' lib/lattice_stripe/tax/calculation.ex` or `path: "/v1/tax/calculations"` in calculation.ex
    - `mix test test/lattice_stripe/object_types_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Calculation unit tests (CALC-01..03)</name>
  <files>test/lattice_stripe/tax/calculation_test.exs</files>
  <read_first>
    - test/lattice_stripe/credit_note_test.exs
    - test/support/fixtures/tax_calculation.ex
    - lib/lattice_stripe/tax/calculation.ex
  </read_first>
  <action>
Create `test/lattice_stripe/tax/calculation_test.exs` with `async: true`, `setup :verify_on_exit!`, import Mox + TestHelpers + TaxCalculation fixture.

Describe blocks:
- `create/3` — POST `/v1/tax/calculations`, assert body contains `currency=usd` (or nested key from sample params), returns `%Tax.Calculation{}`
- `retrieve/3` — GET `/v1/tax/calculations/:id`
- `list_line_items/4` — GET `.../line_items`, returns list of `%Tax.Calculation.LineItem{}`
- `create!/3` bang success path
- Error path: mock 400 with `code: "resource_expired"` for retrieve (PITFALLS #2)

Use dynamic IDs from fixture, not hardcoded production IDs.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax/calculation_test.exs --no-start 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/tax/calculation_test.exs --no-start` exits 0
    - File contains `describe "list_line_items/4"`
    - File contains `resource_expired`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors`
- `mix test test/lattice_stripe/tax/calculation_test.exs test/lattice_stripe/object_types_test.exs --no-start`
</verification>

<success_criteria>
- All CALC-01..03 requirements covered by calculation_test.exs
- `tax.calculation` registered and tested
- No Transaction modules yet (Plan 02)
</success_criteria>
