---
phase: 62-1-1-1-7-what-landed-migration-guide
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md
  - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md
  - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [DOC-01]

must_haves:
  truths:
    - "D-15: the immutable executor implementation range `cc87e3a0963f0e9bf7341bb009d80f12cae7f3d3..9d111dfb4c251d544570aebcce951094ba4161f4` remains an exact four-path audit; no later lifecycle artifact is retroactively attributed to that range."
    - "D-15: a second immutable endpoint records the completed gap-closure boundary, and a reproducible category-based audit proves that Phase 62 changed only the migration guide, its docs-truth test, and named Phase 62 planning/workflow records."
    - "D-15: the category audit rejects every unlisted path and explicitly forbids `lib/**`, `mix.exs`, `mix.lock`, schema/migration paths, and `priv/api/current.txt`; no production module, dependency surface, schema, or public API lock changed."
    - "Before any gap edit, the executor records full `git status --porcelain=v1 --untracked-files=all` output plus exact status/content fingerprints for `.planning/v1.10-MILESTONE-AUDIT.md` and every `.planning/research/.cache/` file; final full-status delta and fingerprint checks prove all pre-existing dirty state is unchanged and every new path belongs to the closed allowlist."
    - "DOC-01 retains fresh mechanical evidence: `mix test test/lattice_stripe/docs_truth_test.exs`, `mix docs --warnings-as-errors`, and `mix ci` all pass, with no human-only verification category under the lattice verification policy."
  artifacts:
    - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md
    - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md
    - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md
  key_links:
    - "`62-VALIDATION.md` immutable executor endpoint → exact four-path implementation audit"
    - "gap-closure summary commit SHA → category-based whole-phase committed-path audit → later evidence commit"
    - "guide and docs-truth test → focused ExUnit contract → warnings-as-errors docs build → full `mix ci`"
    - "protected audit baseline status/blob → final status/blob equality check"
---

<objective>
Close the Phase 62 verification-boundary contradiction without weakening D-15 or rewriting Git history.

Purpose: distinguish the executor's exact four-path implementation range from later required review, verification, and gap-closure records, then prove the whole phase stayed inside a stricter product/workflow category boundary.
Output: a two-boundary validation record, a narrowly clarified original summary, a gap-closure summary with executable coverage evidence, and an immutable endpoint ready for re-verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-CONTEXT.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-RESEARCH.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VERIFICATION.md
@.agents/skills/lattice-verification-policy/SKILL.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reconcile the two verification boundaries and refresh DOC-01 evidence</name>
  <files>.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md, .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VERIFICATION.md` — the sole actionable gap is the later review artifacts outside the original literal allowlist.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md` — preserve its observed executor evidence and exact baseline fingerprints.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md` — preserve the original task history; clarify only the scope of its D-15 statement.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-CONTEXT.md` — D-15 is a zero-production-code guarantee, not permission to erase required workflow records.
    - `.agents/skills/lattice-verification-policy/SKILL.md` — every non-human coverage claim needs a named, observed executable check.
  </read_first>
  <action>Before any gap-task edit, capture the exact full output of `git status --porcelain=v1 --untracked-files=all` and retain it in memory while preparing the first scoped patch. Also capture: `git status --porcelain=v1 --untracked-files=all -- .planning/v1.10-MILESTONE-AUDIT.md`; `git hash-object .planning/v1.10-MILESTONE-AUDIT.md`; full per-file cache status; and a deterministic cache-content fingerprint produced from each sorted cache-relative path plus that file's `git hash-object` blob. In the first 62-VALIDATION.md edit, record the full baseline verbatim between stable `phase62-full-status-baseline:start/end` markers and record the audit/cache status plus content fingerprints as literal values. This capture must precede edits so the later comparison has a trustworthy dirty-worktree baseline, including untracked files that normal `git diff` omits. Then, per D-15, revise 62-VALIDATION.md so its existing exact allowlist is explicitly and immutably scoped to `cc87e3a0963f0e9bf7341bb009d80f12cae7f3d3..9d111dfb4c251d544570aebcce951094ba4161f4`. Preserve the original observed paths and do not change either SHA, falsify the old audit, or imply that later review records existed inside that executor range. Add a separate lifecycle/corrective model whose future closure endpoint will be recorded only after 62-02-SUMMARY.md is committed: permitted product deliverables are exactly `guides/upgrading-1-1-to-1-7.md` and `test/lattice_stripe/docs_truth_test.exs`; permitted workflow records are Phase 62 `NN-PLAN.md`, `NN-SUMMARY.md`, `VALIDATION.md`, `REVIEW.md`, `REVIEW-FIX.md`, and `VERIFICATION.md` files in this phase directory. State that all other paths fail closed, including new untracked paths and `lib/**`, `mix.exs`, `mix.lock`, schema/migration paths, and `priv/api/current.txt`. Add a short correction note to 62-01-SUMMARY.md only because its D-15 wording currently reads as a final whole-phase claim: preserve its task commits, accomplishments, deviations, and original test results; say its exact audit proves the executor boundary and point to the later lifecycle audit for final scope. Then run the focused docs-truth suite, warning-strict docs build, and full CI after the edits, and record the fresh observed counts/results in 62-VALIDATION.md and the clarification note. Do not edit the guide, test, review reports, verification report, production code, dependency files, schemas, API lock, `.planning/v1.10-MILESTONE-AUDIT.md`, or `.planning/research/.cache/`.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs &amp;&amp; mix docs --warnings-as-errors &amp;&amp; mix ci</automated>
  </verify>
  <done>The historical executor audit remains literally true, the pre-edit full porcelain baseline and both protected content fingerprints are recorded, the final-phase claim no longer conflates implementation and lifecycle commits, and fresh named checks prove DOC-01 remains green without changing product implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Create the gap-closure record and seal its immutable endpoint</name>
  <files>.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md` — use the two-boundary vocabulary and the fresh command evidence from Task 1.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md` — link the clarification without duplicating or revising original execution history.
    - `.agents/skills/lattice-verification-policy/SKILL.md` — author the summary coverage block only from passing executable checks.
  </read_first>
  <action>Create 62-02-SUMMARY.md as the gap-closure workflow record. Explain that this plan changes verification metadata only: it preserves the executor boundary, introduces the lifecycle/corrective category audit, and makes no guide, test, runtime, dependency, schema, or public API change. Include a `coverage:` frontmatter entry for D-15/DOC-01, but because Task 3 has not yet run the final scope audit, mark the scope verification `status: pending` and keep `human_judgment: true`; do not cite the future validation section as passing evidence. Record Task 1's already-observed focused/docs/CI results separately without treating them as proof of final scope. Before committing this task, recompute and require exact equality for the milestone-audit status/blob and cache status/content fingerprint recorded in Task 1, compare the current full porcelain output with the recorded baseline, and require every delta line to resolve to one of the three plan-declared metadata files. Commit the pending summary as the gap-closure content commit; its resulting HEAD is the immutable closure endpoint that Task 3 records. Do not amend or rebase this commit afterward.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; validation=.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md; rg -q "status: pending" .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md; rg -q "human_judgment: true" .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md; test "$(git status --porcelain=v1 --untracked-files=all -- .planning/v1.10-MILESTONE-AUDIT.md)" = "$(sed -n "s/^<!-- phase62-audit-status: \(.*\) -->$/\1/p" "$validation")"; test "$(git hash-object .planning/v1.10-MILESTONE-AUDIT.md)" = "$(sed -n "s/^<!-- phase62-audit-blob: \(.*\) -->$/\1/p" "$validation")"; test "$(find .planning/research/.cache -type f -print0 | sort -z | while IFS= read -r -d "" cache_file; do printf "%s\0%s\n" "${cache_file#.planning/research/.cache/}" "$(git hash-object "$cache_file")"; done | shasum -a 256 | awk "{print \$1}")" = "$(sed -n "s/^<!-- phase62-cache-fingerprint: \(.*\) -->$/\1/p" "$validation")"'</automated>
  </verify>
  <done>62-02-SUMMARY.md truthfully records the closure with D-15 scope coverage still pending and human judgment retained, protected/unrelated fingerprints still match, and the committed HEAD provides a stable endpoint for the final audit.</done>
</task>

<task type="auto">
  <name>Task 3: Record the reproducible whole-phase scope audit and route re-verification</name>
  <files>.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md, .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md` — append evidence; do not replace the executor-range record.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md` — update coverage with the now-observed final audit only.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VERIFICATION.md` — route the single failed truth back to re-verification; do not rewrite the verifier's historical report.
  </read_first>
  <action>Immediately after Task 2's content commit, capture `git rev-parse HEAD` as the immutable gap-closure endpoint and record that literal SHA in 62-VALIDATION.md. Audit both `cc87e3a0963f0e9bf7341bb009d80f12cae7f3d3..&lt;closure-sha&gt;` and `9d111dfb4c251d544570aebcce951094ba4161f4..&lt;closure-sha&gt;` with `git diff --name-status`; retain status letters so additions and modifications are reviewable. Fail if any committed, staged, or unstaged path is outside the two exact product paths or the phase-local workflow categories `62-[0-9][0-9]-PLAN.md`, `62-[0-9][0-9]-SUMMARY.md`, `62-VALIDATION.md`, `62-REVIEW.md`, `62-REVIEW-FIX.md`, and `62-VERIFICATION.md`. In addition, read the verbatim pre-edit full porcelain baseline from its validation markers and compare it with fresh `git status --porcelain=v1 --untracked-files=all`: every added or removed status line must resolve to an allowed implementation/workflow path, so new untracked files are audited rather than invisible. Explicitly fail on any new untracked or tracked `lib/**`, `mix.exs`, `mix.lock`, dependency manifest/lock, schema/migration, or `priv/api/current.txt` path. Recompute and require byte-for-byte cache status equality, deterministic cache-content fingerprint equality, milestone-audit status equality, and milestone-audit blob equality against Task 1's recorded baseline; a mere no-staged-path check is insufficient. Record the exact committed, staged, unstaged, full-porcelain delta, and protected-fingerprint evidence. Only after every named check passes, change the 62-02-SUMMARY.md D-15 scope verification from pending to pass and set `human_judgment: false`, citing the actual final scope-audit section plus the already-observed focused docs-truth, warning-strict docs, and full CI results. Commit the two evidence files without amending Task 2, and route Phase 62 to the standard re-verifier. The audit evidence commit is intentionally after the recorded closure endpoint because a commit cannot contain its own SHA; re-verification must independently apply the same category, full-status-delta, protected-fingerprint, and forbidden-path rules through its then-current HEAD.</action>
  <verify>
    <automated>set -euo pipefail; validation=.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md; scope_base=cc87e3a0963f0e9bf7341bb009d80f12cae7f3d3; scope_endpoint=$(git rev-parse HEAD); allowed_path() { case "$1" in guides/upgrading-1-1-to-1-7.md|test/lattice_stripe/docs_truth_test.exs|.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-[0-9][0-9]-PLAN.md|.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-[0-9][0-9]-SUMMARY.md|.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md|.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-REVIEW.md|.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-REVIEW-FIX.md|.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VERIFICATION.md) return 0 ;; *) return 1 ;; esac; }; scope_paths=$( { git diff --name-only "$scope_base..$scope_endpoint"; git diff --cached --name-only; git diff --name-only; } | sed "/^$/d" | sort -u); while IFS= read -r scope_path; do allowed_path "$scope_path" || { echo "forbidden Phase 62 path: $scope_path" &gt;&amp;2; exit 1; }; done &lt;&lt;&lt; "$scope_paths"; baseline_status=$(awk "/phase62-full-status-baseline:start/{inside=1;next}/phase62-full-status-baseline:end/{inside=0}inside" "$validation"); final_status=$(git status --porcelain=v1 --untracked-files=all); status_delta=$(comm -3 &lt;(printf "%s\n" "$baseline_status" | sed "/^$/d" | sort) &lt;(printf "%s\n" "$final_status" | sed "/^$/d" | sort)); while IFS= read -r delta_line; do delta_line=${delta_line#$'\t'}; delta_path=${delta_line:3}; allowed_path "$delta_path" || { echo "forbidden full-status delta: $delta_line" &gt;&amp;2; exit 1; }; done &lt;&lt;&lt; "$status_delta"; ! printf "%s\n%s\n" "$scope_paths" "$status_delta" | rg -q "(^|[?MADRCU! ]{2} )lib/|mix\\.exs|mix\\.lock|priv/api/current\\.txt|(^|/)(deps|dependency|dependencies|schema|schemas|migration|migrations|lock)(/|\\.|$)"; test "$(git status --porcelain=v1 --untracked-files=all -- .planning/v1.10-MILESTONE-AUDIT.md)" = "$(sed -n "s/^<!-- phase62-audit-status: \(.*\) -->$/\1/p" "$validation")"; test "$(git hash-object .planning/v1.10-MILESTONE-AUDIT.md)" = "$(sed -n "s/^<!-- phase62-audit-blob: \(.*\) -->$/\1/p" "$validation")"; cache_status=$(git status --porcelain=v1 --untracked-files=all -- .planning/research/.cache/); test "$cache_status" = "$(awk "/phase62-cache-status:start/{inside=1;next}/phase62-cache-status:end/{inside=0}inside" "$validation")"; cache_fingerprint=$(find .planning/research/.cache -type f -print0 | sort -z | while IFS= read -r -d "" cache_file; do printf "%s\0%s\n" "${cache_file#.planning/research/.cache/}" "$(git hash-object "$cache_file")"; done | shasum -a 256 | awk "{print \$1}"); test "$cache_fingerprint" = "$(sed -n "s/^<!-- phase62-cache-fingerprint: \(.*\) -->$/\1/p" "$validation")"; rg -q "status: pass" .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md; rg -q "human_judgment: false" .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md</automated>
  </verify>
  <done>The validation record names immutable executor and closure endpoints, the category audit and explicit prohibitions pass across committed/staged/unstaged surfaces, summary coverage cites observed evidence, and Phase 62 is ready for re-verification without any history rewrite or production change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history → validation prose | Recorded claims must match immutable commit ranges rather than a mutable interpretation of HEAD. |
| Product deliverables → workflow records | Required review/verification records may coexist with docs work but must never authorize production changes. |
| Pre-existing untracked files → Phase 62 commits | The milestone audit and research cache belong outside this plan and must remain unstaged and untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-04 | Tampering / Repudiation | `62-VALIDATION.md` boundary claims | high | mitigate | Preserve literal executor SHAs and observed four paths; record a distinct immutable closure SHA rather than rewriting the first audit. |
| T-62-05 | Elevation of privilege | Whole-phase path allowlist | high | mitigate | Fail closed on category membership and explicitly reject production, dependency, schema/migration, and API-lock paths. |
| T-62-06 | Tampering | Protected/untracked planning files | medium | mitigate | Capture full pre-edit porcelain state, compare its final symmetric delta against the allowlist, and require exact milestone-audit plus per-file cache status/content fingerprints. |
| T-62-07 | Repudiation | Summary coverage | medium | mitigate | Cite named passing commands and the locatable final scope-audit section per the lattice verification policy. |
</threat_model>

<verification>
- `mix test test/lattice_stripe/docs_truth_test.exs` passes after metadata reconciliation.
- `mix docs --warnings-as-errors` passes after metadata reconciliation.
- `mix ci` passes after metadata reconciliation.
- The immutable executor range remains exactly the original four allowed paths.
- The immutable whole-phase closure range contains only the two product deliverables and named Phase 62 workflow categories.
- Committed, staged, and unstaged path checks reject all unlisted paths and explicitly show no production/dependency/schema/API-lock changes.
- Full final porcelain status differs from the pre-edit baseline only at allowed Phase 62 paths; no untracked path bypasses the audit.
- Protected milestone-audit status/blob and research-cache per-file status/content fingerprints match the original baseline exactly.
</verification>

<success_criteria>
- The sole `62-VERIFICATION.md` gap has a deterministic, history-preserving correction and re-verification route.
- D-15 is stronger after closure: exact executor provenance plus a fail-closed whole-phase category audit.
- DOC-01 remains green under focused semantic, warning-strict documentation, and full CI checks.
- No implementation artifact outside the guide and docs-truth test enters the Phase 62 range; this gap plan itself changes only validation and summary records.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | 1.1 adopters can discover shipped surfaces with examples | 62-01 | COVERED | Already verified 6/7; this plan preserves the green adopter-facing implementation while closing only its scope-proof gap. |
| REQ | DOC-01 | Published, complete 1.1 → 1.7 migration guide | 62-01, 62-02 | COVERED | Plan 01 supplies the guide/test; Plan 02 supplies honest final provenance and reruns all named gates. |
| RESEARCH | — | Semantic docs contract plus warning-strict docs and full CI | 62-01, 62-02 | COVERED | Task 1 reruns the established three-layer validation architecture. |
| RESEARCH | — | No new dependencies, production code, snapshots, network checks, tag CI, or duplicate manifest | 62-02 | COVERED | File ownership, category audit, and explicit prohibitions preserve these constraints. |
| CONTEXT | D-01–D-14 | Historical scope, action-first inventory, ExDoc UX, and maintainable semantic protection | 62-01 | COVERED | Verification already passes these truths; gap closure does not edit their implementation. |
| CONTEXT | D-15 | Zero-library-code boundary | 62-02 | COVERED | Tasks 1–3 preserve the executor audit and add a reproducible whole-phase category audit. |

<output>
Create `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-02-SUMMARY.md`, then route Phase 62 to re-verification.
</output>
