---
phase: 62-1-1-1-7-what-landed-migration-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/upgrading-1-1-to-1-7.md
  - test/lattice_stripe/docs_truth_test.exs
  - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: the published guide identifies itself as the historical 1.1 → 1.7 leg, preserves {:lattice_stripe, \"~> 1.7\"}, gives only a thin 2.0/current-doc handoff, and never attributes the later default Finch pool to 1.7; `mix test test/lattice_stripe/docs_truth_test.exs` proves these anchors."
    - "D-05/D-06: a maintainer reaches a two-minute mandatory checklist before optional capabilities, sees all three `Affected if` predicates with before/after Elixir, gets an explicit safe exit when none applies, and is told to run application tests before deployment; the named docs-truth contract proves the durable anchors."
    - "D-07/D-08/D-09: the optional inventory is routed by adopter need and familiar ExDoc family, and covers BillingPortal.Configuration, Charge list/search, TestHelpers.TestClock, Testing.TestClock/Fixtures, Dispute, File/FileLink, Mandate, SetupAttempt, Tax.*, CreditNote, Payout, Quote, BalanceTransaction, and EventNotification with version-accurate minimum calls and canonical next steps."
    - "D-10/D-11: reader-facing prose stays at the consumer contract level and uses the existing ExDoc design system—semantic headings, labeled callouts, prose before concise Elixir fences, descriptive `.md` links, and the existing Upgrading sidebar group; `mix docs --warnings-as-errors` proves the render/reference contract."
    - "D-12/D-13/D-14: one compact behavior-oriented test named `1.1-to-1.7 guide remains historically scoped and discoverable` protects historical scope, discoverability, mandatory behavior checks, complete inventory/routes, safety anchors, and absence of the known post-1.7 contaminant without snapshots, network checks, tag-dependent CI, or a second manifest."
    - "D-15: Phase 62 changes no production module or public API; `git diff --name-only` for the execution range contains only the guide, docs-truth test, and phase validation record."
    - "DOC-01 is mechanically proven by the focused docs-truth test, `mix docs --warnings-as-errors`, and `mix ci`; per the lattice verification policy, no human-only UI, user-flow, performance-feel, or live-Stripe judgment remains."
  artifacts:
    - guides/upgrading-1-1-to-1-7.md
    - test/lattice_stripe/docs_truth_test.exs
    - .planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md
  key_links:
    - "mix.exs docs `extras` and `groups_for_extras[\"Upgrading\"]` → guides/upgrading-1-1-to-1-7.md → published HexDocs discovery"
    - "CHANGELOG.md release sections and tag v1.7.13 → historical claims and version-accurate examples in the guide"
    - "guides/upgrading-1-1-to-1-7.md semantic anchors → `LatticeStripe.DocsTruthTest` → the required Docs Truth CI lane"
    - "inventory rows → canonical customer-portal, Tax, thin-event, testing, credit-note, and quote guides rather than duplicated workflows"
  prohibitions:
    - statement: "MUST NOT mix post-1.7 current-client setup into the historical dependency leg."
      status: flagged
      verification: unverified
    - statement: "MUST NOT replace the action-first migration experience with release chronology or a duplicate release-capability manifest."
      status: flagged
      verification: unverified
    - statement: "MUST NOT modify production library modules or public API behavior in this documentation phase."
      status: flagged
      verification: unverified
---

<objective>
Correct and complete the already-published 1.1 → 1.7 migration guide, then lock its high-value adopter contract in the existing documentation test suite.

Purpose: adopters pinned to 1.1 need a historically correct, action-first answer to “must I change code?” followed by a complete catalogue of optional 1.3–1.7 capabilities. The current guide has strong migration examples but contains one harmful post-1.7 Finch claim and omits several shipped surfaces from its main catalogue.

Output: a corrected ExDoc guide, one focused semantic regression contract, and a green phase validation record for DOC-01 and D-01 through D-15.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-CONTEXT.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-RESEARCH.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-PATTERNS.md
@.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md
@.planning/quick/260729-h48-ship-upgrading-guide/260729-h48-SUMMARY.md
</context>

## Artifacts this phase produces

### New symbols

- ExUnit test `"1.1-to-1.7 guide remains historically scoped and discoverable"` in `LatticeStripe.DocsTruthTest`.

### Modified artifacts

- `guides/upgrading-1-1-to-1-7.md` — corrected historical boundary, action-first checklist, complete job-routed inventory, canonical routes, and appendix.
- `test/lattice_stripe/docs_truth_test.exs` — one compact semantic contract over the guide and its ExDoc registration.
- `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md` — observed task, wave, and phase-gate evidence.

### Explicitly unchanged

- `mix.exs` — the guide already appears in `extras` and the `Upgrading` group; the test verifies that wiring.
- `lib/**` — no production module, schema, dependency, public symbol, or runtime behavior changes.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Correct the historical dependency path and lock it end to end</name>
  <reversibility rating="costly">The guide is already published adopter-facing HexDocs, so a false historical setup claim can propagate into real integrations even though the local edit is cheap.</reversibility>
  <files>guides/upgrading-1-1-to-1-7.md, test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-CONTEXT.md` — D-01 through D-04 and D-12 through D-15 are locked, not discretionary.
    - `guides/upgrading-1-1-to-1-7.md` — preserve the existing scope callout, historical pin, and three behavior examples while correcting the dependency path.
    - `test/lattice_stripe/docs_truth_test.exs` — reuse `docs_config/0`, the existing `extras`/group assertions, and concise positive/refute anchor style.
    - `mix.exs` — verify, but do not rewrite, the existing extra and `Upgrading` group registration.
    - `CHANGELOG.md` sections 1.7.0, 2.0.0, and Unreleased plus `git show v1.7.13:lib/lattice_stripe/config.ex` — authoritative boundary proving the caller-supplied Finch requirement at the final 1.7 tag.
    - `.planning/phases/61-default-finch-pool-optional-application/61-CONTEXT.md` — authoritative later-phase ownership of the default pool.
  </read_first>
  <behavior>
    - The guide remains an ExDoc extra in the `Upgrading` group and retains its deliberate historical `~> 1.7` dependency pin.
    - The guide prominently says the leg stops at 1.7 and routes onward to 2.0 plus current Getting Started/Client Configuration without teaching current setup as 1.7 setup.
    - The guide retains all three affected-user checks and the production replay-protection warning beside `tolerance: 0`.
    - The known post-1.7 pool attribution fails the new contract before the guide correction and passes afterward.
  </behavior>
  <action>Per D-13/D-14, first add one test named `1.1-to-1.7 guide remains historically scoped and discoverable` inside `LatticeStripe.DocsTruthTest`. Read the guide once; reuse `docs_config/0` to assert publication in both `extras` and the `Upgrading` group; add durable anchors for D-01's historical scope/pin, D-02's 2.0 and current-doc handoff, D-06's three behavior predicates and replay warning, and D-03's historical contamination guard. Run the focused test and confirm the new historical-boundary assertion fails for the present guide before editing it. Then revise only the dependency/scope handoff portion and the 1.7 appendix row: preserve the historical dependency example and prominent banner, replace the later-release setup material with short descriptive links to the 2.0 CHANGELOG, Getting Started, and Client Configuration, and correct the 1.7 chronology. Keep reader copy at module/action/safety level per D-10, do not mention planning phases, and do not change `mix.exs` unless the test proves the existing registration differs from the live file. Do not add snapshots, network requests, tag-dependent test execution, or another manifest per D-12/D-14/D-15.</action>
  <acceptance_criteria>
    - The new test has exactly the name `1.1-to-1.7 guide remains historically scoped and discoverable`, reads the guide once, calls `docs_config/0`, and asserts membership in both `docs[:extras]` and `Map.new(docs[:groups_for_extras])["Upgrading"]`.
    - The guide contains `{:lattice_stripe, "~> 1.7"}`, the visible `1.1 → 1.7` boundary, a 2.0 route, and descriptive `.md` routes to current setup documentation.
    - The focused contract contains `refute guide =~ "default Finch pool"`, and the corrected guide satisfies it.
    - The guide retains three `Affected if:` occurrences, both “Before (1.1)” and “After (1.7)” examples for each affected behavior, and the production replay-protection warning adjacent to `tolerance: 0`.
    - `mix.exs` still contains `guides/upgrading-1-1-to-1-7.md` in `extras` and in the `Upgrading` group without unrelated configuration churn.
    - `git diff --name-only` for this task contains no path beginning with `lib/` and does not include `.planning/v1.10-MILESTONE-AUDIT.md`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs</automated>
  </verify>
  <done>D-01 through D-04 and the boundary portion of D-12 through D-15 are executable documentation truth: a 1.1 adopter can follow the stated 1.7 setup without being told to use a feature that did not exist, and the regression cannot silently return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the action-first migration checklist and capability inventory</name>
  <reversibility rating="costly">The published information architecture and examples shape adopter migration decisions; changing them later is editorially easy but correcting misunderstood integrations is coordinated user work.</reversibility>
  <files>guides/upgrading-1-1-to-1-7.md, test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-CONTEXT.md` — implement D-05 through D-11 and finish D-13/D-14 exactly.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-RESEARCH.md` — use the recommended job/family inventory matrix, safety pitfalls, and idiomatic ExDoc tradeoffs.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-PATTERNS.md` — follow the local heading, callout, `.md` link, guide-routing, and docs-truth patterns.
    - `guides/upgrading-1-1-to-1-7.md` — transform the strong existing material rather than rewriting it blindly.
    - `guides/user-flows-and-jtbd.md` — established consumer-job vocabulary and “Start Here By Situation” routing.
    - `guides/customer-portal.md`, `guides/tax.md`, `guides/webhooks-thin-events.md`, `guides/testing.md`, `guides/credit_notes.md`, `guides/quote-to-billing-operator.md` — canonical destinations; link instead of duplicating their full workflows.
    - `CHANGELOG.md` sections 1.3.0 through 1.7.0 and `git show v1.7.13:lib/lattice_stripe/testing.ex`, `git show v1.7.13:lib/lattice_stripe/testing/test_clock.ex`, and `git ls-tree -r v1.7.13 lib/lattice_stripe/testing` — version-accurate inventory and testing names.
    - `lib/lattice_stripe/webhook.ex` — current public arities for the concise verify-then-fetch example.
  </read_first>
  <behavior>
    - The reader path is target/scope → two-minute mandatory checklist → explicit safe exit and application-test action → optional job-routed additions → canonical guides → version appendix.
    - Each optional inventory row answers Need / surface / minimum call / canonical next step and preserves familiar ExDoc family grouping.
    - The catalogue covers every DOC-01/ROADMAP surface plus File/FileLink, Mandate, and SetupAttempt in the main catalogue, not merely the appendix.
    - Examples remain selective: the three caller-visible changes retain before/after code; portal retains Configuration → Session; thin events teach verify then fetch authoritative state; testing names match v1.7.13; deeper workflows stay in canonical guides.
    - The completed semantic contract protects only durable, user-harmful anchors and permits ordinary prose edits.
  </behavior>
  <action>Restructure the guide per D-05 into an action-first two-tier path while preserving the existing three D-06 before/after examples verbatim where they remain accurate. Add an explicit “none apply” exit and a test-before-deploy instruction before optional discovery. Per D-07/D-08, replace the family-first prose catalogue with compact tables grouped under familiar ExDoc families; every row must use the columns `Need`, `Surface`, `Minimum call`, and `Canonical next step`, start with the adopter's job, and include the full ROADMAP inventory plus File/FileLink, Mandate, and SetupAttempt. Per D-09, retain a concise BillingPortal.Configuration-to-Session snippet, make the thin-event example show verified notification followed by `fetch_event/3` before treating payload state as authoritative, and use the names verified at v1.7.13 for TestHelpers.TestClock, Testing.TestClock, Testing.Fixtures, and wrapper calls. Link detailed Tax, portal, webhook, testing, Credit Note, and Quote flows rather than restating them. Keep the version table last, repair numeric-part cross-references so labels and visible headings agree, and use semantic ExDoc callouts, descriptive `.md` links, prose before short Elixir fences, and consumer-contract microcopy per D-10/D-11. Extend the existing named test—not a second framework or test module—with positive anchors for every inventory family/canonical route and concise guards for obsolete numeric navigation labels. Keep assertions behavior-oriented and grouped by the reader harm they prevent; do not freeze complete paragraphs or rendered output per D-13/D-14.</action>
  <acceptance_criteria>
    - The guide's mandatory section precedes optional additions and explicitly tells unaffected readers they have no code migration, then tells every reader to run application tests before deployment.
    - The guide contains a Markdown table header with `Need`, `Surface`, `Minimum call`, and `Canonical next step`; the main inventory—not only the appendix—names BillingPortal.Configuration, Charge list/search, TestHelpers.TestClock, Testing.TestClock, Testing.Fixtures, Dispute, File, FileLink, Mandate, SetupAttempt, Tax.Calculation/Transaction/Settings/Registration/TaxId, CreditNote, Payout, Quote, BalanceTransaction, EventNotification, parse_event_notification, and fetch_event.
    - The Billing Portal example passes `config.id` into Session creation, and the thin-event example calls `fetch_event` after verification/parsing before describing the fetched event as authoritative.
    - Every local guide route ends in `.md`; full portal, Tax, webhook, testing, Credit Note, and Quote workflows remain owned by their canonical guides.
    - The completed named docs-truth test asserts the required inventory and route anchors with a small number of grouped assertions and contains no full-paragraph, line-number, rendered-HTML, network, or git-tag snapshot mechanism.
    - The completed guide contains no stale visible `Part 2` or `Part 3` navigation label, while the version-by-version table remains the final appendix rather than the primary navigation.
    - `mix test test/lattice_stripe/docs_truth_test.exs` exits 0 after the task commit, and `mix docs --warnings-as-errors` exits 0 after the wave.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs &amp;&amp; mix docs --warnings-as-errors</automated>
  </verify>
  <done>D-05 through D-14 are fully represented: a maintainer can decide required work in under two minutes, discover every shipped 1.3–1.7 surface by job, execute a minimum safe call, and continue in the canonical guide without encountering internal implementation language or later-release leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Run the strict phase gate and record mechanical evidence</name>
  <files>.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-VALIDATION.md` — retain the sampling contract and record observed evidence only after commands run.
    - `.agents/skills/lattice-verification-policy/SKILL.md` — all non-human classifications require named passing executable checks.
    - `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-CONTEXT.md` — cross-check D-01 through D-15 before closing.
    - `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — cross-check the Phase 62 goal, success criteria, and DOC-01.
    - `guides/upgrading-1-1-to-1-7.md`, `test/lattice_stripe/docs_truth_test.exs`, and `mix.exs` — final implementation scope and publication wiring.
  </read_first>
  <action>Run the focused docs-truth suite once more, run `mix docs --warnings-as-errors` as the wave gate, and run `mix ci` as the phase gate per D-12. Update 62-VALIDATION.md from draft/pending to the observed result only when every command exits 0; record command names, test result, docs result, and the D-15 file-scope check as locatable evidence. Confirm all D-01 through D-15 citations have corresponding implemented assertions or guide behavior. Do not reclassify an unproven deliverable as automated and do not touch the unrelated untracked milestone audit. If a command fails, leave validation pending, report the exact failure, and fix only a Phase 62 regression; do not weaken gates or absorb unrelated production changes.</action>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/docs_truth_test.exs`, `mix docs --warnings-as-errors`, and `mix ci` each exit 0 in the final workspace.
    - 62-VALIDATION.md records every planned row green, `nyquist_compliant: true`, `wave_0_complete: true`, and the exact passing command evidence rather than an assertion of success without output.
    - A scoped `git diff --name-only` confirms no production `lib/` path, dependency file, schema file, or public API lock file changed for Phase 62.
    - The ExDoc config still publishes the guide under `Upgrading`, as proven by the named test rather than editorial inspection.
    - `.planning/v1.10-MILESTONE-AUDIT.md` remains unmodified and unstaged.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs &amp;&amp; mix docs --warnings-as-errors &amp;&amp; mix ci</automated>
  </verify>
  <done>DOC-01 and D-01 through D-15 have current-message mechanical evidence; no human verification item remains under the repository's closed verification policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical release record → migration guide | Maintainers trust translated version claims to be true for the exact dependency pin, while the current checkout contains later behavior. |
| Migration guide → adopter application | Copy, examples, and safety callouts influence production configuration, webhook verification, and payload-handling decisions. |
| Guide/ExDoc configuration → HexDocs | A correct file that is absent from `extras` or `Upgrading` is effectively undiscoverable to its intended audience. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-62-01 | Tampering | Historical dependency/setup prose | high | mitigate | D-01/D-03/D-04 and the named docs-truth test preserve the 1.7 pin while rejecting the later default-pool attribution; `CHANGELOG.md` and tag v1.7.13 are the implementation sources. |
| T-62-02 | Elevation of privilege | `tolerance: 0` migration example | high | mitigate | D-06 keeps the testing-only boundary and explicit replay-protection warning adjacent to the before/after example; the semantic test asserts the safety anchor. |
| T-62-03 | Information disclosure / Tampering | Thin-event example and payload authority | high | mitigate | D-09 requires verify/parse followed by `fetch_event/3` before authoritative processing, with the canonical thin-event guide owning idempotency detail. |
| T-62-04 | Repudiation | Claims that docs semantics were validated | medium | mitigate | D-12/D-13 require a named semantic ExUnit contract plus docs and full CI evidence recorded in 62-VALIDATION.md. |
| T-62-05 | Denial of service | Brittle snapshots, network links, or tag-dependent CI | low | accept | D-14 deliberately uses durable local anchors; exact prose/HTML, external network, and tag-dependent gates are excluded because they add flaky maintenance cost without proving history. |
</threat_model>

## Specless probe disposition

| Requirement | Probe | Disposition |
|---|---|---|
| DOC-01 | adjacency | Flagged assumption: non-applicable to a static guide; it has no merge/collision/separation operation. Publication adjacency is instead explicitly proven by membership in the singular `Upgrading` group. |
| DOC-01 | empty/single/null | Flagged assumption: non-applicable to static prose input; the relevant empty failure is a missing guide/inventory anchor, which the focused positive assertions make observable. |
| DOC-01 | equal-order stability | Flagged assumption: no equal-key runtime ordering exists; reader order is intentionally specified by D-05 and asserted structurally as mandatory actions before optional inventory before appendix. |

<verification>
1. After Task 1 commits: `mix test test/lattice_stripe/docs_truth_test.exs`.
2. After Task 2 commits: `mix test test/lattice_stripe/docs_truth_test.exs`.
3. After Wave 1: `mix docs --warnings-as-errors`.
4. Phase gate: `mix ci`.
5. Scope gate: inspect `git diff --name-only` for the Phase 62 execution range; only the guide, docs-truth test, and validation record may differ, and the unrelated untracked milestone audit must remain untouched.
</verification>

<success_criteria>
- DOC-01: the published ExDoc guide enumerates every 1.3–1.7 surface named by the ROADMAP and D-08, with the three caller-visible before/after examples and safe minimum calls.
- Historical accuracy: the 1.7 pin remains intentional, later setup behavior is absent from the leg, and the generic onward routes remain present.
- Developer experience: required migration actions precede optional discovery; rows begin from adopter needs and end at canonical next steps.
- Verification: the focused semantic contract, warnings-as-errors docs build, and full `mix ci` all pass.
- Scope: no production library code or public API changes.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | 1.1 adopters discover every surface shipped since 1.1 with before/after examples | 62-01 | COVERED | Tasks 1–2 correct and complete the guide; Task 3 proves it. |
| REQ | DOC-01 | Publish the 1.1 → 1.7 guide in HexDocs with every surface and before/after examples | 62-01 | COVERED | Named docs-truth test plus ExDoc and CI gates. |
| RESEARCH | — | Action-first checklist before optional job-routed inventory | 62-01 | COVERED | Task 2 implements the researched information architecture. |
| RESEARCH | — | Historical default-Finch correction and thin current-doc handoff | 62-01 | COVERED | Task 1 uses CHANGELOG/tag evidence and semantic absence assertion. |
| RESEARCH | — | Complete inventory including File/FileLink, Mandate, SetupAttempt | 62-01 | COVERED | Task 2 acceptance enumerates all required surfaces. |
| RESEARCH | — | Selective portal, Tax, thin-event, testing examples and canonical routing | 62-01 | COVERED | Task 2 retains decision-bearing snippets and routes deeper workflows. |
| RESEARCH | — | One compact semantic contract; docs warnings plus full CI | 62-01 | COVERED | Tasks 1–3 follow the validation sampling contract. |
| CONTEXT | D-01–D-04 | Strict historical boundary and later-release separation | 62-01 | COVERED | Task 1. |
| CONTEXT | D-05–D-11 | Checklist, complete inventory, examples, copy, ExDoc UX | 62-01 | COVERED | Task 2. |
| CONTEXT | D-12–D-14 | Layered semantic/build/CI drift protection without brittle mechanisms | 62-01 | COVERED | All tasks and final gate. |
| CONTEXT | D-15 | Zero production-library-code boundary | 62-01 | COVERED | File scope and final diff gate. |

<output>
Create `.planning/phases/62-1-1-1-7-what-landed-migration-guide/62-01-SUMMARY.md` when done.
</output>
