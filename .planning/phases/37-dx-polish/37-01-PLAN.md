---
phase: 37-dx-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/testing.ex
  - lib/lattice_stripe/testing/fixtures.ex
  - lib/lattice_stripe/testing/fixtures/file.ex
  - lib/lattice_stripe/testing/fixtures/file_link.ex
  - lib/lattice_stripe/testing/fixtures/dispute.ex
  - lib/lattice_stripe/testing/fixtures/credit_note.ex
  - lib/lattice_stripe/testing/fixtures/mandate.ex
  - lib/lattice_stripe/testing/fixtures/setup_attempt.ex
  - lib/lattice_stripe/testing/fixtures/quote.ex
  - mix.exs
  - test/lattice_stripe/testing_test.exs
autonomous: true
requirements:
  - DX-02

must_haves:
  truths:
    - "`LatticeStripe.Testing.Fixtures.*` exposes public raw-map builders for File, FileLink, Dispute, CreditNote, Mandate, SetupAttempt, and Quote"
    - "Raw Stripe-shaped maps are the canonical fixture source of truth; typed/event/webhook forms are layered explicitly on top"
    - "`LatticeStripe.Testing` exposes discoverable wrappers that convert canonical fixture maps into `%Event{}`, signed webhook payloads, and typed resource structs where appropriate"
    - "The public testing helper surface avoids option-driven polymorphism and uses separate functions or modules for separate return shapes"
    - "Unit tests lock the public helper names, return shapes, and wrapper behavior for all v1.3 families"
  artifacts:
    - path: "lib/lattice_stripe/testing/fixtures.ex"
      provides: "Public entrypoint and documentation for fixture builders"
      exports: ["__info__/1"]
    - path: "lib/lattice_stripe/testing/fixtures/file.ex"
      provides: "Public File fixture builders"
      exports: ["file_json/1"]
    - path: "lib/lattice_stripe/testing/fixtures/file_link.ex"
      provides: "Public FileLink fixture builders"
      exports: ["file_link_json/1"]
    - path: "lib/lattice_stripe/testing/fixtures/dispute.ex"
      provides: "Public Dispute fixture builders"
      exports: ["dispute_json/1"]
    - path: "lib/lattice_stripe/testing/fixtures/credit_note.ex"
      provides: "Public CreditNote fixture builders"
      exports: ["credit_note_json/1"]
    - path: "lib/lattice_stripe/testing/fixtures/mandate.ex"
      provides: "Public Mandate fixture builders"
      exports: ["mandate_json/1"]
    - path: "lib/lattice_stripe/testing/fixtures/setup_attempt.ex"
      provides: "Public SetupAttempt fixture builders"
      exports: ["setup_attempt_json/1"]
    - path: "lib/lattice_stripe/testing/fixtures/quote.ex"
      provides: "Public Quote fixture builders"
      exports: ["quote_json/1"]
    - path: "lib/lattice_stripe/testing.ex"
      provides: "Public wrappers that turn canonical fixture maps into signed payloads, events, or typed structs"
      exports: ["generate_webhook_event/3", "generate_webhook_payload/3"]
    - path: "test/lattice_stripe/testing_test.exs"
      provides: "Coverage for public testing helpers and fixture builders"
      contains: "LatticeStripe.TestingTest"
  key_links:
    - from: "lib/lattice_stripe/testing.ex"
      to: "lib/lattice_stripe/testing/fixtures/*.ex"
      via: "wrapper helpers consume canonical raw fixture maps"
      pattern: "LatticeStripe\\.Testing\\.Fixtures"
    - from: "lib/lattice_stripe/testing.ex"
      to: "lib/lattice_stripe/webhook.ex"
      via: "signed payload generation"
      pattern: "Webhook\\.generate_test_signature"
    - from: "test/lattice_stripe/testing_test.exs"
      to: "lib/lattice_stripe/testing/fixtures/*.ex"
      via: "public fixture-builder coverage"
      pattern: "_json"
---

<objective>
Create the public fixture-builder API for all v1.3 resource families and extend `LatticeStripe.Testing` with explicit wrappers that make those fixtures easy to use in downstream app tests.

Purpose: This plan delivers the public DX API surface behind DX-02 before any guide rewrites teach it.
Output: Public `LatticeStripe.Testing.Fixtures.*` modules, updated testing helpers, and focused unit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-dx-polish/37-CONTEXT.md
@.planning/phases/37-dx-polish/37-RESEARCH.md
@.planning/phases/37-dx-polish/37-PATTERNS.md
@.planning/phases/37-dx-polish/37-VALIDATION.md
@.planning/threads/lattice-stripe-vs-accrue-scope-boundary.md

<interfaces>
From `lib/lattice_stripe/testing.ex`:
```elixir
@spec generate_webhook_event(String.t(), map(), keyword()) :: Event.t()
@spec generate_webhook_payload(String.t(), map(), keyword()) :: {String.t(), String.t()}
```

From resource modules:
```elixir
from_map(map() | nil) :: t() | nil
```

From existing internal fixtures:
```elixir
*_json(map()) :: map()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish canonical raw fixture builders for all v1.3 families</name>
  <files>
    lib/lattice_stripe/testing/fixtures.ex
    lib/lattice_stripe/testing/fixtures/file.ex
    lib/lattice_stripe/testing/fixtures/file_link.ex
    lib/lattice_stripe/testing/fixtures/dispute.ex
    lib/lattice_stripe/testing/fixtures/credit_note.ex
    lib/lattice_stripe/testing/fixtures/mandate.ex
    lib/lattice_stripe/testing/fixtures/setup_attempt.ex
    lib/lattice_stripe/testing/fixtures/quote.ex
    mix.exs
  </files>
  <read_first>
    lib/lattice_stripe/testing.ex
    test/support/fixtures/file.ex
    test/support/fixtures/file_link.ex
    test/support/fixtures/dispute.ex
    test/support/fixtures/credit_note.ex
    test/support/fixtures/mandate.ex
    test/support/fixtures/setup_attempt.ex
    test/support/fixtures/quote.ex
    .planning/phases/37-dx-polish/37-CONTEXT.md
    .planning/phases/37-dx-polish/37-PATTERNS.md
  </read_first>
  <behavior>
    - Each public fixture module returns Stripe-shaped raw maps and remains safe to ship in `lib/`.
    - Public names are explicit and normalized toward `*_json/1` or equivalent scenario helpers; no `basic/1`-style ambiguity on the public surface.
    - Existing internal fixture data is reused or mirrored closely enough that the public surface does not create a second canonical truth.
    - ExDoc grouping includes the new public testing fixture modules in a discoverable location.
  </behavior>
  <action>
Create a public `LatticeStripe.Testing.Fixtures` namespace in `lib/` and add one module per v1.3 family:

- `LatticeStripe.Testing.Fixtures.File`
- `LatticeStripe.Testing.Fixtures.FileLink`
- `LatticeStripe.Testing.Fixtures.Dispute`
- `LatticeStripe.Testing.Fixtures.CreditNote`
- `LatticeStripe.Testing.Fixtures.Mandate`
- `LatticeStripe.Testing.Fixtures.SetupAttempt`
- `LatticeStripe.Testing.Fixtures.Quote`

Implementation requirements:

- use the existing `test/support/fixtures/*.ex` payload shapes as the canonical starting point
- normalize public helper names toward explicit raw-map builders such as `file_json/1`, `file_link_json/1`, `dispute_json/1`, `credit_note_json/1`, `mandate_json/1`, `setup_attempt_json/1`, and `quote_json/1`
- preserve scenario helpers that materially improve readability, such as quote state helpers or expanded-reference variants
- keep these modules free of integration-only setup helpers unless a helper is clearly safe and intentional for downstream app tests
- add a small `lib/lattice_stripe/testing/fixtures.ex` module or namespace doc that explains raw maps are the canonical source of truth and points developers to the resource-specific modules

Update `mix.exs` docs grouping so the new public testing fixture modules are visible in the generated docs.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/testing_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/testing/fixtures/file.ex` contains `defmodule LatticeStripe.Testing.Fixtures.File do`
    - `lib/lattice_stripe/testing/fixtures/file_link.ex` contains `defmodule LatticeStripe.Testing.Fixtures.FileLink do`
    - `lib/lattice_stripe/testing/fixtures/dispute.ex` contains `defmodule LatticeStripe.Testing.Fixtures.Dispute do`
    - `lib/lattice_stripe/testing/fixtures/credit_note.ex` contains `defmodule LatticeStripe.Testing.Fixtures.CreditNote do`
    - `lib/lattice_stripe/testing/fixtures/mandate.ex` contains `defmodule LatticeStripe.Testing.Fixtures.Mandate do`
    - `lib/lattice_stripe/testing/fixtures/setup_attempt.ex` contains `defmodule LatticeStripe.Testing.Fixtures.SetupAttempt do`
    - `lib/lattice_stripe/testing/fixtures/quote.ex` contains `defmodule LatticeStripe.Testing.Fixtures.Quote do`
  </acceptance_criteria>
  <done>Public raw fixture builders exist for every v1.3 family and are discoverable in the package docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layer explicit typed, event, and signed-payload wrappers on top of canonical fixtures</name>
  <files>
    lib/lattice_stripe/testing.ex
    test/lattice_stripe/testing_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/testing.ex
    lib/lattice_stripe/webhook.ex
    lib/lattice_stripe/event.ex
    lib/lattice_stripe/file.ex
    lib/lattice_stripe/file_link.ex
    lib/lattice_stripe/dispute.ex
    lib/lattice_stripe/credit_note.ex
    lib/lattice_stripe/mandate.ex
    lib/lattice_stripe/setup_attempt.ex
    lib/lattice_stripe/quote.ex
    test/lattice_stripe/testing_test.exs
    .planning/phases/37-dx-polish/37-CONTEXT.md
  </read_first>
  <behavior>
    - `LatticeStripe.Testing` stays explicit: one helper per return shape or workflow need.
    - Raw fixture maps remain canonical; typed wrappers call resource `from_map/1` contracts, not parallel hand-written casts.
    - Webhook-oriented helpers keep using the existing signing/event behavior and can accept canonical fixture maps directly.
    - No `as:` or other option-driven return-shape polymorphism is introduced.
  </behavior>
  <action>
Extend `lib/lattice_stripe/testing.ex` so downstream users can turn the canonical public fixture maps into other useful test forms without magic options.

Recommended helper shape:

- keep `generate_webhook_event/3` and `generate_webhook_payload/3`
- add explicit helpers that accept a resource map and/or event type where appropriate
- if typed resource wrappers are added, keep them separate and resource-named rather than option-driven

Implementation requirements:

- reuse `from_map/1` from the real resource modules for typed wrappers
- preserve the existing webhook signing path through `Webhook.generate_test_signature/3`
- update module docs and examples to show the new fixture-builder flow
- keep wrapper additions focused on the v1.3 DX goals, not a generic fixture DSL for the entire library

Update `test/lattice_stripe/testing_test.exs` to cover:

- public fixture maps feeding webhook/event helpers
- at least one typed wrapper per representative family
- stable return-shape expectations
- absence of option-driven shape switching in the public API
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/testing_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/testing.ex` contains `def generate_webhook_event(`
    - `lib/lattice_stripe/testing.ex` contains `def generate_webhook_payload(`
    - `lib/lattice_stripe/testing.ex` contains `LatticeStripe.Testing.Fixtures`
    - `test/lattice_stripe/testing_test.exs` contains `describe "generate_webhook_event`
    - `test/lattice_stripe/testing_test.exs` contains `describe "generate_webhook_payload`
  </acceptance_criteria>
  <done>`LatticeStripe.Testing` now exposes explicit wrappers that build on the canonical public fixture maps instead of hiding return-shape changes behind options.</done>
</task>

</tasks>

<verification>
Run `mix test test/lattice_stripe/testing_test.exs --warnings-as-errors`.
</verification>

<success_criteria>
- Developers can discover public fixture builders for every v1.3 family from `LatticeStripe.Testing.Fixtures.*`.
- Raw fixture maps are the canonical source and typed/event/payload forms are explicit wrappers.
- The public testing helper surface is documented, tested, and free of option-driven polymorphism.
</success_criteria>
