---
phase: 37-dx-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - guides/webhooks.md
  - guides/testing.md
  - guides/recipes.md
  - mix.exs
autonomous: true
requirements:
  - DX-01
  - DX-02
  - DX-03

must_haves:
  truths:
    - "`guides/webhooks.md` teaches one canonical Phoenix quickstart: `LatticeStripe.Webhook.Plug` in `endpoint.ex` before `Plug.Parsers`, path-gated with `at:` and backed by a handler module"
    - "Webhook docs make the raw-body invariant and runtime secret resolution explicit before presenting advanced alternatives"
    - "`guides/testing.md` teaches the new public fixture-builder surface and shows how it composes with signed payload and event helpers"
    - "`guides/recipes.md` exists as a compact bridge guide for dispute handling, credit issuance, and quote-to-invoice workflows"
    - "All guide examples stay library-scoped and reinforce the rule that webhooks confirm async truth"
  artifacts:
    - path: "guides/webhooks.md"
      provides: "Canonical Phoenix webhook setup, advanced alternative, and troubleshooting"
      contains: "LatticeStripe.Webhook.Plug"
    - path: "guides/testing.md"
      provides: "Public testing helper guidance with fixture builders"
      contains: "LatticeStripe.Testing.Fixtures"
    - path: "guides/recipes.md"
      provides: "Compact workflow recipes guide"
      contains: "quote-to-invoice"
    - path: "mix.exs"
      provides: "ExDoc extras registration for the recipes guide"
      contains: "guides/recipes.md"
  key_links:
    - from: "guides/webhooks.md"
      to: "lib/lattice_stripe/webhook/plug.ex"
      via: "canonical setup documentation"
      pattern: "Webhook\\.Plug"
    - from: "guides/testing.md"
      to: "lib/lattice_stripe/testing.ex"
      via: "public helper documentation"
      pattern: "LatticeStripe\\.Testing"
    - from: "guides/recipes.md"
      to: "guides/webhooks.md"
      via: "workflow confirmation via webhooks"
      pattern: "webhooks"
---

<objective>
Rewrite the public v1.3 docs around one obvious webhook path, discoverable testing helpers, and a compact recipes guide that bridges conceptual and topical documentation.

Purpose: This plan delivers the main user-facing DX improvements behind DX-01 and DX-03 while teaching the public helper surface introduced in Plan 01.
Output: Updated webhook and testing guides, a new recipes guide, and ExDoc registration for that guide.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-dx-polish/37-CONTEXT.md
@.planning/phases/37-dx-polish/37-RESEARCH.md
@.planning/phases/37-dx-polish/37-PATTERNS.md
@.planning/phases/37-dx-polish/37-VALIDATION.md
@guides/webhooks.md
@guides/testing.md
@guides/user-flows-and-jtbd.md
@guides/customer-portal.md
@guides/credit_notes.md

<interfaces>
Upstream docs to honor:
```text
https://docs.stripe.com/webhooks
https://docs.stripe.com/webhooks/signature
https://hexdocs.pm/plug/Plug.Parsers.html
https://hexdocs.pm/ex_doc/ExDoc.html
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reframe the webhook guide around one canonical Phoenix quickstart</name>
  <files>
    guides/webhooks.md
  </files>
  <read_first>
    guides/webhooks.md
    lib/lattice_stripe/webhook/plug.ex
    lib/lattice_stripe/webhook/cache_body_reader.ex
    lib/lattice_stripe/testing.ex
    guides/customer-portal.md
    .planning/phases/37-dx-polish/37-CONTEXT.md
    .planning/phases/37-dx-polish/37-RESEARCH.md
  </read_first>
  <behavior>
    - The first complete setup path is the recommended path, not one of several equal choices.
    - Runtime secret resolution is used in primary examples.
    - The raw-body invariant appears before the user reaches implementation details.
    - The advanced `CacheBodyReader` + `forward` route remains supported but clearly secondary.
  </behavior>
  <action>
Rewrite `guides/webhooks.md` so its top-level flow is:

1. why webhooks matter in LatticeStripe’s operating model
2. the raw-body invariant
3. one canonical Phoenix quickstart using `LatticeStripe.Webhook.Plug` in `endpoint.ex` before `Plug.Parsers`
4. a concrete handler module example
5. local testing and common pitfalls
6. advanced alternative using `CacheBodyReader` + router `forward`

Implementation requirements:

- primary example uses runtime secret resolution (`secret: {M, F, A}` or zero-arity function)
- explicitly teach “app flows start work, webhooks confirm reality”
- retain signature-verification and troubleshooting guidance
- keep the advanced path documented, but demote it structurally and rhetorically
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `guides/webhooks.md` contains `LatticeStripe.Webhook.Plug`
    - `guides/webhooks.md` contains `Plug.Parsers`
    - `guides/webhooks.md` contains `raw`
    - `guides/webhooks.md` contains `CacheBodyReader`
  </acceptance_criteria>
  <done>The webhook guide now teaches one obvious Phoenix path first and preserves advanced alternatives as secondary material.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Teach the public testing helper surface and add a compact recipes guide</name>
  <files>
    guides/testing.md
    guides/recipes.md
    mix.exs
  </files>
  <read_first>
    guides/testing.md
    lib/lattice_stripe/testing.ex
    lib/lattice_stripe/testing/fixtures/*.ex
    guides/user-flows-and-jtbd.md
    guides/customer-portal.md
    guides/credit_notes.md
    guides/webhooks.md
    mix.exs
    .planning/phases/37-dx-polish/37-CONTEXT.md
  </read_first>
  <behavior>
    - `guides/testing.md` makes the new public fixture surface easy to discover and use.
    - `guides/recipes.md` stays compact, practical, and library-scoped.
    - Recipes cross-link out to deeper guides instead of becoming a second handbook.
    - ExDoc includes the new guide.
  </behavior>
  <action>
Update `guides/testing.md` to document the new public fixture-builder flow:

- where to find `LatticeStripe.Testing.Fixtures.*`
- how raw fixture maps compose with `generate_webhook_event/3`
- how raw fixture maps compose with `generate_webhook_payload/3`
- when to use typed wrappers if Plan 01 introduced them

Create `guides/recipes.md` with exactly three flagship workflows:

- dispute handling / evidence workflow
- credit issuance / invoice adjustment workflow
- quote-to-invoice workflow

Each recipe should include:

- the job to be done
- the key LatticeStripe calls
- the webhook confirmation point
- links to deeper guides

Update `mix.exs` so `guides/recipes.md` is present in ExDoc extras/groups.
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `guides/testing.md` contains `LatticeStripe.Testing.Fixtures`
    - `guides/recipes.md` contains `dispute`
    - `guides/recipes.md` contains `credit`
    - `guides/recipes.md` contains `quote`
    - `mix.exs` contains `guides/recipes.md`
  </acceptance_criteria>
  <done>The public docs now teach the shipped testing helpers and include a compact workflow recipes bridge guide.</done>
</task>

</tasks>

<verification>
Run `mix docs --warnings-as-errors`.
</verification>

<success_criteria>
- New developers can find one obvious Phoenix webhook setup and copy it successfully.
- Testing docs teach the new public fixture helpers instead of leaving users to hand-roll raw maps.
- `guides/recipes.md` exists, is compact, and bridges conceptual and topical docs without drifting into orchestration.
</success_criteria>
