---
phase: 37-dx-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
files_modified:
  - README.md
  - CHANGELOG.md
  - mix.exs
  - guides/webhooks.md
  - guides/testing.md
  - guides/recipes.md
  - guides/*.md
  - test/lattice_stripe/testing_test.exs
autonomous: true
requirements:
  - DX-04

must_haves:
  truths:
    - "High-visibility public docs and package metadata reflect the actual v1.3-capable branch state"
    - "README install snippets, 'what's new' framing, and guide lists no longer point at the stale v1.1 story"
    - "`mix.exs` docs metadata and extras align with the real guide set"
    - "`CHANGELOG.md` communicates release status truthfully without implying a publish or tag already happened"
    - "Guide cross-links and examples are internally consistent with the current API surface"
  artifacts:
    - path: "README.md"
      provides: "Current installation/version/high-level guide truth"
      contains: "lattice_stripe"
    - path: "CHANGELOG.md"
      provides: "Current release-status framing"
      contains: "Unreleased"
    - path: "mix.exs"
      provides: "Current package version/docs metadata/extras list"
      contains: "@version"
    - path: "guides/*.md"
      provides: "Consistent guide cross-links and examples"
      contains: ".md"
  key_links:
    - from: "README.md"
      to: "mix.exs"
      via: "install snippet and package version truth"
      pattern: "1\\."
    - from: "README.md"
      to: "guides/recipes.md"
      via: "high-visibility guide discovery"
      pattern: "Recipes"
    - from: "CHANGELOG.md"
      to: "README.md"
      via: "release framing consistency"
      pattern: "v1\\."
---

<objective>
Complete the public trust sweep so the package version story, guide indexes, examples, and high-visibility docs all align with the actual v1.3 branch state.

Purpose: This plan closes DX-04 and prevents adoption friction caused by stale or contradictory public documentation.
Output: Updated README/changelog/docs metadata, cross-link cleanup, and any narrow docs-truth checks needed to keep the story coherent.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-dx-polish/37-CONTEXT.md
@.planning/phases/37-dx-polish/37-RESEARCH.md
@.planning/phases/37-dx-polish/37-PATTERNS.md
@.planning/phases/37-dx-polish/37-VALIDATION.md
@README.md
@CHANGELOG.md
@mix.exs
@guides/webhooks.md
@guides/testing.md
@guides/recipes.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align README, changelog, docs metadata, and guide discovery with the real v1.3 branch state</name>
  <files>
    README.md
    CHANGELOG.md
    mix.exs
    guides/recipes.md
    guides/webhooks.md
    guides/testing.md
  </files>
  <read_first>
    README.md
    CHANGELOG.md
    mix.exs
    guides/*.md
    .planning/STATE.md
    .planning/phases/37-dx-polish/37-CONTEXT.md
    .planning/phases/37-dx-polish/37-RESEARCH.md
  </read_first>
  <behavior>
    - README no longer advertises the stale v1.1 story.
    - Install snippets and docs metadata match the intended package/version truth for the branch.
    - Guide discovery surfaces the actual guide set, including recipes.
    - Changelog is explicit about status and avoids pretending a release already happened.
  </behavior>
  <action>
Update `README.md`, `CHANGELOG.md`, and `mix.exs` together so they tell one coherent public story.

Implementation requirements:

- replace stale v1.1-centric framing in `README.md`
- update install snippets to the intended current version line for the branch
- ensure the guide list in `README.md` matches the ExDoc extras set closely enough that adopters are not sent to missing or stale docs
- update `mix.exs` docs metadata, extras, and any visible version-coupled references as needed
- keep `CHANGELOG.md` explicit about what is released versus still on the branch
- surface `guides/recipes.md` in the guide discovery story
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Recipes` or `recipes`
    - `README.md` does not contain `What's new in v1.1`
    - `mix.exs` contains `guides/recipes.md`
    - `CHANGELOG.md` contains `Unreleased`
  </acceptance_criteria>
  <done>Public version/docs truth is aligned across the main entrypoints and guide discovery surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add narrow docs-truth checks or durable assertions for the new DX surface</name>
  <files>
    test/lattice_stripe/testing_test.exs
    test/lattice_stripe/*docs*_test.exs
  </files>
  <read_first>
    test/lattice_stripe/testing_test.exs
    README.md
    mix.exs
    guides/recipes.md
    guides/testing.md
    guides/webhooks.md
    .planning/phases/37-dx-polish/37-VALIDATION.md
  </read_first>
  <behavior>
    - Any new tests are narrow, durable, and focused on high-value drift points.
    - Tests avoid snapshotting whole docs files.
    - The checks reinforce the new public testing surface and guide registration rather than overfitting on prose.
  </behavior>
  <action>
Add the minimum durable verification needed to keep the public DX story from drifting again.

Good candidates:

- a test that the public testing fixture modules load and expose the expected baseline builders
- a test or assertion that ExDoc extras include `guides/recipes.md`
- a narrow version/docs-truth assertion if it can be implemented without brittle prose matching

Do not add large snapshot tests for Markdown content.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/testing_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/lattice_stripe/testing_test.exs` or a new docs-focused test contains `recipes`
    - docs-truth checks do not rely on whole-file snapshots
  </acceptance_criteria>
  <done>Narrow automated checks now cover the highest-value DX drift points without making docs maintenance brittle.</done>
</task>

</tasks>

<verification>
Run `mix test test/lattice_stripe/testing_test.exs --warnings-as-errors` and `mix docs --warnings-as-errors`.
</verification>

<success_criteria>
- High-visibility docs/package surfaces are coherent and current.
- Recipes and testing helper discovery are reflected in README and ExDoc.
- Narrow automated checks cover the most likely future DX drift points.
</success_criteria>
