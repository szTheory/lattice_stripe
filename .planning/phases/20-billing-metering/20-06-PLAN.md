---
phase: 20-billing-metering
plan: 06
type: execute
wave: 5
depends_on: [20-03, 20-04, 20-05]
files_modified:
  - guides/metering.md
  - mix.exs
  - guides/subscriptions.md
  - guides/webhooks.md
  - guides/telemetry.md
  - guides/error-handling.md
  - guides/testing.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
  - DOCS-04
must_haves:
  truths:
    - "guides/metering.md exists with 12 H2 sections and 580 ± 40 line count"
    - "Monitoring section contains 7-row error code table including error_report_triggered codes"
    - "Two-layer idempotency section contrasts body identifier vs idempotency_key: opt"
    - "Backdating window section mentions 35-day past and 5-min future"
    - "AccrueLike.UsageReporter module example present (~40 lines)"
    - "Dunning correction example shows exact cancel.identifier nested shape"
    - "mix.exs groups_for_modules contains 'Billing Metering' with 8 modules"
    - "mix.exs extras includes guides/metering.md"
    - "5 sibling guides reciprocally link to metering.md"
    - "mix docs builds clean and mix credo --strict passes for lib/lattice_stripe/billing/"
  artifacts:
    - path: "guides/metering.md"
      provides: "DOCS-01 — canonical Phase 20 guide per D-05 outline"
      min_lines: 540
      contains: "error_report_triggered"
    - path: "mix.exs"
      provides: "DOCS-03 — Billing Metering group in groups_for_modules + extras entry"
      contains: "Billing Metering"
  key_links:
    - from: "guides/subscriptions.md"
      to: "guides/metering.md"
      via: "markdown link in metered-price section"
      pattern: "metering.md"
    - from: "guides/webhooks.md"
      to: "guides/metering.md#reconciliation-via-webhooks"
      via: "markdown anchor link"
      pattern: "metering.md"
---

<objective>
Ship `guides/metering.md` per D-05 (580 ± 40 lines, 12 H2 sections), register it in
`mix.exs` under the new "Billing Metering" ExDoc group, and land reciprocal crosslinks
from the 5 sibling guides. Final credo/ExDoc sweep for `lib/lattice_stripe/billing/`.

Purpose: The single differentiator over stripity_stripe. Accrue authors land here first.
Output: 1 new guide (~580 LOC), mix.exs docs config, 5 link edits, clean ExDoc build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-billing-metering/20-CONTEXT.md
@.planning/phases/20-billing-metering/20-RESEARCH.md
@guides/invoices.md
@guides/subscriptions.md
@guides/webhooks.md
@mix.exs
@lib/lattice_stripe/billing/meter.ex
@lib/lattice_stripe/billing/meter_event.ex
@lib/lattice_stripe/billing/meter_event_adjustment.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/metering.md per D-05 outline</name>
  <files>guides/metering.md</files>
  <read_first>
    - guides/invoices.md (anchor style, 556 lines, closest length reference)
    - guides/subscriptions.md (webhook-handoff callout precedent)
    - .planning/phases/20-billing-metering/20-CONTEXT.md D-05 (full 12-section outline lines 240-295)
    - .planning/phases/20-billing-metering/20-CONTEXT.md D-05 snippet references (error code table, warnings)
    - lib/lattice_stripe/billing/meter_event.ex (imports into examples)
  </read_first>
  <action>
    Create `guides/metering.md` with the exact 12 H2 sections from D-05. Target 580 lines
    ± 40. Hard ceiling 700 lines (cut dunning example to reference-only if breached).
    Hard floor 450 lines (error-code table and formula section are too shallow below this).

    Required content checklist (ALL must be present verbatim or semantically):

    1. **Intro** — 2 paragraphs. What metering is, what the guide covers.

    2. **Mental model** — H2 with a Markdown ASCII diagram showing
       `Meter (schema) --> MeterEvent (facts) --> Subscription (billing glue)`.
       Crosslink out to `subscriptions.md` metered-price section.

    3. **Defining a meter** — H2 with H3s "Aggregation formulas", "customer_mapping",
       "value_settings", "Lifecycle verbs".
       - Formula H3: one paragraph per formula (`sum`, `count`, `last`) with one
         concrete example each. Callout: `sum`/`last` REQUIRE well-formed
         `value_settings.event_payload_key`.

    4. **Reporting usage (the hot path)** — H2 ⬅ anchor of the guide
       - H3 "The fire-and-forget idiom": full `AccrueLike.UsageReporter` module
         (~40 lines) using `Task.Supervisor.start_child`, telemetry spans,
         error-type classification (retry transient / drop permanent).
       - H3 "Two-layer idempotency": side-by-side code block for `identifier`
         (body, business-layer) vs `idempotency_key:` (transport, HTTP) + explanatory
         table + the "set BOTH in production" rule with rationale. MUST be based on
         the exact MeterEvent.create/3 signature from Plan 20-04.
       - H3 "Timestamp semantics": 35-day past / 5-minute future window, clock skew note.
       - H3 "What NOT to do: nightly batch flush" — explicit anti-pattern code block
         inside `> **Warning:**` blockquote, with the corrected version immediately below.

    5. **Corrections and adjustments** — H2 with H3s "MeterEventAdjustment.create/3",
       "Dunning-style over-report flow (worked example)".
       - Dunning example: ~45 lines end-to-end. MUST show the exact
         `cancel: %{"identifier" => ...}` nested shape, NOT top-level.
       - Reference GUARD-03 raising on wrong shapes.

    6. **Reconciliation via webhooks** — H2 with H3s "The error-report webhook",
       "Error codes you must handle", "Remediation patterns".
       - MUST contain literal `v1.billing.meter.error_report_triggered`.
       - MUST contain the 7-row error code table:

       | `error_code` | When | Silent drop? | Remediation |
       |---|---|---|---|
       | `meter_event_customer_not_found` | customer deleted | YES (async) | Sweep job |
       | `meter_event_no_customer_defined` | payload missing mapping key | YES (async) | Fix reporter |
       | `meter_event_invalid_value` | value not numeric | YES (async) | Fix reporter |
       | `meter_event_value_not_found` | sum/last but no value | YES (async) | Fix payload (likely GUARD-01 bypass) |
       | `archived_meter` | meter deactivated | NO (sync 400) | Alert — data PERMANENTLY LOST |
       | `timestamp_too_far_in_past` | >35 days | NO (sync 400) | Drop batch flush |
       | `timestamp_in_future` | >5 min future | NO (sync 400) | Fix clock skew |

    7. **Observability** — H2 with H3s "Telemetry for the hot path", "Debugging with Inspect".
       - Debugging section documents the D-02 payload masking with escape hatches:
         `IO.inspect(event, structs: false)` and direct `event.payload` field access.
         Explicit: "NEVER log raw MeterEvent.payload — it contains stripe_customer_id."

    8. **Guards and escape hatches** — H2. Frame `check_meter_value_settings!/1` as
       "only for porting from another SDK; production code should fix the meter".
       Show the one-line `LatticeStripe.Client.request/4` bypass.

    9. **Common pitfalls** — H2 with 7 bullets mirroring research PITFALLS.md, each
       2-3 lines with crosslink back into relevant section.

    10. **See also** — H2 with 5 reciprocal links: `subscriptions.md`, `webhooks.md`,
        `telemetry.md`, `error-handling.md`, `testing.md`.

    **Style conventions (copy from v1.0 guides):**
    - `> **Warning:**` blockquote for destructive/silent-data-loss content
    - `> **Note:**` blockquote for easy-to-miss facts
    - Code blocks always `elixir`-fenced
    - Inline `` `code` `` for function names and atoms
    - Cross-references by path: `[subscriptions.md](subscriptions.md#metered-prices)`

    Do NOT add a section not in the D-05 outline. Do NOT omit the error code table.
  </action>
  <verify>
    <automated>test -f guides/metering.md && LINES=$(wc -l < guides/metering.md) && [ $LINES -ge 540 ] && [ $LINES -le 620 ] && grep -q "error_report_triggered" guides/metering.md && grep -q "accepted for processing" guides/metering.md && grep -q 'cancel.*identifier' guides/metering.md && grep -q "35-day" guides/metering.md && grep -q "UsageReporter" guides/metering.md</automated>
  </verify>
  <acceptance_criteria>
    - File guides/metering.md exists
    - Line count: 540 ≤ lines ≤ 620 (580 ± 40)
    - Contains literal strings: "error_report_triggered", "35-day", "UsageReporter", "Two-layer idempotency"
    - Contains the 7-row error code table with `archived_meter`, `meter_event_customer_not_found`, `timestamp_too_far_in_past`
    - Contains an H2 "## See also" section linking to at least 5 sibling guides
    - Contains literal substring `%{"identifier" =>` (exact cancel.identifier nested shape in dunning example)
    - Contains `IO.inspect(event, structs: false)` debugging escape hatch
    - Contains the nightly batch flush anti-pattern inside a `> **Warning:**` blockquote
  </acceptance_criteria>
  <done>DOCS-01 canonical guide landed per D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Register Billing Metering ExDoc group + extras in mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (existing `groups_for_modules` and `extras` config)
    - .planning/phases/20-billing-metering/20-CONTEXT.md Integration Points (lines ~448)
    - lib/lattice_stripe/billing/ (list all 7 modules Phase 20 added)
  </read_first>
  <action>
    Edit `mix.exs` in two places:

    1. In the ExDoc `extras:` list, append `"guides/metering.md"` in the Guides section
       (alphabetically / or at the end of the billing cluster next to invoices.md).

    2. In the `groups_for_modules:` keyword list, add a new entry (placed AFTER the
       existing `"Billing"` group and BEFORE `"Connect"`):

    ```elixir
    "Billing Metering": [
      LatticeStripe.Billing.Meter,
      LatticeStripe.Billing.Meter.DefaultAggregation,
      LatticeStripe.Billing.Meter.CustomerMapping,
      LatticeStripe.Billing.Meter.ValueSettings,
      LatticeStripe.Billing.Meter.StatusTransitions,
      LatticeStripe.Billing.MeterEvent,
      LatticeStripe.Billing.MeterEventAdjustment,
      LatticeStripe.Billing.MeterEventAdjustment.Cancel
    ]
    ```

    Do NOT touch deps, compilers, aliases, or any unrelated field. Do NOT modify the
    existing "Billing" group.
  </action>
  <verify>
    <automated>mix docs 2>&1 | tee /tmp/mix_docs.log; grep -q "Billing Metering" doc/index.html 2>/dev/null || grep -q "Billing Metering" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs contains literal string `"Billing Metering"` in groups_for_modules
    - mix.exs extras list contains `"guides/metering.md"`
    - mix.exs lists all 8 modules (Meter + 4 nested + MeterEvent + MeterEventAdjustment + Cancel) in the Billing Metering group
    - `mix docs` builds without errors
    - `mix docs` output contains no new warnings beyond baseline
    - Existing "Billing" group in groups_for_modules is untouched
  </acceptance_criteria>
  <done>DOCS-03 — ExDoc group landed, docs build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Reciprocal crosslinks in 5 sibling guides + credo sweep</name>
  <files>
    guides/subscriptions.md,
    guides/webhooks.md,
    guides/telemetry.md,
    guides/error-handling.md,
    guides/testing.md
  </files>
  <read_first>
    - guides/metering.md (just-landed anchor IDs to link to)
    - guides/subscriptions.md (metered-price section to host the reciprocal link)
    - guides/webhooks.md (event handler section)
    - guides/telemetry.md (custom handlers section)
    - guides/error-handling.md (where error-code discussion lives)
    - guides/testing.md (where integration test setup lives)
  </read_first>
  <action>
    Add ONE short reciprocal crosslink to each of the 5 sibling guides, as minimal
    edits. Do NOT refactor existing prose. Each link should be a single sentence or
    inline reference. Target locations:

    1. `guides/subscriptions.md` — in the metered-price section (search for "meter"
       or "metered" or "usage"), add a sentence like:
       > For reporting metered usage events, see [metering.md](metering.md#reporting-usage-the-hot-path).

    2. `guides/webhooks.md` — in the event handler section, add:
       > For the `v1.billing.meter.error_report_triggered` event, see [metering.md](metering.md#reconciliation-via-webhooks).

    3. `guides/telemetry.md` — in the custom handlers section, add:
       > See [metering.md](metering.md#observability) for the hot-path telemetry recipe.

    4. `guides/error-handling.md` — add a sentence referencing
       [metering.md](metering.md#reconciliation-via-webhooks) for async billing error codes.

    5. `guides/testing.md` — add a sentence referencing
       [metering.md](metering.md#what-not-to-do-nightly-batch-flush) for metering-specific integration test guidance.

    Then run `mix credo --strict lib/lattice_stripe/billing/` to confirm the entire
    Phase 20 code surface is credo-clean. Fix any credo findings in-place.
  </action>
  <verify>
    <automated>grep -l "metering.md" guides/subscriptions.md guides/webhooks.md guides/telemetry.md guides/error-handling.md guides/testing.md && mix credo --strict lib/lattice_stripe/billing/</automated>
  </verify>
  <acceptance_criteria>
    - All 5 sibling guides contain the literal substring `metering.md`
    - Each link uses a relative path (no `https://`)
    - Existing content in sibling guides is otherwise unchanged (minimal diff)
    - `mix credo --strict lib/lattice_stripe/billing/` exits 0
    - `mix docs` still builds without warnings
  </acceptance_criteria>
  <done>DOCS-04 reciprocal crosslinks landed; credo sweep clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide readers → copy-paste code | examples become production code downstream (Accrue); correctness of examples matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-doc-01 | Repudiation (doc/code drift) | guides/metering.md code examples | mitigate | Examples pattern-match actual Plan 20-03/04/05 function signatures; line-count guardrails (540-620) prevent under/over-documenting |
| T-20-doc-02 | Information disclosure | "Debugging with Inspect" section | mitigate | Section explicitly warns NEVER log raw payload; points to struct field access as the only safe escape hatch |
| T-20-doc-03 | Tampering (dunning example) | cancel.identifier worked example | mitigate | Example uses the exact nested `%{"cancel" => %{"identifier" => ...}}` shape anchored by Plan 20-05 round-trip test |
</threat_model>

<verification>
- `test -f guides/metering.md && wc -l guides/metering.md` — within 540-620
- `grep -q "error_report_triggered" guides/metering.md`
- `mix docs` — builds clean
- `mix credo --strict lib/lattice_stripe/billing/` — exit 0
- All 5 sibling guides contain `metering.md` link
</verification>

<success_criteria>
- DOCS-01: guides/metering.md exists with all 12 H2 sections
- DOCS-03: mix.exs has "Billing Metering" group + metering.md extras entry
- DOCS-04: 5 sibling guides link to metering.md
- Credo strict clean across all Phase 20 source
- ExDoc builds clean
</success_criteria>

<output>
After completion, create `.planning/phases/20-billing-metering/20-06-SUMMARY.md`.
</output>
