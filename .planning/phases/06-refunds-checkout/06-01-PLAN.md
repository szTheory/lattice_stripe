---
phase: 06-refunds-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/fixtures/customer.ex
  - test/support/fixtures/payment_intent.ex
  - test/support/fixtures/setup_intent.ex
  - test/support/fixtures/payment_method.ex
  - test/support/fixtures/refund.ex
  - test/lattice_stripe/customer_test.exs
  - test/lattice_stripe/payment_intent_test.exs
  - test/lattice_stripe/setup_intent_test.exs
  - test/lattice_stripe/payment_method_test.exs
  - lib/lattice_stripe/refund.ex
  - test/lattice_stripe/refund_test.exs
autonomous: true
requirements:
  - RFND-01
  - RFND-02
  - RFND-03
  - RFND-04

must_haves:
  truths:
    - "Developer can create a full or partial refund for a PaymentIntent"
    - "Developer can retrieve a Refund by ID"
    - "Developer can update a Refund's metadata"
    - "Developer can cancel a pending Refund"
    - "Developer can list Refunds with optional filters and auto-paginate via stream"
    - "Refund.create raises ArgumentError if payment_intent param is missing"
    - "All existing Phase 4/5 tests still pass after fixture extraction"
  artifacts:
    - path: "lib/lattice_stripe/refund.ex"
      provides: "Refund struct + CRUD + cancel + list + stream + bang variants"
      exports: ["create/3", "retrieve/3", "update/4", "cancel/4", "list/3", "stream!/3", "from_map/1"]
    - path: "test/lattice_stripe/refund_test.exs"
      provides: "Refund resource tests"
      min_lines: 200
    - path: "test/support/fixtures/refund.ex"
      provides: "Refund test fixture data"
      contains: "defmodule LatticeStripe.Test.Fixtures.Refund"
    - path: "test/support/fixtures/customer.ex"
      provides: "Extracted Customer fixture data"
      contains: "defmodule LatticeStripe.Test.Fixtures.Customer"
    - path: "test/support/fixtures/payment_intent.ex"
      provides: "Extracted PaymentIntent fixture data"
      contains: "defmodule LatticeStripe.Test.Fixtures.PaymentIntent"
    - path: "test/support/fixtures/setup_intent.ex"
      provides: "Extracted SetupIntent fixture data"
      contains: "defmodule LatticeStripe.Test.Fixtures.SetupIntent"
    - path: "test/support/fixtures/payment_method.ex"
      provides: "Extracted PaymentMethod fixture data"
      contains: "defmodule LatticeStripe.Test.Fixtures.PaymentMethod"
  key_links:
    - from: "lib/lattice_stripe/refund.ex"
      to: "lib/lattice_stripe/resource.ex"
      via: "unwrap_singular/2, unwrap_list/2, unwrap_bang!/1, require_param!/3"
      pattern: "Resource\\.(unwrap_singular|unwrap_list|unwrap_bang!|require_param!)"
    - from: "lib/lattice_stripe/refund.ex"
      to: "lib/lattice_stripe/client.ex"
      via: "Client.request/2"
      pattern: "Client\\.request"
    - from: "test/lattice_stripe/refund_test.exs"
      to: "test/support/fixtures/refund.ex"
      via: "import LatticeStripe.Test.Fixtures.Refund"
      pattern: "import LatticeStripe\\.Test\\.Fixtures\\.Refund"
---

<objective>
Extract test fixtures from Phase 4/5 test files into dedicated fixture modules, then build the Refund resource module with full CRUD, cancel, list, stream, and bang variants.

Purpose: Establishes reusable fixture infrastructure for all resource tests, then adds Refund operations following the proven resource module pattern.
Output: 5 fixture modules (4 extracted + 1 new), Refund module with all operations, comprehensive Refund tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refunds-checkout/06-CONTEXT.md
@.planning/phases/06-refunds-checkout/06-RESEARCH.md

@lib/lattice_stripe/resource.ex
@lib/lattice_stripe/customer.ex
@lib/lattice_stripe/payment_intent.ex
@lib/lattice_stripe/payment_method.ex
@test/support/test_helpers.ex
@test/lattice_stripe/customer_test.exs
@test/lattice_stripe/payment_intent_test.exs
@test/lattice_stripe/setup_intent_test.exs
@test/lattice_stripe/payment_method_test.exs

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/lattice_stripe/resource.ex:
```elixir
def unwrap_singular({:ok, %Response{data: data}}, from_map_fn)
def unwrap_list({:ok, %Response{data: %List{} = list} = resp}, from_map_fn)
def unwrap_bang!({:ok, result})
def require_param!(params, key, message)
```

From lib/lattice_stripe/client.ex:
```elixir
def request(%Client{} = client, %Request{} = request)
def request!(%Client{} = client, %Request{} = request)
```

From lib/lattice_stripe/list.ex:
```elixir
def stream!(%Client{} = client, %Request{} = request)
def stream(%Client{} = client, %Request{} = request)
```

From test/support/test_helpers.ex:
```elixir
def test_client(overrides \\ [])
def ok_response(body)
def error_response()
def list_json(items, has_more \\ false)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Phase 4/5 test fixtures into dedicated fixture modules</name>
  <files>
    test/support/fixtures/customer.ex,
    test/support/fixtures/payment_intent.ex,
    test/support/fixtures/setup_intent.ex,
    test/support/fixtures/payment_method.ex,
    test/lattice_stripe/customer_test.exs,
    test/lattice_stripe/payment_intent_test.exs,
    test/lattice_stripe/setup_intent_test.exs,
    test/lattice_stripe/payment_method_test.exs
  </files>
  <read_first>
    test/lattice_stripe/customer_test.exs,
    test/lattice_stripe/payment_intent_test.exs,
    test/lattice_stripe/setup_intent_test.exs,
    test/lattice_stripe/payment_method_test.exs,
    test/support/test_helpers.ex,
    mix.exs
  </read_first>
  <action>
Create `test/support/fixtures/` directory, then create 4 fixture modules by extracting the existing inline `defp *_json/1` functions from test files:

**1. `test/support/fixtures/customer.ex`** — `defmodule LatticeStripe.Test.Fixtures.Customer`:
- Extract the `defp customer_json/1` from `customer_test.exs`
- Make it public: `def customer_json(overrides \\ %{})`
- Enhance with realistic-looking Stripe ID: `"cus_test1234567890"` (not `"cus_test123"`)
- Keep all existing fields and Map.merge pattern
- Add `@moduledoc false`

**2. `test/support/fixtures/payment_intent.ex`** — `defmodule LatticeStripe.Test.Fixtures.PaymentIntent`:
- Extract `defp payment_intent_json/1` from `payment_intent_test.exs`
- Make public: `def payment_intent_json(overrides \\ %{})`
- Enhance ID: `"pi_test1234567890abc"`
- Keep `client_secret` field — tests need it
- Add `@moduledoc false`

**3. `test/support/fixtures/setup_intent.ex`** — `defmodule LatticeStripe.Test.Fixtures.SetupIntent`:
- Extract `defp setup_intent_json/1` from `setup_intent_test.exs`
- Make public: `def setup_intent_json(overrides \\ %{})`
- Enhance ID: `"seti_test1234567890abc"`
- Add `@moduledoc false`

**4. `test/support/fixtures/payment_method.ex`** — `defmodule LatticeStripe.Test.Fixtures.PaymentMethod`:
- Extract `defp payment_method_json/1` from `payment_method_test.exs`
- Make public: `def payment_method_json(overrides \\ %{})`
- Enhance ID: `"pm_test1234567890abc"`
- Keep ALL nested card fields
- Add `@moduledoc false`

**Then update the 4 test files:**
- Remove the `defp *_json/1` private function from each test file
- Add the corresponding import: e.g., `import LatticeStripe.Test.Fixtures.Customer` in customer_test.exs
- Place imports after existing imports (after `import LatticeStripe.TestHelpers`)
- Remove the `# Test helpers` section header/divider comments if they only contained the fixture function

Per D-42, D-43, D-44, D-45, D-46. Per D-47, `ok_response/1`, `error_response/0`, `list_json/2` stay in test_helpers.ex. Per D-50, no mix.exs changes needed — `elixirc_paths(:test)` already covers `test/support/` recursively.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/fixtures/customer.ex` contains `defmodule LatticeStripe.Test.Fixtures.Customer`
    - `test/support/fixtures/customer.ex` contains `def customer_json(overrides \\ %{})`
    - `test/support/fixtures/payment_intent.ex` contains `defmodule LatticeStripe.Test.Fixtures.PaymentIntent`
    - `test/support/fixtures/payment_intent.ex` contains `def payment_intent_json(overrides \\ %{})`
    - `test/support/fixtures/setup_intent.ex` contains `defmodule LatticeStripe.Test.Fixtures.SetupIntent`
    - `test/support/fixtures/setup_intent.ex` contains `def setup_intent_json(overrides \\ %{})`
    - `test/support/fixtures/payment_method.ex` contains `defmodule LatticeStripe.Test.Fixtures.PaymentMethod`
    - `test/support/fixtures/payment_method.ex` contains `def payment_method_json(overrides \\ %{})`
    - `test/lattice_stripe/customer_test.exs` contains `import LatticeStripe.Test.Fixtures.Customer`
    - `test/lattice_stripe/customer_test.exs` does NOT contain `defp customer_json`
    - `test/lattice_stripe/payment_intent_test.exs` contains `import LatticeStripe.Test.Fixtures.PaymentIntent`
    - `test/lattice_stripe/payment_intent_test.exs` does NOT contain `defp payment_intent_json`
    - `test/lattice_stripe/setup_intent_test.exs` contains `import LatticeStripe.Test.Fixtures.SetupIntent`
    - `test/lattice_stripe/setup_intent_test.exs` does NOT contain `defp setup_intent_json`
    - `test/lattice_stripe/payment_method_test.exs` contains `import LatticeStripe.Test.Fixtures.PaymentMethod`
    - `test/lattice_stripe/payment_method_test.exs` does NOT contain `defp payment_method_json`
    - `mix test` exits 0 with all existing tests passing
  </acceptance_criteria>
  <done>All 4 fixture modules exist in test/support/fixtures/, all 4 test files import them instead of defining private json builders, full test suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Refund resource module with full operations and tests</name>
  <files>
    lib/lattice_stripe/refund.ex,
    test/support/fixtures/refund.ex,
    test/lattice_stripe/refund_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/customer.ex,
    lib/lattice_stripe/payment_intent.ex,
    lib/lattice_stripe/payment_method.ex,
    lib/lattice_stripe/resource.ex,
    lib/lattice_stripe/list.ex,
    test/lattice_stripe/payment_intent_test.exs,
    test/support/test_helpers.ex,
    test/support/fixtures/customer.ex
  </read_first>
  <behavior>
    - create/3 sends POST /v1/refunds with params and returns {:ok, %Refund{}}
    - create/3 raises ArgumentError when params lack "payment_intent" key (pre-network validation per D-02)
    - create/3 passes "amount" for partial refund
    - retrieve/3 sends GET /v1/refunds/:id and returns {:ok, %Refund{}}
    - update/4 sends POST /v1/refunds/:id with params and returns {:ok, %Refund{}}
    - cancel/4 sends POST /v1/refunds/:id/cancel and returns {:ok, %Refund{}}
    - list/3 sends GET /v1/refunds and returns {:ok, %Response{data: %List{data: [%Refund{}]}}}
    - list/3 works with no params (all optional, per D-04)
    - stream!/3 returns a Stream that yields %Refund{} structs
    - All bang variants (create!/3, retrieve!/3, update!/4, cancel!/4, list!/3) raise on error
    - from_map/1 populates all known fields and puts unknown fields in extra map
    - Inspect shows only id, object, amount, currency, status (per D-28)
    - Error responses return {:error, %Error{}}
  </behavior>
  <action>
**1. Create fixture module `test/support/fixtures/refund.ex`:**

```elixir
defmodule LatticeStripe.Test.Fixtures.Refund do
  @moduledoc false

  def refund_json(overrides \\ %{}) do
    Map.merge(
      %{
        "id" => "re_test1234567890abc",
        "object" => "refund",
        "amount" => 2000,
        "currency" => "usd",
        "status" => "succeeded",
        "payment_intent" => "pi_test1234567890abc",
        "charge" => "ch_test1234567890abc",
        "reason" => "requested_by_customer",
        "created" => 1_700_000_000,
        "metadata" => %{},
        "balance_transaction" => "txn_test123",
        "receipt_number" => nil,
        "failure_reason" => nil,
        "failure_balance_transaction" => nil,
        "destination_details" => nil,
        "source_transfer_reversal" => nil,
        "transfer_reversal" => nil
      },
      overrides
    )
  end

  def refund_partial_json(overrides \\ %{}) do
    Map.merge(refund_json(%{"amount" => 500}), overrides)
  end

  def refund_pending_json(overrides \\ %{}) do
    Map.merge(refund_json(%{"status" => "pending"}), overrides)
  end
end
```

**2. Create test file `test/lattice_stripe/refund_test.exs`:**

Test structure following PaymentIntent test patterns. Use `import LatticeStripe.Test.Fixtures.Refund`. Describe blocks for:
- `create/3` — success, partial amount, ArgumentError when missing payment_intent, error response
- `retrieve/3` — success, error response
- `update/4` — success with metadata
- `cancel/4` — success for pending refund
- `list/3` — success (no params), success with filters
- `stream!/3` — returns Stream of %Refund{} structs
- Bang variants — `create!/3` success, `create!/3` raises on error
- `from_map/1` — populates fields, extra map for unknown keys
- Inspect — shows only 5 fields (id, object, amount, currency, status)

Each test mocks `LatticeStripe.MockTransport` via Mox `expect`, asserts HTTP method and URL path, returns `ok_response(refund_json())` or similar.

**3. Create `lib/lattice_stripe/refund.ex`:**

`defmodule LatticeStripe.Refund` — follows exact pattern from Customer/PaymentIntent/PaymentMethod.

- `alias LatticeStripe.{Client, Error, List, Request, Resource, Response}`
- `@known_fields ~w[id object amount currency status reason payment_intent charge destination_details metadata created failure_reason failure_balance_transaction balance_transaction receipt_number source_transfer_reversal transfer_reversal]`
- `defstruct` with all known fields as atoms, `object: "refund"`, `extra: %{}`
- `@type t :: %__MODULE__{...}` typespec
- `from_map/1` — standard pattern: `%__MODULE__{id: map["id"], ..., extra: Map.drop(map, @known_fields)}`

Functions per decisions:
- `create/3` (D-01, D-02, D-11): `require_param!(params, "payment_intent", ...)` before building Request. POST /v1/refunds.
- `retrieve/3`: GET /v1/refunds/:id. Standard opts.
- `update/4`: POST /v1/refunds/:id. (D-10: @doc notes metadata-only updates.)
- `cancel/4` (D-03): POST /v1/refunds/:id/cancel. Same action verb pattern as PaymentIntent.cancel.
- `list/3` (D-04): GET /v1/refunds. No required params. Uses `unwrap_list`.
- `stream!/3` (D-06): GET /v1/refunds, wraps `List.stream!(client, req) |> Stream.map(&from_map/1)`.
- Bang variants for all tuple-returning functions (D-37): `create!/3`, `retrieve!/3`, `update!/4`, `cancel!/4`, `list!/3`.
- NO `delete` function (D-09). @moduledoc notes: "Refunds cannot be deleted. Use `cancel/4` to cancel a pending refund."
- NO search endpoint (D-07).

@moduledoc: Brief description, key behaviors (payment_intent required, no delete, metadata-only updates), usage examples with realistic params (D-53), Stripe API reference link (D-52).

Inspect implementation (D-28): `defimpl Inspect, for: LatticeStripe.Refund` showing `id`, `object`, `amount`, `currency`, `status`. Uses `Inspect.Algebra` `concat`/`to_doc` pattern from PaymentIntent.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix test test/lattice_stripe/refund_test.exs && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/refund.ex` contains `defmodule LatticeStripe.Refund`
    - `lib/lattice_stripe/refund.ex` contains `def create(%Client{} = client, params, opts \\ [])`
    - `lib/lattice_stripe/refund.ex` contains `Resource.require_param!(params, "payment_intent"`
    - `lib/lattice_stripe/refund.ex` contains `def retrieve(%Client{} = client, id, opts \\ [])`
    - `lib/lattice_stripe/refund.ex` contains `def update(%Client{} = client, id, params, opts \\ [])`
    - `lib/lattice_stripe/refund.ex` contains `def cancel(%Client{} = client, id, params \\ %{}, opts \\ [])`
    - `lib/lattice_stripe/refund.ex` contains `def list(%Client{} = client, params \\ %{}, opts \\ [])`
    - `lib/lattice_stripe/refund.ex` contains `def stream!(%Client{} = client, params \\ %{}, opts \\ [])`
    - `lib/lattice_stripe/refund.ex` contains `def from_map(map)`
    - `lib/lattice_stripe/refund.ex` contains `defimpl Inspect, for: LatticeStripe.Refund`
    - `lib/lattice_stripe/refund.ex` does NOT contain `def delete`
    - `lib/lattice_stripe/refund.ex` does NOT contain `def search`
    - `lib/lattice_stripe/refund.ex` contains `object: "refund"`
    - `test/lattice_stripe/refund_test.exs` contains `import LatticeStripe.Test.Fixtures.Refund`
    - `test/lattice_stripe/refund_test.exs` contains `assert_raise ArgumentError`
    - `mix test test/lattice_stripe/refund_test.exs` exits 0
    - `mix test` exits 0
  </acceptance_criteria>
  <done>Refund module implements create (with payment_intent validation), retrieve, update, cancel, list, stream, all bang variants. No delete, no search. Tests cover all operations including ArgumentError for missing payment_intent. Full suite green.</done>
</task>

</tasks>

<verification>
1. `mix test` — all tests pass (existing + new Refund tests)
2. `mix compile --warnings-as-errors` — no warnings
3. `grep -r "defp customer_json" test/` — returns nothing (all extracted)
4. `grep -r "defp payment_intent_json" test/` — returns nothing
5. `grep -r "defp setup_intent_json" test/` — returns nothing
6. `grep -r "defp payment_method_json" test/` — returns nothing
7. `grep -r "def refund_json" test/support/fixtures/refund.ex` — exists
</verification>

<success_criteria>
- All 5 fixture modules exist in test/support/fixtures/
- All 4 existing test files use imports instead of private fixture functions
- Refund module provides create/retrieve/update/cancel/list/stream + bang variants
- Refund.create validates payment_intent presence before network call
- No delete or search functions on Refund
- Full test suite passes with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-refunds-checkout/06-01-SUMMARY.md`
</output>
