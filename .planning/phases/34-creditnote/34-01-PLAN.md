---
phase: 34-creditnote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/credit_note.ex
  - lib/lattice_stripe/credit_note/line_item.ex
  - lib/lattice_stripe/object_types.ex
  - mix.exs
  - test/lattice_stripe/object_types_test.exs
  - test/support/fixtures/credit_note.ex
autonomous: true
requirements:
  - CRDN-06

must_haves:
  truths:
    - "Credit note responses can deserialize into a typed %LatticeStripe.CreditNote{} with embedded %LatticeStripe.List{data: [%CreditNote.LineItem{}, ...]}"
    - "Credit note line item responses deserialize into typed %LatticeStripe.CreditNote.LineItem{} structs while preserving both Stripe subtype strings"
    - "ObjectTypes can dispatch both \"credit_note\" and \"credit_note_line_item\" payloads to typed structs"
    - "Fixtures exist for both invoice_line_item and custom_line_item credit note payloads plus integration setup helpers for finalized/open invoice scenarios"
    - "CreditNote modules appear in the Billing ExDoc group beside Invoice"
  artifacts:
    - path: "lib/lattice_stripe/credit_note.ex"
      provides: "CreditNote struct contract and from_map/1 parser"
      exports: ["from_map/1"]
    - path: "lib/lattice_stripe/credit_note/line_item.ex"
      provides: "CreditNote.LineItem struct contract and from_map/1 parser"
      exports: ["from_map/1"]
    - path: "lib/lattice_stripe/object_types.ex"
      provides: "credit_note and credit_note_line_item dispatch entries"
      contains: "\"credit_note\""
    - path: "test/support/fixtures/credit_note.ex"
      provides: "credit note fixtures and integration setup helpers"
      contains: "credit_note_json"
  key_links:
    - from: "lib/lattice_stripe/credit_note.ex"
      to: "lib/lattice_stripe/credit_note/line_item.ex"
      via: "parse_lines/1 and embedded list mapping"
      pattern: "LineItem\\.from_map"
    - from: "lib/lattice_stripe/credit_note.ex"
      to: "lib/lattice_stripe/object_types.ex"
      via: "expandable customer/invoice/customer_balance_transaction deserialization"
      pattern: "ObjectTypes\\.maybe_deserialize"
    - from: "test/lattice_stripe/object_types_test.exs"
      to: "lib/lattice_stripe/object_types.ex"
      via: "dispatch assertions for credit_note and credit_note_line_item"
      pattern: "credit_note"
---

<objective>
Create the typed CreditNote and CreditNote.LineItem data contracts, register both object types, add Billing ExDoc placement, and add reusable fixtures/helpers for unit and integration coverage.

Purpose: This plan establishes the parser and fixture contracts that all resource APIs and tests rely on, while locking in the minimal typing depth from the phase decisions.
Output: `credit_note.ex` parser contract, `credit_note/line_item.ex`, ObjectTypes and ExDoc updates, and `test/support/fixtures/credit_note.ex`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-creditnote/34-CONTEXT.md
@.planning/phases/34-creditnote/34-RESEARCH.md
@.planning/phases/34-creditnote/34-PATTERNS.md
@.planning/phases/34-creditnote/34-VALIDATION.md
@.planning/threads/lattice-stripe-vs-accrue-scope-boundary.md

<interfaces>
From `lib/lattice_stripe/invoice.ex`:
```elixir
defp parse_lines(nil), do: nil

defp parse_lines(%{"object" => "list", "data" => data} = list) when is_list(data) do
  %{LatticeStripe.List.from_map(list) | data: Enum.map(data, &LineItem.from_map/1)}
end
```

From `lib/lattice_stripe/invoice/line_item.ex`:
```elixir
@known_fields ~w[... type ...]
defstruct [..., :type, object: "line_item", extra: %{}]

@spec from_map(map() | nil) :: t() | nil
def from_map(nil), do: nil
```

From `lib/lattice_stripe/object_types.ex`:
```elixir
@spec maybe_deserialize(map() | String.t() | nil) :: struct() | map() | String.t() | nil
def maybe_deserialize(%{"object" => object_type} = map) do
  case Map.fetch(@object_map, object_type) do
    {:ok, module} -> module.from_map(map)
    :error -> map
  end
end
```

From `mix.exs`:
```elixir
Billing: [
  LatticeStripe.Invoice,
  LatticeStripe.Invoice.LineItem,
  ...
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CreditNote line-item contract and reusable credit-note fixtures</name>
  <files>
    lib/lattice_stripe/credit_note/line_item.ex
    test/support/fixtures/credit_note.ex
  </files>
  <read_first>
    lib/lattice_stripe/credit_note/line_item.ex
    test/support/fixtures/credit_note.ex
    lib/lattice_stripe/invoice/line_item.ex
    test/support/fixtures/dispute.ex
    .planning/phases/34-creditnote/34-CONTEXT.md
    .planning/phases/34-creditnote/34-PATTERNS.md
  </read_first>
  <behavior>
    - `CreditNote.LineItem.from_map/1` returns `nil` for `nil` and preserves unknown Stripe keys in `extra`.
    - `CreditNote.LineItem.type` stays a raw string for both `"invoice_line_item"` and `"custom_line_item"` cases per D-14 and D-15.
    - `credit_note_json/1` embeds `%{"lines" => %{"object" => "list", "data" => [...]}}` with helpers for both line-item subtypes.
    - Integration helpers can create a finalized invoice for create/preview tests and an open-invoice credit note fixture for void tests per D-16 and D-17.
  </behavior>
  <action>
Create `lib/lattice_stripe/credit_note/line_item.ex` as `LatticeStripe.CreditNote.LineItem`, following the structure of `Invoice.LineItem` but using CreditNote-specific fields from the phase research. Implement D-09, D-10, D-14, and D-15 directly:

- `@known_fields ~w[id object amount description discount_amount discount_amounts invoice_line_item livemode pretax_credit_amounts quantity tax_rates taxes type unit_amount unit_amount_decimal]`
- `defstruct` fields in atom form with `object: "credit_note_line_item"` and `extra: %{}`
- `@type t` where `type` is `String.t() | nil`, aggregate/tax branches remain `list() | map() | nil`, and no nested structs are introduced for `discount_amounts`, `pretax_credit_amounts`, `tax_rates`, or `taxes`
- `from_map/1` uses `Map.split/2` and explicitly assigns each known field; do not atomize `type`
- Add a compact `Inspect` implementation only if it mirrors `Invoice.LineItem` exactly; otherwise leave default inspect unchanged

Create `test/support/fixtures/credit_note.ex` as `LatticeStripe.Test.Fixtures.CreditNote` with mergeable helpers:

- `credit_note_json/1` base payload keyed as `"object" => "credit_note"` with embedded `"lines"` list data, top-level enums (`"status" => "issued"`, `"reason" => "duplicate"`, `"type" => "pre_payment"`), expandable `"customer"`, `"invoice"`, and `"customer_balance_transaction"` IDs, and aggregate map/list branches left raw per D-10
- `credit_note_line_item_json/1` defaulting to the `"invoice_line_item"` subtype
- `custom_credit_note_line_item_json/1` defaulting to the `"custom_line_item"` subtype
- `create_creditable_invoice!/2` helper that creates a customer, creates a draft invoice with `auto_advance: false` and `collection_method: "send_invoice"`, then finalizes it via `LatticeStripe.Invoice.finalize/3`; return the finalized invoice per D-16
- `create_open_invoice_credit_note!/2` helper that calls `create_creditable_invoice!/2` and then `LatticeStripe.CreditNote.create/3` with a minimal line payload so later tests can exercise `void/3` against an open invoice per D-17

Do not add builders, validations, or local enums. Keep params and fixture payloads Stripe-shaped per D-05.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/credit_note/line_item.ex` contains `defmodule LatticeStripe.CreditNote.LineItem do`
    - `lib/lattice_stripe/credit_note/line_item.ex` contains `type: String.t() | nil` in the typespec
    - `lib/lattice_stripe/credit_note/line_item.ex` contains `object: "credit_note_line_item"`
    - `lib/lattice_stripe/credit_note/line_item.ex` contains `type: known["type"]`
    - `test/support/fixtures/credit_note.ex` contains `def credit_note_json(overrides \\\\ %{}) do`
    - `test/support/fixtures/credit_note.ex` contains `def credit_note_line_item_json(overrides \\\\ %{}) do`
    - `test/support/fixtures/credit_note.ex` contains `def custom_credit_note_line_item_json(overrides \\\\ %{}) do`
    - `test/support/fixtures/credit_note.ex` contains `def create_creditable_invoice!(client, attrs \\\\ %{}) do`
    - `test/support/fixtures/credit_note.ex` contains `Invoice.finalize(client, invoice.id)`
    - `test/support/fixtures/credit_note.ex` contains `def create_open_invoice_credit_note!(client, attrs \\\\ %{}) do`
  </acceptance_criteria>
  <done>Line-item parsing and reusable fixture helpers exist with both Stripe subtype variants and invoice lifecycle setup helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CreditNote parser contract, object registration, and Billing docs grouping</name>
  <files>
    lib/lattice_stripe/credit_note.ex
    lib/lattice_stripe/object_types.ex
    mix.exs
    test/lattice_stripe/object_types_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/credit_note.ex
    lib/lattice_stripe/object_types.ex
    mix.exs
    test/lattice_stripe/object_types_test.exs
    lib/lattice_stripe/invoice.ex
    lib/lattice_stripe/object_types.ex
    .planning/phases/34-creditnote/34-CONTEXT.md
    .planning/phases/34-creditnote/34-PATTERNS.md
  </read_first>
  <behavior>
    - `CreditNote.from_map/1` parses top-level enums to atoms with string passthrough and maps embedded `"lines"` into `%LatticeStripe.List{data: [%CreditNote.LineItem{}, ...]}`.
    - Expandable `customer`, `invoice`, and `customer_balance_transaction` values deserialize through `ObjectTypes.maybe_deserialize/1` only when Stripe returned a map.
    - `ObjectTypes.maybe_deserialize/1` recognizes both `"credit_note"` and `"credit_note_line_item"` objects.
    - ExDoc Billing grouping includes `LatticeStripe.CreditNote` and `LatticeStripe.CreditNote.LineItem`.
  </behavior>
  <action>
Create the parser-focused baseline for `lib/lattice_stripe/credit_note.ex` without the public CRUDL API yet. This task implements D-09 through D-13 and D-11/D-12:

- Define `LatticeStripe.CreditNote` with aliases `alias LatticeStripe.{BalanceTransaction, List, ObjectTypes}` and `alias LatticeStripe.CreditNote.LineItem`
- Add `@known_fields ~w[id object amount amount_shipping created currency customer customer_balance_transaction effective_at invoice lines livemode memo metadata number out_of_band_amount pdf pre_payment_amount post_payment_amount reason refunds shipping_cost status subtotal subtotal_excluding_tax total total_excluding_tax total_taxes type voided_at]`
- Build a `defstruct` with those atoms, `object: "credit_note"`, and `extra: %{}`
- Add `@type t` where:
  - `status`, `reason`, and `type` are `atom() | String.t() | nil`
  - `customer` is `LatticeStripe.Customer.t() | String.t() | nil`
  - `invoice` is `LatticeStripe.Invoice.t() | String.t() | nil`
  - `customer_balance_transaction` is `BalanceTransaction.t() | String.t() | nil`
  - `lines` is `LatticeStripe.List.t() | nil`
  - `refunds`, `shipping_cost`, and `total_taxes` remain raw list/map shapes per D-10
- Implement `from_map(nil)` and `from_map(map)` with explicit field assignment, `parse_expandable/1`, `parse_lines/1`, `atomize_status/1`, `atomize_reason/1`, and `atomize_type/1`; unknown enum values must pass through unchanged
- `parse_lines/1` must use `LatticeStripe.List.from_map/1` then replace `data` with `Enum.map(data, &LineItem.from_map/1)`

Update `lib/lattice_stripe/object_types.ex` by adding:

- `"credit_note" => LatticeStripe.CreditNote`
- `"credit_note_line_item" => LatticeStripe.CreditNote.LineItem`

Keep the registry aligned with the existing style and do not change dispatch behavior.

Update `mix.exs` Billing group by inserting:

- `LatticeStripe.CreditNote`
- `LatticeStripe.CreditNote.LineItem`

Place them next to the Invoice modules, not under Payments and not in a new ExDoc group.

Extend `test/lattice_stripe/object_types_test.exs` with assertions that:

- `%{"object" => "credit_note"}` dispatches to `%LatticeStripe.CreditNote{}`
- `%{"object" => "credit_note_line_item"}` dispatches to `%LatticeStripe.CreditNote.LineItem{}`

Do not add public CRUD functions in this task; Plan 02 owns the request surface.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/credit_note.ex` contains `defmodule LatticeStripe.CreditNote do`
    - `lib/lattice_stripe/credit_note.ex` contains `def from_map(nil), do: nil`
    - `lib/lattice_stripe/credit_note.ex` contains `defp parse_lines(%{"object" => "list", "data" => data} = list)`
    - `lib/lattice_stripe/credit_note.ex` contains `defp atomize_status("issued"), do: :issued`
    - `lib/lattice_stripe/credit_note.ex` contains `defp atomize_status("void"), do: :void`
    - `lib/lattice_stripe/credit_note.ex` contains `defp atomize_reason("product_unsatisfactory"), do: :product_unsatisfactory`
    - `lib/lattice_stripe/credit_note.ex` contains `defp atomize_type("mixed"), do: :mixed`
    - `lib/lattice_stripe/object_types.ex` contains `"credit_note"                  => LatticeStripe.CreditNote`
    - `lib/lattice_stripe/object_types.ex` contains `"credit_note_line_item"        => LatticeStripe.CreditNote.LineItem`
    - `mix.exs` contains `LatticeStripe.CreditNote,`
    - `mix.exs` contains `LatticeStripe.CreditNote.LineItem,`
    - `test/lattice_stripe/object_types_test.exs` contains `dispatches credit_note map`
    - `test/lattice_stripe/object_types_test.exs` contains `dispatches credit_note_line_item map`
  </acceptance_criteria>
  <done>Typed CreditNote parser contracts compile, object dispatch is registered, and Billing docs grouping includes the new modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe API JSON -> `from_map/1` | Untrusted remote payloads become typed SDK structs |
| Test helpers -> integration Stripe state | Fixture helpers create real invoice/credit-note objects and can encode invalid lifecycle assumptions if they drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | T | `lib/lattice_stripe/credit_note.ex` and `lib/lattice_stripe/credit_note/line_item.ex` | mitigate | Use fixed `@known_fields`, explicit field assignment, and `extra` preservation so unknown Stripe keys never create atoms or crash typed parsing. |
| T-34-02 | D | `test/support/fixtures/credit_note.ex` lifecycle helpers | mitigate | Encode finalized-invoice creation and open-invoice void preconditions directly in helper functions per D-16 and D-17 so later tests cannot silently target the wrong lifecycle states. |
| T-34-03 | I | default `Inspect` output for `CreditNote` | accept | No custom inspect in this plan unless execution finds a concrete sensitive-field leak; existing invoice-like hosted URLs already use default inspect in the repo. |
</threat_model>

<verification>
- `mix test test/lattice_stripe/object_types_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `LatticeStripe.CreditNote.from_map/1` and `LatticeStripe.CreditNote.LineItem.from_map/1` exist and preserve unknown keys in `extra`
- Embedded `lines` parse into `%LatticeStripe.List{data: [%CreditNote.LineItem{}, ...]}`
- `ObjectTypes.maybe_deserialize/1` recognizes `credit_note` and `credit_note_line_item`
- Fixture helpers cover both line-item subtype variants and encode finalized/open invoice setup rules
</success_criteria>

<output>
After completion, create `.planning/phases/34-creditnote/34-01-SUMMARY.md`
</output>
