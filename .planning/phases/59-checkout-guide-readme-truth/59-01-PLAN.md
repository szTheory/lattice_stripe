---
phase: 59-checkout-guide-readme-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/checkout.md
autonomous: true
requirements:
  - CHECKOUT-01
  - CHECKOUT-02
must_haves:
  truths:
    - "Checkout Session stream filter examples use atom `payment_status` and `status` compares, not wire strings."
    - "guides/checkout.md includes a status-values callout explaining atomized enums on `%LatticeStripe.Checkout.Session{}` before the first status comparison."
    - "Callout teaches fulfillment-safe filtering: `status == :complete` does not imply paid."
  artifacts:
    - path: "guides/checkout.md"
      provides: "canonical checkout guide with atom-correct stream filter and status callout"
      contains: "Status values:"
    - path: "guides/checkout.md"
      provides: "fulfillment-safe stream example"
      contains: "payment_status == :paid and s.status == :complete"
  key_links:
    - from: "guides/checkout.md"
      to: "guides/payments.md"
      via: "callout shape mirrors PaymentIntent status-values blockquote"
      pattern: "Status values:"
    - from: "guides/checkout.md"
      to: "lib/lattice_stripe/checkout/session.ex"
      via: "documented atoms match atomize_status/1 and atomize_payment_status/1"
      pattern: ":paid"
---

<objective>
Fix `guides/checkout.md` copy-paste bug and add status-values callout so Checkout Session examples are atom-correct and fulfillment-safe.

Purpose: CHECKOUT-01 and CHECKOUT-02 — adopters copying stream filters must get working atom compares and understand struct-vs-wire split.
Output: Updated `guides/checkout.md` with callout before Auto-Pagination stream block and corrected filter example.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/59-checkout-guide-readme-truth/59-CONTEXT.md
@.planning/phases/59-checkout-guide-readme-truth/59-RESEARCH.md
@.planning/phases/59-checkout-guide-readme-truth/59-VALIDATION.md
@guides/payments.md
@guides/checkout.md
@lib/lattice_stripe/checkout/session.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status-values callout before Auto-Pagination stream block</name>
  <files>guides/checkout.md</files>
  <read_first>
    guides/checkout.md
    guides/payments.md
    lib/lattice_stripe/checkout/session.ex
    .planning/phases/59-checkout-guide-readme-truth/59-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `guides/checkout.md` contains `> **Status values:**` blockquote immediately before the `### Auto-Pagination with Streams` code block under `## Listing Sessions` (after the section intro paragraph, before the opening ` ```elixir ` of the stream example).
    - Callout mentions both `%LatticeStripe.Checkout.Session{}` and atom examples including `:paid`, `:complete`, and `:open`.
    - Callout explains wire strings appear in Stripe API reference, Dashboard, list filters, and webhook raw maps (`event.data["object"]`).
    - Callout includes fulfillment note: verify `payment_status == :paid` before fulfilling; `status == :complete` alone is insufficient.
    - Callout includes wire-string bullet lists for session lifecycle (`open`, `complete`, `expired`) and payment status (`paid`, `unpaid`, `no_payment_required`) — no wire→atom mapping tables.
    - Webhook handler examples elsewhere in checkout.md that use `event.data["object"]` string keys remain unchanged.
  </acceptance_criteria>
  <action>
Insert a blockquote callout immediately before the stream pipeline code block in `### Auto-Pagination with Streams` (currently lines 199–208). Mirror `guides/payments.md` lines 110–118 structure but for Checkout Session fields:

Opening line:
`> **Status values:** LatticeStripe atomizes known Checkout Session \`status\` and \`payment_status\` on \`%LatticeStripe.Checkout.Session{}\` (e.g. \`:paid\`, \`:complete\`, \`:open\`). Stripe's API reference, Dashboard, list filters, and webhook raw maps (\`event.data["object"]\`) use the wire string names below.`

Then bullet lists for session lifecycle and payment status with wire-string names in backticks, plus a fulfillment paragraph stating that `status == :complete` does not imply paid — check `payment_status` before fulfilling, especially for async payment methods.

Do NOT add wire→atom mapping tables. Do NOT duplicate this callout at the Retrieving Sessions section (optional one-liner cross-link is Task 2 discretion).
  </action>
  <verify>
    <automated>rg -n 'Status values:|%LatticeStripe\.Checkout\.Session|:paid|:complete|payment_status == :paid' guides/checkout.md</automated>
  </verify>
  <done>Status-values callout is present at the first comparison site with struct-vs-wire and fulfillment guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stream filter to use atom compares with fulfillment-safe filter</name>
  <files>guides/checkout.md</files>
  <read_first>
    guides/checkout.md
    guides/payments.md
    lib/lattice_stripe/checkout/session.ex
  </read_first>
  <acceptance_criteria>
    - `guides/checkout.md` stream filter line reads `s.payment_status == :paid and s.status == :complete` (not `"paid"`).
    - `guides/checkout.md` does NOT contain `payment_status == "paid"` anywhere.
    - `# Status: expired` output comment at line ~219 remains unchanged (wire-string output comments deferred).
    - Optional: one-line cross-link at end of `## Retrieving Sessions` section pointing readers to `### Auto-Pagination with Streams` for status filter examples — omit if redundant.
  </acceptance_criteria>
  <action>
In the Auto-Pagination stream example, replace:
`|> Stream.filter(fn s -> s.payment_status == "paid" end)`
with:
`|> Stream.filter(fn s -> s.payment_status == :paid and s.status == :complete end)`

This teaches fulfillment-safe filtering per CONTEXT D-06. Leave expire-section `# Status: expired` output comment unchanged.

Optionally add at end of Retrieving Sessions section (after the Expanding Fields subsection or before Listing Sessions):
`For filtering sessions by payment or lifecycle status, see [Auto-Pagination with Streams](#auto-pagination-with-streams) below.`
  </action>
  <verify>
    <automated>rg -n 'payment_status == :paid and s.status == :complete' guides/checkout.md && ! rg -n 'payment_status == "paid"' guides/checkout.md</automated>
  </verify>
  <done>Stream filter example is copy-paste correct with fulfillment-safe atom compares.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide examples -> runtime behavior | String status compares on atomized structs silently fail — adopters get empty streams |
| lifecycle complete -> payment success | Treating `status == :complete` as paid causes premature fulfillment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | T | checkout.md stream filter | mitigate | Atom compares + fulfillment-safe `payment_status == :paid and status == :complete` filter |
| T-59-02 | I | checkout.md status callout | mitigate | Explicit struct-vs-wire split; scope atoms to `%Session{}` contexts |
</threat_model>

<verification>
- `rg -n 'Status values:' guides/checkout.md`
- `rg -n 'payment_status == :paid and s.status == :complete' guides/checkout.md`
- `! rg -n 'payment_status == "paid"' guides/checkout.md`
</verification>

<success_criteria>
- CHECKOUT-01: Stream filter uses `:paid` atom, not `"paid"` string.
- CHECKOUT-02: Status-values callout documents atomized `payment_status` and `status` on `%Session{}`.
</success_criteria>

<output>
After completion, create `.planning/phases/59-checkout-guide-readme-truth/59-01-SUMMARY.md`
</output>
