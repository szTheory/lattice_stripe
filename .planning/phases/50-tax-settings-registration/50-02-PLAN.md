---
phase: 50-tax-settings-registration
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - lib/lattice_stripe/tax/settings.ex
  - lib/lattice_stripe/tax/registration.ex
  - lib/lattice_stripe/object_types.ex
  - test/support/fixtures/tax_registration.ex
  - test/lattice_stripe/tax/registration_test.exs
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements: [CONF-03, CONF-04]
must_haves:
  truths:
    - "Adopters call `Tax.Registration.create/3` with nested `country_options` (ROADMAP SC#3, CONF-04)."
    - "Adopters call `retrieve/3`, `update/4`, `list/3`, and `stream!/3` for registrations (ROADMAP SC#4)."
    - "Moduledoc states Registration.create does not register with tax authorities (ROADMAP SC#6)."
    - "`tax.registration` dispatches through ObjectTypes (ROADMAP SC#5 complete)."
  artifacts:
    - path: lib/lattice_stripe/tax/registration.ex
      provides: Tax.Registration CRUDL module
      contains: def stream!(
    - path: lib/lattice_stripe/tax/settings.ex
      provides: full Settings moduledoc
      contains: singleton
    - path: test/lattice_stripe/tax/registration_test.exs
      provides: Registration wire contract tests
      contains: country_options
---

<objective>
Ship `LatticeStripe.Tax.Registration` CRUDL + `stream!/3`, full moduledocs for Settings and Registration (D-03 grep targets), `tax.registration` ObjectTypes, fixtures, and unit tests for CONF-03 and CONF-04.

Depends on Plan 01 (`Tax.Settings` module exists for moduledoc cross-links).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-tax-settings-registration/50-CONTEXT.md
@.planning/phases/50-tax-settings-registration/50-RESEARCH.md
@.planning/phases/50-tax-settings-registration/50-PATTERNS.md
@lib/lattice_stripe/credit_note.ex
@lib/lattice_stripe/account/settings.ex
@lib/lattice_stripe/tax/calculation.ex
@test/lattice_stripe/credit_note_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-50-03 | Adopter confuses API registration with legal tax authority registration | medium | Moduledoc first operational section: "does not register with tax authorities" |
| T-50-04 | Wrong `country_options` nesting causes silent Stripe 400s | medium | Moduledoc + test asserts nested `country_options[us]` in create body |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tax.Registration CRUDL + stream + ObjectTypes</name>
  <files>lib/lattice_stripe/tax/registration.ex, lib/lattice_stripe/object_types.ex, test/support/fixtures/tax_registration.ex, test/lattice_stripe/object_types_test.exs</files>
  <read_first>
    - lib/lattice_stripe/credit_note.ex
    - lib/lattice_stripe/account/settings.ex
    - lib/lattice_stripe/object_types.ex
    - test/support/fixtures/credit_note.ex
  </read_first>
  <action>
Create `LatticeStripe.Tax.Registration`:
- `@known_fields ~w[id object active_from country country_options created expires_at livemode status]`
- `defstruct` with `object: "tax.registration"`
- Verbs: `create/3`, `create!/3`, `retrieve/3`, `retrieve!/3`, `update/4`, `update!/4`, `list/3`, `list!/3`, `stream!/3`
- Paths: collection `/v1/tax/registrations`, member `/v1/tax/registrations/:id`
- `from_map/1`: `country_options` stays **raw map**; atomize `status` for known strings; `extra` for unknown keys
- Minimal `@moduledoc` placeholder (full doc in Task 2)

Register:
```elixir
"tax.registration" => LatticeStripe.Tax.Registration,
```

Fixture `test/support/fixtures/tax_registration.ex` with `basic/0`, `basic/1` including `"id" => "taxreg_123"`, `"country" => "US"`, `"country_options" => %{"us" => %{"type" => "state_sales_tax", "state" => "CA"}}`.

ObjectTypes test case for `"tax.registration"`.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'def stream!' lib/lattice_stripe/tax/registration.ex` succeeds
    - `grep '"tax.registration"' lib/lattice_stripe/object_types.ex` succeeds
    - `mix test test/lattice_stripe/object_types_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full moduledocs (Settings + Registration)</name>
  <files>lib/lattice_stripe/tax/settings.ex, lib/lattice_stripe/tax/registration.ex</files>
  <read_first>
    - lib/lattice_stripe/tax/calculation.ex (moduledoc voice)
    - lib/lattice_stripe/credit_note.ex (pagination language)
    - .planning/phases/50-tax-settings-registration/50-CONTEXT.md (D-03 sections)
  </read_first>
  <action>
Replace Settings `@moduledoc` with full D-03 content:
- purpose → singleton surface (no ID; `retrieve/2` + `update/3` only)
- defaults → Calculation fallback (`tax_code` / `tax_behavior` when line items omit them)
- fence vs `LatticeStripe.Invoice.AutomaticTax`
- Connect `stripe_account:` note
- usage examples (retrieve + update with string keys)
- note: params cannot be unset once set; `status` / `active` path
- API link + Phase 51 guide placeholder
- Must contain grep targets: `singleton`, `tax_code`, `LatticeStripe.Tax.Calculation`

Replace Registration `@moduledoc` with D-03 ordered sections:
- purpose
- **does not register with tax authorities** (prominent)
- relationship: `LatticeStripe.Tax.Settings`, `LatticeStripe.Tax.Calculation`, not `Invoice.AutomaticTax`; filing **out of SDK scope**
- **`country_options`**: lowercase ISO keys; US + one non-US example; anti-pattern top-level `type`
- usage (`create/3`, `list/3`)
- pagination: `list/3` one page; `stream!/3` for many jurisdictions; `List` not `Enumerable`
- API link + guide placeholder
- Must contain: `tax authorities`, `country_options`, `stream!`
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "tax authorities" lib/lattice_stripe/tax/registration.ex` succeeds
    - `grep "singleton" lib/lattice_stripe/tax/settings.ex` succeeds
    - `grep "Invoice.AutomaticTax" lib/lattice_stripe/tax/registration.ex` succeeds
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: registration_test.exs</name>
  <files>test/lattice_stripe/tax/registration_test.exs</files>
  <read_first>
    - test/lattice_stripe/credit_note_test.exs
    - test/support/fixtures/tax_registration.ex
    - lib/lattice_stripe/tax/registration.ex
  </read_first>
  <action>
Create `test/lattice_stripe/tax/registration_test.exs` with Mox, `async: true`, `setup :verify_on_exit!`.

Per-verb describes (CreditNote-shaped):
- **create/3**: POST `/v1/tax/registrations`; assert body encodes nested `"country_options"` (e.g. `"country_options[us][state]"` or equivalent FormEncoder shape — match existing credit_note create assert style)
- **retrieve/3**: GET `/v1/tax/registrations/taxreg_123`
- **update/4**: POST `/v1/tax/registrations/taxreg_123`
- **list/3**: GET collection; returns `%List{}`
- **stream!/3**: exported and returns a stream (smoke: `Stream.take(stream, 0)` or first page call)

**from_map/1**: `country_options` remains map; status atomized when `"active"`.

Target ~8–10 tests.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax/ --no-start 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/tax/ --no-start` exits 0
    - `grep 'country_options' test/lattice_stripe/tax/registration_test.exs` succeeds
    - `mix test test/lattice_stripe/tax/settings_test.exs test/lattice_stripe/tax/registration_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks:
- `mix test test/lattice_stripe/tax/ --no-start`
- `mix compile --warnings-as-errors`
- Manual grep: moduledoc targets from CONTEXT D-03 present
</verification>
