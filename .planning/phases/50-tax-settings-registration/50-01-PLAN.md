---
phase: 50-tax-settings-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/tax/settings.ex
  - lib/lattice_stripe/tax/settings/defaults.ex
  - lib/lattice_stripe/tax/settings/head_office.ex
  - lib/lattice_stripe/tax/settings/status_details.ex
  - lib/lattice_stripe/object_types.ex
  - test/support/fixtures/tax_settings.ex
  - test/lattice_stripe/tax/settings_test.exs
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements: [CONF-01, CONF-02]
must_haves:
  truths:
    - "Adopters call `Tax.Settings.retrieve/2` with no ID param and receive `{:ok, %Tax.Settings{}}` (ROADMAP SC#1)."
    - "Adopters call `Tax.Settings.update/3` against POST `/v1/tax/settings` (ROADMAP SC#2)."
    - "Settings paths never include a resource ID segment (Pitfall #7)."
    - "`tax.settings` dispatches through ObjectTypes (ROADMAP SC#5 partial)."
  artifacts:
    - path: lib/lattice_stripe/tax/settings.ex
      provides: Tax.Settings singleton module
      contains: path: "/v1/tax/settings"
    - path: test/lattice_stripe/tax/settings_test.exs
      provides: singleton module surface + URL tests
      contains: describe "module surface"
---

<objective>
Ship `LatticeStripe.Tax.Settings` as the codebase's first singleton-with-update resource: nested `Defaults`, `HeadOffice`, `StatusDetails`, `retrieve/2`, `update/3`, `tax.settings` ObjectTypes registration, fixtures, and load-bearing unit tests for CONF-01 and CONF-02.

Minimal `@moduledoc` one-liner in this plan; full Settings moduledoc ships in Plan 02 alongside Registration (both modules must exist before cross-links).

Output: settings module tree, `tax_settings` fixture, `settings_test.exs`, ObjectTypes entry + dispatch test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-tax-settings-registration/50-CONTEXT.md
@.planning/phases/50-tax-settings-registration/50-RESEARCH.md
@.planning/phases/50-tax-settings-registration/50-PATTERNS.md
@lib/lattice_stripe/balance.ex
@lib/lattice_stripe/tax/calculation.ex
@lib/lattice_stripe/object_types.ex
@test/lattice_stripe/balance_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-50-01 | Settings implemented as CRUD with ID in URL (Pitfall #7) | high | Module surface refutes `retrieve/3`, `update/4`; URL test asserts path ends with `/v1/tax/settings` with no trailing segment |
| T-50-02 | Logging raw API keys in fixtures | low | Fixtures use `sk_test_` placeholders via TestHelpers only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Nested Settings structs</name>
  <files>lib/lattice_stripe/tax/settings/defaults.ex, lib/lattice_stripe/tax/settings/head_office.ex, lib/lattice_stripe/tax/settings/status_details.ex</files>
  <read_first>
    - lib/lattice_stripe/tax/customer_details.ex
    - .planning/phases/50-tax-settings-registration/50-CONTEXT.md (D-02)
    - .planning/phases/50-tax-settings-registration/50-RESEARCH.md
  </read_first>
  <action>
Create `LatticeStripe.Tax.Settings.Defaults`:
- `@known_fields ~w[tax_behavior tax_code provider]`
- `object` default omitted (nested object, not top-level Stripe object)
- `from_map/1`: atomize `tax_behavior` for `"exclusive"`, `"inclusive"`, `"inferred_by_currency"`; pass through unknown strings; `tax_code` and `provider` remain strings
- `extra` for unknown keys

Create `LatticeStripe.Tax.Settings.HeadOffice`:
- `@known_fields ~w[address]`
- `address` stored as **map()** (no Address submodule)

Create `LatticeStripe.Tax.Settings.StatusDetails`:
- thin module — keep inner `active` / `pending` as maps if present on wire (do not sub-struct jurisdiction trees)
- `@known_fields` per Stripe doc verification at implement time (minimum: fields present in fixture)
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "defmodule LatticeStripe.Tax.Settings.Defaults" lib/lattice_stripe/tax/settings/defaults.ex` succeeds
    - `grep -l "defmodule LatticeStripe.Tax.Settings.HeadOffice" lib/lattice_stripe/tax/settings/head_office.ex` succeeds
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tax.Settings singleton verbs + from_map + ObjectTypes</name>
  <files>lib/lattice_stripe/tax/settings.ex, lib/lattice_stripe/object_types.ex, test/support/fixtures/tax_settings.ex, test/lattice_stripe/object_types_test.exs</files>
  <read_first>
    - lib/lattice_stripe/balance.ex
    - lib/lattice_stripe/credit_note.ex (update POST pattern only)
    - lib/lattice_stripe/object_types.ex
    - test/support/fixtures/credit_note.ex
  </read_first>
  <action>
Create `LatticeStripe.Tax.Settings` with:
- `@known_fields ~w[defaults head_office livemode object status status_details]`
- `defstruct` **without** `:id` field; `object: "tax.settings"`
- `retrieve/2`, `retrieve!/2` → `GET /v1/tax/settings`, `params: %{}`
- `update/3`, `update!/3` → `POST /v1/tax/settings`, raw string-key `params`
- `from_map/1`: decode `defaults` → `%Defaults{}`, `head_office` → `%HeadOffice{}`, `status_details` → `%StatusDetails{}`; atomize top-level `status` for `"active"`, `"pending"`; `extra` for unknown keys
- `@moduledoc` one-liner: "Stripe Tax Settings singleton — full docs in Plan 02."

Register in `object_types.ex`:
```elixir
"tax.settings" => LatticeStripe.Tax.Settings,
```

Add `test/support/fixtures/tax_settings.ex` with `basic/0` and `basic/1` merge helper returning a wire map including `"object" => "tax.settings"`, nested `"defaults"`, `"head_office"`, `"status" => "active"`.

Add one `object_types_test.exs` case: `assert %Tax.Settings{} = ObjectTypes.deserialize(%{"object" => "tax.settings", ...})`.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'path: "/v1/tax/settings"' lib/lattice_stripe/tax/settings.ex` returns 2 matches (GET + POST)
    - `grep '"tax.settings"' lib/lattice_stripe/object_types.ex` returns a match
    - `mix test test/lattice_stripe/object_types_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: settings_test.exs — Pitfall #7 guards</name>
  <files>test/lattice_stripe/tax/settings_test.exs</files>
  <read_first>
    - test/lattice_stripe/balance_test.exs (module surface describe)
    - test/support/fixtures/tax_settings.ex
    - lib/lattice_stripe/tax/settings.ex
  </read_first>
  <action>
Create `test/lattice_stripe/tax/settings_test.exs` with `async: true`, `setup :verify_on_exit!`, Mox transport.

**describe "retrieve/2"**: assert GET URL ends with `/v1/tax/settings` and does NOT match `~r{/v1/tax/settings/[^/]}` (no trailing ID).

**describe "update/3"**: assert POST same path; body includes nested key e.g. `"defaults"` when passed in params.

**describe "retrieve/2 with stripe_account opt"**: assert `{"stripe-account", "acct_123"}` header present when opt set; absent when not.

**describe "module surface"** (mirror balance_test):
- refute `list`, `create`, `delete` exported (arities 1–3)
- refute `retrieve/3`, `update/4`
- refute `%Tax.Settings{}` has `:id` key
- assert `retrieve/2`, `retrieve!/2`, `update/3`, `update!/2` exported

**describe "from_map/1"**: defaults decode to `%Defaults{}`; unknown top-level key in `extra`.

Target ~10–12 tests total.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax/settings_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/tax/settings_test.exs --no-start` exits 0
    - `grep 'describe "module surface"' test/lattice_stripe/tax/settings_test.exs` succeeds
    - `grep 'refute Map.has_key?(%LatticeStripe.Tax.Settings{}, :id)' test/lattice_stripe/tax/settings_test.exs` succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks:
- `mix test test/lattice_stripe/tax/settings_test.exs test/lattice_stripe/object_types_test.exs --no-start`
- `mix compile --warnings-as-errors`
</verification>
