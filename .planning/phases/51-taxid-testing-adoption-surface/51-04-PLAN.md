---
phase: 51-taxid-testing-adoption-surface
plan: 04
type: execute
wave: 4
depends_on: [51-03]
files_modified:
  - test/lattice_stripe/docs_truth_test.exs
  - test/lattice_stripe/tax/transaction_test.exs
autonomous: true
requirements: [DX-05]
must_haves:
  truths:
    - "Docs-truth locks `guides/tax.md` in ExDoc Canonical Guides (3C)."
    - "Guide content anchors cannot drift (3A)."
    - "Cross-link graph includes tax guide forward/reverse links (3D)."
    - "Five Tax moduledocs locked by semantic grep anchors (3E, ROADMAP SC#6)."
  artifacts:
    - path: test/lattice_stripe/docs_truth_test.exs
      provides: Tax docs-truth regression
      contains: guides/tax.md
---

<objective>
Extend `docs_truth_test.exs` with Tax guide and moduledoc grep blocks (DX-05), and remove any partial moduledoc grep from `transaction_test.exs` migrated to central docs-truth.

Depends on Plan 03 (guide exists, moduledocs link `guides/tax.md`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md
@.planning/phases/51-taxid-testing-adoption-surface/51-RESEARCH.md
@test/lattice_stripe/docs_truth_test.exs
@.planning/milestones/v1.5-phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-51-08 | Docs drift after phase ships | medium | Semantic anchor greps on guide + five moduledocs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: docs_truth_test — ExDoc 3C + guide content 3A + cross-links 3D</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs (existing Phase 48 patterns)
    - guides/tax.md
    - mix.exs
    - .planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md (D-04)
  </read_first>
  <action>
Add tests to `docs_truth_test.exs`:

**3C — ExDoc:**
- Assert `"guides/tax.md" in extras`
- Assert `"guides/tax.md" in groups["Canonical Guides"]`

**3A — Guide content locks** (`File.read!("guides/tax.md")`):
- `Calculation.create`
- `create_from_calculation`
- `create_reversal`
- `Invoice.AutomaticTax`
- `out of SDK scope`
- `90` AND (`expires_at` OR `days`)
- `reference` AND (`globally` OR `unique`)
- Optional: `country_options`, `Tax.Settings`, `Tax.Registration`

**3D — Cross-link graph:**
- Forward: `tax.md` contains `testing.md`, `error-handling.md`, `payments.md`
- Reverse: `user-flows-and-jtbd.md` contains `tax.md`
- Reverse: all five Tax `@moduledoc` strings contain `guides/tax.md` (read via `@moduledoc` attribute or module doc helper pattern used in existing tests)
- Optional: `recipes.md` contains `tax.md`
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --no-start 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'guides/tax.md' test/lattice_stripe/docs_truth_test.exs` succeeds
    - `grep 'Canonical Guides' test/lattice_stripe/docs_truth_test.exs` succeeds
    - `mix test test/lattice_stripe/docs_truth_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docs_truth_test — moduledoc 3E anchors + migrate transaction_test grep</name>
  <files>test/lattice_stripe/docs_truth_test.exs, test/lattice_stripe/tax/transaction_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs
    - test/lattice_stripe/tax/transaction_test.exs
    - lib/lattice_stripe/tax/calculation.ex
    - lib/lattice_stripe/tax/transaction.ex
    - lib/lattice_stripe/tax/settings.ex
    - lib/lattice_stripe/tax/registration.ex
    - lib/lattice_stripe/tax_id.ex
    - .planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md (3E table)
  </read_first>
  <action>
Add **3E moduledoc grep tests** reading each module's `@moduledoc` (match existing docs_truth style):

**Calculation:** must match ALL of:
- `90` + (`days` OR `expires_at`)
- `Invoice.AutomaticTax`
- `out of SDK scope`
- `LatticeStripe.Tax.Transaction`
- `guides/tax.md`

**Transaction:**
- `reference` + (`globally` OR `unique`)
- `create_from_calculation`
- `create_reversal`
- `Invoice.AutomaticTax`
- `LatticeStripe.Tax.Calculation`
- `guides/tax.md`

**Settings:**
- `singleton`
- `tax_code`
- `LatticeStripe.Tax.Calculation`
- `Invoice.AutomaticTax`
- `stripe_account`
- `guides/tax.md`

**Registration:**
- `tax authorities`
- `country_options`
- `LatticeStripe.Tax.Settings`
- `LatticeStripe.Tax.Calculation`
- `Invoice.AutomaticTax`
- `out of SDK scope`
- `stream!`
- `guides/tax.md`

**TaxId:**
- `/v1/tax_ids` AND `customers` (dual-path prose)
- `Invoice.AutomaticTax`
- `guides/tax.md`

**Migrate:** Remove any `describe` or test in `transaction_test.exs` that duplicates moduledoc grep targets (Phase 49 partial DX-03 lock). Centralize in docs_truth only.

**Do not grep** removed placeholder `A canonical tax guide ships in v1.6 Phase 51.`
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/tax/ --no-start 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Tax.Calculation' test/lattice_stripe/docs_truth_test.exs` succeeds
    - `grep 'TaxId' test/lattice_stripe/docs_truth_test.exs` succeeds
    - `! grep 'moduledoc grep' test/lattice_stripe/tax/transaction_test.exs` OR no duplicate anchor tests remain in transaction_test.exs
    - `mix test test/lattice_stripe/docs_truth_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Full phase verification slice:
```bash
mix test test/lattice_stripe/tax/ test/lattice_stripe/tax_id_test.exs test/lattice_stripe/tax_object_types_expand_test.exs test/lattice_stripe/docs_truth_test.exs --no-start
mix compile --warnings-as-errors
```
</verification>
