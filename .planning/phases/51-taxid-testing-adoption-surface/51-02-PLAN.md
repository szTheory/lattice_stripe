---
phase: 51-taxid-testing-adoption-surface
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - lib/lattice_stripe/testing/fixtures/tax_calculation.ex
  - lib/lattice_stripe/testing/fixtures/tax_transaction.ex
  - lib/lattice_stripe/testing/fixtures/tax_id.ex
  - lib/lattice_stripe/testing.ex
  - mix.exs
  - test/lattice_stripe/tax/calculation_transaction_test.exs
  - test/lattice_stripe/tax/calculation_test.exs
  - test/lattice_stripe/tax/transaction_test.exs
  - test/lattice_stripe/object_types_test.exs
  - test/lattice_stripe/tax_object_types_expand_test.exs
  - test/support/fixtures/tax_calculation.ex
  - test/support/fixtures/tax_transaction.ex
  - guides/testing.md
autonomous: true
requirements: [DX-01, DX-02]
must_haves:
  truths:
    - "`LatticeStripe.Testing.tax_calculation/1`, `tax_transaction/1`, `tax_id/1` return typed structs from wire maps (ROADMAP SC#3, DX-02)."
    - "All five Tax object types resolve via `fetch_module/1` and expand to typed structs (ROADMAP SC#2, DX-01)."
    - "calculation_transaction_test.exs uses public Testing imports after migration."
  artifacts:
    - path: lib/lattice_stripe/testing/fixtures/tax_calculation.ex
      provides: public tax_calculation_json/1
      contains: tax_calculation_json
    - path: test/lattice_stripe/tax_object_types_expand_test.exs
      provides: expand-through-parent proof
      contains: Tax.Calculation.from_map
---

<objective>
Promote Tax wire fixtures to public `LatticeStripe.Testing.Fixtures.*` modules, add `Testing` typed wrappers, prove five-type ObjectTypes expand deserialization (DX-01), and extend `guides/testing.md` with a Tax subsection (DX-02).

Depends on Plan 01 (`TaxId` module exists for `tax_id/1` wrapper).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md
@.planning/phases/51-taxid-testing-adoption-surface/51-RESEARCH.md
@.planning/phases/51-taxid-testing-adoption-surface/51-PATTERNS.md
@lib/lattice_stripe/testing.ex
@lib/lattice_stripe/testing/fixtures/credit_note.ex
@test/support/fixtures/tax_calculation.ex
@test/support/fixtures/tax_transaction.ex
@test/lattice_stripe/tax/calculation_transaction_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-51-04 | Expand paths return raw maps inside Tax parents (Pitfall #6) | high | Four explicit expand scenarios in dedicated test file |
| T-51-05 | Fixture name collision (`calculation/1`) | low | Enforce `tax_` prefix per CONTEXT D-03 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Promote Tax calculation/transaction fixtures to lib/</name>
  <files>lib/lattice_stripe/testing/fixtures/tax_calculation.ex, lib/lattice_stripe/testing/fixtures/tax_transaction.ex, lib/lattice_stripe/testing.ex, mix.exs, test/support/fixtures/tax_calculation.ex, test/support/fixtures/tax_transaction.ex, test/lattice_stripe/tax/calculation_transaction_test.exs, test/lattice_stripe/tax/calculation_test.exs, test/lattice_stripe/tax/transaction_test.exs</files>
  <read_first>
    - test/support/fixtures/tax_calculation.ex
    - test/support/fixtures/tax_transaction.ex
    - lib/lattice_stripe/testing/fixtures/credit_note.ex
    - lib/lattice_stripe/testing.ex
    - mix.exs (docs groups_for_modules Testing section)
  </read_first>
  <action>
Move and rename:
- `test/support/fixtures/tax_calculation.ex` → `lib/lattice_stripe/testing/fixtures/tax_calculation.ex`
  - Module: `LatticeStripe.Testing.Fixtures.TaxCalculation`
  - Keep `tax_calculation_json/1`, `tax_calculation_line_item_json/1` signatures
- `test/support/fixtures/tax_transaction.ex` → `lib/lattice_stripe/testing/fixtures/tax_transaction.ex`
  - Module: `LatticeStripe.Testing.Fixtures.TaxTransaction`
  - Keep `tax_transaction_json/1`, `tax_transaction_line_item_json/1`

Add to `LatticeStripe.Testing`:
```elixir
def tax_calculation(raw_map), do: Tax.Calculation.from_map(raw_map)
def tax_transaction(raw_map), do: Tax.Transaction.from_map(raw_map)
```

Add aliases for `Tax.Calculation`, `Tax.Transaction` at top of `testing.ex`.

Update `mix.exs` docs `groups_for_modules` Testing group to list the two new fixture modules.

Update all imports in tax tests from `LatticeStripe.Test.Fixtures.TaxCalculation` to `LatticeStripe.Testing.Fixtures.TaxCalculation` (and Transaction).

**Delete** `test/support/fixtures/tax_calculation.ex` and `tax_transaction.ex` after migration.

Do **not** promote `tax_settings.ex` / `tax_registration.ex` (remain internal).
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax/calculation_transaction_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "defmodule LatticeStripe.Testing.Fixtures.TaxCalculation" lib/lattice_stripe/testing/fixtures/tax_calculation.ex` succeeds
    - `test -f test/support/fixtures/tax_calculation.ex` fails (file deleted)
    - `grep 'def tax_calculation(' lib/lattice_stripe/testing.ex` succeeds
    - `mix test test/lattice_stripe/tax/calculation_transaction_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TaxId public fixture + Testing wrapper</name>
  <files>lib/lattice_stripe/testing/fixtures/tax_id.ex, lib/lattice_stripe/testing.ex, mix.exs</files>
  <read_first>
    - lib/lattice_stripe/tax_id.ex
    - lib/lattice_stripe/testing/fixtures/credit_note.ex
    - .planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md (D-03)
  </read_first>
  <action>
Create `lib/lattice_stripe/testing/fixtures/tax_id.ex`:
- `tax_id_json/1` default map: `"object" => "tax_id"`, `"id" => "txi_test123"`, `"type" => "eu_vat"`, `"value" => "DE123456789"`, `"country" => "DE"`, `"livemode" => false`, nested `"verification" => %{"status" => "pending"}`

Add to `LatticeStripe.Testing`:
```elixir
def tax_id(raw_map), do: TaxId.from_map(raw_map)
```

Add `TaxId` alias. Register module in `mix.exs` Testing docs group.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax_id_test.exs --no-start 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'def tax_id_json' lib/lattice_stripe/testing/fixtures/tax_id.ex` succeeds
    - `grep 'def tax_id(' lib/lattice_stripe/testing.ex` succeeds
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ObjectTypes five-type dispatch + expand proof</name>
  <files>test/lattice_stripe/object_types_test.exs, test/lattice_stripe/tax_object_types_expand_test.exs</files>
  <read_first>
    - test/lattice_stripe/object_types_test.exs
    - lib/lattice_stripe/tax/calculation.ex
    - lib/lattice_stripe/tax/transaction.ex
    - lib/lattice_stripe/object_types.ex
    - test/lattice_stripe/credit_note_test.exs (expand patterns if any)
  </read_first>
  <action>
In `object_types_test.exs`:
- Add test: `ObjectTypes.fetch_module("tax_id") == LatticeStripe.TaxId`
- Add test: all five resolve — `tax.calculation`, `tax.transaction`, `tax.settings`, `tax.registration`, `tax_id`

Create `test/lattice_stripe/tax_object_types_expand_test.exs` with four scenarios (CONTEXT D-05):
1. `Tax.Calculation.from_map/1` with expanded `"customer"` map → `%LatticeStripe.Customer{}`
2. `Tax.Calculation.LineItem.from_map/1` (or line item on calculation) with expanded `"product"` → typed product via ObjectTypes
3. `Tax.Transaction.from_map/1` with expanded `"customer"` → `%Customer{}`
4. `ObjectTypes.maybe_deserialize/1` on standalone `tax_id` wire map → `%TaxId{}`

Use minimal expanded child maps with correct `"object"` keys. Import public fixtures where helpful.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax_object_types_expand_test.exs test/lattice_stripe/object_types_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/lattice_stripe/tax_object_types_expand_test.exs` succeeds
    - `grep 'Tax.Calculation.from_map' test/lattice_stripe/tax_object_types_expand_test.exs` succeeds
    - `grep '%Customer{}' test/lattice_stripe/tax_object_types_expand_test.exs` succeeds
    - `mix test test/lattice_stripe/tax_object_types_expand_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: guides/testing.md Tax subsection</name>
  <files>guides/testing.md</files>
  <read_first>
    - guides/testing.md
    - test/lattice_stripe/tax/calculation_transaction_test.exs
    - lib/lattice_stripe/testing.ex
  </read_first>
  <action>
Add `## Tax` subsection to `guides/testing.md` covering:
- Import `LatticeStripe.Testing` and use `tax_calculation_json/1` / `tax_transaction_json/1` / `tax_id_json/1`
- Short Mox-at-Transport example mirroring calc→txn chain (reference `calculation_transaction_test.exs`)
- Note Settings/Registration fixtures remain internal (`test/support/fixtures/`)
  </action>
  <verify>
    <automated>grep -q 'tax_calculation_json' guides/testing.md && grep -q 'Tax' guides/testing.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/testing.md` contains substring `tax_calculation_json`
    - `guides/testing.md` contains substring `LatticeStripe.Testing`
    - `guides/testing.md` contains heading `## Tax`
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix test test/lattice_stripe/tax/ test/lattice_stripe/tax_object_types_expand_test.exs --no-start
mix compile --warnings-as-errors
```
</verification>
