---
phase: 51-taxid-testing-adoption-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/tax_id.ex
  - lib/lattice_stripe/tax_id/verification.ex
  - lib/lattice_stripe/tax_id/owner.ex
  - lib/lattice_stripe/object_types.ex
  - test/lattice_stripe/tax_id_test.exs
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements: [TAXID-01, TAXID-02, TAXID-03, TAXID-04]
must_haves:
  truths:
    - "Adopters create tax IDs via POST `/v1/tax_ids` (top-level) and POST `/v1/customers/:id/tax_ids` (nested) from one module (ROADMAP SC#1, TAXID-01/02)."
    - "Adopters retrieve, list, delete on both paths with arity-based routing (TAXID-03)."
    - "`tax_id` dispatches through ObjectTypes to `%TaxId{}` (TAXID-04, ROADMAP SC#2 partial)."
    - "No update or search verbs exist (Coupon precedent)."
  artifacts:
    - path: lib/lattice_stripe/tax_id.ex
      provides: dual-path TaxId CRUDL
      contains: "/v1/tax_ids"
    - path: test/lattice_stripe/tax_id_test.exs
      provides: per-path Mox URL contract
      contains: describe "module surface"
---

<objective>
Ship `LatticeStripe.TaxId` with dual-path arity routing (LoginLink precedent), nested `Verification` and `Owner` structs, PII-redacted `value` in Inspect, `tax_id` ObjectTypes registration, and load-bearing unit tests for TAXID-01 through TAXID-04.

Output: `tax_id.ex` tree, `tax_id_test.exs`, ObjectTypes entry + dispatch test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md
@.planning/phases/51-taxid-testing-adoption-surface/51-RESEARCH.md
@.planning/phases/51-taxid-testing-adoption-surface/51-PATTERNS.md
@lib/lattice_stripe/login_link.ex
@lib/lattice_stripe/coupon.ex
@lib/lattice_stripe/credit_note.ex
@lib/lattice_stripe/account/company.ex
@lib/lattice_stripe/object_types.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-51-01 | Only customer-nested path implemented (Pitfall #5 / stripity_stripe footgun) | high | Separate Mox describes for top-level vs nested; assert `/v1/tax_ids` and `/v1/customers/cus_/tax_ids` |
| T-51-02 | Tax ID `value` logged in IEx/telemetry | medium | `defimpl Inspect` redacts `:value` when non-nil (Company precedent) |
| T-51-03 | Nested create sends duplicate `customer` in body | low | Test asserts body omits `"customer"` key on nested create |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: TaxId.Verification and TaxId.Owner nested structs</name>
  <files>lib/lattice_stripe/tax_id/verification.ex, lib/lattice_stripe/tax_id/owner.ex</files>
  <read_first>
    - lib/lattice_stripe/tax/customer_details.ex
    - .planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md (D-06)
    - .planning/phases/51-taxid-testing-adoption-surface/51-RESEARCH.md
  </read_first>
  <action>
Create `LatticeStripe.TaxId.Verification`:
- `@known_fields ~w[status verified_address verified_name]`
- `from_map/1`: atomize `status` for `"pending"`, `"verified"`, `"unverified"`, `"unavailable"`; pass through unknown strings
- `extra` for unknown keys

Create `LatticeStripe.TaxId.Owner`:
- `@known_fields ~w[account application customer customer_account type]`
- `from_map/1`: atomize `type` for `"account"`, `"application"`, `"customer"`, `"self"`
- Decode `account`, `customer`, `application` through `ObjectTypes.maybe_deserialize/1` when expanded map
- `extra` for unknown keys
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "defmodule LatticeStripe.TaxId.Verification" lib/lattice_stripe/tax_id/verification.ex` succeeds
    - `grep -l "defmodule LatticeStripe.TaxId.Owner" lib/lattice_stripe/tax_id/owner.ex` succeeds
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TaxId dual-path verbs + from_map + Inspect + ObjectTypes</name>
  <files>lib/lattice_stripe/tax_id.ex, lib/lattice_stripe/object_types.ex, test/lattice_stripe/object_types_test.exs</files>
  <read_first>
    - lib/lattice_stripe/login_link.ex
    - lib/lattice_stripe/coupon.ex
    - lib/lattice_stripe/credit_note.ex
    - lib/lattice_stripe/account/company.ex
    - lib/lattice_stripe/object_types.ex
  </read_first>
  <action>
Create `LatticeStripe.TaxId` at `lib/lattice_stripe/tax_id.ex` (top-level, not under `tax/`):

- `@known_fields ~w[id object country created customer customer_account deleted livemode owner type value verification]`
- `defstruct` with `object: "tax_id"`, `extra: %{}`
- **Top-level verbs** (3-arg after client): `create/3`, `retrieve/3`, `list/3`, `delete/3`, `stream!/3` + bang variants
  - Paths: `/v1/tax_ids`, `/v1/tax_ids/:id`
- **Nested verbs** (4-arg): `create/4`, `retrieve/4`, `list/4`, `delete/4`, `stream!/4` + bang variants
  - Paths: `/v1/customers/:customer_id/tax_ids`, `/v1/customers/:customer_id/tax_ids/:id`
  - Nested `create/4`: POST path above; **do not** include `"customer"` in encoded body params
- `from_map/1`: decode `verification` → `%Verification{}`, `owner` → `%Owner{}`; `customer` through `ObjectTypes.maybe_deserialize/1`; `type` stays string; `extra` for unknown keys
- `@moduledoc` opens with **dual-path URL table** (top-level vs customer-nested); document arity routing before examples; include `Invoice.AutomaticTax` scope fence; placeholder line: `A canonical tax guide ships in v1.6 Phase 51.` (Plan 03 replaces with guide link)
- `defimpl Inspect` redacting `:value` when non-nil (copy pattern from `Account.Company`)

Register in `object_types.ex`:
```elixir
"tax_id" => LatticeStripe.TaxId,
```

Add `object_types_test.exs` case: wire map with `"object" => "tax_id"` deserializes to `%TaxId{}`.
Add `fetch_module/1` test asserting `"tax_id"` resolves to `LatticeStripe.TaxId` alongside existing four Tax types.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"/v1/tax_ids"' lib/lattice_stripe/tax_id.ex` returns at least 1 match
    - `grep '"/v1/customers' lib/lattice_stripe/tax_id.ex` returns at least 1 match
    - `grep '"tax_id"' lib/lattice_stripe/object_types.ex` returns a match
    - `mix test test/lattice_stripe/object_types_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: tax_id_test.exs — dual-path Mox + module surface</name>
  <files>test/lattice_stripe/tax_id_test.exs</files>
  <read_first>
    - test/lattice_stripe/credit_note_test.exs
    - test/lattice_stripe/coupon_test.exs (if exists) or balance_test.exs for module surface
    - lib/lattice_stripe/tax_id.ex
  </read_first>
  <action>
Create `test/lattice_stripe/tax_id_test.exs` with `async: true`, `setup :verify_on_exit!`, Mox transport.

**describe "module surface"**: `refute function_exported?(__MODULE__, :update, 3)` (and 4); refute `search/*`; assert `create/3` and `create/4` both exported.

**describe "top-level create/3"**: POST URL ends with `/v1/tax_ids` and does NOT contain `/customers/`.

**describe "nested create/4"**: POST URL matches `/v1/customers/cus_test123/tax_ids`; request body does NOT contain `"customer"`.

**describe "top-level retrieve/3"**: GET `/v1/tax_ids/txi_test123`.

**describe "nested retrieve/4"**: GET `/v1/customers/cus_test123/tax_ids/txi_test123`.

**describe "top-level list/3"** and **nested list/4**: GET collection paths.

**describe "top-level delete/3"** and **describe "nested delete/4"**: DELETE member paths.

Use minimal wire response maps with `"object" => "tax_id"`, `"id" => "txi_test123"`.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/tax_id_test.exs --no-start 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'describe "module surface"' test/lattice_stripe/tax_id_test.exs` succeeds
    - `grep '/v1/tax_ids' test/lattice_stripe/tax_id_test.exs` succeeds
    - `grep 'customers' test/lattice_stripe/tax_id_test.exs` succeeds
    - `mix test test/lattice_stripe/tax_id_test.exs --no-start` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks:
```bash
mix test test/lattice_stripe/tax_id_test.exs test/lattice_stripe/object_types_test.exs --no-start
mix compile --warnings-as-errors
```
</verification>
