---
phase: 51-taxid-testing-adoption-surface
plan: 03
type: execute
wave: 3
depends_on: [51-02]
files_modified:
  - guides/tax.md
  - mix.exs
  - README.md
  - guides/user-flows-and-jtbd.md
  - guides/recipes.md
  - guides/payments.md
  - guides/invoices.md
  - guides/subscriptions.md
  - lib/lattice_stripe/tax/calculation.ex
  - lib/lattice_stripe/tax/transaction.ex
  - lib/lattice_stripe/tax/settings.ex
  - lib/lattice_stripe/tax/registration.ex
  - lib/lattice_stripe/tax_id.ex
autonomous: true
requirements: [DX-04]
must_haves:
  truths:
    - "`guides/tax.md` teaches calculate → record → reverse with scope boundary (ROADMAP SC#4)."
    - "Guide appears in ExDoc Canonical Guides group (ROADMAP SC#5 partial)."
    - "Discovery ladder routes adopters to tax guide (ROADMAP SC#5)."
    - "All five Tax moduledocs link `guides/tax.md` instead of Phase 51 placeholder."
  artifacts:
    - path: guides/tax.md
      provides: canonical Tax workflow guide
      contains: create_from_calculation
    - path: mix.exs
      provides: ExDoc extras entry
      contains: guides/tax.md
---

<objective>
Publish `guides/tax.md` (~280–350 lines), wire ExDoc + discovery surfaces (Phase 48 D-04 pattern), and replace Phase 51 placeholder strings in all Tax moduledocs with a stable link to the guide.

Depends on Plan 02 (public Testing fixtures referenced in guide §7).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md
@.planning/phases/51-taxid-testing-adoption-surface/51-RESEARCH.md
@guides/metering.md
@guides/payments.md
@.planning/milestones/v1.5-phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
@test/lattice_stripe/tax/calculation_transaction_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in plan |
|----|--------|----------|-------------------|
| T-51-06 | Guide conflates AutomaticTax with standalone Tax API (Pitfall #4) | medium | Section 2 "Choose your path" before API spine; grep targets include `Invoice.AutomaticTax` |
| T-51-07 | Scope bleed into Accrue filing product | medium | Section 1 explicit "out of SDK scope" / filing boundary |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/tax.md</name>
  <files>guides/tax.md</files>
  <read_first>
    - .planning/phases/51-taxid-testing-adoption-surface/51-CONTEXT.md (D-02 spine)
    - guides/metering.md (depth/ tone reference)
    - test/lattice_stripe/tax/calculation_transaction_test.exs
    - lib/lattice_stripe/tax/calculation.ex
    - lib/lattice_stripe/tax/transaction.ex
  </read_first>
  <action>
Create `guides/tax.md` (~280–350 lines) with these sections (order may adjust):

1. **Scope boundary** — SDK primitives only; filing/returns/threshold monitoring out of scope; mention Accrue if named in PROJECT.md
2. **Choose your path** — `Invoice.AutomaticTax` / Checkout / Subscription / Quote vs standalone `Tax.Calculation` API
3. **Mental model** — Settings/Registration configure → Calculation (ephemeral, ~90-day expiry) → Transaction (durable) → reversal
4. **Configure once** — `Tax.Settings` defaults; `Tax.Registration` does not register with tax authorities; US + one non-US `country_options` example
5. **Primary spine** — Elixir examples aligned with `calculation_transaction_test.exs`: `Calculation.create`, `Transaction.create_from_calculation`, `create_reversal`; dynamic `reference` with uniqueness footgun
6. **TaxId** — dual-path summary + one nested `create/4` example
7. **Testing** — `LatticeStripe.Testing.tax_calculation_json/1` etc.; link `guides/testing.md`
8. **See also** — `payments.md`, `error-handling.md`, Stripe custom-tax URL

Required substrings for Plan 04 docs-truth (embed verbatim where natural):
- `Calculation.create`, `create_from_calculation`, `create_reversal`
- `Invoice.AutomaticTax`
- `out of SDK scope`
- `90` with `expires_at` or `days`
- `reference` with `globally` or `unique`
- `country_options`, `Tax.Settings`, `Tax.Registration`
- Forward links: `testing.md`, `error-handling.md`, `payments.md`
  </action>
  <verify>
    <automated>wc -l guides/tax.md | awk '{print $1 >= 200 && $1 <= 400}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f guides/tax.md` succeeds
    - `grep -q 'create_from_calculation' guides/tax.md` succeeds
    - `grep -q 'Invoice.AutomaticTax' guides/tax.md` succeeds
    - `grep -q 'out of SDK scope' guides/tax.md` succeeds
    - `wc -l guides/tax.md` reports line count between 200 and 400
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ExDoc extras + discovery wiring</name>
  <files>mix.exs, README.md, guides/user-flows-and-jtbd.md, guides/recipes.md, guides/payments.md, guides/invoices.md, guides/subscriptions.md</files>
  <read_first>
    - mix.exs (docs extras and groups_for_extras)
    - README.md (Choose Your Route / Features sections)
    - guides/user-flows-and-jtbd.md
    - guides/recipes.md
    - guides/payments.md
    - .planning/milestones/v1.5-phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-04)
  </read_first>
  <action>
**mix.exs:** Add `"guides/tax.md"` to `extras` and to `groups_for_extras["Canonical Guides"]` (near `metering.md`).

**README.md:**
- Add Features bullet for Tax / standalone custom-payment tax
- Add "Choose Your Route" (or equivalent) entry linking to `guides/tax.md` for custom-payment tax path

**guides/user-flows-and-jtbd.md:**
- Add Start Here route for custom-payment / standalone Tax API
- Add `Read next` link to `tax.md` on Job 1 or primary billing job — **no new Job 8**

**guides/recipes.md:** Compact bridge paragraph linking to `tax.md`

**guides/payments.md:** Closing reverse-link to `tax.md`

**guides/invoices.md** and **guides/subscriptions.md:** One-line pointer to `tax.md` for `automatic_tax` vs standalone API decision

Do **not** add 3B install canary `~> 1.6` unless README still shows `~> 1.3` only (skip per CONTEXT).
  </action>
  <verify>
    <automated>grep -q 'guides/tax.md' mix.exs && grep -q 'tax.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"guides/tax.md"' mix.exs` succeeds
    - `grep 'tax.md' README.md` succeeds
    - `grep 'tax.md' guides/user-flows-and-jtbd.md` succeeds
    - `grep 'tax.md' guides/recipes.md` succeeds
    - `grep 'tax.md' guides/payments.md` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replace Phase 51 placeholder in Tax moduledocs</name>
  <files>lib/lattice_stripe/tax/calculation.ex, lib/lattice_stripe/tax/transaction.ex, lib/lattice_stripe/tax/settings.ex, lib/lattice_stripe/tax/registration.ex, lib/lattice_stripe/tax_id.ex</files>
  <read_first>
    - lib/lattice_stripe/tax/calculation.ex
    - lib/lattice_stripe/tax/transaction.ex
    - lib/lattice_stripe/tax/settings.ex
    - lib/lattice_stripe/tax/registration.ex
    - lib/lattice_stripe/tax_id.ex
  </read_first>
  <action>
In all five modules, replace the exact placeholder string:
`A canonical tax guide ships in v1.6 Phase 51.`

With a stable ExDoc link line such as:
`See [Standalone Tax API](guides/tax.md) for the canonical calculate → record → reverse workflow.`

Ensure **TaxId** moduledoc also includes dual-path prose: `/v1/tax_ids` and `customers` (for Plan 04 grep targets).

Do **not** remove existing grep-prep strings from Phases 49–50 (90-day, reference, singleton, tax authorities, etc.).
  </action>
  <verify>
    <automated>! grep -r 'Phase 51' lib/lattice_stripe/tax lib/lattice_stripe/tax_id.ex 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'Phase 51' lib/lattice_stripe/tax lib/lattice_stripe/tax_id.ex` returns no matches
    - `grep -l 'guides/tax.md' lib/lattice_stripe/tax/calculation.ex` succeeds
    - `grep -l 'guides/tax.md' lib/lattice_stripe/tax_id.ex` succeeds
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix compile --warnings-as-errors
grep -q 'guides/tax.md' mix.exs
! grep -r 'Phase 51' lib/lattice_stripe/tax lib/lattice_stripe/tax_id.ex
```
</verification>
