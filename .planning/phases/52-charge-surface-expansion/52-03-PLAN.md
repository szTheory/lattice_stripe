---
phase: 52-charge-surface-expansion
plan: 03
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - test/lattice_stripe/docs_truth_test.exs
  - test/integration/charge_integration_test.exs
autonomous: true
requirements: [CHRG-05]
must_haves:
  truths:
    - "docs-truth grep locks Charge @moduledoc against retrieve-only drift"
    - "stripe-mock integration smokes prove list/search/update/capture routing"
  artifacts:
    - path: "test/lattice_stripe/docs_truth_test.exs"
      provides: "CHRG-05 four-surface triangulation (docs leg)"
      contains: "Charge @moduledoc"
    - path: "test/integration/charge_integration_test.exs"
      provides: "Shape-first integration smokes"
      contains: "Charge.list"
  key_links:
    - from: "test/lattice_stripe/docs_truth_test.exs"
      to: "lib/lattice_stripe/charge.ex"
      via: "File.read! + regex assertions"
      pattern: "retrieve-only"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-01 | Moduledoc drift reintroduces retrieve-only language | low | docs-truth test refutes stale phrases; asserts expanded surface names |
</threat_model>

<objective>
Close CHRG-05 four-surface triangulation: add docs-truth grep block for Charge @moduledoc and extend stripe-mock integration smokes for list/search/update/capture (D-04 recommended polish).

Depends on plan 52-01 (@moduledoc rewrite). Can run parallel to plan 52-02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-charge-surface-expansion/52-CONTEXT.md
@.planning/phases/52-charge-surface-expansion/52-RESEARCH.md
@test/lattice_stripe/docs_truth_test.exs
@test/integration/charge_integration_test.exs
@test/integration/dispute_integration_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Charge @moduledoc docs-truth grep test</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs (existing grep test patterns, e.g. Webhook.Plug ~line 355)
    - lib/lattice_stripe/charge.ex
    - .planning/phases/52-charge-surface-expansion/52-CONTEXT.md (CHRG-05 four-surface)
  </read_first>
  <action>
    Add a new test after the tax guide block (or in a logical grouping near other moduledoc locks):

    ```elixir
    test "Charge @moduledoc reflects expanded PI-first surface" do
      source = File.read!("lib/lattice_stripe/charge.ex")

      # Positive: expanded surface + PI-first + Connect anchor
      assert source =~ "PaymentIntent"
      assert source =~ "list/3"
      assert source =~ "search/3"
      assert source =~ "update/4"
      assert source =~ "capture/4"
      assert source =~ "application_fee"

      # Negative: stale retrieve-only language must not return
      refute source =~ "retrieve-only"
      refute source =~ "Only three public functions"
      refute source =~ "never directly manipulated"
    end
    ```

    Do NOT add README, JTBD, or guide file assertions — out of scope per CONTEXT.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --only "Charge @moduledoc" --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Charge @moduledoc reflects expanded' test/lattice_stripe/docs_truth_test.exs` exits 0
    - `mix test test/lattice_stripe/docs_truth_test.exs --only "Charge @moduledoc" --warnings-as-errors` exits 0
    - Test contains `refute source =~ "retrieve-only"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend charge_integration_test.exs with shape-first smokes</name>
  <files>test/integration/charge_integration_test.exs</files>
  <read_first>
    - test/integration/charge_integration_test.exs
    - test/integration/dispute_integration_test.exs (list/update smoke pattern)
    - lib/lattice_stripe/charge.ex
  </read_first>
  <action>
    Update `@moduledoc` to note expanded surface (not retrieve-only only).

    Add four tests mirroring `dispute_integration_test.exs` shape-first stance:

    ```elixir
    test "list/3 returns a response with a charge list", %{client: client} do
      assert {:ok, %LatticeStripe.Response{data: %LatticeStripe.List{data: charges}}} =
               Charge.list(client)
      assert is_list(charges)
    end

    test "search/3 returns typed results", %{client: client} do
      assert {:ok, %LatticeStripe.Response{data: %LatticeStripe.List{data: charges}}} =
               Charge.search(client, "status:'succeeded'")
      assert is_list(charges)
    end

    test "update/4 returns a typed charge struct", %{client: client} do
      assert {:ok, %Charge{id: id}} =
               Charge.update(client, "ch_test", %{"metadata" => %{"phase_52" => "true"}})
      assert is_binary(id)
    end

    test "capture/4 returns a typed charge struct", %{client: client} do
      assert {:ok, %Charge{id: id}} = Charge.capture(client, "ch_test")
      assert is_binary(id)
    end
    ```

    Add comment: stripe-mock is stateless — tests prove routing + typed decode only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/integration/charge_integration_test.exs --include integration --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Charge.list' test/integration/charge_integration_test.exs` exits 0
    - `grep 'Charge.search' test/integration/charge_integration_test.exs` exits 0
    - `grep 'Charge.update' test/integration/charge_integration_test.exs` exits 0
    - `grep 'Charge.capture' test/integration/charge_integration_test.exs` exits 0
    - Integration test file compiles (stripe-mock may be required at runtime; compile must succeed)
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix test test/lattice_stripe/docs_truth_test.exs --only "Charge @moduledoc" --warnings-as-errors
# With stripe-mock running:
mix test test/integration/charge_integration_test.exs --include integration --warnings-as-errors
```
</verification>

<success_criteria>
- CHRG-05 satisfied: moduledoc + code (52-01) + Mox tests (52-02) + docs-truth grep
- Roadmap criterion 7 satisfied: docs-truth locks Charge moduledoc surface
- Integration smokes optional in CI when stripe-mock available
</success_criteria>
