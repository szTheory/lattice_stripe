---
phase: 52-charge-surface-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/charge.ex
autonomous: true
requirements: [CHRG-01, CHRG-02, CHRG-03, CHRG-04]
must_haves:
  truths:
    - "Adopters can call Charge.list/3, list!/3, stream!/3 with GET /v1/charges"
    - "Adopters can call Charge.search/3, search!/3, search_stream!/3 with GET /v1/charges/search"
    - "Adopters can call Charge.update/4, update!/4 with POST /v1/charges/:id"
    - "Adopters can call Charge.capture/4, capture!/4 with POST /v1/charges/:id/capture"
    - "@moduledoc presents PI-first narrative without retrieve-only language"
  artifacts:
    - path: "lib/lattice_stripe/charge.ex"
      provides: "Expanded Charge resource module"
      contains: "def list("
  key_links:
    - from: "lib/lattice_stripe/charge.ex"
      to: "lib/lattice_stripe/payment_intent.ex"
      via: "Mechanical copy of list/search/stream/update/capture wiring"
      pattern: "/v1/charges"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-01 | PII leakage via expanded logging after new operations | medium | Preserve existing Inspect hide-list; do not add PII fields to inspect output |
| T-52-02 | Adopters capture PI-initiated charges via Charge.capture/4 | medium | @doc on capture/4 warns to use PaymentIntent.capture/4 for PI charges |
</threat_model>

<objective>
Expand `LatticeStripe.Charge` from retrieve-only to list/search/update/capture parity by mechanically implementing PaymentIntent patterns against `/v1/charges`, and rewrite `@moduledoc` to PI-first result-record narrative (D-01).

Output: `lib/lattice_stripe/charge.ex` with 12 new public functions (list, list!, stream!, search, search!, search_stream!, update, update!, capture, capture!) plus updated moduledoc. Existing retrieve/retrieve!/from_map/Inspect unchanged.

Scope: Implementation only — tests land in plans 02 and 03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-charge-surface-expansion/52-CONTEXT.md
@.planning/phases/52-charge-surface-expansion/52-RESEARCH.md
@lib/lattice_stripe/charge.ex
@lib/lattice_stripe/payment_intent.ex
@lib/lattice_stripe/refund.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add List/Response aliases and list/stream/search/update/capture functions</name>
  <files>lib/lattice_stripe/charge.ex</files>
  <read_first>
    - lib/lattice_stripe/charge.ex
    - lib/lattice_stripe/payment_intent.ex (list/search/stream/update/capture sections ~lines 263-549)
    - .planning/phases/52-charge-surface-expansion/52-CONTEXT.md (D-02 mechanical template)
    - .planning/phases/52-charge-surface-expansion/52-RESEARCH.md (endpoint table)
  </read_first>
  <action>
    1. Change alias line from `alias LatticeStripe.{Client, Error, ObjectTypes, Request, Resource}` to include `List` and `Response`.

    2. After the retrieve!/3 section and before from_map/1, add a new section `# Public API: list, search, update, capture` with these functions copied from PaymentIntent, swapping paths to `/v1/charges`:

    - `list/3` — GET `/v1/charges`, `Resource.unwrap_list(&from_map/1)`
    - `list!/3` — bang via `Resource.unwrap_bang!/`
    - `stream!/3` — `List.stream!(client, %Request{method: :get, path: "/v1/charges", ...}) |> Stream.map(&from_map/1)`
    - `search/3` — GET `/v1/charges/search`, params `%{"query" => query}`, `when is_binary(query)`
    - `search!/3`, `search_stream!/3` — same as PaymentIntent
    - `update/4` — POST `/v1/charges/#{id}`, `when is_binary(id)`
    - `update!/4` — bang variant
    - `capture/4` — POST `/v1/charges/#{id}/capture`, default params `%{}`
    - `capture!/4` — bang variant

    3. Add `@spec` for every new function matching PaymentIntent arity patterns.

    4. `@doc` for `update/4`: state Stripe allows updating **metadata and description** (copy constraint style from Refund moduledoc but with both fields — NOT "metadata only").

    5. `@doc` for `capture/4`: MUST include warning — for charges created by PaymentIntent confirmation, use `LatticeStripe.PaymentIntent.capture/4` instead; `Charge.capture/4` is for legacy direct-charge flows. Reference stripe-node `charges.capture` docstring pattern.

    6. `@doc` for `search/3`: include eventual-consistency note (copy PaymentIntent search caveat wording).

    7. Do NOT add `create/3` or `cancel/3`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3 && grep -c 'def list(' lib/lattice_stripe/charge.ex && grep -c 'def capture(' lib/lattice_stripe/charge.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0
    - `grep 'def list(' lib/lattice_stripe/charge.ex` returns 1
    - `grep 'def search(' lib/lattice_stripe/charge.ex` returns 1
    - `grep 'def update(' lib/lattice_stripe/charge.ex` returns 1
    - `grep 'def capture(' lib/lattice_stripe/charge.ex` returns 1
    - `grep '/v1/charges/search' lib/lattice_stripe/charge.ex` returns at least 1
    - `grep 'PaymentIntent.capture' lib/lattice_stripe/charge.ex` returns at least 1 (capture @doc warning)
    - `grep 'def create(' lib/lattice_stripe/charge.ex` returns 0
    - `grep 'def cancel(' lib/lattice_stripe/charge.ex` returns 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite Charge @moduledoc to PI-first expanded surface (D-01)</name>
  <files>lib/lattice_stripe/charge.ex</files>
  <read_first>
    - lib/lattice_stripe/charge.ex (current @moduledoc)
    - .planning/phases/52-charge-surface-expansion/52-CONTEXT.md (D-01 section order)
    - lib/lattice_stripe/dispute.ex (support-workflow moduledoc tone reference)
  </read_first>
  <action>
    Replace the entire `@moduledoc` (lines 2-60) with PI-first narrative per D-01:

    **Section order (mandatory):**
    1. Opening paragraph — Charge is the result record of a payment attempt; PaymentIntent confirmation creates the Charge; new integrations start with `LatticeStripe.PaymentIntent`.
    2. `## When to use this module` — Connect reconciliation, support/audit list+search, post-hoc metadata updates.
    3. `## When not to use this module` — accept payment → `PaymentIntent.create/3`; capture PI charge → `PaymentIntent.capture/4`; cancel → `PaymentIntent.cancel/4`; refund → `Refund.create/3`.
    4. `## Usage` — code examples for: retrieve with expand, list/stream, search, update (metadata+description), capture.
    5. `## Connect platform fee reconciliation` — preserve existing fee_details walkthrough verbatim (application_fee filter example).
    6. `## SDK surface (intentionally omitted)` — no `create`/`cancel`; D-06 reframed as **no payment initiation**, not read-only.
    7. `## Security and Inspect` — preserve existing PII hide-list bullets.
    8. `## Stripe API Reference` — link to Stripe Charge API docs.

    **Strings that MUST NOT appear anywhere in @moduledoc:**
    - `retrieve-only`
    - `Only three public functions`
    - `Charges are never directly manipulated`
    - `never directly manipulated`

    Mention `list/3`, `search/3`, `update/4`, `capture/4` by name in Usage section.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && ! grep -q 'retrieve-only' lib/lattice_stripe/charge.ex && grep -q '## When to use this module' lib/lattice_stripe/charge.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep retrieve-only lib/lattice_stripe/charge.ex` exits 1 (no matches)
    - `grep 'Only three public' lib/lattice_stripe/charge.ex` exits 1
    - `grep '## When to use this module' lib/lattice_stripe/charge.ex` exits 0
    - `grep '## SDK surface (intentionally omitted)' lib/lattice_stripe/charge.ex` exits 0
    - `grep 'list/3' lib/lattice_stripe/charge.ex` exits 0 (in @moduledoc region)
    - `grep 'PaymentIntent' lib/lattice_stripe/charge.ex` exits 0
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks:
```bash
mix compile --warnings-as-errors
mix test test/lattice_stripe/charge_test.exs --only retrieve --warnings-as-errors
```
Existing retrieve tests must still pass (no regression to retrieve/from_map/Inspect).
</verification>

<success_criteria>
- All CHRG-01 through CHRG-04 functions exist and compile
- @moduledoc reflects expanded PI-first surface without stale retrieve-only language
- No create/cancel functions added
</success_criteria>
