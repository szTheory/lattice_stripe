---
phase: 52-charge-surface-expansion
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - test/lattice_stripe/charge/list_test.exs
  - test/lattice_stripe/charge/search_test.exs
  - test/lattice_stripe/charge/update_test.exs
  - test/lattice_stripe/charge/capture_test.exs
  - test/lattice_stripe/charge_test.exs
autonomous: true
requirements: [CHRG-01, CHRG-02, CHRG-03, CHRG-04, CHRG-05]
must_haves:
  truths:
    - "Mox wire tests under test/lattice_stripe/charge/ prove HTTP method/path for list, search, update, capture"
    - "Module surface test asserts positive export matrix and refutes create/cancel only"
  artifacts:
    - path: "test/lattice_stripe/charge/list_test.exs"
      provides: "CHRG-01 Mox wire proof"
      contains: "/v1/charges"
    - path: "test/lattice_stripe/charge_test.exs"
      provides: "TaxId-style module surface contract"
      contains: 'describe "module surface"'
  key_links:
    - from: "test/lattice_stripe/charge/list_test.exs"
      to: "lib/lattice_stripe/charge.ex"
      via: "MockTransport expect on GET /v1/charges"
      pattern: "MockTransport"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-02 | Wire tests assert wrong path, shipping broken routing | low | Mirror payment_intent_test.exs URL assertions exactly with /v1/charges prefix |
</threat_model>

<objective>
Add Mox-at-Transport wire tests for new Charge operations under `test/lattice_stripe/charge/`, and replace the D-06 retrieve-only negative export block with TaxId-style dual contract (D-03).

Depends on plan 52-01 (implementation must exist).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-charge-surface-expansion/52-CONTEXT.md
@.planning/phases/52-charge-surface-expansion/52-RESEARCH.md
@test/lattice_stripe/charge_test.exs
@test/lattice_stripe/payment_intent_test.exs
@test/support/fixtures/charge.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Mox wire tests in test/lattice_stripe/charge/</name>
  <files>
    test/lattice_stripe/charge/list_test.exs
    test/lattice_stripe/charge/search_test.exs
    test/lattice_stripe/charge/update_test.exs
    test/lattice_stripe/charge/capture_test.exs
  </files>
  <read_first>
    - test/lattice_stripe/payment_intent_test.exs (list/search/update/capture describe blocks)
    - test/support/fixtures/charge.ex
    - test/lattice_stripe/charge_test.exs (ok_response/error_response imports)
    - lib/lattice_stripe/charge.ex
  </read_first>
  <action>
    Create four test modules under `test/lattice_stripe/charge/`. Each uses `use ExUnit.Case, async: true`, `import Mox`, `import LatticeStripe.TestHelpers`, `import LatticeStripe.Test.Fixtures.Charge`, `setup :verify_on_exit!`.

    **list_test.exs** (`LatticeStripe.Charge.ListTest`):
    - `list/3` sends GET ending with `/v1/charges`, returns `{:ok, %Response{data: %List{data: [%Charge{}]}}}`
    - Pass `%{"limit" => "10"}` and assert limit appears in request URL or params
    - `list!/3` success and error raise cases
    - `stream!/3` emits `%Charge{}` (one page mock sufficient)

    **search_test.exs** (`LatticeStripe.Charge.SearchTest`):
    - `search/3` sends GET to path containing `/v1/charges/search` with query param
    - `search!/3` success case
    - `search_stream!/3` emits `%Charge{}`

    **update_test.exs** (`LatticeStripe.Charge.UpdateTest`):
    - `update/4` sends POST to `/v1/charges/ch_3OoLqrJ2eZvKYlo20wxYzAbC`
    - Body or params include `metadata` and `description` keys
    - `update!/3` success and error raise cases

    **capture_test.exs** (`LatticeStripe.Charge.CaptureTest`):
    - `capture/4` sends POST to `/v1/charges/ch_3OoLqrJ2eZvKYlo20wxYzAbC/capture`
    - Optional `%{"amount" => 1000}` in params
    - `capture!/4` success and error raise cases
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/charge/ --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/charge/ --warnings-as-errors` exits 0
    - `test/lattice_stripe/charge/list_test.exs` exists
    - `test/lattice_stripe/charge/search_test.exs` exists
    - `test/lattice_stripe/charge/update_test.exs` exists
    - `test/lattice_stripe/charge/capture_test.exs` exists
    - `grep '/v1/charges/search' test/lattice_stripe/charge/search_test.exs` exits 0
    - `grep '/capture' test/lattice_stripe/charge/capture_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace D-06 retrieve-only module surface block with TaxId-style contract</name>
  <files>test/lattice_stripe/charge_test.exs</files>
  <read_first>
    - test/lattice_stripe/charge_test.exs (lines 297-359 — block to delete)
    - test/lattice_stripe/tax_id_test.exs (module surface pattern)
    - .planning/phases/52-charge-surface-expansion/52-RESEARCH.md (export matrix)
  </read_first>
  <action>
    1. **Delete entirely** the `describe "module surface (D-06 retrieve-only)"` block (lines 297-359, all 8 tests).

    2. **Replace with** new `describe "module surface"` containing exactly two tests:

    **Test 1 — positive exports:** Assert `function_exported?` for:
    `retrieve` (2,3), `retrieve!` (2,3), `list` (2,3), `list!` (2,3), `stream!` (1,2,3), `search` (2,3), `search!` (2,3), `search_stream!` (2,3), `update` (3,4), `update!` (3,4), `capture` (3,4), `capture!` (3,4), `from_map` (1).

    **Test 2 — negative refutes:** Refute `create` and `cancel` at arities 2, 3, 4 (and bang variants if exported).

    3. Do NOT change retrieve/from_map/Inspect describes.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/charge_test.exs --only module_surface --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'D-06 retrieve-only' test/lattice_stripe/charge_test.exs` exits 1
    - `grep 'describe "module surface"' test/lattice_stripe/charge_test.exs` exits 0
    - `mix test test/lattice_stripe/charge_test.exs --warnings-as-errors` exits 0
    - All existing retrieve/from_map/Inspect tests still pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix test test/lattice_stripe/charge_test.exs test/lattice_stripe/charge/ --warnings-as-errors
```
</verification>

<success_criteria>
- Roadmap criterion 6 satisfied: integration tests under `test/lattice_stripe/charge/` for list/search/update/capture
- D-06 negative-only block removed; TaxId-style dual contract in place
</success_criteria>
