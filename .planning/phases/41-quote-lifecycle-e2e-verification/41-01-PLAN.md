---
phase: 41-quote-lifecycle-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/quote_integration_test.exs
  - test/support/fixtures/quote.ex
autonomous: true
requirements:
  - QUOT-01
  - QUOT-02
  - QUOT-03
  - QUOT-04
  - QUOT-05
must_haves:
  truths:
    - "Quote integration setup succeeds against the current local stripe-mock instead of failing on stale quote-create payload shape."
    - "Fresh Quote runtime evidence covers create, retrieve, update, list, and stream!/3 with the repaired integration setup per D-01."
    - "Quote integration proof explicitly exercises Quote.pdf/3, Quote.accept/3, and Quote.cancel/3 per D-02."
    - "Fresh Quote runtime evidence covers list_line_items/4 and stream_line_items!/4, while same-run unit evidence keeps Quote.LineItem typed-struct proof anchored in `quote_test` and `object_types_test` per D-01."
    - "The downstream inspection step is attempted exactly once after accept/3 and records the current `stripe-mock` truth about `invoice`, `subscription`, and `subscription_schedule` exposure without inventing a retrieve the environment cannot support."
    - "The integration suite states that stripe-mock evidence is limited to routing, request encoding, binary transport, and typed top-level decode sanity per D-03."
  artifacts:
    - path: test/support/fixtures/quote.ex
      provides: "Current stripe-mock-compatible quote creation helpers using Product-backed price_data for integration evidence"
      contains: "create_quote!"
    - path: test/integration/quote_integration_test.exs
      provides: "Quote lifecycle integration proof for PDF, accept, cancel, line items, and one bounded downstream-reference inspection step"
      contains: "@moduletag :integration"
  key_links:
    - from: test/support/fixtures/quote.ex
      to: test/integration/quote_integration_test.exs
      via: "create_quote!/2 and any new prerequisite-resource helper used by integration setup"
      pattern: "create_quote!"
    - from: test/integration/quote_integration_test.exs
      to: lib/lattice_stripe/quote.ex
      via: "Quote.create/retrieve/update/list/stream!/finalize/accept/cancel/list_line_items/stream_line_items!/pdf calls"
      pattern: "Quote\\.(create|retrieve|update|list|stream!|finalize|accept|cancel|list_line_items|stream_line_items!|pdf)"
---

<objective>
Repair the Quote integration evidence surface so Phase 41 starts from executable runtime proof instead of a stale fixture failure.

Purpose: Close the real audit gap first by making Quote integration evidence truthful under the current local `stripe-mock`, per D-01 through D-13.
Output: Updated Quote integration fixture/setup and a targeted Quote proof path that covers CRUD/list/stream runtime evidence, PDF/lifecycle verbs, line-item streaming, and one bounded downstream-reference inspection step.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-quote-lifecycle-e2e-verification/41-CONTEXT.md
@.planning/phases/41-quote-lifecycle-e2e-verification/41-RESEARCH.md
@.planning/phases/41-quote-lifecycle-e2e-verification/41-PATTERNS.md
@.planning/phases/41-quote-lifecycle-e2e-verification/41-VALIDATION.md
@.planning/phases/36-quote/36-01-SUMMARY.md
@.planning/phases/36-quote/36-02-SUMMARY.md
@test/integration/quote_integration_test.exs
@test/support/fixtures/quote.ex
@lib/lattice_stripe/quote.ex

<interfaces>
From `lib/lattice_stripe/quote.ex`:
```elixir
@spec create(Client.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec retrieve(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec list(Client.t(), map(), keyword()) :: {:ok, Response.t()} | {:error, Error.t()}
@spec finalize(Client.t(), String.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec accept(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec cancel(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec list_line_items(Client.t(), String.t(), map(), keyword()) :: {:ok, Response.t()} | {:error, Error.t()}
@spec pdf(Client.t(), String.t(), keyword()) :: {:ok, binary()} | {:error, Error.t()}
```

From `test/support/fixtures/quote.ex`:
```elixir
def create_quote_customer!(client, attrs \\ %{}) do
  {:ok, customer} = Customer.create(client, %{"email" => Map.get(attrs, "customer_email", "quote-test@example.com")})
  customer
end

def create_quote!(client, attrs \\ %{}) do
  customer = create_quote_customer!(client, attrs)
  ...
  {:ok, quote} = Quote.create(client, params)
  quote
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair Quote integration setup for current stripe-mock validation</name>
  <read_first>
    - test/support/fixtures/quote.ex
    - test/integration/quote_integration_test.exs
    - .planning/phases/41-quote-lifecycle-e2e-verification/41-RESEARCH.md
    - .planning/phases/41-quote-lifecycle-e2e-verification/41-PATTERNS.md
    - test/integration/credit_note_integration_test.exs
  </read_first>
  <files>test/support/fixtures/quote.ex, test/integration/quote_integration_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'price_data.*product_data' test/integration/quote_integration_test.exs test/support/fixtures/quote.ex` returns no Quote-create integration payloads that still rely on `product_data` for the repaired path.
    - `rg -n 'Product\\.create|price_data.*\"product\"' test/support/fixtures/quote.ex test/integration/quote_integration_test.exs` shows the Product-backed `price_data.product` repair used by integration setup.
    - `rg -n 'Quote\\.(create|retrieve|update|list|stream!)' test/integration/quote_integration_test.exs` shows explicit fresh runtime coverage for the QUOT-01 create/retrieve/update/list/stream path.
    - `mix test test/integration/quote_integration_test.exs --include integration` reaches Quote create/retrieve/update/list/stream assertions without the prior request-validation failure about missing `price_data.product`.
  </acceptance_criteria>
  <action>Update `test/support/fixtures/quote.ex` first execution concern per the phase constraint: repair `create_quote!/2` and any helper it needs so Quote integration creation uses a real Product-backed `price_data.product` payload accepted by the current local `stripe-mock`, not the stale `price_data.product_data.name` shape. Keep the change narrow per D-13: preserve existing helper layering, create prerequisite Stripe resources only as needed for executable evidence, and do not alter the public Quote API. Then tighten `test/integration/quote_integration_test.exs` so its QUOT-01 runtime proof consumes the repaired helper and explicitly exercises `Quote.create/3`, `Quote.retrieve/3`, `Quote.update/4`, `Quote.list/3`, and `Quote.stream!/3` instead of relying on stale inline payloads or indirect evidence.</action>
  <verify>
    <automated>mix test test/integration/quote_integration_test.exs --include integration</automated>
  </verify>
  <done>Quote integration setup is green against current `stripe-mock`, the suite no longer fails on stale quote-create request shape, and QUOT-01 has direct create/retrieve/update/list/stream runtime evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded Quote lifecycle, PDF, and downstream-reference inspection proof</name>
  <read_first>
    - test/integration/quote_integration_test.exs
    - lib/lattice_stripe/quote.ex
    - .planning/phases/41-quote-lifecycle-e2e-verification/41-CONTEXT.md
    - .planning/phases/41-quote-lifecycle-e2e-verification/41-RESEARCH.md
    - test/lattice_stripe/quote_test.exs
    - lib/lattice_stripe/invoice.ex
  </read_first>
  <files>test/integration/quote_integration_test.exs</files>
  <acceptance_criteria>
    - `rg -n 'Quote\\.pdf|Quote\\.accept|Quote\\.cancel' test/integration/quote_integration_test.exs` shows explicit integration calls for the three D-02 lifecycle/PDF requirements.
    - `rg -n 'Quote\\.list_line_items|Quote\\.stream_line_items!' test/integration/quote_integration_test.exs` shows explicit QUOT-03 runtime coverage for line-item list and stream paths.
    - `rg -n 'invoice|subscription|subscription_schedule' test/integration/quote_integration_test.exs` shows the downstream-reference inspection preference order from D-06 and records the current `stripe-mock` result explicitly.
    - `rg -n 'stripe-mock|routing|binary transport|typed decode' test/integration/quote_integration_test.exs` shows audit-facing wording that bounds what the integration suite proves per D-03 and D-12.
    - `mix test test/lattice_stripe/object_types_test.exs test/lattice_stripe/quote_test.exs test/integration/quote_integration_test.exs --include integration --warnings-as-errors` passes, proving typed object dispatch, Quote.LineItem unit contracts, and bounded integration route/decode checks coexist cleanly.
  </acceptance_criteria>
  <action>Extend `test/integration/quote_integration_test.exs` per D-01, D-02, D-03, D-05, and D-09 through D-12. Add a module or test-level note that this suite proves route/encoding/binary/decode sanity under `stripe-mock`, not real persisted quote lifecycle semantics. Add explicit integration tests for `Quote.pdf/3`, `Quote.accept/3`, and `Quote.cancel/3`, plus direct runtime coverage for `Quote.list_line_items/4` and `Quote.stream_line_items!/4`. For the acceptance flow, exercise `Quote.create/3 -> Quote.finalize/4 -> Quote.accept/3`, inspect the accepted quote response in the D-06 priority order (`invoice`, then `subscription`, then `subscription_schedule`), and record the exact current `stripe-mock` result. If all three downstream refs remain absent under the current `stripe-mock`, assert that absence explicitly and make the limitation easy for Plan 02 to cite in the verifier. Pair this with fresh same-run unit evidence from `test/lattice_stripe/quote_test.exs` and `test/lattice_stripe/object_types_test.exs` so QUOT-05 still has an explicit unit-primary proof path for `%Quote.LineItem{}` decode and object-type registration. Do not assert integration lifecycle statuses like `:open`, `:accepted`, or `:canceled` when the mock does not persist them.</action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs test/lattice_stripe/quote_test.exs test/integration/quote_integration_test.exs --include integration --warnings-as-errors</automated>
  </verify>
  <done>The Quote proof path covers PDF, accept, cancel, list_line_items, stream_line_items!, and the unit-primary Quote.LineItem/object-types checks, with one bounded downstream-reference inspection step and honest `stripe-mock` scope limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture input -> Stripe HTTP API | Integration helpers send untrusted request payloads that current `stripe-mock` validates strictly |
| stripe-mock response -> Quote/downstream structs | External HTTP bodies are decoded into typed SDK structs and can be over-interpreted if assertions exceed what the mock proves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | T | `test/support/fixtures/quote.ex` | mitigate | Repair the create payload to use the current Stripe-shaped `price_data.product` contract so integration evidence reflects real route validation instead of stale fixture semantics. |
| T-41-02 | R | `test/integration/quote_integration_test.exs` | mitigate | Add explicit audit-facing wording and bounded assertions so the suite cannot falsely claim real Quote state-machine or downstream-creation semantics beyond route/decode sanity. |
| T-41-03 | I | downstream follow-through assertions | mitigate | Retrieve at most one downstream resource and assert typed top-level decode only, avoiding disclosure or accidental claims about payment, subscription activation, or broader billing workflow behavior. |
</threat_model>

<verification>
- `mix test test/integration/quote_integration_test.exs --include integration`
- `mix test test/lattice_stripe/object_types_test.exs test/lattice_stripe/quote_test.exs test/integration/quote_integration_test.exs --include integration --warnings-as-errors`
</verification>

<success_criteria>
- The repaired Quote integration harness runs green against current local `stripe-mock`.
- Create/retrieve/update/list/stream runtime evidence exists for QUOT-01, and list_line_items/stream_line_items evidence exists for QUOT-03.
- `Quote.pdf/3`, `Quote.accept/3`, and `Quote.cancel/3` each have explicit integration evidence.
- `quote_test` plus `object_types_test` provide fresh same-run unit-primary proof for QUOT-05 typed `Quote.LineItem` decoding.
- The downstream-reference inspection step is implemented exactly once and records the current `stripe-mock` boundary honestly if no reference is exposed.
</success_criteria>

<output>
After completion, create `.planning/phases/41-quote-lifecycle-e2e-verification/41-01-SUMMARY.md`
</output>
