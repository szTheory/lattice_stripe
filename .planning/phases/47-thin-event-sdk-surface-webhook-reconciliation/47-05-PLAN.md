---
phase: 47-thin-event-sdk-surface-webhook-reconciliation
plan: 05
type: execute
wave: 3
depends_on: [47-01, 47-02]
files_modified:
  - lib/lattice_stripe/testing.ex
  - test/lattice_stripe/testing_test.exs
autonomous: true
requirements: [TESTING-01]
must_haves:
  truths:
    - "`LatticeStripe.Testing.generate_thin_event_payload/3` produces a `{payload, sig_header}` tuple whose payload is wire-format thin-event JSON (`object: \"v2.core.event\"`, ISO 8601 `created`) and whose sig_header verifies against `parse_event_notification/4` end-to-end (ROADMAP success criterion #6)."
    - "`LatticeStripe.Testing.generate_thin_event_payload/3` accepts `nil` for `related_object_data` to produce snapshot-style v2 events with `related_object: nil` (D-06)."
    - "`LatticeStripe.Testing.event_notification/1` builds a typed `%EventNotification{}` from a raw map without signing — parallel to the existing `dispute/1`, `customer/1`, etc. typed builders (D-06)."
    - "The existing `LatticeStripe.Testing.generate_webhook_payload/3` and `generate_webhook_event/3` snapshot helpers remain unchanged — no `:shape` opt overload, no behavior drift (D-06 backwards-compat invariant)."
  artifacts:
    - path: "lib/lattice_stripe/testing.ex"
      provides: "Two new public functions: `generate_thin_event_payload/3` (signed wire-format builder) + `event_notification/1` (typed-struct builder)"
      contains: "generate_thin_event_payload"
    - path: "test/lattice_stripe/testing_test.exs"
      provides: "Unit tests covering: payload roundtrip via parse_event_notification/4, nil related_object_data path, event_notification/1, snapshot-helper backwards-compat regression"
      contains: "generate_thin_event_payload"
  key_links:
    - from: "lib/lattice_stripe/testing.ex (generate_thin_event_payload/3)"
      to: "lib/lattice_stripe/webhook.ex (Webhook.generate_test_signature/3 — unchanged)"
      via: "signs the JSON-encoded payload with the existing HMAC scheme — same function used for snapshot path"
      pattern: "Webhook.generate_test_signature"
    - from: "lib/lattice_stripe/testing.ex (generate_thin_event_payload/3)"
      to: "lib/lattice_stripe/webhook.ex (Webhook.parse_event_notification/4 — created in plan 02)"
      via: "roundtrip test: generate, then parse, then assert struct fields"
      pattern: "parse_event_notification"
    - from: "lib/lattice_stripe/testing.ex (event_notification/1)"
      to: "lib/lattice_stripe/event_notification.ex (EventNotification.from_map/1 — created in plan 01)"
      via: "direct delegation"
      pattern: "EventNotification.from_map"
---

<objective>
Ship the TESTING-01 helpers: `LatticeStripe.Testing.generate_thin_event_payload/3` (paralleling the existing snapshot helper `generate_webhook_payload/3`) and `LatticeStripe.Testing.event_notification/1` (paralleling existing typed builders like `dispute/1`).

Purpose: Adopters writing tests against `Webhook.parse_event_notification/4` need a way to produce wire-format thin-event payloads with valid HMAC signatures, mirroring how they already use `Testing.generate_webhook_payload/3` for snapshot-path tests. Phase 48's canonical guide will lean on these helpers heavily, so getting the API surface symmetric (separate generators per shape, parallel typed builders) makes the doc story clean.

The `generate_thin_event_payload/3` helper must produce a payload that round-trips through `Webhook.parse_event_notification/4` and decodes to a `%EventNotification{}` with the expected fields — this is end-to-end proof that plans 01, 02, and 05 are mutually consistent. The roundtrip test is the most load-bearing assertion in the phase: if it passes, the wire format is correct, the HMAC scheme is correct, and the decode logic is correct.

Output:
- `lib/lattice_stripe/testing.ex` (modified: add 2 public functions + matching `@spec`s + docstrings + `EventNotification` alias)
- `test/lattice_stripe/testing_test.exs` (modified: add 4 test cases for the new helpers + 1 backwards-compat regression for the snapshot helpers)

Depends on plan 01 (`EventNotification` + `RelatedObject` exist) and plan 02 (`parse_event_notification/4` exists — the roundtrip test calls it). Runs in Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-RESEARCH.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-PATTERNS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-VALIDATION.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-01-SUMMARY.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-02-SUMMARY.md

# Reference implementations (read in tasks below)
@lib/lattice_stripe/testing.ex
@lib/lattice_stripe/webhook.ex
@test/lattice_stripe/testing_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `generate_thin_event_payload/3` + `event_notification/1` to `LatticeStripe.Testing`</name>
  <files>lib/lattice_stripe/testing.ex</files>
  <read_first>
    - lib/lattice_stripe/testing.ex (full file — specifically `generate_webhook_payload/3` at `:197-221` which is the snapshot-shape signed-payload builder to mirror; `dispute/1` at `:78-79` and similar typed-builder helpers from `:78-103` to mirror for `event_notification/1`; the alias block at `:49-59` where `EventNotification` must be added)
    - lib/lattice_stripe/webhook.ex (`generate_test_signature/3` at `:206-212` — the HMAC sign function reused for both snapshot and thin-event payloads byte-for-byte)
    - lib/lattice_stripe/event_notification.ex (plan 01 — fields and defaults; especially `object: "v2.core.event"` and ISO 8601 `created`)
    - test/support/fixtures/event_notification.ex (plan 01 — canonical map shape to reference for default values)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-PATTERNS.md section "lib/lattice_stripe/testing.ex — generate_thin_event_payload/3 + event_notification/1 (MOD)"
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md `<decisions>` block (D-06 Testing Helper API Surface)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-RESEARCH.md Finding 1 (object: "v2.core.event") and Finding 2 (ISO 8601 created)
  </read_first>
  <action>
Add `LatticeStripe.EventNotification` to the existing alias block in `lib/lattice_stripe/testing.ex` (group with existing module aliases).

Add public `generate_thin_event_payload/3` adapted from `generate_webhook_payload/3` (lib/lattice_stripe/testing.ex:197-221) per PATTERNS.md adaptation block. Signature: `def generate_thin_event_payload(type, related_object_data \\ nil, opts)`. `@spec generate_thin_event_payload(String.t(), map() | nil, keyword()) :: {String.t(), String.t()}`. Behavior:

1. `{secret, opts} = Keyword.pop!(opts, :secret)` — `:secret` is required; raise on missing.
2. `{timestamp, notif_opts} = Keyword.pop(opts, :timestamp, System.system_time(:second))` — `:timestamp` is the integer Unix-seconds timestamp used by `generate_test_signature/3`. Default is current system time.
3. Pull optional fields from `notif_opts`: `id` (default `"evt_test_" <> random_hex(16)` — reuse the existing `random_hex/1` private function in `testing.ex`), `context` (default `nil`), `livemode` (default `false`).
4. Compute `created_iso = DateTime.from_unix!(timestamp) |> DateTime.to_iso8601()` — per RESEARCH Finding 2, the wire-format `created` field is an ISO 8601 string derived from the same Unix-seconds timestamp used to sign the payload.
5. Build the raw map per RESEARCH Finding 1 (`"object" => "v2.core.event"` — NOT `"v2.core.event_notification"`):
   ```
   raw_map = %{
     "id" => id,
     "object" => "v2.core.event",
     "type" => type,
     "created" => created_iso,
     "context" => context,
     "livemode" => livemode,
     "related_object" => related_object_data
   }
   ```
   When `related_object_data` is `nil`, the wire map has `"related_object" => nil` — this is the snapshot-style v2 event shape (D-06: `nil` for snapshot-style v2 events). When `related_object_data` is a map (e.g., `%{"id" => "acct_test_123", "type" => "v2.core.account", "url" => "/v2/core/accounts/acct_test_123"}`), the wire map preserves it verbatim.
6. `payload = Jason.encode!(raw_map)`.
7. `sig_header = LatticeStripe.Webhook.generate_test_signature(payload, secret, timestamp: timestamp)` — reuses the existing snapshot signature function byte-for-byte (verified by RESEARCH "Pattern 5" + the existing `generate_webhook_payload/3` line `:215-219`).
8. Return `{payload, sig_header}`.

Do NOT touch `generate_webhook_payload/3` or `generate_webhook_event/3` (the existing snapshot helpers) — D-06 explicitly says they remain unchanged. Do NOT add a `:shape` opt overload that selects snapshot vs thin behavior within a single function — D-06 explicitly prefers separate API surface for "obviously distinct adopter test paths".

Add public `event_notification/1` adapted from `dispute/1` (lib/lattice_stripe/testing.ex:78-79) per PATTERNS.md "Analog B" block. Signature: `def event_notification(raw_map)`. `@spec event_notification(map()) :: EventNotification.t()`. Implementation: `def event_notification(raw_map), do: EventNotification.from_map(raw_map)` — direct delegation, no transformation. This parallels the existing `dispute/1`, `customer/1`, etc. typed builders that adopters use in tests to construct typed structs from raw maps without going through HTTP.

`@doc` blocks must:
- For `generate_thin_event_payload/3`: describe it as the thin-event counterpart to `generate_webhook_payload/3`; cross-reference; document `opts`: `:secret` (required), `:timestamp` (default current time, Unix seconds), `:id` (default `"evt_test_..."`), `:context` (default nil), `:livemode` (default false); state that `related_object_data` of `nil` produces a snapshot-style v2 event; show the canonical usage example from RESEARCH "Code Examples — Building a signed thin-event payload for tests (TESTING-01)".
- For `event_notification/1`: describe it as the typed-builder parallel to `dispute/1` / `customer/1`; one-line example showing direct use with `event_notification_map/0` from the fixture file.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -E '^\s+def generate_thin_event_payload\(type, related_object_data \\\\ nil, opts\)' lib/lattice_stripe/testing.ex` returns a match.
    - Source: `grep -E '@spec generate_thin_event_payload\(' lib/lattice_stripe/testing.ex` returns a match including `{String.t(), String.t()}` return type.
    - Source: `grep -E '^\s+def event_notification\(raw_map\)' lib/lattice_stripe/testing.ex` returns a match.
    - Source: `grep -E '@spec event_notification\(' lib/lattice_stripe/testing.ex` returns a match returning `EventNotification.t()`.
    - Source: `grep '"object" => "v2.core.event"' lib/lattice_stripe/testing.ex` returns a match (NOT `"v2.core.event_notification"`).
    - Source: `grep -c '"v2.core.event_notification"' lib/lattice_stripe/testing.ex` returns `0` (the wrong CONTEXT value must not appear).
    - Source: `grep -E 'DateTime\.from_unix!|DateTime\.to_iso8601' lib/lattice_stripe/testing.ex` returns at least one match per function (ISO 8601 created encoding per RESEARCH Finding 2).
    - Source: `grep 'Webhook.generate_test_signature' lib/lattice_stripe/testing.ex` returns at least 2 matches (one in snapshot helper, one in thin-event helper).
    - Source: `grep 'alias LatticeStripe.EventNotification' lib/lattice_stripe/testing.ex` returns a match.
    - Source: `grep 'EventNotification.from_map' lib/lattice_stripe/testing.ex` returns a match.
    - Source: existing `generate_webhook_payload` signature unchanged — `grep -E 'def generate_webhook_payload\(type, object_data \\\\ %\{\}, opts\)' lib/lattice_stripe/testing.ex` returns a match.
    - Behavior: `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    `generate_thin_event_payload/3` builds wire-format thin-event JSON with the correct `object: "v2.core.event"` value and ISO 8601 `created`, signs via the existing `Webhook.generate_test_signature/3`, and accepts `nil` for the snapshot-style v2 event shape. `event_notification/1` directly delegates to `EventNotification.from_map/1`. Snapshot helpers are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add testing_test.exs cases including end-to-end roundtrip via `parse_event_notification/4`</name>
  <files>test/lattice_stripe/testing_test.exs</files>
  <read_first>
    - test/lattice_stripe/testing_test.exs (full file — existing test conventions, existing snapshot-helper tests for `generate_webhook_payload/3` to mirror; existing typed-builder tests for `dispute/1`, `customer/1`, etc.)
    - lib/lattice_stripe/testing.ex (the new functions from task 1)
    - lib/lattice_stripe/webhook.ex (`parse_event_notification/4` from plan 02 — called in the roundtrip test)
    - lib/lattice_stripe/event_notification.ex + lib/lattice_stripe/event_notification/related_object.ex (plan 01 — shapes to assert)
    - test/support/fixtures/event_notification.ex (plan 01 — `event_notification_map/0,1` for `event_notification/1` test)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-VALIDATION.md (TESTING-01 rows — roundtrip pass, nil related_object_data, event_notification/1, snapshot helper regression)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md `<decisions>` block (D-06)
  </read_first>
  <action>
Add the following test cases to `test/lattice_stripe/testing_test.exs`. If there's an existing `describe "generate_webhook_payload/3"` block, add a parallel `describe "generate_thin_event_payload/3"` block after it. Alias `LatticeStripe.{EventNotification, Testing, Webhook}` and `LatticeStripe.EventNotification.RelatedObject` at the top of the test module if not already present.

In `describe "generate_thin_event_payload/3"`:

1. "produces a payload that round-trips through Webhook.parse_event_notification/4" — the load-bearing end-to-end assertion: `{payload, sig_header} = Testing.generate_thin_event_payload("v2.core.account.updated", %{"id" => "acct_test_123", "type" => "v2.core.account", "url" => "/v2/core/accounts/acct_test_123"}, secret: "whsec_test")`; then `{:ok, %EventNotification{} = notif} = Webhook.parse_event_notification(payload, sig_header, "whsec_test")`; assert `notif.type == "v2.core.account.updated"`, `notif.object == "v2.core.event"`, `notif.livemode == false` (or whatever the default is), and `match?(%RelatedObject{id: "acct_test_123", type: "v2.core.account", url: "/v2/core/accounts/acct_test_123"}, notif.related_object)`. This single test proves plans 01 + 02 + 05 are mutually consistent end-to-end.

2. "accepts nil for related_object_data (snapshot-style v2 events)" per D-06: `{payload, sig_header} = Testing.generate_thin_event_payload("v2.core.event.something", nil, secret: "whsec_test")`; `{:ok, notif} = Webhook.parse_event_notification(payload, sig_header, "whsec_test")`; assert `notif.related_object == nil`.

3. "encodes created as ISO 8601 string from the :timestamp opt" per RESEARCH Finding 2: pass `timestamp: 1_741_524_028` (a fixed Unix-seconds value); decode `Jason.decode!(payload)`; assert the `"created"` field equals `"2025-03-09T13:20:28Z"` (or whatever the correct ISO 8601 representation of that Unix timestamp is — let the executor compute via `DateTime.from_unix!(1_741_524_028) |> DateTime.to_iso8601()` in iex during task work to get the exact string). The point is to lock the ISO 8601 representation, not the specific timestamp.

4. "uses 'v2.core.event' as object field (NOT 'v2.core.event_notification')" — wire-format regression: decode `Jason.decode!(payload)`; assert `decoded["object"] == "v2.core.event"`; assert `decoded["object"] != "v2.core.event_notification"`.

5. "raises if :secret opt is missing": `assert_raise KeyError, fn -> Testing.generate_thin_event_payload("v2.core.account.updated", nil, []) end` — `Keyword.pop!` raises KeyError on missing required key.

In `describe "event_notification/1"`:

6. "builds %EventNotification{} from a raw map (no signing, no HTTP)": `import LatticeStripe.Test.Fixtures.EventNotification`; `notif = Testing.event_notification(event_notification_map())`; assert `match?(%EventNotification{id: "evt_test_65UIRNU7G1XbhCfOim416TgmEI4ASQ3jHxXt8RFwXoeVwO", type: "v2.core.account.updated"}, notif)`.

In a backwards-compat regression block (in `describe "generate_webhook_payload/3"` if it exists, or a new `describe "snapshot helper backwards-compat"` block):

7. "generate_webhook_payload/3 still produces 'object' => 'event' (NOT 'v2.core.event')" per D-06 invariant: `{payload, _sig} = Testing.generate_webhook_payload("customer.created", %{"id" => "cus_1"}, secret: "whsec_test")`; `decoded = Jason.decode!(payload)`; assert `decoded["object"] == "event"`; assert `decoded["object"] != "v2.core.event"`. This regression-locks that the snapshot helper was not accidentally retargeted at the thin-event shape.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/testing_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'describe "generate_thin_event_payload/3"' test/lattice_stripe/testing_test.exs` returns at least `1`.
    - Source: `grep -c 'describe "event_notification/1"' test/lattice_stripe/testing_test.exs` returns at least `1`.
    - Source: `grep 'Webhook.parse_event_notification' test/lattice_stripe/testing_test.exs` returns at least one match (roundtrip test).
    - Source: `grep '"v2.core.event"' test/lattice_stripe/testing_test.exs` returns at least one match (object regression assertion).
    - Source: `grep -E '"object" => "event"|decoded\["object"\] == "event"' test/lattice_stripe/testing_test.exs` returns at least one match (snapshot backwards-compat regression).
    - Source: `grep -E 'related_object_data.*nil|generate_thin_event_payload\(\s*"[^"]+",\s*nil' test/lattice_stripe/testing_test.exs` returns at least one match (snapshot-style v2 events test).
    - Source: `grep -E 'DateTime.from_unix!|DateTime.to_iso8601|"created"' test/lattice_stripe/testing_test.exs` returns at least one match in a test that asserts the wire-format ISO 8601 created value.
    - Source: `grep 'assert_raise KeyError' test/lattice_stripe/testing_test.exs` returns at least one match (missing :secret test) — note KeyError is what Keyword.pop! raises.
    - Behavior: `mix test test/lattice_stripe/testing_test.exs` exits 0.
    - Behavior: `mix test test/lattice_stripe/testing_test.exs test/lattice_stripe/webhook_test.exs` exits 0 (combined run validates cross-plan consistency).
    - Behavior: `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    TESTING-01 fully delivered: the roundtrip test in case 1 is the end-to-end proof that plans 01 + 02 + 05 produce mutually consistent code (correct wire format, correct HMAC, correct decode). RESEARCH Findings 1 + 2 are regression-locked at the test layer. Snapshot helpers are unchanged — backwards-compat regression-locked by case 7.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only code → production-shaped HMAC scheme | `generate_test_signature/3` uses the same HMAC-SHA256 + secret scheme as production verification. Test code never escapes `test/` boundaries; helpers are public on `LatticeStripe.Testing` for adopters' own test suites — same exposure model as `generate_webhook_payload/3`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-16 | Tampering | Adopter accidentally uses `generate_thin_event_payload/3` in production (e.g., to "generate test webhooks" against real Stripe accounts) | accept | `LatticeStripe.Testing` moduledoc clearly states "for use in adopter test suites only"; PR review + docs are the mitigation. Same exposure model as the existing snapshot helper. |
| T-47-17 | Information disclosure | The `:secret` opt is passed through `Keyword.pop!` and used in HMAC — no log surface in this code path; matches existing snapshot helper's secret-handling | mitigate | Reuses `generate_test_signature/3` byte-for-byte — no new secret-handling code introduced. |
| T-47-18 | Tampering | A future contributor adds a `:shape` opt to `generate_webhook_payload/3` instead of keeping the helpers separate, drifting from D-06 | mitigate | D-06 explicitly rejects the `:shape` overload. Task 2 case 7 regression-locks that `generate_webhook_payload/3` still emits `"object" => "event"` — a `:shape` overload that retargets behavior would break this test. |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Phase 47 uses only already-pinned mix.lock deps. |
</threat_model>

<verification>
Per-plan quick verify from `47-VALIDATION.md`:

```
mix test test/lattice_stripe/testing_test.exs test/lattice_stripe/webhook_test.exs
```

Full-phase sanity run (all 7 test files modified/created across the 5 plans):

```
mix test test/lattice_stripe/webhook_test.exs \
         test/lattice_stripe/event_notification_test.exs \
         test/lattice_stripe/webhook/fetch_test.exs \
         test/lattice_stripe/testing_test.exs \
         test/lattice_stripe/event_test.exs \
         test/lattice_stripe/object_types_test.exs \
         test/lattice_stripe/webhook/plug_test.exs \
         test/lattice_stripe/docs_truth_test.exs
```

All exit 0. `mix ci` (the phase gate) must be green before `/gsd:verify-work`.
</verification>

<success_criteria>
- ROADMAP success criterion #6 delivered: `LatticeStripe.Testing` exposes thin-event payload builders that produce wire-format payloads + matching `Stripe-Signature` headers parseable by `Webhook.parse_event_notification/4`, while existing snapshot helpers continue to work unchanged.
- `generate_thin_event_payload/3` produces a payload that round-trips through `parse_event_notification/4` end-to-end — the most load-bearing assertion in the phase, proving plans 01 + 02 + 05 are mutually consistent.
- `event_notification/1` direct-delegates to `EventNotification.from_map/1` paralleling existing typed builders.
- Wire-format regression-locked at the test layer: `"object" => "v2.core.event"` (NOT `"v2.core.event_notification"`); `"created"` as ISO 8601 string (NOT Unix integer).
- Snapshot helpers (`generate_webhook_payload/3`, `generate_webhook_event/3`) unchanged — regression-locked by backwards-compat test asserting `"object" => "event"`.
- TESTING-01 requirement is fully implemented.
</success_criteria>

<output>
Create `.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-05-SUMMARY.md` documenting: the two new public Testing functions, the roundtrip test that ties together plans 01 + 02 + 05, the snapshot-helper backwards-compat regression, and a forward note that Phase 48's canonical guide example code will use `generate_thin_event_payload/3` as its test-suite primitive.
</output>
