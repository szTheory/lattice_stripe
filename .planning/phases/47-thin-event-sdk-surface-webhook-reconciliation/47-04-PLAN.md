---
phase: 47-thin-event-sdk-surface-webhook-reconciliation
plan: 04
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - lib/lattice_stripe/webhook.ex
  - test/lattice_stripe/webhook/fetch_test.exs
autonomous: true
requirements: [THIN-02, THIN-03]
must_haves:
  truths:
    - "Adopters can call `Webhook.fetch_event/3` with a notification (or its `id`) and receive `{:ok, %Event{}}`, honoring `:api_version`, `:idempotency_key`, and explicit-client semantics (ROADMAP success criterion #3)."
    - "`fetch_event/3` issues a GET to `/v2/core/events/{id}` — NOT the v1 `/v1/events/{id}` endpoint (RESEARCH Finding 3 regression-locked)."
    - "Adopters can call `Webhook.fetch_related_object/3` and receive `{:ok, resource}` typed through `ObjectTypes.maybe_deserialize/1` dispatch — no new dispatch table (ROADMAP success criterion #4)."
    - "Unknown `related_object.type` is gated BEFORE any HTTP request — `fetch_related_object/3` returns `{:error, {:unknown_object_type, type}}` via `ObjectTypes.fetch_module/1` with Mox expectation count = 0 (D-05 fail-fast contract)."
    - "`fetch_related_object/3` returns `{:error, :no_related_object}` when `notification.related_object == nil` (snapshot-style v2 events) per D-07."
    - "`:expand` opt is honored on `fetch_related_object/3` via the existing v1.2 expand machinery in `Client.request/2` — no new expand plumbing introduced."
    - "Bang variants `fetch_event!/3` and `fetch_related_object!/3` raise `LatticeStripe.Error` for both HTTP errors and typed-error atoms (collapsed into `%Error{}` for consistency)."
  artifacts:
    - path: "lib/lattice_stripe/webhook.ex"
      provides: "`fetch_event/3` (accepts `%EventNotification{}` or `String.t() | nil` id), `fetch_event!/3`, `fetch_related_object/3`, `fetch_related_object!/3` per D-04 + D-05 + D-07"
      contains: "fetch_event"
    - path: "test/lattice_stripe/webhook/fetch_test.exs"
      provides: "Mox-driven unit tests covering THIN-02 + THIN-03 happy paths, typed errors, opt forwarding, and bang variants"
      contains: "describe \"fetch_event/3\""
  key_links:
    - from: "lib/lattice_stripe/webhook.ex (fetch_event/3)"
      to: "lib/lattice_stripe/client.ex (Client.request/2 + Request struct)"
      via: "%Request{method: :get, path: \"/v2/core/events/#{id}\"} |> Client.request(client, _) — v2-aware retrieve, NOT Event.retrieve/3 which is v1"
      pattern: "/v2/core/events/"
    - from: "lib/lattice_stripe/webhook.ex (fetch_event/3)"
      to: "lib/lattice_stripe/event.ex (Event.from_map/1)"
      via: "Resource.unwrap_singular(&Event.from_map/1) — decodes v2 payload via the Event extension from plan 01"
      pattern: "Event.from_map"
    - from: "lib/lattice_stripe/webhook.ex (fetch_related_object/3)"
      to: "lib/lattice_stripe/object_types.ex (ObjectTypes.fetch_module/1)"
      via: "typed-gate lookup BEFORE Client.request/2 — D-05 fail-fast"
      pattern: "ObjectTypes.fetch_module"
    - from: "lib/lattice_stripe/webhook.ex (fetch_related_object/3)"
      to: "lib/lattice_stripe/object_types.ex (ObjectTypes.maybe_deserialize/1)"
      via: "called on the response body after dispatch-table membership is confirmed"
      pattern: "ObjectTypes.maybe_deserialize"
---

<objective>
Ship the THIN-02 + THIN-03 fetch-after-verify primitives on `LatticeStripe.Webhook`: `fetch_event/3` (retrieves the full v2 `Event.t()` via `/v2/core/events/{id}`) and `fetch_related_object/3` (retrieves the typed underlying resource referenced by `notification.related_object` via `ObjectTypes` dispatch), plus their bang variants.

Purpose: After `parse_event_notification/4` (plan 02) verifies and decodes a thin-event payload, adopters need authoritative state. Stripe ships thin events with minimal context — adopters must fetch the canonical resource. Per RESEARCH Finding 3, `fetch_event/3` MUST call `/v2/core/events/{id}` (the v2 path) — `Event.retrieve/3` hits `/v1/events/{id}` which 404s on v2 IDs. Per D-04, both fetchers take `%Client{}` explicitly so the notification struct stays pure serializable data (safe for logs, ETS, distributed Erlang). Per D-05, unknown `related_object.type` is gated via `ObjectTypes.fetch_module/1` BEFORE any HTTP call — fail fast with a typed error, no wasted request.

Output:
- `lib/lattice_stripe/webhook.ex` (modified: add 4 public functions — `fetch_event/3`, `fetch_event!/3`, `fetch_related_object/3`, `fetch_related_object!/3`)
- `test/lattice_stripe/webhook/fetch_test.exs` (new file: Mox-driven unit tests)

Depends on plan 01 (`EventNotification`, `RelatedObject`, `Event.related_object` extension, `ObjectTypes.fetch_module/1`, fixture file must exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-RESEARCH.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-PATTERNS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-VALIDATION.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-01-SUMMARY.md

# Reference implementations (read in tasks below)
@lib/lattice_stripe/webhook.ex
@lib/lattice_stripe/event.ex
@lib/lattice_stripe/client.ex
@lib/lattice_stripe/object_types.ex
@lib/lattice_stripe/request.ex
@lib/lattice_stripe/resource.ex
@lib/lattice_stripe/error.ex
@test/support/test_helpers.ex
@test/support/fixtures/event.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `fetch_event/3` + `fetch_event!/3` to `LatticeStripe.Webhook` (v2-aware retrieve at `/v2/core/events/{id}`)</name>
  <files>lib/lattice_stripe/webhook.ex</files>
  <read_first>
    - lib/lattice_stripe/webhook.ex (current — alias block at top, `parse_event_notification/4` from plan 02; new aliases for `Client`, `Error`, `Request`, `Resource`, `Response`, `ObjectTypes`, `Event`, `EventNotification.RelatedObject` must be added)
    - lib/lattice_stripe/event.ex (`Event.retrieve/3` at `:122-127` and `Event.retrieve!/3` at `:132-135` — pipeline pattern to mirror; note this calls `/v1/events/` which `fetch_event/3` MUST NOT use)
    - lib/lattice_stripe/client.ex (`Client.request/2` signature, especially `:180-204` for per-request opts handling — `:api_version`, `:idempotency_key`, `:expand` all flow through here; `:680-740` for expand machinery)
    - lib/lattice_stripe/request.ex (`%Request{method, path, params, opts}` struct shape)
    - lib/lattice_stripe/resource.ex (`Resource.unwrap_singular/2` at `:69-90` and `Resource.unwrap_bang!/1` at `:91-93`)
    - lib/lattice_stripe/error.ex (`%LatticeStripe.Error{}` shape, `type:` field values)
    - lib/lattice_stripe/event_notification.ex (plan 01 — `id` field type `String.t() | nil`)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-PATTERNS.md section "lib/lattice_stripe/webhook.ex — fetch_event/3 + bang variant (MOD)"
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md `<decisions>` block (D-04, D-07)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-RESEARCH.md "Critical Findings — Finding 3" (the `/v2/core/events/{id}` path correction) and "Pitfall 3" (passing v2 ID to `Event.retrieve/3`)
  </read_first>
  <action>
Add the following aliases to the top alias block of `lib/lattice_stripe/webhook.ex` (group with existing aliases): `alias LatticeStripe.{Client, Error, Event, EventNotification, Request, Resource, Response}` and `alias LatticeStripe.EventNotification.RelatedObject` and `alias LatticeStripe.ObjectTypes`. Reuse the existing `EventNotification` alias added by plan 02 if it already exists.

Define `fetch_event/3` per D-04 + RESEARCH Finding 3 with THREE function clauses to handle the input shapes specified in D-04 and RESEARCH Open Question 3 (defensive `id: nil` case from D-07):

1. Defensive clause for malformed notifications (D-07 `:no_event_id` case):
   - Matches `%Client{} = _client, %EventNotification{id: nil}, _opts`
   - Returns `{:error, :no_event_id}` immediately — does NOT call `Client.request/2`

2. Notification-extract clause:
   - Matches `%Client{} = client, %EventNotification{id: id}, opts when is_binary(id)`
   - Delegates: `fetch_event(client, id, opts)`

3. Bare-id clause (the actual HTTP path):
   - Matches `%Client{} = client, id, opts when is_binary(id)`
   - Builds `%Request{method: :get, path: "/v2/core/events/#{id}", params: %{}, opts: opts}` — note path is `/v2/core/events/` per RESEARCH Finding 3, NOT `/v1/events/`
   - Pipes through `Client.request(client, request)` then `Resource.unwrap_singular(&Event.from_map/1)` — mirrors `Event.retrieve/3` pipeline exactly, swapping path + skipping the `Event.retrieve/3` delegation

Signature: `@spec fetch_event(Client.t(), EventNotification.t() | String.t(), keyword()) :: {:ok, Event.t()} | {:error, Error.t() | :no_event_id}`. Default arg on opts via the rightmost clause OR define a single 2-arity wrapper that delegates to 3-arity with `[]` — match how `Event.retrieve/3` handles this in lib/lattice_stripe/event.ex.

Define `fetch_event!/3` bang variant. Signature: `@spec fetch_event!(Client.t(), EventNotification.t() | String.t(), keyword()) :: Event.t()`. Implementation: case on `fetch_event/3` result; return on `{:ok, %Event{} = event}`; for `{:error, %Error{} = err}` use `Resource.unwrap_bang!/1` (or raise `err` directly — check `Resource.unwrap_bang!` API at lib/lattice_stripe/resource.ex:91-93); for `{:error, :no_event_id}` raise `%Error{type: :invalid_request, message: "EventNotification id is nil"}`.

Per-request opts (`:api_version`, `:idempotency_key`) flow through `Client.request/2` automatically — confirm at `lib/lattice_stripe/client.ex:180-204`. Do NOT add the `Stripe-Request-Trigger: event=<id>` header — RESEARCH Pitfall 7 explicitly defers this to a future follow-up; adding it requires `:additional_headers` opt plumbing which is not in scope for v1.5.

`@doc` block must:
- Describe `fetch_event/3` as "Retrieves the full v2 `Event.t()` for a thin-event notification" and explicitly note it calls `/v2/core/events/{id}`.
- Cross-reference `LatticeStripe.Event.retrieve/3` saying "For snapshot/v1 event IDs (returned by `construct_event/4`), use `LatticeStripe.Event.retrieve/3` instead — it hits `/v1/events/{id}` which differs from the v2 endpoint and returns a different payload shape (no `related_object`, integer `created`)."
- Per RESEARCH Open Question 2 + Pitfall 2: include a note that on a v2-fetched `Event`, the `created` field arrives as an ISO 8601 string (whereas snapshot v1 events have integer Unix timestamps) — this is a wire-format reality and must be documented for adopter clarity.
- Show the canonical usage example: `Webhook.fetch_event(client, notification)` and the `String.t()` form `Webhook.fetch_event(client, "evt_test_...")`.

This task does NOT modify `Event.@type t created` to widen from `integer() | nil` to `integer() | String.t() | nil`. RESEARCH Open Question 2 flagged this as a planner decision — the recommendation is to accept the asymmetry. We document it via the `fetch_event/3` docstring + a TODO note in this plan's SUMMARY so future plans (or a v1.5.x patch) can widen the type. The runtime behavior is correct either way because `Event.t()` uses `from_map/1` which is infallible-deserialize — Dialyzer is not in use per CLAUDE.md.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -E 'def fetch_event\(%Client\{\} = _client, %EventNotification\{id: nil\}' lib/lattice_stripe/webhook.ex` returns a match (the `:no_event_id` defensive clause).
    - Source: `grep -E 'def fetch_event\(%Client\{\} = client, %EventNotification\{id: id\}' lib/lattice_stripe/webhook.ex` returns a match (the notification-extract clause).
    - Source: `grep -E 'def fetch_event\(%Client\{\} = client, id' lib/lattice_stripe/webhook.ex` returns a match (the bare-id clause).
    - Source: `grep 'def fetch_event!' lib/lattice_stripe/webhook.ex` returns at least one match.
    - Source: `grep '"/v2/core/events/' lib/lattice_stripe/webhook.ex` returns at least one match (v2 path per RESEARCH Finding 3).
    - Source: `grep -c '"/v1/events/' lib/lattice_stripe/webhook.ex` returns `0` (must NOT use the v1 path).
    - Source: `grep '{:error, :no_event_id}' lib/lattice_stripe/webhook.ex` returns at least one match.
    - Source: `grep '@spec fetch_event' lib/lattice_stripe/webhook.ex` returns at least 2 matches (one for `/3`, one for `!/3`).
    - Source: `grep 'Event.retrieve/3' lib/lattice_stripe/webhook.ex` returns at least one match WITHIN a `@doc` block context (cross-reference to v1 path — verify via `grep -B5 -A5 'Event.retrieve/3' lib/lattice_stripe/webhook.ex` showing it inside docstring lines beginning with `#` or inside a `@doc` block; or `grep 'snapshot/v1' lib/lattice_stripe/webhook.ex` returns a match in the same area).
    - Source: `grep -E 'Stripe-Request-Trigger' lib/lattice_stripe/webhook.ex` returns `0` matches (Pitfall 7 deferral honored).
    - Behavior: `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    `fetch_event/3` (4-arity if you count the optional opts default) and `fetch_event!/3` are defined with the correct three-clause input handling, the `/v2/core/events/{id}` path is hard-coded, `Event.retrieve/3` is NOT called (cross-referenced in the docstring instead), and Pitfall 7 deferral is honored. Plan 01's `Event.related_object` extension means the response payload's `related_object` map will be decoded into a `%RelatedObject{}` — no additional plumbing needed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `fetch_related_object/3` + bang variant with D-05 typed-error gate</name>
  <files>lib/lattice_stripe/webhook.ex</files>
  <read_first>
    - lib/lattice_stripe/webhook.ex (current state after task 1; the new aliases at the top of the file are reused)
    - lib/lattice_stripe/object_types.ex (`ObjectTypes.fetch_module/1` from plan 01 — typed-gate accessor; `ObjectTypes.maybe_deserialize/1` at `:55-60` — used AFTER the gate passes; the `@object_map` defines which types are known)
    - lib/lattice_stripe/client.ex (`Client.request/2` at `:180-204` — `:expand` opt support is at `:200`; expand merging at `:680-740`)
    - lib/lattice_stripe/event_notification.ex + lib/lattice_stripe/event_notification/related_object.ex (plan 01 — `%EventNotification{related_object: %RelatedObject{type, url} | nil}` shape)
    - lib/lattice_stripe/error.ex (`%LatticeStripe.Error{}` shape for bang-variant wrapping)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-PATTERNS.md section "lib/lattice_stripe/webhook.ex — fetch_related_object/3 + bang variant (MOD)"
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md `<decisions>` block (D-05, D-07)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-RESEARCH.md "Pattern 4: Typed-error gate before HTTP (D-05)"
  </read_first>
  <action>
Define `fetch_related_object/3` per D-05 + D-07 with TWO function clauses:

1. Nil-related-object clause (D-07 `:no_related_object` case):
   - Matches `%Client{} = _client, %EventNotification{related_object: nil}, _opts`
   - Returns `{:error, :no_related_object}` immediately — does NOT call `ObjectTypes.fetch_module/1` and does NOT call `Client.request/2`

2. Typed-related-object clause (D-05 fail-fast + dispatch):
   - Matches `%Client{} = client, %EventNotification{related_object: %RelatedObject{type: type, url: url}}, opts`
   - **First**: `case ObjectTypes.fetch_module(type)` — typed gate BEFORE any HTTP
   - On `{:ok, _module}` — build `%Request{method: :get, path: url, params: %{}, opts: opts}` (use `related_object.url` verbatim — Stripe ships the path; do NOT prefix `/v1/` or transform); pipe through `Client.request(client, request)`. On `{:ok, %Response{data: raw}}` return `{:ok, ObjectTypes.maybe_deserialize(raw)}`. On `{:error, %Error{}} = error` return `error` unchanged.
   - On `:error` — return `{:error, {:unknown_object_type, type}}` — Mox expectation count for `LatticeStripe.MockTransport` is 0 (NO HTTP request issued)

Signature: `@spec fetch_related_object(Client.t(), EventNotification.t(), keyword()) :: {:ok, struct()} | {:error, Error.t() | {:unknown_object_type, String.t()} | :no_related_object}`. Add a 2-arity convenience that defaults `opts` to `[]` (mirroring the Webhook public-API convention).

`:expand` opt support is automatic — `Client.request/2` reads `:expand` from `req.opts` at lib/lattice_stripe/client.ex:200 and merges via `merge_expand/2` at `:680-740`. Tests verify it works end-to-end via Mox assertion on `req.url` containing `expand[0]=...` query string OR the `req.opts[:expand]` field being forwarded — pick whichever assertion is the cleanest given the Mox capture surface (tests likely inspect `req.opts` directly).

Define `fetch_related_object!/3` bang variant per PATTERNS.md "Bang-variant pattern":
- Case on `fetch_related_object/3` result
- `{:ok, obj}` → return `obj`
- `{:error, %Error{} = err}` → raise `err`
- `{:error, {:unknown_object_type, type}}` → raise `%LatticeStripe.Error{type: :invalid_request, message: "Unknown Stripe object type: #{type}"}`
- `{:error, :no_related_object}` → raise `%LatticeStripe.Error{type: :invalid_request, message: "EventNotification has no related_object"}`

`@doc` block must:
- Describe `fetch_related_object/3` as "Retrieves the typed underlying resource referenced by a thin-event notification's `related_object`."
- Document D-05's error contract: `{:error, {:unknown_object_type, type}}` for unknown types (with the explanation "Stripe shipped a new resource type — opening a PR to add the module to `LatticeStripe.ObjectTypes.@object_map` resolves this") and `{:error, :no_related_object}` for snapshot-style v2 events (recommend the adopter call `fetch_event/3` instead).
- Show the canonical usage example from RESEARCH "Code Examples — Fetch related object with typed-error gate (D-05)".
- List the `opts` keyword: `:expand` (list of paths to expand inline, reuses v1.2 expand machinery), `:api_version`, `:idempotency_key`.

Do NOT introduce a `:raw_on_unknown` opt or a `fetch_related_object_raw/3` escape hatch — RESEARCH "Anti-Patterns to Avoid" and CONTEXT Deferred Ideas explicitly reject this for v1.5.
Do NOT auto-fall-through to `fetch_event/3` when `related_object == nil` — D-04 / RESEARCH "Anti-Patterns to Avoid" / CONTEXT Deferred Ideas all reject this. Adopters explicitly handle the `:no_related_object` case.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -E 'def fetch_related_object\(%Client\{\} = _client, %EventNotification\{related_object: nil\}' lib/lattice_stripe/webhook.ex` returns a match (the `:no_related_object` clause).
    - Source: `grep -E 'def fetch_related_object\(\s*%Client\{\} = client,\s*%EventNotification\{related_object: %RelatedObject' lib/lattice_stripe/webhook.ex` returns a match (the typed-gate clause).
    - Source: `grep -E 'ObjectTypes.fetch_module\(type\)' lib/lattice_stripe/webhook.ex` returns a match.
    - Source: `grep -E '\{:unknown_object_type, type\}' lib/lattice_stripe/webhook.ex` returns at least one match.
    - Source: `grep -E '\{:error, :no_related_object\}' lib/lattice_stripe/webhook.ex` returns at least one match.
    - Source: `grep -E 'ObjectTypes.maybe_deserialize' lib/lattice_stripe/webhook.ex` returns at least one match.
    - Source: `grep -E 'def fetch_related_object!' lib/lattice_stripe/webhook.ex` returns at least one match.
    - Source: `grep -E '@spec fetch_related_object' lib/lattice_stripe/webhook.ex` returns at least 2 matches (`/3` and `!/3`).
    - Source: `grep -E 'fetch_related_object_raw|:raw_on_unknown' lib/lattice_stripe/webhook.ex` returns `0` matches (deferred items honored).
    - Source: ordering check — within the typed-gate clause body, `ObjectTypes.fetch_module(type)` appears BEFORE `Client.request(`. Verify via `awk '/def fetch_related_object\(.*%Client\{\} = client,/,/^  end$/' lib/lattice_stripe/webhook.ex | grep -n 'fetch_module\|Client.request' | head -5` showing `fetch_module` line number is less than `Client.request` line number.
    - Behavior: `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    `fetch_related_object/3` enforces the D-05 typed-error gate BEFORE issuing an HTTP request, returns `:no_related_object` for nil-related cases, reuses `ObjectTypes.maybe_deserialize/1` for typed deserialization without introducing a new dispatch table, and the bang variant collapses typed-error atoms into `%LatticeStripe.Error{}` for consistent exception semantics. No raw-fallback escape hatch, no auto-fall-through to `fetch_event/3`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Mox-driven unit tests at `test/lattice_stripe/webhook/fetch_test.exs`</name>
  <files>test/lattice_stripe/webhook/fetch_test.exs</files>
  <read_first>
    - test/lattice_stripe/event_test.exs (full file — the `describe "retrieve/3"` block at `:122-145` is the canonical Mox-driven HTTP-test precedent; the `describe "retrieve!/3"` block at `:151-162` is the bang-variant test precedent)
    - test/support/test_helpers.ex (full file — `test_client/0` helper at `:6-16` wires `transport: LatticeStripe.MockTransport`, `max_retries: 0`, `telemetry_enabled: false`; `ok_response/1` and `error_response/0` produce the canonical transport response shapes)
    - test/support/fixtures/event.ex (`event_map/0,1` — used to build the v2 event response body)
    - test/support/fixtures/event_notification.ex (plan 01 — `event_notification_map/0,1`, `event_notification_map_no_related_object/0`)
    - test/support/fixtures/customer.ex (or whichever customer fixture exists — needed for the `fetch_related_object/3` happy path)
    - lib/lattice_stripe/webhook.ex (from tasks 1 + 2 above — the public functions under test)
    - lib/lattice_stripe/event_notification.ex + lib/lattice_stripe/event_notification/related_object.ex (plan 01 — shapes)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-PATTERNS.md section "test/lattice_stripe/webhook/fetch_test.exs (NEW — Mox-driven HTTP tests)"
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-VALIDATION.md (THIN-02 + THIN-03 rows)
  </read_first>
  <action>
Create `test/lattice_stripe/webhook/fetch_test.exs` mirroring `test/lattice_stripe/event_test.exs`'s `describe "retrieve/3"` block structure. Top of file:

```
defmodule LatticeStripe.Webhook.FetchTest do
  use ExUnit.Case, async: true

  import Mox
  import LatticeStripe.TestHelpers
  import LatticeStripe.Test.Fixtures.Event, only: [event_map: 0, event_map: 1]
  import LatticeStripe.Test.Fixtures.EventNotification

  alias LatticeStripe.{Error, Event, EventNotification, Webhook}
  alias LatticeStripe.EventNotification.RelatedObject

  setup :verify_on_exit!
end
```

Add `describe "fetch_event/3"` block covering THIN-02 obligations from VALIDATION.md:

1. "sends GET /v2/core/events/{id} and returns {:ok, %Event{}}": build `notif = EventNotification.from_map(event_notification_map())`; `expect(LatticeStripe.MockTransport, :request, fn req -> assert req.method == :get; assert String.contains?(req.url, "/v2/core/events/#{notif.id}"); refute String.contains?(req.url, "/v1/events/"); ok_response(event_map(%{"id" => notif.id})) end)`; assert `{:ok, %Event{id: id}} = Webhook.fetch_event(client, notif)` and `id == notif.id`. The `refute String.contains?(req.url, "/v1/events/")` assertion is the RESEARCH Finding 3 regression-lock.

2. "accepts bare String.t() id form": expect on transport with assertion `String.contains?(req.url, "/v2/core/events/evt_test_123")`; call `Webhook.fetch_event(client, "evt_test_123")`; assert `{:ok, %Event{}}`.

3. "returns {:error, :no_event_id} for %EventNotification{id: nil}": call `Webhook.fetch_event(client, %EventNotification{id: nil})`. Do NOT call `expect(LatticeStripe.MockTransport, :request, _)` — Mox `setup :verify_on_exit!` will verify no transport call was made. Assert `{:error, :no_event_id}`.

4. "honors :api_version opt": `expect` on transport with assertion `req.opts[:api_version] == "2024-09-30.acacia"` (or whatever version string is passed); call `Webhook.fetch_event(client, notif, api_version: "2024-09-30.acacia")`.

5. "honors :idempotency_key opt": `expect` with assertion `req.opts[:idempotency_key] == "ik_test_phase47"`; call with `idempotency_key: "ik_test_phase47"`.

6. "decodes v2 payload with related_object map into %RelatedObject% on the returned Event": build the response body via `event_map(%{"related_object" => %{"id" => "cus_1", "type" => "customer", "url" => "/v1/customers/cus_1"}})`; assert the returned `%Event{related_object: %RelatedObject{id: "cus_1", type: "customer"}}`.

Add `describe "fetch_event!/3"` block:

7. "returns Event on happy path"
8. "raises %LatticeStripe.Error{} on HTTP error response": `expect` returns `error_response()`; assert `assert_raise LatticeStripe.Error, fn -> Webhook.fetch_event!(client, "evt_test_123") end`.

Add `describe "fetch_related_object/3"` block covering THIN-03 obligations from VALIDATION.md:

9. "returns {:ok, %Customer{}} for known related_object.type": build `notif` with `related_object: %{"id" => "cus_1", "type" => "customer", "url" => "/v1/customers/cus_1"}`; `expect` on transport with `req.url` containing `/v1/customers/cus_1` (verbatim from `related_object.url`); response body is the customer fixture map; assert `{:ok, %LatticeStripe.Customer{id: "cus_1"}}`.

10. "returns {:error, {:unknown_object_type, type}} BEFORE any HTTP call (Mox expectation count = 0)" — D-05 fail-fast regression: build `notif` with `related_object: %{"id" => "x", "type" => "v2.core.account", "url" => "/v2/core/accounts/x"}` (v2.core.account is NOT in `@object_map` per plan 01 task 2 acceptance criteria). Do NOT call `expect(LatticeStripe.MockTransport, :request, _)`. Assert `{:error, {:unknown_object_type, "v2.core.account"}} = Webhook.fetch_related_object(client, notif)`. `setup :verify_on_exit!` enforces zero transport calls.

11. "returns {:error, :no_related_object} when related_object is nil": build `notif = EventNotification.from_map(event_notification_map_no_related_object())`; do NOT expect on transport; assert `{:error, :no_related_object}`.

12. "honors :expand opt": `expect` with `req.opts[:expand] == ["customer", "invoice"]`; call `Webhook.fetch_related_object(client, notif, expand: ["customer", "invoice"])`.

13. "uses related_object.url verbatim as request path": build `notif` with `related_object.url = "/v1/customers/cus_1"`; `expect` with `req.path == "/v1/customers/cus_1"` (or `String.contains?(req.url, "/v1/customers/cus_1")` depending on the Request struct shape — verify via test_helpers.ex how req fields look).

Add `describe "fetch_related_object!/3"` block:

14. "raises %LatticeStripe.Error{type: :invalid_request} on {:error, {:unknown_object_type, _}}": call with unknown type; assert `assert_raise LatticeStripe.Error, ~r/Unknown Stripe object type/, fn -> ... end`.

15. "raises %LatticeStripe.Error{type: :invalid_request} on {:error, :no_related_object}": call with nil-related notif; assert `assert_raise LatticeStripe.Error, ~r/no related_object/, fn -> ... end`.

16. "raises %LatticeStripe.Error{} on HTTP error response": expect returns `error_response()`; assert raise.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/webhook/fetch_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `test/lattice_stripe/webhook/fetch_test.exs` exists and `defmodule LatticeStripe.Webhook.FetchTest do` is on a line.
    - Source: `grep -c 'describe "fetch_event/3"' test/lattice_stripe/webhook/fetch_test.exs` returns at least `1`.
    - Source: `grep -c 'describe "fetch_event!/3"' test/lattice_stripe/webhook/fetch_test.exs` returns at least `1`.
    - Source: `grep -c 'describe "fetch_related_object/3"' test/lattice_stripe/webhook/fetch_test.exs` returns at least `1`.
    - Source: `grep -c 'describe "fetch_related_object!/3"' test/lattice_stripe/webhook/fetch_test.exs` returns at least `1`.
    - Source: `grep -E 'refute.*"/v1/events/"|refute String\.contains\?\(req\.url, "/v1/events/"\)' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match (RESEARCH Finding 3 regression-lock).
    - Source: `grep '/v2/core/events/' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep ':no_event_id' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep ':no_related_object' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep ':unknown_object_type' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep ':api_version' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep ':idempotency_key' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep ':expand' test/lattice_stripe/webhook/fetch_test.exs` returns at least one match.
    - Source: `grep 'setup :verify_on_exit!' test/lattice_stripe/webhook/fetch_test.exs` returns a match.
    - Behavior: `mix test test/lattice_stripe/webhook/fetch_test.exs` exits 0.
    - Behavior: `mix test test/lattice_stripe/webhook_test.exs test/lattice_stripe/webhook/fetch_test.exs` exits 0 (combined run — no cross-test interference).
    - Behavior: `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    THIN-02 + THIN-03 are fully regression-locked: 16 tests cover happy paths, all typed-error cases, opt forwarding (`:api_version`, `:idempotency_key`, `:expand`), the v2 path regression (RESEARCH Finding 3), the D-05 fail-fast contract (no HTTP for unknown types, verified via Mox expectation count), and the bang-variant `%Error{}` collapse semantics. The `Webhook.FetchTest` module runs with `async: true` and `setup :verify_on_exit!`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter-provided `%EventNotification{}` → fetcher functions | Notification is opaque/pre-verified by `parse_event_notification/4` (plan 02); fetchers don't re-verify. Pure data — no `%Client{}` embedded (D-04 invariant). |
| Stripe API → `Client.request/2` (outbound) | Standard HTTPS via Finch transport; retries + idempotency handled by `Client.request/2` (v1.2 surface — unchanged). |
| Stripe API → `Event.from_map/1` (response decode) | Unknown fields go to `:extra`; new related_object shapes decoded via `RelatedObject.from_map(nil)`-safe pattern. |
| Adopter → `fetch_related_object/3` with crafted `related_object.url` | URL comes verbatim from Stripe in the notification — but if a thin-event payload is mis-handled (e.g., parse_event_notification/4 was not called and the adopter built `%EventNotification{}` manually), a malicious URL could redirect. Mitigation: `parse_event_notification/4` is the only sanctioned producer of `%EventNotification{}` — document this expectation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-11 | Tampering | `fetch_event/3` called with v2 ID but routed to `/v1/events/` (404 + adopter confusion) | mitigate | Hard-coded `/v2/core/events/#{id}` path; regression-locked by test asserting `refute String.contains?(req.url, "/v1/events/")` (RESEARCH Finding 3 + Pitfall 3). |
| T-47-12 | Denial of service | Stripe ships new resource type → `fetch_related_object/3` makes HTTP call, dispatch crashes adopter `case {:ok, %Customer{}}` clause at 3am | mitigate | D-05 typed-error gate via `ObjectTypes.fetch_module/1` BEFORE HTTP; returns `{:error, {:unknown_object_type, _}}`. Regression-locked by Mox-expectation-count-=-0 test. |
| T-47-13 | Information disclosure | `%Client{}` (with API key) leaks to logs via `%EventNotification{}` | mitigate | D-04 invariant: notification struct contains no `%Client{}`. Plan 01's `EventNotificationTest` Inspect-regression test asserts no `"%Client"`/`"sk_"`/`"api_key"` substrings in inspect output. Pattern reinforced here: `fetch_*/3` take `%Client{}` explicitly as the first arg, not stored on the notification. |
| T-47-14 | Tampering | Adopter constructs malicious `%EventNotification{related_object: %RelatedObject{url: attacker_url}}` → `fetch_related_object/3` fetches attacker's URL | accept | `%EventNotification{}` is only produced by `parse_event_notification/4` (which verifies HMAC signature against Stripe's secret) or by direct construction (adopter's responsibility). Documented in `EventNotification` moduledoc. Future hardening could add an `:expected_path_prefix` opt; out of v1.5 scope. |
| T-47-15 | Information disclosure | Telemetry/logging surfaces leaking sensitive opts (`:idempotency_key` shouldn't be sensitive; `:api_version` is metadata) | accept | `Client.request/2` is unchanged; existing telemetry posture (lib/lattice_stripe/client.ex span metadata) is the v1.2 contract — no change in this plan. |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Phase 47 uses only already-pinned mix.lock deps. |
</threat_model>

<verification>
Per-plan quick verify from `47-VALIDATION.md`:

```
mix test test/lattice_stripe/webhook/fetch_test.exs
```

Plus a combined sanity run:

```
mix test test/lattice_stripe/webhook_test.exs test/lattice_stripe/webhook/fetch_test.exs test/lattice_stripe/event_test.exs
```

All exit 0. `mix compile --warnings-as-errors` exits 0.
</verification>

<success_criteria>
- ROADMAP success criterion #3 delivered: adopters can call `Webhook.fetch_event/2` (with default opts, or `/3` with opts) with a notification or its `id` and receive `{:ok, %Event{}}`, honoring `:api_version`, `:idempotency_key`, and explicit-client semantics.
- ROADMAP success criterion #4 delivered: adopters can call `Webhook.fetch_related_object/2` and receive `{:ok, resource}` typed via the existing `ObjectTypes` dispatch — no new dispatch table introduced.
- RESEARCH Finding 3 encoded in source (`/v2/core/events/{id}` path) AND regression-locked in test (`refute /v1/events/`).
- D-05 fail-fast contract regression-locked: typed-error gate fires BEFORE any HTTP request, verified via Mox expectation count = 0 in the unknown-type test.
- D-07 typed-error atoms (`:no_event_id`, `:no_related_object`, `{:unknown_object_type, type}`) all returned by the appropriate functions, regression-locked by tests.
- `:expand` opt support flows through existing v1.2 expand machinery — no new plumbing.
- Bang variants collapse all error paths into `%LatticeStripe.Error{}` for consistent exception semantics.
- THIN-02 + THIN-03 requirements are fully implemented and regression-locked.
</success_criteria>

<output>
Create `.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-04-SUMMARY.md` documenting: the four new public functions, the three-clause input handling of `fetch_event/3`, the two-clause D-05 gate on `fetch_related_object/3`, exact path string `/v2/core/events/{id}` (encoded RESEARCH Finding 3), all 16 test cases, and a forward note on RESEARCH Open Question 2 (whether to widen `Event.@type t created` to `integer() | String.t() | nil` for v2-fetched events) flagged for the executor to surface in the summary.
</output>
