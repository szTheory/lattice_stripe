---
phase: 77-adopter-edge-profiles-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs
  - test_apps/phoenix_adopter/README.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [ADOPT-03, ADOPT-04, ADOPT-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ADOPT-03, D-01, D-02: `b2b_invoice_profile_test.exs` runs independently in the existing Phoenix adopter and proves Invoice.retrieve/3 sends Stripe-Version 2026-05-27.dahlia and returns a typed Invoice with amount_paid_off_stripe; the named test is the executable proof."
    - "ADOPT-04, D-03: A synthetic 400 Invoice retrieval returns {:error, %LatticeStripe.Error{type: :invalid_request_error, status: 400}} in a named B2B profile test."
    - "ADOPT-04, D-03: The complete adopter suite still runs core_flow_test.exs, whose signed and tampered raw-body webhook cases prove verification before handler dispatch."
    - "ADOPT-05, D-04: A dedicated adopter CI job on Elixir 1.19 / OTP 28 resolves the nested lock with mix deps.get --check-locked, runs mix test --warnings-as-errors from test_apps/phoenix_adopter, and is required by ci-gate."
    - "D-01, D-04: The nested README names each profile's focused test command and one full-suite command, and states the synthetic proof boundary."
  artifacts:
    - path: test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs
      provides: "Independently selectable typed invoice and structured error contract"
    - path: .github/workflows/ci.yml
      provides: "One required nested adopter CI lane on the primary toolchain"
    - path: test_apps/phoenix_adopter/README.md
      provides: "Profile selectors and complete local command"
  key_links:
    - from: "b2b_invoice_profile_test.exs"
      to: "LatticeStripe.Invoice.retrieve/3"
      via: "Explicit synthetic Client and existing PhoenixAdopter.MockTransport, including request version header and typed decode"
    - from: "adopter CI job"
      to: "nested Mix project and ci-gate"
      via: "working-directory on both dependency and test steps, plus needs/result/loop additions"
  assumptions:
    - id: EDGE-77-ADOPT-03
      status: flagged
      statement: "Spec-less edge probe left ADOPT-03 unclassified and unresolved. The selected three profile contracts follow D-01/D-02 and source-backed SDK operations; no extra edge classification is claimed."
    - id: EDGE-77-ADOPT-04
      status: flagged
      statement: "Spec-less edge probe left ADOPT-04 unclassified and unresolved. Named synthetic error, pagination, idempotency, and existing raw-webhook tests cover the selected seams; live Stripe behavior remains unproven."
    - id: EDGE-77-ADOPT-05
      status: flagged
      statement: "Spec-less edge probe left ADOPT-05 unclassified and unresolved. The local nested command and workflow definition are planned evidence; remote CI success must be observed after execution."
  prohibitions:
    - statement: "The adopter proof must not present a synthetic Invoice read or webhook receipt as settled billing, durable reconciliation, or live Stripe delivery."
      status: unresolved
      flagged: true
    - statement: "The adopter suite and CI must not require real credentials, production adopter data, or a live Stripe service."
      status: unresolved
      flagged: true
---

<objective>
Make one typed B2B invoice contract independently runnable in the shared Phoenix adopter and connect the complete nested suite to a required CI lane.

Purpose: Establish the Phase 77 end-to-end tracer from a real public SDK call through synthetic transport, typed decode, nested Mix execution, and the aggregate CI gate (ADOPT-03 through ADOPT-05; D-01 through D-04).
Output: B2B profile test, dedicated adopter CI job, and profile instructions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-PATTERNS.md
@.planning/phases/77-adopter-edge-profiles-and-ci/COVERAGE.md
@.planning/phases/76-phoenix-adopter-core-flow/76-01-SUMMARY.md
@.agents/skills/lattice-verification-policy/SKILL.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove versioned typed Invoice retrieval in the shared adopter and run it through CI</name>
  <files>test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs, .github/workflows/ci.yml</files>
  <read_first>.planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md D-01 through D-04; .planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md Pattern 1, Validation Architecture, Security Domain; .planning/phases/77-adopter-edge-profiles-and-ci/77-PATTERNS.md B2B and CI analogs; test_apps/phoenix_adopter/test/core_flow_test.exs; test_apps/phoenix_adopter/test/test_helper.exs; test_apps/phoenix_adopter/mix.exs; test/lattice_stripe/invoice_test.exs versioned field test; lib/lattice_stripe/invoice.ex retrieve/3 and amount_paid_off_stripe; .github/workflows/ci.yml existing primary toolchain and ci-gate.</read_first>
  <behavior>
    - Named `versioned invoice retrieval exposes typed off-Stripe amount` test issues one Invoice.retrieve/3 call from the adopter with `stripe_version: "2026-05-27.dahlia"`, checks GET /v1/invoices/{synthetic_id} and the exact stripe-version request header, returns synthetic invoice JSON, and matches %Invoice{amount_paid: 300, amount_paid_off_stripe: 700}.
    - The nested full-suite command executes this new test alongside the Phase 76 core test under the same Phoenix application and Mox test helper.
  </behavior>
  <action>Per D-01 and D-02, add one ordinary ExUnit profile file inside the existing adopter app. Build an explicit LatticeStripe.Client using the app's visibly synthetic API key, PhoenixAdopter.MockTransport, max_retries: 0, and telemetry_enabled: false; use Mox expect/3 and setup :verify_on_exit! to inspect method, path, and version header and return a synthetic Stripe-shaped Invoice response. Use Invoice.retrieve/3 with the request-level version option; assert the typed integer reconciliation fields without changing the SDK default version or asserting webhook availability for the field. Per D-04, add one `adopter` workflow job using the existing checkout and erlef/setup-beam pinned actions, Elixir 1.19 and OTP 28. Give dependency resolution and full suite steps `working-directory: test_apps/phoenix_adopter`; execute `mix deps.get --check-locked` then `mix test --warnings-as-errors`. Add the job to ci-gate `needs`, its result environment, and its required-lane loop. Keep this job free of Stripe secrets and services. The tracer is complete when the same nested command used by CI runs the typed profile and the existing core webhook test locally.</action>
  <verify>
    <automated>cd test_apps/phoenix_adopter && mix deps.get --check-locked && mix test --warnings-as-errors</automated>
    <fails_when>Nonzero exit from a lockfile mismatch, dependency fetch, compilation, failed ExUnit assertion, or warning treated as an error; or the ExUnit summary reports 0 tests.</fails_when>
  </verify>
  <acceptance_criteria>The named typed Invoice test passes; workflow job uses the nested lock and full-suite command on 1.19/28 and appears in all three ci-gate dependency checks.</acceptance_criteria>
  <done>The B2B success test passes through the public Invoice API and host Mox transport, and the dedicated CI job executes the exact nested full-suite command as a required ci-gate dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the structured Invoice error and document profile selection</name>
  <files>test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs, test_apps/phoenix_adopter/README.md</files>
  <read_first>test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs from Task 1; test_apps/phoenix_adopter/README.md; test_apps/phoenix_adopter/test/core_flow_test.exs signed and tampered webhook tests; lib/lattice_stripe/error.ex error struct; test/lattice_stripe/client/response_decoding_test.exs invalid-request response; .planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md D-03/D-04.</read_first>
  <behavior>
    - Named `invalid invoice retrieval returns structured SDK error` test responds with synthetic HTTP 400 and Stripe-shaped error JSON, then asserts {:error, %LatticeStripe.Error{type: :invalid_request_error, status: 400}}.
    - README gives focused B2B, usage, and Connect file commands once those files exist and retains one complete local suite command.
  </behavior>
  <action>Per D-03, add one failure case through the same explicit Client and Mox transport; assert the stable Error tuple rather than mapping it into application billing policy. Keep the existing signed and tampered raw-body webhook tests in core_flow_test.exs as the shared webhook-verification proof, and ensure the full nested suite includes them. Per D-01/D-04, extend the adopter README with the three selected profile filenames and their independent `mix test test/<file>.exs` invocations, then retain `cd test_apps/phoenix_adopter && mix test` as the obvious complete local command; name `mix deps.get --check-locked` as the fresh-checkout prerequisite and state that CI uses warnings-as-errors. Explain the typed Invoice amount's API version, synthetic fixture scope, and distinction between an SDK response or verified event and durable reconciliation. The usage and Connect files are created by later plans, so document their agreed file names exactly.</action>
  <verify>
    <automated>cd test_apps/phoenix_adopter && mix test test/b2b_invoice_profile_test.exs</automated>
    <fails_when>Nonzero exit from a compile error or failed ExUnit assertion, or the focused ExUnit summary reports 0 tests.</fails_when>
  </verify>
  <acceptance_criteria>The focused file passes success and synthetic 400 cases; README names the agreed focused paths and full nested command with the synthetic scope stated accurately.</acceptance_criteria>
  <done>The B2B file runs independently with typed success and structured 400 failure, and the README identifies all profile selectors and complete nested suite invocation without claiming live or durable billing proof.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

This plan produces the B2B profile test, required adopter CI job, and updated nested README. Plans 77-02 and 77-03 add the usage and Connect profile tests named in that README. The phase-local API coverage decision is recorded in `COVERAGE.md`.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter test to Stripe SDK transport | Explicit Client reaches host Mox transport; fixture bytes stand in for an untrusted Stripe response. |
| Raw webhook request to host handler | Existing Endpoint verifies original request bytes before dispatching a typed event. |
| GitHub workflow to nested Mix project | CI resolves the adopter's own lockfile and runs synthetic tests on the selected toolchain. |

## STRIDE Threat Register (ASVS level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-01 | Spoofing/Tampering | Existing webhook Endpoint | high | mitigate | Keep core_flow_test.exs signed-success and changed-body rejection in the nested full suite; do not change Webhook.Plug-before-Parsers ordering. |
| T-77-02 | Information disclosure | Adopter fixture and CI | high | mitigate | Use only synthetic key, IDs, invoice data, and Mox transport; CI job has no credential env or Stripe service, and its full suite fails on unexpected transport calls. |
| T-77-03 | Tampering | Invoice versioned response | medium | mitigate | Assert the outgoing Stripe-Version header and typed invoice fields against a controlled synthetic payload so the version-dependent contract is explicit. |
</threat_model>

<verification>
The focused B2B command proves Invoice success and structured error. The full nested warnings-as-errors command proves the new profile coexists with the core signed and tampered webhook tests. Inspect the workflow job's `needs`, result env, and required-lane loop additions; the existing actionlint CI lane validates workflow syntax remotely.
</verification>

<success_criteria>
Maintainers can select the B2B profile independently, observe the typed versioned reconciliation amount and structured error, and run the same full nested suite that the required CI job executes without live credentials.
</success_criteria>

<output>
Create `.planning/phases/77-adopter-edge-profiles-and-ci/77-01-SUMMARY.md` with named test and CI configuration evidence for ADOPT-03 through ADOPT-05; do not claim remote CI success until observed.
</output>
