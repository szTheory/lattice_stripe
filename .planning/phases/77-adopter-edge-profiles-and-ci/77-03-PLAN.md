---
phase: 77-adopter-edge-profiles-and-ci
plan: 03
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - test_apps/phoenix_adopter/test/connect_context_profile_test.exs
autonomous: true
requirements: [ADOPT-03, ADOPT-04, ADOPT-05]
estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ADOPT-03, D-01, D-02: `connect_context_profile_test.exs` runs independently in the shared adopter; one platform Client makes sequential Balance.retrieve/2 calls for two synthetic connected accounts, and each Mox expectation sees only its request's stripe-account header and a typed Balance result."
    - "ADOPT-04, D-03: A named Connect test proves per-request stripe_account: nil suppresses a client-level account header, while a later request on the same Client still sends its selected account value."
    - "ADOPT-05, D-04: The full nested mix test --warnings-as-errors command includes Connect, usage, B2B, and Phase 76 core/webhook tests without live credentials or production data."
  artifacts:
    - path: test_apps/phoenix_adopter/test/connect_context_profile_test.exs
      provides: "Independently selectable typed Connect Balance and request-scoped tenant-header proof"
  key_links:
    - from: "Connect profile"
      to: "LatticeStripe.Balance.retrieve/2"
      via: "One explicit platform Client and per-request stripe_account option"
    - from: "Balance.retrieve/2"
      to: "PhoenixAdopter.MockTransport"
      via: "Exact stripe-account header assertions and typed synthetic Balance decode"
  assumptions:
    - id: EDGE-77-ADOPT-03
      status: flagged
      statement: "Spec-less edge probe left ADOPT-03 unclassified and unresolved; Balance.retrieve/2 is the source-backed Connect contract selected for this profile, not a platform onboarding flow."
    - id: EDGE-77-ADOPT-04
      status: flagged
      statement: "Spec-less edge probe left ADOPT-04 unclassified and unresolved; the sequential transport assertions cover the selected tenant routing seam without certifying application authorization policy."
    - id: EDGE-77-ADOPT-05
      status: flagged
      statement: "Spec-less edge probe left ADOPT-05 unclassified and unresolved; the local full-suite result is executable evidence while remote CI success requires the later workflow run."
  prohibitions:
    - statement: "The Connect profile must not make tenant routing depend on a mutable global client or persistent cross-request state."
      status: unresolved
      flagged: true
    - statement: "The adopter proof must not imply that a request-scoped account header alone implements platform tenant authorization or compliance policy."
      status: unresolved
      flagged: true
---

<objective>
Add an independently selectable Connect tenant-context profile that proves typed Balance reads stay scoped to the account selected on each request.

Purpose: Establish ADOPT-03/ADOPT-04 account-header behavior at the host/SDK transport seam and keep it inside the ADOPT-05 complete synthetic suite (D-01 through D-04).
Output: One focused Connect ExUnit profile file.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-PATTERNS.md
@.planning/phases/77-adopter-edge-profiles-and-ci/COVERAGE.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route two typed Balance reads to two accounts through one Client</name>
  <files>test_apps/phoenix_adopter/test/connect_context_profile_test.exs</files>
  <read_first>.planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md D-01/D-02; .planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md Pattern 4 and Pitfall 3; .planning/phases/77-adopter-edge-profiles-and-ci/77-PATTERNS.md Connect analog; test_apps/phoenix_adopter/test/test_helper.exs; test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs client construction; test/lattice_stripe/balance_test.exs retrieve/2 typed fixture; test/lattice_stripe/client_stripe_account_header_test.exs override behavior; lib/lattice_stripe/balance.ex retrieve/2; lib/lattice_stripe/client/request_builder.ex account precedence.</read_first>
  <behavior>
    - Named `connected balances use only their per-request tenant header` test constructs one platform Client with a synthetic key and existing host Mox transport, and makes sequential Balance.retrieve/2 calls with acct_connected_customer_a and acct_connected_customer_b.
    - Each Mox expectation inspects GET /v1/balance and asserts exactly its selected stripe-account header and the absence of the other account value; each returned body decodes to a typed %LatticeStripe.Balance{}.
  </behavior>
  <action>Per D-01/D-02, create the Connect profile as a separate ExUnit file in the same nested adopter. Import Mox, use setup :verify_on_exit!, and build one explicit Client with the synthetic key, PhoenixAdopter.MockTransport, max_retries: 0, and telemetry_enabled: false. Make sequential Balance.retrieve/2 calls with two different `stripe_account:` options on that same Client. In each Mox expectation assert GET /v1/balance, the exact selected `stripe-account` header, and absence of the other account's header; return a small source-shaped synthetic Balance payload and assert typed Balance results. Keep account selection at the request option rather than creating one client per tenant or constructing headers manually.</action>
  <verify>
    <automated>cd test_apps/phoenix_adopter && mix test test/connect_context_profile_test.exs</automated>
    <fails_when>Nonzero exit from a compile error, unmet Mox expectation, or failed ExUnit assertion; or the focused ExUnit summary reports 0 tests.</fails_when>
  </verify>
  <acceptance_criteria>The named two-account test observes separate exact Stripe-Account headers and typed Balance results on one Client.</acceptance_criteria>
  <done>The focused Connect file proves two sequential account-scoped Balance reads on one Client with distinct request headers and typed decoded results.</done>
</task>

<task type="auto">
  <name>Task 2: Prove explicit account suppression and finish the complete adopter gate</name>
  <files>test_apps/phoenix_adopter/test/connect_context_profile_test.exs</files>
  <read_first>test_apps/phoenix_adopter/test/connect_context_profile_test.exs from Task 1; .planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md D-02 through D-04; test/lattice_stripe/client_stripe_account_header_test.exs per-request nil suppression; lib/lattice_stripe/client/request_builder.ex maybe_add_stripe_account/2; test_apps/phoenix_adopter/test/core_flow_test.exs webhook verification tests; .github/workflows/ci.yml adopter job from Plan 77-01.</read_first>
  <behavior>
    - Named `nil request context suppresses client account without contaminating next request` test uses a Client with a synthetic client-level account, makes Balance.retrieve(client, stripe_account: nil) and observes no stripe-account header, then makes Balance.retrieve(client, stripe_account: "acct_connected_customer_b") and observes only that account header.
    - The complete nested suite runs core, B2B, usage, and Connect cases together with warnings treated as errors.
  </behavior>
  <action>Per D-02/D-03, extend the profile with one sequence testing the documented explicit-nil override. Use the same Client instance for both calls, make two Mox expectations, and assert the first outgoing request omits the account header while the second sends only its per-request account. Match typed Balance results so this remains a public resource proof. Per D-04, run the complete nested adopter suite using the command installed in the dedicated CI job; inspect its output for all four test files and retain the existing signed and tampered raw webhook assertions. If CI has not run remotely yet, report the local result and leave remote status unclaimed.</action>
  <verify>
    <automated>cd test_apps/phoenix_adopter && mix test --warnings-as-errors</automated>
    <fails_when>Nonzero exit from compilation, failed ExUnit assertion, or warning treated as an error; or the ExUnit summary reports 0 tests.</fails_when>
  </verify>
  <acceptance_criteria>The named nil-suppression test observes an absent then selected account header; the full core/B2B/usage/Connect suite passes with warnings as errors.</acceptance_criteria>
  <done>The Connect file proves account override and nil suppression without cross-request header bleed, and the complete adopter suite passes locally under the same command as CI.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

This plan produces the Connect tenant-context profile test. Together, Plans 77-01 through 77-03 produce one shared adopter with three independently selectable profiles and one complete CI suite.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host-selected tenant to SDK request builder | The request-scoped stripe_account option determines the Stripe-Account header on each outbound call. |
| SDK transport to typed Balance result | Mox supplies synthetic Balance JSON and the SDK decoder returns the public type. |

## STRIDE Threat Register (ASVS level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-07 | Elevation of privilege/Information disclosure | Connect request account context | high | mitigate | On one Client, assert exact selected account headers for sequential Balance calls, absence of the other account value, and explicit nil suppression before a subsequent scoped call. |
| T-77-08 | Spoofing | Synthetic connected accounts | medium | mitigate | Use fixed test-only account IDs and inspect the SDK-generated header at Mox; no account ID comes from an unauthenticated host endpoint in this profile. |
</threat_model>

## Source Coverage Audit

| Source | Item | Plan | Status | Evidence path |
|--------|------|------|--------|---------------|
| GOAL | One adopter app proves distinct selected Stripe contracts with repeatable CI | 77-01, 77-02, 77-03 | COVERED | Three focused files; nested full suite required by CI |
| REQ | ADOPT-03 opt-in B2B, usage, Connect profiles | 77-01, 77-02, 77-03 | COVERED | Invoice.retrieve/3, MeterEventSummary.stream!/4, Balance.retrieve/2 |
| REQ | ADOPT-04 error, pagination/streaming, idempotency, webhook verification | 77-01, 77-02, 77-03 | COVERED | Invoice 400 Error; two summary pages; dual meter-event keys; existing core signed/tampered webhook tests |
| REQ | ADOPT-05 deterministic complete CI suite | 77-01, 77-02, 77-03 | COVERED | Dedicated nested adopter job and same full-suite command after expansions |
| RESEARCH | Versioned typed Invoice field and structured error | 77-01 | COVERED | Header, typed amount, 400 tuple |
| RESEARCH | Two-page summary stream with filters and bounded fixture | 77-02 | COVERED | Exact cursor and typed rows |
| RESEARCH | Meter-event identifier and HTTP idempotency | 77-02 | COVERED | Distinct body/header assertions |
| RESEARCH | Request-scoped Connect header, override and nil suppression | 77-03 | COVERED | Sequential one-Client Balance calls |
| RESEARCH | Synthetic Mox seam, raw webhook verification, nested lock and primary CI toolchain | 77-01, 77-02, 77-03 | COVERED | Shared host, inherited core tests, one CI job |
| CONTEXT | D-01 one app and independent profiles | 77-01, 77-02, 77-03 | COVERED | Three focused ExUnit files in one nested app |
| CONTEXT | D-02 distinct typed invoice, usage stream, Connect account contracts | 77-01, 77-02, 77-03 | COVERED | Public SDK calls at transport seam |
| CONTEXT | D-03 shared error/idempotency/webhook boundaries and synthetic-only scope | 77-01, 77-02, 77-03 | COVERED | Named tests and phase prohibitions |
| CONTEXT | D-04 one dedicated CI job, nested lock, full suite, local command | 77-01, 77-02, 77-03 | COVERED | Workflow, README, full suite checks |

Deferred separate apps, business workflows, persistence, live Stripe, production data, and additional resource families are excluded from the audit by CONTEXT and COVERAGE.md. The spec-less edge probe's three unclassified/unresolved requirements are recorded as flagged assumptions in the plans; this table does not resolve that classification.

<verification>
Run the focused Connect file and then the complete nested warnings-as-errors suite. The complete suite must include all profile files and the Phase 76 core file; the workflow's required `adopter` job should use that same nested command and be included in ci-gate.
</verification>

<success_criteria>
One platform Client can retrieve typed balances for separate connected accounts without account-header bleed, the focused Connect profile runs independently, and the complete synthetic adopter suite passes under the CI command.
</success_criteria>

<output>
Create `.planning/phases/77-adopter-edge-profiles-and-ci/77-03-SUMMARY.md` with named Connect and complete-suite evidence. Preserve the flagged spec-less probe assumptions and report remote CI status only after observation.
</output>
