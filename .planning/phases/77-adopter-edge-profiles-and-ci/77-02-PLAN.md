---
phase: 77-adopter-edge-profiles-and-ci
plan: 02
type: execute
wave: 2
depends_on: [77-01]
files_modified:
  - test_apps/phoenix_adopter/test/usage_reconciliation_profile_test.exs
autonomous: true
requirements: [ADOPT-03, ADOPT-04, ADOPT-05]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ADOPT-03, ADOPT-04, D-01, D-02: `usage_reconciliation_profile_test.exs` runs independently in the shared adopter and proves MeterEventSummary.stream!/4 consumes exactly two synthetic pages, carries page one's last ID as starting_after, preserves customer and aligned window filters, and yields typed summaries."
    - "ADOPT-04, D-03: A named usage test proves MeterEvent.create/3 sends a stable body identifier and a separate HTTP idempotency-key header through the existing Mox transport and decodes a typed response."
    - "ADOPT-05, D-04: The complete nested mix test --warnings-as-errors command includes both usage tests and the earlier B2B/core cases without a live service."
  artifacts:
    - path: test_apps/phoenix_adopter/test/usage_reconciliation_profile_test.exs
      provides: "Independently selectable two-page typed summary stream and dual-idempotency request proof"
  key_links:
    - from: "usage profile stream test"
      to: "LatticeStripe.Billing.MeterEventSummary.stream!/4"
      via: "Mox page responses with has_more and exact second-request cursor/filter assertions"
    - from: "usage profile event test"
      to: "LatticeStripe.Billing.MeterEvent.create/3"
      via: "Encoded identifier body field and idempotency-key header observed at the transport seam"
  assumptions:
    - id: EDGE-77-ADOPT-03
      status: flagged
      statement: "Spec-less edge probe left ADOPT-03 unclassified and unresolved; the selected usage stream and event calls are source-backed SDK contracts, not production reconciliation policy."
    - id: EDGE-77-ADOPT-04
      status: flagged
      statement: "Spec-less edge probe left ADOPT-04 unclassified and unresolved; these synthetic two-page and dual-key cases prove only the selected pagination and request boundaries."
  prohibitions:
    - statement: "The usage profile must not present accepted meter events or eventually consistent summary rows as settled billing totals or durable reconciliation."
      status: unresolved
      flagged: true
    - statement: "The usage profile must not accumulate an unbounded stream or turn the host harness into a billing policy engine."
      status: unresolved
      flagged: true
---

<objective>
Add an independently selectable usage reconciliation profile that proves the SDK's cursor stream and distinct meter-event idempotency contracts.

Purpose: Give adopters executable ADOPT-03/ADOPT-04 usage boundary proof through the same Phase 76 host transport, while preserving the complete ADOPT-05 CI suite (D-01 through D-04).
Output: One focused ExUnit profile file with two named contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-PATTERNS.md
@.planning/phases/77-adopter-edge-profiles-and-ci/COVERAGE.md
@.planning/phases/77-adopter-edge-profiles-and-ci/77-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Traverse two typed usage-summary pages from the adopter</name>
  <files>test_apps/phoenix_adopter/test/usage_reconciliation_profile_test.exs</files>
  <read_first>.planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md D-01/D-02; .planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md Pattern 2 and Pitfall 2; .planning/phases/77-adopter-edge-profiles-and-ci/77-PATTERNS.md usage profile analog; test_apps/phoenix_adopter/test/test_helper.exs; test_apps/phoenix_adopter/test/b2b_invoice_profile_test.exs for explicit client setup; test/lattice_stripe/billing/meter_event_summary_pagination_test.exs two-page fixture and query assertions; lib/lattice_stripe/billing/meter_event_summary.ex stream!/4 required params and typed fields.</read_first>
  <behavior>
    - Named `usage summary stream traverses two pages with a stable cursor` test constructs one explicit synthetic Client and a customer-specific, UTC-aligned day window with `value_grouping_window: "day"`.
    - First Mox response contains two synthetic mtrusg IDs and has_more true. The second request includes starting_after equal to the second ID while retaining customer, start_time, end_time, and grouping window; its response contains one final ID and has_more false.
    - Bounded enumeration returns exactly three %MeterEventSummary{} values in order, with no repeated first-page value.
  </behavior>
  <action>Per D-01/D-02, create the usage profile as a separate ExUnit file in the existing app, importing Mox and using setup :verify_on_exit!. Call MeterEventSummary.stream!/4 on an explicit Client with the synthetic key and PhoenixAdopter.MockTransport; disable retries and telemetry. Build small local JSON helpers for the Stripe list envelope and three source-shaped summary objects, adapting the package pagination test rather than importing private root test helpers. Assert GET path and all required query filters on both requests, plus exact starting_after on page two. Enumerate only the intentionally small three-row fixture (or use a bounded Stream.take/2 if the fixture changes); assert typed summary IDs/order. Keep customer attribution in the request/test context because the typed summary does not contain a customer field. Do not compute or claim a settled invoice total from these eventually consistent rows.</action>
  <verify><automated>cd test_apps/phoenix_adopter && mix test test/usage_reconciliation_profile_test.exs</automated></verify>
  <acceptance_criteria>The named two-page test receives three typed rows, exactly two Mox requests, and the second request carries the last first-page ID and original filters.</acceptance_criteria>
  <done>The focused usage test executes two transport calls, carries the correct cursor and filters, and yields exactly three ordered typed summary rows.</done>
</task>

<task type="auto">
  <name>Task 2: Prove meter-event body and HTTP idempotency independently</name>
  <files>test_apps/phoenix_adopter/test/usage_reconciliation_profile_test.exs</files>
  <read_first>test_apps/phoenix_adopter/test/usage_reconciliation_profile_test.exs from Task 1; .planning/phases/77-adopter-edge-profiles-and-ci/77-CONTEXT.md D-03/D-04; .planning/phases/77-adopter-edge-profiles-and-ci/77-RESEARCH.md Pattern 3 and Pitfall 4; test/lattice_stripe/billing/meter_event_test.exs payload assertion; test/lattice_stripe/client/request_building_test.exs idempotency header assertion; lib/lattice_stripe/billing/meter_event.ex create/3 result contract.</read_first>
  <behavior>
    - Named `meter event carries body identifier and request idempotency key` test calls MeterEvent.create/3 with one synthetic customer, event_name, payload value, stable body identifier, and distinct `idempotency_key:` option.
    - Mox inspects POST /v1/billing/meter_events, asserts the URL-encoded body identifier and payload, asserts the exact idempotency-key request header, and returns a typed %MeterEvent{} result.
  </behavior>
  <action>Per D-03, add the write-seam test in the same usage profile. Keep the event identifier and HTTP idempotency key as visibly distinct values and assert both at the outbound Mox request. Return a synthetic Stripe-shaped success body and match {:ok, %MeterEvent{}} with the stable identifier. Do not add queueing, retry loops, event persistence, or assertions that Stripe has finalized usage; the API response proves only the accepted request contract. Per D-04, run the full nested warnings-as-errors suite after the focused test so the existing core webhook and B2B profile stay included in the CI command.</action>
  <verify><automated>cd test_apps/phoenix_adopter && mix test --warnings-as-errors</automated></verify>
  <acceptance_criteria>The named write test observes distinct event identifier and idempotency-key values; all current nested tests pass with warnings as errors.</acceptance_criteria>
  <done>The usage file independently proves cursor continuation and dual idempotency, and the complete nested adopter suite passes with warnings treated as errors.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

This plan produces the usage reconciliation profile test. The leading plan already supplies the shared app, required CI job, and README selector for this exact filename.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic usage response to stream decoder | Mox supplies two bounded pages; the SDK owns cursor advancement and typed decoding. |
| Meter event request to transport | A stable event identifier and a separate HTTP idempotency header cross the SDK request builder. |

## STRIDE Threat Register (ASVS level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-04 | Tampering | Meter event request | medium | mitigate | Assert the body identifier, customer payload, value, and idempotency-key header separately at Mox; use a synthetic typed response. |
| T-77-05 | Denial of service | Usage summary stream | medium | mitigate | Enumerate only the fixed three-row, two-page synthetic fixture; assert has_more false on page two and exact cursor/filter continuation. |
| T-77-06 | Information disclosure | Usage test data | medium | mitigate | Use synthetic customer/meter IDs and no credential or production data lookup. |
</threat_model>

<verification>
Run the focused usage file after each task and the full nested warnings-as-errors suite after the second. The selected proof is request/response behavior only; no live Stripe timing or settlement claim is inferred.
</verification>

<success_criteria>
Maintainers can run the usage file by itself and see a two-page typed reconciliation read and a distinct meter-event idempotency request; the full adopter CI command still passes.
</success_criteria>

<output>
Create `.planning/phases/77-adopter-edge-profiles-and-ci/77-02-SUMMARY.md` with the named two-page and dual-key tests as evidence for ADOPT-03/ADOPT-04, plus the full-suite result for ADOPT-05.
</output>
