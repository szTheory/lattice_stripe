---
phase: 65-webhook-objecttypes-testing-fixtures
plan: 06
type: execute
wave: 5
depends_on: ["65-04", "65-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/65-webhook-objecttypes-testing-fixtures/65-RESEARCH.md
  - .planning/phases/65-webhook-objecttypes-testing-fixtures/65-VALIDATION.md
  - lib/lattice_stripe/testing/fixtures.ex
  - guides/testing.md
  - test/lattice_stripe/docs_truth_test.exs
autonomous: true
requirements: [OBJ-01, OBJ-02, OBJ-03]

must_haves:
  truths:
    - "ROADMAP.md lines 36 and 155 say FOUR entitlement/meter webhook object types, not five — the stale count written before Phase 64's D-14 no longer survives into the milestone summary."
    - "The 'v1.3 resource families' claim is corrected in both places it appears — lib/lattice_stripe/testing/fixtures.ex's @moduledoc and guides/testing.md — because Phase 65 extends the public fixture surface past v1.3."
    - "The full five-step differential phase gate is green: format+compile, credo, mix test at count >= 2305, mix docs exit 0 at <= 38 warnings, and zero warnings matching entitlement|meter|testing|fixture."
    - "MIX_ENV=prod mix compile and mix hex.build both succeed with the enlarged lib/ — run once per phase, as required because promoted fixtures changed what ships in the tarball."
    - "The Hex tarball file count reflects every promoted fixture and contains no test-support file that was not deliberately promoted."
  artifacts:
    - .planning/ROADMAP.md
    - lib/lattice_stripe/testing/fixtures.ex
    - guides/testing.md
  key_links:
    - "mix.exs files: [\"lib\", ...] → what mix hex.build packages; every fixture promoted this phase now ships"
    - "the four/five correction → the milestone summary, which reads ROADMAP prose verbatim"
  prohibitions:
    - statement: "MUST NOT raise the ExDoc warning baseline above 38 or narrow the gate-5 substring list to make a step pass — rescoping a gate is the same move as raising a baseline, and Phase 63 and Phase 64 both settled that a failing gate is fixed at its cause."
      status: active
      verification: "gate step 4 asserts the count is <= 38 and gate step 5 asserts zero matches across all four substrings, each as a separate acceptance criterion so narrowing is visible in the diff"
---

<objective>
Close Phase 65: correct the stale counts and stale prose the phase invalidated, then run the full
five-step differential gate plus the once-per-phase build checks.

Purpose: two documentation facts became wrong during this phase and one was already wrong before
it. The ROADMAP still says "five" object types — prose written before Phase 64's D-14 proved the
fifth unregistrable. The public fixture umbrella still claims it covers "the v1.3 resource
families" — true until this phase pushed it past v1.3. Left alone, both propagate into the
milestone summary as fact. The gate steps run here because promoted fixtures changed what ships in
the Hex tarball, and that is verified once per phase rather than per wave.

Output: accurate planning prose, a green differential gate, and a verified Hex build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-RESEARCH.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-VALIDATION.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-04-SUMMARY.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-05-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section in `65-01-PLAN.md` for the full phase-wide list.
This plan creates no new symbols; it corrects prose and runs gates.

<tasks>

<task type="auto">
  <name>Task 1: Correct the stale counts and the stale v1.3 claim</name>

  <read_first>
    - `.planning/ROADMAP.md` line 36 (the Phase 65 checklist line) and line 155 (the Phase 65 `**Goal:**` line) — both say "Five"/"five".
    - `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-RESEARCH.md` line 91 ("47-entry compile-time map") and line 289 (the ASCII diagram's `@object_map  (47 rows today → 51 after 65)`) — both carry the stale row count.
    - `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-04-SUMMARY.md` — the measured `map_size` after the registry rows landed, which is the number the RESEARCH correction must match.
    - `.planning/REQUIREMENTS.md` line 27 (OBJ-01) and `.planning/ROADMAP.md` line 160 (Success Criterion 1) — the two authoritative sources that both say four and enumerate them.
    - `lib/lattice_stripe/testing/fixtures.ex` — the umbrella `@moduledoc` claiming "the v1.3 resource families".
    - `guides/testing.md` lines 12-34 — the same "v1.3 resource families" claim in prose, plus the bullet list as extended by 65-01 through 65-05.
    - `test/lattice_stripe/docs_truth_test.exs` — the prose-assertion idiom (`File.read!("guides/testing.md")` then `assert guide =~ "<literal>"`).
  </read_first>

  <files>
    .planning/ROADMAP.md,
    lib/lattice_stripe/testing/fixtures.ex,
    guides/testing.md,
    test/lattice_stripe/docs_truth_test.exs
  </files>

  <action>
**Correct the four-versus-five count.** In `.planning/ROADMAP.md`, change "Five" to "Four" on line
36 and "five" to "four" on line 155. Both describe the Phase 65 entitlement/meter webhook object
types. They are stale prose written before Phase 64's D-14 established that
`billing.meter_error_report` is structurally unregistrable — it is v2 thin-event `data` carrying no
`"object"` key, so `maybe_deserialize/1`'s dispatch head can never match it. OBJ-01
(`REQUIREMENTS.md:27`), Success Criterion 1 (`ROADMAP.md:160`), and the already-green `refute` at
`object_types_test.exs` all say four. Use scoped edits on those two lines only; do not rewrite the
Phase 65 section or any other phase entry.

**Correct the stale `@object_map` row count in `65-RESEARCH.md`.** Two lines assert 47 rows today
and 51 after this phase. The live measurement is **48 today and 52 after** — taken by executing
`map_size(LatticeStripe.ObjectTypes.object_map())` against the tree at plan time and independently
re-measured since; 65-04's summary records the post-registration figure. Change line 91's
"47-entry compile-time map" to "48-entry compile-time map" and line 289's
`@object_map  (47 rows today → 51 after 65)` to `@object_map  (48 rows today → 52 after 65)`.
Line 289 sits inside a box-drawn ASCII diagram whose right-hand `║` border is column-aligned; both
substitutions are two digits for two digits, so the alignment is preserved — verify the border still
lines up with the rows above and below after editing. Correct only these two counts; leave the rest
of RESEARCH.md as the historical record of what was known when it was written.

**Correct the "v1.3 resource families" claim in both places it appears.** Phase 65 extended the
public fixture surface past v1.3 with entitlement, meter, and core-billing fixtures, so the claim
is now false. Update the `@moduledoc` in `lib/lattice_stripe/testing/fixtures.ex` and the matching
prose in `guides/testing.md` (around lines 13-14) to describe the surface by what it covers rather
than by a version number that will keep going stale — for example, that the modules under
`LatticeStripe.Testing.Fixtures.*` are the canonical fixture source of truth for the resource
families LatticeStripe ships. Do not simply bump "v1.3" to another version; a version-pinned claim
in a moduledoc is a recurring staleness trap, which is why it is wrong now.

While editing `guides/testing.md`, verify the public-fixture bullet list names every module the
phase published — Entitlements, the meter fixtures per the Q1 decision, Customer, Subscription,
PaymentIntent, and Invoice — and that the typed-struct wrapper sentence names the new wrappers.
Plans 65-01 through 65-05 each added their own entries; this is the consolidated check that none
was missed.

Do not backtick-autolink any `@moduledoc false` module. `LatticeStripe.ObjectTypes` is hidden, and
a hidden-module autolink raises the ExDoc warning count above the 38 baseline and fails gate steps
4 and 5 (the 64-04 lesson, Assumption A5). Reference hidden modules as plain prose.

**Add the consolidated docs-truth prose lock.** In `test/lattice_stripe/docs_truth_test.exs`, using
the file's established `File.read!` + `assert guide =~` idiom, assert that `guides/testing.md` names
every public fixture module Phase 65 published. A single test asserting the full set is what stops
a later edit from silently dropping one bullet.
  </action>

  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs &amp;&amp; mix test &amp;&amp; mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix credo --strict</automated>
  </verify>

  <acceptance_criteria>
    - `sed -n '36p;155p' .planning/ROADMAP.md | grep -ci 'five'` returns 0
    - `sed -n '36p;155p' .planning/ROADMAP.md | grep -ci 'four'` returns 2
    - `grep -c 'Phase 66\|Phase 67' .planning/ROADMAP.md` is unchanged from its pre-plan value (the edit was scoped, not a rewrite)
    - `grep -c '47 rows\|47-entry\|→ 51' .planning/phases/65-webhook-objecttypes-testing-fixtures/65-RESEARCH.md` returns 0
    - `grep -c '48 rows today → 52 after 65' .planning/phases/65-webhook-objecttypes-testing-fixtures/65-RESEARCH.md` returns 1
    - The `65-RESEARCH.md` ASCII diagram's right-hand `║` border remains column-aligned on the edited line (compare against the lines immediately above and below)
    - `grep -c 'v1.3' lib/lattice_stripe/testing/fixtures.ex` returns 0
    - `grep -c 'v1.3' guides/testing.md` returns 0
    - `guides/testing.md` contains each of the literals `Testing.Fixtures.Entitlements`, `Testing.Fixtures.Customer`, `Testing.Fixtures.Subscription`, `Testing.Fixtures.PaymentIntent`, `Testing.Fixtures.Invoice`, and each meter fixture module named in `65-02-SUMMARY.md`
    - `mix test test/lattice_stripe/docs_truth_test.exs` exits 0
    - Mutation check: deleting any one public-fixture bullet from `guides/testing.md` makes the new consolidated prose test fail. Restore afterward.
    - `mix docs 2>&1 | grep -c 'warning:'` returns <= 38
  </acceptance_criteria>

  <done>
    The ROADMAP says four in both places, no file claims the fixture surface stops at v1.3, and the
    guide's module list is locked against silent omission. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the five-step differential phase gate and the once-per-phase build checks</name>

  <read_first>
    - `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-VALIDATION.md` — the phase gate definition and the measured baselines it is differential against.
    - `.planning/phases/64-meter-event-summary-reads/deferred-items.md` — the two known pre-existing flakes and their exact test locations, so a spurious red is recognized rather than investigated as a regression.
    - `mix.exs` — the `package/0` `files:` list, to know what `mix hex.build` is expected to include.
  </read_first>

  <files>
    .planning/phases/65-webhook-objecttypes-testing-fixtures/65-VALIDATION.md
  </files>

  <action>
Run the full gate, record the measured numbers, then backfill `65-VALIDATION.md` from the result.
This task changes no application source — its only edit is to the validation artifact in the final
step below. Use the five-step differential gate from `65-VALIDATION.md`, **not** `mix ci` — `mix ci`'s
final step is `docs --warnings-as-errors`, which is RED at clean HEAD on 38 pre-existing warnings
(Tax.* nested types, `File.create/3`, `../README.md`, and the hidden `ObjectTypes`,
`BillingPortal.Guards`, and `Webhook.check_tolerance` modules). Steps 1 through 4 of `mix ci` pass;
clearing those warnings is Phase 67-shaped work and out of scope here.

Step 1: `mix format --check-formatted && mix compile --warnings-as-errors` — must be green.
Step 2: `mix credo --strict` — must be green. Every module promoted into `lib/` this phase carries
a real `@moduledoc`, which is what `Readability.ModuleDoc` requires.
Step 3: `mix test` — must be green with a count of at least 2305, the Phase 64 close baseline. The
count should in fact be materially higher after this phase's added cases. A single failure in
`test/lattice_stripe/client_test.exs:912` or `test/lattice_stripe/batch_test.exs:72` is a known
pre-existing flake — roughly 1 in 20 and 1 in 30 respectively, combined about 1 in 12 per full run,
both proven pre-existing on commit `a22e197`. Re-run once and check the test name against the
deferred-items file before treating it as a regression. Do not attribute such a failure to Phase 65
and do not attempt to fix it here.
Step 4: `mix docs` must exit 0 and `mix docs 2>&1 | grep -c 'warning:'` must return 38 or fewer.
Step 5: `mix docs 2>&1 | grep -ciE 'entitlement|meter|testing|fixture'` must return 0. This is
unconditional. If a warning appears, fix it at its cause — the most likely cause is a
backticked autolink to a hidden `@moduledoc false` module introduced in a new moduledoc or guide
bullet. **Do not narrow the substring list and do not raise the baseline to make a step pass.**
Both Phase 63 (STATE `[63-07]`, where gate 3 failed at 48 against a baseline of 42 and was answered
by fixing the autolinks) and Phase 64 (D-29) settled that rescoping a gate is the same move as
raising a baseline.

Then the two once-per-phase build checks, required because fixtures crossing from `test/support/`
into `lib/` change what ships:
- `MIX_ENV=prod mix compile` — must exit 0. CI does not run this; it compiles in the default env
  only. This is what would catch a promoted fixture referencing the test-only
  `LatticeStripe.TestHelpers`.
- `mix hex.build` — must succeed. Review the reported file list and confirm every fixture promoted
  this phase is present under `lib/lattice_stripe/testing/fixtures/`, and that no `test/support/`
  file appears.

If `_build` state looks inconsistent — for example a structural assertion behaving differently
across environments — run `mix clean && mix compile` first. This phase renamed modules across the
`elixirc_paths` boundary, and `_build/dev` and `_build/test` can hold stale `.beam` files for the
old `LatticeStripe.Test.Fixtures.*` names until Mix's manifest prunes them. A stale beam for an
unreferenced module is inert, but it is the reason no plan in this phase asserts on the *absence*
of the old module name via `Code.ensure_loaded?/1`.

**Backfill `65-VALIDATION.md` from the completed run.** The file was seeded as a draft at plan
time and its Per-Task Verification Map is still a template: 19 rows whose Task ID, Plan, and Wave
columns all read literally `TBD`, 18 rows at `⬜ pending` and one at `⬜ verify`. Complete it now,
because this is the first moment the real values exist:

- Fill the **Task ID / Plan / Wave** columns of each row from the six executed plans. The mapping
  is determinate from the requirement and behavior columns already in the table — for example the
  four positive-dispatch rows and the `fetch_module/1` row belong to `65-04` (wave 3), the
  entitlement fixture and typed-wrapper rows to `65-01` (wave 1) and `65-02`/`65-03`/`65-05` for
  their respective promotions, the ExDoc-placement and `guides/testing.md` prose rows to the plan
  that authored each assertion, and the `MIX_ENV=prod mix compile` and `mix hex.build` rows to this
  plan (wave 5).
- Flip each **Status** cell to ✅ where the run above proved it, using the legend already in the
  file (⬜ pending · ✅ green · ❌ red · ⚠️ flaky). The row marked `⬜ verify` is the pre-existing
  `refute` lock that 65-04 verified rather than re-authored — mark it ✅ on that basis. Do **not**
  mark a row ✅ that the run did not actually exercise; leave it ⬜ and say so in the summary.
- Set the frontmatter to `status: validated`, `nyquist_compliant: true`, and
  `wave_0_complete: true`. Wave 0 for this phase was cases rather than files (every target test
  file already existed), and all four Wave 0 Requirements checkboxes are discharged by 65-01
  through 65-05 — tick them.
- Tick the six **Validation Sign-Off** checkboxes and change **Approval** from `pending` to the
  measured outcome.

If any gate step is red, do **not** set `nyquist_compliant: true`. Report the failure instead; a
validated flag over a red gate is worse than no flag.

Record every measured number in the summary: test count, ExDoc warning count, and the four
substring match counts. The next phase gates against these.
  </action>

  <verify>
    <automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix credo --strict &amp;&amp; mix test &amp;&amp; mix docs &amp;&amp; MIX_ENV=prod mix compile &amp;&amp; mix hex.build</automated>
  </verify>

  <acceptance_criteria>
    - `mix format --check-formatted` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - `mix credo --strict` exits 0
    - `mix test` exits 0 and reports a test count >= 2305 (expected materially higher); any single failure is matched against the two known flakes and re-run once before being reported
    - `mix docs` exits 0
    - `mix docs 2>&1 | grep -c 'warning:'` returns a value <= 38, recorded verbatim in the summary
    - `mix docs 2>&1 | grep -ci 'entitlement'` returns 0
    - `mix docs 2>&1 | grep -ci 'meter'` returns 0
    - `mix docs 2>&1 | grep -ci 'testing'` returns 0
    - `mix docs 2>&1 | grep -ci 'fixture'` returns 0
    - `MIX_ENV=prod mix compile` exits 0
    - `mix hex.build` exits 0, and its file list includes every fixture promoted this phase and no `test/support/` path
    - `65-VALIDATION.md` frontmatter has `status: validated`, `nyquist_compliant: true`, and `wave_0_complete: true`
    - `grep -c '⬜' .planning/phases/65-webhook-objecttypes-testing-fixtures/65-VALIDATION.md` returns 0 outside the Status legend line
    - `grep -c '| TBD | TBD | TBD |' .planning/phases/65-webhook-objecttypes-testing-fixtures/65-VALIDATION.md` returns 0 (was 19 at plan time)
    - Every Per-Task Verification Map row names a real plan id in the `Plan` column, matching one of `65-01` through `65-06`
    - `65-VALIDATION.md` Approval reads a measured outcome, not `pending`
  </acceptance_criteria>

  <done>
    All five gate steps are green against the recorded baselines, the Hex tarball builds correctly
    with the enlarged `lib/`, and the measured numbers are recorded for the next phase to gate
    against. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → hex.pm tarball | `mix hex.build` is the last automated checkpoint before the phase's promoted fixtures become publishable. |
| project quality signal → operator trust | The gate baselines are the only mechanism preventing warning debt from accumulating silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-03 | Information Disclosure | the Hex tarball with the enlarged `lib/` | high | mitigate | `mix hex.build` file-list review as an acceptance criterion, backstopping the per-file `grep -nE 'sk_live\|pk_live\|whsec_\|rk_live\|acct_1'` scrubs that 65-01, 65-02, 65-03, and 65-05 each ran at the moment of their move. |
| T-65-08 | Repudiation | the differential gate itself | medium | mitigate | A gate that can be rescoped to pass records a false green. Step 4's baseline and step 5's four substrings are fixed by an active prohibition in this plan's `must_haves`, and each substring is a separate acceptance criterion so narrowing the list is visible in the diff rather than buried in a regex. |
| T-65-SC | Tampering | package-manager installs | low | accept | Phase 65 installs zero packages and makes no `mix.exs` `deps/0` change. `mix hex.build` confirms the dependency set is unchanged. |
</threat_model>

<verification>
This plan *is* the phase verification. All five differential gate steps plus
`MIX_ENV=prod mix compile` and `mix hex.build`, with every measured number recorded.
</verification>

<success_criteria>
- ROADMAP says four in both places; no file claims the fixture surface stops at v1.3
- All five gate steps green, with the baseline neither raised nor the substring list narrowed
- `MIX_ENV=prod mix compile` and `mix hex.build` both succeed
- Measured test count and ExDoc warning count recorded for the next phase
</success_criteria>

<output>
Create `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-06-SUMMARY.md` when done.
Record the measured test count, the ExDoc warning count, the four substring counts, and whether
either known flake fired during the run — the next phase gates against these numbers.
</output>
