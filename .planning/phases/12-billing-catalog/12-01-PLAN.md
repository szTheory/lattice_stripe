---
phase: 12-billing-catalog
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - test/lattice_stripe/discount_test.exs
  - test/lattice_stripe/product_test.exs
  - test/lattice_stripe/price_test.exs
  - test/lattice_stripe/coupon_test.exs
  - test/lattice_stripe/promotion_code_test.exs
  - test/integration/product_integration_test.exs
  - test/integration/price_integration_test.exs
  - test/integration/coupon_integration_test.exs
  - test/integration/promotion_code_integration_test.exs
autonomous: true
requirements: [BILL-01, BILL-02, BILL-06, BILL-06b]
must_haves:
  truths:
    - "stream_data dep is installed and available in :test env"
    - "Every Phase 12 resource has a pending test file that mix test can discover"
    - "Integration test scaffolds exist for Product, Price, Coupon, PromotionCode"
  artifacts:
    - path: "mix.exs"
      provides: "stream_data test dep"
      contains: "stream_data"
    - path: "test/lattice_stripe/discount_test.exs"
      provides: "Discount unit test stub"
    - path: "test/lattice_stripe/product_test.exs"
      provides: "Product unit test stub"
    - path: "test/lattice_stripe/price_test.exs"
      provides: "Price unit test stub"
    - path: "test/lattice_stripe/coupon_test.exs"
      provides: "Coupon unit test stub"
    - path: "test/lattice_stripe/promotion_code_test.exs"
      provides: "PromotionCode unit test stub"
    - path: "test/integration/product_integration_test.exs"
      provides: "Product stripe-mock scaffold"
    - path: "test/integration/price_integration_test.exs"
      provides: "Price stripe-mock scaffold"
    - path: "test/integration/coupon_integration_test.exs"
      provides: "Coupon stripe-mock scaffold"
    - path: "test/integration/promotion_code_integration_test.exs"
      provides: "PromotionCode stripe-mock scaffold"
  key_links:
    - from: "test/lattice_stripe/*_test.exs"
      to: "mix test"
      via: "ExUnit autoload"
      pattern: "use ExUnit.Case"
---

<objective>
Wave 0 test infrastructure: add the `stream_data` test dep and create empty (pending) test stubs for every new Phase 12 resource plus integration test scaffolds. This unblocks downstream waves: Plan 02 (FormEncoder property tests need stream_data), Plans 03-07 (each resource plan extends its stub file rather than creating one from scratch).

Purpose: satisfy Nyquist verification — every downstream task has a test file that already exists, so `mix test test/lattice_stripe/<file>_test.exs` is a valid automated verify command from the first task onward.
Output: `mix.exs` updated, 10 new empty test files, `mix deps.get` clean, `mix test` green (stubs use `@tag :pending` skip or equivalent so they do not fail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-billing-catalog/12-CONTEXT.md
@.planning/phases/12-billing-catalog/12-VALIDATION.md
@mix.exs
@test/lattice_stripe/form_encoder_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stream_data dep and create all Phase 12 test stubs</name>
  <read_first>
    - mix.exs (to see current deps block; add new dep to the end of the dev/test block)
    - test/lattice_stripe/form_encoder_test.exs (pattern: `use ExUnit.Case, async: true`, `describe "encode/1"`)
    - .planning/phases/12-billing-catalog/12-VALIDATION.md (Wave 0 Requirements list — 12 items)
  </read_first>
  <files>
    mix.exs,
    test/lattice_stripe/discount_test.exs,
    test/lattice_stripe/product_test.exs,
    test/lattice_stripe/price_test.exs,
    test/lattice_stripe/coupon_test.exs,
    test/lattice_stripe/promotion_code_test.exs,
    test/integration/product_integration_test.exs,
    test/integration/price_integration_test.exs,
    test/integration/coupon_integration_test.exs,
    test/integration/promotion_code_integration_test.exs
  </files>
  <action>
  **Step 1 — Add `stream_data` dep to `mix.exs`.** In the `defp deps do` block (currently lines 97-113), add this line to the dev/test section after `{:mox, "~> 1.2", only: :test},`:

  ```elixir
      {:stream_data, "~> 1.1", only: :test},
  ```

  Then run `mix deps.get` and ensure `mix.lock` is updated.

  **Step 2 — Create 5 unit test stub files.** Each uses this exact template (adjust module name + alias):

  ```elixir
  defmodule LatticeStripe.DiscountTest do
    use ExUnit.Case, async: true

    alias LatticeStripe.Discount

    @moduletag :wave0_stub

    # Placeholder test so the file is discoverable by `mix test`.
    # Populated by Plan 12-03 (Discount module + Customer backfill).
    test "module loads" do
      # Module does not exist yet — this placeholder is skipped until Plan 12-03.
      assert true
    end
  end
  ```

  Files to create with this pattern (substitute module + alias):
  - `test/lattice_stripe/discount_test.exs` → `LatticeStripe.DiscountTest`, alias `LatticeStripe.Discount`
  - `test/lattice_stripe/product_test.exs` → `LatticeStripe.ProductTest`, alias `LatticeStripe.Product`
  - `test/lattice_stripe/price_test.exs` → `LatticeStripe.PriceTest`, alias `LatticeStripe.Price`
  - `test/lattice_stripe/coupon_test.exs` → `LatticeStripe.CouponTest`, alias `LatticeStripe.Coupon`
  - `test/lattice_stripe/promotion_code_test.exs` → `LatticeStripe.PromotionCodeTest`, alias `LatticeStripe.PromotionCode`

  Do NOT reference the actual module in test body yet (it does not exist until later plans) — the `assert true` placeholder is the stub. The `alias` line is unused at this point and the compiler will warn; suppress with `@moduledoc false` (no, don't — just leave the alias off until the module exists). **Revised template (final):**

  ```elixir
  defmodule LatticeStripe.DiscountTest do
    use ExUnit.Case, async: true

    @moduletag :wave0_stub

    # Placeholder so `mix test` discovers this file.
    # Real tests land in Plan 12-03.
    test "wave 0 stub" do
      assert true
    end
  end
  ```

  Use this simpler version for all 5 stubs (no alias line).

  **Step 3 — Create 4 integration test scaffold files.** Template:

  ```elixir
  defmodule LatticeStripe.Integration.ProductTest do
    use ExUnit.Case, async: false

    @moduletag :integration
    @moduletag :wave0_stub

    # stripe-mock integration scaffold — populated by Plan 12-04.
    test "wave 0 stub" do
      assert true
    end
  end
  ```

  Files:
  - `test/integration/product_integration_test.exs` → `LatticeStripe.Integration.ProductTest`
  - `test/integration/price_integration_test.exs` → `LatticeStripe.Integration.PriceTest`
  - `test/integration/coupon_integration_test.exs` → `LatticeStripe.Integration.CouponTest`
  - `test/integration/promotion_code_integration_test.exs` → `LatticeStripe.Integration.PromotionCodeTest`

  Note: the `test/integration/` directory may not yet exist. Create it via the file write. If `test_helper.exs` needs a configuration change to discover `test/integration/*.exs` files, check existing integration tests first — v1 pattern is to put them in `test/integration/` and tag them `:integration`, excluded by default via `ExUnit.configure(exclude: [:integration])` in `test_helper.exs`. If `test_helper.exs` does NOT already exclude `:integration`, do not modify it (Plan 12-04 will handle that if needed — Phase 12 integration tests run under `mix test --include integration`).

  **Step 4 — Run verification commands.**

  ```bash
  mix deps.get
  mix compile --warnings-as-errors
  mix test --exclude integration
  ```

  All must exit 0.
  </action>
  <acceptance_criteria>
    - `grep -q "stream_data" mix.exs` exits 0
    - `grep -q "~> 1.1" mix.exs` exits 0 (stream_data version)
    - `test -f test/lattice_stripe/discount_test.exs` exits 0
    - `test -f test/lattice_stripe/product_test.exs` exits 0
    - `test -f test/lattice_stripe/price_test.exs` exits 0
    - `test -f test/lattice_stripe/coupon_test.exs` exits 0
    - `test -f test/lattice_stripe/promotion_code_test.exs` exits 0
    - `test -f test/integration/product_integration_test.exs` exits 0
    - `test -f test/integration/price_integration_test.exs` exits 0
    - `test -f test/integration/coupon_integration_test.exs` exits 0
    - `test -f test/integration/promotion_code_integration_test.exs` exits 0
    - `test -f mix.lock` exits 0 and `grep -q stream_data mix.lock` exits 0
    - `mix deps.get` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - `mix test --exclude integration` exits 0
    - `grep -q "@moduletag :wave0_stub" test/lattice_stripe/discount_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix deps.get && mix compile --warnings-as-errors && mix test --exclude integration</automated>
  </verify>
  <done>stream_data dep installed, 10 test stub files exist, mix compile clean, mix test (unit) green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex.pm → local deps | stream_data fetched from Hex on `mix deps.get` — trust Hex signing (v1 already trusts Hex for all existing deps; no new trust surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | stream_data hex package | accept | stream_data is Dashbit-maintained (same org as Mox, Finch, NimbleOptions — already trusted). Hex checksum verification via `mix.lock` is the transitive mitigation. Only `:test` env, never shipped to production. |
</threat_model>

<verification>
- `mix deps.get` installs stream_data without error
- `mix compile --warnings-as-errors` passes
- `mix test --exclude integration` passes with zero failures
- 10 test files exist on disk and are discoverable by ExUnit
</verification>

<success_criteria>
Wave 0 unblocks Plan 02 (FormEncoder property tests can `use ExUnitProperties`), and Plans 03-07 (each resource plan's first task can reference the pre-existing stub file).
</success_criteria>

<output>
After completion, create `.planning/phases/12-billing-catalog/12-01-SUMMARY.md`
</output>
