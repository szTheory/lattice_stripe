---
phase: 63-stripe-native-entitlements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/entitlements/active_entitlement.ex
  - lib/lattice_stripe/entitlements/feature.ex
  - test/support/fixtures/entitlements.ex
  - test/support/test_helpers.ex
  - test/lattice_stripe/entitlements/active_entitlement_test.exs
  - .planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt
autonomous: true
requirements: [ENT-01]

must_haves:
  truths:
    - "A developer calls LatticeStripe.Entitlements.ActiveEntitlement.list/3 with a customer id and receives {:ok, %LatticeStripe.Response{data: %LatticeStripe.List{data: [%ActiveEntitlement{}]}}} — typed structs, not raw maps"
    - "An expanded feature object on an active entitlement decodes to %LatticeStripe.Entitlements.Feature{}; an unexpanded one stays the bare feat_ id string"
    - "ActiveEntitlement.list/3 called without a customer param raises ArgumentError before any transport call is made (zero Mox expectations are consumed)"
    - "Two active entitlements that share the same lookup_key decode to two distinct structs — from_map/1 never merges, dedups, or collides entries that compare equal on lookup_key"
    - "A list page with data: [] and has_more: false decodes to %LatticeStripe.List{data: [], has_more: false} — an empty page is a typed empty list, never nil and never an error"
    - "Items are emitted in Stripe's wire order; Enum.map over data preserves position exactly, so entitlements that compare equal on lookup_key keep their relative wire order"
    - "The Entitlements namespace has no parent module: lib/lattice_stripe/entitlements.ex does not exist"
    - "ActiveEntitlement.from_map/1 is idempotent — from_map(from_map(m)) equals from_map(m) — and from_map(nil) returns nil"
  prohibitions:
    - statement: "MUST NOT expose any per-request network gate helper (an entitled?-style predicate that calls Stripe to answer an authorization question). An authorization check that makes a network call fails OPEN under partition, granting a customer access they did not buy."
      category: safety
      requirement_id: ENT-01
  artifacts:
    - path: "lib/lattice_stripe/entitlements/active_entitlement.ex"
      provides: "ENT-01 list/3 + list!/3 + from_map/1 + the canonical @list_path attribute"
      contains: "defmodule LatticeStripe.Entitlements.ActiveEntitlement"
    - path: "lib/lattice_stripe/entitlements/feature.ex"
      provides: "The entitlements.feature struct and from_map/1 so the expandable feature field types"
      contains: "defmodule LatticeStripe.Entitlements.Feature"
    - path: "test/support/fixtures/entitlements.ex"
      provides: "Private wire-shaped fixtures with the exact function names Phase 65 will promote"
      contains: "defmodule LatticeStripe.Test.Fixtures.Entitlements"
    - path: "test/lattice_stripe/entitlements/active_entitlement_test.exs"
      provides: "ENT-01 Mox unit proof plus the L1 structural surface locks"
      contains: "refute function_exported?"
    - path: ".planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt"
      provides: "Clean-HEAD ExDoc warning count captured before any lib change, consumed by the differential docs gate in 63-07"
  key_links:
    - from: "lib/lattice_stripe/entitlements/active_entitlement.ex"
      to: "lib/lattice_stripe/entitlements/feature.ex"
      via: "from_map/1 decodes the expandable feature field by calling Feature.from_map/1 directly (never through ObjectTypes)"
      pattern: "Feature\\.from_map"
    - from: "lib/lattice_stripe/entitlements/active_entitlement.ex"
      to: "lib/lattice_stripe/resource.ex"
      via: "require_param!/3 guards customer pre-network; unwrap_list/2 types the response"
      pattern: "Resource\\.(require_param!|unwrap_list)"
---

<objective>
Prove the whole Phase 63 architecture on one thin, real, end-to-end path: a developer lists a
customer's active entitlements and gets typed structs back, with the nested `feature` typed too.

Purpose: this is the phase's tracer slice. Every later plan expands horizontally from the shape it
establishes — the `Entitlements` namespace layout, the `meter.ex`-derived module skeleton, the
`Resource.require_param!/3` pre-network guard, the wire-shaped private fixtures, and the
Mox-at-Transport unit-test harness. If any of that is wrong, it is wrong after one commit instead of
after six.

Output: `LatticeStripe.Entitlements.ActiveEntitlement` (list path), `LatticeStripe.Entitlements.Feature`
(decode half), the private fixture module, a widened `TestHelpers.list_json/3`, the first unit test
file, and the clean-HEAD ExDoc warning baseline.

**Reversibility note:** D-16 (no parent `entitlements.ex`; the `Entitlements.ActiveEntitlement`
name itself) is rated `one-way` — module names become the semver contract once v1.10 tags. No
`checkpoint:decision` is inserted because CONTEXT.md records the developer choosing this exact
option; the door is already walked through. The rating is carried on the task for the record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-stripe-native-entitlements/63-CONTEXT.md
@.planning/phases/63-stripe-native-entitlements/63-RESEARCH.md
@.planning/phases/63-stripe-native-entitlements/63-PATTERNS.md
@.planning/phases/63-stripe-native-entitlements/63-VALIDATION.md
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 63** and does not exist at clean HEAD
(`grep -ril entitlement lib` returns 0 hits). Drift verification must treat these as new, not as
references to pre-existing surface.

**New modules**
- `LatticeStripe.Entitlements.ActiveEntitlement`
- `LatticeStripe.Entitlements.ActiveEntitlementSummary`
- `LatticeStripe.Entitlements.Feature`
- `LatticeStripe.Test.Fixtures.Entitlements` (test-support only, `@moduledoc false`)

**New functions (with arities)**
- `ActiveEntitlement.list/3`, `list!/3`, `retrieve/3`, `retrieve!/3`, `stream!/3`, `from_map/1`, `list_path/0`
- `Feature.create/3`, `create!/3`, `retrieve/3`, `retrieve!/3`, `update/4`, `update!/4`, `list/3`, `list!/3`, `stream!/3`, `from_map/1`
- `ActiveEntitlementSummary.from_map/1`, `stream_entitlements!/3`
- `LatticeStripe.Test.Fixtures.Entitlements.active_entitlement_json/1`, `active_entitlement_summary_json/1`, `feature_json/1`, `active_entitlement_list_json/2`
- `LatticeStripe.TestHelpers.list_json/3` (widened from the existing `/2`; `/2` remains callable)

**New struct fields**
- `%ActiveEntitlement{}` — `:id`, `:feature`, `:lookup_key`, `:livemode`, `object: "entitlements.active_entitlement"`, `extra: %{}`
- `%Feature{}` — `:id`, `:active`, `:lookup_key`, `:name`, `:metadata`, `:livemode`, `object: "entitlements.feature"`, `extra: %{}`
- `%ActiveEntitlementSummary{}` — `:customer`, `:entitlements`, `:livemode`, `object: "entitlements.active_entitlement_summary"`, `extra: %{}` — **deliberately has no `:id` field**

**New module attributes**
- `ActiveEntitlement.@list_path` = `"/v1/entitlements/active_entitlements"`
- `Feature.@list_path` = `"/v1/entitlements/features"`

**New file paths**
- `lib/lattice_stripe/entitlements/active_entitlement.ex`
- `lib/lattice_stripe/entitlements/active_entitlement_summary.ex`
- `lib/lattice_stripe/entitlements/feature.ex`
- `guides/entitlements.md`
- `test/support/fixtures/entitlements.ex`
- `test/lattice_stripe/entitlements/active_entitlement_test.exs`
- `test/lattice_stripe/entitlements/active_entitlement_stream_test.exs`
- `test/lattice_stripe/entitlements/active_entitlement_summary_test.exs`
- `test/lattice_stripe/entitlements/feature_test.exs`
- `test/integration/entitlements_integration_test.exs`
- `.planning/phases/63-stripe-native-entitlements/COVERAGE.md`
- `.planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt`

**New `mix.exs` ExDoc names**
- `groups_for_modules` group `Entitlements:` (bare atom key, no quoting — it contains no space)
- `extras:` entry `"guides/entitlements.md"`
- `groups_for_extras` `"Canonical Guides"` entry `"guides/entitlements.md"`

**New guide path and section anchors** — `guides/entitlements.md` with `## Scope boundary`,
`## Mental model`, `## Reading a customer's entitlements`, `## The reconciler pattern`,
`## The active entitlement summary`, `## Managing features`, `## lookup_key as your system identifier`,
`## Attaching features to products`, `## Testing`, `## Webhooks`, `## Error handling`, `## See also`.

**Explicit non-targets** — `lib/lattice_stripe/object_types.ex` (Phase 65 owns it) and a parent
`lib/lattice_stripe/entitlements.ex` (D-16: must never exist).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "list a customer's active entitlements" — one path only</name>
  <reversibility rating="one-way">Per D-16, `LatticeStripe.Entitlements.ActiveEntitlement` and the
  parent-less directory layout become the published semver contract when v1.10 tags; renaming after
  release is a breaking change. Recorded, not gated — CONTEXT.md already locks this choice.</reversibility>
  <files>
    .planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt,
    test/support/test_helpers.ex,
    test/support/fixtures/entitlements.ex,
    lib/lattice_stripe/entitlements/feature.ex,
    lib/lattice_stripe/entitlements/active_entitlement.ex,
    test/lattice_stripe/entitlements/active_entitlement_test.exs
  </files>
  <read_first>
    - `.planning/phases/63-stripe-native-entitlements/63-PATTERNS.md` — the analog map; §"active_entitlement.ex and feature.ex", §"test/support/fixtures/entitlements.ex", §"test/support/test_helpers.ex"
    - `.planning/phases/63-stripe-native-entitlements/63-RESEARCH.md` — § Standard Stack → Wire Surface (the exact 5- and 7-field object schemas), § Code Examples, § Corrections C-01/C-03/C-05
    - `lib/lattice_stripe/billing/meter.ex` — **full file.** The structural template: `@known_fields` sigil, `@type t` before `defstruct`, 77-column banner comments, the `require_param!` + `%Request{} |> then(&Client.request(client, &1)) |> Resource.unwrap_*` pipe, the bang-twin one-liner, `from_map/1` via `Map.split/2`
    - `lib/lattice_stripe/resource.ex` — **full file.** `require_param!/3` (presence, not emptiness), `unwrap_singular/2`, `unwrap_list/2`, `unwrap_bang!/1`
    - `lib/lattice_stripe/charge.ex` lines 495-534 — the expandable-field decode idiom (the only thing to take from `charge.ex`; C-05)
    - `lib/lattice_stripe/tax/transaction.ex` lines 1-40 — the "Relationship to other … surfaces" moduledoc section shape (D-15) and the `\#{}` escaping required inside a `@moduledoc` heredoc
    - `lib/lattice_stripe/balance.ex` line 10 — the `{: .warning}` admonition syntax (D-19.1)
    - `lib/lattice_stripe/list.ex` lines 60-108 and 240-296 — `from_json/3`, the `_last_id` derivation, `build_next_page_request/1`, and the documented-non-contract note on `_params`/`_opts`
    - `test/support/test_helpers.ex` — **full file.** `test_client/1`, `ok_response/1`, and the `list_json/2` to be widened
    - `test/support/fixtures/metering.ex` lines 1-60 — the fixture-module shape (`@moduledoc false`, `overrides \\ %{}` + `Map.merge`)
    - `test/lattice_stripe/billing/meter_test.exs` lines 1-60 — the unit-test header (`async: true`, `import Mox`, `import LatticeStripe.TestHelpers`, `setup :verify_on_exit!`) and `describe "Mod.fun/arity"` blocks
    - `test/lattice_stripe/tax_id_test.exs` lines 12-45 — the `describe "module surface"` structural-lock idiom
  </read_first>
  <behavior>
    Test file `test/lattice_stripe/entitlements/active_entitlement_test.exs`, written before the two
    lib modules exist so it starts RED:
    - `list/3` issues exactly one transport call whose `:method` is `:get` and whose `:url` contains
      `/v1/entitlements/active_entitlements` and `customer=cus_123`, and returns
      `{:ok, %LatticeStripe.Response{}}` whose `data` is a `%LatticeStripe.List{}` whose `data` is
      `[%ActiveEntitlement{id: "ent_123"}]`.
    - When the wire `feature` value is a nested map, the decoded struct's `feature` is a
      `%LatticeStripe.Entitlements.Feature{id: "feat_123", lookup_key: "premium_support"}`.
    - When the wire `feature` value is the bare string `"feat_123"`, the decoded struct's `feature`
      is exactly that string.
    - A page whose `data` is `[]` and whose `has_more` is `false` yields
      `%LatticeStripe.List{data: [], has_more: false}` — not `nil`, not an error tuple.
    - Two entitlements sharing `lookup_key: "premium_support"` but with ids `"ent_a"` and `"ent_b"`
      decode to two distinct structs in that exact wire order.
    - An unknown wire key (for example `"future_field"`) lands in the struct's `extra` map and is
      absent from the named fields.
    - `from_map(nil)` returns `nil`; `from_map/1` applied twice to the same wire map returns a value
      equal to applying it once.
  </behavior>
  <action>
    First, before touching any file under `lib/`, capture the clean-HEAD ExDoc warning baseline that
    the differential docs gate in plan 63-07 consumes. Run `mix docs --warnings-as-errors`, pipe its
    combined output through `grep -c 'warning:'`, and `tee` the resulting integer into
    `.planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt`. Use `tee` (not `>` with
    a `||` fallback) so the pipeline exit status comes from `tee` and a genuine zero-warning result is
    still recorded as the literal `0`. Do not hardcode a count — research correction C-02 established
    that the number CONTEXT.md quotes is already stale.

    Widen `LatticeStripe.TestHelpers.list_json/2` to `list_json(items, url \\ "/v1/objects", has_more \\ false)`
    per D-28, setting the envelope's `"has_more"` from the new third argument. This is purely additive;
    every existing `/1` and `/2` call site must keep compiling unchanged.

    Create `test/support/fixtures/entitlements.ex` defining `LatticeStripe.Test.Fixtures.Entitlements`
    with `@moduledoc false`. Note the namespace carefully — private fixtures live under
    `LatticeStripe.Test.Fixtures.*`, the public Phase 65 ones under `LatticeStripe.Testing.Fixtures.*`
    (research correction C-01). Use a **flat** module with four public functions, not the nested-submodule
    form `metering.ex` uses, because the flat names are what Phase 65 consumes verbatim:
    `active_entitlement_json(overrides \\ %{})`, `feature_json(overrides \\ %{})`,
    `active_entitlement_summary_json(overrides \\ %{})`, and
    `active_entitlement_list_json(items \\ [active_entitlement_json()], has_more \\ false)`. Each returns a
    string-keyed map matching Stripe's wire format and ends with `|> Map.merge(overrides)` following the
    `metering.ex` convention. Field values to use — `active_entitlement_json`: `"id" => "ent_123"`,
    `"object" => "entitlements.active_entitlement"`, `"feature" => "feat_123"`,
    `"lookup_key" => "premium_support"`, `"livemode" => false`. `feature_json`: `"id" => "feat_123"`,
    `"object" => "entitlements.feature"`, `"active" => true`, `"lookup_key" => "premium_support"`,
    `"name" => "Premium Support"`, `"metadata" => %{}`, `"livemode" => false`.
    `active_entitlement_list_json`: `"object" => "list"`, `"data" => items`, `"has_more" => has_more`,
    `"url" => "/v1/entitlements/active_entitlements"`. `active_entitlement_summary_json`:
    `"object" => "entitlements.active_entitlement_summary"`, `"customer" => "cus_ABC123customer"`,
    `"livemode" => false`, and an `"entitlements"` value that is an inline list envelope whose `"url"`
    is the webhook-shaped `"/v1/customer/cus_ABC123customer/entitlements"` (the exact string Stripe
    publishes, per correction C-06) — the summary module rewrites it, so the fixture must carry the
    un-rewritten original for that rewrite to be provable.

    Add the D-27 promotion header as the first comment line of the fixture file, worded per correction
    C-01 so Phase 65 does not hit a compile error: it must say that Phase 65 / OBJ-02 moves this file to
    `lib/lattice_stripe/testing/fixtures/entitlements.ex` **and renames the module** to
    `LatticeStripe.Testing.Fixtures.Entitlements`, that function names and bodies transfer unchanged,
    and that it must not be re-authored.

    Create `lib/lattice_stripe/entitlements/feature.ex` defining `LatticeStripe.Entitlements.Feature`
    with only its decode half in this task — `alias LatticeStripe.{Client, Request, Resource}`,
    `@list_path "/v1/entitlements/features"`, `@known_fields ~w(id object active lookup_key name
    metadata livemode)`, a `@type t`, `defstruct [:id, :active, :lookup_key, :name, :metadata,
    :livemode, object: "entitlements.feature", extra: %{}]`, and `from_map/1` with the three clauses
    `from_map(nil)`, `from_map(%__MODULE__{} = feature)`, and `from_map(map) when is_map(map)` in that
    order (the struct clause must precede the `is_map` clause because a struct is a map). Spec `@spec
    from_map(map() | t() | nil) :: t() | nil`. Give it a working `@moduledoc` now — a one-paragraph
    description plus a `## Relationship to other feature surfaces` section stating that this module is
    the entitlement feature **definition** (wire object `entitlements.feature`, ids prefixed `feat_`)
    and is **not** `LatticeStripe.Product.Feature`, which is the product **attachment** (wire object
    `product_feature`, ids prefixed `prodft_`) and carries the full definition under its
    `entitlement_feature` field as a direct reference that is never a bare id string (correction C-04).
    The verb surface and the remaining moduledoc sections land in plan 63-03; do **not** add a custom
    `Inspect` implementation (D-14) and do **not** create a parent `lib/lattice_stripe/entitlements.ex`
    (D-16).

    Create `lib/lattice_stripe/entitlements/active_entitlement.ex` defining
    `LatticeStripe.Entitlements.ActiveEntitlement`. Per D-06 it owns the canonical path as a single
    module attribute `@list_path "/v1/entitlements/active_entitlements"` plus a `@doc false def
    list_path, do: @list_path` accessor, so `list/3`, the `stream!/3` added in 63-02, and the summary
    module's url rewrite in 63-04 physically cannot diverge. Fields are exactly the five the spec marks
    required (correction C-03): `@known_fields ~w(id object feature lookup_key livemode)` and
    `defstruct [:id, :feature, :lookup_key, :livemode, object: "entitlements.active_entitlement",
    extra: %{}]`. Implement `list/3` with the signature `list(%Client{} = client, params \\ %{}, opts
    \\ [])`, guarding `"customer"` first via `Resource.require_param!/3` with the message
    `"LatticeStripe.Entitlements.ActiveEntitlement.list/3 requires a customer param"` (D-10 — the
    format is copied verbatim from `Billing.Meter.create/3`), then the standard `%Request{method: :get,
    path: @list_path, params: params, opts: opts}` pipe through `Client.request/2` into
    `Resource.unwrap_list(&from_map/1)`. Add the formulaic `list!/3` bang twin via
    `Resource.unwrap_bang!/1`. Implement `from_map/1` with the same three-clause idempotent shape as
    `Feature` (D-07), decoding the expandable `feature` field by calling
    `LatticeStripe.Entitlements.Feature.from_map/1` **directly** when the value is a map and passing it
    through unchanged otherwise — add a source comment stating that routing this through `ObjectTypes`
    would create a false Phase 65 dependency and silently fall through to a raw map (F-09).

    The `ActiveEntitlement` `@moduledoc` is load-bearing and must ship complete in this task. It needs:
    a one-paragraph description naming the wire object and the canonical path; a note that `customer`
    is a required filter and that Stripe's `limit` defaults to 10 with a maximum of 100 (F-04), which
    is why full enumeration wants the streaming variant; a note that `require_param!/3` checks presence,
    not emptiness (D-10); a note that the struct's own `lookup_key` mirrors the feature's, so a local
    gate can key on it without expanding `feature` (correction C-03); a `## Relationship to other
    feature surfaces` cross-reference mirroring the one in `Feature` (D-15); and — per D-19.1 — a
    `{: .warning}` admonition that is the highest-probability interception point for a contributor who
    greps for a gate helper. That admonition must name the rejected helper by its exact name
    `entitled?` in prose (D-24 — the string must be **present** so a searcher lands on the explanation;
    it is never refuted), must use the word `gate`, and must use the phrase `fail closed`. It must
    state the working replacement rather than only the refusal: reconcile with `list/3` or the
    streaming variant, persist to a local store, gate locally against that store, and fail closed when
    the store is stale — because an authorization check that makes a network call fails open under
    partition.

    Create `test/lattice_stripe/entitlements/active_entitlement_test.exs` covering the `<behavior>`
    block above. Header follows `meter_test.exs`: `use ExUnit.Case, async: true`, `import Mox`,
    `import LatticeStripe.TestHelpers`, `alias LatticeStripe.Entitlements.{ActiveEntitlement, Feature}`,
    `alias LatticeStripe.Test.Fixtures.Entitlements`, and `setup :verify_on_exit!`. Build responses with
    `ok_response/1` over `Entitlements.active_entitlement_list_json/2`. Remember the Mox callback
    receives a plain map with `:method`, `:url`, `:headers`, `:body`, `:opts` and that GET params arrive
    query-encoded inside `:url`, so filter assertions are `req.url =~ "customer=cus_123"` string matches.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors && mix test test/lattice_stripe/entitlements/active_entitlement_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/entitlements/active_entitlement_test.exs` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - `mix format --check-formatted` exits 0
    - `lib/lattice_stripe/entitlements/active_entitlement.ex` contains `@list_path "/v1/entitlements/active_entitlements"`
    - `lib/lattice_stripe/entitlements/active_entitlement.ex` contains `def list(%Client{} = client, params \\ %{}, opts \\ [])`
    - `lib/lattice_stripe/entitlements/active_entitlement.ex` contains `def list_path, do: @list_path`
    - `lib/lattice_stripe/entitlements/active_entitlement.ex` contains `~w(id object feature lookup_key livemode)`
    - `lib/lattice_stripe/entitlements/active_entitlement.ex` contains the message string `LatticeStripe.Entitlements.ActiveEntitlement.list/3 requires a customer param`
    - `lib/lattice_stripe/entitlements/feature.ex` contains `~w(id object active lookup_key name metadata livemode)`
    - `lib/lattice_stripe/entitlements/feature.ex` contains `object: "entitlements.feature"`
    - Behavior: `LatticeStripe.Entitlements.ActiveEntitlement.from_map(nil)` returns `nil`
    - Behavior: applying `ActiveEntitlement.from_map/1` to an already-typed `%ActiveEntitlement{}` returns it unchanged
    - Behavior: `ActiveEntitlement.from_map(%{"feature" => %{"id" => "feat_123", "object" => "entitlements.feature"}})` has a `feature` that is a `%LatticeStripe.Entitlements.Feature{}`
    - Behavior: `ActiveEntitlement.from_map(%{"feature" => "feat_123"})` has `feature == "feat_123"`
    - Structural: `test -f lib/lattice_stripe/entitlements.ex` is false — no parent module exists (D-16)
    - Structural: `git diff --name-only` does not include `lib/lattice_stripe/object_types.ex` (Phase 65 owns it)
    - CLI: `test -s .planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt` succeeds and its content matches `^[0-9]+$`
    - Source: `grep -c 'has_more' test/support/test_helpers.ex` is at least 2, and `mix test` still passes for every pre-existing `list_json` call site
    - *Reading the count criteria above: each is a claim about the number a counting grep **prints**, not about its exit status. A counting grep exits 1 when it prints `0`, so a non-zero exit alongside `0` on stdout means the criterion is met. Do not paste these into an `&&` chain without `|| true`.*
  </acceptance_criteria>
  <done>A caller can list one customer's active entitlements and receive typed `%ActiveEntitlement{}` structs with a typed nested `%Feature{}`, proven end-to-end by a green Mox unit test, and the whole path is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the read-only surface and the pre-network guard</name>
  <files>test/lattice_stripe/entitlements/active_entitlement_test.exs</files>
  <read_first>
    - `test/lattice_stripe/entitlements/active_entitlement_test.exs` — the file created in Task 1
    - `test/lattice_stripe/tax_id_test.exs` lines 27-39 — the verbatim `describe "module surface"` shape with `refute function_exported?` / `assert function_exported?` pairs
    - `test/lattice_stripe/balance_test.exs` lines 139-143 and `test/lattice_stripe/transfer_test.exs` lines 204-208 — two more instances of the same house idiom
    - `.planning/phases/63-stripe-native-entitlements/63-CONTEXT.md` — D-23 (locks L1), D-24 (assert presence, never refute), D-11 (no `id in [nil, ""]` clauses)
    - `.planning/phases/63-stripe-native-entitlements/63-RESEARCH.md` — correction C-07 (stripe-mock returns 400 for a missing required param, which the client-side guard means we never reach) and Pitfall 6
  </read_first>
  <action>
    Append two `describe` blocks to the existing test file.

    The first is the C-07 pre-network guard proof for ENT-01. Assert that
    `ActiveEntitlement.list(test_client(), %{})` raises `ArgumentError` with the message
    `LatticeStripe.Entitlements.ActiveEntitlement.list/3 requires a customer param`, and set **zero**
    Mox expectations in that test so `verify_on_exit!` proves no transport call was attempted. Add a
    second case asserting that a params map carrying only unrelated keys (for example `%{"limit" => "5"}`)
    raises the same error — the guard checks key presence, not value emptiness.

    The second is the D-23 L1 structural surface lock, following the `tax_id_test.exs` shape exactly.
    `refute function_exported?(ActiveEntitlement, :entitled?, 2)`, `:entitled?, 3`, and `:entitled?, 4`
    — this is the mechanism that actually forbids the helper, and it is defeated only by adding the
    function, unlike a source grep which a rename defeats. Because this is a read-only resource, also
    `refute function_exported?` for `:create` at arities 2 and 3, `:update` at 3 and 4, and `:delete`
    at 2 and 3. Then `assert function_exported?(ActiveEntitlement, :list, 3)`, `:list, 1`, `:list!, 3`,
    `:from_map, 1`, and `:list_path, 0` so the shipped surface is pinned positively too. With no
    Dialyzer in this project and typespecs documentation-only, these locks are the only enforcement of
    public surface shape.

    Do **not** add any assertion that greps the module source for the rejected helper name and refutes
    it — D-24 resolves that directly: absence is proven structurally here, while the name must remain
    **present** in prose for discoverability. Do not add `id in [nil, ""]` clauses or tests demanding
    them (D-11 — that pattern appears in only 5 of 55 top-level modules and is not a house convention).
  </action>
  <verify>
    <automated>mix format --check-formatted && mix test test/lattice_stripe/entitlements/active_entitlement_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/entitlements/active_entitlement_test.exs` exits 0
    - Source: `grep -c 'refute function_exported?(ActiveEntitlement, :entitled?' test/lattice_stripe/entitlements/active_entitlement_test.exs` returns 3
    - Source: `test/lattice_stripe/entitlements/active_entitlement_test.exs` contains `assert function_exported?(ActiveEntitlement, :list_path, 0)`
    - Source: `test/lattice_stripe/entitlements/active_entitlement_test.exs` contains `assert_raise ArgumentError`
    - Behavior: calling `list/3` with a params map lacking `"customer"` raises `ArgumentError` and consumes zero Mox expectations (the test declares no `expect/3` and `verify_on_exit!` passes)
    - Structural: the test file contains no assertion of the form `refute <source_var> =~` applied to the module source (D-24)
    - *Reading the count criteria above: each is a claim about the number a counting grep **prints**, not about its exit status. A counting grep exits 1 when it prints `0`, so a non-zero exit alongside `0` on stdout means the criterion is met. Do not paste these into an `&&` chain without `|| true`.*
  </acceptance_criteria>
  <done>The read-only surface shape and the pre-network `customer` guard are both locked by tests that fail if the surface changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `LatticeStripe.Client` | Caller-supplied `params` (including the tenant-scoping `customer` id) cross into request construction. |
| `LatticeStripe.Client` → Stripe HTTP | Encoded query string and auth/Connect headers leave the process. |
| Stripe HTTP → `from_map/1` | Untrusted wire JSON becomes typed structs; unknown keys land in `extra`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Information Disclosure | `ActiveEntitlement.list/3` tenant scoping — the `customer` filter | high | mitigate | `Resource.require_param!/3` makes `customer` mandatory pre-network (D-10), so an unscoped account-wide list is impossible to construct by omission. Task 2's zero-expectation `assert_raise ArgumentError` proves the guard fires before any transport call. The page-2 half of this threat is carried by T-63-02 in plan 63-02. |
| T-63-04 | Elevation of Privilege | absence of a per-request network gate helper | high | mitigate | D-23 L1 `refute function_exported?(ActiveEntitlement, :entitled?, 2/3/4)` in Task 2 structurally forbids the helper; the D-19.1 moduledoc admonition in Task 1 ships the fail-closed local-gate replacement so the refusal is not "fixed" by the next contributor. |
| T-63-06 | Information Disclosure | struct `Inspect` output in application logs | low | accept | The three entitlement objects carry only ids, a `lookup_key`, a boolean, and a `name` the Stripe spec documents as *"not meant to be displayable to the customer"* — no PII to redact, so the derived default `Inspect` is correct and a custom redacting impl would be cargo-culted from `charge.ex` (D-14). |
| T-63-SC | Tampering | package-manager installs | low | accept | This phase installs **zero** packages — no `mix.exs` `deps/0` change, `mix deps.get` only. Every library used is already declared and locked. `63-RESEARCH.md` § Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]`, or `[SLOP]` packages. |
</threat_model>

<verification>
1. `mix format --check-formatted` exits 0
2. `mix compile --warnings-as-errors` exits 0
3. `mix test test/lattice_stripe/entitlements/` exits 0
4. `mix test` (full unit suite) exits 0 — the widened `list_json/3` must not regress any existing call site
5. `mix credo --strict` exits 0
6. `ls lib/lattice_stripe/entitlements.ex` fails (D-16 — no parent module)
7. **Do NOT run `mix ci`** — the alias includes `docs --warnings-as-errors`, which is RED at clean HEAD for pre-existing reasons unrelated to this phase (research correction C-02)
</verification>

<success_criteria>
- ENT-01's list path works end-to-end against a mocked transport and returns typed structs with a typed nested feature
- The `customer` filter is mandatory and enforced before any network call
- `lib/lattice_stripe/entitlements/` exists with exactly two files; no parent module; `object_types.ex` untouched
- The private fixture module carries the exact four function names Phase 65 will promote, with a promotion header that names the required module rename
- `TestHelpers.list_json/3` is available and backward compatible
- The clean-HEAD ExDoc warning baseline is captured as a plain integer for the 63-07 differential gate
</success_criteria>

<output>
Create `.planning/phases/63-stripe-native-entitlements/63-01-SUMMARY.md` when done.
</output>
