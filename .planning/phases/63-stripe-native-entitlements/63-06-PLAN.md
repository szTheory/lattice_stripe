---
phase: 63-stripe-native-entitlements
plan: 06
type: execute
wave: 4
depends_on: ["63-03", "63-04"]
files_modified:
  - guides/entitlements.md
  - guides/scope.md
  - mix.exs
autonomous: true
requirements: [ENT-01, ENT-02, ENT-03, ENT-04, ENT-05]

must_haves:
  truths:
    - "A reader arriving at the published docs finds a canonical Entitlements guide in the Canonical Guides sidebar group, between the customer portal guide and the metering guide"
    - "The three Entitlements modules appear together under their own Entitlements sidebar group, adjacent to Billing Metering rather than buried in the 22-module Billing group"
    - "The guide's Scope boundary section refuses the per-request gate helper AND ships the working replacement — reconcile, persist locally, gate locally, fail closed on staleness"
    - "The reconciler example is one call with no has_more branch: the reader never learns that Stripe inlines ten"
    - "guides/scope.md records the refused gate helper as a project-wide, permanent deferred-scope entry"
    - "The Managing features section is a verb table, so Phase 66 and any future verb append one row rather than rewriting prose"
    - "Three pre-cut stub sections exist for Attaching features to products, Testing, and Webhooks, so Phases 65 and 66 append rather than restructure"
    - "mix docs builds the guide with no new warnings and no warning naming the new surface"
  prohibitions:
    - statement: "MUST NOT document the refusal of the per-request gate helper without also documenting the working fail-closed replacement. A refusal with no alternative reads as an omission and is what the next contributor 'fixes' — leaving the reader to hand-roll the fail-open version the refusal exists to prevent."
      category: transparency
      requirement_id: ENT-01
  artifacts:
    - path: "guides/entitlements.md"
      provides: "The canonical Entitlements guide, spine-only, with three pre-cut stubs"
      contains: "## Scope boundary"
    - path: "mix.exs"
      provides: "ExDoc registration: the Entitlements module group and both guide registrations"
      contains: "Entitlements: ["
    - path: "guides/scope.md"
      provides: "The project-wide permanent record of the refused gate helper"
  key_links:
    - from: "mix.exs"
      to: "guides/entitlements.md"
      via: "the guide is registered in BOTH extras: and groups_for_extras Canonical Guides — a file in a group but absent from extras: is silently dropped"
      pattern: "guides/entitlements\\.md"
    - from: "guides/entitlements.md"
      to: "lib/lattice_stripe/entitlements/active_entitlement_summary.ex"
      via: "the reconciler pattern section is the headline example and calls stream_entitlements!/3"
      pattern: "stream_entitlements!"
---

<objective>
Ship `guides/entitlements.md` — the canonical guide for the whole family — register it and the three
new modules in ExDoc, and record the refused gate helper in the project-wide deferred-scope contract.

Purpose: the reader's chain through the Canonical Guides sidebar is *what they bought → how they
self-manage → **what they can access** → what they consumed → what they owe*. Entitlements slots
between the customer portal and metering guides, and this is where a developer who searches for a
gate helper actually lands. The refusal only holds if it ships with the working replacement in the
same breath; a refusal with no alternative is what the next contributor deletes.

Output: a spine-only guide (~200-260 lines) with three pre-cut stub sections that Phases 65 and 66
append to rather than restructure, plus the three `mix.exs` `docs/0` edits and two or three lines in
`guides/scope.md`.

**Reversibility note:** D-19 (the fence shipping in all three surfaces) is rated `one-way` by intent —
the permanence is the point. D-17 (the sidebar group) is `reversible`; it is one commit and touches
no published contract. No `checkpoint:decision` is inserted for either: CONTEXT.md records the
developer choosing both, so the doors are already walked through.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-stripe-native-entitlements/63-CONTEXT.md
@.planning/phases/63-stripe-native-entitlements/63-RESEARCH.md
@.planning/phases/63-stripe-native-entitlements/63-PATTERNS.md
@.planning/phases/63-stripe-native-entitlements/63-03-SUMMARY.md
@.planning/phases/63-stripe-native-entitlements/63-04-SUMMARY.md
</context>

## Artifacts this phase produces

Phase 63 creates all of the following from scratch — none exist at clean HEAD. Treat every symbol as
new, not as a reference to pre-existing surface.

**New modules** — `LatticeStripe.Entitlements.ActiveEntitlement`,
`LatticeStripe.Entitlements.ActiveEntitlementSummary`, `LatticeStripe.Entitlements.Feature`,
`LatticeStripe.Test.Fixtures.Entitlements`.

**New functions (with arities)** — `ActiveEntitlement.list/3`, `list!/3`, `retrieve/3`, `retrieve!/3`,
`stream!/3`, `from_map/1`, `list_path/0`; `Feature.create/3`, `create!/3`, `retrieve/3`, `retrieve!/3`,
`update/4`, `update!/4`, `list/3`, `list!/3`, `stream!/3`, `from_map/1`;
`ActiveEntitlementSummary.from_map/1`, `stream_entitlements!/3`;
`LatticeStripe.Test.Fixtures.Entitlements.active_entitlement_json/1`, `active_entitlement_summary_json/1`,
`feature_json/1`, `active_entitlement_list_json/2`; `LatticeStripe.TestHelpers.list_json/3`.

**New struct fields** — `%ActiveEntitlement{}`: `:id`, `:feature`, `:lookup_key`, `:livemode`,
`object:`, `extra:`. `%Feature{}`: `:id`, `:active`, `:lookup_key`, `:name`, `:metadata`, `:livemode`,
`object:`, `extra:`. `%ActiveEntitlementSummary{}`: `:customer`, `:entitlements`, `:livemode`,
`object:`, `extra:` — **no `:id`**.

**New `mix.exs` ExDoc names** — `groups_for_modules` group `Entitlements:` (a bare atom key, since the
name contains no space — the quoted form is used only for names like `"Billing Metering"`); `extras:`
entry `"guides/entitlements.md"`; `groups_for_extras` `"Canonical Guides"` entry
`"guides/entitlements.md"`.

**New guide path and section anchors** — `guides/entitlements.md` with `## Scope boundary`,
`## Mental model`, `## Reading a customer's entitlements`, `## The reconciler pattern`,
`## The active entitlement summary`, `## Managing features`,
`## lookup_key as your system identifier`, `## Attaching features to products`, `## Testing`,
`## Webhooks`, `## Error handling`, `## See also`.

**New file paths** — `lib/lattice_stripe/entitlements/{active_entitlement,active_entitlement_summary,feature}.ex`,
`guides/entitlements.md`, `test/support/fixtures/entitlements.ex`,
`test/lattice_stripe/entitlements/{active_entitlement,active_entitlement_stream,active_entitlement_summary,feature}_test.exs`,
`test/integration/entitlements_integration_test.exs`,
`.planning/phases/63-stripe-native-entitlements/COVERAGE.md`,
`.planning/phases/63-stripe-native-entitlements/docs-warning-baseline.txt`.

**Explicit non-targets** — `lib/lattice_stripe/object_types.ex` and a parent
`lib/lattice_stripe/entitlements.ex` (must never exist).

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical Entitlements guide</name>
  <reversibility rating="one-way">D-19 puts the "no per-request gate helper — gate locally, fail
  closed" rationale in three surfaces on purpose; its permanence is the feature. Recorded, not gated —
  CONTEXT.md already locks this choice.</reversibility>
  <files>guides/entitlements.md</files>
  <read_first>
    - `guides/tax.md` — **the whole file.** The closest precedent: a canonical guide shipped in the same milestone as a new resource family. Mirror its opening shape (one-paragraph what-it-is, one-paragraph what-this-guide-covers, then a bolded in-scope list and an explicit out-of-scope paragraph), its `## Scope boundary` structure, its ASCII mental-model diagram style, and its relative `.md` cross-link form
    - `guides/metering.md` and `guides/customer-portal.md` — the sidebar neighbours and cross-link targets
    - `guides/scope.md` — the tone of the project's deferred-scope contract
    - `lib/lattice_stripe/entitlements/active_entitlement.ex`, `feature.ex`, `active_entitlement_summary.ex` — the shipped surfaces and moduledocs from plans 63-01 through 63-04, so the guide describes what actually exists
    - `.planning/phases/63-stripe-native-entitlements/63-CONTEXT.md` — D-18 (spine, filename, placement, no fifth flagship recipe), D-19.2 (the scope-boundary section must carry the working replacement), and `<specifics>` (the exact reconciler snippet shape, the guide section order, and the verb-table instruction)
    - `.planning/phases/63-stripe-native-entitlements/63-RESEARCH.md` — § Standard Stack → Wire Surface, for the field lists and filter names the guide describes
  </read_first>
  <action>
    Create `guides/entitlements.md` with the title `# Entitlements`, flat kebab-case filename, roughly
    200-260 lines. Section order follows the reader's chain and must be exactly this:

    `## Scope boundary`, `## Mental model`, `## Reading a customer's entitlements`,
    `## The reconciler pattern`, `## The active entitlement summary`, `## Managing features`,
    `## lookup_key as your system identifier`, `## Attaching features to products`, `## Testing`,
    `## Webhooks`, `## Error handling`, `## See also`.

    `## Scope boundary` comes first and is the load-bearing section. State what the library is — an
    HTTP client SDK: typed resources, `{:ok, struct}` / `{:error, %LatticeStripe.Error{}}`, and
    testing fixtures — then the in-scope list naming the three modules. Then state, plainly and by
    name, that there is no `entitled?` helper and no per-request network gate, and give the reason: an
    authorization check that makes a network call fails **open** under a network partition, granting
    access the customer did not buy. Use the phrase `fail closed` and the word `gate`. Then — and this
    is the half that makes the refusal survive — give the working replacement as a numbered recipe:
    reconcile a customer's entitlements with `stream_entitlements!/3` (or `ActiveEntitlement.stream!/3`)
    when the summary webhook fires, persist the result to a local store alongside a reconciled-at
    timestamp, gate authorization against that local store on every request, and fail closed when the
    stored snapshot is older than the application's staleness tolerance. Refusal without an alternative
    is what gets "fixed."

    `## Mental model` gets a short ASCII diagram in `tax.md`'s style showing the chain Feature →
    product attachment → purchase → ActiveEntitlement, and names which of those this library covers
    today and which arrives in a later phase.

    `## Reading a customer's entitlements` covers `list/3` (noting `customer` is required and Stripe's
    `limit` defaults to 10), `stream!/3` as the complete-enumeration path, and `retrieve/3`. Show
    `Stream.take/2` alongside `Enum.to_list/1` so the memory characteristics are visible.

    `## The reconciler pattern` is the headline example and must read exactly as cleanly as the
    `<specifics>` snippet in CONTEXT.md: deserialize the event object, pipe the summary through
    `Entitlements.ActiveEntitlementSummary.stream_entitlements!/3`, `Enum.to_list/1`, and hand the
    result to the application's reconcile function. One call, no `has_more` branch, no cursor
    bookkeeping — the consumer never learns that Stripe inlines ten. Follow it with two or three
    sentences on why this is a full canonical re-fetch rather than a resume from the inline page: one
    call means one point in time, whereas resuming stitches a head captured when the webhook fired to
    a tail queried later.

    `## The active entitlement summary` explains the webhook-only object, its lack of a top-level
    `id`, and that the nested `entitlements` field is a typed `%LatticeStripe.List{}`.

    `## Managing features` is a **verb table**, not prose — one row per verb with the function, the
    HTTP method and path, and a one-line purpose. Phase 66 and any future verb append one row. State
    below the table that there is no delete verb because Stripe ships no DELETE for features, and that
    archiving is `update/4` with `active: false`. Repeat the `active` field versus `archived` filter
    warning here in one sentence, with the pointer to pass `%{"archived" => true}` for catalog
    reconciliation.

    `## lookup_key as your system identifier` covers the list filter, that it returns a list rather
    than a singleton, and that the key is immutable after create — which is what makes it safe to key
    host configuration on.

    `## Attaching features to products`, `## Testing`, and `## Webhooks` are **pre-cut stubs**: a
    heading plus one or two sentences saying what will live there and which phase fills it (products →
    Phase 66; testing and webhooks → Phase 65). The skeleton reserves the space so later phases append
    rather than rewrite. Keep each stub short enough that it reads as deliberate scaffolding, not as an
    unfinished section.

    `## Error handling` cross-links the error-handling guide; `## See also` cross-links the customer
    portal, metering and subscriptions guides.

    All cross-links are relative markdown filenames in the `tax.md` form — for example
    `error-handling.md` and `metering.md`, optionally with a `#anchor`. Do **not** write absolute
    documentation-site URLs anywhere in this file; use ExDoc autolinks for module and function
    references so the differential docs gate stays clean. Do **not** add a new
    `groups_for_extras` group and do **not** promote this to a flagship recipe — the flagship set is
    four curated multi-resource journeys and a single-family guide dilutes it.
  </action>
  <verify>
    <automated>mix docs &amp;&amp; test -s guides/entitlements.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix docs` exits 0
    - `test -s guides/entitlements.md` succeeds
    - `head -1 guides/entitlements.md` is exactly `# Entitlements`
    - `grep -c '^## ' guides/entitlements.md` returns 12
    - `grep -n '^## Scope boundary' guides/entitlements.md` reports the first `## ` heading in the file
    - Source: the guide contains the literal `entitled?`
    - Source: the guide contains the literal `fail closed`
    - Source: the guide contains the literal `stream_entitlements!`
    - Source: the guide contains the literal `%{"archived" => true}`
    - Source: the guide contains the literal `error-handling.md`
    - Source: `grep -c 'https://' guides/entitlements.md` returns 0
    - Source: `awk '/^## Managing features/,/^## lookup_key/' guides/entitlements.md | grep -c '^|'` is at least 6 — the section is a table, not prose
    - Source: `wc -l < guides/entitlements.md` is between 180 and 300
  </acceptance_criteria>
  <done>The guide exists, builds under `mix docs`, refuses the gate helper by name while shipping the fail-closed replacement, and reserves space for Phases 65 and 66.</done>
</task>

<task type="auto">
  <name>Task 2: Register the guide and the module group in ExDoc, and fence the gate helper project-wide</name>
  <reversibility rating="reversible">D-17's sidebar group is one commit against a docs config; it
  publishes no contract and can be moved or folded later at no cost.</reversibility>
  <files>mix.exs, guides/scope.md</files>
  <read_first>
    - `mix.exs` `docs/0` — the `extras:` list (a flat list of guide-path strings), the `groups_for_extras:` list of `{group, [paths]}` tuples including `{"Canonical Guides", [...]}`, and the `groups_for_modules:` keyword list where `"Billing Metering":` is immediately followed by `Connect:`
    - `guides/scope.md` — the existing deferred-scope entries and their format
    - `test/lattice_stripe/docs_truth_test.exs` around line 341 — the existing `guides/scope.md` lock, which plan 63-07 extends; the new prose must give that lock something stable to match
    - `test/lattice_stripe/docs_truth_test.exs` lines 447-455 — how the tax guide's dual registration is asserted, via the `docs_config/0` helper reading `LatticeStripe.MixProject.project()[:docs]`
    - `.planning/phases/63-stripe-native-entitlements/63-CONTEXT.md` — D-17, D-18, D-19.3
  </read_first>
  <action>
    Make three scoped edits to `mix.exs` `docs/0`. Do not rewrite the file.

    First, insert the string `"guides/entitlements.md"` into the flat `extras:` list. Second, insert
    the same string into the `{"Canonical Guides", [...]}` tuple inside `groups_for_extras:`,
    positioned between `"guides/customer-portal.md"` and `"guides/metering.md"` — that is the sidebar
    order the reader's chain requires. Both edits are mandatory: a guide listed in `groups_for_extras`
    but absent from `extras:` is silently dropped from the build.

    Third, insert a new `Entitlements:` group into `groups_for_modules:`, positioned between the
    existing `"Billing Metering":` group and `Connect:`. Use the bare atom key form `Entitlements:` —
    the quoted-string form is used only when a group name contains a space. The group lists exactly
    three modules: `LatticeStripe.Entitlements.ActiveEntitlement`,
    `LatticeStripe.Entitlements.ActiveEntitlementSummary`, and `LatticeStripe.Entitlements.Feature`.
    A sibling group rather than a fold-in is deliberate: `"Billing Metering"` already establishes that
    a Billing sub-product earns its own group, and the `Billing` group already holds 22 modules. Phase
    66 later appends `LatticeStripe.Product.Feature` to this same group with a one-line diff, so the
    two halves of one concept sit adjacent in the sidebar and the "two things called Feature"
    confusion is defused at the point of discovery.

    Do **not** touch any other `groups_for_modules` entry. The backlog of 32 ungrouped modules —
    including the whole `Tax.*` family — is Phase 67's work and is an explicit scope fence here.

    Then append two or three lines to `guides/scope.md` recording the refused gate helper as a
    project-wide deferred-scope entry, in the format the existing entries use. It must name
    `entitled?` explicitly, say that per-request network authorization gates are out of scope because
    they fail open under partition, and point at `entitlements.md` for the fail-closed local-gate
    replacement. This page is the canonical "what this library deliberately does not do" surface and is
    already docs-truth-locked; a decision that is not recorded here is not locked project-wide. Keep
    the existing anchors in that file intact — the current lock asserts on `Identity`, on `Reporting`
    or `Sigma`, on `adopter pull` or `maintenance mode`, and on `Client.request`.
  </action>
  <verify>
    <automated>mix docs &amp;&amp; mix test test/lattice_stripe/docs_truth_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix docs` exits 0
    - `mix test test/lattice_stripe/docs_truth_test.exs` exits 0 — the pre-existing locks still pass
    - `grep -c '"guides/entitlements.md"' mix.exs` returns 2 — once in `extras:`, once in `"Canonical Guides"`
    - Source: `mix.exs` contains `Entitlements: [`
    - Source: `awk '/Entitlements: \[/,/\]/' mix.exs | grep -c 'LatticeStripe.Entitlements.'` returns 3
    - Ordering: `awk '/"Canonical Guides"/,/\]}/' mix.exs | grep -n 'customer-portal\|entitlements\|metering'` lists customer-portal, then entitlements, then metering, in that order
    - Ordering: `grep -n 'Billing Metering\|Entitlements: \[\|Connect: \[' mix.exs` shows `"Billing Metering"` before `Entitlements: [` before `Connect: [`
    - Source: `guides/scope.md` contains the literal `entitled?`
    - Source: `guides/scope.md` contains the literal `entitlements.md`
    - Source: `guides/scope.md` still contains `Identity` and `Client.request`
    - Behavior: `LatticeStripe.MixProject.project()[:docs][:extras]` includes `"guides/entitlements.md"`, and the `"Canonical Guides"` entry of `groups_for_extras` includes it too
  </acceptance_criteria>
  <done>The guide is discoverable in the sidebar, the three modules share their own group adjacent to Billing Metering, and the refused gate helper is recorded on the project's canonical deferred-scope page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published documentation → adopter's authorization code | The guide is the primary input to how an adopter builds their entitlement gate. Guidance that is wrong or incomplete here becomes an authorization defect in every downstream application. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-04 | Elevation of Privilege | documented guidance on how to gate access | **high** | mitigate | The `## Scope boundary` section refuses the per-request network gate *and* ships the fail-closed local-gate recipe in the same section. A refusal alone would leave the reader to hand-roll the fail-open version; that is why the working replacement is a hard requirement of this task and why the same rationale lands in three separate surfaces. |
| T-63-17 | Repudiation | a decision recorded only in a phase artifact rather than a published page | medium | mitigate | `guides/scope.md` is the project's canonical, docs-truth-locked deferred-scope contract. Recording the refusal there is what makes it project-wide rather than phase-local; plan 63-07 extends the existing lock to keep it from being deleted. |
| T-63-18 | Information Disclosure | guide examples that encourage logging entitlement payloads | low | accept | Entitlement objects carry ids, a lookup key, and booleans — no PII. Examples show reconciling into a local store, not logging. |
| T-63-SC | Tampering | package-manager installs | low | accept | Zero packages installed; this plan's `mix.exs` edit is confined to `docs/0` and does not touch `deps/0`. |
</threat_model>

<verification>
1. `mix docs` exits 0
2. `mix test test/lattice_stripe/docs_truth_test.exs` exits 0
3. `mix test` (full unit suite) exits 0
4. `mix format --check-formatted` exits 0
5. `mix credo --strict` exits 0
6. `git diff mix.exs` touches only the `docs/0` block — `deps/0` and `aliases/0` are unchanged
7. **Do NOT run `mix ci`** — RED at clean HEAD for pre-existing docs reasons (research correction C-02)
</verification>

<success_criteria>
- `guides/entitlements.md` exists, is registered in both `extras:` and the `"Canonical Guides"` group, and sits between the customer portal and metering guides
- The three modules share a new `Entitlements:` sidebar group placed between `"Billing Metering"` and `Connect:`
- The gate-helper refusal ships with the fail-closed replacement in the guide, and is recorded on `guides/scope.md`
- The Managing features section is a table so future verbs append one row
- Three stub sections reserve space for Phases 65 and 66
- No absolute documentation-site URLs in the new guide
</success_criteria>

<output>
Create `.planning/phases/63-stripe-native-entitlements/63-06-SUMMARY.md` when done.
</output>
