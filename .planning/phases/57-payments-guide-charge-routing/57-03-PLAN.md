---
phase: 57-payments-guide-charge-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 57-02
files_modified:
  - guides/production-checklist.md
  - guides/event-debugging.md
  - test/lattice_stripe/docs_truth_test.exs
autonomous: true
requirements:
  - ROUTE-02
must_haves:
  truths:
    - "Operator guides replace stale 'no separate Charge guide in v1.7' with cross-link to payments.md#charge-reconciliation."
    - "production-checklist mentions Charge retrieve/update/capture verb routing alongside list/search."
    - "event-debugging mentions Charge.update/4 and anti-pattern against Charge.capture/4 in PI webhook handlers."
    - "Full docs_truth suite passes."
  artifacts:
    - path: "guides/production-checklist.md"
      provides: "Charge update/capture audit routing"
      contains: "Charge.update/4"
    - path: "guides/event-debugging.md"
      provides: "charge.* webhook update/capture guidance"
      contains: "Charge.capture/4"
    - path: "guides/production-checklist.md"
      provides: "cross-link to canonical Charge section"
      contains: "payments.md#charge-reconciliation"
  key_links:
    - from: "guides/production-checklist.md"
      to: "guides/payments.md"
      via: "payments.md#charge-reconciliation anchor"
      pattern: "charge-reconciliation"
    - from: "guides/event-debugging.md"
      to: "guides/payments.md"
      via: "payments.md#charge-reconciliation anchor"
      pattern: "charge-reconciliation"
---

<objective>
Extend operator guide Charge routing spines with retrieve/update/capture verb bullets and cross-links to the canonical Charge reconciliation section in payments.md — completing ROUTE-02 without duplicating copy-paste snippets.

Purpose: Adopters following production-checklist or event-debugging for Charge workflows should discover update/capture alongside list/search and land on payments.md for full examples.
Output: Updated operator guides + optional docs_truth grep asserts for ROUTE-02 routing contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-payments-guide-charge-routing/57-CONTEXT.md
@.planning/phases/57-payments-guide-charge-routing/57-RESEARCH.md
@.planning/phases/57-payments-guide-charge-routing/57-PATTERNS.md
@guides/production-checklist.md
@guides/event-debugging.md
@guides/payments.md
@test/lattice_stripe/docs_truth_test.exs

<interfaces>
Replacement routing paragraph (both guides — D-19):
```markdown
Full workflows: [Payments — Charge reconciliation](payments.md#charge-reconciliation) and
`LatticeStripe.Charge` moduledoc.
```

production-checklist bullets to add (D-20):
```markdown
- Retrieve a known charge id from Dashboard/support: `LatticeStripe.Charge.retrieve/3`
- Update metadata or description on settled charges: `LatticeStripe.Charge.update/4` (not payment state)
- Capture uncaptured legacy direct charges only: `LatticeStripe.Charge.capture/4`; PI manual capture → [`PaymentIntent.capture/4`](payments.md#capturing-a-paymentintent-manual-capture)
```

event-debugging additions (D-21):
```markdown
- Attach support context after dispatch: `LatticeStripe.Charge.update/4` (e.g. ticket id in metadata); idempotency still keyed on `event.id`
- **Anti-pattern:** do not call `LatticeStripe.Charge.capture/4` from `charge.*` handlers for PaymentIntent flows — use PaymentIntent state or [`PaymentIntent.capture/4`](payments.md#capturing-a-paymentintent-manual-capture)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update production-checklist Charge routing spine (ROUTE-02)</name>
  <files>guides/production-checklist.md</files>
  <read_first>
    guides/production-checklist.md
    guides/payments.md
    .planning/phases/57-payments-guide-charge-routing/57-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `guides/production-checklist.md` §Support and audit lookups no longer contains `no separate Charge guide in v1.7`.
    - Section contains cross-link `payments.md#charge-reconciliation`.
    - Section contains bullet mentioning `LatticeStripe.Charge.retrieve/3`.
    - Section contains bullet mentioning `LatticeStripe.Charge.update/4`.
    - Section contains bullet mentioning `LatticeStripe.Charge.capture/4` with legacy/direct-charge framing.
    - Section contains cross-link to PaymentIntent.capture/4 (payments.md anchor or inline reference).
    - Existing list/3 and search/3 bullets remain present.
    - No new Elixir code fences added to this section (bullet shape only per D-22).
  </acceptance_criteria>
  <action>
In `## Support and audit lookups` section (lines 161–176):

1. Replace the line `Full API: \`LatticeStripe.Charge\` moduledoc (no separate Charge guide in v1.7).` with:

```markdown
Full workflows: [Payments — Charge reconciliation](payments.md#charge-reconciliation) and
`LatticeStripe.Charge` moduledoc.
```

2. Add these bullets after the existing search/3 bullet and before the Connect fee bullet:

```markdown
- Retrieve a known charge id from Dashboard/support: `LatticeStripe.Charge.retrieve/3`
- Update metadata or description on settled charges: `LatticeStripe.Charge.update/4` (not payment state)
- Capture uncaptured legacy direct charges only: `LatticeStripe.Charge.capture/4`; PI manual capture → [`PaymentIntent.capture/4`](payments.md#capturing-a-paymentintent-manual-capture)
```
  </action>
  <verify>
    <automated>rg -n 'charge-reconciliation|Charge.retrieve/3|Charge.update/4|Charge.capture/4' guides/production-checklist.md && ! rg 'no separate Charge guide' guides/production-checklist.md</automated>
  </verify>
  <done>production-checklist routes retrieve/update/capture with cross-link to payments.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update event-debugging charge.* routing spine (ROUTE-02)</name>
  <files>guides/event-debugging.md</files>
  <read_first>
    guides/event-debugging.md
    guides/payments.md
    .planning/phases/57-payments-guide-charge-routing/57-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `guides/event-debugging.md` §`charge.*` events no longer contains `no separate Charge guide in v1.7`.
    - Section contains cross-link `payments.md#charge-reconciliation`.
    - Section contains bullet mentioning `LatticeStripe.Charge.update/4`.
    - Section contains anti-pattern bullet mentioning do not call `Charge.capture/4` for PaymentIntent flows.
    - Existing retrieve/3 and search anti-pattern bullets remain present.
    - No new Elixir code fences added (bullet shape only per D-22).
  </acceptance_criteria>
  <action>
In `## \`charge.*\` events` section (lines 207–219):

1. Replace `Full API: \`LatticeStripe.Charge\` moduledoc (no separate Charge guide in v1.7).` with:

```markdown
Full workflows: [Payments — Charge reconciliation](payments.md#charge-reconciliation) and
`LatticeStripe.Charge` moduledoc.
```

2. Add after the existing search anti-pattern bullet:

```markdown
- Attach support context after dispatch: `LatticeStripe.Charge.update/4` (e.g. ticket id in metadata); idempotency still keyed on `event.id`
- **Anti-pattern:** do not call `LatticeStripe.Charge.capture/4` from `charge.*` handlers for PaymentIntent flows — use PaymentIntent state or [`PaymentIntent.capture/4`](payments.md#capturing-a-paymentintent-manual-capture)
```
  </action>
  <verify>
    <automated>rg -n 'charge-reconciliation|Charge.update/4|Anti-pattern.*Charge.capture/4' guides/event-debugging.md && ! rg 'no separate Charge guide' guides/event-debugging.md</automated>
  </verify>
  <done>event-debugging routes update/capture with PI-flow anti-pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional docs_truth operator guide routing asserts (ROUTE-02)</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    test/lattice_stripe/docs_truth_test.exs
    guides/production-checklist.md
    guides/event-debugging.md
    .planning/phases/57-payments-guide-charge-routing/57-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - Test file contains test named `"operator guides route Charge update/capture to payments reconciliation"`.
    - Test reads `guides/production-checklist.md` and asserts `checklist =~ "Charge.update/4"`.
    - Test asserts `checklist =~ "Charge.capture/4"`.
    - Test asserts `checklist =~ "payments.md#charge-reconciliation"`.
    - Test reads `guides/event-debugging.md` and asserts `debugging =~ "Charge.update/4"`.
    - Test asserts `debugging =~ "Charge.capture/4"`.
    - Test asserts `debugging =~ "payments.md#charge-reconciliation"`.
    - `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>
Add standalone test after the `describe "guides/payments.md"` block (before Charge moduledoc test):

```elixir
test "operator guides route Charge update/capture to payments reconciliation" do
  checklist = File.read!("guides/production-checklist.md")
  debugging = File.read!("guides/event-debugging.md")

  assert checklist =~ "Charge.update/4"
  assert checklist =~ "Charge.capture/4"
  assert checklist =~ "payments.md#charge-reconciliation"

  assert debugging =~ "Charge.update/4"
  assert debugging =~ "Charge.capture/4"
  assert debugging =~ "payments.md#charge-reconciliation"
end
```
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors</automated>
  </verify>
  <done>ROUTE-02 grep-locked; full docs_truth suite green.</done>
</task>

</tasks>

<verification_criteria>
- [ ] Both operator guides cross-link to payments.md#charge-reconciliation
- [ ] Stale "no separate Charge guide in v1.7" removed from both guides
- [ ] production-checklist has retrieve/update/capture verb bullets
- [ ] event-debugging has update bullet and capture anti-pattern
- [ ] docs_truth operator routing test passes
- [ ] `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` passes
</verification_criteria>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Duplicate snippets drift from payments.md SSOT | low | Bullet + link only; no code fences in operator guides (D-18, D-22) |
| Charge.capture in PI webhook handler | medium | Explicit anti-pattern in event-debugging (D-21) |
| Broken anchor link if section renamed | low | docs_truth asserts payments.md#charge-reconciliation in both guides |
</threat_model>
