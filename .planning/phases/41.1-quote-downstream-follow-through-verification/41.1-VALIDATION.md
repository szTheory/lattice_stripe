---
phase: 41.1
slug: quote-downstream-follow-through-verification
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-25
---

# Phase 41.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `mix run` proof script against Stripe sandbox plus existing ExUnit unit suites |
| **Config file** | none for the probe script; existing Mix test config for supporting unit suites |
| **Quick run command** | `STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs` |
| **Full suite command** | `mix test test/lattice_stripe/object_types_test.exs test/lattice_stripe/quote_test.exs --warnings-as-errors` |
| **Estimated runtime** | ~15-60 seconds depending on sandbox latency |

---

## Sampling Rate

- **After every task commit:** Run the task’s `<automated>` command
- **After every plan wave:** Re-run the sandbox probe command
- **Before `$gsd-verify-work`:** Re-run the sandbox probe command and record the observed downstream reference type
- **Max feedback latency:** ~60 seconds once sandbox credentials are available

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 41.1-01-01 | 01 | 1 | follow-through proof | T-41.1-01 | Script reads `STRIPE_SECRET_KEY` from env only and uses the shipped SDK client against real Stripe, not `stripe-mock` | docs + runtime | `rg -n 'STRIPE_SECRET_KEY|Quote\\.accept|Invoice\\.retrieve|Subscription\\.retrieve|SubscriptionSchedule\\.retrieve' scripts/verify_quote_follow_through.exs` | ❌ W0 | ⬜ pending |
| 41.1-01-02 | 01 | 1 | follow-through proof | T-41.1-02 | Probe produces exactly one downstream retrieve in priority order and exits non-zero when no reference is exposed | runtime | `STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs` | ❌ W0 | ⬜ pending |
| 41.1-02-01 | 02 | 2 | verifier credibility | T-41.1-03 | `41.1-VERIFICATION.md` records the chosen environment, exact command, observed downstream ref type, and bounded claim | docs + evidence audit | `rg -n 'status: closed|sandbox|test mode|invoice|subscription|subscription_schedule|mix run scripts/verify_quote_follow_through.exs' .planning/phases/41.1-quote-downstream-follow-through-verification/41.1-VERIFICATION.md` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠ flaky*

---

## Wave-Level Verification

- **After Plan 01:** `STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs`
- **After Plan 02:** `rg -n "status: closed|sandbox|invoice|subscription|subscription_schedule|mix run scripts/verify_quote_follow_through.exs" .planning/phases/41.1-quote-downstream-follow-through-verification/41.1-VERIFICATION.md`

---

## Wave 0 Requirements

- [ ] `scripts/verify_quote_follow_through.exs` — env-gated sandbox probe exists
- [ ] `.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-VERIFICATION.md` — closed verifier artifact exists

*Existing Quote and object-type unit infrastructure covers the rest once these new artifacts exist.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The provided `STRIPE_SECRET_KEY` belongs to a Stripe sandbox that allows object creation | follow-through proof | The repo cannot guarantee external account state or secret provenance | Export `STRIPE_SECRET_KEY` from a dedicated sandbox, run the probe script, and confirm object-creation calls succeed without local `base_url` overrides |
| The verifier does not overclaim real billing semantics beyond one downstream retrieve | verifier credibility | Human judgment is required to keep external-state claims bounded | Read `41.1-VERIFICATION.md` and confirm it cites only accepted quote reference exposure plus one typed top-level downstream retrieve |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 references are identified explicitly
- [x] No watch-mode flags
- [x] Feedback latency acceptable for a credential-gated external proof
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-25
