# Phase 41.1: Quote Downstream Follow-Through Verification - Pattern Map

**Mapped:** 2026-05-25
**Files analyzed:** 3
**Analogs found:** 3 / 3

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `scripts/verify_quote_follow_through.exs` | script | request-response | `scripts/verify_portal_endpoint.exs` + `scripts/verify_stripe_mock_reject.exs` | exact |
| `.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-VERIFICATION.md` | docs | evidence-audit | `.planning/phases/35-mandate-setupattempt/35-VERIFICATION.md` | exact |
| optional supporting reuse from `test/support/fixtures/quote.ex` | test helper reference | transform | current `test/support/fixtures/quote.ex` | exact |

## Pattern Assignments

### `scripts/verify_quote_follow_through.exs`

**Primary analog:** [scripts/verify_portal_endpoint.exs](/Users/jon/projects/lattice_stripe/scripts/verify_portal_endpoint.exs:1)  
**Supporting analog:** [scripts/verify_stripe_mock_reject.exs](/Users/jon/projects/lattice_stripe/scripts/verify_stripe_mock_reject.exs:1)

Copy these patterns:

- File header documents purpose, usage, prerequisites, and exit codes
- Script starts required runtime services defensively
- Output is concise and machine-readable enough for verifier citation
- Failure cases call `System.halt(1)` with an explicit reason

Do **not** copy:

- Raw `:httpc` request construction for the main proof path

Instead:

- Use `LatticeStripe.Client.new!/1` and the shipped SDK modules so the proof exercises the library surface, not a side-channel HTTP client

### Quote creation and lifecycle shape

**Primary analog:** [test/support/fixtures/quote.ex](/Users/jon/projects/lattice_stripe/test/support/fixtures/quote.ex:172)  
**Supporting analog:** [test/integration/quote_integration_test.exs](/Users/jon/projects/lattice_stripe/test/integration/quote_integration_test.exs:35)

Copy these patterns:

- Create a customer first
- Create a product first
- Build quote line items with `price_data.product`
- Use explicit `Quote.finalize/4` then `Quote.accept/3`
- Keep downstream proof bounded to one retrieve

Do **not** copy:

- Local `stripe-mock` setup assumptions such as `base_url: "http://localhost:12111"`
- Assertions that the mock-specific lifecycle boundary proves more than route/decode sanity

### `41.1-VERIFICATION.md`

**Primary analog:** [.planning/phases/35-mandate-setupattempt/35-VERIFICATION.md](/Users/jon/projects/lattice_stripe/.planning/phases/35-mandate-setupattempt/35-VERIFICATION.md:1)

Copy these patterns:

- Closed-verifier frontmatter
- Goal Achievement -> Observable Truths table
- Behavioral Spot-Checks and Verification Evidence sections
- Explicit evidence column tied to exact files and commands

Adapt for Phase 41.1:

- No v1.3 requirement rows need updating
- The verifier must emphasize the chosen environment and exact downstream reference type
- The verifier must state that Phase 41 remains the bounded `stripe-mock` closure and that Phase 41.1 adds the external follow-through proof without rewriting that boundary
