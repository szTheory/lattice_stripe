---
phase: 41.1-quote-downstream-follow-through-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_quote_follow_through.exs
autonomous: false
requirements: []
must_haves:
  truths:
    - "Phase 41.1 uses an environment that can actually expose one downstream reference after `Quote.accept/3`, rather than retrying known-bad local `stripe-mock` behavior."
    - "The proof path exercises the shipped SDK client and resource modules, not ad-hoc raw HTTP."
    - "Exactly one downstream resource is retrieved in the preference order invoice -> subscription -> subscription_schedule."
    - "The proof stays bounded to typed top-level decode and does not broaden into payment, activation, webhook, or orchestration semantics."
    - "Secrets stay environment-only and the proof remains opt-in outside the default test loop."
  artifacts:
    - path: scripts/verify_quote_follow_through.exs
      provides: "Opt-in Stripe sandbox proof for quote accept -> one downstream retrieve"
      contains: "System.fetch_env!(\"STRIPE_SECRET_KEY\")"
  key_links:
    - from: scripts/verify_quote_follow_through.exs
      to: lib/lattice_stripe/quote.ex
      via: "Quote.create/finalize/accept proof path"
      pattern: "Quote\\.(create|finalize|accept)"
    - from: scripts/verify_quote_follow_through.exs
      to: lib/lattice_stripe/(invoice|subscription|subscription_schedule).ex
      via: "exactly one downstream retrieve"
      pattern: "(Invoice|Subscription|SubscriptionSchedule)\\.retrieve"
---

<objective>
Create the smallest credible external proof path for the displaced Quote downstream follow-through requirement.

Purpose: Phase 41 already proved current local `stripe-mock` cannot expose downstream references after `Quote.accept/3`. This plan adds an opt-in sandbox probe that uses the shipped SDK against real Stripe test infrastructure and proves exactly one downstream retrieve when the environment exposes it.
Output: `scripts/verify_quote_follow_through.exs` plus a successful sandbox probe run recorded for Plan 02 to cite.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-CONTEXT.md
@.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-RESEARCH.md
@.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-PATTERNS.md
@.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-VALIDATION.md
@.planning/phases/41-quote-lifecycle-e2e-verification/41-RESEARCH.md
@scripts/verify_portal_endpoint.exs
@scripts/verify_stripe_mock_reject.exs
@test/support/fixtures/quote.ex
@test/integration/quote_integration_test.exs
@lib/lattice_stripe/quote.ex
@lib/lattice_stripe/invoice.ex
@lib/lattice_stripe/subscription.ex
@lib/lattice_stripe/subscription_schedule.ex

<interfaces>
Use the shipped client surface:
```elixir
client = LatticeStripe.Client.new!(
  api_key: System.fetch_env!("STRIPE_SECRET_KEY"),
  finch: VerifyQuoteFollowThrough.Finch,
  telemetry_enabled: false,
  max_retries: 0
)
```

Required proof depth:
```elixir
Quote.create/3
Quote.finalize/4
Quote.accept/3
Invoice.retrieve/3 | Subscription.retrieve/3 | SubscriptionSchedule.retrieve/3
```

Required verification command:
```bash
STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the sandbox probe script around the shipped SDK path</name>
  <read_first>
    - scripts/verify_portal_endpoint.exs
    - scripts/verify_stripe_mock_reject.exs
    - test/support/fixtures/quote.ex
    - test/integration/quote_integration_test.exs
    - lib/lattice_stripe/quote.ex
    - lib/lattice_stripe/invoice.ex
    - lib/lattice_stripe/subscription.ex
    - lib/lattice_stripe/subscription_schedule.ex
    - .planning/phases/41.1-quote-downstream-follow-through-verification/41.1-RESEARCH.md
  </read_first>
  <files>scripts/verify_quote_follow_through.exs</files>
  <acceptance_criteria>
    - `scripts/verify_quote_follow_through.exs` exists.
    - `rg -n 'System\\.fetch_env!\\(\"STRIPE_SECRET_KEY\"\\)|LatticeStripe\\.Client\\.new!|Quote\\.create|Quote\\.finalize|Quote\\.accept' scripts/verify_quote_follow_through.exs` finds the env-gated client and the Quote proof path.
    - `rg -n 'Invoice\\.retrieve|Subscription\\.retrieve|SubscriptionSchedule\\.retrieve' scripts/verify_quote_follow_through.exs` shows ordered fallback support across all three downstream retrieval modules.
    - `rg -n 'invoice|subscription|subscription_schedule|System\\.halt' scripts/verify_quote_follow_through.exs` shows preference-order inspection plus explicit pass/fail exits.
  </acceptance_criteria>
  <action>Create `scripts/verify_quote_follow_through.exs` as a repo-local, opt-in sandbox probe. Follow the script-documentation style from `verify_portal_endpoint.exs`: include a header with purpose, usage, required env vars, and exit codes. Start Finch in-script if needed. Build a real Stripe client with `api_key: System.fetch_env!(\"STRIPE_SECRET_KEY\")`, default `base_url` (do not point at localhost), `telemetry_enabled: false`, and `max_retries: 0`. Inside the script, create a disposable customer and product, then create a quote using Product-backed `price_data.product`, finalize it, accept it, inspect the accepted quote in this exact order: `invoice`, otherwise `subscription`, otherwise `subscription_schedule`. Retrieve exactly one linked downstream resource with the corresponding SDK module and pattern-match on a typed top-level struct only. Print a compact success line that includes the chosen downstream type and resource ID. If the accepted quote still exposes no downstream reference, print that exact failure and exit non-zero. Do not add any new public library APIs or test-only helpers for this phase.</action>
  <verify>
    <automated>rg -n 'System\.fetch_env!\("STRIPE_SECRET_KEY"\)|Quote\.(create|finalize|accept)|(Invoice|Subscription|SubscriptionSchedule)\.retrieve|System\.halt' scripts/verify_quote_follow_through.exs</automated>
  </verify>
  <done>The repo contains an opt-in sandbox probe that exercises the shipped SDK and can prove one downstream retrieve without polluting the default test loop.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sandbox probe and capture the exact downstream follow-through outcome</name>
  <read_first>
    - scripts/verify_quote_follow_through.exs
    - .planning/phases/41.1-quote-downstream-follow-through-verification/41.1-CONTEXT.md
    - .planning/phases/41.1-quote-downstream-follow-through-verification/41.1-RESEARCH.md
  </read_first>
  <files>scripts/verify_quote_follow_through.exs</files>
  <acceptance_criteria>
    - `STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs` exits 0 in the chosen sandbox.
    - The observed output identifies exactly one downstream type: `invoice`, `subscription`, or `subscription_schedule`.
    - The successful run does not rely on `base_url: "http://localhost:12111"` or any `stripe-mock` wording.
    - If the script exits non-zero because the sandbox still exposes no downstream reference, that exact outcome is captured in the execution summary for Plan 02 instead of being silently ignored.
  </acceptance_criteria>
  <action>Execute `mix run scripts/verify_quote_follow_through.exs` with `STRIPE_SECRET_KEY` set from a dedicated Stripe sandbox. If the sandbox credentials are missing, stop and request them rather than faking proof. If the script succeeds, keep the output concise and preserve the observed downstream type and resource ID in the plan summary so Plan 02 can cite them verbatim. If the script fails because the environment still exposes no downstream reference, preserve the failure message and stop there; Phase 41.1 exists specifically to surface that truth, not to bury it.</action>
  <verify>
    <automated>STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs</automated>
  </verify>
  <done>The exact downstream follow-through outcome is freshly observed in a real Stripe sandbox and ready for verifier capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> Stripe sandbox | The phase depends on external state and test credentials, so proof must be explicit about environment and secret handling |
| accepted quote -> downstream retrieve claim | Over-reading the accepted quote could convert one observed link into broader workflow claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-01 | I | sandbox credentials | mitigate | Read `STRIPE_SECRET_KEY` from environment only, never commit secrets or dashboard-specific IDs |
| T-41.1-02 | R | downstream follow-through proof | mitigate | Retrieve exactly one downstream resource in fixed priority order and stop after typed top-level decode |
| T-41.1-03 | T | scope boundary | mitigate | Forbid new Quote APIs, orchestration helpers, or test-framework expansion beyond the single probe script |
</threat_model>

<verification>
- `rg -n 'System\.fetch_env!\("STRIPE_SECRET_KEY"\)|Quote\.(create|finalize|accept)|(Invoice|Subscription|SubscriptionSchedule)\.retrieve|System\.halt' scripts/verify_quote_follow_through.exs`
- `STRIPE_SECRET_KEY=sk_test_... mix run scripts/verify_quote_follow_through.exs`
</verification>

<success_criteria>
- The repo contains an opt-in sandbox probe that uses the shipped SDK rather than `stripe-mock`.
- A fresh probe run exposes exactly one downstream reference type and retrieves exactly one linked downstream resource.
- The proof remains bounded to typed top-level decode and does not broaden into business-semantics claims.
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-quote-downstream-follow-through-verification/41.1-01-SUMMARY.md`
</output>
