---
phase: 14-invoices-invoice-line-items
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - lib/lattice_stripe/invoice.ex
  - lib/lattice_stripe/invoice_item.ex
  - test/lattice_stripe/invoice_test.exs
  - test/lattice_stripe/invoice_item_test.exs
autonomous: true
requirements: [BILL-04, BILL-10]

must_haves:
  truths:
    - "Developer can create, retrieve, update, delete, list, and stream Invoices"
    - "Developer can create, retrieve, update, delete, list, and stream InvoiceItems"
    - "Invoice.from_map/1 parses nested StatusTransitions, AutomaticTax, Discount, lines as List of LineItems"
    - "Invoice status, collection_method, billing_reason, customer_tax_exempt are atomized per D-14g whitelist"
    - "Invoice moduledoc contains ASCII lifecycle state diagram per D-14i"
    - "InvoiceItem.from_map/1 parses nested Period struct"
    - "InvoiceItem moduledoc documents InvoiceItem vs Invoice Line Item distinction per D-14e"
    - "InvoiceItem moduledoc documents no-search constraint per D-05"
  artifacts:
    - path: "lib/lattice_stripe/invoice.ex"
      provides: "Invoice resource module with CRUD + struct + from_map"
      contains: "defmodule LatticeStripe.Invoice"
      exports: ["create", "retrieve", "update", "delete", "list", "stream!"]
    - path: "lib/lattice_stripe/invoice_item.ex"
      provides: "InvoiceItem standalone CRUD resource"
      contains: "defmodule LatticeStripe.InvoiceItem"
      exports: ["create", "retrieve", "update", "delete", "list", "stream!"]
  key_links:
    - from: "lib/lattice_stripe/invoice.ex"
      to: "lib/lattice_stripe/invoice/status_transitions.ex"
      via: "StatusTransitions.from_map in Invoice.from_map"
      pattern: "StatusTransitions\\.from_map"
    - from: "lib/lattice_stripe/invoice.ex"
      to: "lib/lattice_stripe/invoice/automatic_tax.ex"
      via: "AutomaticTax.from_map in Invoice.from_map"
      pattern: "AutomaticTax\\.from_map"
    - from: "lib/lattice_stripe/invoice.ex"
      to: "lib/lattice_stripe/invoice/line_item.ex"
      via: "LineItem.from_map in lines List parsing"
      pattern: "LineItem\\.from_map"
    - from: "lib/lattice_stripe/invoice_item.ex"
      to: "lib/lattice_stripe/invoice_item/period.ex"
      via: "Period.from_map in InvoiceItem.from_map"
      pattern: "Period\\.from_map"
---

<objective>
Create the Invoice and InvoiceItem resource modules with full CRUD operations, struct definitions, from_map/1 parsing with typed nested structs, atomized enum fields, and lifecycle documentation.

Purpose: These are the two primary resource modules for Phase 14. All subsequent plans (action verbs, search, upcoming, telemetry) build on top of the Invoice module created here.
Output: lib/lattice_stripe/invoice.ex and lib/lattice_stripe/invoice_item.ex with comprehensive unit tests.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-invoices-invoice-line-items/14-CONTEXT.md
@.planning/phases/14-invoices-invoice-line-items/14-01-SUMMARY.md

<interfaces>
<!-- Typed structs from Plan 01 — executor uses these in from_map/1 -->

From lib/lattice_stripe/invoice/status_transitions.ex:
```elixir
defmodule LatticeStripe.Invoice.StatusTransitions do
  defstruct [:finalized_at, :marked_uncollectible_at, :paid_at, :voided_at]
  @spec from_map(map() | nil) :: t() | nil
end
```

From lib/lattice_stripe/invoice/automatic_tax.ex:
```elixir
defmodule LatticeStripe.Invoice.AutomaticTax do
  defstruct [:enabled, :liability, :status]
  @spec from_map(map() | nil) :: t() | nil
end
```

From lib/lattice_stripe/invoice/line_item.ex:
```elixir
defmodule LatticeStripe.Invoice.LineItem do
  @known_fields ~w[id object amount amount_excluding_tax currency description ...]
  defstruct [..., object: "line_item", extra: %{}]
  @spec from_map(map() | nil) :: t() | nil
end
```

From lib/lattice_stripe/invoice_item/period.ex:
```elixir
defmodule LatticeStripe.InvoiceItem.Period do
  defstruct [:start, :end]
  @spec from_map(map() | nil) :: t() | nil
end
```

From lib/lattice_stripe/customer.ex (CRUD template):
```elixir
def create(%Client{} = client, params \\ %{}, opts \\ []) do
  %Request{method: :post, path: "/v1/customers", params: params, opts: opts}
  |> then(&Client.request(client, &1))
  |> Resource.unwrap_singular(&from_map/1)
end
def retrieve(%Client{} = client, id, opts \\ []) when is_binary(id) do
  %Request{method: :get, path: "/v1/customers/#{id}", opts: opts}
  |> then(&Client.request(client, &1))
  |> Resource.unwrap_singular(&from_map/1)
end
```

From lib/lattice_stripe/resource.ex:
```elixir
def unwrap_singular(result, from_map_fn)
def unwrap_list(result, from_map_fn)
```

From lib/lattice_stripe/list.ex:
```elixir
def from_json(json_map, item_from_map_fn, request_context \\ {%{}, []})
```

From lib/lattice_stripe/discount.ex:
```elixir
defmodule LatticeStripe.Discount do
  @spec from_map(map() | nil) :: t() | nil
end
```

From lib/lattice_stripe/product.ex (atomization pattern):
```elixir
defp atomize_type("good"), do: :good
defp atomize_type("service"), do: :service
defp atomize_type(other), do: other
```

From lib/lattice_stripe/coupon.ex (no-search moduledoc pattern):
```elixir
## Operations not supported by the Stripe API
- **search** — The `/v1/coupons/search` endpoint does not exist. Use `list/2` with filters for discovery.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LatticeStripe.Invoice module — struct, from_map/1 with typed nesteds + atomization, CRUD operations</name>
  <files>
    lib/lattice_stripe/invoice.ex
    test/lattice_stripe/invoice_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/customer.ex
    lib/lattice_stripe/payment_intent.ex
    lib/lattice_stripe/product.ex
    lib/lattice_stripe/coupon.ex
    lib/lattice_stripe/list.ex
    lib/lattice_stripe/discount.ex
    lib/lattice_stripe/invoice/status_transitions.ex
    lib/lattice_stripe/invoice/automatic_tax.ex
    lib/lattice_stripe/invoice/line_item.ex
    test/lattice_stripe/customer_test.exs
    test/lattice_stripe/product_test.exs
  </read_first>
  <behavior>
    - Invoice.from_map(%{"id" => "in_xxx", "status" => "draft", ...}) returns %Invoice{id: "in_xxx", status: :draft, ...}
    - Invoice.from_map atomizes status to :draft/:open/:paid/:void/:uncollectible, unknown strings pass through
    - Invoice.from_map atomizes collection_method to :charge_automatically/:send_invoice
    - Invoice.from_map atomizes billing_reason to :subscription_cycle/:subscription_create/:subscription_update/:subscription_threshold/:subscription/:manual/:upcoming
    - Invoice.from_map atomizes customer_tax_exempt to :none/:exempt/:reverse
    - Invoice.from_map parses status_transitions via StatusTransitions.from_map
    - Invoice.from_map parses automatic_tax via AutomaticTax.from_map
    - Invoice.from_map parses discount via Discount.from_map
    - Invoice.from_map parses discounts as list via Enum.map(Discount.from_map)
    - Invoice.from_map parses lines via List.from_json + LineItem.from_map when lines is a map with "object" => "list"
    - Invoice.from_map captures unknown fields in extra: %{}
    - Invoice.create/3 POSTs to /v1/invoices
    - Invoice.retrieve/3 GETs /v1/invoices/:id
    - Invoice.update/4 POSTs to /v1/invoices/:id
    - Invoice.delete/3 DELETEs /v1/invoices/:id
    - Invoice.list/3 GETs /v1/invoices
    - Invoice.stream!/2 streams invoices
    - All CRUD functions have bang variants
  </behavior>
  <action>
Create `lib/lattice_stripe/invoice.ex` as `LatticeStripe.Invoice` following the Customer template exactly for CRUD and the Product template for atomization.

**Module structure:**
1. `@moduledoc` with:
   - Brief description: "Operations on Stripe Invoice objects."
   - ASCII lifecycle state diagram per D-14i:
     ```
     draft --> (finalize) --> open --> (pay) --> paid
                                |
                              (void) --> void
                                |
                        (mark_uncollectible) --> uncollectible
     ```
   - "Common workflow" section: `create(auto_advance: false) -> add InvoiceItems -> finalize -> pay`
   - Note about `auto_advance` default behavior and ~1h finalization window

2. Aliases: `LatticeStripe.{Client, Discount, Error, List, Request, Resource, Response}` and `LatticeStripe.Invoice.{AutomaticTax, LineItem, StatusTransitions}`

3. `@known_fields ~w[id object account_country account_name account_tax_ids amount_due amount_paid amount_remaining amount_shipping application application_fee_amount attempt_count attempted auto_advance automatic_tax billing_reason charge collection_method created currency customer customer_address customer_email customer_name customer_phone customer_shipping customer_tax_exempt customer_tax_ids custom_fields default_payment_method default_source default_tax_rates description discount discounts due_date effective_at ending_balance footer from_invoice hosted_invoice_url invoice_pdf issuer last_finalization_error latest_revision lines livemode metadata next_payment_attempt number on_behalf_of paid paid_out_of_band payment_intent payment_settings period_end period_start post_payment_credit_notes_amount pre_payment_credit_notes_amount quote receipt_number rendering rendering_options shipping_cost shipping_details starting_balance statement_descriptor status status_transitions subscription subscription_details subscription_proration_date subtotal subtotal_excluding_tax tax test_clock threshold_reason total total_discount_amounts total_excluding_tax total_tax_amounts transfer_data webhooks_delivered_at]`

4. Add `# credo:disable-for-next-line Credo.Check.Warning.StructFieldAmount` before defstruct (>15 fields)

5. `defstruct` all known fields as nil-defaulted atoms, plus `object: "invoice"` and `extra: %{}`

6. `@type t` with appropriate types: `status` as `atom() | String.t() | nil`, `collection_method` as `atom() | String.t() | nil`, etc. `status_transitions` as `StatusTransitions.t() | nil`, `automatic_tax` as `AutomaticTax.t() | nil`, `discount` as `Discount.t() | nil`, `discounts` as `list(Discount.t()) | nil`, `lines` as `List.t() | nil`

7. `from_map/1`:
   - `def from_map(nil), do: nil`
   - `def from_map(%{} = map)` — split known/extra, assign fields from string keys
   - Atomize: `status`, `collection_method`, `billing_reason`, `customer_tax_exempt` via private `atomize_*` helpers with whitelist + String.t() catch-all (per D-14g, exact values listed in CONTEXT.md)
   - Parse nested: `StatusTransitions.from_map(map["status_transitions"])`, `AutomaticTax.from_map(map["automatic_tax"])`, `Discount.from_map(map["discount"])`
   - Parse discounts list: `parse_discounts(map["discounts"])` private helper mapping over list
   - Parse lines: `parse_lines(map["lines"])` private helper that calls `List.from_json(lines_map, &LineItem.from_map/1)` when map has `"object" => "list"`, else nil

8. CRUD functions (follow Customer exactly):
   - `create/3` — `POST /v1/invoices`
   - `retrieve/3` — `GET /v1/invoices/:id` (3rd arg is opts keyword, NOT params — per existing pattern, retrieve passes opts not params)
   - `update/4` — `POST /v1/invoices/:id`
   - `delete/3` — `DELETE /v1/invoices/:id` (per D-14i, exists for draft invoices)
   - `list/3` — `GET /v1/invoices`
   - `stream!/2` — lazy stream using `List.stream!/2` pattern
   - Bang variants for all: `create!/3`, `retrieve!/3`, `update!/4`, `delete!/3`
   - Per-function `@doc` with brief description. `delete/3` notes "Only applicable to draft invoices."

9. Custom `Inspect` impl hiding `:extra` when empty (follow Customer pattern if exists, otherwise Product)

Write `test/lattice_stripe/invoice_test.exs` with:
- `from_map/1` tests: full map with all nested types, nil input, atomization for all 4 fields (known + unknown values), nested struct parsing, lines List parsing, extra field capture
- Mox-based CRUD tests for create/retrieve/update/delete/list using test_client and Transport mock (follow customer_test.exs pattern exactly)
- Bang variant test for at least one operation
- All tests use `async: true`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix test test/lattice_stripe/invoice_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - lib/lattice_stripe/invoice.ex contains `defmodule LatticeStripe.Invoice`
    - lib/lattice_stripe/invoice.ex contains `@known_fields ~w[` with at least 60 fields
    - lib/lattice_stripe/invoice.ex contains `object: "invoice"` in defstruct
    - lib/lattice_stripe/invoice.ex contains `extra: %{}` in defstruct
    - lib/lattice_stripe/invoice.ex contains `def from_map(nil), do: nil`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_status("draft"), do: :draft`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_status("open"), do: :open`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_status("paid"), do: :paid`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_status("void"), do: :void`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_status("uncollectible"), do: :uncollectible`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_collection_method("charge_automatically"), do: :charge_automatically`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_collection_method("send_invoice"), do: :send_invoice`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_billing_reason("manual"), do: :manual`
    - lib/lattice_stripe/invoice.ex contains `defp atomize_customer_tax_exempt("exempt"), do: :exempt`
    - lib/lattice_stripe/invoice.ex contains `StatusTransitions.from_map`
    - lib/lattice_stripe/invoice.ex contains `AutomaticTax.from_map`
    - lib/lattice_stripe/invoice.ex contains `Discount.from_map`
    - lib/lattice_stripe/invoice.ex contains `LineItem.from_map`
    - lib/lattice_stripe/invoice.ex contains `def create(%Client{} = client`
    - lib/lattice_stripe/invoice.ex contains `path: "/v1/invoices"`
    - lib/lattice_stripe/invoice.ex contains `def retrieve(%Client{} = client`
    - lib/lattice_stripe/invoice.ex contains `def update(%Client{} = client`
    - lib/lattice_stripe/invoice.ex contains `def delete(%Client{} = client`
    - lib/lattice_stripe/invoice.ex contains `def list(%Client{} = client`
    - lib/lattice_stripe/invoice.ex contains `draft` and `finalize` and `open` and `paid` in moduledoc lifecycle diagram
    - test/lattice_stripe/invoice_test.exs passes with 0 failures
  </acceptance_criteria>
  <done>Invoice module has struct with 60+ known fields, from_map/1 parsing all 4 atomized fields + 4 typed nested structs + lines List + extra catch-all, full CRUD (create/retrieve/update/delete/list/stream!) with bang variants, lifecycle ASCII diagram in moduledoc, and comprehensive unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LatticeStripe.InvoiceItem module — struct, from_map/1 with Period nested, CRUD operations</name>
  <files>
    lib/lattice_stripe/invoice_item.ex
    test/lattice_stripe/invoice_item_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/coupon.ex
    lib/lattice_stripe/customer.ex
    lib/lattice_stripe/invoice_item/period.ex
    test/lattice_stripe/coupon_test.exs
  </read_first>
  <behavior>
    - InvoiceItem.from_map(%{"id" => "ii_xxx", ...}) returns %InvoiceItem{id: "ii_xxx", ...}
    - InvoiceItem.from_map parses period via Period.from_map
    - InvoiceItem.from_map captures unknown fields in extra
    - InvoiceItem.create/3 POSTs to /v1/invoiceitems
    - InvoiceItem.retrieve/3 GETs /v1/invoiceitems/:id
    - InvoiceItem.update/4 POSTs to /v1/invoiceitems/:id
    - InvoiceItem.delete/3 DELETEs /v1/invoiceitems/:id
    - InvoiceItem.list/3 GETs /v1/invoiceitems
    - InvoiceItem.stream!/2 streams invoice items
  </behavior>
  <action>
Create `lib/lattice_stripe/invoice_item.ex` as `LatticeStripe.InvoiceItem` following the Coupon template (CRUD with no search).

**Module structure:**
1. `@moduledoc` with:
   - Brief description: "Operations on Stripe InvoiceItem objects."
   - "InvoiceItem vs Invoice Line Item" disambiguation section per D-14e:
     "**InvoiceItem** (this module) is a standalone resource you create explicitly at `/v1/invoiceitems`. Mutatable while the target invoice is in draft status. **Invoice Line Item** (`LatticeStripe.Invoice.LineItem`) is a read-only rendered row on a finalized invoice, accessed via `Invoice.list_line_items/4`."
   - "Operations not supported by the Stripe API" section per D-05: "**search** -- The `/v1/invoiceitems/search` endpoint does not exist. Use `list/2` with filters for discovery."
   - Note on draft constraint: "InvoiceItems can only be added to invoices in draft status. Once finalized, line items are locked."

2. Aliases: `LatticeStripe.{Client, Error, Request, Resource, Response}` and `LatticeStripe.InvoiceItem.Period`

3. `@known_fields ~w[id object amount currency customer date description discountable discounts invoice livemode metadata period plan price proration proration_details quantity subscription subscription_item tax_rates test_clock unit_amount unit_amount_decimal unit_amount_excluding_tax]`

4. `defstruct` all known fields as nil-defaulted atoms, plus `object: "invoiceitem"` and `extra: %{}`

5. `@type t` struct type

6. `from_map/1`:
   - `def from_map(nil), do: nil`
   - `def from_map(%{} = map)` — split known/extra, assign from string keys
   - Parse nested: `Period.from_map(map["period"])`
   - Everything else stays as raw map values per D-14e

7. CRUD functions at `/v1/invoiceitems` (note: Stripe uses "invoiceitems" not "invoice_items"):
   - `create/3` — `POST /v1/invoiceitems`. `@doc` notes "Only applicable to draft invoices."
   - `retrieve/3` — `GET /v1/invoiceitems/:id`
   - `update/4` — `POST /v1/invoiceitems/:id`. `@doc` notes "Only applicable to draft invoices."
   - `delete/3` — `DELETE /v1/invoiceitems/:id`. `@doc` notes "Only applicable to draft invoices."
   - `list/3` — `GET /v1/invoiceitems`
   - `stream!/2` — lazy stream
   - Bang variants for all

8. Custom `Inspect` impl hiding `:extra` when empty

Write `test/lattice_stripe/invoice_item_test.exs` with:
- `from_map/1` tests: full map, nil input, Period nested parsing, extra field capture
- Mox-based CRUD tests for create/retrieve/update/delete/list
- All tests `async: true`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix test test/lattice_stripe/invoice_item_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - lib/lattice_stripe/invoice_item.ex contains `defmodule LatticeStripe.InvoiceItem`
    - lib/lattice_stripe/invoice_item.ex contains `@known_fields ~w[` with at least 20 fields
    - lib/lattice_stripe/invoice_item.ex contains `object: "invoiceitem"` in defstruct
    - lib/lattice_stripe/invoice_item.ex contains `extra: %{}` in defstruct
    - lib/lattice_stripe/invoice_item.ex contains `Period.from_map`
    - lib/lattice_stripe/invoice_item.ex contains `path: "/v1/invoiceitems"`
    - lib/lattice_stripe/invoice_item.ex contains `def create(%Client{} = client`
    - lib/lattice_stripe/invoice_item.ex contains `def retrieve(%Client{} = client`
    - lib/lattice_stripe/invoice_item.ex contains `def update(%Client{} = client`
    - lib/lattice_stripe/invoice_item.ex contains `def delete(%Client{} = client`
    - lib/lattice_stripe/invoice_item.ex contains `def list(%Client{} = client`
    - lib/lattice_stripe/invoice_item.ex contains `InvoiceItem vs Invoice Line Item` in moduledoc
    - lib/lattice_stripe/invoice_item.ex contains `Operations not supported` in moduledoc
    - lib/lattice_stripe/invoice_item.ex contains `draft` in docs (draft constraint notes)
    - test/lattice_stripe/invoice_item_test.exs passes with 0 failures
  </acceptance_criteria>
  <done>InvoiceItem module has struct with 20+ known fields, from_map/1 with Period nested parsing + extra catch-all, full CRUD at /v1/invoiceitems with bang variants, InvoiceItem vs LineItem disambiguation, no-search D-05 callout, draft constraint notes, and comprehensive unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe API -> from_map/1 | Untrusted JSON parsed into structs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | Invoice.from_map atomization | mitigate | Whitelist atomization with String.t() catch-all prevents atom table exhaustion from unknown values |
| T-14-05 | Information Disclosure | Invoice struct | accept | Invoice contains billing data but no secrets (API keys, tokens). Standard struct, no custom Inspect hiding needed beyond extra field |
| T-14-06 | Denial of Service | Invoice @known_fields | accept | Large field count (60+) is inherent to Stripe's Invoice object; Credo warning suppressed with inline disable comment |
</threat_model>

<verification>
```bash
cd /Users/jon/projects/lattice_stripe && mix test test/lattice_stripe/invoice_test.exs test/lattice_stripe/invoice_item_test.exs --trace
```
Both resource modules compile, parse Stripe responses correctly, and CRUD operations route to correct endpoints.
</verification>

<success_criteria>
- Invoice module: 60+ known fields, 4 atomized enums, 4 typed nested structs in from_map, full CRUD, lifecycle diagram
- InvoiceItem module: 20+ known fields, Period nested parsing, full CRUD at /v1/invoiceitems, D-05 no-search callout
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-invoices-invoice-line-items/14-02-SUMMARY.md`
</output>
