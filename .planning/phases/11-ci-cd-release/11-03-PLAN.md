---
phase: 11-ci-cd-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - SECURITY.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: false

requirements:
  - CICD-01
  - CICD-02
  - CICD-03
  - CICD-04
  - CICD-05

user_setup:
  - service: hex.pm
    why: "Hex API key for automated package publishing"
    env_vars:
      - name: HEX_API_KEY
        source: "hex.pm -> Account Settings -> Keys -> Generate key scoped to lattice_stripe package"
    dashboard_config:
      - task: "Add HEX_API_KEY as GitHub repo secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"
  - service: github
    why: "Repository settings for branch protection, merge strategy, auto-delete"
    dashboard_config:
      - task: "Enable squash merge, disable merge commit and rebase merge"
        location: "GitHub repo -> Settings -> General -> Pull Requests"
      - task: "Enable auto-delete head branches"
        location: "GitHub repo -> Settings -> General -> Pull Requests -> Automatically delete head branches"
      - task: "Add branch protection rule for main: require status checks (lint, test, integration), prevent force push, allow maintainer bypass"
        location: "GitHub repo -> Settings -> Branches -> Add rule for 'main'"
      - task: "Add repository topics: elixir, stripe, payments, sdk"
        location: "GitHub repo -> About (gear icon) -> Topics"

must_haves:
  truths:
    - "Contributors can find dev setup, testing, and PR process in CONTRIBUTING.md"
    - "Security vulnerabilities have a clear private reporting channel via SECURITY.md"
    - "Bug reports and feature requests have structured GitHub issue templates"
    - "PRs have a checklist template to ensure quality"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Developer contribution guide"
      contains: "stripe-mock"
    - path: "SECURITY.md"
      provides: "Security vulnerability reporting process"
      contains: "security"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug report template"
      contains: "bug"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Structured feature request template"
      contains: "feature"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR checklist template"
      contains: "checklist"
  key_links:
    - from: "CONTRIBUTING.md"
      to: ".github/workflows/ci.yml"
      via: "documents what CI checks run"
      pattern: "mix ci"
---

<objective>
Create repository community files (CONTRIBUTING, SECURITY, issue/PR templates) and verify repo settings.

Purpose: Provides contributors with clear guidelines, security reporting channels, and structured templates. Completes the repo infrastructure for a professional open-source project ready for Hex publishing.

Output: CONTRIBUTING.md, SECURITY.md, issue templates, PR template. Checkpoint for manual repo settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ci-cd-release/11-CONTEXT.md

@mix.exs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md, SECURITY.md, issue templates, and PR template</name>
  <files>CONTRIBUTING.md, SECURITY.md, .github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>mix.exs, README.md</read_first>
  <action>
1. Create `CONTRIBUTING.md` at repo root (per D-39). Content must include these sections:

```markdown
# Contributing to LatticeStripe

Thank you for your interest in contributing to LatticeStripe!

## Development Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/lattice-stripe/lattice_stripe.git
   cd lattice_stripe
   ```

2. Install dependencies:
   ```bash
   mix deps.get
   ```

3. Run the test suite:
   ```bash
   mix test
   ```

4. Run the full CI checks locally:
   ```bash
   mix ci
   ```
   This runs: format check, compile warnings, Credo strict, tests, and docs build.

## Running Integration Tests

Integration tests require [stripe-mock](https://github.com/stripe/stripe-mock), Stripe's official mock HTTP server.

Start stripe-mock via Docker:
```bash
docker run -p 12111:12111 -p 12112:12112 stripe/stripe-mock:latest
```

Then run integration tests:
```bash
mix test --include integration
```

## Commit Convention

This project uses [Conventional Commits](https://www.conventionalcommits.org/). All commit messages must follow this format:

```
type(scope): description

[optional body]
```

Types: `feat`, `fix`, `docs`, `chore`, `refactor`, `test`, `perf`

Examples:
- `feat(customer): add search endpoint`
- `fix(retry): respect Stripe-Should-Retry header`
- `docs(webhook): add Phoenix mounting guide`

Release Please uses these commit messages to automate version bumps and changelog generation.

## Pull Request Process

1. Create a branch from `main` using the naming convention:
   - `feat/description` for features
   - `fix/description` for bug fixes
   - `chore/description` for maintenance
   - `docs/description` for documentation

2. Make your changes with Conventional Commit messages.

3. Ensure all CI checks pass:
   - `mix format --check-formatted`
   - `mix compile --warnings-as-errors`
   - `mix credo --strict`
   - `mix test`
   - `mix docs`

4. Open a PR against `main`. Fill out the PR template.

5. PRs are squash-merged to keep a clean linear history on `main`.

**Note:** Docs-only PRs (changes only to `.md`, `.planning/`, or `guides/` files) may require maintainer bypass of CI status checks since CI is skipped for documentation-only changes.

## Code Style

- Follow existing patterns in the codebase
- Run `mix format` before committing
- Run `mix credo --strict` for style checks
- Typespecs are for documentation only (no Dialyzer)
```

2. Create `SECURITY.md` at repo root (per D-42):

```markdown
# Security Policy

## Supported Versions

| Version | Supported          |
| ------- | ------------------ |
| 0.x.x   | :white_check_mark: |

## Reporting a Vulnerability

If you discover a security vulnerability in LatticeStripe, please report it responsibly.

**Do not open a public GitHub issue for security vulnerabilities.**

Instead, please email: **security@latticestripe.dev**

### What to expect

- **Acknowledgment:** Within 48 hours of your report
- **Assessment:** Within 7 days, we will assess the vulnerability and provide an initial response
- **Fix timeline:** Critical vulnerabilities will be patched within 30 days
- **Disclosure:** We will coordinate disclosure timing with you

### What to include

- Description of the vulnerability
- Steps to reproduce
- Potential impact
- Suggested fix (if any)

Thank you for helping keep LatticeStripe and its users safe.
```

3. Create `.github/ISSUE_TEMPLATE/bug_report.yml` (per D-40):

```yaml
name: Bug Report
description: Report a bug in LatticeStripe
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thank you for reporting a bug! Please fill out the information below.
  - type: input
    id: version
    attributes:
      label: LatticeStripe Version
      description: "Which version of LatticeStripe are you using?"
      placeholder: "0.1.0"
    validations:
      required: true
  - type: input
    id: elixir-version
    attributes:
      label: Elixir / OTP Version
      description: "Output of `elixir --version`"
      placeholder: "Elixir 1.19.5 (compiled with Erlang/OTP 28)"
    validations:
      required: true
  - type: textarea
    id: description
    attributes:
      label: Description
      description: "A clear description of the bug."
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to Reproduce
      description: "Minimal steps to reproduce the behavior."
      value: |
        1.
        2.
        3.
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected Behavior
      description: "What you expected to happen."
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: Actual Behavior
      description: "What actually happened. Include error messages and stack traces."
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: Additional Context
      description: "Any other context, logs, or screenshots."
    validations:
      required: false
```

4. Create `.github/ISSUE_TEMPLATE/feature_request.yml` (per D-40):

```yaml
name: Feature Request
description: Suggest a new feature for LatticeStripe
title: "[Feature]: "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Thank you for suggesting a feature! Please describe what you'd like.
  - type: textarea
    id: problem
    attributes:
      label: Problem
      description: "What problem does this feature solve? Is it related to a frustration?"
    validations:
      required: true
  - type: textarea
    id: solution
    attributes:
      label: Proposed Solution
      description: "Describe the solution you'd like."
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives Considered
      description: "Any alternative solutions or features you've considered."
    validations:
      required: false
  - type: input
    id: stripe-api
    attributes:
      label: Related Stripe API
      description: "If this relates to a specific Stripe API resource or endpoint."
      placeholder: "e.g., Subscriptions, Invoices, /v1/billing_portal/sessions"
    validations:
      required: false
```

5. Create `.github/PULL_REQUEST_TEMPLATE.md` (per D-40):

```markdown
## Summary

<!-- Brief description of changes -->

## Type of Change

- [ ] Bug fix (non-breaking change that fixes an issue)
- [ ] New feature (non-breaking change that adds functionality)
- [ ] Breaking change (fix or feature that would cause existing functionality to not work as expected)
- [ ] Documentation update
- [ ] Maintenance / chore

## Checklist

- [ ] My code follows the existing code style
- [ ] I have run `mix format`
- [ ] I have run `mix credo --strict` with no issues
- [ ] I have added tests that prove my fix/feature works
- [ ] All tests pass (`mix test`)
- [ ] I have updated documentation if needed
- [ ] My commits follow [Conventional Commits](https://www.conventionalcommits.org/) format

## Test Plan

<!-- How did you test this? -->
```
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && test -f CONTRIBUTING.md && test -f SECURITY.md && test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && test -f .github/PULL_REQUEST_TEMPLATE.md && grep -q 'stripe-mock' CONTRIBUTING.md && grep -q 'Conventional Commits' CONTRIBUTING.md && grep -q 'mix ci' CONTRIBUTING.md && grep -q 'security' SECURITY.md && grep -q '48 hours' SECURITY.md && grep -q 'Bug Report' .github/ISSUE_TEMPLATE/bug_report.yml && grep -q 'Feature Request' .github/ISSUE_TEMPLATE/feature_request.yml && grep -q 'Checklist' .github/PULL_REQUEST_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
- CONTRIBUTING.md exists at repo root
- CONTRIBUTING.md contains "stripe-mock" in integration test section
- CONTRIBUTING.md contains "Conventional Commits" in commit convention section
- CONTRIBUTING.md contains "mix ci" in dev setup section
- CONTRIBUTING.md contains branch naming convention (feat/, fix/, chore/, docs/)
- CONTRIBUTING.md documents docs-only PR bypass note
- SECURITY.md exists at repo root
- SECURITY.md contains email reporting instructions (not public issue)
- SECURITY.md contains "48 hours" acknowledgment timeline
- .github/ISSUE_TEMPLATE/bug_report.yml exists with "Bug Report" name
- .github/ISSUE_TEMPLATE/bug_report.yml has LatticeStripe version and Elixir version fields
- .github/ISSUE_TEMPLATE/feature_request.yml exists with "Feature Request" name
- .github/ISSUE_TEMPLATE/feature_request.yml has Stripe API field
- .github/PULL_REQUEST_TEMPLATE.md exists with checklist
- .github/PULL_REQUEST_TEMPLATE.md includes `mix format` and `mix credo --strict` checklist items
- .github/PULL_REQUEST_TEMPLATE.md includes Conventional Commits reference
  </acceptance_criteria>
  <done>All 5 community files created: CONTRIBUTING.md with full dev/test/PR workflow, SECURITY.md with private reporting, bug/feature issue templates, PR checklist template</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI/CD infrastructure and configure repo settings</name>
  <files>CONTRIBUTING.md, SECURITY.md</files>
  <action>
This is a human verification checkpoint. After all 3 plans are executed and pushed to GitHub:

1. Push the branch to GitHub and verify CI workflow triggers — all 3 jobs (lint, test matrix, integration) should appear and run in parallel.
2. Verify Release Please config files exist at repo root.
3. Verify Dependabot is enabled in repo Settings -> Code security.
4. Configure GitHub repo settings manually (per D-35, D-36, D-37, D-43):
   - Settings -> General -> Pull Requests: enable "Allow squash merging" only, enable "Automatically delete head branches"
   - Settings -> Branches: add rule for `main` — require status checks (lint, test, integration), prevent force push, allow bypass
   - Repo About (gear icon): add topics `elixir`, `stripe`, `payments`, `sdk`
5. Add HEX_API_KEY secret: generate on hex.pm scoped to `lattice_stripe` package, add as GitHub repo secret.
6. Verify community files render correctly on GitHub (CONTRIBUTING, SECURITY, issue templates, PR template).
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — no automated verification"</automated>
  </verify>
  <acceptance_criteria>
- CI workflow runs green on GitHub with all 3 jobs (lint, test, integration)
- GitHub repo settings configured: squash merge, auto-delete branches, branch protection on main
- HEX_API_KEY secret added to repo
- Community files render correctly on GitHub
  </acceptance_criteria>
  <done>CI runs green, repo settings configured, HEX_API_KEY secret added, community files visible on GitHub</done>
</task>

</tasks>

<verification>
- All community files exist and contain required content
- Issue templates use YAML form format (not markdown)
- PR template has conventional commits checklist
- CONTRIBUTING.md documents the docs-only PR bypass caveat
- Repo settings verified via human checkpoint
</verification>

<success_criteria>
- Contributors have clear guide for dev setup, testing, commits, and PRs
- Security vulnerabilities have a private reporting channel
- GitHub issue templates provide structured bug/feature forms
- PR template enforces quality checklist
- Repo settings configured for squash merge, branch protection, auto-delete, topics
- HEX_API_KEY secret added for automated Hex publishing
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-cd-release/11-03-SUMMARY.md`
</output>
