---
phase: 11-ci-cd-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - LICENSE
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CICD-01
  - CICD-05

must_haves:
  truths:
    - "GitHub Actions CI runs lint, test matrix, and integration jobs on PR and push to main"
    - "CI tests across Elixir 1.15/OTP 26, 1.17/OTP 27, 1.19/OTP 28 with fail-fast"
    - "Integration tests run against stripe-mock service container in CI"
    - "mix hex.build succeeds with complete package metadata and LICENSE file"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with lint, test matrix, integration jobs"
      contains: "erlef/setup-beam"
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "mix.exs"
      provides: "mix_audit dep + complete package metadata"
      contains: "mix_audit"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "mix deps.get fetches all deps including mix_audit"
      pattern: "mix deps\\.audit"
    - from: ".github/workflows/ci.yml"
      to: "stripe/stripe-mock"
      via: "service container on ports 12111-12112"
      pattern: "stripe-mock"
---

<objective>
Create the GitHub Actions CI workflow and prepare mix.exs for Hex publishing.

Purpose: Establishes automated quality gates (format, compile, credo, test, docs, hex.build, deps.audit) that run on every PR and push to main, across 3 Elixir/OTP combos, with stripe-mock integration tests.

Output: `.github/workflows/ci.yml`, updated `mix.exs` (mix_audit dep + package metadata), `LICENSE` file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ci-cd-release/11-CONTEXT.md
@.planning/phases/11-ci-cd-release/11-RESEARCH.md

@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mix_audit dependency, complete package metadata, create LICENSE</name>
  <files>mix.exs, LICENSE</files>
  <read_first>mix.exs</read_first>
  <action>
1. Create `LICENSE` file at repo root with full MIT License text. Copyright line: `Copyright (c) 2026 LatticeStripe Contributors`. Year 2026, standard MIT body.

2. Update `mix.exs` — add mix_audit to deps (per RESEARCH.md Open Question 2):

In `defp deps do`, add after the credo line:
```elixir
{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}
```

3. Update `defp package do` to match D-22 exactly:
```elixir
defp package do
  [
    name: "lattice_stripe",
    description: "A production-grade, idiomatic Elixir SDK for the Stripe API",
    licenses: ["MIT"],
    links: %{
      "GitHub" => @source_url,
      "Changelog" => "#{@source_url}/blob/main/CHANGELOG.md",
      "HexDocs" => "https://hexdocs.pm/lattice_stripe"
    },
    files: ["lib", "mix.exs", "README.md", "CHANGELOG.md", "LICENSE"]
  ]
end
```

4. Run `mix deps.get` to fetch mix_audit.
5. Run `mix hex.build` to verify package metadata is valid and LICENSE is found.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && grep -q 'mix_audit' mix.exs && grep -q 'name: "lattice_stripe"' mix.exs && grep -q '"HexDocs"' mix.exs && grep -q 'files:' mix.exs && test -f LICENSE && grep -q 'MIT License' LICENSE && mix deps.get && mix hex.build</automated>
  </verify>
  <acceptance_criteria>
- mix.exs contains `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}`
- mix.exs `package()` contains `name: "lattice_stripe"`
- mix.exs `package()` contains `"HexDocs" => "https://hexdocs.pm/lattice_stripe"`
- mix.exs `package()` contains `files: ["lib", "mix.exs", "README.md", "CHANGELOG.md", "LICENSE"]`
- LICENSE file exists at repo root
- LICENSE contains "MIT License"
- LICENSE contains "Copyright (c) 2026 LatticeStripe Contributors"
- `mix deps.get` succeeds
- `mix hex.build` exits 0
  </acceptance_criteria>
  <done>mix_audit added as dev dep, package metadata complete per D-22, LICENSE file created, mix hex.build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>mix.exs, .planning/phases/11-ci-cd-release/11-RESEARCH.md</read_first>
  <action>
Create `.github/workflows/ci.yml` with the exact structure from RESEARCH.md Pattern 1, implementing all locked decisions D-01 through D-12.

The file must contain these three parallel jobs (no `needs:` between them per D-01):

**Workflow-level config:**
- `name: CI`
- Triggers: `push: branches: [main]` and `pull_request:` (any branch), both with `paths-ignore: ["**.md", ".planning/**", "guides/**"]` per D-02
- `concurrency: { group: "${{ github.workflow }}-${{ github.ref }}", cancel-in-progress: true }` per D-07
- `env: { MIX_ENV: test }` per D-11
- `permissions: { contents: read }` per D-08

**Job 1: lint** (per D-04, D-09, D-10)
- `name: Lint`
- `runs-on: ubuntu-latest`
- `timeout-minutes: 15`
- Steps: checkout@v4, setup-beam@v1 (elixir 1.19, otp 28), cache deps + _build (keys include `1.19-28` and `mix.lock` hash per D-06), `mix deps.get`, then individual steps: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix docs`, `mix hex.build`, `mix deps.audit`
- Cache keys pattern: `${{ runner.os }}-1.19-28-mix-${{ hashFiles('**/mix.lock') }}` for deps, `${{ runner.os }}-1.19-28-build-${{ hashFiles('**/mix.lock') }}` for _build

**Job 2: test** (per D-03, D-10)
- `name: "Test (Elixir ${{ matrix.elixir }} / OTP ${{ matrix.otp }})"`
- `runs-on: ubuntu-latest`
- `timeout-minutes: 15`
- Strategy: `fail-fast: true`, matrix.include with 3 combos: {elixir: '1.15', otp: '26'}, {elixir: '1.17', otp: '27'}, {elixir: '1.19', otp: '28'}
- Steps: checkout@v4, setup-beam@v1 (matrix versions), cache deps + _build (keys include `${{ matrix.elixir }}-${{ matrix.otp }}` per Pitfall 2), `mix deps.get`, `mix test`

**Job 3: integration** (per D-05, D-10, D-11)
- `name: Integration Tests`
- `runs-on: ubuntu-latest`
- `timeout-minutes: 15`
- `services:` block with `stripe-mock:` image `stripe/stripe-mock:latest`, ports `['12111:12111', '12112:12112']`
- Job-level `env:` adds `STRIPE_MOCK_HOST: localhost`, `STRIPE_MOCK_PORT: '12111'`
- Steps: checkout@v4, setup-beam@v1 (elixir 1.19, otp 28), cache deps + _build (same keys as lint job), `mix deps.get`, `mix test --include integration`

YAML style notes:
- Quote all version strings ('1.15', '1.19', '26', '28') to prevent YAML float parsing
- Use actions/cache@v3 for all cache steps
- Each cache step has `path`, `key`, and `restore-keys`
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && test -f .github/workflows/ci.yml && grep -q 'erlef/setup-beam@v1' .github/workflows/ci.yml && grep -q 'stripe/stripe-mock:latest' .github/workflows/ci.yml && grep -q 'mix deps.audit' .github/workflows/ci.yml && grep -q "fail-fast: true" .github/workflows/ci.yml && grep -q 'paths-ignore' .github/workflows/ci.yml && grep -q 'cancel-in-progress: true' .github/workflows/ci.yml && grep -q "permissions:" .github/workflows/ci.yml && grep -c "timeout-minutes: 15" .github/workflows/ci.yml | grep -q 3</automated>
  </verify>
  <acceptance_criteria>
- .github/workflows/ci.yml exists
- Contains `name: CI`
- Contains `erlef/setup-beam@v1`
- Contains `actions/checkout@v4`
- Contains `actions/cache@v3`
- Contains `stripe/stripe-mock:latest` in services block
- Contains `mix deps.audit` in lint job
- Contains `mix hex.build` in lint job
- Contains `fail-fast: true` in test matrix strategy
- Contains 3 matrix entries: 1.15/26, 1.17/27, 1.19/28
- Contains `paths-ignore` with `**.md`, `.planning/**`, `guides/**`
- Contains `cancel-in-progress: true`
- Contains `permissions:` with `contents: read`
- Contains `MIX_ENV: test` at workflow env level
- Contains `STRIPE_MOCK_HOST: localhost` in integration job
- All 3 jobs have `timeout-minutes: 15`
- No `needs:` between the 3 jobs (parallel execution per D-01)
  </acceptance_criteria>
  <done>CI workflow created with lint (6 checks), test matrix (3 combos), and integration (stripe-mock) jobs, all parallel, matching every locked decision D-01 through D-12</done>
</task>

</tasks>

<verification>
- `mix hex.build` exits 0 (proves package metadata + LICENSE valid)
- `.github/workflows/ci.yml` has correct YAML syntax (no parse errors)
- All 3 CI jobs defined with no inter-job dependencies
- Cache keys include Elixir/OTP versions to avoid Pitfall 2
- stripe-mock service container configured on correct ports
</verification>

<success_criteria>
- CI workflow file ready for GitHub Actions to pick up on next push
- mix.exs has complete Hex package metadata per D-22
- mix_audit added as dev dependency
- LICENSE file exists with MIT text
- mix hex.build validates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-cd-release/11-01-SUMMARY.md`
</output>
