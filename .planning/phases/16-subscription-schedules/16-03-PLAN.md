---
phase: 16
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
requirements: [BILL-03]
autonomous: true
files_modified:
  - test/integration/subscription_schedule_integration_test.exs
  - test/lattice_stripe/form_encoder_test.exs
  - guides/subscriptions.md
  - mix.exs

must_haves:
  truths:
    - "SubscriptionSchedule full lifecycle (create → retrieve → update → cancel) round-trips against stripe-mock"
    - "SubscriptionSchedule release path (create → release) round-trips against stripe-mock"
    - "SubscriptionSchedule list + stream! return shape-valid pages against stripe-mock"
    - "Form encoder correctly encodes phases[0][items][0][price_data][recurring][interval] (Phase 16 deepest-path regression guard)"
    - "guides/subscriptions.md contains a ## Subscription Schedules section documenting Creation modes, cancel vs release, and proration guard"
    - "mix.exs ExDoc Billing module group includes SubscriptionSchedule + 4 nested typed struct modules"
    - "mix docs exits 0 with no warnings"
  artifacts:
    - path: "test/integration/subscription_schedule_integration_test.exs"
      provides: "stripe-mock integration coverage for all 6 SubscriptionSchedule endpoints"
      contains: "@moduletag :integration"
    - path: "test/lattice_stripe/form_encoder_test.exs"
      provides: "Phase 16 deepest-path regression guard for phases[0][items][0][price_data][recurring][interval]"
      contains: "phases[0][items][0][price_data]"
    - path: "guides/subscriptions.md"
      provides: "Developer guide extended with Subscription Schedules section"
      contains: "## Subscription Schedules"
    - path: "mix.exs"
      provides: "ExDoc Billing module group extension for SubscriptionSchedule + nested types"
      contains: "LatticeStripe.SubscriptionSchedule"
  key_links:
    - from: "test/integration/subscription_schedule_integration_test.exs"
      to: "LatticeStripe.SubscriptionSchedule"
      via: "test_integration_client/0 + real HTTP to stripe-mock:12111"
      pattern: "SubscriptionSchedule.create"
    - from: "mix.exs"
      to: "guides/subscriptions.md"
      via: "docs: [extras: [...]] entry (already exists from Phase 15; just ensure module group wiring)"
      pattern: "LatticeStripe.SubscriptionSchedule"
---

<objective>
Close out Phase 16 with stripe-mock integration tests covering all 6 SubscriptionSchedule endpoints, a form-encoder regression test for the deepest known Phase 16 param path, an extension of the existing `guides/subscriptions.md` with a `## Subscription Schedules` section, and `mix.exs` ExDoc module group wiring so the new modules land under the Billing group on HexDocs.

Purpose: Verify end-to-end that LatticeStripe's wire encoding + HTTP dispatch match Stripe's OpenAPI spec (via stripe-mock), guard against historical form-encoder nested-array regressions (stripity_stripe had bugs here), and give developers HexDocs-ready documentation that connects SubscriptionSchedule to the existing Subscription + proration story.

Output:
- `test/integration/subscription_schedule_integration_test.exs` — full lifecycle + list + stream + release path
- Extended `test/lattice_stripe/form_encoder_test.exs` with one Phase 16 regression test
- Extended `guides/subscriptions.md` with `## Subscription Schedules` section (no new guide file)
- Extended `mix.exs` ExDoc `groups_for_modules` Billing group
- No new telemetry events (piggyback on general `[:lattice_stripe, :request, *]`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-subscription-schedules/16-CONTEXT.md
@.planning/phases/16-subscription-schedules/16-RESEARCH.md
@.planning/phases/16-subscription-schedules/16-VALIDATION.md
@.planning/phases/16-subscription-schedules/16-01-PLAN.md
@.planning/phases/16-subscription-schedules/16-02-PLAN.md
@test/integration/subscription_integration_test.exs
@test/integration/invoice_integration_test.exs
@test/support/test_helpers.ex
@test/support/fixtures/subscription_schedule.ex
@test/lattice_stripe/form_encoder_test.exs
@lib/lattice_stripe/subscription_schedule.ex
@lib/lattice_stripe/form_encoder.ex
@guides/subscriptions.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add form_encoder regression test for phases[0][items][0][price_data][recurring][interval]</name>
  <files>
    test/lattice_stripe/form_encoder_test.exs
  </files>
  <read_first>
    - lib/lattice_stripe/form_encoder.ex (confirm the existing `flatten/2` recursive encoder — NO changes needed)
    - test/lattice_stripe/form_encoder_test.exs (existing test patterns — find the "deeply nested (4+ levels)" test as the closest precedent)
    - .planning/phases/16-subscription-schedules/16-RESEARCH.md (§Form Encoder Nested Arrays — exact test code provided)
  </read_first>
  <behavior>
    - Form encoder correctly produces `phases[0][items][0][price_data][currency]=usd` from a nested input
    - Form encoder correctly produces `phases[0][items][0][price_data][recurring][interval]=month` (the deepest known Phase 16 path)
    - Form encoder correctly produces `phases[0][proration_behavior]=create_prorations`
    - Test is a REGRESSION GUARD — it must reference Phase 16 in the test name or comment so a future refactor does not remove it
  </behavior>
  <action>
    APPEND to `test/lattice_stripe/form_encoder_test.exs` (do not replace existing tests):

    ```elixir
    test "phases[].items[].price_data nested encoding (Phase 16 regression guard)" do
      params = %{
        "phases" => [
          %{
            "items" => [
              %{
                "price_data" => %{
                  "currency" => "usd",
                  "recurring" => %{"interval" => "month"}
                }
              }
            ],
            "proration_behavior" => "create_prorations"
          }
        ]
      }

      result = LatticeStripe.FormEncoder.encode(params)

      assert result =~ "phases[0][items][0][price_data][currency]=usd"
      assert result =~ "phases[0][items][0][price_data][recurring][interval]=month"
      assert result =~ "phases[0][proration_behavior]=create_prorations"
    end
    ```

    Place this test inside the existing describe block that covers nested encoding (probably `describe "encode/1 nested structures"` or similar — confirm by reading the existing file first). If no suitable describe block exists, add the test at the end of the module.

    Do NOT modify `lib/lattice_stripe/form_encoder.ex`. Per 16-RESEARCH.md, the encoder already handles arbitrary-depth nesting via recursive `flatten/2` — this test exists to PROVE it handles Phase 16's deepest known path and to catch any future regression.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/form_encoder_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - `test/lattice_stripe/form_encoder_test.exs` contains the literal string `phases[0][items][0][price_data][currency]=usd`
    - `test/lattice_stripe/form_encoder_test.exs` contains the literal string `phases[0][items][0][price_data][recurring][interval]=month`
    - `test/lattice_stripe/form_encoder_test.exs` contains the literal string `phases[0][proration_behavior]=create_prorations`
    - Test name or inline comment contains `Phase 16` (grep-verifiable)
    - `lib/lattice_stripe/form_encoder.ex` is UNCHANGED (`git diff lib/lattice_stripe/form_encoder.ex` is empty)
    - `mix test test/lattice_stripe/form_encoder_test.exs` exits 0
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write SubscriptionSchedule stripe-mock integration test (all 6 endpoints)</name>
  <files>
    test/integration/subscription_schedule_integration_test.exs
  </files>
  <read_first>
    - test/integration/subscription_integration_test.exs (PRIMARY TEMPLATE — module tag, test_integration_client/0, setup_all stripe-mock guard)
    - test/integration/invoice_integration_test.exs (secondary template)
    - lib/lattice_stripe/subscription_schedule.ex (signatures available after Plans 16-01 + 16-02)
    - test/support/fixtures/subscription_schedule.ex (Plan 16-01 output)
    - test/support/test_helpers.ex (test_integration_client/0 — points at stripe-mock :12111)
    - lib/lattice_stripe/price.ex (restored via Phase 15 D1 — for creating real Price fixtures in integration setup)
    - lib/lattice_stripe/customer.ex (for creating Customer fixture)
    - .planning/phases/16-subscription-schedules/16-RESEARCH.md (§stripe-mock Coverage — assert shape NOT semantics; §Endpoint Table)
    - .planning/phases/16-subscription-schedules/16-CONTEXT.md (§Specifics — deepest param path validation via stripe-mock 200 response)
  </read_first>
  <behavior>
    All tests `@moduletag :integration` — only run under `mix test --include integration`.

    **setup_all:** Guard that stripe-mock is reachable on `localhost:12111` via `:gen_tcp.connect/3`; raise with docker command if down (copy from `test/integration/subscription_integration_test.exs`).

    **setup (per test):** Build `client = LatticeStripe.Test.Helpers.test_integration_client()`.

    **Tests (minimum set):**

    1. `test "create (customer + phases mode) returns shape-valid %SubscriptionSchedule{}"`:
       - Create Product + Price fixtures via `Product.create!/2` and `Price.create!/2`
       - Create Customer via `Customer.create!/2`
       - Call `SubscriptionSchedule.create(client, %{"customer" => cus.id, "start_date" => "now", "end_behavior" => "release", "phases" => [%{"items" => [%{"price" => price.id, "quantity" => 1}], "iterations" => 12}]})`
       - Assert `{:ok, %SubscriptionSchedule{id: id}} = result` AND `is_binary(id)` AND `String.starts_with?(id, "sub_sched_")` (or just `is_binary(id)` — stripe-mock fixture ids may vary)
       - Assert decoded `phases` is a list and first element is `%SubscriptionSchedule.Phase{}`
       - Do NOT assert on status transitions (stripe-mock is stateless)

    2. `test "create (from_subscription mode) returns shape-valid %SubscriptionSchedule{}"`:
       - Create a live Subscription first (using Phase 15's `Subscription.create!/2`)
       - Call `SubscriptionSchedule.create(client, %{"from_subscription" => sub.id})`
       - Assert `{:ok, %SubscriptionSchedule{}} = result`

    3. `test "retrieve returns shape-valid %SubscriptionSchedule{}"`:
       - Create one, then retrieve by id
       - Assert `{:ok, %SubscriptionSchedule{}}`

    4. `test "update with phases[].proration_behavior succeeds"`:
       - Create a schedule
       - Call `SubscriptionSchedule.update(client, sched.id, %{"phases" => [%{"items" => [%{"price" => price.id}], "proration_behavior" => "create_prorations"}]})`
       - Assert `{:ok, %SubscriptionSchedule{}}` — stripe-mock's 200 response proves the form encoder round-trip worked for nested phases (T-16-05 mitigation)

    5. `test "cancel uses POST and returns %SubscriptionSchedule{}"`:
       - Create a schedule
       - Call `SubscriptionSchedule.cancel(client, sched.id, %{"invoice_now" => false, "prorate" => false})`
       - Assert `{:ok, %SubscriptionSchedule{}}`
       - Do NOT assert `status == "canceled"` — stripe-mock fixture may not reflect semantic transition

    6. `test "release uses POST and returns %SubscriptionSchedule{}"`:
       - Create a fresh schedule (do not reuse the one from cancel test — stateless mock means we don't care, but isolation is cleaner)
       - Call `SubscriptionSchedule.release(client, sched.id, %{"preserve_cancel_date" => false})`
       - Assert `{:ok, %SubscriptionSchedule{}}`

    7. `test "list returns %List{data: [%SubscriptionSchedule{}, ...]}"`:
       - Call `SubscriptionSchedule.list(client, %{"limit" => 3})`
       - Assert `{:ok, %LatticeStripe.List{data: data}}` AND `is_list(data)`
       - If non-empty: assert `match?(%SubscriptionSchedule{}, hd(data))`

    8. `test "stream!/3 yields %SubscriptionSchedule{} items"`:
       - Call `SubscriptionSchedule.stream!(client, %{"limit" => 2}) |> Enum.take(2)`
       - Assert result is a list; if non-empty, assert every element is `%SubscriptionSchedule{}`

    9. `test "strict client rejects update with phases[] missing proration_behavior (pre-network)"`:
       - Strict client (`require_explicit_proration: true`) built via `test_integration_client(require_explicit_proration: true)` if helper supports it, otherwise build a normal strict client (no network call expected)
       - Call `SubscriptionSchedule.update(client, "sub_sched_fake", %{"phases" => [%{"items" => [%{"price" => "price_1"}]}]}, [])`
       - Assert `{:error, %LatticeStripe.Error{type: :proration_required}}` — proves the guard fires pre-network even in an integration context

    10. `test "idempotency_key is forwarded on create"`:
        - Pass `idempotency_key: "test-ik-phase16-create"` via opts
        - Assert success
        - Second call with same key returns a successful response (stripe-mock honors Idempotency-Key header)

    **Regression guard assertion (T-16-04 — wire-verb correctness):**
    The cancel and release tests implicitly verify the HTTP verb is correct — if LatticeStripe sent DELETE to `/v1/subscription_schedules/:id/cancel`, stripe-mock would return 404 or 405, failing the assertion. Add an inline comment on each test: `# Wire-verb regression guard (T-16-04): stripe-mock returns 200 only if POST is used.`
  </behavior>
  <action>
    Create `test/integration/subscription_schedule_integration_test.exs`:

    ```elixir
    defmodule LatticeStripe.Integration.SubscriptionScheduleTest do
      @moduledoc """
      Integration tests for `LatticeStripe.SubscriptionSchedule` against
      `stripe-mock` running on `localhost:12111`. Run via:

          mix test --include integration test/integration/subscription_schedule_integration_test.exs

      stripe-mock is stateless against our requests — it validates against the
      OpenAPI spec and returns canned-but-randomized responses. These tests
      assert SHAPE (structs, is_binary(id), is_list) NOT SEMANTICS (status
      transitions). See 16-RESEARCH.md §stripe-mock Coverage for details.
      """
      use ExUnit.Case, async: false
      @moduletag :integration

      alias LatticeStripe.{Customer, Price, Product, Subscription, SubscriptionSchedule}
      alias LatticeStripe.Test.Helpers

      setup_all do
        # Guard: stripe-mock must be reachable
        case :gen_tcp.connect(~c"localhost", 12_111, [:binary, active: false], 500) do
          {:ok, sock} ->
            :gen_tcp.close(sock)
            :ok

          {:error, reason} ->
            raise """
            stripe-mock not reachable on localhost:12111 (#{inspect(reason)}).

            Start it with:
                docker run --rm -p 12111-12112:12111-12112 stripe/stripe-mock:latest
            """
        end

        :ok
      end

      setup do
        {:ok, client: Helpers.test_integration_client()}
      end

      # ... tests from <behavior> section ...
    end
    ```

    Implement every test listed in `<behavior>` using the helper pattern from `test/integration/subscription_integration_test.exs`. For tests requiring Price/Product/Customer fixtures, create them inline inside each test (stripe-mock is stateless so test isolation is free).

    For the strict-client guard test (test 9), build the strict client via:
    ```elixir
    client = %{Helpers.test_integration_client() | require_explicit_proration: true}
    ```
    if the helper does not expose an option — or use `LatticeStripe.Client.new!/1` directly with the appropriate options. Executor: read `test/support/test_helpers.ex` first to determine the correct pattern.
  </action>
  <verify>
    <automated>mix test --include integration test/integration/subscription_schedule_integration_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/subscription_schedule_integration_test.exs` contains `@moduletag :integration`
    - File contains `setup_all` with `:gen_tcp.connect` guard against stripe-mock on port 12_111
    - File contains `test "create (customer + phases mode)` or equivalent
    - File contains `test "create (from_subscription mode)` or equivalent
    - File contains `SubscriptionSchedule.cancel(` (cancel test)
    - File contains `SubscriptionSchedule.release(` (release test)
    - File contains `SubscriptionSchedule.stream!(` (stream test)
    - File contains `test "strict client rejects update with phases[] missing proration_behavior"` or equivalent with `{:error, %LatticeStripe.Error{type: :proration_required}}` assertion
    - File contains `test "idempotency_key is forwarded"` or equivalent
    - File contains the literal string `T-16-04` (wire-verb regression guard comment)
    - File contains `match?(%SubscriptionSchedule{}` or `%SubscriptionSchedule{id: id}` assertion (shape check)
    - With stripe-mock running on :12111, `mix test --include integration test/integration/subscription_schedule_integration_test.exs` exits 0
    - `mix credo --strict test/integration/subscription_schedule_integration_test.exs` exits 0
    - `mix format --check-formatted` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend guides/subscriptions.md with Subscription Schedules section + wire mix.exs ExDoc Billing group</name>
  <files>
    guides/subscriptions.md,
    mix.exs
  </files>
  <read_first>
    - guides/subscriptions.md (existing guide — find the Proration section and the SubscriptionItem operations section; new content goes between them per 16-RESEARCH.md §Open Questions item 3)
    - mix.exs (existing `docs:` config and `groups_for_modules` Billing group from Phase 15)
    - lib/lattice_stripe/subscription_schedule.ex (Plan 16-01 + 16-02 output — reference for @doc examples)
    - .planning/phases/16-subscription-schedules/16-CONTEXT.md (§D2 — release contrast prose; §D3 — Creation modes examples)
    - .planning/phases/16-subscription-schedules/16-RESEARCH.md (§Open Questions item 3 — section placement; §Subscription Inspect — PII story)
  </read_first>
  <behavior>
    **guides/subscriptions.md extension:**

    New section titled `## Subscription Schedules`, placed immediately after the existing `## Proration` section and before `## SubscriptionItem operations` (per 16-RESEARCH.md §Open Questions item 3).

    Subsections (minimum):

    1. `### When to use a Subscription Schedule` — one paragraph explaining phased billing (free trial → discounted → full price, etc.). Reference Stripe's docs link.
    2. `### Creation modes` — two code examples: Mode 1 (`from_subscription`), Mode 2 (`customer` + `phases`). Mention the Stripe 400 that fires on mixing modes and how it surfaces as `%LatticeStripe.Error{type: :invalid_request_error}`.
    3. `### cancel vs release` — prose explanation with the irreversibility warning, plus a code example for each.
    4. `### Proration on update` — note that `update/4` runs the proration guard when `require_explicit_proration: true`. Example with `phases[].proration_behavior`. Explicit note: "Stripe does NOT accept `proration_behavior` at `phases[].items[]` — only at top-level and per-phase. The guard reflects this."
    5. `### Webhook-driven state transitions` — reuse the Phase 15 webhook-handoff callout: "Drive your application state from `customer.subscription.*` and `subscription_schedule.*` webhook events, not from SDK responses. Stripe may transition a schedule after your SDK call returns." This reinforces T-16-02 / Phase 15 D5.

    **mix.exs ExDoc extension:**

    Find the existing `groups_for_modules` Billing group (added in Phase 14/15 — includes `Invoice`, `InvoiceItem`, `Subscription`, `SubscriptionItem`, nested types). Extend the Billing group's module list with:
    - `LatticeStripe.SubscriptionSchedule`
    - `LatticeStripe.SubscriptionSchedule.Phase`
    - `LatticeStripe.SubscriptionSchedule.CurrentPhase`
    - `LatticeStripe.SubscriptionSchedule.PhaseItem`
    - `LatticeStripe.SubscriptionSchedule.AddInvoiceItem`

    Do NOT create a new group. Do NOT add a new entry to `extras:` — the existing `guides/subscriptions.md` entry already covers the new section.

    **Verification:** `mix docs` exits 0 with no warnings. Open `doc/index.html` → Billing group → confirm all 5 new modules appear.
  </behavior>
  <action>
    **1. Extend guides/subscriptions.md:**

    Read the existing `guides/subscriptions.md` first to find the exact markdown anchors. Locate the `## Proration` section and the `## SubscriptionItem operations` section. Insert a new `## Subscription Schedules` section between them.

    Section skeleton (fill in prose; executor's discretion on exact wording, but all sub-headings below are required):

    ```markdown
    ## Subscription Schedules

    A Subscription Schedule defines a phased billing timeline. Each phase
    specifies the prices, quantities, proration behavior, and trial settings
    for a slice of time. When a phase ends, the schedule automatically
    transitions to the next.

    Use schedules for flows like:

    - Free trial → discounted intro price → full price
    - Annual → monthly transition
    - Step-up pricing as usage grows
    - Contract-based fixed-term subscriptions

    See the [Stripe Subscription Schedules API](https://docs.stripe.com/api/subscription_schedules)
    for the full object reference.

    ### When to use a Subscription Schedule

    Reach for a schedule when you need _deterministic_ future billing changes
    at known dates. For ad-hoc changes driven by user actions (upgrades,
    cancellations), use `LatticeStripe.Subscription.update/4` directly.

    ### Creation modes

    Stripe accepts two mutually-exclusive parameter shapes on create.

    **Mode 1: from_subscription**

    Convert an existing Subscription into a schedule whose first phase
    captures the subscription's current state.

        LatticeStripe.SubscriptionSchedule.create(client, %{
          "from_subscription" => "sub_1234567890"
        })

    **Mode 2: customer + phases**

    Build a new schedule from scratch with an explicit phase timeline.

        LatticeStripe.SubscriptionSchedule.create(client, %{
          "customer" => "cus_1234567890",
          "start_date" => "now",
          "end_behavior" => "release",
          "phases" => [
            %{
              "items" => [%{"price" => "price_intro", "quantity" => 1}],
              "iterations" => 3,
              "proration_behavior" => "create_prorations"
            },
            %{
              "items" => [%{"price" => "price_full", "quantity" => 1}],
              "iterations" => 12
            }
          ]
        })

    Mixing `from_subscription` with `customer`/`phases` in a single call
    raises a Stripe 400 that surfaces as
    `{:error, %LatticeStripe.Error{type: :invalid_request_error}}`.
    LatticeStripe does not client-side-validate the mode — Stripe's own error
    message is already actionable.

    ### cancel vs release

    Two different ways to end phased billing.

    **`cancel/4`** terminates BOTH the schedule AND the underlying
    Subscription. Both entities move to `canceled` status.

        LatticeStripe.SubscriptionSchedule.cancel(client, sched.id, %{
          "invoice_now" => true,
          "prorate" => true
        })

    **`release/4`** detaches the schedule from its Subscription. The
    Subscription remains active and billable but is no longer phase-governed
    — subsequent configuration changes must go through
    `LatticeStripe.Subscription.update/4` directly. **This is irreversible.**

        LatticeStripe.SubscriptionSchedule.release(client, sched.id)

    Use `release/4` when you want to graduate a subscription off a phased
    plan into a flat ongoing subscription. Use `cancel/4` when you want to
    end billing entirely.

    Both dispatch `POST` to `/v1/subscription_schedules/:id/{cancel,release}`
    — not `DELETE` (which is what `LatticeStripe.Subscription.cancel/4` uses).
    This difference matters if you're reading wire logs.

    ### Proration on update

    When a client has `require_explicit_proration: true`, `update/4` requires
    `proration_behavior` at either the top level of `params` OR inside any
    element of `params["phases"][]`:

        LatticeStripe.SubscriptionSchedule.update(client, sched.id, %{
          "phases" => [
            %{
              "items" => [%{"price" => "price_full"}],
              "proration_behavior" => "create_prorations"
            }
          ]
        })

    Stripe does NOT accept `proration_behavior` at `phases[].items[]` — only
    at top-level and per-phase. The guard reflects this wire shape and does
    not walk deeper. If your Phase 15 Subscription mutations worked, your
    Phase 16 Schedule mutations use the same mental model — just one level
    deeper into `phases[]`.

    ### Webhook-driven state transitions

    As with Subscriptions (Phase 15), **drive your application state from
    webhook events, not from SDK responses**. An SDK response reflects the
    state at the moment of the call, but Stripe may transition the schedule
    moments later (phase boundaries, billing failures, automatic release,
    etc.).

    Wire `subscription_schedule.created`, `subscription_schedule.updated`,
    `subscription_schedule.canceled`, `subscription_schedule.released`, and
    `subscription_schedule.aborted` into your webhook handler via
    `LatticeStripe.Webhook`.
    ```

    **2. Extend mix.exs:**

    Read `mix.exs` first to find the existing `groups_for_modules: [Billing: [...]]` list (added in Phase 14/15). Extend that list — do not create a new group. Result shape (executor: match actual existing formatting):

    ```elixir
    groups_for_modules: [
      # ... other groups ...
      Billing: [
        LatticeStripe.Invoice,
        LatticeStripe.Invoice.AutomaticTax,
        LatticeStripe.Invoice.LineItem,
        LatticeStripe.Invoice.StatusTransitions,
        LatticeStripe.InvoiceItem,
        LatticeStripe.Subscription,
        LatticeStripe.Subscription.CancellationDetails,
        LatticeStripe.Subscription.PauseCollection,
        LatticeStripe.Subscription.TrialSettings,
        LatticeStripe.SubscriptionItem,
        LatticeStripe.SubscriptionSchedule,
        LatticeStripe.SubscriptionSchedule.Phase,
        LatticeStripe.SubscriptionSchedule.CurrentPhase,
        LatticeStripe.SubscriptionSchedule.PhaseItem,
        LatticeStripe.SubscriptionSchedule.AddInvoiceItem,
        LatticeStripe.Billing.Guards
      ],
      # ... other groups ...
    ]
    ```

    If the existing Billing list does not include `LatticeStripe.Billing.Guards`, leave that unchanged (do not introduce new members beyond Phase 16's scope). The 5 new members are the 5 Phase 16 modules listed above.

    **3. Verify `mix docs` exits 0:**

    After edits, run `mix docs` and confirm no warnings. If warnings appear about missing modules, the Billing group extension used an incorrect module name (typo or wrong namespace — fix and re-run).
  </action>
  <verify>
    <automated>mix docs 2>&1 | tee /tmp/mix-docs-16-03.log; grep -q "Subscription Schedules" guides/subscriptions.md && grep -q "LatticeStripe.SubscriptionSchedule" mix.exs && ! grep -qi "warning" /tmp/mix-docs-16-03.log</automated>
  </verify>
  <acceptance_criteria>
    - `guides/subscriptions.md` contains `## Subscription Schedules` (exact literal, grep-verifiable)
    - `guides/subscriptions.md` contains `### Creation modes`
    - `guides/subscriptions.md` contains `### cancel vs release`
    - `guides/subscriptions.md` contains `### Proration on update`
    - `guides/subscriptions.md` contains `### Webhook-driven state transitions`
    - `guides/subscriptions.md` contains both `from_subscription` and `customer` strings in code examples
    - `guides/subscriptions.md` contains `irreversible` in the cancel vs release section
    - `guides/subscriptions.md` contains `subscription_schedule.created` (webhook event name)
    - `mix.exs` contains `LatticeStripe.SubscriptionSchedule` inside the `groups_for_modules` block (grep-verifiable)
    - `mix.exs` contains `LatticeStripe.SubscriptionSchedule.Phase`
    - `mix.exs` contains `LatticeStripe.SubscriptionSchedule.CurrentPhase`
    - `mix.exs` contains `LatticeStripe.SubscriptionSchedule.PhaseItem`
    - `mix.exs` contains `LatticeStripe.SubscriptionSchedule.AddInvoiceItem`
    - `mix docs` exits 0
    - `mix docs` output contains no `warning:` lines
    - `doc/LatticeStripe.SubscriptionSchedule.html` exists after `mix docs`
    - `mix format --check-formatted` exits 0 (applies to mix.exs)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LatticeStripe form encoder → Stripe HTTPS | Deep-nested array params must encode to the exact bracket notation Stripe expects |
| LatticeStripe → stripe-mock (OpenAPI-spec validator) | Mock rejects malformed form bodies with 400 — server-side validation IS our regression guard |
| Developer reading HexDocs | Must understand cancel vs release semantics BEFORE calling either — docs are the guardrail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering (wrong HTTP verb) | `SubscriptionSchedule.cancel/4`, `release/4` wire shape | mitigate | Task 2 stripe-mock integration tests: stripe-mock returns 200 only if LatticeStripe POSTs to `/cancel` and `/release`. A DELETE would return 404 or 405 and fail the assertion. Inline comment marks each test as a `T-16-04` regression guard. |
| T-16-05 | Tampering (deep-nested param encoding drift) | `LatticeStripe.FormEncoder.encode/1` when handed `phases[].items[].price_data` | mitigate | Task 1 adds an explicit unit test asserting the exact wire-string output for the deepest known Phase 16 path. Task 2 adds an integration test where stripe-mock's OpenAPI validator serves as a second line of defense — if encoding drifts, stripe-mock returns 400 and the integration test fails. |
| T-16-02 | Tampering / Replay (idempotency) | Integration tests for mutations | mitigate | Task 2 test 10 verifies `opts[:idempotency_key]` forwarding end-to-end against stripe-mock. |
| T-16-03 | Elevation of Privilege (silent proration on update) | Integration path for strict client | mitigate | Task 2 test 9 verifies the guard fires pre-network even in an integration context — the strict client rejects `phases[]` without `proration_behavior` without any HTTP call. |
| T-16-Docs | Information Disclosure (misuse via missing docs) | `guides/subscriptions.md` | mitigate | Task 3 adds prominent `cancel vs release` section emphasizing irreversibility of release; webhook-handoff subsection echoes Phase 15's operational guardrail. |

Out of scope for this plan:
- T-16-01 (Inspect PII masking) — Plan 16-01 handles it
- T-16-02 in pure unit tests — Plan 16-02 handles the Mox-capture version
</threat_model>

<verification>
```bash
# Unit: form encoder regression
mix test test/lattice_stripe/form_encoder_test.exs

# Integration: stripe-mock must be running on :12111
mix test --include integration test/integration/subscription_schedule_integration_test.exs

# Full suite (no regressions)
mix test --exclude integration
mix test --include integration

# Docs build clean
mix docs

# Formatting + linting
mix format --check-formatted
mix credo --strict
mix compile --warnings-as-errors
```
</verification>

<success_criteria>
- `test/integration/subscription_schedule_integration_test.exs` exists with `@moduletag :integration` and covers all 6 endpoints (create both modes, retrieve, update, cancel, release, list, stream)
- stripe-mock integration tests assert shape (struct match, `is_binary(id)`) not semantics (no status transition assertions)
- Strict-client integration test proves the proration guard fires pre-network
- Wire-verb regression guards (T-16-04) explicitly commented in cancel/release integration tests
- `test/lattice_stripe/form_encoder_test.exs` contains the Phase 16 deepest-path regression test
- `lib/lattice_stripe/form_encoder.ex` is UNCHANGED (no encoder edits needed)
- `guides/subscriptions.md` has a `## Subscription Schedules` section covering Creation modes, cancel vs release, Proration on update, and Webhook-driven state transitions
- `mix.exs` ExDoc Billing module group includes all 5 Phase 16 modules
- `mix docs` builds without warnings
- `mix test --include integration` exits 0
- No Phase 14/15 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-subscription-schedules/16-03-SUMMARY.md` capturing:
- Integration test count and which endpoints they exercise
- Form encoder regression test line reference
- Exact line-count delta for `guides/subscriptions.md`
- Exact modules added to `mix.exs` Billing group
- Confirmation that `mix docs` is warning-free
- Confirmation that `mix test --include integration` passes
- Any follow-ups for `/gsd-verify-work` (Phase 16 gate)
</output>
