---
phase: 15
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
requirements: [BILL-03]
autonomous: true
files_modified:
  - test/integration/subscription_integration_test.exs
  - test/integration/subscription_item_integration_test.exs
  - guides/subscriptions.md
  - mix.exs

must_haves:
  truths:
    - "Full Subscription CRUD + lifecycle round-trip (create → update with items[] proration → pause_collection → resume → cancel) passes against stripe-mock"
    - "Full SubscriptionItem CRUD round-trip passes against stripe-mock"
    - "form_encoder.ex correctly encodes items[0][price], items[0][quantity], items[0][proration_behavior] via an explicit integration assertion"
    - "guides/subscriptions.md renders under Billing module group and contains an explicit 'webhook handlers own state transitions' callout"
    - "ExDoc Billing module group in mix.exs includes Subscription, SubscriptionItem, PauseCollection, CancellationDetails, TrialSettings"
  artifacts:
    - path: "test/integration/subscription_integration_test.exs"
      provides: "stripe-mock integration coverage for Subscription lifecycle"
      contains: "@moduletag :integration"
    - path: "test/integration/subscription_item_integration_test.exs"
      provides: "stripe-mock integration coverage for SubscriptionItem CRUD"
      contains: "@moduletag :integration"
    - path: "guides/subscriptions.md"
      provides: "Developer guide with webhook handoff callout"
      contains: "webhook handlers"
    - path: "mix.exs"
      provides: "ExDoc Billing module group extension"
      contains: "LatticeStripe.Subscription"
  key_links:
    - from: "test/integration/subscription_integration_test.exs"
      to: "LatticeStripe.Subscription"
      via: "test_integration_client/0 + real HTTP to stripe-mock:12111"
      pattern: "Subscription.create"
    - from: "mix.exs"
      to: "guides/subscriptions.md"
      via: "docs: [extras: [...]] entry"
      pattern: "guides/subscriptions.md"
---

<objective>
Close out Phase 15 with stripe-mock integration tests, a developer guide that includes the webhook-handler boundary callout (T-15-04 mitigation), and ExDoc wiring so the new modules land in the Billing module group.

Purpose: Verify end-to-end that Subscription and SubscriptionItem work against a real HTTP server running Stripe's OpenAPI spec, and give developers documentation that steers them toward correct operational patterns (webhook-driven state transitions, explicit proration).

Output:
- `test/integration/subscription_integration_test.exs` (full lifecycle round-trip against stripe-mock)
- `test/integration/subscription_item_integration_test.exs` (full CRUD round-trip)
- `guides/subscriptions.md` (guide with sections listed below)
- `mix.exs` extras + Billing module group updated
- No new telemetry events (explicitly confirmed in guide and/or module docs)

**Note per D1:** This plan assumes `LatticeStripe.Price` and `LatticeStripe.Testing.TestClock` modules exist on `main` at execution time (Phase 12/13 restore commit). If they do not, executor must halt and notify the user — restore is a pre-phase prerequisite, not an executor fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-subscriptions-subscription-items/15-CONTEXT.md
@.planning/phases/15-subscriptions-subscription-items/15-RESEARCH.md
@.planning/phases/15-subscriptions-subscription-items/15-01-PLAN.md
@.planning/phases/15-subscriptions-subscription-items/15-02-PLAN.md
@test/integration/invoice_integration_test.exs
@test/support/test_helpers.ex
@test/support/fixtures/subscription.ex
@test/support/fixtures/subscription_item.ex
@lib/lattice_stripe/subscription.ex
@lib/lattice_stripe/subscription_item.ex
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Subscription stripe-mock integration test (full lifecycle round-trip)</name>
  <files>
    test/integration/subscription_integration_test.exs
  </files>
  <read_first>
    - test/integration/invoice_integration_test.exs (PRIMARY TEMPLATE — module tag, test_integration_client/0 usage)
    - test/support/test_helpers.ex (`test_integration_client/0` — points at stripe-mock on :12111)
    - lib/lattice_stripe/subscription.ex (signatures available after Plan 15-01)
    - test/support/fixtures/subscription.ex (fixtures from Plan 15-01)
    - lib/lattice_stripe/price.ex (restored via D1 — needed to create real Price fixtures for Subscription.create)
    - lib/lattice_stripe/testing/test_clock.ex (restored via D1 — needed for pause/resume time travel if used)
    - .planning/phases/15-subscriptions-subscription-items/15-RESEARCH.md (§Validation Architecture item 4)
    - .planning/phases/15-subscriptions-subscription-items/15-CONTEXT.md (§Specifics — items[0][...] encoding verification)
  </read_first>
  <action>
    Create `test/integration/subscription_integration_test.exs` following the `test/integration/invoice_integration_test.exs` template.

    **Module header:**
    ```elixir
    defmodule LatticeStripe.Integration.SubscriptionTest do
      use ExUnit.Case, async: false
      @moduletag :integration

      alias LatticeStripe.{Customer, Price, Product, Subscription, SubscriptionItem}
      alias LatticeStripe.Test.Helpers

      setup do
        client = Helpers.test_integration_client()
        {:ok, client: client}
      end
    end
    ```

    **Test cases (minimum):**

    1. `test "CRUD + lifecycle round-trip", %{client: client}` — the full happy-path flow:
       ```
       - Create Product (Product.create) and Price (Price.create) fixtures
       - Create Customer (Customer.create)
       - Create Subscription: Subscription.create(client, %{"customer" => cus.id, "items" => [%{"price" => price.id, "quantity" => 1}]})
       - Assert {:ok, %Subscription{items: [%SubscriptionItem{id: si_id} | _]}} (id is preserved — stripity_stripe regression)
       - Retrieve: Subscription.retrieve(client, sub.id) → matches
       - Update items[] with proration_behavior:
           Subscription.update(client, sub.id, %{
             "items" => [%{"id" => si_id, "quantity" => 3, "proration_behavior" => "create_prorations"}]
           })
       - Pause collection:
           Subscription.pause_collection(client, sub.id, :keep_as_draft)
         Assert `%Subscription{pause_collection: %Subscription.PauseCollection{behavior: "keep_as_draft"}}`
       - Resume:
           Subscription.resume(client, sub.id)
       - Cancel:
           Subscription.cancel(client, sub.id)
         Assert status transitions observable on returned struct
       ```

    2. `test "list + stream! paginate", %{client: client}` — create several subscriptions, assert `Subscription.stream!/2 |> Enum.take(N)` works.

    3. `test "search_stream! paginates search results", %{client: client}` — call `Subscription.search_stream!(client, %{"query" => "status:'active'"}) |> Enum.take(1)`; assert no crash and lazy behavior (even if stripe-mock returns an empty page, the pagination machinery must not explode).

    4. `test "form encoder encodes items[0][...] nested params correctly", %{client: client}` — T-15-05 sanity test. Build a create call with explicit nested items params including `proration_behavior`, assert that Stripe-mock accepts it (i.e., we get `{:ok, %Subscription{}}` rather than a 400). The server-side validation *is* the verification of the form encoder.

    5. `test "strict client rejects items[] update without proration_behavior", %{client: _}` — uses a strict client (`require_explicit_proration: true`) and asserts `{:error, %LatticeStripe.Error{type: :proration_required}}` WITHOUT hitting stripe-mock (guard fails pre-network). Verifies the items[] extension in an end-to-end-shaped test.

    6. `test "idempotency_key is forwarded", %{client: client}` — pass `idempotency_key: "test-ik-123"` to `Subscription.create` and assert success (stripe-mock honors idempotency headers). Second call with same key should return the same subscription id.

    All tests MUST be tagged `@moduletag :integration` so they only run under `mix test --include integration`.
  </action>
  <verify>
    <automated>mix test --include integration --only integration test/integration/subscription_integration_test.exs</automated>
  </verify>
  <done>
    - `test/integration/subscription_integration_test.exs` contains `@moduletag :integration`
    - File contains `test "CRUD + lifecycle round-trip"`
    - File contains `Subscription.pause_collection(client, sub.id, :keep_as_draft)`
    - File contains `Subscription.resume(client, `
    - File contains `"proration_behavior" => "create_prorations"`
    - File contains `test "idempotency_key is forwarded"` or equivalent
    - File contains `test "strict client rejects items[] update without proration_behavior"` or equivalent
    - With `stripe-mock` running on :12111, `mix test --include integration test/integration/subscription_integration_test.exs` exits 0
    - `mix credo --strict test/integration/subscription_integration_test.exs` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SubscriptionItem stripe-mock integration test</name>
  <files>
    test/integration/subscription_item_integration_test.exs
  </files>
  <read_first>
    - test/integration/invoice_integration_test.exs (template)
    - lib/lattice_stripe/subscription_item.ex (Plan 15-02 output)
    - test/support/fixtures/subscription_item.ex
    - test/support/test_helpers.ex
    - lib/lattice_stripe/price.ex (D1 restore)
  </read_first>
  <action>
    Create `test/integration/subscription_item_integration_test.exs` covering:

    1. `test "create → retrieve → update → delete round-trip"` — create a Subscription first, then exercise every `SubscriptionItem` CRUD fn against its items.
    2. `test "list/3 requires subscription param"` — asserts `assert_raise ArgumentError, ~r/subscription/, fn -> SubscriptionItem.list(client, %{}) end`. This is a pure ArgumentError path and does not actually need stripe-mock running, but keep it in the integration file alongside its peers.
    3. `test "list + stream! filtered by subscription"` — create subscription with 2 items, call `SubscriptionItem.list(client, %{"subscription" => sub.id})` and `SubscriptionItem.stream!(client, %{"subscription" => sub.id}) |> Enum.to_list()`.
    4. `test "update with proration_behavior succeeds against strict client"` — strict client, explicit proration, should reach stripe-mock successfully.
    5. `test "idempotency_key is forwarded on create"` — same pattern as Subscription test.

    Tag `@moduletag :integration`.
  </action>
  <verify>
    <automated>mix test --include integration --only integration test/integration/subscription_item_integration_test.exs</automated>
  </verify>
  <done>
    - `test/integration/subscription_item_integration_test.exs` contains `@moduletag :integration`
    - File contains `test "create → retrieve → update → delete round-trip"` (or ASCII arrows — executor's discretion, but the test name must exist)
    - File contains `assert_raise ArgumentError` around `SubscriptionItem.list`
    - File contains `SubscriptionItem.stream!(client`
    - With stripe-mock running, `mix test --include integration test/integration/subscription_item_integration_test.exs` exits 0
    - `mix credo --strict test/integration/subscription_item_integration_test.exs` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Write guides/subscriptions.md with webhook callout (T-15-04 mitigation)</name>
  <files>
    guides/subscriptions.md
  </files>
  <read_first>
    - Any existing guides/*.md file for tone + formatting (e.g. guides/invoices.md if present, otherwise the top-level README + module docs)
    - lib/lattice_stripe/subscription.ex (function signatures — document exactly what Plan 15-01 shipped)
    - lib/lattice_stripe/subscription_item.ex (Plan 15-02)
    - .planning/phases/15-subscriptions-subscription-items/15-CONTEXT.md (§Specifics for PII safety and telemetry notes)
    - .planning/phases/15-subscriptions-subscription-items/15-RESEARCH.md (§Security Threat Model Hooks — T4)
  </read_first>
  <action>
    Create `guides/subscriptions.md`. Required sections (in order):

    1. **`# Subscriptions`** — one-paragraph overview pointing at Stripe docs.
    2. **`## Creating a subscription`** — example using real `Customer.create` + `Price.create` + `Subscription.create` flow. Show both map-based params and the `items` field shape. Mention `idempotency_key` in opts.
    3. **`## The subscription lifecycle`** — diagram or ASCII state machine (incomplete → active → past_due → canceled, plus paused and trialing branches). Describe each transition and which SDK call triggers it.
    4. **`## Lifecycle operations`** — subsections for `update`, `cancel`, `resume`, `pause_collection`. For `pause_collection`, document the three atom values `:keep_as_draft`, `:mark_uncollectible`, `:void`. Document `cancel_at` as a one-liner update pattern (no dedicated helper — per D2 deferred).
    5. **`## Proration`** — explain `require_explicit_proration` client option and show both top-level and `items[]` param shapes. Explicitly note: "The guard detects `proration_behavior` at the top level, inside `subscription_details`, OR inside any `items[]` element."
    6. **`## SubscriptionItem operations`** — CRUD examples. Call out that `list/3` requires the `subscription` param (OQ-2 rationale).
    7. **`## Webhooks own state transitions`** (T-15-04 CALLOUT — MANDATORY SECTION). Exact required text to include (verbatim or close):
       > **Important:** The response from any SDK call reflects Stripe's state at the moment of that call. Subscription state transitions (trial ending, payment failing, subscription canceling at period end, dunning retries) are driven by Stripe's internal billing engine, not by SDK calls. **Always drive your application state from webhook events, not from SDK responses.** LatticeStripe provides `LatticeStripe.Webhook` for signature verification — wire `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed`, and `invoice.payment_succeeded` into your handler.
    8. **`## Telemetry`** — one paragraph confirming no subscription-specific events; point at the general `[:lattice_stripe, :request, :start|:stop|:exception]` events. Explicitly state "No new telemetry events were added for Subscriptions — subscription state transitions belong to webhook handlers."
    9. **`## PII and logging`** — note that `Inspect` on `%Subscription{}` hides `customer`, `payment_settings`, `default_payment_method`, and `CancellationDetails.comment`.
  </action>
  <verify>
    <automated>test -f guides/subscriptions.md && grep -q "Always drive your application state from webhook events" guides/subscriptions.md && grep -q "pause_collection" guides/subscriptions.md && grep -q ":keep_as_draft" guides/subscriptions.md && grep -q "No new telemetry events" guides/subscriptions.md && echo OK</automated>
  </verify>
  <done>
    - `guides/subscriptions.md` exists
    - File contains section heading `## Webhooks own state transitions`
    - File contains the exact phrase `Always drive your application state from webhook events, not from SDK responses`
    - File contains `:keep_as_draft`, `:mark_uncollectible`, `:void` (all three pause_collection atoms)
    - File contains `require_explicit_proration`
    - File contains `No new telemetry events`
    - File contains a SubscriptionItem section with `list/3 requires the subscription param` or equivalent wording
    - `grep` check in verify command succeeds
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire guide + modules into mix.exs ExDoc Billing group</name>
  <files>
    mix.exs
  </files>
  <read_first>
    - mix.exs (current `docs/0` function, `extras:` list, `groups_for_modules:` — whatever structure exists)
    - .planning/phases/14-invoices-invoice-line-items/ (any SUMMARY that shows how the Phase 14 group was wired)
    - lib/lattice_stripe/subscription.ex + lib/lattice_stripe/subscription_item.ex (to confirm module names)
  </read_first>
  <action>
    In `mix.exs`'s `docs/0` function:

    1. **Extras:** add `"guides/subscriptions.md"` to the `extras:` list. If extras groups exist (e.g. `groups_for_extras:`), place it under a `"Guides"` or `"Billing"` group alongside any existing billing guide.

    2. **Module groups:** in `groups_for_modules:` (or whatever structure mix.exs uses), extend the **Billing** group to include:
       - `LatticeStripe.Subscription`
       - `LatticeStripe.SubscriptionItem`
       - `LatticeStripe.Subscription.PauseCollection`
       - `LatticeStripe.Subscription.CancellationDetails`
       - `LatticeStripe.Subscription.TrialSettings`

    Keep the module group alphabetized (or matching the existing precedent — whatever Phase 14 established).

    Do NOT touch unrelated sections (deps, application, package) — this is a docs-only wiring change.
  </action>
  <verify>
    <automated>mix docs 2>&1 | tee /tmp/mix_docs.log && test $? -eq 0 && grep -q "LatticeStripe.Subscription" doc/api-reference.html && grep -q "Subscriptions" doc/api-reference.html && echo OK</automated>
  </verify>
  <done>
    - `mix.exs` contains `"guides/subscriptions.md"` in the docs extras list
    - `mix.exs` contains `LatticeStripe.Subscription` in the Billing module group
    - `mix.exs` contains `LatticeStripe.SubscriptionItem` in the Billing module group
    - `mix.exs` contains `LatticeStripe.Subscription.PauseCollection`
    - `mix.exs` contains `LatticeStripe.Subscription.CancellationDetails`
    - `mix.exs` contains `LatticeStripe.Subscription.TrialSettings`
    - `mix docs` exits 0 with no warnings about missing modules
    - `doc/LatticeStripe.Subscription.html` exists after build
    - `doc/subscriptions.html` (or equivalent guide HTML) exists after build
    - `mix format --check-formatted` exits 0
    - `mix credo --strict` exits 0 (full project sweep)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User reads docs → writes integration code | Documentation is the control surface that prevents misuse patterns (webhook confusion) |
| Integration tests → stripe-mock HTTPS | Verifies form encoding, idempotency headers, nested param encoding against real OpenAPI spec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering / Replay | Integration tests for idempotency | mitigate (verification) | Task 1 + Task 2 assert `idempotency_key` forwarded end-to-end through HTTP (stripe-mock honors the header); second call with same key returns identical resource. |
| T-15-03 | Elevation of Privilege | Integration test for strict client + items[] | mitigate (verification) | Task 1 includes a strict-client test asserting `{:error, :proration_required}` for items[] update without `proration_behavior`. |
| T-15-04 | Information Disclosure / Operational | `guides/subscriptions.md` | mitigate | Task 3 adds mandatory "Webhooks own state transitions" callout with exact wording. Verified by grep in the automated check. |
| T-15-05 | Tampering (form encoding) | Integration test for nested items[] encoding | mitigate (verification) | Task 1 includes an explicit nested-param encoding test that stripe-mock validates against the real OpenAPI spec — server rejecting malformed nested params would fail the test. |
</threat_model>

<verification>
With stripe-mock running (`docker run -p 12111:12111 stripe/stripe-mock:latest`):

```bash
mix test --include integration
mix credo --strict
mix format --check-formatted
mix docs
```

All must exit 0.
</verification>

<success_criteria>
- Subscription + SubscriptionItem integration tests pass against stripe-mock
- Items[] nested encoding verified by server-side acceptance
- `guides/subscriptions.md` renders cleanly under Billing group with webhook callout
- All 5 Phase 15 modules (Subscription, SubscriptionItem, PauseCollection, CancellationDetails, TrialSettings) appear in ExDoc Billing group
- Full unit + integration suite green
- No new telemetry events introduced (confirmed in guide + module docs)
</success_criteria>

<output>
After completion, create `.planning/phases/15-subscriptions-subscription-items/15-03-SUMMARY.md` capturing:
- Integration test counts
- Confirmation that `mix docs` renders without warnings
- Confirmation that the webhook callout grep passes
- Any follow-ups for Phase 16 (Schedules)
</output>
