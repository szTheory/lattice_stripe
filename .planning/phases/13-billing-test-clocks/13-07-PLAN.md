---
phase: 13-billing-test-clocks
plan: 07
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05]
files_modified:
  - mix.exs
  - lib/lattice_stripe/test_helpers/test_clock.ex
  - lib/lattice_stripe/testing/test_clock.ex
  - CHANGELOG.md
autonomous: true
requirements: [BILL-08, BILL-08b, BILL-08c, TEST-09, TEST-10]
must_haves:
  truths:
    - "mix.exs groups_for_modules includes LatticeStripe.TestHelpers.TestClock and LatticeStripe.Testing.TestClock in a sensible group (likely 'Testing' or a new 'Test Helpers' group)"
    - "LatticeStripe.TestHelpers.TestClock @moduledoc includes a worked example of create/advance_and_wait (referencing Plan 04's public API)"
    - "LatticeStripe.Testing.TestClock @moduledoc contains the D-13d canonical happy-path example verbatim"
    - "CHANGELOG.md has an Unreleased section entry documenting Phase 13: TestClock resource, advance_and_wait, Testing.TestClock helper, Mix task, :real_stripe tier"
    - "mix docs --warnings-as-errors runs clean — every @doc and @moduledoc renders with no broken links"
    - "mix format --check-formatted passes on all Phase 13 files"
    - "mix credo --strict passes on all Phase 13 files (with credo:disable-for-next-line annotations where justified)"
  artifacts:
    - path: "mix.exs"
      provides: "Updated groups_for_modules with new Phase 13 modules"
      contains: "LatticeStripe.TestHelpers.TestClock"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry for Phase 13"
      contains: "Test Clock"
  key_links:
    - from: "mix.exs:groups_for_modules"
      to: "LatticeStripe.TestHelpers.TestClock + LatticeStripe.Testing.TestClock"
      via: "explicit module listing"
      pattern: "LatticeStripe.TestHelpers.TestClock"
---

<objective>
Polish pass on the Phase 13 docs and release-prep artifacts. Everything that ships to HexDocs and CHANGELOG lands here. Ensures `mix docs --warnings-as-errors`, `mix format --check-formatted`, and `mix credo --strict` all pass so the phase is mergeable.

Purpose: close Phase 13 cleanly with production-quality documentation. No new functionality ships in this plan — everything here is docs, formatting, linting, and one CHANGELOG entry.
Output: updated `mix.exs` groups_for_modules, verified/extended `@moduledoc`s on the two new public modules, CHANGELOG.md entry, verified quality gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-billing-test-clocks/13-CONTEXT.md
@.planning/phases/13-billing-test-clocks/13-RESEARCH.md
@lib/lattice_stripe/test_helpers/test_clock.ex
@lib/lattice_stripe/testing/test_clock.ex
@mix.exs
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mix.exs groups_for_modules and verify moduledocs</name>
  <read_first>
    - mix.exs (the groups_for_modules block — typically around lines 40-80)
    - lib/lattice_stripe/test_helpers/test_clock.ex (from Plan 04 — verify moduledoc has a worked example)
    - lib/lattice_stripe/testing/test_clock.ex (from Plan 05 — verify moduledoc has the D-13d canonical example)
    - .planning/phases/13-billing-test-clocks/13-CONTEXT.md D-13d (lines 65-87 — the happy-path example to ensure is present verbatim)
  </read_first>
  <files>
    mix.exs,
    lib/lattice_stripe/test_helpers/test_clock.ex,
    lib/lattice_stripe/testing/test_clock.ex
  </files>
  <action>
  **Step 1 — Update `mix.exs` groups_for_modules.** Read the existing block. Phase 12 likely added entries under "Billing" or "Resources" groups. Add a new group OR extend an existing one:

  Either create a new group:

  ```elixir
  "Test Helpers": [
    LatticeStripe.TestHelpers.TestClock,
    LatticeStripe.Testing.TestClock
  ]
  ```

  Or, if a "Testing" group already exists from Phase 9, add both modules there. Match the existing convention — alphabetize or keep Phase 13's additions grouped per the surrounding code.

  Do NOT add `LatticeStripe.Testing.TestClock.Owner` or `LatticeStripe.Testing.TestClock.Error` to groups — they are internal (`@moduledoc false`) and should not appear in ExDoc output.

  Do NOT add `LatticeStripe.Testing.RealStripeCase` — it lives in `test/support/` and is not shipped.

  **Step 2 — Verify `lib/lattice_stripe/test_helpers/test_clock.ex` @moduledoc.** Read the file. Confirm it has:
  - A one-paragraph summary of Test Clocks and what the module does
  - The 100-clock account limit callout
  - The metadata probe result (from Plan 02)
  - A "Status values" section enumerating `:ready`, `:advancing`, `:internal_failure`
  - A "Deletion cascades" warning
  - At least one worked example showing `create/2` + `advance_and_wait/4`

  If any of these is missing or under-developed, extend it. The target shape is "Phase 12 Product.ex moduledoc quality."

  Add (if not already present) a worked example in the `advance_and_wait/4` `@doc` itself:

  ```elixir
  ## Example

      frozen_time = System.system_time(:second)

      {:ok, clock} = LatticeStripe.TestHelpers.TestClock.create(client, %{frozen_time: frozen_time})

      {:ok, ready} =
        LatticeStripe.TestHelpers.TestClock.advance_and_wait(
          client,
          clock.id,
          frozen_time + 86_400 * 30
        )

      assert ready.status == :ready
  ```

  **Step 3 — Verify `lib/lattice_stripe/testing/test_clock.ex` @moduledoc.** Read the file. Confirm it contains the D-13d canonical happy-path example verbatim (from CONTEXT lines 65-87). The example should appear as a code block in the moduledoc:

  ```elixir
  defmodule MyApp.StripeCase do
    use ExUnit.CaseTemplate
    using do
      quote do
        use LatticeStripe.Testing.TestClock, client: MyApp.StripeClient
      end
    end
  end

  defmodule MyApp.BillingTest do
    use MyApp.StripeCase, async: true
    setup :with_test_clock

    test "sub renews after 30 days", %{test_clock: clock} do
      customer = create_customer(clock, email: "a@b.c")
      {:ok, sub} = MyApp.Billing.subscribe(customer, "price_monthly")
      advance(clock, days: 30)
      assert {:ok, %{status: "active"}} = MyApp.Billing.get_subscription(sub.id)
    end
  end
  ```

  If Plan 05 already included this, verify it compiles as a doctest or standalone example code block. If Plan 05 omitted or altered it, extend the moduledoc now. Do NOT change the example — it's the locked D-13d contract.

  Also verify the moduledoc includes:
  - The `## Usage` section with the use-macro example
  - The `## Cleanup strategy` section (on_exit + Mix task backstop)
  - The `## Metadata marker` section documenting `@cleanup_marker`
  - The `## Supported advance units (v1)` section with the A-13d months/years limitation callout
  - The `## Client override` section documenting the per-call `client:` opt

  **Step 4 — Run:**

  ```bash
  mix compile --warnings-as-errors
  mix docs --warnings-as-errors
  ```

  `mix docs` must run clean. If there are broken links or doc warnings, fix them here.
  </action>
  <acceptance_criteria>
    - `grep -q "LatticeStripe.TestHelpers.TestClock" mix.exs` exits 0 (in groups_for_modules block)
    - `grep -q "LatticeStripe.Testing.TestClock" mix.exs` exits 0 (in groups_for_modules block, and NOT as a sub-module path match — verify it's the top-level Testing.TestClock)
    - `grep -q "Owner" mix.exs` exits 1 OR the Owner is NOT in a documentation group (internal module)
    - `grep -q "RealStripeCase" mix.exs` exits 1 (test/support/ module should NOT be in ExDoc groups)
    - `grep -q "MyApp.StripeCase" lib/lattice_stripe/testing/test_clock.ex` exits 0 (D-13d canonical example)
    - `grep -q "advance(clock, days: 30)" lib/lattice_stripe/testing/test_clock.ex` exits 0 (D-13d example)
    - `grep -q "100" lib/lattice_stripe/test_helpers/test_clock.ex` exits 0 (100-clock limit in moduledoc)
    - `mix compile --warnings-as-errors` exits 0
    - `mix docs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && mix docs --warnings-as-errors</automated>
  </verify>
  <done>mix.exs groups_for_modules updated; both public modules have production-quality moduledocs; mix docs runs clean</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG.md Unreleased entry and run quality gates</name>
  <read_first>
    - CHANGELOG.md (existing — read to see the format Phase 12 used)
    - .planning/phases/13-billing-test-clocks/13-CONTEXT.md (for the high-level Phase 13 pitch)
  </read_first>
  <files>
    CHANGELOG.md
  </files>
  <action>
  **Step 1 — Update `CHANGELOG.md`.** Read the current file. If there's an `## [Unreleased]` section from Phase 12, extend it. Otherwise, create one at the top (under the main heading, above the most recent released version).

  Add an entry like:

  ```markdown
  ## [Unreleased]

  ### Added (Phase 13: Billing Test Clocks)

  - `LatticeStripe.TestHelpers.TestClock` — full SDK resource for Stripe's
    `/v1/test_helpers/test_clocks` API: `create/2`, `retrieve/3`, `list/3`,
    `stream!/3`, `delete/3`, `advance/4`, and the differentiating
    `advance_and_wait/4` and `advance_and_wait!/4` helpers that poll until
    `:ready` with exponential backoff + full jitter (500ms floor), monotonic
    deadline, and a configurable 60-second default timeout. `advance_and_wait`
    is not shipped by any other Stripe SDK.
  - `LatticeStripe.Testing.TestClock` — user-facing ExUnit helper library
    with a compile-time `use`-macro for client binding (Oban.Testing-style),
    automatic per-test cleanup via a lightweight `Owner` GenServer
    (Ecto.Sandbox-style `start_owner!` + `on_exit`), `test_clock/1`,
    `advance/2` (supports `:seconds`, `:minutes`, `:hours`, `:days`, `:to`
    on Elixir 1.15+), `freeze/1`, `create_customer/3` (auto-injects
    `test_clock:` to close the silent-correctness footgun), and
    `with_test_clock/1` setup callback.
  - `LatticeStripe.Error` extended with `:test_clock_timeout` and
    `:test_clock_failed` types for `advance_and_wait` failure modes. Clock
    context (`clock_id`, `last_status`, `attempts`, `elapsed_ms`) stored in
    the existing `:raw_body` field — no schema change.
  - `LatticeStripe.Client` extended with `:idempotency_key_prefix` option
    for per-environment idempotency key namespacing (backward-compatible,
    default `nil` preserves the existing `idk_ltc_` prefix).
  - `mix lattice_stripe.test_clock.cleanup` — Mix task backstop for
    SIGKILL / CI timeout scenarios that bypass `on_exit`. Safe-by-default
    (`--dry-run true`, `--yes false`); destructive delete requires both
    `--yes` and `--dry-run false`. Supports `--older-than` (e.g. `1h`,
    `24h`), `--client` (module name), stripe-mock detection.
  - `LatticeStripe.Testing.RealStripeCase` — internal CaseTemplate
    (`test/support/real_stripe_case.ex`, not shipped in Hex) establishing
    the `:real_stripe` test tier used by phases 14-19. Gates on
    `STRIPE_TEST_SECRET_KEY`, refuses `sk_live_*` keys as a non-negotiable
    safety guard, 120-second per-test timeout, `async: false` implicit.
  - First `@moduletag :real_stripe` test in the repo:
    `test/real_stripe/test_clock_real_stripe_test.exs` — full TestClock
    round-trip (create → advance 30 days → assert ready → delete) against
    live Stripe test mode.
  - `.planning/CONVENTIONS.md` — codifies the flat-core / nested-subproduct
    namespace rule (Phase 17 Connect and future sub-product families will
    follow it).
  - `CONTRIBUTING.md` — new section documenting the `direnv` + `.envrc`
    workflow for `STRIPE_TEST_SECRET_KEY`.

  ### Changed

  - `test/support/test_helpers.ex` (internal test-only) renamed to
    `test/support/test_support.ex`; module name `LatticeStripe.TestHelpers`
    → `LatticeStripe.TestSupport` to free the `TestHelpers` namespace for
    the public `LatticeStripe.TestHelpers.TestClock` submodule.
  ```

  Keep the format consistent with Phase 12's CHANGELOG entries. Do NOT add a version number — this is `[Unreleased]` and will be versioned at the v0.3.0 release (Phase 19).

  **Step 2 — Run quality gates:**

  ```bash
  mix format --check-formatted
  mix credo --strict
  mix compile --warnings-as-errors
  mix docs --warnings-as-errors
  mix test --exclude integration
  ```

  ALL must pass. If `mix format --check-formatted` fails on any Phase 13 file, run `mix format` and commit the formatting fix as part of this task.

  If `mix credo --strict` fails with actionable warnings, fix the code OR add `credo:disable-for-next-line <CheckName>` annotations with a comment explaining why. Do NOT globally disable credo checks in `.credo.exs` without justification — the goal is to keep credo strict for the whole codebase.

  Typical expected credo noise on Phase 13 code:
  - `Credo.Check.Refactor.LongQuoteBlocks` on the `__using__` macro in `Testing.TestClock` — add `credo:disable-for-next-line` with the justification "quote block is a single logical unit"
  - `Credo.Check.Readability.LargeNumbers` on `60_000` / `86_400` constants — these are already underscored
  - `Credo.Check.Refactor.CyclomaticComplexity` on the `poll_until_ready/9` function — acceptable for a state machine; add `credo:disable-for-next-line` with the justification "poll state machine"

  **Step 3 — Run the full test matrix:**

  ```bash
  mix test --exclude integration
  ```

  And if stripe-mock is available locally:

  ```bash
  mix test --include integration
  ```
  </action>
  <acceptance_criteria>
    - `grep -q "\\[Unreleased\\]" CHANGELOG.md` exits 0
    - `grep -q "Test Clock" CHANGELOG.md` exits 0
    - `grep -q "advance_and_wait" CHANGELOG.md` exits 0
    - `grep -q "Testing.TestClock" CHANGELOG.md` exits 0
    - `grep -q "real_stripe" CHANGELOG.md` exits 0
    - `grep -q "idempotency_key_prefix" CHANGELOG.md` exits 0
    - `mix format --check-formatted` exits 0
    - `mix credo --strict` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - `mix docs --warnings-as-errors` exits 0
    - `mix test --exclude integration` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted && mix credo --strict && mix compile --warnings-as-errors && mix docs --warnings-as-errors && mix test --exclude integration</automated>
  </verify>
  <done>CHANGELOG Unreleased entry lands; all quality gates green; Phase 13 is merge-ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — docs-only plan) | |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-23 | Information Disclosure | CHANGELOG content | accept | No secrets, no internal file paths, no PII in the CHANGELOG text. Only public API surface. |
</threat_model>

<verification>
- Every quality gate (format, credo, compile, docs, test) green
- CHANGELOG updated with Phase 13 additions
- mix.exs groups_for_modules routes new modules to sensible ExDoc groups
- Both public module @moduledocs meet Phase 12 quality bar
</verification>

<success_criteria>
Phase 13 is mergeable to `main`. Running `mix docs` produces a HexDocs-quality artifact for `LatticeStripe.TestHelpers.TestClock` and `LatticeStripe.Testing.TestClock`. The `/gsd-verify-work` step can run all quality gates from this plan's final verify command and get a single green result.
</success_criteria>

<output>
After completion, create `.planning/phases/13-billing-test-clocks/13-07-SUMMARY.md`
</output>
