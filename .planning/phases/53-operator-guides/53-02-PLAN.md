---
phase: 53-operator-guides
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/event-debugging.md
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "Adopters can diagnose webhook production pain via symptom → cause → fix spine without re-reading full build guides."
    - "Snapshot vs thin event wrong-entry-point failure mode is documented with decision table."
  artifacts:
    - path: "guides/event-debugging.md"
      provides: "OPS-02 event debugging playbook"
      contains: "Debug from the delivery boundary inward"
  key_links:
    - from: "guides/event-debugging.md"
      to: "guides/webhooks-thin-events.md"
      via: "markdown link"
      pattern: "webhooks-thin-events.md"
---

<objective>
Create `guides/event-debugging.md` — the post-incident webhook debugging trust rail (OPS-02).

Purpose: Close JTBD Gap 2 with a ~220–280 line symptom spine. Build content stays in `webhooks.md` / `webhooks-thin-events.md`; this guide owns production diagnosis.

Output: New file `guides/event-debugging.md` with v1.7 install canary, decision table, symptom index, diagnostic checklists, replay semantics, dispatch debug lens, and bounded Charge paragraph per D-05.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-operator-guides/53-CONTEXT.md
@.planning/phases/53-operator-guides/53-RESEARCH.md
@guides/webhooks-thin-events.md
@guides/webhooks.md
@guides/error-handling.md
@guides/telemetry.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| PII/credential leak in debug steps | high | No `IO.inspect` on payloads; log event.id/type only |
| Wrong Charge.search for fresh payments | medium | Explicit anti-pattern in charge.* section |
| Dashboard Resend footgun | low | Document CLI vs Dashboard Resend difference |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/event-debugging.md</name>
  <files>guides/event-debugging.md</files>
  <read_first>
    - .planning/phases/53-operator-guides/53-CONTEXT.md (D-01, D-03, D-04, D-05)
    - guides/webhooks-thin-events.md (D-14 lineage, verify error atoms, fetch-after-verify)
    - guides/webhooks.md (troubleshooting bullets — link, do not copy Plug setup)
    - guides/error-handling.md (request_id support escalation)
    - guides/telemetry.md (webhook verify event names)
  </read_first>
  <action>
    Create `guides/event-debugging.md` with this structure:

    1. **Title:** `# Event Debugging`
    2. **Anchor opener (verbatim):** *"Debug from the delivery boundary inward: Dashboard → HTTP status → signature → payload shape → dispatch."*
    3. **D-14 reuse:** *"Your app starts work. Webhooks confirm reality."*
    4. **Install block:**
       ```elixir
       {:lattice_stripe, "~> 1.7"}
       ```
    5. **Start here — Snapshot vs thin:** Decision table (columns: Endpoint surface | Payload shape | First SDK call | Wrong symptom). Include wrong-entry-point: mostly-nil struct from `construct_event` on thin payload → link `webhooks-thin-events.md`.
    6. **Symptom index table** with rows for at minimum:
       - 400 verify fail
       - 500 after verify
       - duplicate deliveries
       - missing events
       - handler timeouts / slow 5xx retry storm
    7. **Signature verification failures:** Diagnostic checklist covering CLI vs Dashboard secret mismatch, raw body (`conn.private[:raw_body]`), Plug order, `:timestamp_expired`, `:no_matching_signature`, `:missing_header`, `:invalid_header`.
    8. **Fetch-after-verify debugging:** Log `%LatticeStripe.Error{request_id: _}`; 429 rate-limit symptoms; idempotency on `event.id` not resource state; link `error-handling.md`.
    9. **Delivery / replay / Stripe retries:** At-least-once delivery; Stripe automatic retries; `stripe events resend` CLI vs Dashboard **Resend** footgun (re-sends same event id).
    10. **Common dispatch patterns:** 2xx-after-enqueue, 5xx retry storm, Connect `event.context` routing — debug lens only, no full controller spine (>15 lines max code).
    11. **Observability checklist:** `[:lattice_stripe, :webhook, :verify, :stop]` and request events with `request_id`; link `telemetry.md`.
    12. **`charge.*` events (~1 paragraph):** D-05 guardrail verbatim; follow `payment_intent` when present; `Charge.retrieve/3` for charge-specific fields; **anti-pattern:** do not use `Charge.search/3` for fresh payments (search lags).
    13. **See also:** `webhooks.md`, `webhooks-thin-events.md`, `production-checklist.md`, `error-handling.md`, `testing.md`.
    14. Target **220–280 lines**. Do NOT duplicate full thin Phoenix controller, Ecto idempotency sketch, or Plug setup prose from webhooks.md.
  </action>
  <verify>wc -l guides/event-debugging.md | awk '{print $1 >= 220 && $1 <= 290}'</verify>
  <acceptance_criteria>
    - File `guides/event-debugging.md` exists
    - Contains `Debug from the delivery boundary inward`
    - Contains `{:lattice_stripe, "~> 1.7"}`
    - Contains `:no_matching_signature`, `:timestamp_expired`, `request_id`, `event.id`
    - Contains `parse_event_notification` OR `construct_event` AND `fetch_event`
    - Contains `stripe events resend` AND `at-least-once` (or `at least once`)
    - Contains `[:lattice_stripe, :webhook, :verify, :stop]` or equivalent telemetry mention
    - Contains D-05 guardrail substring `result record`
    - Contains anti-pattern mention of `Charge.search` for fresh payments
    - Line count between 220 and 290
    - Does NOT contain `IO.inspect`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `wc -l guides/event-debugging.md` — within 220–290 lines
2. Boundary check: no `defmodule MyAppWeb.StripeThinEventController` full spine (>50 lines)
3. Cross-links to webhooks.md and webhooks-thin-events.md present
</verification>

<success_criteria>
- OPS-02 guide exists with symptom spine and all D-03 must-add items
- Build vs diagnose boundary preserved
- v1.7 install canary present
</success_criteria>
