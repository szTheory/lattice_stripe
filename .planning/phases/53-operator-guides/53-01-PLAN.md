---
phase: 53-operator-guides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/production-checklist.md
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Adopters have a scannable pre-launch operator checklist covering API keys, Finch, webhooks, idempotency, errors, telemetry, and bounded Charge support lookups."
    - "The checklist composes canonical guides via cross-links rather than duplicating webhook setup or telemetry recipes."
  artifacts:
    - path: "guides/production-checklist.md"
      provides: "OPS-01 production operator playbook"
      contains: "Production Stripe integrations fail at boundaries"
  key_links:
    - from: "guides/production-checklist.md"
      to: "guides/client-configuration.md"
      via: "markdown link"
      pattern: "client-configuration.md"
---

<objective>
Create `guides/production-checklist.md` — the pre-launch operator trust rail (OPS-01).

Purpose: Give production operators a ~180–220 line gate checklist that composes existing Operations & DX guides into a printable pre-launch spine. This guide owns routing and inline essentials where launch fails silently; canonical guides own behavior truth.

Output: New file `guides/production-checklist.md` with v1.7 install canary, anchor opener, 12-section outline from CONTEXT D-02, Charge support subsection per D-05, and cross-links to sibling guides.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-operator-guides/53-CONTEXT.md
@.planning/phases/53-operator-guides/53-RESEARCH.md
@guides/webhooks-thin-events.md
@guides/client-configuration.md
@guides/webhooks.md
@lib/lattice_stripe/charge.ex
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| PII leak via debug advice | medium | Do not recommend `IO.inspect` on webhook payloads |
| Hardcoded API keys in examples | high | Use `System.fetch_env!/1` in all key examples |
| Misleading Charge guidance | medium | Include D-05 guardrail verbatim; PI-first routing |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/production-checklist.md</name>
  <files>guides/production-checklist.md</files>
  <read_first>
    - .planning/phases/53-operator-guides/53-CONTEXT.md (D-01, D-02, D-04, D-05 — section outline and constraints)
    - guides/webhooks-thin-events.md (tone, anchor opener pattern, install block format)
    - guides/client-configuration.md (Client.new!, idempotency — link targets)
    - guides/webhooks.md (Plug order — minimal snippet source)
    - lib/lattice_stripe/charge.ex (@moduledoc — PI-first guardrail wording)
  </read_first>
  <action>
    Create `guides/production-checklist.md` with this structure:

    1. **Title:** `# Production Checklist`
    2. **Opening paragraph** explaining pre-launch gate purpose (2–3 sentences).
    3. **Anchor rule (verbatim):** *"Production Stripe integrations fail at boundaries — keys, raw bodies, idempotency, and observability."*
    4. **Install block (early):**
       ```elixir
       {:lattice_stripe, "~> 1.7"}
       ```
    5. **Section 1 — Audience and scope:** SDK integration hygiene; link Stripe account checklist for Dashboard setup; point post-launch to `event-debugging.md`.
    6. **Section 2 — Quick checklist:** 12–16 one-line `- [ ]` items covering: API keys, environments, Finch supervision, Webhook.Plug before Parsers, idempotency, error request_id, telemetry, circuit breaker (recommended), Connect (if applicable), smoke tests.
    7. **Sections 3–11** per CONTEXT D-02 outline with **one minimal inline snippet each** where listed:
       - `Client.new!(api_key: System.fetch_env!("STRIPE_SECRET_KEY"))`
       - Finch in supervision tree (1–3 lines + link `performance.md`)
       - `plug LatticeStripe.Webhook.Plug` before `Plug.Parsers` (minimal endpoint.ex snippet)
       - D-14 rule verbatim: *"Your app starts work. Webhooks confirm reality."*
       - `%LatticeStripe.Error{request_id: request_id}` logging mention
       - Idempotency: SDK auto-keys POST + explicit keys for money-moving writes
    8. **Section — Support & audit lookups (~8–12 lines):** Include D-05 guardrail verbatim:
       > Charge is the **result record** of a payment attempt, not payment initiation. Use `PaymentIntent` for payment flows; use `Charge` to read/reconcile existing charges. Full API: `LatticeStripe.Charge` moduledoc (no separate Charge guide in v1.7).
       Mention `Charge.list/3`, `Charge.search/3`, search eventual-consistency caveat, link `connect-money-movement.md`. Omit capture/update/stream!.
    9. **Section — Read next:** Link `event-debugging.md`, `webhooks.md`, `error-handling.md`, `testing.md`, `telemetry.md`.
    10. Target **180–220 lines**. Do NOT duplicate full webhook setup, telemetry handler recipes, or testing.md content.
  </action>
  <verify>wc -l guides/production-checklist.md | awk '{print $1 >= 180 && $1 <= 230}'</verify>
  <acceptance_criteria>
    - File `guides/production-checklist.md` exists
    - Contains anchor string `Production Stripe integrations fail at boundaries`
    - Contains `{:lattice_stripe, "~> 1.7"}`
    - Contains `Client.new!`, `Webhook.Plug`, `request_id`, and D-14 phrase `Your app starts work. Webhooks confirm reality.`
    - Contains D-05 guardrail substring `result record` and `PaymentIntent`
    - Contains markdown links to `client-configuration.md`, `webhooks.md`, `event-debugging.md`
    - Line count between 180 and 230 (`wc -l`)
    - Does NOT contain `Charge.capture` or `IO.inspect`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `wc -l guides/production-checklist.md` — within 180–230 lines
2. `grep -c "client-configuration.md" guides/production-checklist.md` — at least 1
3. Read file — tone matches webhooks-thin-events.md (assertive, low-magic)
</verification>

<success_criteria>
- OPS-01 guide file exists with all D-02 sections and D-05 Charge subsection
- Guide is self-contained for pre-launch scanning with cross-links for depth
- v1.7 install canary present for Phase 54 lockstep
</success_criteria>
