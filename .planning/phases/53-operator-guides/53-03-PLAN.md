---
phase: 53-operator-guides
plan: 03
type: execute
wave: 2
depends_on: [53-01, 53-02]
files_modified:
  - mix.exs
  - README.md
  - guides/user-flows-and-jtbd.md
  - guides/webhooks.md
  - guides/error-handling.md
  - guides/testing.md
  - guides/webhooks-thin-events.md
autonomous: true
requirements: [OPS-01, OPS-02]
must_haves:
  truths:
    - "Both operator guides appear in ExDoc Operations & DX group at hybrid positions."
    - "README hardening route and JTBD Job 7 surface both guides."
    - "Reverse cross-links from webhooks, error-handling, and testing guides exist."
  artifacts:
    - path: "mix.exs"
      provides: "ExDoc Operations & DX group with operator guides"
      contains: "guides/production-checklist.md"
    - path: "README.md"
      provides: "Discovery ladder wiring"
      contains: "production-checklist.md"
---

<objective>
Wire both operator guides into ExDoc, README, JTBD, and reverse cross-link graph (CONTEXT D-04).

Purpose: Guides invisible in discovery ladder fail OPS-01/OPS-02 adoption criteria. This plan connects the prose artifacts from Wave 1 into all public routing surfaces without changing README install block (`~> 1.3` stays until Phase 54).

Output: Updated `mix.exs`, `README.md`, `guides/user-flows-and-jtbd.md`, and reverse links in `webhooks.md`, `error-handling.md`, `testing.md`, optionally `webhooks-thin-events.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-operator-guides/53-CONTEXT.md
@.planning/phases/53-operator-guides/53-RESEARCH.md
@mix.exs
@README.md
@guides/user-flows-and-jtbd.md
@guides/webhooks.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Premature global 1.7 install flip | medium | Do NOT change README Installation block |
| Broken discovery graph | low | Exact path strings `guides/production-checklist.md` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Register guides in mix.exs ExDoc Operations &amp; DX group</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (lines 84–98 — current Operations & DX list)
    - .planning/phases/53-operator-guides/53-CONTEXT.md (D-04 ExDoc order)
  </read_first>
  <action>
    In `mix.exs`, within the `"Operations & DX"` group list:

    1. Add `"guides/production-checklist.md"` immediately after `"guides/client-configuration.md"`.
    2. Add `"guides/event-debugging.md"` immediately after `"guides/webhooks-thin-events.md"` and before `"guides/error-handling.md"`.

    Resulting order start:
    ```
    "guides/client-configuration.md",
    "guides/production-checklist.md",
    "guides/webhooks.md",
    "guides/webhooks-thin-events.md",
    "guides/event-debugging.md",
    "guides/error-handling.md",
    ...
    ```

    Also ensure both new paths appear in the top-level `extras:` list if other guides are listed there (match existing pattern for webhooks-thin-events).
  </action>
  <verify>grep -A20 'Operations & DX' mix.exs | grep production-checklist</verify>
  <acceptance_criteria>
    - `mix.exs` contains `"guides/production-checklist.md"` in Operations & DX group
    - `mix.exs` contains `"guides/event-debugging.md"` in Operations & DX group
    - `event-debugging.md` appears before `error-handling.md` in the group list
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire README discovery ladder</name>
  <files>README.md</files>
  <read_first>
    - README.md (Docs Ladder ~line 29, hardening route ~43–44, Operations section ~107, HexDocs index ~156)
    - .planning/phases/53-operator-guides/53-CONTEXT.md (D-04 README wiring)
  </read_first>
  <action>
    Update README.md:

    1. **Docs Ladder ops bullet (~line 29):** Add links to Production Checklist and Event Debugging alongside webhooks/testing/error-handling cluster.
    2. **Hardening route (~line 43–44):** Prepend `[Production Checklist](guides/production-checklist.md),` as first item. Append `[Event Debugging](guides/event-debugging.md)` after Thin Events link.
    3. **Operations and diagnostics section (~line 107–112):** Mention operator playbooks with links to both guides.
    4. **HexDocs Operations and DX index (~line 156–157):** Add hexdoc links for production-checklist, event-debugging, and webhooks-thin-events if missing from index.
    5. **Do NOT change** Installation block — must still show `~> 1.3`.
  </action>
  <verify>grep production-checklist README.md && grep event-debugging README.md && grep '~> 1.3' README.md</verify>
  <acceptance_criteria>
    - README contains `guides/production-checklist.md` at least twice (route + index or ladder)
    - README contains `guides/event-debugging.md` at least twice
    - README Installation still contains `~> 1.3`
    - README hardening route lists Production Checklist before Error Handling
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire JTBD routes and reverse cross-links</name>
  <files>guides/user-flows-and-jtbd.md, guides/webhooks.md, guides/error-handling.md, guides/testing.md, guides/webhooks-thin-events.md</files>
  <read_first>
    - guides/user-flows-and-jtbd.md (Runtime truth ~95–96, Job 7 Read next ~334–342)
    - guides/webhooks.md (Thin events closing section ~218–224)
    - guides/error-handling.md (See also ~332)
    - guides/testing.md (See also ~571)
  </read_first>
  <action>
    1. **JTBD Runtime truth route (~line 95–96):** Add `[Production Checklist](production-checklist.md)` and `[Event Debugging](event-debugging.md)` to the cluster alongside webhooks/error-handling/testing/thin-events.

    2. **Job 7 Read next (~line 334–342):** Add at top of list (before Testing):
       - `[Production Checklist](production-checklist.md)`
       - `[Event Debugging](event-debugging.md)`

    3. **webhooks.md:** In or after the "Thin events" closing section, add link to `[Event Debugging](event-debugging.md)` for production diagnosis (mirror thin-events cross-link pattern).

    4. **error-handling.md See also:** Add bullets linking `production-checklist.md` and `event-debugging.md`.

    5. **testing.md See also:** Add bullet linking `event-debugging.md`.

    6. **webhooks-thin-events.md (optional):** Add forward link to `event-debugging.md` in See also or closing paragraph if natural.
  </action>
  <verify>grep -l event-debugging guides/webhooks.md guides/error-handling.md guides/testing.md guides/user-flows-and-jtbd.md</verify>
  <acceptance_criteria>
    - `guides/user-flows-and-jtbd.md` contains `production-checklist.md` and `event-debugging.md`
    - Job 7 Read next section lists Production Checklist before Testing link
    - `guides/webhooks.md` contains `event-debugging.md`
    - `guides/error-handling.md` contains both `production-checklist.md` and `event-debugging.md`
    - `guides/testing.md` contains `event-debugging.md`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep production-checklist mix.exs README.md guides/user-flows-and-jtbd.md` — all match
2. `grep event-debugging mix.exs README.md guides/webhooks.md` — all match
3. Confirm README install line unchanged: `grep '~> 1.3' README.md`
</verification>

<success_criteria>
- ExDoc group membership for both guides
- Full forward + reverse discovery graph wired
- README install block still at 1.3
</success_criteria>
