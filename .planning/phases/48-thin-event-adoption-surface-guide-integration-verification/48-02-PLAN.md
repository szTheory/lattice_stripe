---
phase: 48-thin-event-adoption-surface-guide-integration-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/webhooks-thin-events.md
  - mix.exs
autonomous: true
requirements: [GUIDE-03]
must_haves:
  truths:
    - "Adopters opening `guides/webhooks-thin-events.md` find one canonical Phoenix controller spine teaching `parse_event_notification/4` → fetch-after-verify → idempotent dispatch (D-01)."
    - "The new guide is visible in HexDocs under `Operations & DX` next to `guides/webhooks.md` (D-03 sub-decision 3C — `mix.exs` placement)."
    - "Every load-bearing phrase the new guide teaches (function names, error atoms, rate-limit phrasing, idempotency anchor, Connect routing anchor, truth anchor, surface name, verification-vs-payload-shape phrasing) appears in the file so the Plan 04 docs-truth grep locks turn GREEN (D-03 3A)."
    - "The guide's install snippet says `{:lattice_stripe, \"~> 1.5\"}` — the v1.5 canary per D-03 sub-decision 3B."
  artifacts:
    - path: "guides/webhooks-thin-events.md"
      provides: "Canonical Phoenix thin-event adopter guide — ~140-180 lines"
      contains: "Webhooks confirm"
      min_lines: 130
    - path: "mix.exs"
      provides: "ExDoc `extras` + `Operations & DX` group includes `guides/webhooks-thin-events.md`"
      contains: "guides/webhooks-thin-events.md"
  key_links:
    - from: "mix.exs `extras` list"
      to: "guides/webhooks-thin-events.md"
      via: "string literal in the `extras:` keyword option"
      pattern: "\"guides/webhooks-thin-events.md\""
    - from: "mix.exs `groups_for_extras` → `Operations & DX`"
      to: "guides/webhooks-thin-events.md"
      via: "string literal in the `Operations & DX` group list"
      pattern: "\"guides/webhooks-thin-events.md\""
    - from: "guides/webhooks-thin-events.md"
      to: "guides/webhooks.md / guides/testing.md / guides/error-handling.md"
      via: "Markdown link refs in body prose and `See also`"
      pattern: "webhooks.md|testing.md|error-handling.md"
---

<objective>
Ship `guides/webhooks-thin-events.md` — the canonical Phoenix thin-event adopter guide that satisfies GUIDE-03 — and wire it into `mix.exs` ExDoc placement (D-03 sub-decision 3C) so HexDocs renders it under `Operations & DX` adjacent to `guides/webhooks.md`. The guide is the load-bearing artifact for Phase 48's "guide" half of the deliverable pair (the "verification" half is Plans 04 + 05).

Purpose: Adopters who want to consume Stripe `/v2/events` (thin events) in a Phoenix app need one canonical, opinionated, low-magic guide that teaches the diff vs snapshot webhooks: custom controller (not `Webhook.Plug`), verify via `parse_event_notification/4`, fetch-after-verify via `fetch_event/3` and `fetch_related_object/3`, idempotency keyed on `event.id`, rate-limit posture under Stripe's 100 req/s ceiling, and Connect routing via `event.context`. The guide is a *trust rail* in the `Operations & DX` ExDoc group — sibling to `webhooks.md`, NOT a flagship recipe playbook (D-01 explicitly rejects recipe scale).

Output:
- `guides/webhooks-thin-events.md` — new file, ~140-180 lines, mirror `guides/webhooks.md` section ordering adjusted for controller-owned dispatch.
- `mix.exs` — two insertions: `"guides/webhooks-thin-events.md"` added to the top-level `extras:` list (adjacent to `webhooks.md`) AND to the `Operations & DX` entry in `groups_for_extras` (adjacent to `webhooks.md`).

This plan does NOT add reverse links from `README.md` / `guides/user-flows-and-jtbd.md` / `guides/webhooks.md` — those are Plan 03's scope (D-04 discovery wiring). This plan does NOT add docs-truth grep locks — those are Plan 04's scope (D-03 3A + 3B + 3C grep extension + 3D). The grep tests will FAIL CI until Plans 03 + 04 land in Wave 2; that is expected and intended (Wave 0 RED-to-GREEN progression per VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md
@guides/webhooks.md
@mix.exs
@lib/lattice_stripe/webhook.ex
@lib/lattice_stripe/event_notification.ex
@lib/lattice_stripe/event_notification/related_object.ex
@lib/lattice_stripe/webhook/cache_body_reader.ex

<interfaces>
<!-- D-03 sub-decision 3A locks these substrings in the new guide; the Plan 04 docs-truth grep enforces them. The executor MUST ensure every substring below appears in the final file. -->

REQUIRED SUBSTRINGS in `guides/webhooks-thin-events.md` (Plan 04's grep test will assert each):

Function names (helper surface — verbatim, no backticks needed to satisfy `=~`):
- `parse_event_notification`
- `fetch_event`
- `fetch_related_object`

Verify-error atoms (the verification-vs-payload-shape failure boundary):
- `:no_matching_signature`
- `:timestamp_expired`
- `:no_related_object`
- `:unknown_object_type`

Rate-limit phrasing (REQUIREMENTS.md GUIDE-03 — BOTH required):
- `100 req/s`
- `90/s`

Anchors (idempotency + Connect routing + Phase 44 D-14 + canonical surface name):
- `event.id`
- `event.context`
- `Webhooks confirm`
- `/v2/events`

Verification-vs-payload-shape failure boundary phrasing (REQUIREMENTS.md GUIDE-03):
- `verification`
- `payload shape`

Cross-link Markdown refs (D-03 3D forward-link grep — Plan 04 asserts these):
- `webhooks.md`
- `testing.md`
- `error-handling.md`

Install snippet (D-03 sub-decision 3B canary — Plan 04 asserts this):
- `{:lattice_stripe, "~> 1.5"}`

<!-- Canonical adopter controller pattern — verbatim from RESEARCH.md §"Phoenix Controller Idiom" (corrects the CONTEXT.md `<specifics>` drift on `conn.assigns.raw_body` → `conn.private[:raw_body]`). -->

CONTROLLER PATTERN (one canonical spine per Phase 45 D-03):
```elixir
defmodule MyAppWeb.StripeThinEventController do
  use MyAppWeb, :controller

  alias LatticeStripe.{EventNotification, Webhook}
  alias LatticeStripe.EventNotification.RelatedObject

  @secret System.fetch_env!("STRIPE_THIN_EVENT_SECRET")

  def receive(conn, _params) do
    raw_body = conn.private[:raw_body] || ""
    sig_header = conn |> get_req_header("stripe-signature") |> List.first()

    with {:ok, %EventNotification{} = notif} <-
           Webhook.parse_event_notification(raw_body, sig_header, @secret),
         :ok <- dispatch(MyApp.Stripe.client(), notif) do
      send_resp(conn, 200, "")
    else
      {:error, :missing_header}        -> send_resp(conn, 400, "missing header")
      {:error, :invalid_header}        -> send_resp(conn, 400, "invalid header")
      {:error, :no_matching_signature} -> send_resp(conn, 400, "bad signature")
      {:error, :timestamp_expired}     -> send_resp(conn, 400, "stale")
    end
  end

  defp dispatch(client, %EventNotification{id: id} = notif) do
    case MyApp.Stripe.IdempotentEvents.claim(id) do
      :ok                -> dispatch_typed(client, notif)
      :already_processed -> :ok
    end
  end

  defp dispatch_typed(client, %EventNotification{
         related_object: %RelatedObject{type: "customer"}
       } = notif) do
    {:ok, %LatticeStripe.Customer{} = customer} =
      Webhook.fetch_related_object(client, notif)
    MyApp.Workers.SyncCustomer.enqueue(customer)
    :ok
  end

  defp dispatch_typed(client, %EventNotification{related_object: nil} = notif) do
    {:ok, %LatticeStripe.Event{} = event} = Webhook.fetch_event(client, notif)
    MyApp.Workers.LogSnapshotEvent.enqueue(event)
    :ok
  end

  defp dispatch_typed(_client, _notif), do: :ok
end
```

<!-- Idempotency Ecto sketch — verbatim from RESEARCH.md §"Idempotency Sketch Recommendation" -->

IDEMPOTENCY SKETCH (Ecto-style, library-scoped — adopter brings their own persistence):
- Migration: `create table(:processed_stripe_events, primary_key: false)` with `add :event_id, :string, primary_key: true`, `add :type, :string`, `add :processed_at, :utc_datetime_usec`
- Schema: `use Ecto.Schema` + `@primary_key {:event_id, :string, autogenerate: false}`
- Claim function: keyed on `%EventNotification{id: id, type: type}` → `Repo.insert/1` → pattern-match `{:ok, _}` → `:ok` OR `{:error, %{errors: [event_id: {_, [constraint: :unique, ...]}]}}` → `:already_processed`

<!-- mix.exs current shape (verified) -->

CURRENT `mix.exs` `extras` list (lines ~40-52):
```elixir
extras: [
  "guides/getting-started.md",
  # ...
  "guides/customer-portal.md",
  "guides/webhooks.md",           # <-- insert NEW after this line
  "guides/error-handling.md",
  "guides/testing.md",
  # ...
]
```

CURRENT `mix.exs` `groups_for_extras` → `Operations & DX` (lines ~81-94):
```elixir
{"Operations & DX",
 [
   "guides/client-configuration.md",
   "guides/webhooks.md",          # <-- insert NEW after this line
   "guides/error-handling.md",
   "guides/testing.md",
   "guides/performance.md",
   # ...
   "guides/cheatsheet.cheatmd"
 ]},
```

Both edits: insert `"guides/webhooks-thin-events.md",` immediately after `"guides/webhooks.md",` to preserve sidebar ordering (v2 sibling directly under v1 trust rail).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `guides/webhooks-thin-events.md` — the canonical Phoenix thin-event adopter guide</name>
  <files>guides/webhooks-thin-events.md</files>
  <read_first>
    - guides/webhooks.md (full file — 216 lines; the sibling analog to mirror for section ordering, opening anchor pattern, code-block conventions, "See also" footer — section ordering is the load-bearing template)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-01 guide shape + length + posture; D-04 cross-link list)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Phoenix Controller Idiom for Thin-Event Adoption" — corrected `conn.private[:raw_body]`; §"Idempotency Sketch Recommendation" — Ecto schema + claim function; §"Stripe `/v2/events` Public Contract Reference" — rate-limit + Connect context; §"Cross-Link Graph Locks" — forward link list)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"guides/webhooks-thin-events.md (NEW — guide, Operations & DX sibling)" — section ordering + opening anchor + Phoenix controller code-block pattern + local-testing pattern + See also pattern)
    - lib/lattice_stripe/webhook.ex (Phase 47 helper surface — confirm signatures of `parse_event_notification/4`, `fetch_event/3`, `fetch_related_object/3`; lines 224, 255, 323, 433)
    - lib/lattice_stripe/event_notification.ex (struct shape — confirm field set `:id, :object, :type, :created, :context, :livemode, :related_object, :reason, :extra`)
    - lib/lattice_stripe/event_notification/related_object.ex (sub-struct — confirm `:id, :type, :url` field set)
    - lib/lattice_stripe/webhook/cache_body_reader.ex (CRITICAL for the controller example — `conn.private[:raw_body]` is the truth surface, NOT `conn.assigns.raw_body`)
  </read_first>
  <action>
    Create `guides/webhooks-thin-events.md`, ~140-180 lines, following the locked structure below. Use the analog `guides/webhooks.md` for opening-anchor cadence, code-block fences, link-ref style, and "See also" footer. Write assertive, low-magic prose per Phase 45 D-25 ("Your app starts work. Webhooks confirm reality." is the tone reference). Stay shorter than `webhooks.md` (216 lines) because this guide cross-links to `webhooks.md` for the raw-body invariant rather than re-explaining (Phase 44 D-24 cross-link discipline).

    REQUIRED section ordering (mirrors `webhooks.md` H2 sequence, adjusted for controller-owned dispatch per D-01):

    1. **Title + opening anchor** (~10-15 lines) — Title: `# Webhooks: Thin Events`. Open with one paragraph orienting snapshot-vs-thin (thin events deliver `{id, type, related_object}` only; adopter fetches authoritative state after verify). Include the Phase 44 D-14 anchor by quoting the canonical assertion (the substring `Webhooks confirm` MUST appear in body prose — easiest: keep the same anchor used in `webhooks.md`: "**Your app starts work. Webhooks confirm reality.**"). Include the canonical surface-name substring `/v2/events`. Add the install snippet block containing exactly `{:lattice_stripe, "~> 1.5"}` (B2 canary — Plan 04 grep asserts this).

    2. **The verification-vs-payload-shape failure boundary** (~15-20 lines) — Explain the diff between verify errors (atoms returned BEFORE JSON decode) and payload-shape errors (raised AFTER verify when JSON is malformed; current Phase 47 contract is `Jason.DecodeError` raised — document this honestly per RESEARCH.md §"Verify-then-decode invariant"). The substrings `verification` AND `payload shape` MUST both appear in body prose. List the four verify-error atoms `:no_matching_signature`, `:timestamp_expired`, `:missing_header`, `:invalid_header` (the first two are grep-locked by Plan 04). Add the typed-error footguns from `fetch_related_object/3`: `:no_related_object` and `:unknown_object_type` (both grep-locked).

    3. **The Phoenix controller spine** (~30-40 lines) — One canonical controller (Phase 45 D-03 "one recommended path"). Use the exact module + controller body from the `<interfaces>` block above. Cross-link back to `webhooks.md` for the raw-body invariant (`Plug.Parsers` + `body_reader: {LatticeStripe.Webhook.CacheBodyReader, :read_body, []}`) rather than re-explaining — use a Markdown link to `webhooks.md` so the substring `webhooks.md` appears. CRITICAL: use `conn.private[:raw_body]`, NOT `conn.assigns.raw_body` (the CONTEXT.md `<specifics>` block has drift; the corrected version is in RESEARCH.md §"Phoenix Controller Idiom" and the `<interfaces>` block above).

    4. **Fetch-after-verify worked example** (~20-25 lines) — Walk through both branches: `Webhook.fetch_event/3` (returns `%Event{}` from `/v2/core/events/{id}`) when adopters need the full snapshot view, and `Webhook.fetch_related_object/3` (returns the typed underlying resource via `ObjectTypes` dispatch) when adopters need the resource directly. Surface the two typed-error footguns explicitly: `{:error, :no_related_object}` (snapshot-event branch — `related_object` is `nil`) and `{:error, {:unknown_object_type, type}}` (typed-gate fail-fast — Phase 47 D-05 contract). The substrings `fetch_event` and `fetch_related_object` MUST appear.

    5. **Idempotency keyed on `event.id`** (~20-25 lines) — Use the Ecto-style schema sketch from RESEARCH.md §"Idempotency Sketch Recommendation" (migration + schema + claim function — see `<interfaces>` block above for the bullet outline; the executor expands into fenced ```elixir blocks). Make the load-bearing point explicit: idempotency is keyed on `event.id`, NOT on fetched resource state (resource state can change between Stripe-send and adopter-fetch). The substring `event.id` MUST appear in body prose. Add the disclosure paragraph from RESEARCH.md noting adopters can swap Ecto for Redis/etcd; this is a sketch not a mandate. Library-scoped per Phase 45 D-04 + D-27.

    6. **Rate-limit posture** (~10-15 lines) — Teach the <90/s under Stripe's 100 req/s ceiling guidance from REQUIREMENTS.md GUIDE-03. Both substrings `100 req/s` AND `90/s` MUST appear in body prose (Plan 04 grep asserts both). Acknowledge fetch-after-verify roughly doubles call rate vs snapshot processing (RESEARCH.md §"Rate-limit ceiling"). One-line note that sandbox mode is 25 req/s (4× lower). Link to `https://docs.stripe.com/rate-limits` for the authoritative reference rather than enumerating per-endpoint stricter limits.

    7. **Connect / context-aware routing via `event.context`** (~10-15 lines) — Show pattern-matching on `notif.context` for Connect platforms; non-Connect adopters can ignore (`context` will be `nil`). Substring `event.context` MUST appear in body prose.

    8. **Testing** (~15-20 lines) — The stripe-mock `/v2/` gap one-liner from CONTEXT.md `<specifics>` lines 274-275: "Stripe's open-source `stripe-mock` server does not currently validate `/v2/` endpoints. Test your thin-event handler with `LatticeStripe.Testing.generate_thin_event_payload/3` plus `Mox` at the `LatticeStripe.Transport` behaviour boundary — see LatticeStripe's own `test/lattice_stripe/webhook/thin_event_test.exs` for the reference pattern." Mirror the `webhooks.md` "Local testing" code-block convention (per PATTERNS.md). Cross-link to `testing.md` (substring required for D-03 3D grep).

    9. **See also** (~5-10 lines) — Markdown bullet list linking to `[Webhooks](webhooks.md)`, `[Testing](testing.md)`, `[Error Handling](error-handling.md)`. The three substrings `webhooks.md`, `testing.md`, `error-handling.md` are forward-link grep locks (D-03 3D — Plan 04 asserts each).

    Code blocks: triple-backtick `elixir` fenced; module names follow `MyApp` / `MyAppWeb` Phoenix convention. No `IO.inspect(notification)` debug patterns (per Phase 47 REVIEW WR-05 — the `RelatedObject` Inspect impl currently shows `:extra` when non-empty; the guide should pattern-match typed fields instead).

    DO NOT include:
    - Worker-queue / Oban / dunning state machine framing (Phase 45 D-27 library-scoped discipline; Accrue territory)
    - A `LatticeStripe.Webhook.Handler` callback variant for thin events (Phase 47 D-08 deferred; the controller IS the dispatch surface in v1.5)
    - `IO.inspect(notification)` debug patterns (WR-05 caution)
    - A "v2" `Webhook.Plug` dispatch mode example (Phase 47 D-08 deferred)
    - Tax / Charge / Identity / Treasury surface examples (out of v1.5 scope per REQUIREMENTS.md)

    Target final length: 140-180 lines (D-01 calibration; `webhooks.md` is 216 lines).
  </action>
  <verify>
    <automated>test -f guides/webhooks-thin-events.md && wc -l guides/webhooks-thin-events.md && for s in "parse_event_notification" "fetch_event" "fetch_related_object" ":no_matching_signature" ":timestamp_expired" ":no_related_object" ":unknown_object_type" "100 req/s" "90/s" "event.id" "event.context" "Webhooks confirm" "/v2/events" "verification" "payload shape" "webhooks.md" "testing.md" "error-handling.md" '{:lattice_stripe, "~> 1.5"}'; do printf '%-40s ' "$s"; grep -c -F -- "$s" guides/webhooks-thin-events.md; done</automated>
  </verify>
  <acceptance_criteria>
    - `guides/webhooks-thin-events.md` exists and is between 130 and 200 lines (target 140-180 per D-01 calibration; soft floor of 130 to allow for tight, cross-linked writing; hard ceiling 200 to prevent recipe-scale drift per D-01)
    - All required substrings (per `<interfaces>` block above) appear at least once in the file. The full grep verification command above must show every count ≥ 1.
    - `conn.private[:raw_body]` appears in the controller example (NOT `conn.assigns.raw_body` — RESEARCH.md §"Phoenix Controller Idiom" drift correction)
    - The install snippet contains EXACTLY `{:lattice_stripe, "~> 1.5"}` (not `~> 1.3`, not `~> 1.4` — B2 canary)
    - Section ordering matches the 9-section structure above (title + anchor → verify-vs-shape boundary → controller spine → fetch-after-verify → idempotency → rate-limit → Connect context → testing → See also)
    - No `IO.inspect` calls in any code block (WR-05 caution)
    - `mix docs --warnings-as-errors` exits 0 (no broken ExDoc references)
    - The file does NOT mention `LatticeStripe.Webhook.Handler.handle_event_notification/1` or any v2 `Webhook.Plug` dispatch mode (Phase 47 D-08 deferred items)
    - `mix format --check-formatted guides/webhooks-thin-events.md` is not applicable (Markdown is not Elixir-formatted; this acceptance line is a non-criterion clarification)
  </acceptance_criteria>
  <done>
    - `guides/webhooks-thin-events.md` exists with all 19 required substrings present
    - Length 130-200 lines, 9 H2 sections in the locked order
    - Code blocks use `conn.private[:raw_body]` per RESEARCH.md correction
    - No worker-queue / dunning / IO.inspect patterns
    - `mix docs --warnings-as-errors` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `guides/webhooks-thin-events.md` into `mix.exs` ExDoc `extras` and `Operations & DX` group (D-03 sub-decision 3C)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (full file — 314 lines; relevant blocks are `extras:` at ~lines 40-52 and `groups_for_extras:` at ~lines 54-96; the executor MUST confirm exact line positions before editing because the file may have shifted since the snapshot in `<interfaces>` above)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-03 sub-decision 3C — extras + groups[Operations & DX] both required)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"ExDoc `groups_for_extras` Placement Strategy" — exact insertion points)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"mix.exs (MOD — add new guide to `extras` + `Operations & DX` group)" — symmetric webhooks.md template)
  </read_first>
  <action>
    Two surgical insertions in `mix.exs`. Use Edit, not Write — preserve every other line.

    **Insertion 1** — `extras:` list. Find the line containing exactly `"guides/webhooks.md",` (with the trailing comma) inside the `extras:` keyword-list value (NOT inside `groups_for_extras` — both contain this string; use the surrounding lines to disambiguate). Insert a new line directly after it: `"guides/webhooks-thin-events.md",` at the same indentation depth. This places the v2 sibling immediately under the v1 trust rail in the rendered ExDoc sidebar.

    **Insertion 2** — `groups_for_extras:` → `"Operations & DX"` group. Find the line containing `"guides/webhooks.md",` inside the tuple `{"Operations & DX", [...]}`. Insert a new line directly after it: `"guides/webhooks-thin-events.md",` at the same indentation depth.

    Do NOT modify any other group (do NOT add to `"Canonical Guides"`, `"Flagship Recipes"`, `"Start Here"`, or `"Changelog"` — per RESEARCH.md §"ExDoc Placement Strategy", the new guide is an Operations & DX trust rail extension, NOT a flagship recipe or canonical resource reference).

    Do NOT reorder any existing entries — append-after-`webhooks.md` is the only edit.

    Do NOT touch `package:` `extra_files` / `files` keyword (release-prep step, deferred per CONTEXT.md `<deferred>` items — the v1.5 install-line refresh across the rest of the docs is a separate task).
  </action>
  <verify>
    <automated>grep -c "guides/webhooks-thin-events.md" mix.exs && mix compile --warnings-as-errors 2>&1 | tail -3 && mix docs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "guides/webhooks-thin-events.md" mix.exs` returns 2 (one in `extras:`, one in `groups_for_extras` → `Operations & DX`)
    - The two insertions are EACH immediately after a `"guides/webhooks.md",` line (no entries between them)
    - `mix compile --warnings-as-errors` exits 0
    - `mix docs` exits 0 with no warnings about missing extras (the file must exist from Task 1 of this plan — they are in the same wave but Task 1 writes the file first; if Task 1 has not run, this acceptance fails as expected)
    - No other groups received an entry — `grep -n "webhooks-thin-events" mix.exs` shows exactly 2 lines, both indented within the `extras:` and `Operations & DX` regions
    - `git diff mix.exs` shows ONLY two insertions (no modifications, no deletions, no reorderings)
  </acceptance_criteria>
  <done>
    - `mix.exs` has 2 new lines, both `"guides/webhooks-thin-events.md",` at the appropriate indentation
    - Both insertions are adjacent-to-and-after the existing `webhooks.md` entries
    - `mix docs` clean (assuming Task 1 of this plan has landed the guide file)
    - `mix compile --warnings-as-errors` clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter reading guide → operational deployment | The guide teaches the pattern adopters deploy in production; unsafe patterns become security incidents |
| Maintainer modifying guide → docs drift | A future PR that silently drops load-bearing phrases breaks the docs-truth contract (Plan 04 catches this) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Information Disclosure | Adopter implementation taught by the guide | mitigate | Guide explicitly avoids `IO.inspect(notification)` debug patterns per Phase 47 REVIEW WR-05 (`RelatedObject` Inspect impl shows `:extra` when non-empty — could leak credential-shaped Stripe wire-payload extras to logs). The guide teaches pattern-matching against typed fields instead. |
| T-48-05 | Tampering | Adopter idempotency keyed on fetched resource state | mitigate | Guide explicitly anchors idempotency on `event.id` (NOT on fetched resource state) per REQUIREMENTS.md GUIDE-03. Resource state can change between Stripe-send and adopter-fetch; resource-state-keyed dedup would replay/double-process. The grep lock `event.id` (Plan 04) enforces the anchor remains visible. |
| T-48-06 | Denial of Service | Adopter exceeding Stripe's 100 req/s ceiling | mitigate | Guide explicitly teaches `<90/s` under the `100 req/s` live-mode ceiling and acknowledges fetch-after-verify roughly doubles call rate vs snapshot processing. Both substrings grep-locked by Plan 04. Sandbox-mode 25 req/s 4× lower limit also called out. |
| T-48-07 | Tampering | Adopter disables tolerance staleness check in production via `tolerance: 0` | mitigate | The Plug-side mitigation lives in Plan 01 (`@moduledoc` WR-04 closure). The guide-side mitigation in this plan: the verification-vs-payload-shape failure boundary section names `:timestamp_expired` as a verify-error atom and the guide does NOT teach `tolerance: 0` as a recommended setting. |
| T-48-SC | Tampering | npm/pip/cargo install supply-chain | accept | No new dependencies introduced in this plan (Phase 48 Hex-deps delta is zero — only existing Finch/Jason/Mox/Plug surfaces are touched). RESEARCH.md `## Package Legitimacy Audit` not required because this plan is docs+config only. |
</threat_model>

<verification>
- `test -f guides/webhooks-thin-events.md` succeeds
- `wc -l guides/webhooks-thin-events.md` reports 130-200 lines
- All 19 required substrings present (verify command in Task 1 `<verify>` block)
- `mix compile --warnings-as-errors` exits 0
- `mix docs --warnings-as-errors` exits 0
- `grep -c "guides/webhooks-thin-events.md" mix.exs` returns exactly 2
- `git diff mix.exs` shows ONLY two single-line insertions
- Full test suite still green: `mix test --warnings-as-errors` exits 0 (note: Plan 04's grep tests are NOT YET in place; this plan's tests pass independently — the failing-RED state lives in Plan 04 until Wave 2 lands)
</verification>

<success_criteria>
- GUIDE-03 (guide content) advanced: `guides/webhooks-thin-events.md` exists with the canonical Phoenix thin-event adopter pattern, ~140-180 lines, 9 H2 sections in the locked order, all 19 D-03 3A + 3B + 3D-forward substrings present
- D-03 sub-decision 3C satisfied: `mix.exs` `extras:` AND `groups_for_extras` → `Operations & DX` both include `guides/webhooks-thin-events.md` adjacent to `webhooks.md`
- D-01 posture satisfied: guide is sibling-not-recipe (no worker-queue/dunning framing; one canonical controller spine; library-scoped Phoenix examples)
- D-01 length calibration satisfied: 130-200 lines
- Canonical adopter pattern uses `conn.private[:raw_body]` per RESEARCH.md drift correction
- B2 canary install line: `{:lattice_stripe, "~> 1.5"}` (the only doc in the repo claiming `~> 1.5` until release prep)
- Phase 47 D-08 honored: NO `Webhook.Handler.handle_event_notification/1` callback shown; the controller IS the dispatch surface
- WR-05 caution honored: NO `IO.inspect` patterns in code blocks
</success_criteria>

<output>
Create `.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-02-SUMMARY.md` when done. Include:
- Final line count of `guides/webhooks-thin-events.md` and a list of the 9 H2 section headings in order
- `mix.exs` line ranges for the two insertions (e.g., `mix.exs:45` extras + `mix.exs:85` Operations & DX)
- Confirmation that all 19 required substrings are present (paste the grep verification output if helpful)
- Confirmation `mix docs --warnings-as-errors` is clean
- Note for Plan 04: grep tests against this guide will turn GREEN once Plan 04 lands its 3A/3B/3C/3D test blocks; until then the grep-test surface is RED-by-design per Wave 0 progression in VALIDATION.md
</output>
