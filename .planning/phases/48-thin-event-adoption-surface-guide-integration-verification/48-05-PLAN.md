---
phase: 48-thin-event-adoption-surface-guide-integration-verification
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - test/lattice_stripe/webhook/thin_event_test.exs
autonomous: true
requirements: [VERIFY-03]
must_haves:
  truths:
    - "An adopter calling `Testing.generate_thin_event_payload/3` then `Webhook.parse_event_notification/4` gets back a typed `%EventNotification{}` (verify happy path)."
    - "A chained `Testing.generate_thin_event_payload/3` then `parse_event_notification/4` then `fetch_event/3` flow hits the verbatim `/v2/core/events/{id}` path and decodes to `%Event{}` (fetch-after-verify Event branch)."
    - "A chained `Testing.generate_thin_event_payload/3` then `parse_event_notification/4` then `fetch_related_object/3` flow hits the verbatim `related_object.url` and decodes to the typed underlying resource via `ObjectTypes` dispatch (fetch-after-verify RelatedObject branch)."
    - "Malformed-payload boundary: wrong signature gives `{:error, :no_matching_signature}`; missing header gives `{:error, :missing_header}`; invalid header gives `{:error, :invalid_header}`; bad JSON post-verify raises `Jason.DecodeError` (Phase 47 contract — honest documentation, WR-02 stays deferred)."
    - "`tolerance: 0` reconciled semantics on the thin-event surface: stale `:timestamp` with `tolerance: 0` returns `{:ok, _}`; with default tolerance returns `{:error, :timestamp_expired}` (extends Phase 47 WEBFIX-01 regression net to `parse_event_notification/4`)."
  artifacts:
    - path: "test/lattice_stripe/webhook/thin_event_test.exs"
      provides: "5 describe blocks, ~12-15 tests covering chained generate-parse-fetch flows + malformed-payload boundary + tolerance: 0 reconciliation at the thin-event surface"
      contains: "describe \"verify happy path"
      min_lines: 150
  key_links:
    - from: "test/lattice_stripe/webhook/thin_event_test.exs"
      to: "LatticeStripe.Testing.generate_thin_event_payload/3 + LatticeStripe.Webhook.parse_event_notification/4 + Webhook.fetch_event/3 + Webhook.fetch_related_object/3"
      via: "chained generate-parse-fetch flow with Mox at LatticeStripe.Transport boundary"
      pattern: "Testing.generate_thin_event_payload.*Webhook.parse_event_notification.*Webhook.fetch_(event|related_object)"
    - from: "test/lattice_stripe/webhook/thin_event_test.exs"
      to: "LatticeStripe.MockTransport"
      via: "expect/3 + verify_on_exit! + zero-HTTP fail-fast pattern"
      pattern: "expect.LatticeStripe.MockTransport"
---

<objective>
Ship `test/lattice_stripe/webhook/thin_event_test.exs` — the chained-roundtrip Mox-at-Transport integration suite that proves Phase 47's thin-event helper surface behaves end-to-end at the helper boundary under happy-path, malformed-payload, and `tolerance: 0` boundary conditions. This is the load-bearing artifact for VERIFY-03 ("Integration test coverage for thin-event verification happy path, fetch-after-verify roundtrip, malformed-payload failure boundary, and `tolerance: 0` reconciliation").

Purpose: Phase 47 shipped unit-level coverage at `test/lattice_stripe/webhook/fetch_test.exs` (378 lines) and the load-bearing roundtrip test in `test/lattice_stripe/testing_test.exs`. What is missing is **chained** coverage: `Testing.generate_thin_event_payload/3` then `parse_event_notification/4` then `fetch_event/3` OR `fetch_related_object/3` all in one test body, asserting the helpers compose correctly end-to-end. The new file extends the `fetch_test.exs` idiom with these chained flows.

Output: One new file `test/lattice_stripe/webhook/thin_event_test.exs` with:
- `use ExUnit.Case, async: true` (Mox at Transport boundary, no shared state)
- `import Mox` + `import LatticeStripe.TestHelpers` + fixture imports
- `setup :verify_on_exit!` (enforces zero-HTTP on fail-fast paths)
- 5 `describe` blocks (one per VERIFY-03 must-have category)
- ~12-15 tests total
- ~150-220 lines

This plan does NOT introduce new fixtures (reuse `event_notification_map/0`, `event_notification_map_no_related_object/0` from `test/support/fixtures/event_notification.ex`, `customer_json/1` from `test/support/fixtures/customer.ex`, `event_map/1` from `test/support/fixtures/event.ex`). It does NOT touch `lib/` code. It does NOT use `@tag :integration` (the in-repo semantics of `:integration` means real-HTTP stripe-mock precondition; this file is Mox-at-Transport per CONTEXT.md D-02). It does NOT introduce Bypass (explicitly excluded per CLAUDE.md "What NOT to Use" + D-02 rationale).

**Wave 1, independent:** This plan has no dependency on Plans 02, 03, or 04 because it tests the Phase 47-shipped helper surface. It can be run in parallel with Plan 01 (which only touches plug.ex `@moduledoc` + docs-truth grep — different file space) and Plan 02 (which writes guide markdown + mix.exs — different file space).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-CONTEXT.md
@test/lattice_stripe/webhook/fetch_test.exs
@test/lattice_stripe/webhook_test.exs
@test/lattice_stripe/testing_test.exs
@test/support/fixtures/event_notification.ex
@test/support/fixtures/customer.ex
@test/support/fixtures/event.ex
@test/support/test_helpers.ex
@lib/lattice_stripe/webhook.ex
@lib/lattice_stripe/testing.ex
@lib/lattice_stripe/event_notification.ex
@lib/lattice_stripe/event_notification/related_object.ex
@lib/lattice_stripe/event.ex
@lib/lattice_stripe/customer.ex

<interfaces>
Direct template — verbatim outline from RESEARCH.md §"Test Idiom Template Survey". The executor MUST mirror this idiom line-for-line.

Module header + imports (mirror `fetch_test.exs:1-13` with the additions noted):

```elixir
defmodule LatticeStripe.Webhook.ThinEventTest do
  use ExUnit.Case, async: true

  import Mox
  import LatticeStripe.TestHelpers
  import LatticeStripe.Test.Fixtures.EventNotification, only: [
    event_notification_map: 0,
    event_notification_map: 1,
    event_notification_map_no_related_object: 0
  ]
  import LatticeStripe.Test.Fixtures.Customer, only: [customer_json: 1]
  import LatticeStripe.Test.Fixtures.Event, only: [event_map: 1]

  alias LatticeStripe.{Customer, Event, EventNotification, Testing, Webhook}
  alias LatticeStripe.EventNotification.RelatedObject

  setup :verify_on_exit!

  @secret "whsec_test_thinevent"
```

Helper surface available (Phase 47-shipped — DO NOT redefine):
- `Testing.generate_thin_event_payload(type, related_object_map_or_nil, opts)` — returns `{payload, sig_header}`. Opts: `:secret`, `:timestamp` (Unix seconds, drives `created` ISO 8601).
- `Webhook.parse_event_notification(payload, sig_header, secret, opts \\ [])` — returns `{:ok, %EventNotification{}}` or `{:error, atom}` from the set `:missing_header | :invalid_header | :no_matching_signature | :timestamp_expired`. Opts: `:tolerance` (default 300).
- `Webhook.fetch_event(client, notification_or_id, opts \\ [])` — GET `/v2/core/events/{id}`; returns `{:ok, %Event{}}` or `{:error, :no_event_id}` (no HTTP).
- `Webhook.fetch_related_object(client, notification, opts \\ [])` — GET against `related_object.url` verbatim, typed-gate via `ObjectTypes.fetch_module/1`; returns `{:ok, typed_resource}` or `{:error, :no_related_object}` (no HTTP) or `{:error, {:unknown_object_type, type}}` (no HTTP, fail-fast per Phase 47 D-05).
- `Webhook.generate_test_signature(payload, secret, opts \\ [])` — used to forge a malformed-payload sig in the bad-JSON test below.

Mox + helpers (from `test/support/test_helpers.ex`):
- `test_client/0` — returns `%LatticeStripe.Client{}` with `transport: LatticeStripe.MockTransport`, `max_retries: 0` (critical for async tests).
- `ok_response/1` — returns `{:ok, %{status: 200, headers: [...], body: Jason.encode!(body)}}` for Mox `expect` return value.
- `LatticeStripe.MockTransport` — Mox-defined behaviour mock; use `expect(LatticeStripe.MockTransport, :request, fn req -> ... end)`.

Fixtures available:
- `event_notification_map/0` — full thin-event map with `related_object: %{"id" => "acct_1", "type" => "v2.core.account", "url" => "/v2/core/accounts/acct_1"}`.
- `event_notification_map/1` — same as above with overrides via map merge.
- `event_notification_map_no_related_object/0` — snapshot-style v2 event with `related_object: nil`.
- `customer_json/1` — Customer fixture map for the RelatedObject Customer-branch test.
- `event_map/1` — Event fixture map for the fetch_event branch test.

5 describe blocks per VERIFY-03 must-have. The executor mirrors fetch_test.exs idiom verbatim.

DESCRIBE BLOCK 1 — `"verify happy path (Testing → parse)"` — 1 test:
- `"generate_thin_event_payload + parse_event_notification returns typed EventNotification"` — call `Testing.generate_thin_event_payload("v2.core.account.updated", %{"id" => "acct_1", "type" => "v2.core.account", "url" => "/v2/core/accounts/acct_1"}, secret: @secret)` then `Webhook.parse_event_notification(payload, sig_header, @secret)` then assert `{:ok, %EventNotification{} = notif}` with `notif.type == "v2.core.account.updated"`, `notif.related_object == %RelatedObject{id: "acct_1", type: "v2.core.account", ...}`. NO `expect/3` call (zero HTTP — parse is pure).

DESCRIBE BLOCK 2 — `"fetch-after-verify roundtrip — Event branch (parse → fetch_event)"` — 1 test:
- `"chained generate → parse → fetch_event/3 returns typed %Event{}"` — generate signed payload (with `related_object: nil` for snapshot-style v2 event). Parse then bind `notif`. `client = test_client()`. `expect(LatticeStripe.MockTransport, :request, fn req -> assert req.method == :get; assert String.contains?(req.url, "/v2/core/events/#{notif.id}"); refute String.contains?(req.url, "/v1/events/"); ok_response(event_map(%{"id" => notif.id})) end)`. Call `Webhook.fetch_event(client, notif)` then assert `{:ok, %Event{id: ^id}}`. The `refute /v1/events/` is load-bearing (RESEARCH Finding 3 in Phase 47).

DESCRIBE BLOCK 3 — `"fetch-after-verify roundtrip — RelatedObject branch (parse → fetch_related_object)"` — 1 test:
- `"chained generate → parse → fetch_related_object/3 returns typed %Customer{}"` — generate signed payload with `related_object: %{"id" => "cus_1", "type" => "customer", "url" => "/v1/customers/cus_1"}`. Parse then bind `notif`. `expect(LatticeStripe.MockTransport, :request, fn req -> assert req.method == :get; assert String.contains?(req.url, "/v1/customers/cus_1"); ok_response(customer_json(%{"id" => "cus_1"})) end)`. Call `Webhook.fetch_related_object(client, notif)` then assert `{:ok, %Customer{id: "cus_1"}}`.

DESCRIBE BLOCK 4 — `"malformed-payload failure boundary"` — 4 tests:
1. `"wrong secret returns {:error, :no_matching_signature}"` — generate with `@secret`, parse with `"whsec_wrong"`. NO `expect/3`. Assert `{:error, :no_matching_signature}`.
2. `"missing header returns {:error, :missing_header}"` — call `Webhook.parse_event_notification("{}", nil, @secret)`. NO `expect/3`. Assert `{:error, :missing_header}`.
3. `"malformed header returns {:error, :invalid_header}"` — call with `"not-a-real-header"`. NO `expect/3`. Assert `{:error, :invalid_header}`.
4. `"bad JSON post-verify currently raises Jason.DecodeError (Phase 47 contract)"` — `bad_payload = "not-json-at-all"`; `sig_header = Webhook.generate_test_signature(bad_payload, @secret)`. NO `expect/3`. `assert_raise Jason.DecodeError, fn -> Webhook.parse_event_notification(bad_payload, sig_header, @secret) end`. Block comment explains this documents the Phase 47 contract honestly (WR-02 stays deferred to v1.5.x / v1.6 with its own discuss-phase) — if WR-02 is later resolved, this test gets rewritten in lockstep.

DESCRIBE BLOCK 5 — `"tolerance: 0 reconciled semantics on the thin-event surface"` — 2 tests:
1. `"stale timestamp + tolerance: 0 returns {:ok, notif} (WEBFIX-01 extends to thin events)"` — `old_ts = System.system_time(:second) - 86_400`. Generate with `timestamp: old_ts`. Parse with `tolerance: 0`. NO `expect/3`. Assert `{:ok, %EventNotification{}}`. Block comment notes this extends Phase 47 D-03 / WEBFIX-01 to `parse_event_notification/4`.
2. `"stale timestamp + default tolerance still returns {:error, :timestamp_expired}"` — same `old_ts`. Generate with `timestamp: old_ts`. Parse without `:tolerance` opt (default 300). NO `expect/3`. Assert `{:error, :timestamp_expired}`.

Zero-HTTP fail-fast pattern (verified against fetch_test.exs:50-57, 199-219):
`setup :verify_on_exit!` enforces that ANY test which OMITS an `expect/3` call MUST also issue zero requests to `LatticeStripe.MockTransport`. If a test code path accidentally calls into `Client.request/2`, Mox raises at exit. This is the load-bearing primitive for fail-fast assertions — all 4 malformed-payload tests AND both tolerance tests OMIT `expect/3` because no HTTP should ever fire on those paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `test/lattice_stripe/webhook/thin_event_test.exs` — 5 describe blocks, ~12-15 tests covering VERIFY-03 chained flows + boundaries</name>
  <files>test/lattice_stripe/webhook/thin_event_test.exs</files>
  <read_first>
    - test/lattice_stripe/webhook/fetch_test.exs (full file — 378 lines; the direct idiom template; mirror module header + imports + setup + describe-per-helper pattern + Mox `expect/3` URL-assertion pattern + zero-HTTP fail-fast pattern at lines 50-57, 199-219)
    - test/support/test_helpers.ex (full file — 63 lines; confirm `test_client/0`, `ok_response/1` signatures + `max_retries: 0` setting)
    - test/support/fixtures/event_notification.ex (confirm `event_notification_map/0`, `event_notification_map/1`, `event_notification_map_no_related_object/0`)
    - test/support/fixtures/customer.ex (confirm `customer_json/1` signature)
    - test/support/fixtures/event.ex (confirm `event_map/1` signature)
    - lib/lattice_stripe/testing.ex (Phase 47 lines 246-332: `generate_thin_event_payload/3` signature, opts list, return value `{payload, sig_header}`)
    - lib/lattice_stripe/webhook.ex (Phase 47 lines 224-280: `parse_event_notification/4` + bang variant; lines 323-368: `fetch_event/3`; lines 433-497: `fetch_related_object/3`; line 647: `check_tolerance(_, 0) :: :ok`)
    - lib/lattice_stripe/event_notification.ex (struct shape — `id, object, type, created, context, livemode, related_object, reason, extra`)
    - lib/lattice_stripe/event_notification/related_object.ex (sub-struct — `id, type, url`)
    - lib/lattice_stripe/event.ex (struct shape — includes `:related_object` after Phase 47 D-02)
    - lib/lattice_stripe/customer.ex (confirm `%Customer{}` struct + `from_map/1`)
    - test/lattice_stripe/webhook_test.exs (lines 297-411 — `describe "parse_event_notification/4"` blocks; reference idiom for the verify-side of the chained roundtrip)
    - test/lattice_stripe/testing_test.exs (lines 119-225 — `describe "generate_thin_event_payload/3"` block + the existing end-to-end roundtrip; reference for the chained generate-then-parse case)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-02 — Mox at Transport boundary; `async: true`; NO `@tag :integration`)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Test Idiom Template Survey" — the verbatim test outline shown in the `<interfaces>` block above)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"test/lattice_stripe/webhook/thin_event_test.exs" — module header, Mox expect, zero-HTTP fail-fast patterns)
  </read_first>
  <action>
    Create `test/lattice_stripe/webhook/thin_event_test.exs` with the following structure (mirror `fetch_test.exs` idiom line-for-line):

    Module header + imports — copy from the `<interfaces>` block above. Module name `LatticeStripe.Webhook.ThinEventTest`. `use ExUnit.Case, async: true`. Aliases include `Testing` in addition to the `fetch_test.exs` set. Module attribute `@secret "whsec_test_thinevent"`.

    `setup :verify_on_exit!` — at the top of the module after imports/aliases, before the first `describe`.

    Five `describe` blocks, in this exact order (one per VERIFY-03 must-have):
    1. `describe "verify happy path (Testing → parse)" do ... end` — 1 test.
    2. `describe "fetch-after-verify roundtrip — Event branch (parse → fetch_event)" do ... end` — 1 test.
    3. `describe "fetch-after-verify roundtrip — RelatedObject branch (parse → fetch_related_object)" do ... end` — 1 test.
    4. `describe "malformed-payload failure boundary" do ... end` — 4 tests (wrong secret; missing header; invalid header; bad JSON post-verify).
    5. `describe "tolerance: 0 reconciled semantics on the thin-event surface" do ... end` — 2 tests (tolerance: 0 returns ok; default tolerance returns timestamp_expired).

    Test bodies — implement per the spec in the `<interfaces>` block above. For each test:
    - Use `Testing.generate_thin_event_payload/3` for the payload-build phase (NOT hand-rolled JSON).
    - Use `Webhook.parse_event_notification/4` for the verify phase.
    - For chained tests, bind the result with `assert {:ok, %EventNotification{} = notif} = ...`.
    - For Mox-stubbed branches (Event branch test + RelatedObject branch test ONLY), use `expect(LatticeStripe.MockTransport, :request, fn req -> ... end)` with explicit `assert req.method == :get`, `assert String.contains?(req.url, ...)` shape per `fetch_test.exs:20-34` template, returning `ok_response(...)`.
    - For zero-HTTP tests (all 4 malformed-payload tests + both tolerance tests), OMIT `expect/3`. The `setup :verify_on_exit!` enforces zero HTTP traffic — that IS the fail-fast contract proof.
    - For the Event-branch test, include the `refute String.contains?(req.url, "/v1/events/")` guard (RESEARCH Finding 3 in Phase 47 — `/v2/core/events/{id}` is the runtime path; `/v1/events/` substring appears only in docstrings).
    - For the bad-JSON-post-verify test, use `Webhook.generate_test_signature(bad_payload, @secret)` to manually forge a valid signature over the bad payload, then `assert_raise Jason.DecodeError, fn -> ... end`. Add a 2-3 line block comment explaining this documents the Phase 47 contract honestly and that WR-02 is deferred.
    - For the `tolerance: 0` test, use `old_ts = System.system_time(:second) - 86_400` (24 hours in the past — well outside the 300-second default tolerance), pass `timestamp: old_ts` to `generate_thin_event_payload/3`, and `tolerance: 0` to `parse_event_notification/4`. Add a block comment referencing Phase 47 D-03 / WEBFIX-01 and that this test extends the regression net to the `parse_event_notification/4` surface specifically.

    DO NOT:
    - Tag any test or describe with `@tag :integration` (Phase 48 D-02 forbids this; `:integration` means stripe-mock real-HTTP precondition in this repo).
    - Add `@moduletag :skip` (Phase 48 D-02 explicitly rejects the `meter_event_stream_integration_test.exs` pattern as the in-repo anti-pattern).
    - Introduce Bypass or any HTTP-server-based test infrastructure (CLAUDE.md "What NOT to Use" + D-02).
    - Add new fixtures (reuse existing `event_notification_map/0`, `event_notification_map_no_related_object/0`, `customer_json/1`, `event_map/1`).
    - Use `IO.inspect`, `IO.puts`, `dbg/0`, or any debug calls in test bodies (clean idiomatic style; matches `fetch_test.exs`).
    - Edit any other file (no `lib/` edits, no fixture edits, no `mix.exs` edits — Phase 48 surfaces stay scope-disciplined per CONTEXT.md `<deferred>`).

    Target final length: 150-220 lines (RESEARCH.md §"Wave 0 Requirements" estimates ~12-15 tests; `fetch_test.exs` is 378 lines with 21 tests, so per-test density predicts ~150-220 lines for 12 tests + chained-roundtrip extension overhead).
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/webhook/thin_event_test.exs --warnings-as-errors 2>&amp;1 | tail -20 &amp;&amp; wc -l test/lattice_stripe/webhook/thin_event_test.exs &amp;&amp; grep -c "  describe " test/lattice_stripe/webhook/thin_event_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/lattice_stripe/webhook/thin_event_test.exs` exists, between 150 and 250 lines (target 150-220; soft ceiling 250 to allow for descriptive block comments + the 4 malformed-payload tests which each add a few lines of zero-HTTP comment)
    - `mix test test/lattice_stripe/webhook/thin_event_test.exs --warnings-as-errors` exits 0 with EXACTLY 9 tests passing (DB1=1, DB2=1, DB3=1, DB4=4, DB5=2 → 1+1+1+4+2 = 9 tests total)
    - `grep -c "  describe " test/lattice_stripe/webhook/thin_event_test.exs` returns exactly 5
    - Each describe name matches the canonical strings in `<interfaces>` (verify happy path (Testing → parse); fetch-after-verify roundtrip — Event branch (parse → fetch_event); fetch-after-verify roundtrip — RelatedObject branch (parse → fetch_related_object); malformed-payload failure boundary; tolerance: 0 reconciled semantics on the thin-event surface)
    - The file uses `use ExUnit.Case, async: true` at the top (NOT `async: false`)
    - The file does NOT contain `@tag :integration` or `@moduletag :skip` (`grep -c "@tag :integration\\|@moduletag :skip" test/lattice_stripe/webhook/thin_event_test.exs` returns 0)
    - The file imports `Mox` AND `LatticeStripe.TestHelpers` (both `import` lines present)
    - The file contains `setup :verify_on_exit!`
    - The Event-branch test contains BOTH the `assert String.contains?(req.url, "/v2/core/events/` AND the `refute String.contains?(req.url, "/v1/events/")` guards
    - The bad-JSON-post-verify test uses `assert_raise Jason.DecodeError`
    - The `tolerance: 0` test passes both `timestamp: old_ts` to generate AND `tolerance: 0` to parse, asserting `{:ok, %EventNotification{}}`
    - The default-tolerance-stale-timestamp test asserts `{:error, :timestamp_expired}`
    - `mix compile --warnings-as-errors` exits 0
    - `mix credo --strict` exits 0 on the new file
    - Full suite still green: `mix test --warnings-as-errors` exits 0 with 9 new tests added to the pre-existing 177 (or 178 if Plan 01 has landed by Wave 1 race ordering)
    - No `IO.inspect`, `IO.puts`, `dbg/0` calls (`grep -c "IO\\.inspect\\|IO\\.puts\\|dbg(" test/lattice_stripe/webhook/thin_event_test.exs` returns 0)
    - No new fixtures introduced (`git status test/support/` shows no modifications)
    - No edits to any `lib/` file or `mix.exs` (`git status lib/ mix.exs` shows no modifications)
  </acceptance_criteria>
  <done>
    - `test/lattice_stripe/webhook/thin_event_test.exs` exists with 5 describe blocks + 9 tests
    - All tests pass with `async: true`
    - Mox `verify_on_exit!` enforces zero-HTTP on the 6 fail-fast tests
    - Module is idiomatically clean (matches `fetch_test.exs` style); no Credo violations
    - Full suite still green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite assertion to helper surface | Phase 47 helpers are the only behavior under test; the suite proves the chained composition is honest |
| Mox `verify_on_exit!` to helper internal HTTP | Fail-fast paths must NOT issue HTTP; `verify_on_exit!` catches accidental call-through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-17 | Tampering | Future helper-signature change breaks the chained roundtrip silently | mitigate | The chained generate-parse-fetch tests bind the parsed `notif` and pass it directly into the fetch; any helper signature change that breaks the data flow surfaces as a test failure in this file |
| T-48-18 | Tampering | Future `parse_event_notification/4` change silently drops one of the 4 verify error atoms | mitigate | 4 dedicated tests in DB4 assert each specific atom; dropping or renaming one breaks the matching test immediately |
| T-48-19 | Tampering | Future `check_tolerance/2` change silently re-introduces the Phase 47 WEBFIX-01 bug at the thin-event surface | mitigate | DB5's 2 tests pin both branches: `tolerance: 0` with stale timestamp returns `:ok` AND default tolerance with same stale timestamp returns `:timestamp_expired`. Either branch breaking fails CI |
| T-48-20 | Tampering | Future `fetch_event/3` change accidentally hits `/v1/events/` instead of `/v2/core/events/{id}` | mitigate | DB2 test contains both `assert String.contains?(req.url, "/v2/core/events/")` AND `refute String.contains?(req.url, "/v1/events/")` — a regression that flips the URL fails CI |
| T-48-21 | Information Disclosure | Accidental HTTP traffic from a fail-fast code path leaks request data to a flaky Mock | mitigate | `setup :verify_on_exit!` enforces zero traffic on the 6 fail-fast tests; if the helper code regresses to call out unexpectedly, the test exit raises |
</threat_model>

<verification>
- `test -f test/lattice_stripe/webhook/thin_event_test.exs` succeeds
- `mix test test/lattice_stripe/webhook/thin_event_test.exs --warnings-as-errors` exits 0 with 9 tests passing
- `grep -c "  describe " test/lattice_stripe/webhook/thin_event_test.exs` returns exactly 5
- `wc -l test/lattice_stripe/webhook/thin_event_test.exs` reports 150-250 lines
- `mix compile --warnings-as-errors` exits 0
- `mix credo --strict --files-included test/lattice_stripe/webhook/thin_event_test.exs` exits 0
- Full test suite: `mix test --warnings-as-errors` exits 0; total test count = 177 (Phase 47 baseline) + 9 (this plan) + any tests added by Plans 01/04 = expected 186 at the end of the phase
- `grep -c "@tag :integration" test/lattice_stripe/webhook/thin_event_test.exs` returns 0
- `grep -c "Bypass" test/lattice_stripe/webhook/thin_event_test.exs` returns 0 (Bypass excluded)
- `git status lib/ mix.exs test/support/` shows no modifications
</verification>

<success_criteria>
- VERIFY-03 satisfied at the integration-test surface:
  - Verify happy path: DB1 covers `generate_thin_event_payload/3` then `parse_event_notification/4` chained flow
  - Fetch-after-verify roundtrip — Event branch: DB2 chains all the way to `%Event{}` via `/v2/core/events/{id}` with Mox
  - Fetch-after-verify roundtrip — RelatedObject branch: DB3 chains to `%Customer{}` via the verbatim `related_object.url` with Mox
  - Malformed-payload failure boundary: DB4 covers the 4 verify error atoms + the bad-JSON post-verify Phase 47 contract
  - `tolerance: 0` reconciled semantics: DB5 covers BOTH branches (tolerance: 0 returns ok; default tolerance returns timestamp_expired) extending Phase 47 WEBFIX-01 regression net to `parse_event_notification/4`
- D-02 satisfied: Mox at `LatticeStripe.Transport` boundary, `async: true`, no `@tag :integration`, no Bypass
- File lives under `test/lattice_stripe/webhook*` namespace per REQUIREMENTS.md VERIFY-03
- Test file is mechanically derived from `fetch_test.exs` idiom — same imports, same setup, same Mox patterns; the only load-bearing addition is the chained generate-parse-fetch composition
- ROADMAP Phase 48 Success Criterion #4 satisfied: "Integration tests under `test/lattice_stripe/webhook*` cover thin-event verification happy path, fetch-after-verify roundtrip (returning typed resources via `ObjectTypes` dispatch), malformed-payload rejection, and `tolerance: 0` reconciled semantics — all green in CI"
</success_criteria>

<output>
Create `.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-05-SUMMARY.md` when done. Include:
- Final line count of `test/lattice_stripe/webhook/thin_event_test.exs`
- Final test count (expected: 9 tests across 5 describe blocks)
- List of the 5 describe block names verbatim
- Confirmation full suite is green (test count delta from Phase 47 baseline)
- Confirmation `mix credo --strict` is clean on the new file
- Confirmation no `lib/` or fixture or `mix.exs` modifications occurred
</output>
