---
phase: 48-thin-event-adoption-surface-guide-integration-verification
plan: 03
type: execute
wave: 2
depends_on: ["48-02"]
files_modified:
  - README.md
  - guides/webhooks.md
  - guides/user-flows-and-jtbd.md
autonomous: true
requirements: [GUIDE-03]
must_haves:
  truths:
    - "A reader landing on README.md can find `guides/webhooks-thin-events.md` from the `\"I am hardening ops and support paths\"` route (D-04)."
    - "A reader on `guides/webhooks.md` finds a `Thin events (/v2/events)` closing section forward-linking to the new guide (D-04 + D-03 3D reverse-link lock)."
    - "A reader on `guides/user-flows-and-jtbd.md` finds the new guide from the `Runtime truth, support, and debugging` Start Here route AND from the Job 7 `Read next` block (D-04)."
    - "The README `What's already in the box` Webhooks bullet now mentions thin-event helpers and `/v2/events` (D-04)."
  artifacts:
    - path: "README.md"
      provides: "Hardening-ops route extended with webhooks-thin-events.md link; Webhooks bullet expanded with thin-event + /v2/events mention"
      contains: "webhooks-thin-events.md"
    - path: "guides/webhooks.md"
      provides: "New closing `## Thin events (/v2/events)` section with forward link"
      contains: "webhooks-thin-events.md"
    - path: "guides/user-flows-and-jtbd.md"
      provides: "Start Here `Runtime truth, support, and debugging` route + Job 7 `Read next` block both link to webhooks-thin-events.md"
      contains: "webhooks-thin-events.md"
  key_links:
    - from: "README.md `I am hardening ops and support paths`"
      to: "guides/webhooks-thin-events.md"
      via: "Markdown link in bullet body"
      pattern: "webhooks-thin-events.md"
    - from: "guides/webhooks.md `## Thin events (/v2/events)` closing section"
      to: "guides/webhooks-thin-events.md"
      via: "Markdown link AND prose substring `thin event`"
      pattern: "thin event.*webhooks-thin-events.md|webhooks-thin-events.md.*thin event"
    - from: "guides/user-flows-and-jtbd.md Start Here `Runtime truth, support, and debugging` route"
      to: "guides/webhooks-thin-events.md"
      via: "Markdown link in list"
      pattern: "webhooks-thin-events.md"
    - from: "guides/user-flows-and-jtbd.md Job 7 `Read next` block"
      to: "guides/webhooks-thin-events.md"
      via: "Markdown link in bullet list"
      pattern: "webhooks-thin-events.md"
---

<objective>
Wire `guides/webhooks-thin-events.md` (created in Plan 02) into the project's discovery ladder per D-04: extend the README hardening-ops route + Webhooks-bullet, add a closing `Thin events (/v2/events)` section to `guides/webhooks.md` (the reverse-link sibling), and extend `guides/user-flows-and-jtbd.md` Start Here Runtime route + Job 7 Read next. Phase 44 D-06 elevated webhooks/testing/error-handling/etc. as first-class trust rails; this plan adds the thin-event guide as a v2 branch of the webhook trust rail (NOT as a new peer-rank surface — Phase 44 D-08 scope discipline preserved, no new JTBD Job 8 per CONTEXT.md `<deferred>`).

Purpose: An evaluator who arrives via README, JTBD, or `guides/webhooks.md` must be able to reach the new thin-event guide via the same discovery ladder the v1.4 flagship recipes use (ROADMAP Phase 48 Success Criterion #3). Without these wires, the guide is invisible to anyone who doesn't already know its name.

Output:
- `README.md` — 2 edits: hardening-ops route line gains `webhooks-thin-events.md` link; `What's already in the box` Webhooks bullet expands to mention thin-event helpers + `/v2/events`. NO changes to the v1.3 Release status block, NO changes to the `~> 1.3` install line (both deferred per CONTEXT.md).
- `guides/webhooks.md` — 1 edit: append a new `## Thin events (/v2/events)` closing section (~6 lines) AFTER the existing `## See also` section.
- `guides/user-flows-and-jtbd.md` — 2 edits: Start Here `Runtime truth, support, and debugging` route gains the new guide link; Job 7 `Read next` block gains the new guide link.

This plan does NOT add docs-truth grep locks — those live in Plan 04 (D-03 3D). It does NOT modify CHANGELOG.md (Plan 06). It does NOT touch any of the new guide's content (Plan 02 owns that).

**Wave 2 dependency:** This plan depends on Plan 02 because the `guides/webhooks.md` reverse-link section in PATTERNS.md links to `webhooks-thin-events.md` — that target must exist for `mix docs --warnings-as-errors` to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md
@README.md
@guides/webhooks.md
@guides/user-flows-and-jtbd.md

<interfaces>
<!-- D-04 edit targets (verified against source line ranges from RESEARCH.md §"Cross-Link Graph Locks"). -->

README.md edits:

**Edit 1 — `Choose Your Route` → `I am hardening ops and support paths`** (~lines 31-42).
Current line (approximate; executor MUST confirm via Read):
```markdown
- **I am hardening ops and support paths**:
  [Error Handling](guides/error-handling.md), [Testing](guides/testing.md), [Webhooks](guides/webhooks.md)
```
Edit: append `, [Webhooks: Thin Events](guides/webhooks-thin-events.md)` to the list. The link text "Webhooks: Thin Events" matches the new guide's H1 title and the JTBD link-text convention.

**Edit 2 — `What's already in the box` → Webhooks bullet** (~line 126).
Current line (verified):
```markdown
- Phoenix-ready `Webhook.Plug` with raw-body capture and signature verification
```
Suggested replacement (planner discretion — must surface `thin-event` AND `/v2/events`):
```markdown
- Phoenix-ready `Webhook.Plug` snapshot path + thin-event (`/v2/events`) helpers for fetch-after-verify integration
```

**Edit prohibitions:**
- DO NOT touch the v1.3 Release status block (~lines 8-11) — release prep, deferred per CONTEXT.md `<deferred>`
- DO NOT flip the `~> 1.3` install line — canary architecture per D-03 sub-decision 3B (only `webhooks-thin-events.md` claims `~> 1.5` until release prep)
- DO NOT touch the Docs Ladder section — no load-bearing readability gap identified

guides/webhooks.md edit:

**Edit — append closing `## Thin events (/v2/events)` section AFTER the existing `## See also` section** (after line ~216).
Suggested content (per CONTEXT.md `<specifics>` lines 277-286 + PATTERNS.md reverse-link section shape):
```markdown
## Thin events (`/v2/events`)

Stripe also delivers **thin events** to `/v2/event-destinations` endpoints.
A thin event payload carries only `{id, type, related_object}` — your app
fetches authoritative state after verification. See
[Webhooks: Thin Events](webhooks-thin-events.md) for the canonical Phoenix
pattern, fetch-after-verify idempotency, and rate-limit guidance.
```
**Critical phrasing requirements (D-03 3D reverse-link grep — Plan 04 asserts both):**
- Substring `thin event` (lowercase, in body prose — NOT just in the link text `Thin Events` with capital T/E, because `=~` is case-sensitive). The opening sentence "Stripe also delivers **thin events**" provides the lowercase substring via "thin events" (note: `=~ "thin event"` matches `"thin events"` because it's a substring match — verified).
- Substring `webhooks-thin-events.md` (the Markdown link target).
- Placement AFTER `## See also` so the trust rail's first impression (snapshot quickstart) stays stable.

guides/user-flows-and-jtbd.md edits:

**Edit 1 — Start Here `Runtime truth, support, and debugging` route** (~lines 93-94).
Current line (per RESEARCH.md §"Reverse links TO ..."):
```markdown
- **Runtime truth, support, and debugging**:
  [Webhooks](webhooks.md), [Error Handling](error-handling.md), [Testing](testing.md)
```
Edit: append `, [Webhooks: Thin Events](webhooks-thin-events.md)` to the list.

**Edit 2 — Job 7 `Read next` block** (~lines 331-338, "Sleep at night after shipping billing").
Current bullet list (per PATTERNS.md):
```markdown
Read next:

- [Testing](testing.md)
- [Telemetry](telemetry.md)
- [OpenTelemetry](opentelemetry.md)
- [Circuit Breaker](circuit-breaker.md)
- [Performance](performance.md)
- [Webhooks](webhooks.md)
```
Edit: append a new bullet `- [Webhooks: Thin Events](webhooks-thin-events.md)` at the END of the list (per PATTERNS.md: planner discretion on placement — recommended LAST to preserve the Phase 47 v1.4 ordering).

**Edit prohibition:**
- DO NOT add a new Job 8 — Phase 44 D-08 + CONTEXT.md `<deferred>` explicitly reject elevating thin events to peer rank with Jobs 1-7. Thin events are an evolution of Job 7 (Sleep at night) and the hardening-ops route, not a new job.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `README.md` — hardening-ops route + Webhooks bullet (D-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — 163 lines; confirm exact line position for both edit targets BEFORE editing because line numbers may have shifted since the snapshot in `<interfaces>`)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-04 — exact discovery wiring targets)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Cross-Link Graph Locks" + §"What NOT to touch")
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"README.md (MOD — hardening-ops route + Webhooks bullet expansion)" — link-text convention, edit prohibitions)
  </read_first>
  <action>
    Two surgical edits in `README.md` using Edit (NOT Write).

    **Edit 1 — `Choose Your Route` → `I am hardening ops and support paths` line.** Find the line that lists `[Error Handling](guides/error-handling.md), [Testing](guides/testing.md), [Webhooks](guides/webhooks.md)` (it's the second-line continuation of the hardening-ops bullet at approximately README.md:41-42). Append `, [Webhooks: Thin Events](guides/webhooks-thin-events.md)` to the end of that line. Use the canonical link text `Webhooks: Thin Events` (title-case, matches the new guide's H1 — see PATTERNS.md "link-text convention").

    **Edit 2 — `What's already in the box` → Webhooks bullet.** Find the line at approximately README.md:126 with exact current text `- Phoenix-ready \`Webhook.Plug\` with raw-body capture and signature verification`. Replace the entire line with the suggested expansion from `<interfaces>` block above:
    ```
    - Phoenix-ready `Webhook.Plug` snapshot path + thin-event (`/v2/events`) helpers for fetch-after-verify integration
    ```
    Planner discretion on exact wording per CONTEXT.md D-04 — but the bullet MUST surface both substrings `thin-event` (with hyphen, lowercase) AND `/v2/events` (with leading slash) so future docs-truth assertions can grep these (Plan 04 only asserts `webhooks-thin-events.md` in README, but the box-bullet expansion is the canonical surface description for adopters reading the README front-matter — semantic correctness over grep-only proof).

    **DO NOT EDIT:**
    - Release status block (~lines 8-11) — stays at v1.3 (CONTEXT.md `<deferred>` items)
    - Install line (`~> 1.3` at ~line 51) — canary architecture per D-03 sub-decision 3B
    - Any other route bullets (e.g., "I am wiring billing", "I am evaluating LatticeStripe") — only the hardening-ops route receives the link
    - Docs Ladder section — not load-bearing for D-04 per RESEARCH.md §"What NOT to touch"
  </action>
  <verify>
    <automated>grep -c "webhooks-thin-events.md" README.md && grep -c "thin-event" README.md && grep -c "/v2/events" README.md && grep -c '~> 1.3' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "webhooks-thin-events.md" README.md` returns at least 1 (currently 0)
    - `grep -c "thin-event" README.md` returns at least 1 (the Webhooks bullet must surface this substring with hyphen)
    - `grep -c "/v2/events" README.md` returns at least 1 (the Webhooks bullet must surface this canonical surface name)
    - `grep -c '~> 1.3' README.md` returns at least 1 (the install line is UNCHANGED — canary architecture preservation; the existing docs-truth test at `docs_truth_test.exs:54` continues to assert this)
    - The hardening-ops route line still mentions `error-handling.md`, `testing.md`, `webhooks.md` AND now `webhooks-thin-events.md` (4 links on that route — the existing 3 preserved + 1 new)
    - The v1.3 Release status block is byte-identical to its prior content (`git diff` shows no edits in the lines around the Release status header)
    - No reorderings of unrelated bullets — `git diff README.md` shows exactly the 2 intended changes
  </acceptance_criteria>
  <done>
    - README hardening-ops route lists the new guide
    - README Webhooks bullet mentions `thin-event` + `/v2/events`
    - Release status, install line, and unrelated routes untouched
  </done>
</task>

<task type="auto">
  <name>Task 2: Append closing `## Thin events (/v2/events)` section to `guides/webhooks.md` (D-04 + D-03 3D reverse link)</name>
  <files>guides/webhooks.md</files>
  <read_first>
    - guides/webhooks.md (full file — 216 lines; confirm the existing `## See also` section position since the new section must be placed AFTER it; also confirm the file does not already contain `webhooks-thin-events.md` references)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-04 — closing section specification + placement)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"guides/webhooks.md (MOD — add closing `Thin events (/v2/events)` section)" — exact section shape + grep lock requirements)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Lock-vs-asset confusion to avoid" — lowercase `thin event` in body prose, NOT only in title-case link text)
  </read_first>
  <action>
    Append the new closing section to `guides/webhooks.md`, AFTER the existing `## See also` section (which appears at approximately `webhooks.md:206-216` per RESEARCH.md and PATTERNS.md). Use the Edit tool to find the last line of the file (or the end of the `## See also` block) and insert the new section after.

    **EXACT content to append** (~6 lines, mirrors CONTEXT.md `<specifics>` reverse-link section shape verbatim):
    ```markdown

    ## Thin events (`/v2/events`)

    Stripe also delivers **thin events** to `/v2/event-destinations` endpoints.
    A thin event payload carries only `{id, type, related_object}` — your app
    fetches authoritative state after verification. See
    [Webhooks: Thin Events](webhooks-thin-events.md) for the canonical Phoenix
    pattern, fetch-after-verify idempotency, and rate-limit guidance.
    ```

    **CRITICAL phrasing requirements** (D-03 3D reverse-link grep — Plan 04 asserts both substrings):
    - Body prose MUST contain lowercase `thin event` (as a substring; the phrase `thin events` satisfies this because substring matching). The opening sentence "Stripe also delivers **thin events**" provides this. RESEARCH.md §"Lock-vs-asset confusion to avoid" warns: do NOT have `thin event` appear ONLY via the title-case link text `[Webhooks: Thin Events]` because `=~` is case-sensitive.
    - The Markdown link target MUST be `webhooks-thin-events.md` (relative path; same directory).
    - Placement AFTER `## See also` so the trust rail's first impression (snapshot quickstart) stays stable per CONTEXT.md `<specifics>`.

    **DO NOT touch any existing section** — only append. `git diff guides/webhooks.md` should show ONLY additions after the current EOF (or after the `## See also` block).
  </action>
  <verify>
    <automated>grep -c "webhooks-thin-events.md" guides/webhooks.md && grep -c "thin event" guides/webhooks.md && grep -n "^## " guides/webhooks.md | tail -3 && mix docs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "webhooks-thin-events.md" guides/webhooks.md` returns at least 1 (currently 0)
    - `grep -c "thin event" guides/webhooks.md` returns at least 1 in body prose (case-sensitive; the phrase `thin events` from the opening sentence satisfies the substring match)
    - The output of `grep -n "^## " guides/webhooks.md | tail -3` ends with `## Thin events (\`/v2/events\`)` (the new section is the LAST H2 in the file)
    - `mix docs --warnings-as-errors` exits 0 with no broken cross-reference warning (the `webhooks-thin-events.md` link target exists because this plan depends on Plan 02 which created the target)
    - The existing `## See also` section is unchanged (verify by `git diff guides/webhooks.md` showing additions only after the See also block)
    - File line count delta: +7 lines (1 blank line + 6 content lines per the appended section)
  </acceptance_criteria>
  <done>
    - `guides/webhooks.md` ends with a new `## Thin events (/v2/events)` section
    - Section contains both the lowercase `thin event` substring (body prose) and `webhooks-thin-events.md` link
    - All prior sections unchanged
    - `mix docs --warnings-as-errors` clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend `guides/user-flows-and-jtbd.md` — Start Here route + Job 7 Read next (D-04)</name>
  <files>guides/user-flows-and-jtbd.md</files>
  <read_first>
    - guides/user-flows-and-jtbd.md (full file — 451 lines; confirm exact line positions for both edit targets: Start Here `Runtime truth, support, and debugging` route at ~lines 93-94 and Job 7 `Read next` block at ~lines 331-338; line numbers may have shifted since the snapshot in `<interfaces>`)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-04 — Start Here route + Job 7 Read next specification)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"guides/user-flows-and-jtbd.md (MOD — Start Here route + Job 7 Read next)" — bullet placement conventions)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Reverse links TO `guides/webhooks-thin-events.md`" — table row for `user-flows-and-jtbd.md`)
  </read_first>
  <action>
    Two surgical edits in `guides/user-flows-and-jtbd.md` using Edit (NOT Write). Phase 44 D-08 scope discipline is mandatory: NO new Job 8 — thin events are an evolution of Job 7 and the hardening-ops route per CONTEXT.md `<deferred>`.

    **Edit 1 — Start Here `Runtime truth, support, and debugging` route** (~lines 93-94). Find the line that currently lists `[Webhooks](webhooks.md), [Error Handling](error-handling.md), [Testing](testing.md)` (under the bold heading `**Runtime truth, support, and debugging**:`). Append `, [Webhooks: Thin Events](webhooks-thin-events.md)` to the end of that list. Title-case link text matches the README convention (Task 1 of this plan).

    **Edit 2 — Job 7 `Read next` block** (~lines 331-338, "Sleep at night after shipping billing"). Find the `Read next:` bullet list with 6 existing bullets (`Testing`, `Telemetry`, `OpenTelemetry`, `Circuit Breaker`, `Performance`, `Webhooks`). Append a new bullet at the END of the list:
    ```markdown
    - [Webhooks: Thin Events](webhooks-thin-events.md)
    ```
    Placement: LAST per PATTERNS.md "planner discretion on placement order; recommended last to preserve the Phase 47 v1.4 ordering."

    **DO NOT EDIT:**
    - Any other Start Here route (e.g., `Evaluator path`, `Just integrating Stripe`) — only the Runtime truth route receives the link
    - Any other Job's Read next block (Jobs 1-6 untouched)
    - Job 7 body content, intro paragraph, "Done when" criteria — only the Read next bullet list is extended by one bullet
    - The JTBD intro / "Start Here" section header / any TOC entries

    **DO NOT ADD a Job 8** — Phase 44 D-08 + CONTEXT.md `<deferred>` reject elevating thin events to peer rank with Jobs 1-7.
  </action>
  <verify>
    <automated>grep -c "webhooks-thin-events.md" guides/user-flows-and-jtbd.md && grep -B 2 -A 1 "Runtime truth, support" guides/user-flows-and-jtbd.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "webhooks-thin-events.md" guides/user-flows-and-jtbd.md` returns exactly 2 (one in Start Here Runtime route, one in Job 7 Read next)
    - The Start Here Runtime route line now contains all 4 link refs: `webhooks.md`, `error-handling.md`, `testing.md`, `webhooks-thin-events.md`
    - The Job 7 Read next block now contains 7 bullets (was 6); the new bullet is the LAST entry per PATTERNS.md ordering recommendation
    - No new H2 / H3 / H4 section was added — no `## Job 8` heading exists (`grep -c "^## Job 8" guides/user-flows-and-jtbd.md` returns 0)
    - Other Start Here routes are byte-identical to their prior content (`git diff` shows only edits in the Runtime truth route + Job 7 Read next regions)
    - `mix docs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>
    - Start Here Runtime route includes `webhooks-thin-events.md`
    - Job 7 Read next list now has 7 bullets ending with `[Webhooks: Thin Events](webhooks-thin-events.md)`
    - No Job 8; other Jobs and other Start Here routes unchanged
    - `mix docs --warnings-as-errors` clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evaluator reading README → discovery ladder | New guide must be reachable via established trust rails; invisibility is a documentation failure |
| Maintainer modifying discovery surfaces → Plan 04 grep | Drift in any of the 4 link locations must fail Plan 04 cross-link grep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-08 | Information Disclosure | New guide remains invisible to evaluators | mitigate | Three discovery surfaces (README hardening-ops route + box-bullet + `guides/webhooks.md` closing section + JTBD Start Here route + JTBD Job 7 Read next) — 4 distinct entry points into the guide; Plan 04 grep-locks all 4 against drift |
| T-48-09 | Tampering | Scope creep: introducing a Job 8 or peer-rank thin-event surface | mitigate | Plan explicitly forbids new Job 8; Phase 44 D-08 + CONTEXT.md `<deferred>` cite the contract; acceptance criteria asserts `grep -c "^## Job 8" guides/user-flows-and-jtbd.md` returns 0 |
| T-48-10 | Tampering | Release-status block or install line accidentally flipped | mitigate | Plan explicitly forbids touching the v1.3 Release status block AND the `~> 1.3` install line; acceptance criteria asserts `grep -c '~> 1.3' README.md` returns at least 1 (canary architecture per D-03 sub-decision 3B preserved) |
</threat_model>

<verification>
- `grep -c "webhooks-thin-events.md" README.md` returns at least 1
- `grep -c "webhooks-thin-events.md" guides/webhooks.md` returns at least 1
- `grep -c "webhooks-thin-events.md" guides/user-flows-and-jtbd.md` returns exactly 2 (Start Here + Job 7 Read next)
- `grep -c "thin event" guides/webhooks.md` returns at least 1 (lowercase in body prose)
- `grep -c '~> 1.3' README.md` returns at least 1 (canary preserved)
- `grep -c "^## Job 8" guides/user-flows-and-jtbd.md` returns 0 (no peer Job 8)
- `mix docs --warnings-as-errors` exits 0 (all link targets resolve)
- `mix test --warnings-as-errors` exits 0 (full suite still green; Plan 04's grep locks are not yet in place but no existing tests should break)
</verification>

<success_criteria>
- D-04 satisfied: README + webhooks.md + JTBD all link to the new guide
- Reverse-link substrings present for Plan 04 grep (`thin event` lowercase in webhooks.md body prose; `webhooks-thin-events.md` Markdown link target in all 3 files)
- No peer Job 8 added (Phase 44 D-08 + CONTEXT.md `<deferred>` honored)
- v1.3 Release status block and install line untouched (D-03 sub-decision 3B canary preserved)
- ROADMAP Phase 48 Success Criterion #3 advanced: "The guide is wired into the ExDoc layered grouping (Canonical Guides) and the JTBD discovery ladder so evaluators land on it from README → JTBD → canonical guide path the same way the v1.4 flagship recipes are surfaced." (Note: the ExDoc grouping half is owned by Plan 02; the JTBD discovery ladder half is owned by this plan.)
</success_criteria>

<output>
Create `.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-03-SUMMARY.md` when done. Include:
- README.md line numbers of the two edits (hardening-ops route + Webhooks bullet)
- guides/webhooks.md line range of the appended section (e.g., `webhooks.md:218-224`)
- guides/user-flows-and-jtbd.md line numbers of the two edits (Start Here Runtime route + Job 7 Read next bullet)
- Confirmation that `grep -c "webhooks-thin-events.md"` returns the expected count in each file
- Confirmation that the v1.3 Release status block, README install line, and JTBD jobs 1-6 are unchanged
- Confirmation `mix docs --warnings-as-errors` is clean
</output>
