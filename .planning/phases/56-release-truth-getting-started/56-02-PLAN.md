---
phase: 56-release-truth-getting-started
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - guides/getting-started.md
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
must_haves:
  truths:
    - "First-run adopters reading guides/getting-started.md see 1.7.x as the current published Hex line — aligned with install pin and README."
    - "No stale 1.3.x published-surface claim remains in getting-started."
    - "No git-dependency-from-main steering remains in getting-started onboarding path."
    - "All docs_truth tests pass including new release-status prose lock."
  artifacts:
    - path: "guides/getting-started.md"
      provides: "HexDocs main-page truthful release-status prose"
      contains: "Release status"
    - path: "guides/getting-started.md"
      provides: "no stale 1.3.x claim"
      min_lines: 18
  key_links:
    - from: "README.md"
      to: "guides/getting-started.md"
      via: "one-liner blockquote tone match"
      pattern: "current published line on Hex"
    - from: "guides/getting-started.md"
      to: "test/lattice_stripe/docs_truth_test.exs"
      via: "prose lock test passes"
      pattern: "release-status prose matches current Hex surface"
---

<objective>
Replace stale release-status prose in `guides/getting-started.md` with a minimal README-style blockquote one-liner, removing the obsolete git-dependency paragraph — making TRUTH-01 true and turning Plan 01's docs_truth locks green.

Purpose: HexDocs main page is the first surface adopters see after install. Prose must match the already-correct `~> 1.7` install pin and README release block.
Output: Updated Installation section in `guides/getting-started.md` with truthful release-status blockquote; full docs_truth suite green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-release-truth-getting-started/56-CONTEXT.md
@.planning/phases/56-release-truth-getting-started/56-RESEARCH.md
@.planning/phases/56-release-truth-getting-started/56-PATTERNS.md
@.planning/phases/56-release-truth-getting-started/56-VALIDATION.md
@guides/getting-started.md
@README.md
@test/lattice_stripe/docs_truth_test.exs
@mix.exs

<interfaces>
Current bug (lines 20–21 — DELETE entirely):
```markdown
The `1.3.x` line is the current published Hex surface. If you specifically need
unreleased work from `main`, use a git dependency instead of the published release.
```

Target replacement (after install code block, before "Then fetch your dependencies:"):
```markdown
> **Release status:** **`1.7.x`** ships as the current published line on Hex (capstone release **1.7.0**).
```

README canonical tone (line 8):
```markdown
> **Release status:** **`1.7.x`** ships as the current published line on Hex (capstone release **1.7.0**).
```

Install snippet stays unchanged: `{:lattice_stripe, "~> 1.7"}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale release prose with README-style blockquote one-liner</name>
  <files>guides/getting-started.md</files>
  <read_first>
    guides/getting-started.md
    README.md
    mix.exs
    test/lattice_stripe/docs_truth_test.exs
    .planning/phases/56-release-truth-getting-started/56-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `guides/getting-started.md` does NOT contain `1.3.x` anywhere.
    - `guides/getting-started.md` does NOT contain `unreleased work from \`main\``.
    - `guides/getting-started.md` contains `> **Release status:**` blockquote immediately after the install code block closing ``` and before "Then fetch your dependencies:".
    - Blockquote contains `1.7.x` and `current published line on Hex` and `capstone release **1.7.0**`.
    - Install code block still contains `{:lattice_stripe, "~> 1.7"}` and `{:finch, "~> 0.21"}` unchanged.
    - `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <action>
In `guides/getting-started.md` Installation section:

1. Delete lines 20–21 entirely (the stale 1.3.x paragraph AND the git dependency sentence — both removed per D-11).

2. Insert after the install code block (after the closing ``` of the elixir deps block):

```markdown
> **Release status:** **`1.7.x`** ships as the current published line on Hex (capstone release **1.7.0**).

```

3. Ensure "Then fetch your dependencies:" follows the blockquote with no intervening stale prose.

4. Do NOT add README milestone bullets (1.4–1.7 list) — one-liner only per D-04.

5. Do NOT change any other section of the guide.
  </action>
  <verify>
    <automated>! rg -n '1\.3\.x|unreleased work from' guides/getting-started.md && rg -n 'Release status.*1\.7\.x.*current published line on Hex' guides/getting-started.md && mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors</automated>
  </verify>
  <done>getting-started Installation section tells truthful 1.7.x release story; all docs_truth tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → getting-started prose | One-liner must match README tone without duplicating milestone archaeology |
| getting-started → adopter trust | First HexDocs page must not contradict install pin or README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03 | T | release-status prose | mitigate | Use exact README one-liner shape; docs_truth positive/refute locks in Plan 01 |
| T-56-04 | I | git-dep removal | accept | Intentional — post-REL-04 Hex capstone makes git-dep onboarding obsolete (D-12) |
</threat_model>

<verification>
- `! rg -n '1\.3\.x|unreleased work from' guides/getting-started.md`
- `rg -n 'Release status' guides/getting-started.md`
- `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- TRUTH-01: Adopter sees 1.7.x as current published surface in getting-started.
- TRUTH-02: docs_truth prose lock passes.
- Install pin unchanged at `~> 1.7`.
</success_criteria>

<output>
After completion, create `.planning/phases/56-release-truth-getting-started/56-02-SUMMARY.md`
</output>
