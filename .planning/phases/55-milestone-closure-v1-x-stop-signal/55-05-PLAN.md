---
phase: 55-milestone-closure-v1-x-stop-signal
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .release-please-manifest.json
  - release-please-config.json
autonomous: false
gap_closure: true
requirements: [CLOSE-01, CLOSE-02]
must_haves:
  truths:
    - "Package lattice_stripe 1.7.0 is published on Hex.pm."
    - "mix hex.info lattice_stripe shows 1.7.0 in recent releases."
  artifacts:
    - path: ".release-please-manifest.json"
      provides: "manifest sync"
      contains: '"1.7.0"'
  key_links:
    - from: "Hex.pm"
      to: "mix.exs"
      via: "version 1.7.0"
      pattern: "1.7.0"
---

<objective>
Close the REL-04 verification gap blocking Phase 55 milestone close-ready posture.

Purpose: VERIFICATION.md gap #1 — `mix hex.info lattice_stripe` shows 1.1.0, not 1.7.0. Phase 54-04 stopped at maintainer checkpoint; this gap-closure plan completes the publish gate so 55-03 can finish.

Output: Hex package live at 1.7.0; git tag `v1.7.0` on remote; manifest synced.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md
@.planning/phases/54-release-truth-capstone/54-04-PLAN.md
@.planning/phases/54-release-truth-capstone/54-04-SUMMARY.md
@.planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Publish without green CI | high | Run preflight checklist before publish |
| Wrong tarball version | high | `mix hex.publish --dry-run` + `mix hex.info` verification |
| Marking REL-04 done without Hex publish | high | Task 3 verification gate before 55-06 executes |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: REL-04 current-state probe</name>
  <files></files>
  <read_first>
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md (gaps section)
    - .planning/phases/54-release-truth-capstone/54-04-SUMMARY.md (preflight results)
    - mix.exs (@version line)
  </read_first>
  <action>
    Run current-state probes and record in SUMMARY:

    ```bash
    mix hex.info lattice_stripe 2>/dev/null | head -20
    git tag -l 'v1.7.0'
    grep '@version' mix.exs
    cat .release-please-manifest.json
    ```

    **If `mix hex.info` already shows 1.7.0:** skip Task 2 publish steps; proceed to Task 3 verification only.

    **If not published:** proceed to Task 2.
  </action>
  <verify>mix hex.info lattice_stripe 2>/dev/null | head -5</verify>
  <acceptance_criteria>
    - SUMMARY records hex.info output (version line visible)
    - SUMMARY records whether 1.7.0 already published or publish still needed
    - `grep '@version' mix.exs` shows `1.7.0`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Preflight before publish</name>
  <files></files>
  <read_first>
    - .planning/phases/54-release-truth-capstone/54-04-PLAN.md (Task 1 preflight)
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-04 checklist)
  </read_first>
  <action>
    Skip if Task 1 found 1.7.0 already on Hex.

    Run preflight commands; stop on first failure:

    ```bash
    mix compile --warnings-as-errors
    mix test --warnings-as-errors
    mix docs
    mix hex.build
    mix hex.publish --dry-run
    ```

    Record pass/fail per command in SUMMARY. If `--warnings-as-errors` fails on pre-existing warnings unrelated to release scope, document the specific modules and confirm 54-04-SUMMARY precedent (2077 tests pass without flag).
  </action>
  <verify>mix hex.build && mix hex.publish --dry-run</verify>
  <acceptance_criteria>
    - `mix hex.build` exits 0 producing `lattice_stripe-1.7.0.tar`
    - `mix hex.publish --dry-run` exits 0
    - SUMMARY documents any pre-existing warning exceptions with module names
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 3: CI publish to Hex (maintainer)</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/release.yml
    - .github/workflows/publish-hex.yml
    - .planning/ROADMAP.md (Next Step maintainer block)
  </read_first>
  <action>
    Skip if Task 1 found 1.7.0 already on Hex.

    **CI publish path** (no local `mix hex.publish`):

    1. Ensure release commits are on `main`; `git push origin main`
    2. Ensure tag exists on remote: `git tag -a v1.7.0 -m "Release 1.7.0"` (if needed); `git push origin v1.7.0`
    3. Wait for `ci.yml` to succeed on the tagged SHA
    4. GitHub Actions → **Publish Hex Recovery** → dispatch with:
       - `tag`: `v1.7.0`
       - `release_version`: `1.7.0`
       - `dry_run`: `false` (use `true` first for rehearsal)
    5. Confirm workflow run is green (preflight, publish, Hex index verify)

    Requires repository secret `HEX_API_KEY`. Manifest is already `"1.7.0"` — do not hand-edit.

    Do NOT publish intermediate 1.4/1.5/1.6 versions.
  </action>
  <verify>mix hex.info lattice_stripe 1.7.0</verify>
  <acceptance_criteria>
    - `mix hex.info lattice_stripe 1.7.0` shows version 1.7.0
    - `mix hex.info lattice_stripe | grep -q '1.7.0'` in recent releases
    - `.release-please-manifest.json` contains `"1.7.0"`
    - Git tag `v1.7.0` exists on remote
    - Publish Hex Recovery workflow run succeeded on GitHub Actions
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: REL-04 pass gate for downstream 55-06</name>
  <files></files>
  <read_first>
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-03-PLAN.md (Task 1 REL-04 pre-flight)
  </read_first>
  <action>
    Final REL-04 verification gate:

    ```bash
    mix hex.info lattice_stripe 2>/dev/null | grep -q '1.7.0' || (echo 'REL-04 NOT VERIFIED' && exit 1)
    ```

    Record PASS in SUMMARY. If FAIL, halt — do not proceed to 55-06.
  </action>
  <verify>mix hex.info lattice_stripe 2>/dev/null | grep -q '1.7.0'</verify>
  <acceptance_criteria>
    - `mix hex.info lattice_stripe` output includes `1.7.0`
    - SUMMARY Self-Check: PASSED for REL-04
    - No downstream 55-06 execution if this task fails
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix hex.info lattice_stripe 1.7.0
grep '"1.7.0"' .release-please-manifest.json
git tag -l 'v1.7.0'
```
</verification>

<success_criteria>
- REL-04 gap closed: Hex package published at 1.7.0
- 55-06 can proceed with close_ready reconciliation
</success_criteria>
