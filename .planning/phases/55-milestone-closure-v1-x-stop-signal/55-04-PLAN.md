---
phase: 55-milestone-closure-v1-x-stop-signal
plan: 04
type: execute
wave: 3
depends_on: [55-02, 55-03]
files_modified:
  - test/lattice_stripe/docs_truth_test.exs
  - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md
autonomous: true
requirements: [CLOSE-01, CLOSE-02]
must_haves:
  truths:
    - "docs_truth_test locks stop-signal phrases and refutes false completeness claims."
    - "55-VERIFICATION.md documents grep-backed evidence for audit-milestone."
  artifacts:
    - path: "test/lattice_stripe/docs_truth_test.exs"
      provides: "Regression locks"
      contains: "feature-complete for its intended scope"
    - path: ".planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md"
      provides: "Phase verification report"
      contains: "CLOSE-01"
  key_links:
    - from: "docs_truth_test.exs"
      to: "guides/scope.md"
      via: "File.read! assertions"
      pattern: "guides/scope.md"
---

<objective>
Extend docs-truth regression for v1.x stop signal and scope anchors; produce `55-VERIFICATION.md` with grep-backed gates for `/gsd-audit-milestone v1.7`.

Purpose: CONTEXT D-05c, D-06.

Output: Automated locks + audit-ready verification report (adapt Phase 52/53 VERIFICATION patterns).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md
@.planning/phases/55-milestone-closure-v1-x-stop-signal/55-RESEARCH.md
@test/lattice_stripe/docs_truth_test.exs
@.planning/phases/52-charge-surface-expansion/52-VERIFICATION.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Stop-signal drift after merge | medium | docs_truth positive asserts on README |
| False completeness marketing | high | refute tests for forbidden phrases |
| scope.md not in ExDoc | medium | assert extras + optional group membership |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs_truth_test.exs</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs (exdoc test ~L25, readme release test ~L99)
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md (D-06)
    - README.md and guides/scope.md (after 55-02)
  </read_first>
  <action>
    Add test `describe "v1.x stop signal and scope boundaries"` with:

    1. **README positive asserts:**
       - `feature-complete for its intended scope`
       - `maintenance and adoption-driven`
       - `guides/user-flows-and-jtbd.md`
       - `guides/api_stability.md`
       - `## v1.x scope`
       - deferred anchors: `Identity`, `Reporting`

    2. **README negative refutes:**
       - `complete Stripe SDK` (case insensitive)
       - `all endpoints` (case insensitive)

    3. **guides/scope.md asserts** (read file directly):
       - `Identity`
       - `Reporting` or `Sigma`
       - `adopter pull` OR `maintenance mode`
       - `Client.request` OR `Client.request/2`

    4. **ExDoc placement** — extend existing `exdoc keeps the primary public truth surfaces` test OR add assertion:
       - `"guides/scope.md" in extras`
       - `"guides/scope.md" in groups["Start Here"]` (if 55-02 placed there)

    Follow existing test style: `File.read!/1`, `assert x =~ y`, `refute`.
  </action>
  <verify>mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` exits 0
    - New describe block exists with stop-signal test name
    - Tests fail if `complete Stripe SDK` added to README (sanity: optional local check)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Produce 55-VERIFICATION.md</name>
  <files>.planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/52-charge-surface-expansion/52-VERIFICATION.md (report structure)
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-01-PLAN.md through 55-03-PLAN.md must_haves
    - .planning/ROADMAP.md Phase 55 success criteria
  </read_first>
  <action>
    Create `55-VERIFICATION.md` with YAML frontmatter:
    ```yaml
    phase: 55-milestone-closure-v1-x-stop-signal
    verified: {ISO8601 timestamp}
    status: passed | gaps
    score: N/N must-haves verified
    requirements: CLOSE-01, CLOSE-02 satisfied
    ```

    Sections:
    1. **Goal Achievement** — table mapping ROADMAP success criteria 1–5 to grep/file evidence
    2. **CLOSE-01 evidence** — `41.1-VERIFICATION.md` path, `accepted-external-verification` rg output, retirement heading
    3. **CLOSE-02 evidence** — README phrases, scope.md exists, PROJECT v1.x Status, docs_truth test name
    4. **Requirements Coverage** — CLOSE-01, CLOSE-02 SATISFIED with evidence column
    5. **Behavioral Spot-Checks** — commands run and exit codes:
       - `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors`
       - `rg 'close_ready' .planning/STATE.md`
       - `rg 'accepted-external-verification' .planning/phases/41.1`

    Run grep commands during authoring; paste representative output in Evidence column.

    If any gate fails, set `status: gaps` and list in `gaps:` frontmatter array.
  </action>
  <verify>test -f .planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md && rg 'CLOSE-01' .planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md</verify>
  <acceptance_criteria>
    - `55-VERIFICATION.md` exists with frontmatter `phase: 55`
    - Report includes CLOSE-01 and CLOSE-02 requirement rows
    - Report references `mix test test/lattice_stripe/docs_truth_test.exs` spot-check
    - All 5 ROADMAP success criteria have a row in Observable Truths or equivalent table
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors
test -f .planning/phases/55-milestone-closure-v1-x-stop-signal/55-VERIFICATION.md
rg 'feature-complete for its intended scope' README.md guides/scope.md
```
</verification>
