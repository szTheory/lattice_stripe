---
phase: 55-milestone-closure-v1-x-stop-signal
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - guides/scope.md
  - README.md
  - .planning/PROJECT.md
  - CHANGELOG.md
  - mix.exs
autonomous: true
requirements: [CLOSE-02]
must_haves:
  truths:
    - "README blockquote includes v1.x stop-signal with JTBD and api_stability links."
    - "guides/scope.md is the canonical deferred-scope contract wired into ExDoc."
    - "PROJECT.md has v1.x Status (post–1.7.0) section with maintenance posture."
  artifacts:
    - path: "guides/scope.md"
      provides: "Public scope boundary"
      contains: "Specialist Stripe families"
    - path: "README.md"
      provides: "Stop signal + v1.x scope section"
      contains: "feature-complete for its intended scope"
  key_links:
    - from: "README.md"
      to: "guides/scope.md"
      via: "v1.x scope section"
      pattern: "guides/scope.md"
---

<objective>
Publish the v1.x stop signal in public documentation: new `guides/scope.md`, README blockquote extension + `## v1.x scope`, PROJECT.md status header, CHANGELOG pointer, and ExDoc wiring.

Purpose: CLOSE-02 public half (CONTEXT D-02, D-03).

Output: Adopters see feature-complete-for-intended-scope framing with honest deferred-family disclosure.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md
@.planning/phases/55-milestone-closure-v1-x-stop-signal/55-RESEARCH.md
@README.md
@guides/tax.md
@mix.exs
@CHANGELOG.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Claiming complete Stripe SDK | high | Never use "complete Stripe SDK" or "all endpoints"; CONTEXT refute tests in 55-04 |
| Duplicating deferred list in blockquote | medium | Blockquote gets stop-signal paragraph only; deferred lists live in `## v1.x scope` + scope.md |
| Tax core misread as incomplete | medium | Separate Tax narrow follow-ups list from specialist families (D-03a) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create guides/scope.md</name>
  <files>guides/scope.md</files>
  <read_first>
    - guides/tax.md (Scope boundary section, lines 13–29)
    - .planning/REQUIREMENTS.md (Future Requirements SPEC/TAX, Out of Scope table)
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md (D-03b section outline)
    - prompts/stripe-sdk-api-surface-area-deep-research.md (partial coverage honesty — if present)
  </read_first>
  <action>
    Create `guides/scope.md` with these sections (H2 headings):

    1. **Introduction** — LatticeStripe is an HTTP client SDK; v1.x mainstream SaaS scope as of 1.7.0.

    2. **Intended audience** — Mainstream SaaS: payments, billing, Connect, tax on custom flows, webhooks, operator diagnostics.

    3. **What v1.x includes** — Positive clusters (Payments/PI, Billing, Metering, Connect, Tax core, Webhooks including thin events, operator guides). Link `guides/user-flows-and-jtbd.md`.

    4. **Deferred by design** — Two grouped bullet lists exactly:
       - **Specialist Stripe families:** Identity; Treasury; Issuing; Terminal; Financial Connections; Climate; Sigma; Reporting
       - **Tax narrow follow-ups:** Tax Code lookup (`/v1/tax_codes`); Tax Transaction list (if Stripe adds the endpoint)
       One-line why each group is deferred (adopter pull / specialist).

    5. **Tax note** — v1.6 Tax core is complete; follow-ups are lookup/list gaps only (not "Tax incomplete").

    6. **Escape hatch** — `LatticeStripe.Client.request/2` for unwrapped endpoints; link `guides/extending-lattice-stripe.md`.

    7. **Accrue boundary** — SDK stays lower-level than Accrue (mirror tax.md tone).

    8. **Maintenance and adopter pull** — v1.x is maintenance and adoption-driven; no planned new resource-family breadth absent fresh pull.

    9. **Requesting coverage** — GitHub issue welcome; not a roadmap commitment.

    Use phrases: `maintenance mode`, `adopter pull` at least once in deferred/maintenance sections.
  </action>
  <verify>test -f guides/scope.md && rg 'Identity' guides/scope.md && rg 'Tax Code lookup' guides/scope.md && rg 'Client.request' guides/scope.md</verify>
  <acceptance_criteria>
    - `guides/scope.md` exists
    - Contains both grouped negative lists (Specialist + Tax narrow follow-ups)
    - Contains `maintenance` or `adopter pull`
    - Contains link to `guides/user-flows-and-jtbd.md`
    - Does not contain `complete Stripe SDK` or `all endpoints`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: README stop signal + v1.x scope section</name>
  <files>README.md</files>
  <read_first>
    - README.md (release blockquote lines 8–15, Docs Ladder ~line 24)
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md (D-02f blockquote append, D-03f section)
    - .planning/phases/54-release-truth-capstone/54-02-PLAN.md (do NOT duplicate deferred list in blockquote)
  </read_first>
  <action>
    1. **Append to release-status blockquote** after the 1.7 bullet, **before** the CHANGELOG link line:

       ```markdown
       > **v1.x scope:** LatticeStripe is **feature-complete for its intended scope** — mainstream SaaS payments, billing, usage metering, Connect, tax calculation, webhooks, and operator diagnostics. Further **1.x** work is **maintenance and adoption-driven**: bugfixes, Stripe API drift, and narrow additions when real adopters need them. See [User Flows & JTBD](guides/user-flows-and-jtbd.md) for fit and [API Stability](guides/api_stability.md) for the semver contract.
       ```

    2. **Insert new section** `## v1.x scope` immediately **before** `## Docs Ladder`:

       ```markdown
       ## v1.x scope

       As of **1.7.0**, LatticeStripe is **feature-complete for its intended v1.x scope**: mainstream SaaS integrations — payments, billing, Connect, tax on custom flows, webhooks (including thin events), and production operator guides.

       **Not in v1.x scope** (maintenance mode; additions only on adopter pull):

       - **Specialist Stripe families:** Identity; Treasury; Issuing; Terminal; Financial Connections; Climate; Sigma; Reporting
       - **Tax narrow follow-ups:** Tax Code lookup (`/v1/tax_codes`); Tax Transaction list (if Stripe adds the endpoint)

       See [Scope](guides/scope.md) for boundaries, escape hatches, and how to request coverage.
       ```

    3. **Docs Ladder** — add Scope link in appropriate bullet (after evaluator JTBD or under Documentation).

    4. **Do not** add deferred families to Features inventory (positive-only per D-03f).
  </action>
  <verify>rg 'feature-complete for its intended scope' README.md && rg 'guides/scope.md' README.md && rg 'maintenance and adoption-driven' README.md</verify>
  <acceptance_criteria>
    - README contains `feature-complete for its intended scope`
    - README contains `maintenance and adoption-driven`
    - README links `guides/user-flows-and-jtbd.md` and `guides/api_stability.md` in blockquote
    - README contains `## v1.x scope` section with `Identity` and `Reporting` in deferred list
    - `rg -i 'complete Stripe SDK' README.md` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md, CHANGELOG pointer, mix.exs ExDoc</name>
  <files>.planning/PROJECT.md, CHANGELOG.md, mix.exs</files>
  <read_first>
    - .planning/PROJECT.md (current v1.7 goal section)
    - CHANGELOG.md (`## [1.7.0]` section)
    - mix.exs (`:docs` extras and groups_for_extras)
    - .planning/phases/55-milestone-closure-v1-x-stop-signal/55-CONTEXT.md (D-02b, D-02c, D-03c)
  </read_first>
  <action>
    1. **PROJECT.md** — Add after v1.7 milestone goal block:

       ```markdown
       ## v1.x Status (post–1.7.0)

       The library is **done for v1.x scope** — intended mainstream SaaS Stripe coverage is shipped and documented. Identity, Treasury, Issuing, Terminal, Financial Connections, Climate, Sigma, and Reporting remain deferred unless adopter pull justifies a future milestone.

       **Forward posture:** Maintenance mode — bugfixes, Stripe API drift, adopter-driven narrow additions. No planned new resource-family breadth in v1.x absent fresh adopter pull.
       ```

       Update any `pending-external-verification` sentence for Phase 41.1 to `accepted-external-verification` (pointer to 55-03 for full sweep).

    2. **CHANGELOG.md** — Under `## [1.7.0]`, add one sentence:
       `v1.x scope and maintenance posture: see README and [User Flows & JTBD](guides/user-flows-and-jtbd.md).`
       (No full narrative duplicate.)

    3. **mix.exs** — Add `"guides/scope.md"` to `:docs` extras list and to `Start Here` group (after `guides/user-flows-and-jtbd.md` recommended).
  </action>
  <verify>rg 'v1.x Status' .planning/PROJECT.md && rg 'scope.md' mix.exs && rg 'guides/scope.md' mix.exs</verify>
  <acceptance_criteria>
    - PROJECT.md contains `## v1.x Status (post–1.7.0)` and `done for v1.x scope`
    - CHANGELOG 1.7.0 section references README or JTBD guide
    - `mix.exs` extras includes `guides/scope.md`
    - `groups_for_extras` Start Here includes `guides/scope.md`
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
test -f guides/scope.md
rg 'feature-complete for its intended scope' README.md
rg 'guides/scope.md' README.md mix.exs
rg -i 'complete Stripe SDK' README.md guides/scope.md && exit 1 || true
mix compile --warnings-as-errors
```
</verification>
