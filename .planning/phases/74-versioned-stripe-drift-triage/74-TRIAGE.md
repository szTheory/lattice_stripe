# Phase 74: Versioned Stripe Drift Triage

**Reviewed:** 2026-09-23
**Scope:** Candidate changes on already-supported resources; no implementation, new resource families, generator, validator, or API-pin migration.
**Default API version:** `2026-03-25.dahlia` — unchanged.
**Evidence method:** Retry the local drift task; search repository/planning files for a dated exact-path inventory; compare named research leads with dated Stripe changelog/API references; inspect the official GA OpenAPI source and local decoders before promoting anything.

## Coverage and source results

**Inventory coverage: PARTIAL / NOT EXHAUSTIVE.** No successful current exact-path output or dated exact-path inventory was available. The 2026-09-23 milestone assessment at `.planning/threads/v1-12-next-milestone-assessment.md` records 104 additions across 23 modules, but has no paths; it is a count-only discovery note, not an inventory. A repository/planning search found no separate dated path list.

Refresh attempt on 2026-09-23: `mix lattice_stripe.check_drift` exited 1 before task execution. Mix 1.19.5 failed to start `Mix.PubSub.Subscriber` because opening the TCP socket in `Mix.Sync.PubSub.subscribe/1` returned `:eperm`. No current inventory was produced. Do not infer membership from the dated count or from a changelog resource-family mention. No complete-ledger or exhaustive-coverage claim is made.

The candidate classifications below describe what the dated Stripe changelog says; inventory membership and GA-schema corroboration remain unconfirmed. No lead is selected. Status vocabulary: `current stable` means stable evidence applies at the package pin; `later stable` means stable evidence has a later minimum API version; `preview-only` means Stripe labels the change preview; `unconfirmed/schema noise` means stable/versioned or GA evidence is absent or the finding may be a schema-shape artifact. A successful runtime probe can corroborate behavior only after the stable-source and GA gates; it cannot establish GA status or replace versioned provenance. No runtime probe was used.

## Candidate ledger

| Exact resource path | Stable Stripe evidence and version | Status / pin applicability | Inventory membership; GA corroboration | Adopter job and semantic value | Type/decode implications | Disposition and reopening evidence |
|---|---|---|---|---|---|---|
| `Invoice.amount_paid_off_stripe` | [Invoice changelog, 2026-05-27](https://docs.stripe.com/changelog/dahlia/2026-05-27/invoice-object-amount-paid-off-stripe-property); minimum `2026-05-27.dahlia`. Integer in the smallest currency unit; dynamically reflects off-Stripe Payment Record amounts. Available on API requests, not event payloads. | `later stable`; later than `2026-03-25.dahlia`. | Inventory membership: **unconfirmed**. GA: exact snapshot JSON path not checked; mutable target is [latest GA JSON](https://github.com/stripe/openapi/blob/master/latest/openapi.spec3.json). Snapshot commit/ref, JSON path, and SHA-256: **unconfirmed/unavailable**. | Finance teams reconcile partial and full off-Stripe invoice payments without treating a boolean as an amount. Retrieval-only availability matters for event consumers. | Optional includable integer amount; smallest-currency-unit semantics are explicit. Confirm omission/null behavior against GA JSON. Invoice decoding uses `@known_fields`/`from_map/1` and retains unknown keys in `extra`. Do not imply webhook-event availability. | **Deferred.** Reopen when a successful exact-path inventory or dated exact-path list confirms membership, then record immutable GA snapshot identity, exact JSON path/hash, and a versioned fixture. |
| `Refund.customer`, `Refund.customer_account`, `Refund.payment_method` | [Refund changelog, 2026-07-29](https://docs.stripe.com/changelog/dahlia/2026-07-29/adds-customer-and-payment-method-details-to-the-refunds-api); minimum `2026-07-29.dahlia`. Returned on refund endpoints; customer and payment method IDs can be expanded. | `later stable`; later than `2026-03-25.dahlia`. | Inventory membership: **unconfirmed**. GA: exact snapshot JSON paths not checked; [latest GA JSON](https://github.com/stripe/openapi/blob/master/latest/openapi.spec3.json) was not inspectable as JSON in this review. Snapshot commit/ref, JSON paths, and SHA-256: **unconfirmed/unavailable**. | Refund support and reconciliation can attribute a refund to its customer/account and payment method without follow-up reads. | ID strings, with `customer` and `payment_method` expandable to objects; verify nullability and exact `customer_account` semantics in GA schema. Preserve ID/object alternatives and unknown fields through the existing decoder `extra` seam. | **Deferred.** Reopen on exact inventory membership plus per-property GA JSON evidence and a versioned fixture covering ID/expanded forms and nullable cases. |
| `Dispute.payment_method_details.card.network` | [Dispute changelog, 2026-07-29](https://docs.stripe.com/changelog/dahlia/2026-07-29/dispute-payment-method-details-card-network); minimum `2026-07-29.dahlia`. | `later stable`; later than `2026-03-25.dahlia`. | Inventory membership: **unconfirmed**. GA: exact nested JSON path not checked; [latest GA JSON](https://github.com/stripe/openapi/blob/master/latest/openapi.spec3.json) was not inspectable as JSON in this review. Snapshot commit/ref, JSON path, and SHA-256: **unconfirmed/unavailable**. | Dispute operators can identify the card network associated with a disputed charge. This is useful operational context, but source qualification alone does not establish priority over other paths. | Nested card detail; exact optionality/nullability and network value contract require GA JSON confirmation. Keep network values open to upstream additions; nested decoding must preserve unknown keys; event-payload availability remains unconfirmed. | **Deferred.** Reopen on exact inventory membership and immutable GA path evidence; verify nested decoder behavior and fixture before Phase 75 selection. |
| `InvoiceItem.frozen_fields` | [Invoice Item changelog, 2026-08-26](https://docs.stripe.com/changelog/dahlia/2026-08-26/adds-frozen-fields-property-to-invoice-items); minimum `2026-08-26.dahlia`. Read-only list of fields that cannot be changed on derived invoice items. | `later stable`; later than `2026-03-25.dahlia`. | Inventory membership: **unconfirmed**. GA: exact snapshot JSON path not checked; [latest GA JSON](https://github.com/stripe/openapi/blob/master/latest/openapi.spec3.json) was not inspectable as JSON in this review. Snapshot commit/ref, JSON path, and SHA-256: **unconfirmed/unavailable**. | Billing operators and update flows can explain why changing a derived line item would fail and avoid an unnecessary 400 response. | Array of field-name strings; possible values include `discounts`, `pricing`, and `quantity`, not a closed consumer enum. Confirm response nullability and whether this is represented on `InvoiceItem` versus line-item forms in GA schema; event availability remains unconfirmed. | **Deferred.** Reopen on exact inventory membership and GA path/snapshot evidence, then prove array and unknown-value behavior in a versioned fixture. |
| `Invoice.payment_settings.payment_method_types` and `Subscription.payment_settings.payment_method_types` (including request paths) | [Billie changelog, 2026-08-26](https://docs.stripe.com/changelog/dahlia/2026-08-26/billie-support-for-invoices-and-subscriptions); minimum `2026-08-26.dahlia`. Adds `billie` for `send_invoice`; not for `charge_automatically`. | `later stable`; later than `2026-03-25.dahlia`. | Inventory membership: **unconfirmed**. GA: exact request/response JSON paths not checked; [latest GA JSON](https://github.com/stripe/openapi/blob/master/latest/openapi.spec3.json) was not inspectable as JSON in this review. Snapshot commit/ref, paths, and SHA-256: **unconfirmed/unavailable**. | B2B billing adopters can offer Billie on hosted invoice pages where Stripe and account settings support it. Applicability is conditional, not a general payment-method capability. | Open list of strings in request and response settings; do not close the enum around today's values. Existing Invoice and Subscription `payment_settings` decoding is map-based; retain `extra` and unknown values. Event-payload availability remains unconfirmed. Confirm exact schema and constraints in GA JSON. | **Deferred.** Reopen on exact inventory membership plus GA request/response path evidence; check `send_invoice`/`charge_automatically` semantics and add versioned fixture coverage. |

### GA snapshot inspection record

Stripe's [OpenAPI repository](https://github.com/stripe/openapi) distinguishes `/latest/` (GA) from `/preview/`; its [latest README](https://github.com/stripe/openapi/blob/master/latest/README.md) identifies `latest/openapi.spec3.json` as the public GA spec. On 2026-09-23 the GitHub file page for `latest/openapi.spec3.json` was identified, but its large JSON contents could not be inspected in this environment. The mutable `master` URL is not an immutable snapshot identity. No commit/ref, JSON pointer, or SHA-256 is claimed for any candidate. This source limitation keeps every candidate deferred.

## Ranking and compatibility decision

There is no evidence-qualified selected set to rank. Once evidence gates pass, priority should follow the adopter job, operational consequence, and whether related fields form a coherent resource addition, with extra weight for money movement, tax, fulfillment, webhook interpretation, Connect context, and financial reconciliation. The leads above are retained for review because they touch reconciliation, refund attribution, dispute operations, invoice-update errors, and conditional B2B payment choice. This is not a score, quota, count-based or recency ranking, or assertion that they are present in the drift inventory. No broader candidate ledger can be built from the available count-only source.

`LatticeStripe.api_version/0` remains pinned to `2026-03-25.dahlia` in `lib/lattice_stripe.ex`. The pin was confirmed locally; this phase does not change it. The cited later-stable changelogs establish that the five leads have later minimum versions. Stripe's versioned references recommend testing the integration and webhook version when upgrading; that is relevant upgrade guidance, not a compatibility review of this package.

D-10 review not performed here: a complete intervening changelog analysis; request-semantic compatibility; old/new response-decoding behavior; webhook event/version behavior; supported adopter-flow testing; and package SemVer impact. An official 2026-08-26 **preview** changelog entry also removes writable `payment_method_types` from PaymentIntent and SetupIntent create/update/confirm operations while keeping it readable; this is preview-only context, not a stable migration trigger. Any future default-pin move requires a separate review covering all D-10 evidence, not date freshness or one fixture.

Any Phase 75 implementation considered later must remain additive and optional, preserve existing unknown response keys in `extra`, retain expanded ID/object shapes where applicable, and avoid closing enum-like Stripe values without an explicit stability guarantee.

### Guarded-write verification

The final artifact save uses `.planning/tools/guarded_markdown_write.py`, which compares the inspected SHA-256 immediately before replacement, writes a complete same-directory temporary file, and atomically renames it into place. Its smoke check rejects a stale hash without overwriting the competing edit, then confirms a successful atomic replacement. Re-run with `python3 .planning/tools/guarded_markdown_write.py --self-test`.
