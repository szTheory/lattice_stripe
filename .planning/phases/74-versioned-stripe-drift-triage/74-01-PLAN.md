---
phase: 74-versioned-stripe-drift-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
autonomous: true
requirements:
  - DRIFT-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every considered candidate from an already-supported resource is tied to an exact inventory path, stable versioned Stripe evidence, an explicit current/later/preview/unconfirmed classification, and an exact GA OpenAPI snapshot identity or an explicit unconfirmed disposition."
    - "Every selected or deferred candidate has its adopter job, operational and semantic rationale, type/decode implications, and a reason plus reopening evidence when deferred; ranking does not use counts, recency, a fixed score, or a resource quota."
    - "The default API version decision is recorded against inspected compatibility evidence and retains 2026-03-25.dahlia unless the separate D-10 compatibility bar is met."
    - "The complete decision record is saved atomically, and a repeated or parallel writer detects intervening edits before replacement instead of silently overwriting them."
  artifacts:
    - path: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      provides: "Human-reviewable Phase 74 selection, deferral, source-provenance, and API-pin decision record"
  key_links:
    - from: .planning/threads/v1-12-next-milestone-assessment.md and any successful current drift output
      to: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      via: "Exact candidate-path intersection; the master-based drift output and dated 104-field inventory are discovery leads only (D-01)."
    - from: Official dated Stripe changelog or explicitly versioned API reference and the exact official GA /latest OpenAPI snapshot
      to: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      via: "Stable API-version classification, exact field-path corroboration, and recorded snapshot identity; preview and unconfirmed findings cannot be selected (D-02, D-03)."
    - from: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      to: Phase 75 selected-field implementation
      via: "Each selected row supplies the version floor, response scope, and type/decode implications while preserving extra and open-enum behavior (D-03, D-09, D-11)."
  assumptions:
    - id: EDGE-74-01
      status: flagged
      statement: "Same-directory atomic rename and a pre-write content-hash check are available for the Markdown artifact; if either cannot be used, retain the last complete file and stop for safe recovery."
---

<objective>
Create the sole Phase 74 decision artifact: a concise, reviewable triage of evidence-qualified changes in already-supported Stripe resources, with a bounded selection and explicit deferrals.

Purpose: Separate stable, versioned Stripe contract changes from preview changes and OpenAPI-shape noise, then recommend Phase 75 work by adopter value and operational consequence. Keep the package default API version at 2026-03-25.dahlia unless explicit compatibility evidence justifies a separately reviewed move.
Output: `.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md`; no source code, tests, generator, validator, new resource family, issue-per-field workflow, or API-pin migration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-versioned-stripe-drift-triage/74-CONTEXT.md
@.planning/phases/74-versioned-stripe-drift-triage/74-RESEARCH.md
@.planning/phases/74-versioned-stripe-drift-triage/74-PATTERNS.md
@.planning/phases/74-versioned-stripe-drift-triage/74-VALIDATION.md
@.planning/phases/74-versioned-stripe-drift-triage/74-COVERAGE.md
@.planning/threads/v1-12-next-milestone-assessment.md
@.planning/JTBD-MAP.md
@guides/api_stability.md
@lib/lattice_stripe.ex

Research limitation carried forward: the local `mix lattice_stripe.check_drift` attempt failed before running because Mix could not open its PubSub TCP socket, and the researcher could not inspect the exact 4.31 MB GA OpenAPI snapshot. Treat the dated 104-field list as a lead only. The later-stable examples (`Invoice.amount_paid_off_stripe`, Refund customer/account/payment-method references, `Dispute.payment_method_details.card.network`, Invoice Item frozen fields, and Billie support) remain leads only; verify exact inventory intersection, exact paths in the identified GA snapshot, and decoder/type semantics before selecting any of them. If access or corroboration remains unavailable, record the candidate as unconfirmed/deferred with the evidence needed to reopen it.

Scope and policy: satisfy DRIFT-01 only. Carry D-01–D-11 from CONTEXT.md into the artifact and actions. The spec-less edge probe surfaced one unresolved concurrency question; it is preserved in `must_haves.assumptions` as EDGE-74-01 and addressed by the atomic-save/edit-detection truth. The phase coverage declaration says there is no external API integration: this is public-source review, not Stripe API integration work.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one inventory lead through evidence to a complete disposition</name>
  <files>.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</files>
  <read_first>
    .planning/phases/74-versioned-stripe-drift-triage/74-CONTEXT.md, .planning/phases/74-versioned-stripe-drift-triage/74-RESEARCH.md, .planning/phases/74-versioned-stripe-drift-triage/74-PATTERNS.md, .planning/phases/74-versioned-stripe-drift-triage/74-VALIDATION.md, .planning/threads/v1-12-next-milestone-assessment.md, .planning/JTBD-MAP.md, guides/api_stability.md, lib/lattice_stripe.ex, and the selected candidate's current resource decoder once (for example lib/lattice_stripe/invoice.ex, lib/lattice_stripe/refund.ex, or lib/lattice_stripe/dispute/payment_method_details.ex).</read_first>
  <precondition>The target triage file is absent, or its existing content has been read and its current hash recorded. If it changed after inspection or an existing decision cannot be reconciled, stop and preserve it for explicit merge rather than replacing it.</precondition>
  <action>Start `74-TRIAGE.md` with date, scope, current pin, evidence method, status definitions, and a compact row schema. Refresh `mix lattice_stripe.check_drift` only if it runs normally; if it fails as previously reported, record that fact and use the dated candidate inventory as a stale discovery lead (D-01). Choose one candidate path only after confirming it intersects that inventory and belongs to an already-supported resource. Follow that one candidate end to end: cite its exact dated stable source or explicitly record missing/preview evidence; compare its minimum version with `2026-03-25.dahlia`; inspect the exact field path in the official GA `/latest` OpenAPI JSON and record the source URL, immutable commit/ref or snapshot identity, retrieval date, JSON path, and SHA-256. If the JSON cannot be fetched or inspected, label GA corroboration `unconfirmed` and defer the row. Inspect the resource decoder and Stripe response scope; record optionality, nullability, ID/object shape, units, nested path, and event-versus-retrieval scope as applicable. Preserve unknown response keys in `extra`, keep enum-like upstream values open, and do not let a runtime probe replace stable-source or GA evidence (D-02, D-03, D-04, D-11). Give the row an adopter job, operational/semantic rationale, disposition, and specific reopening evidence when deferred (D-05–D-09). This first complete row is the production-quality tracer through inventory, source qualification, schema corroboration, type safety, adopter reasoning, and saved decision; do not populate placeholder claims for evidence that is not available. Save by writing a complete temporary file in the same directory and atomically renaming it into place. Immediately before replacement, compare the current target hash with the hash captured on read; if another writer changed it, stop and reconcile rather than clobbering it.</action>
  <verify>
    <automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
    <fails_when>Exit status is non-zero and reports whitespace errors or conflict markers in the phase change; also inspect the new file explicitly because an untracked file is outside the ordinary worktree diff until staged.</fails_when>
    <human-check>Trace the single row from the dated inventory path to its cited Stripe source and exact GA snapshot. Confirm the full resource path, version/status, pin applicability, type/decode note, and disposition are evidenced; any unresolved lookup is visibly unconfirmed and deferred.</human-check>
  </verify>
  <acceptance_criteria>
    - `74-TRIAGE.md` exists and contains one fully populated candidate row or one fully populated unconfirmed/deferred row, plus explicit evidence method and status definitions.
    - The row's candidate path is confirmed in the current or dated lead inventory and is in an already-supported resource; otherwise it is recorded as not selected with the missing intersection evidence.
    - No GA corroboration is claimed without the exact snapshot identity and exact field path; unavailable source access results in an unconfirmed deferral and reopening evidence.
    - The file replacement uses same-directory temp plus atomic rename and detects a changed target hash before replacement (EDGE-74-01).
  </acceptance_criteria>
  <done>The artifact has one end-to-end evidence-qualified disposition row, with no unsupported stable/GA claims, and the write path preserves complete prior content on interruption or concurrent edits.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the supported-resource candidate ledger and bounded selection</name>
  <files>.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</files>
  <read_first>
    .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md, .planning/phases/74-versioned-stripe-drift-triage/74-CONTEXT.md, .planning/phases/74-versioned-stripe-drift-triage/74-RESEARCH.md, .planning/phases/74-versioned-stripe-drift-triage/74-PATTERNS.md, .planning/threads/v1-12-next-milestone-assessment.md, .planning/JTBD-MAP.md, and each candidate's resource decoder after selecting the inventory-backed candidates.</read_first>
  <action>Inspect the complete available current drift output or dated 104-field lead inventory. Intersect exact field paths with already-supported resources; do not infer intersection from a resource-family mention. Carry the research examples through the same check: `Invoice.amount_paid_off_stripe`, Refund customer/account/payment-method references, `Dispute.payment_method_details.card.network`, Invoice Item frozen fields, and Billie support. Record whether each is actually present in the lead inventory; any non-intersection is explicitly deferred from this review and reopened by appearance in a successful dated inventory. For every in-scope candidate chosen for consideration, give its full path and versioned source, stable/preview status, explicit API version floor, current-pin applicability, exact GA snapshot identity and JSON path or an explicit unconfirmed state, adopter job, operational consequence, semantic rationale, type/decode implications, disposition, and (if deferred) why it waits and what precise evidence reopens it (D-02, D-03, D-05, D-06, D-09). Classify observed findings as current stable, later stable, preview-only, or unconfirmed/schema noise; preview-only and unconfirmed rows cannot be selected. Use the complete official dated source and minimum-version evidence; any behavior probe is corroboration after qualification only (D-04). Select only after stable-source, exact-inventory, GA-snapshot, and type/decode safety gates all pass. Keep optional fields compatible with `extra`; identify event payload limits and ID-or-object unions where relevant, and preserve unknown Stripe enum-like values (D-11). Rank eligible candidates in prose by adopter job, operational consequence, and coherent resource addition; exclude count/recency ordering, fixed numeric scores, and per-resource quotas (D-05–D-07). Keep the record concise by grouping genuinely coherent candidate clusters without dropping required per-row evidence. Do not add implementation, resource families, a generator/validator, or one issue per field (D-08). Before each replacement, re-read and hash the current artifact; if the hash changed since inspection, merge deliberately or stop. Save each complete revision through same-directory temp file and atomic rename.</action>
  <verify>
    <automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
    <fails_when>Exit status is non-zero and reports whitespace errors or conflict markers in the phase change; inspect the complete new artifact directly as part of the task review.</fails_when>
    <human-check>Review every selected and deferred row against the exact inventory path, stable versioned source, API version/status, GA snapshot provenance or explicit unconfirmed state, pin applicability, adopter job, rationale, type/decode implications, disposition, and reopening evidence. Confirm no preview/unconfirmed candidate is selected and no field is promoted from the master diff alone.</human-check>
  </verify>
  <acceptance_criteria>
    - Every considered in-scope candidate has a disposition and all D-09 evidence; every deferral names both a reason and specific reopening evidence.
    - Build the ledger from the complete exact-path intersection first: every path in the available lead inventory whose resource is already supported is accounted for as selected, deferred, preview-only, or unconfirmed; do not omit low-priority or unresolved paths.
    - Each of the five research examples is explicitly checked against the current/dated candidate inventory; none is treated as inventory-backed or GA-confirmed by assumption.
    - Every selected candidate has stable dated/versioned provenance and exact GA snapshot corroboration; unavailable evidence is labeled unconfirmed/deferred.
    - The selected set is bounded through named adopter work and operational rationale, not candidate count, recency, fixed score, or quota (D-05–D-07).
    - The artifact contains no implementation, package, generator, validator, added-resource-family, or per-field-issue work (D-08 and phase boundary).
  </acceptance_criteria>
  <done>The record explains the selected subset and all considered deferrals with enough field-level evidence for Phase 75 to act without treating discovery output as Stripe contract proof.</done>
</task>

<task type="auto">
  <name>Task 3: Record the default-version decision and finish the evidence review</name>
  <files>.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</files>
  <read_first>
    .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md, .planning/phases/74-versioned-stripe-drift-triage/74-CONTEXT.md, .planning/phases/74-versioned-stripe-drift-triage/74-RESEARCH.md, .planning/phases/74-versioned-stripe-drift-triage/74-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, `.planning/JTBD-MAP.md`, and `lib/lattice_stripe.ex`.</read_first>
  <action>Complete a dedicated API-version decision section recording the pinned default `2026-03-25.dahlia`, exact compatibility evidence inspected, which candidate minimum versions do or do not apply to that pin, and any evidence gaps. Cite the current pin source and relevant Stripe versioned sources. Preserve the default pin: Phase 74 does not perform a migration. State that date freshness, a single fixture, or later-stable optional fields alone do not meet D-10. A move can be considered only after the separate review covers the intervening changelog, request semantics, response decoding, webhook event/version behavior, supported adopter flows, and package SemVer impact; if that full evidence is absent, record `unchanged` and the specific missing review evidence rather than implying compatibility was proven (D-10). Finish a row-by-row source review of all selected and deferred rows, confirm status vocabulary distinguishes stable, preview, and OpenAPI shape noise, and verify that no out-of-scope implementation has entered the artifact. Keep the evidence review human-reviewable; do not add a test, source validator, or external API integration. Record review date and exact mutable-snapshot identity so the result remains auditable. Check the current file hash immediately before the final atomic replacement and stop on an intervening change; save the complete record through same-directory temp plus atomic rename.</action>
  <verify>
    <automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
    <fails_when>Exit status is non-zero and reports whitespace errors or conflict markers in the phase change; inspect the entire final artifact directly for untracked content as well.</fails_when>
    <human-check>For every row, open the cited dated Stripe source and compare the exact path/version/status against the recorded GA snapshot or unconfirmed disposition. Confirm pin applicability and the unchanged-pin rationale against D-10, then review all D-01–D-11 decisions and the explicit no-external-API-integration declaration.</human-check>
  </verify>
  <acceptance_criteria>
    - The decision section names the pin, explicit compatibility evidence, applicability conclusions, and remaining evidence required for any future move; the selected default remains `2026-03-25.dahlia` (D-10).
    - The final row-by-row human source review passes; any inaccessible or ambiguous evidence remains explicitly unconfirmed/deferred.
    - The final artifact has a review date and exact GA snapshot identity where inspected, preserves `extra` and open-enum semantics (D-11), and satisfies the human-only evidence checks in `74-VALIDATION.md`.
    - No code, test, schema, package dependency, new external API integration, generator, validator, or pin migration is added.
  </acceptance_criteria>
  <done>`74-TRIAGE.md` is a complete, source-reviewed handoff with an evidence-based unchanged-pin decision and safe, durable update behavior.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL | Distinguish stable/versioned contract changes from preview and OpenAPI noise, then select by adopter value and operational consequence | Tasks 1–3 | COVERED |
| REQ | DRIFT-01 | Tasks 1–3; frontmatter requirements | COVERED |
| RESEARCH | Current drift output and dated 104-field list are discovery leads; local drift command failed before inventory refresh | Tasks 1–2 | COVERED |
| RESEARCH | Exact 4.31 MB GA OpenAPI snapshot was inaccessible; exact candidate path plus snapshot identity required, otherwise unconfirmed/deferred | Tasks 1–3 | COVERED |
| RESEARCH | Later-stable leads: Invoice amount paid off Stripe; Refund references; Dispute card network; Invoice Item frozen fields; Billie support | Task 2, each explicitly checked against inventory and evidence gates | COVERED |
| RESEARCH | Hand-written decoders, `extra`, open enum values, event/retrieval scope; no package dependency | Tasks 1–3 | COVERED |
| CONTEXT | D-01–D-04: discovery/evidence/status/probe boundaries | Tasks 1–2 | COVERED |
| CONTEXT | D-05–D-07: adopter and operational prioritization; no count, recency, score, or quota ranking | Task 2 | COVERED |
| CONTEXT | D-08–D-09: concise Markdown artifact and row-level evidence/deferrals | Tasks 1–2 | COVERED |
| CONTEXT | D-10: unchanged default unless separate compatibility evidence meets full bar | Task 3 | COVERED |
| CONTEXT | D-11: preserve `extra` and open enum semantics | Tasks 1–3 | COVERED |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Stripe public changelog/reference/OpenAPI → triage record | External contract claims enter a maintainer decision record; GA versus preview and snapshot identity must remain explicit. |
| Repeated or parallel local writer → triage file | A second run may update the same `.planning` artifact; partial writes or silent replacement could lose a decision. |
| Runtime security | Not applicable: no application endpoint, secret, authentication, authorization, session, input, or cryptographic behavior is changed; no live Stripe credentials are required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-74-01 | Tampering | Source status and candidate qualification | high | mitigate | Require a dated/versioned official source, exact inventory path, and exact official GA snapshot path/identity before selection; inaccessible evidence is explicitly unconfirmed/deferred. |
| T-74-02 | Tampering | `.planning` decision artifact | medium | mitigate | Write complete same-directory temporary content, compare the current hash immediately before atomic rename, and stop for reconciliation on concurrent changes. |
| T-74-03 | Repudiation | Candidate provenance rows | medium | mitigate | Store exact URLs, API-version/status, snapshot revision/hash, path, review date, and per-row rationale in the artifact. |
| T-74-04 | Information disclosure | Public-source review | low | mitigate | Use only public Stripe sources; do not put secrets in the record or require live credentials/probes. |
| T-74-05 | Elevation of privilege | Runtime package | low | accept | No runtime or authorization behavior is in scope; no package is installed and no privilege path changes. |
</threat_model>

<verification>
`git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md` checks the uncommitted artifact, including its initial creation. No code tests are added or run. Human source-by-source review is mandatory for every selected and deferred row, the exact candidate-inventory intersection, GA provenance/status, type/decode implications, reopening evidence, and the unchanged API pin. If source access cannot resolve a row, that row remains unconfirmed/deferred. The human review cannot be replaced by whitespace hygiene.

<automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
<fails_when>Any non-zero exit with whitespace or conflict-marker diagnostics; untracked artifact content must be opened and reviewed directly because this command does not prove source status or semantic value.</fails_when>
<human-check>Review every row against its primary Stripe source and the identified exact GA snapshot. Verify the four status classes and current-pin applicability, the semantic/type rationale and adopter job, every deferral's reopening evidence, the pin decision, and D-01–D-11. Confirm the file is safely updated and the phase remains documentation-only.</human-check>
</verification>

<success_criteria>
- DRIFT-01 is met by an exact-path, version-aware candidate record grounded in dated Stripe sources, with every promotion corroborated by an identified official GA snapshot.
- All selected/deferred rows contain required adopter, semantic, type/decode, and disposition detail; deferrals identify reopening evidence.
- Unconfirmed and preview-only evidence is not promoted; `extra` and open-enum semantics are preserved in the Phase 75 recommendations.
- The recorded default remains `2026-03-25.dahlia` unless D-10's separate compatibility review bar is explicitly met.
- Human source-by-source review is complete; hygiene checks are not represented as proof of Stripe status or adopter value.
</success_criteria>

<output>
Create `.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md` when done.
</output>
