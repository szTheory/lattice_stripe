---
phase: 67-dx-hardening-milestone-doc-close
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/charge.ex
  - guides/payments.md
  - test/lattice_stripe/docs_truth_test.exs
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "D-10/D-11: exactly the Charge moduledoc and bounded `## Charge reconciliation` guide section carry the full permanent policy naming Charge.create/3, PaymentIntent.create/3, and the exact direct-server confirmation example; docs_truth_test.exs proves both surfaces."
    - "D-12: the guide distinguishes direct server confirmation from browser/client-SDK confirmation and states that customer action/SCA may still be required; the bounded docs-truth test proves the distinction."
    - "D-13/D-14: section-scoped assertions plus the existing Charge module-surface test jointly prove prose and structural absence without broad repository grep."
    - "Fallback edge DOC-02/idempotency: repeated bounded extraction of each canonical source yields identical policy text; docs_truth_test.exs proves equality and exact two-surface ownership."
    - "Fallback edge DOC-02/concurrency: parallel readers of the committed moduledoc and guide section observe identical immutable policy content; docs_truth_test.exs proves concurrent extraction equality."
  artifacts:
    - path: lib/lattice_stripe/charge.ex
      provides: "Canonical permanent Charge read/reconciliation policy"
    - path: guides/payments.md
      provides: "Direct-server PaymentIntent example and browser/SCA distinction in Charge reconciliation"
    - path: test/lattice_stripe/docs_truth_test.exs
      provides: "Section-scoped policy, ownership, idempotency, and concurrent-reader regression contract"
  key_links:
    - from: test/lattice_stripe/docs_truth_test.exs
      to: lib/lattice_stripe/charge.ex
      via: "bounded moduledoc extraction asserts the permanent omission and replacement route"
      pattern: "Charge.create/3.*PaymentIntent.create/3"
    - from: test/lattice_stripe/docs_truth_test.exs
      to: guides/payments.md
      via: "extracts only `## Charge reconciliation` before checking the exact direct-server example and client-flow distinction"
      pattern: "## Charge reconciliation"
  prohibitions:
    - requirement: DOC-02
      category: safety
      statement: "The payment guidance must not imply that server-side confirmation universally removes customer action, authentication, or SCA requirements."
      status: unverified
      flagged: true
    - requirement: DOC-02
      category: product-values
      statement: "The permanent Charge policy must not silently expand into a Charge.create API or duplicate its full explanation beyond the two locked canonical surfaces."
      status: unverified
      flagged: true
---

<objective>
Make the PaymentIntent-first Charge creation policy permanent, task-oriented, and mechanically scoped to its two canonical consumer surfaces.

Purpose: Prevent adopters from looking for a Charge mutation API or misreading direct server confirmation as a universal escape from client authentication flows.
Output: Durable Charge moduledoc, exact PaymentIntent server example, browser/SCA distinction, and section-scoped docs-truth locks per D-10 through D-14.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-RESEARCH.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-PATTERNS.md

<interfaces>
`LatticeStripe.PaymentIntent.create/3` accepts a params map and options. `test/lattice_stripe/charge_test.exs` already refutes Charge create/create!/cancel/cancel! across arities. `DocsTruthTest` reads source and guide files directly and has existing payment-section ordering checks.
</interfaces>
</context>

## Artifacts this phase produces

- No new runtime symbol, field, function, or file.
- Expanded existing `LatticeStripe.Charge` moduledoc contract.
- Expanded existing `guides/payments.md` Charge-reconciliation contract.
- New named docs-truth tests for the two canonical policy surfaces and fallback edge predicates.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock and publish the permanent PaymentIntent-first Charge policy</name>
  <read_first>lib/lattice_stripe/charge.ex, lib/lattice_stripe/payment_intent.ex, guides/payments.md, README.md, test/lattice_stripe/charge_test.exs, test/lattice_stripe/docs_truth_test.exs, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>lib/lattice_stripe/charge.ex, guides/payments.md, test/lattice_stripe/docs_truth_test.exs</files>
  <behavior>
    - The Charge moduledoc and only the `## Charge reconciliation` guide section explicitly state that `LatticeStripe.Charge.create/3` will not be added and direct server initiation uses `LatticeStripe.PaymentIntent.create/3` with amount `4_999`, currency `"usd"`, payment method `"pm_card_visa"`, and `"confirm" => true` per D-10/D-11.
    - The guide explains that successful confirmation creates the Charge used for reconciliation, while browser/client flows confirm with Stripe.js or equivalent and may enter customer-action/SCA states per D-12.
    - Existing structural absence tests remain green; bounded source tests cannot be satisfied by README, scope, changelog, or planning prose per D-13/D-14.
  </behavior>
  <action>Write one named section-scoped docs-truth test before editing prose. Extract the Charge moduledoc region from `lib/lattice_stripe/charge.ex` and the `## Charge reconciliation` range from `guides/payments.md`; assert exact policy anchors, the required map values, successful-intent-to-Charge explanation, and the client-side authentication distinction. Replace Phase-number archaeology with durable consumer language in the two canonical surfaces per D-10/D-11. Keep README as its existing compact cue, leave scope/changelog/historical planning untouched per D-14, and retain the existing Charge mutation refutations per D-13. Do not imply that confirmation always reaches success without customer action.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/charge_test.exs --warnings-as-errors &amp;&amp; mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Both and only the canonical full-policy regions contain the permanent exclusion, sanctioned function, exact four-field direct-server example, resulting-Charge explanation, and browser/SCA distinction; structural Charge mutation refutations still pass.</acceptance_criteria>
  <done>DOC-02's permanent policy, exact example, SCA distinction, and two canonical surfaces are executable documentation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove canonical ownership is stable under repeated and parallel readers</name>
  <read_first>test/lattice_stripe/docs_truth_test.exs, lib/lattice_stripe/charge.ex, guides/payments.md, README.md, test/lattice_stripe/charge_test.exs, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <behavior>
    - Repeated bounded extraction of the Charge moduledoc and guide reconciliation section returns identical policy content.
    - `Task.async_stream/3` readers of both committed files observe identical bounded sections.
    - The full policy remains owned by exactly the two canonical regions while README remains only a compact cue per D-10/D-14.
  </behavior>
  <action>Add focused repeatability and parallel-reader assertions around the same bounded extraction helpers created in Task 1, resolving the DOC-02 idempotency and concurrency probes without broad repository grep. Assert ownership by naming the two canonical files/regions and checking README remains a compact route rather than copying the complete direct-server example. Keep these as deterministic file-content tests and retain the Charge function-export refutations as the runtime backstop.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/charge_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Repeated and concurrent reads return byte-equal bounded content, exactly two full-policy owners are enforced, and Charge mutation functions remain absent.</acceptance_criteria>
  <done>Both DOC-02 fallback edge rows have explicit executable predicates and cannot be satisfied by unrelated prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK documentation → adopter payment architecture | Incorrect initiation or authentication guidance can cause broken payment flows or unsafe assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-06 | Spoofing / Information Disclosure | `guides/payments.md` confirmation guidance | high | mitigate | Bound assertions to Charge reconciliation, show exact PaymentIntent API, and state client SDK/customer-action/SCA paths explicitly. |
| T-67-07 | Tampering | Charge public surface and policy prose | medium | mitigate | Pair section-scoped docs truth with existing function_exported? refutations for create/create!/cancel/cancel!. |

## ASVS L1 Control Mapping

| ASVS category | Applicability | L1 control and task mapping | Executable/docs evidence |
|---|---|---|---|
| V2 Authentication | not applicable | The plan documents payment initiation but implements no customer authentication mechanism. | Scope confirmed by the Task 1/2 file lists. |
| V3 Session Management | not applicable | No session, cookie, or token lifecycle changes. | Scope confirmed by the Task 1/2 file lists. |
| V4 Access Control | not applicable | No authorization checks or protected-resource policy changes. | Scope confirmed by the Task 1/2 file lists. |
| V5 Input Validation | not applicable | The exact PaymentIntent map is documentation truth, not a new input parser or validation boundary. | `mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/charge_test.exs --warnings-as-errors`. |
| V6 Cryptographic Integrity | not applicable | No signing, encryption, key handling, or webhook verification changes. | Scope confirmed by the Task 1/2 file lists. |

</threat_model>

<verification>
- `mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/charge_test.exs --warnings-as-errors`
- `mix docs --warnings-as-errors`
</verification>

<success_criteria>
- D-10 through D-14 are each present in bounded executable checks.
- DOC-02's repeatability and parallel-reader probes are explicitly resolved.
- No Charge mutation, payment flow, or noncanonical policy surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/67-dx-hardening-milestone-doc-close/67-04-SUMMARY.md` when done.
</output>
