---
phase: 67-dx-hardening-milestone-doc-close
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/webhook/cache_body_reader.ex
  - test/lattice_stripe/webhook/plug_test.exs
autonomous: true
requirements: [DX-03]
must_haves:
  truths:
    - "D-06: successive `:more` and terminal `:ok` chunks are accumulated in original byte order while each invocation returns Plug.Conn.read_body/2's current tag and chunk; plug_test.exs proves `abc` then `def` yields final raw_body `abcdef`."
    - "D-07: after terminal `:ok`, only `conn.private[:raw_body]` holds the exact complete binary under the fixed key; error passthrough and existing Webhook.Plug consumption remain green."
  assumptions:
    - requirement: DX-03
      category: unclassified
      statement: "Fallback edge probe could not classify DX-03; planner assumes the locked exact-byte, fixed-key, conditional-module contract plus existing Plug integration exhausts the behavior edge surface. This remains flagged for verifier review."
      status: unresolved
      flagged: true
  artifacts:
    - path: lib/lattice_stripe/webhook/cache_body_reader.ex
      provides: "Exact ordered multi-chunk raw-body accumulation behind the optional Plug guard"
      exports: ["read_body/2"]
    - path: test/lattice_stripe/webhook/plug_test.exs
      provides: "Forced multi-call byte-integrity and Webhook.Plug reachability proofs"
  key_links:
    - from: lib/lattice_stripe/webhook/cache_body_reader.ex
      to: lib/lattice_stripe/webhook/plug.ex
      via: "fixed conn.private[:raw_body] key consumed before fallback body reading"
      pattern: "private\\[:raw_body\\]"
  prohibitions:
    - requirement: DX-03
      category: safety
      statement: "The body reader must not silently become configurable retained storage, disk spooling, a multipart solution, or a default global parser recommendation."
      status: unverified
      flagged: true
---

<objective>
Correct the latent multi-chunk raw-body bug before the helper becomes a public contract.

Purpose: Ensure Stripe webhook HMAC verification receives the exact byte stream when Plug returns multiple body chunks.
Output: Production-quality `CacheBodyReader.read_body/2` accumulation and an end-to-end regression through `Webhook.Plug` per D-06 and D-07.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-RESEARCH.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-PATTERNS.md

<interfaces>
`CacheBodyReader.read_body/2` currently mirrors `Plug.Conn.read_body/2` but overwrites the fixed private key on every chunk. `Webhook.Plug.get_raw_body/1` first reads `conn.private[:raw_body]`, so exact accumulation is the signature-verification bridge.

Stable D-07 return contract:
`read_body(Plug.Conn.t(), keyword()) :: {:ok, binary(), Plug.Conn.t()} | {:more, binary(), Plug.Conn.t()} | {:error, term()}`.
</interfaces>
</context>

## Artifacts this phase produces

- Hardened existing function `LatticeStripe.Webhook.CacheBodyReader.read_body/2` with exact ordered multi-chunk accumulation.
- No new field, symbol, or file in this plan; the public exposure is completed in Plan 67-03.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve exact bytes across every Plug body chunk</name>
  <read_first>lib/lattice_stripe/webhook/cache_body_reader.ex, lib/lattice_stripe/webhook/plug.ex, lib/lattice_stripe/webhook.ex, test/lattice_stripe/webhook/plug_test.exs, guides/webhooks.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>lib/lattice_stripe/webhook/cache_body_reader.ex, test/lattice_stripe/webhook/plug_test.exs</files>
  <behavior>
    - Reading `"abcdef"` with `length: 3` returns `{:more, "abc", conn}` then `{:ok, "def", conn}` and final `conn.private[:raw_body] == "abcdef"` per D-06/D-09.
    - Single-chunk reads retain the exact existing return tuple and private value.
  </behavior>
  <action>Write the forced multi-call test before changing the reader. For both `:more` and `:ok`, append the current chunk to `Map.get(conn.private, :raw_body, "")`, store the result under the fixed `:raw_body` key, and return the original tag plus current chunk exactly per D-06/D-07. Keep the whole module inside `if Code.ensure_loaded?(Plug)`. Add no alternate key, external store, request option, multipart handling, or new consumer path.</action>
  <verify>
    <automated>mix test test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>The exact two-call sequence is asserted as `abc`, `def`, and final `abcdef`; native return/error behavior and signature-verification reachability remain green.</acceptance_criteria>
  <done>The helper is correct enough to publish without locking in the current byte-loss bug.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve error passthrough and prove Webhook.Plug consumes the completed body</name>
  <read_first>lib/lattice_stripe/webhook/cache_body_reader.ex, lib/lattice_stripe/webhook/plug.ex, lib/lattice_stripe/webhook.ex, test/lattice_stripe/webhook/plug_test.exs, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>test/lattice_stripe/webhook/plug_test.exs</files>
  <behavior>
    - `{:error, reason}` from the underlying read remains unchanged and no partial replacement value is fabricated.
    - The completed `conn.private[:raw_body]` passes through the existing signature-verification route and produces a typed `%LatticeStripe.Event{}` per D-07/D-09.
  </behavior>
  <action>Define a test-local Plug adapter whose `read_req_body/2` returns `{:error, :closed}` and assert CacheBodyReader returns that tuple unchanged. Strengthen the existing CacheBodyReader scenario so the value consumed by `Webhook.Plug` is the completed multi-chunk binary produced in Task 1 rather than a manually inserted stand-in. Keep signature construction and typed-event expectations in the existing test module; do not create a second webhook verification path.</action>
  <verify>
    <automated>mix test test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>Error behavior is unchanged and the exact accumulated body is demonstrably reachable through public Webhook.Plug signature verification.</acceptance_criteria>
  <done>The byte accumulator is proven at both its native Plug boundary and its real SDK consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request socket → Plug body reader | Untrusted body bytes arrive incrementally and must retain exact order and content. |
| `conn.private[:raw_body]` → Webhook HMAC verification | Any truncation, duplication, or reordering invalidates the signed message. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-02 | Tampering | `CacheBodyReader.read_body/2` | high | mitigate | Accumulate every returned chunk in order and prove a forced `:more`→`:ok` sequence plus downstream signature verification. |
| T-67-03 | Information Disclosure / Denial of Service | `conn.private[:raw_body]` | medium | mitigate | Retain only one connection-lifetime binary under the fixed key; publication guidance in 67-03 limits this to the advanced non-multipart route. |

## ASVS L1 Control Mapping

| ASVS category | Applicability | L1 control and task mapping | Executable/docs evidence |
|---|---|---|---|
| V2 Authentication | not applicable | No adopter authentication or identity mechanism changes. | Scope confirmed by the Task 1/2 file lists. |
| V3 Session Management | not applicable | Plug connection-private request data is not an application session. | Scope confirmed by the Task 1/2 file lists. |
| V4 Access Control | not applicable | The reader performs no authorization decision. | Scope confirmed by the Task 1/2 file lists. |
| V5 Input Validation | not applicable | This plan preserves opaque request bytes; interpreting or normalizing them would violate the signature boundary. | Exact opaque-byte behavior is exercised by `plug_test.exs`. |
| V6 Cryptographic Integrity | applicable | Tasks 1 and 2 preserve every webhook byte in order so the existing HMAC verifier receives the exact signed message per D-06/D-07/D-09. | `mix test test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors`. |

</threat_model>

<verification>
- `mix test test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors`
- `mix format --check-formatted`
</verification>

<success_criteria>
- D-06 and D-07 are executable invariants, not documentation-only claims.
- The existing optional Plug compile boundary and Webhook.Plug consumer remain intact.
- The unresolved DX-03 classification is carried explicitly for verifier review.
</success_criteria>

<output>
Create `.planning/phases/67-dx-hardening-milestone-doc-close/67-02-SUMMARY.md` when done.
</output>
