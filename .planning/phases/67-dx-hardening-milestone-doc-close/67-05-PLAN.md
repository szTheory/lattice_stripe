---
phase: 67-dx-hardening-milestone-doc-close
plan: "05"
type: execute
wave: 3
depends_on: [67-01, 67-03, 67-04]
files_modified:
  - .planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md
  - .planning/phases/67-dx-hardening-milestone-doc-close/67-PHASE-BASE.md
  - .planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md
autonomous: false
requirements: [DX-02, DX-03, DOC-02]
must_haves:
  truths:
    - "D-15/D-16: zero warnings is the only documentation baseline; `mix docs --warnings-as-errors`, docs truth, API lock, and `mix ci` all pass with current observed evidence recorded in 67-VALIDATION.md."
    - "D-17: `.planning/v1.10-MILESTONE-AUDIT.md` retains SHA-256 `96d0ee3584c074566aaf3f3c516d005bb227e4916268b00cecf0073ba09d2726`; after strict gates, the normal milestone-audit workflow produces `.planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md` as current evidence."
    - "D-18: neither retry-telemetry nor Batch flaky-test investigation appears in the implementation diff; an incidental known flake is reported under the existing policy and never converted into weaker acceptance."
  artifacts:
    - path: .planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md
      provides: "Reconciled task rows and exact strict-docs/API/full-CI evidence"
    - path: .planning/phases/67-dx-hardening-milestone-doc-close/67-PHASE-BASE.md
      provides: "Persisted Phase 67 execution base derived from the first Phase 67 task commit for committed and worktree scope auditing"
    - path: .planning/phases/67-dx-hardening-milestone-doc-close/COVERAGE.md
      provides: "Only when the deterministic final-scope detector requires it: reasoned no-new-external-integration declaration"
    - path: .planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md
      provides: "Current post-Phase-67 milestone evidence produced by the normal audit workflow without overwriting the historical audit"
  key_links:
    - from: mix.exs
      to: .github/workflows/ci.yml
      via: "mix ci and the required Quality job both retain docs warnings-as-errors enforcement"
      pattern: "docs --warnings-as-errors"
    - from: .planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md
      to: test/lattice_stripe/docs_truth_test.exs
      via: "named passing checks recorded as mechanical evidence under the lattice verification policy"
      pattern: "docs_truth_test.exs"
  prohibitions:
    - requirement: DX-02
      category: safety
      statement: "Phase closure must not weaken a strict gate, reinterpret a known flake as success, or replace zero-warning evidence with a differential baseline."
      status: unverified
      flagged: true
---

<objective>
Converge every Phase 67 seam under the repository's strict documentation, public API, and full-CI gates, then hand current evidence to the normal milestone re-audit workflow.

Purpose: Close v1.10 with executable proof while preserving the historical pre-Phase-67 audit and unrelated user-owned files.
Output: Validated phase evidence, deterministic API-coverage disposition, and a post-phase audit handoff per D-15 through D-18.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-01-SUMMARY.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-03-SUMMARY.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-04-SUMMARY.md
@.agents/skills/lattice-verification-policy/SKILL.md

<interfaces>
`mix ci` runs format, warnings-strict compile, Credo, full ExUnit, API surface check, version prose, and strict docs. `.github/workflows/ci.yml` Quality also runs `mix docs --warnings-as-errors`. The project verification policy permits `human_judgment: false` only when a named executable check passes. `$gsd-audit-milestone v1.10` accepts the milestone version only and writes the standard `.planning/v1.10-MILESTONE-AUDIT.md`; `$gsd-workspace --new` supports `--name`, `--repos`, `--path`, `--strategy`, `--branch`, and `--auto`, while `$gsd-workspace --remove NAME` performs dirty-worktree checks and exact worktree cleanup. Task 3 therefore runs the normal audit in an isolated workspace and copies its standard report to D-17's distinct primary path.
</interfaces>
</context>

## Artifacts this phase produces

- No new runtime symbol, field, or function.
- Updated existing file `67-VALIDATION.md` with observed named-check evidence and `nyquist_compliant: true` only after every required gate passes.
- `67-PHASE-BASE.md` with the literal first Phase 67 task commit, its parent SHA, and the derivation command used by the commit-aware D-18 audit.
- `COVERAGE.md` only if the final deterministic detector returns `detected: true`; its sole allowed content is a reasoned no-new-external-integration declaration, never fabricated capability rows.

## Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Consumers can honor Retry-After, rely on public CacheBodyReader, and find permanent Charge policy | 67-01..67-05 | COVERED | Three seams plus strict convergence gate. |
| REQ | DX-02 | Error headers and parsed retry_after | 67-01 | COVERED | D-01..D-05. |
| REQ | DX-03 | Public semver-covered CacheBodyReader | 67-02, 67-03 | COVERED | Correctness precedes publication; D-06..D-09. |
| REQ | DOC-02 | Permanent Charge.create exclusion and PaymentIntent route | 67-04 | COVERED | D-10..D-14. |
| RESEARCH | — | Strict parser, all response paths, terminal-attempt evidence | 67-01 | COVERED | JSON, non-JSON, download, connection, retry context. |
| RESEARCH | — | Exact multi-chunk accumulation and optional Plug publication | 67-02, 67-03 | COVERED | Correctness, docs, grouping, API lock. |
| RESEARCH | — | Section-scoped prose locks and strict phase gates | 67-04, 67-05 | COVERED | No broad grep or differential warning baseline. |
| CONTEXT | D-01..D-05 | Error metadata decisions | 67-01 | COVERED | Every ID cited in truths/tasks. |
| CONTEXT | D-06..D-09 | Body-reader decisions | 67-02, 67-03 | COVERED | Every ID cited in truths/tasks. |
| CONTEXT | D-10..D-14 | Charge policy decisions | 67-04 | COVERED | Every ID cited in truths/tasks. |
| CONTEXT | D-15..D-18 | Milestone documentation close | 67-05 | COVERED | Historical audit stays read-only. |

## Prohibition Recall Audit

Stage 1 adversarial recall surfaced routine correctness candidates (nil/default fields, malformed integers, chunk order, docs-section placement, generator idempotency), product-specific safety candidates, and canonical security candidates.

Stage 2 disposition:

- Kept as descriptor-less flagged/unverified prohibitions: no SDK-owned blocking/scheduling policy (DX-02); no configurable/global/multipart body-retention promise (DX-03); no universal no-SCA claim and no Charge mutation/full-policy spread (DOC-02).
- Dropped as routine engineering because executable truths/tests own them: struct defaults, strict integer boundaries, chunk ordering, bounded section extraction, and generator repeatability.
- Canon breadcrumb: generic header injection/log-redaction and generic PII/DoS controls are covered by this phase's STRIDE register plus `$gsd-secure-phase`; they are not minted again as bespoke prohibitions.

<tasks>

<task type="auto">
  <name>Task 1: Sample focused Phase 67, strict ExDoc, and public API evidence</name>
  <read_first>.planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md, mix.exs, .github/workflows/ci.yml, CONTRIBUTING.md, test/lattice_stripe/docs_truth_test.exs, test/lattice_stripe/api_surface_lock_test.exs, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md, .agents/skills/lattice-verification-policy/SKILL.md</read_first>
  <files>.planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md</files>
  <action>Run and record the focused Phase 67 suite first, then the standalone strict ExDoc build and API surface check per D-15/D-16. Update the matching 67-VALIDATION.md rows with observed command results and named test evidence, but leave final status and Nyquist sign-off pending until Task 2 completes the full project gate. Keep zero warnings as the sole documentation state; do not replace it with a warning count or differential baseline.</action>
  <verify>
    <automated>mix test test/lattice_stripe/error_test.exs test/lattice_stripe/client_test.exs test/lattice_stripe/retry_strategy_test.exs test/lattice_stripe/webhook/plug_test.exs test/lattice_stripe/charge_test.exs test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/api_surface_lock_test.exs --warnings-as-errors &amp;&amp; mix docs --warnings-as-errors &amp;&amp; mix lattice_stripe.api_surface --check</automated>
  </verify>
  <acceptance_criteria>Focused tests pass before the full gate, ExDoc reports zero warnings, the API lock is exact, and the corresponding validation rows record observed evidence rather than recalled counts.</acceptance_criteria>
  <done>The fast-feedback evidence is green and recorded before full convergence begins.</done>
</task>

<task type="auto">
  <name>Task 2: Run full CI, preserve scope evidence, and finalize validation</name>
  <read_first>.planning/STATE.md, .planning/ROADMAP.md, .planning/v1.10-MILESTONE-AUDIT.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md, .planning/phases/67-dx-hardening-milestone-doc-close/COVERAGE.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>.planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-PHASE-BASE.md</files>
  <action>After Task 1's focused evidence is green, run `mix ci` as the final D-16 convergence gate. Run the installed `api-coverage.cjs` detector over the final Phase 67 ROADMAP section plus all PLAN bodies. If it reports false, confirm `COVERAGE.md` is absent; if true, require and validate the existing reasoned `No external API integration:` declaration without inventing capability rows. Derive the immutable Phase 67 execution base from repository history: select the oldest ancestor commit whose subject matches the executor convention `type(67-NN): ...`, take that commit's first parent, and persist both literal SHAs plus the derivation command in `67-PHASE-BASE.md`. Fail closed if no matching task commit exists or the base is not an ancestor of HEAD. Use that persisted base for the required `git diff --name-only <base>..HEAD` committed-path inventory and compare it with staged and unstaged inventories. Per D-18, require the exact Batch flake test `one failing task returns {:error, %Error{}} in its slot, others succeed` and the exact retry-telemetry flake test `stop event metadata includes attempts and retries counts` to be byte-identical to their base versions; also require `lib/lattice_stripe/telemetry.ex` and the `Client.maybe_retry/5` block containing `LatticeStripe.Telemetry.emit_retry/5` to be byte-identical. This permits the intentional final-attempt header tests elsewhere in `client_test.exs` while excluding retry-telemetry investigation. Require the full research-cache status and deterministic content fingerprint to remain exactly the planning-time baseline (`?? .planning/research/.cache/dda606cfeda5d4a510c525a62aca3da5743d07123b8fe9eff3f7a3ab1aae6af2.json`; SHA-256 `e6f22a0f9abbaf0822b178c301fc2a4bae22ff67fc8cb6faa33340393e54c1c4`). Verify the historical audit's fixed SHA-256 before audit handoff. Record the base, committed/staged/unstaged inventories, protected-block comparisons, cache status/fingerprint, and observed command results in `67-VALIDATION.md`. Only after all checks pass, set `status: validated`, `nyquist_compliant: true`, and complete the sign-off.</action>
  <verify>
    <automated>set -euo pipefail; phase_dir=.planning/phases/67-dx-hardening-milestone-doc-close; evidence="$phase_dir/67-PHASE-BASE.md"; first_commit=$(git log --reverse --format=%H --extended-regexp --grep='^[a-z]+\(67-[0-9][0-9]\):' HEAD | sed -n '1p'); test -n "$first_commit"; base=$(git rev-parse "$first_commit^"); git merge-base --is-ancestor "$base" HEAD; printf '%s\n' '# Phase 67 execution base' '' "first_task_commit: $first_commit" "base_sha: $base" 'derivation: oldest HEAD-ancestor commit matching ^[a-z]+\(67-[0-9][0-9]\):, then first parent' &gt; "$evidence"; GSD_API_COVERAGE=/Users/jon/.codex/gsd-core/bin/lib/api-coverage.cjs; mix ci; scope="$(sed -n '/^### Phase 67:/,/^### Phase /p' .planning/ROADMAP.md; sed -n '1,99999p' "$phase_dir"/*-PLAN.md)"; printf '%s' "$scope" | node "$GSD_API_COVERAGE" --json; git diff --name-only "$base..HEAD"; git diff --cached --name-only; git diff --name-only; extract_named_test() { awk -v needle="$1" 'index($0, "test \"" needle "\" do") {printing=1} printing &amp;&amp; seen &amp;&amp; /^    test \"/ {exit} printing {print; seen=1}' ; }; extract_maybe_retry() { awk '/^  defp maybe_retry\(/ {printing=1} printing &amp;&amp; seen &amp;&amp; /^  defp / {exit} printing {print; seen=1}' ; }; test "$(git show "$base:test/lattice_stripe/batch_test.exs" | extract_named_test 'one failing task returns {:error, %Error{}} in its slot, others succeed')" = "$(extract_named_test 'one failing task returns {:error, %Error{}} in its slot, others succeed' &lt; test/lattice_stripe/batch_test.exs)"; test "$(git show "$base:test/lattice_stripe/client_test.exs" | extract_named_test 'stop event metadata includes attempts and retries counts')" = "$(extract_named_test 'stop event metadata includes attempts and retries counts' &lt; test/lattice_stripe/client_test.exs)"; git diff --quiet "$base" -- lib/lattice_stripe/telemetry.ex; test "$(git show "$base:lib/lattice_stripe/client.ex" | extract_maybe_retry)" = "$(extract_maybe_retry &lt; lib/lattice_stripe/client.ex)"; cache_status=$(git status --porcelain=v1 --untracked-files=all -- .planning/research/.cache/); test "$cache_status" = '?? .planning/research/.cache/dda606cfeda5d4a510c525a62aca3da5743d07123b8fe9eff3f7a3ab1aae6af2.json'; cache_fingerprint=$(find .planning/research/.cache -type f -print0 | sort -z | while IFS= read -r -d '' cache_file; do printf '%s\0%s\n' "${cache_file#.planning/research/.cache/}" "$(git hash-object "$cache_file")"; done | shasum -a 256 | awk '{print $1}'); test "$cache_fingerprint" = e6f22a0f9abbaf0822b178c301fc2a4bae22ff67fc8cb6faa33340393e54c1c4; printf '%s  %s\n' '96d0ee3584c074566aaf3f3c516d005bb227e4916268b00cecf0073ba09d2726' '.planning/v1.10-MILESTONE-AUDIT.md' | shasum -a 256 -c -</automated>
  </verify>
  <acceptance_criteria>Full CI passes after the focused sample; the detector has a valid false/declared-no-integration disposition; `67-PHASE-BASE.md` contains an ancestor base derived from the oldest Phase 67 executor commit; committed, staged, and unstaged path inventories are recorded; both exact flaky-test blocks, the retry telemetry source seam, and the research-cache status/content are unchanged from their explicit baselines; the historical audit hash matches; final validation is signed off.</acceptance_criteria>
  <done>All DX-02, DX-03, DOC-02, scope, and Nyquist gates qualify for mechanical verification under the project policy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Produce and transfer the current audit from an isolated detached workspace</name>
  <read_first>.planning/v1.10-MILESTONE-AUDIT.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md, /Users/jon/.agents/skills/gsd-audit-milestone/SKILL.md, /Users/jon/.agents/skills/gsd-workspace/SKILL.md, /Users/jon/.codex/gsd-core/workflows/audit-milestone.md, /Users/jon/.codex/gsd-core/workflows/new-workspace.md, /Users/jon/.codex/gsd-core/workflows/remove-workspace.md</read_first>
  <files>.planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md</files>
  <action>Per D-17, use the supported workspace and audit interfaces exactly; there is no audit output-path override. From the primary repository after Task 2's commit, verify the fixed historical SHA-256 and capture `git rev-parse HEAD` as the post-Phase-67 commit. Invoke `$gsd-workspace --new --name lattice-stripe-phase67-audit --repos /Users/jon/projects/lattice_stripe --path /Users/jon/gsd-workspaces/lattice-stripe-phase67-audit --strategy worktree --branch workspace/lattice-stripe-phase67-audit --auto`. Enter `/Users/jon/gsd-workspaces/lattice-stripe-phase67-audit/lattice_stripe`, run `git switch --detach &lt;post-phase-67-sha&gt;`, and require `git symbolic-ref -q HEAD` to fail so the audit is detached at exactly that commit. Require the standard audit path to be absent there before invocation; this proves the primary untracked historical artifact was not copied into the isolated worktree. From that member-repository cwd invoke `$gsd-audit-milestone v1.10`, allowing it to write only its supported standard `.planning/v1.10-MILESTONE-AUDIT.md`. Require non-empty v1.10 frontmatter, an `audited` UTC date equal to the current UTC date, status `passed` or `tech_debt`, and a Phase 67 marker/evidence row. Copy that exact file to `/Users/jon/projects/lattice_stripe/.planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md`, then verify the destination is non-empty, has the same SHA-256 as the isolated source, names Phase 67, and is a different path from the protected historical file. Recheck the primary historical SHA-256. Before cleanup, require the isolated member repo's only dirty path to be `.planning/v1.10-MILESTONE-AUDIT.md`; if any other path changed, stop and preserve the workspace for diagnosis. Remove only that exact generated isolated report, require the member repo clean, then invoke `$gsd-workspace --remove lattice-stripe-phase67-audit` and type the exact workspace name at its blocking confirmation. Do not use manual recursive deletion, broad cleanup, or touch any primary untracked file.</action>
  <instructions>Run the commands from the exact cwd stated above. Workspace creation and removal are blocking GSD skill invocations: use `$gsd-workspace --new ... --auto`, then later `$gsd-workspace --remove lattice-stripe-phase67-audit`; run `$gsd-audit-milestone v1.10` only inside the detached member repository. Resume with the post-Phase-67 SHA, isolated report SHA-256, copied report SHA-256, audited date/status, Phase 67 marker line, primary historical hash result, clean isolated `git status --porcelain=v1 --untracked-files=all`, and workspace-removal confirmation.</instructions>
  <verification>Run the automated primary-tree gate below after the supported remove workflow reports success; it proves the transferred report is fresh/current and the historical artifact is still byte-identical.</verification>
  <verify><automated>set -euo pipefail; report=.planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md; test -s "$report"; test "$(sed -n 's/^milestone: //p' "$report" | head -n 1)" = v1.10; audited=$(sed -n 's/^audited: //p' "$report" | head -n 1); test "${audited%%T*}" = "$(date -u +%F)"; rg -q '^status: (passed|tech_debt)$' "$report"; rg -q 'Phase 67|67-dx-hardening|DX-02|DX-03|DOC-02' "$report"; test "$report" != .planning/v1.10-MILESTONE-AUDIT.md; printf '%s  %s\n' '96d0ee3584c074566aaf3f3c516d005bb227e4916268b00cecf0073ba09d2726' '.planning/v1.10-MILESTONE-AUDIT.md' | shasum -a 256 -c -; test ! -e /Users/jon/gsd-workspaces/lattice-stripe-phase67-audit</automated></verify>
  <acceptance_criteria>The supported normal audit ran in a detached workspace at the post-Phase-67 commit; its standard report has current-date Phase 67 evidence and a non-blocking audit status; the byte-identical copy exists only at the primary post-phase path; the primary historical audit retains its fixed SHA-256; and the supported workspace-removal workflow removed only the named clean workspace.</acceptance_criteria>
  <resume-signal>Type `audit-transferred` only with the requested SHA/date/status/marker/hash/clean-status/removal evidence after every gate passes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase artifacts → release/milestone evidence | Stale or selectively weakened evidence could falsely certify the release. |
| External HTTP/request bytes → public SDK metadata | The convergence gate must retain response-disclosure, byte-integrity, memory-retention, and non-blocking mitigations from earlier plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-01 | Information Disclosure / Input Ambiguity | Error metadata | high | mitigate | Require focused strict parser/propagation tests and safe guide contract before closure. |
| T-67-02 | Tampering | webhook raw body | high | mitigate | Require forced multi-chunk exact-byte test and Webhook.Plug integration. |
| T-67-03 | Information Disclosure / Denial of Service | global raw-body retention | high | mitigate | Require bounded docs truth for advanced positioning, PII/no-log, extra-copy, and multipart limits. |
| T-67-04 | Denial of Service | Phoenix Retry-After handling | medium | mitigate | Require non-blocking background-work guidance and no SDK scheduler/sleep changes. |
| T-67-08 | Repudiation | milestone evidence | medium | mitigate | Record exact commands/results, preserve historical audit bytes, and require a fresh post-phase audit workflow. |

## ASVS L1 Control Mapping

| ASVS category | Applicability | L1 control and task mapping | Executable/docs evidence |
|---|---|---|---|
| V2 Authentication | not applicable | Phase convergence introduces no authentication or identity control. | Focused and full-scope file lists plus `mix ci`. |
| V3 Session Management | not applicable | No session lifecycle exists in this headless library phase. | Focused and full-scope file lists plus `mix ci`. |
| V4 Access Control | not applicable | No authorization decision is added or changed. | Focused and full-scope file lists plus `mix ci`. |
| V5 Input Validation | applicable | Task 1 reruns the strict D-03 delay-seconds parser and all response propagation paths; Task 2 retains them in the full gate. | Focused Error/Client/RetryStrategy tests, `mix docs --warnings-as-errors`, and `mix ci`. |
| V6 Cryptographic Integrity | applicable | Task 1 reruns exact multi-chunk webhook-byte and Webhook.Plug verification evidence; Task 2 retains it in the full gate. | `mix test test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors` within the focused suite and `mix ci`. |

</threat_model>

<verification>
- Focused Phase 67 ExUnit suite with warnings-as-errors.
- `mix docs --warnings-as-errors`.
- `mix lattice_stripe.api_surface --check`.
- `mix ci`.
- Deterministic final-scope API-coverage detector and protected-path diff check.
- `$gsd-audit-milestone v1.10` produces `.planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md`; fixed SHA-256 proves the historical audit is unchanged.
</verification>

<success_criteria>
- D-15 through D-18 close with strict current evidence and no stale warning baseline.
- Every ROADMAP goal, requirement, research constraint, and D-01 through D-18 decision is covered by the five-plan source audit.
- All five fallback edge rows are either executable truths or explicit flagged assumptions; no probe item is silently dropped.
- The historical milestone audit and user-owned research cache remain untouched; the next workflow reruns the milestone audit against current evidence.
</success_criteria>

<output>
Create `.planning/phases/67-dx-hardening-milestone-doc-close/67-05-SUMMARY.md` only after the normal milestone-audit workflow has produced `.planning/v1.10-POST-PHASE-67-MILESTONE-AUDIT.md` and preserved the historical audit hash.
</output>
