---
phase: 54-release-truth-capstone
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - README.md
  - guides/getting-started.md
  - guides/cheatsheet.cheatmd
  - guides/webhooks-thin-events.md
  - guides/opentelemetry.md
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "All seven public install surfaces show {:lattice_stripe, \"~> 1.7\"}."
    - "README release-status and HexDocs index reflect v1.4–v1.7 shipped surface."
  artifacts:
    - path: "README.md"
      provides: "REL-03 install + release narrative"
      contains: '~> 1.7'
  key_links:
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "release-status link"
      pattern: "CHANGELOG.md"
---

<objective>
Lockstep flip of public install anchors to `~> 1.7` and refresh README release-status + HexDocs index per CONTEXT D-01 and D-03.

Purpose: REL-03 doc half — atomic install truth before docs_truth_test migration (54-03).

Output: Seven surfaces at `~> 1.7`; README milestone blockquote; tax.html + Charge.html in HexDocs clusters.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-release-truth-capstone/54-CONTEXT.md
@.planning/phases/54-release-truth-capstone/54-RESEARCH.md
@README.md
@guides/getting-started.md
@guides/cheatsheet.cheatmd
@guides/webhooks-thin-events.md
@guides/production-checklist.md
@guides/event-debugging.md
@guides/opentelemetry.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Partial flip leaves mixed install bands | high | Touch all 7 surfaces in one commit; verify with rg |
| api_stability.md accidentally flipped | medium | Explicitly exclude; only change listed files |
| Stale 1.3.x release-status after flip | medium | Replace blockquote per D-03 Option B |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lockstep install-line flip (7 surfaces)</name>
  <files>README.md, guides/getting-started.md, guides/cheatsheet.cheatmd, guides/webhooks-thin-events.md, guides/opentelemetry.md</files>
  <read_first>
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-01 lockstep file list)
    - README.md (Installation block ~line 53)
    - guides/getting-started.md (install snippet)
    - guides/cheatsheet.cheatmd, guides/webhooks-thin-events.md, guides/opentelemetry.md
    - guides/production-checklist.md, guides/event-debugging.md (verify already 1.7 — do not regress)
  </read_first>
  <action>
    Replace every `{:lattice_stripe, "~> X.Y"}` install snippet with exactly:
    ```elixir
    {:lattice_stripe, "~> 1.7"}
    ```
    on these files only:
    1. `README.md` — Installation `deps` block (`~> 1.3` → `~> 1.7`)
    2. `guides/getting-started.md` (`~> 1.3` → `~> 1.7`)
    3. `guides/cheatsheet.cheatmd` (`~> 1.3` → `~> 1.7`)
    4. `guides/webhooks-thin-events.md` (`~> 1.5` → `~> 1.7`)
    5. `guides/opentelemetry.md` (`~> 1.1` → `~> 1.7`)

    **Verify-only** (must already contain `~> 1.7`, fix if not):
    6. `guides/production-checklist.md`
    7. `guides/event-debugging.md`

    **Do NOT edit:** `guides/api_stability.md`, `.planning/**`, `notebooks/` (optional comment only).
  </action>
  <verify>for f in README.md guides/getting-started.md guides/cheatsheet.cheatmd guides/webhooks-thin-events.md guides/production-checklist.md guides/event-debugging.md guides/opentelemetry.md; do grep -q '{:lattice_stripe, "~> 1.7"}' "$f" || exit 1; done</verify>
  <acceptance_criteria>
    - All 7 listed files contain `{:lattice_stripe, "~> 1.7"}`
    - `rg '{:lattice_stripe, "~> 1\.[1235]"' README.md guides/getting-started.md guides/cheatsheet.cheatmd guides/webhooks-thin-events.md guides/opentelemetry.md` returns no matches
    - `guides/api_stability.md` unchanged if it contains `~> 1.0` policy prose
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: README release-status blockquote + HexDocs index</name>
  <files>README.md</files>
  <read_first>
    - README.md (release-status blockquote lines 8–11, Features, Documentation clusters)
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-03 Option B + HexDocs index)
    - CHANGELOG.md (1.7.0 section for link anchor)
  </read_first>
  <action>
    1. **Replace release-status blockquote** (lines ~8–11) with D-03 Option B milestone bullets:
       - Lead: **`1.7.x`** is the current published line on Hex (or "ships as 1.7.0" pre-publish — honest wording)
       - Four bullets: **1.4** adoption/recipes, **1.5** thin events + link to `guides/webhooks-thin-events.md`, **1.6** Tax + link to `guides/tax.md`, **1.7** Charge list/search/update/capture (PI-first; no create) + operator playbooks with links to `production-checklist.md` and `event-debugging.md`
       - Link: `[CHANGELOG.md](CHANGELOG.md#170)` (adjust anchor if heading differs)
       - Do NOT include Phase 55 stop-signal language

    2. **Features — Payments section:** Add Charge support line: list/search/update/capture for reconciliation; link `LatticeStripe.Charge` moduledoc on HexDocs (`https://hexdocs.pm/lattice_stripe/LatticeStripe.Charge.html`)

    3. **HexDocs index — Payments and billing cluster** (line ~154): Add after Metering link:
       - `[Tax](https://hexdocs.pm/lattice_stripe/tax.html)`
       - `[Charge API](https://hexdocs.pm/lattice_stripe/LatticeStripe.Charge.html)` (moduledoc, no charges guide)

    4. Remove any `1.3.x` "current published surface" wording from release-status.
  </action>
  <verify>grep -q '1\.7' README.md && grep -q 'tax.html' README.md && grep -q 'LatticeStripe.Charge.html' README.md</verify>
  <acceptance_criteria>
    - README release block contains substring `1.7`
    - README contains `hexdocs.pm/lattice_stripe/tax.html`
    - README contains `LatticeStripe.Charge.html`
    - README release block does not contain `1.3.x` as current published line
    - README contains link to `CHANGELOG.md`
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
for f in README.md guides/getting-started.md guides/cheatsheet.cheatmd guides/webhooks-thin-events.md guides/production-checklist.md guides/event-debugging.md guides/opentelemetry.md; do
  grep -q '{:lattice_stripe, "~> 1.7"}' "$f" || echo "MISSING: $f"
done
grep 'tax.html' README.md
grep 'Charge.html' README.md
```
</verification>

<success_criteria>
- REL-03: All install anchors reference `~> 1.7`
- ROADMAP criterion #5: README HexDocs index includes tax + Charge
</success_criteria>
