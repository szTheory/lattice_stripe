---
phase: 54-release-truth-capstone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - CHANGELOG.md
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "Package version in mix.exs reads 1.7.0."
    - "CHANGELOG documents v1.4–v1.7 milestone work with honest Hex publish narrative."
  artifacts:
    - path: "mix.exs"
      provides: "REL-01 version bump"
      contains: '@version "1.7.0"'
    - path: "CHANGELOG.md"
      provides: "REL-02 milestone sections"
      contains: "## [1.7.0]"
  key_links:
    - from: "CHANGELOG.md"
      to: "mix.exs"
      via: "version alignment"
      pattern: "1.7.0"
---

<objective>
Bump `mix.exs` to `1.7.0` and restructure `CHANGELOG.md` per CONTEXT D-02 (four milestone sections + single Hex release narrative).

Purpose: Establish REL-01/REL-02 foundation before lockstep doc flip. Downstream plans depend on version string for docs-truth SSOT.

Output: `@version "1.7.0"` in `mix.exs`; CHANGELOG with banner, `[Unreleased]` empty placeholder, and `## [1.7.0]`, `## [1.6.0]`, `## [1.5.0]`, `## [1.4.0]` sections.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-release-truth-capstone/54-CONTEXT.md
@.planning/phases/54-release-truth-capstone/54-RESEARCH.md
@CHANGELOG.md
@mix.exs
@.planning/milestones/v1.4-ROADMAP.md
@.planning/milestones/v1.5-ROADMAP.md
@.planning/milestones/v1.6-ROADMAP.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Misleading Hex history (implying 1.4–1.6 were on Hex) | medium | Top banner states last Hex was 1.1.0; milestone sublines say "included in 1.7.0" |
| Missing WEBFIX-01 migration | low | Preserve tolerance:0 callout under 1.5.0 Fixed |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bump mix.exs to 1.7.0</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current @version and docs_source_ref/0)
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-02, D-04 — tag must match version)
  </read_first>
  <action>
    In `mix.exs`, change line `@version "1.3.0"` to `@version "1.7.0"`.
    Do NOT change `docs_source_ref/0` logic — it already uses `v#{@version}`.
  </action>
  <verify>grep -q '@version "1.7.0"' mix.exs</verify>
  <acceptance_criteria>
    - `mix.exs` contains exactly `@version "1.7.0"`
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restructure CHANGELOG for v1.4–v1.7</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (existing Unreleased 1.5.0 draft and 1.3.0 entry)
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-02 full structure)
    - .planning/milestones/v1.4-ROADMAP.md, v1.5-ROADMAP.md, v1.6-ROADMAP.md (bullet source)
    - .planning/phases/52-charge-surface-expansion/52-CONTEXT.md (Charge bullets for 1.7.0)
    - .planning/phases/53-operator-guides/53-CONTEXT.md (operator guide bullets for 1.7.0)
  </read_first>
  <action>
    Restructure `CHANGELOG.md` per CONTEXT D-02:

    1. **Top banner** (after SemVer note): state Hex last published **1.1.0**; sections 1.4.0–1.6.0 are milestone checkpoints in **1.7.0**; install `{:lattice_stripe, "~> 1.7"}`.

    2. **`## [Unreleased]`** with `_No unreleased changes._` only.

    3. **`## [1.7.0]`** with date (2026-05-27) and compare link `v1.1.0...v1.7.0` (honest Hex gap). Include:
       - `### Highlights` — jump from Hex 1.1.0; `~> 1.1` resolves to 1.7.0
       - `### Added` — Charge list/search/update/capture (PI-first; no create); operator guides `production-checklist.md`, `event-debugging.md`; install/docs-truth lockstep
       - Short upgrading paragraph from 1.1.x/1.3.x

    4. **`## [1.6.0] — Tax`** — subline: *Milestone included in 1.7.0. Not published separately to Hex.* Bullets: Tax.Calculation, Tax.Transaction, Tax.Settings, Tax.Registration, TaxId, `guides/tax.md`.

    5. **`## [1.5.0] — Thin-Event Webhooks`** — promote existing Unreleased content (WEBFIX-01, thin-event helpers, `webhooks-thin-events.md`). Remove nested `### [1.5.0]` and pre-release blockquote. Keep WEBFIX-01 migration callout under `### Fixed`.

    6. **`## [1.4.0] — Adoption Closure`** — recipes, docs-truth baseline, discovery ladder (Phases 43–46).

    7. Keep **`## [1.3.0]`** historical entry unchanged below.

    Use `### Added`/`Changed`/`Fixed` subsections; guide paths as `` `guides/foo.md` ``.
  </action>
  <verify>grep -E '^## \[1\.[4567]\.0\]' CHANGELOG.md | wc -l | awk '{print $1 >= 4}'</verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains `## [1.7.0]`, `## [1.6.0]`, `## [1.5.0]`, `## [1.4.0]`
    - Banner mentions last Hex **1.1.0**
    - `1.6.0` section contains substring `included in 1.7.0`
    - `CHANGELOG.md` contains `WEBFIX-01` under 1.5.0 area
    - `[Unreleased]` does not contain nested `### [1.5.0]`
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
grep '@version "1.7.0"' mix.exs
grep -E '^## \[1\.[4567]\.0\]' CHANGELOG.md
mix compile --warnings-as-errors
```
</verification>

<success_criteria>
- REL-01: `mix.exs` version is 1.7.0
- REL-02: CHANGELOG has v1.4–v1.7 sections with honest Hex narrative
</success_criteria>
