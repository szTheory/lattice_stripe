---
phase: 54-release-truth-capstone
plan: 04
type: execute
wave: 3
depends_on: [54-03]
files_modified:
  - .release-please-manifest.json
  - release-please-config.json
autonomous: false
requirements: [REL-04]
must_haves:
  truths:
    - "Package lattice_stripe 1.7.0 is published on Hex.pm."
    - "release-please manifest matches published version."
  artifacts:
    - path: ".release-please-manifest.json"
      provides: "manifest sync"
      contains: '"1.7.0"'
  key_links:
    - from: "Hex.pm"
      to: "mix.exs"
      via: "version 1.7.0"
      pattern: "1.7.0"
---

<objective>
Publish `lattice_stripe` **1.7.0** to Hex.pm and sync release tooling per CONTEXT D-04.

Purpose: REL-04 capstone — single Hex artifact spanning v1.4–v1.7 milestone work.

Output: Hex package live at 1.7.0; `v1.7.0` git tag; manifest `"1.7.0"`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-release-truth-capstone/54-CONTEXT.md
@.planning/phases/54-release-truth-capstone/54-RESEARCH.md
@.github/workflows/release.yml
@.release-please-manifest.json
@release-please-config.json
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Publish without green CI | high | Run full preflight checklist before publish |
| Missing git tag breaks ExDoc source_ref | medium | Tag v1.7.0 before hex.publish |
| Wrong tarball (wrong version) | high | hex.publish --dry-run + hex.info verification |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Preflight gate</name>
  <files></files>
  <read_first>
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-04 preflight checklist)
    - mix.exs, CHANGELOG.md, README.md
  </read_first>
  <action>
    Run preflight commands in order; stop on first failure:
    ```bash
    mix format --check-formatted
    mix compile --warnings-as-errors
    mix credo --strict
    mix test --warnings-as-errors
    mix docs
    mix hex.build
    mix hex.build --unpack
    mix hex.publish --dry-run
    ```
    Record output summary in plan SUMMARY.md.
  </action>
  <verify>mix test --warnings-as-errors</verify>
  <acceptance_criteria>
    - All preflight commands exit 0
    - `mix hex.publish --dry-run` completes without error
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 2: Merge, tag, and publish to Hex (maintainer)</name>
  <files>.release-please-manifest.json</files>
  <read_first>
    - .planning/phases/54-release-truth-capstone/54-CONTEXT.md (D-04 publish steps)
    - .github/workflows/release.yml
  </read_first>
  <action>
    **Maintainer manual steps** (requires HEX_API_KEY):

    1. Merge Phase 54 PR to `main`; `git push origin main`
    2. `git tag -a v1.7.0 -m "Release 1.7.0"`; `git push origin v1.7.0`
    3. `mix hex.publish --yes`
    4. Verify: `mix hex.info lattice_stripe 1.7.0` and https://hexdocs.pm/lattice_stripe/1.7.0
    5. Set `.release-please-manifest.json` to `{"." : "1.7.0"}`
    6. Optionally add one-shot `"release-as": "1.7.0"` in `release-please-config.json`, remove after successful publish (Phase 19 pattern)

    Do NOT publish intermediate 1.4/1.5/1.6 versions.
  </action>
  <verify>mix hex.info lattice_stripe 1.7.0</verify>
  <acceptance_criteria>
    - `mix hex.info lattice_stripe 1.7.0` shows version 1.7.0
    - `.release-please-manifest.json` contains `"1.7.0"`
    - Git tag `v1.7.0` exists on remote
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix hex.info lattice_stripe 1.7.0
grep '"1.7.0"' .release-please-manifest.json
git tag -l 'v1.7.0'
```
</verification>

<success_criteria>
- REL-04: Package published to Hex.pm at 1.7.0
- Manifest synced to 1.7.0
</success_criteria>
