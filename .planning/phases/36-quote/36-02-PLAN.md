---
phase: 36-quote
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - lib/lattice_stripe/quote.ex
  - test/lattice_stripe/quote_test.exs
  - test/integration/quote_integration_test.exs
  - test/support/fixtures/quote.ex
autonomous: true
requirements:
  - QUOT-01
  - QUOT-02
  - QUOT-03
  - QUOT-04
  - QUOT-05

must_haves:
  truths:
    - "Developers can create, retrieve, update, list, and stream Quote resources through a Stripe-shaped public API"
    - "Developers can finalize, accept, and cancel quotes via explicit verbs with the locked param/no-param semantics"
    - "Both Quote line-item endpoint families return typed `%LatticeStripe.Quote.LineItem{}` items through list, bang, and stream helpers"
    - "`Quote.pdf/3` returns raw binary at the resource layer and does not leak `%Response{}` to callers"
    - "Unit tests lock request shapes, parser behavior, bang helpers, expandable accept responses, and binary PDF behavior"
  artifacts:
    - path: "lib/lattice_stripe/quote.ex"
      provides: "Complete public Quote API surface"
      exports: ["create/3", "retrieve/3", "update/4", "list/3", "stream!/3", "finalize/4", "accept/3", "cancel/3", "list_line_items/4", "list_computed_upfront_line_items/4", "pdf/3"]
    - path: "test/lattice_stripe/quote_test.exs"
      provides: "Focused Quote unit coverage"
      contains: "QuoteTest"
    - path: "test/integration/quote_integration_test.exs"
      provides: "Quote route-sanity integration coverage"
      contains: "@moduletag :integration"
  key_links:
    - from: "lib/lattice_stripe/quote.ex"
      to: "lib/lattice_stripe/client.ex"
      via: "binary PDF path"
      pattern: "Client\\.download"
    - from: "lib/lattice_stripe/quote.ex"
      to: "lib/lattice_stripe/resource.ex"
      via: "tuple/list/bang unwrapping"
      pattern: "Resource\\.(unwrap_singular|unwrap_list|unwrap_bang!)"
    - from: "test/lattice_stripe/quote_test.exs"
      to: "test/support/fixtures/quote.ex"
      via: "fixture-backed request-shape and parser coverage"
      pattern: "Fixtures\\.Quote"
---

<objective>
Implement the public Quote API surface, lifecycle verbs, both line-item endpoint families, and binary PDF access, then prove the behavior with focused unit and integration tests.

Purpose: This plan delivers the actual developer-facing Phase 36 functionality while preserving the Stripe-shaped resource boundary and the explicit-lifecycle ergonomics locked in context.
Output: Completed `LatticeStripe.Quote` public API plus test coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-quote/36-CONTEXT.md
@.planning/phases/36-quote/36-RESEARCH.md
@.planning/phases/36-quote/36-PATTERNS.md
@.planning/phases/36-quote/36-VALIDATION.md
@.planning/phases/36-quote/36-01-PLAN.md

<interfaces>
From `lib/lattice_stripe/client.ex`:
```elixir
@spec download(t(), String.t(), keyword()) :: {:ok, Response.t()} | {:error, Error.t()}
def download(%__MODULE__{} = client, path, opts \\ [])
```

From `lib/lattice_stripe/invoice.ex`:
```elixir
@spec finalize(Client.t(), String.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
def finalize(%Client{} = client, id, params \\ %{}, opts \\ []) when is_binary(id)
```

From `lib/lattice_stripe/dispute.ex`:
```elixir
@spec close(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
def close(%Client{} = client, id, opts \\ []) when is_binary(id)
```

From `lib/lattice_stripe/checkout/session.ex`:
```elixir
@spec list_line_items(Client.t(), String.t(), map(), keyword()) ::
        {:ok, Response.t()} | {:error, Error.t()}
@spec stream_line_items!(Client.t(), String.t(), map(), keyword()) :: Enumerable.t()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Quote CRUDL, lifecycle verbs, and lifecycle-focused docs</name>
  <files>
    lib/lattice_stripe/quote.ex
    test/lattice_stripe/quote_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/quote.ex
    lib/lattice_stripe/credit_note.ex
    lib/lattice_stripe/invoice.ex
    lib/lattice_stripe/dispute.ex
    test/lattice_stripe/credit_note_test.exs
    .planning/phases/36-quote/36-CONTEXT.md
  </read_first>
  <behavior>
    - `create/3`, `retrieve/3`, `update/4`, `list/3`, and `stream!/3` follow the repo's standard resource conventions.
    - `finalize/4` accepts a raw params map plus opts and mirrors the `Invoice.finalize/4` shape.
    - `accept/3` and `cancel/3` are parameterless lifecycle verbs plus opts only.
    - Moduledoc and `@doc` text teach the quote lifecycle and keep the Stripe-vs-Accrue boundary explicit.
  </behavior>
  <action>
Extend `lib/lattice_stripe/quote.ex` from the parser contract created in Plan 01 into the public Quote module.

Add:

- `create/3`, `create!/3`
- `retrieve/3`, `retrieve!/3`
- `update/4`, `update!/4`
- `list/3`, `list!/3`
- `stream!/3`
- `finalize/4`, `finalize!/4`
- `accept/3`, `accept!/3`
- `cancel/3`, `cancel!/3`

Implementation requirements:

- `finalize/4` uses `POST /v1/quotes/:id/finalize` with raw params
- `accept/3` uses `POST /v1/quotes/:id/accept` with empty params
- `cancel/3` uses `POST /v1/quotes/:id/cancel` with empty params
- all singular endpoints use `Resource.unwrap_singular(&from_map/1)`
- list endpoints use `Resource.unwrap_list(&from_map/1)`
- stream uses `List.stream!(client, req) |> Stream.map(&from_map/1)`

Update the moduledoc to explain:

- draft -> open -> accepted/canceled lifecycle
- what `accept/3` can generate downstream
- why this module intentionally stays at low-level resource coverage instead of workflow orchestration
- the difference between embedded snapshots and paginated line-item routes

Create `test/lattice_stripe/quote_test.exs` with request-shape coverage for CRUDL and lifecycle verbs, plus parser tests for enum atomization and expanded downstream references.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/quote_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/quote.ex` contains `def create(%Client{} = client, params, opts \\\\ []) do`
    - `lib/lattice_stripe/quote.ex` contains `def finalize(%Client{} = client, id, params \\\\ %{}, opts \\\\ [])`
    - `lib/lattice_stripe/quote.ex` contains `def accept(%Client{} = client, id, opts \\\\ [])`
    - `lib/lattice_stripe/quote.ex` contains `def cancel(%Client{} = client, id, opts \\\\ [])`
    - `test/lattice_stripe/quote_test.exs` contains `describe "finalize/4" do`
    - `test/lattice_stripe/quote_test.exs` contains `describe "accept/3" do`
  </acceptance_criteria>
  <done>Quote CRUDL and lifecycle verbs exist with explicit docs and request-shape coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add both line-item endpoint families, binary PDF access, and focused unit coverage</name>
  <files>
    lib/lattice_stripe/quote.ex
    test/lattice_stripe/quote_test.exs
    test/support/fixtures/quote.ex
  </files>
  <read_first>
    lib/lattice_stripe/quote.ex
    lib/lattice_stripe/client.ex
    lib/lattice_stripe/checkout/session.ex
    lib/lattice_stripe/credit_note.ex
    test/lattice_stripe/client_test.exs
    test/support/fixtures/quote.ex
    .planning/phases/36-quote/36-CONTEXT.md
  </read_first>
  <behavior>
    - Both line-item endpoint families expose list, bang, and stream forms.
    - `Quote.pdf/3` returns raw binary and `Quote.pdf!/3` raises on failure.
    - Unit tests cover the binary unwrap contract, both line-item route families, and the docs-critical PDF precondition note.
  </behavior>
  <action>
Extend `lib/lattice_stripe/quote.ex` with:

- `list_line_items/4`, `list_line_items!/4`, `stream_line_items!/4`
- `list_computed_upfront_line_items/4`, `list_computed_upfront_line_items!/4`, `stream_computed_upfront_line_items!/4`
- `pdf/3`, `pdf!/3`

Implementation requirements:

- reuse one private list helper and one private stream helper for the two line-item path variants
- `/v1/quotes/:id/line_items` maps through `Quote.LineItem.from_map/1`
- `/v1/quotes/:id/computed_upfront_line_items` maps through the same `Quote.LineItem.from_map/1`
- `pdf/3` calls `Client.download(client, "/v1/quotes/#{id}/pdf", opts)` and unwraps `%Response{data: binary}` to `{:ok, binary}`
- `pdf!/3` raises `LatticeStripe.Error` on failure and returns binary on success

Update docs so `pdf/3` explicitly notes:

- the response is binary, not a `%Quote{}`
- Stripe may return 404 for `draft` and `canceled` quotes

Extend `test/lattice_stripe/quote_test.exs` with:

- request-shape coverage for both line-item endpoint families
- stream coverage for both endpoint families
- `pdf/3` returns `{:ok, "pdf-binary-data"}`
- `pdf!/3` raises on error
- parser tests confirming line-item typed deserialization
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/quote_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/quote.ex` contains `def list_line_items(%Client{} = client, id, params \\\\ %{}, opts \\\\ [])`
    - `lib/lattice_stripe/quote.ex` contains `def list_computed_upfront_line_items(%Client{} = client, id, params \\\\ %{}, opts \\\\ [])`
    - `lib/lattice_stripe/quote.ex` contains `def pdf(%Client{} = client, id, opts \\\\ []) when is_binary(id) do`
    - `lib/lattice_stripe/quote.ex` contains `Client.download(client, "/v1/quotes/#{id}/pdf", opts)`
    - `test/lattice_stripe/quote_test.exs` contains `describe "pdf/3" do`
    - `test/lattice_stripe/quote_test.exs` contains `describe "list_computed_upfront_line_items/4" do`
  </acceptance_criteria>
  <done>Quote line-item and PDF surfaces exist with the correct binary and typed-list contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add narrow integration route sanity for the primary Quote surface</name>
  <files>
    test/integration/quote_integration_test.exs
    test/support/fixtures/quote.ex
  </files>
  <read_first>
    test/integration/credit_note_integration_test.exs
    test/integration/invoice_integration_test.exs
    test/support/fixtures/quote.ex
    .planning/phases/36-quote/36-RESEARCH.md
  </read_first>
  <behavior>
    - Integration scope stays narrow and route-focused.
    - The suite proves the primary Quote surface against `stripe-mock` without trying to encode product-owned business workflows.
  </behavior>
  <action>
Create `test/integration/quote_integration_test.exs` using the repo's standard `stripe-mock` guard and integration-client setup.

Cover the narrowest route-sanity set that still exercises the main phase surface:

- create + retrieve or retrieve existing quote fixture, whichever is more stable under `stripe-mock`
- list
- finalize if supported by the mock fixture flow
- one standard line-item route

If `stripe-mock` cannot realistically prove a lifecycle nuance like accept/cancel or binary PDF download, keep those behaviors unit-tested and document the limitation in the integration test module comments rather than weakening the unit contract.

Use fixture helpers only to keep test setup readable; do not introduce local builders or wrappers in `lib/`.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/quote_test.exs test/integration/quote_integration_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/quote_integration_test.exs` contains `@moduletag :integration`
    - `test/integration/quote_integration_test.exs` contains at least one happy-path assertion for Quote list or retrieve
    - `test/integration/quote_integration_test.exs` contains at least one happy-path assertion for a Quote line-item route
  </acceptance_criteria>
  <done>Quote integration coverage exists at the route-sanity level appropriate for this phase.</done>
</task>

</tasks>

<verification>
  <step>Run `mix test test/lattice_stripe/quote_test.exs --warnings-as-errors` after Tasks 1 and 2.</step>
  <step>Run `mix test test/lattice_stripe/quote_test.exs test/integration/quote_integration_test.exs --warnings-as-errors` after Task 3.</step>
  <step>Manually inspect `LatticeStripe.Quote` docs to confirm the PDF precondition and lifecycle consequences are explicitly stated.</step>
</verification>

<success_criteria>
  <criterion>All public APIs required by the roadmap success criteria exist with stable Stripe-shaped contracts.</criterion>
  <criterion>`Quote.pdf/3` returns binary and both line-item endpoint families return typed `Quote.LineItem` values.</criterion>
  <criterion>Unit coverage locks the main request shapes and parser rules, with route-sanity integration coverage for the primary surface.</criterion>
</success_criteria>
