---
phase: 36-quote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/quote.ex
  - lib/lattice_stripe/quote/line_item.ex
  - lib/lattice_stripe/quote/computed.ex
  - lib/lattice_stripe/quote/status_transitions.ex
  - lib/lattice_stripe/object_types.ex
  - lib/lattice_stripe/invoice.ex
  - mix.exs
  - test/support/fixtures/quote.ex
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements:
  - QUOT-01
  - QUOT-02
  - QUOT-03
  - QUOT-04
  - QUOT-05

must_haves:
  truths:
    - "Quote responses can deserialize into a typed %LatticeStripe.Quote{} with selective nested structs for `computed` and `status_transitions`"
    - "Quote line-item payloads deserialize into typed %LatticeStripe.Quote.LineItem{} structs for both embedded snapshots and paginated endpoints"
    - "ObjectTypes recognizes both \"quote\" and \"quote_line_item\" objects"
    - "Expanded `Invoice.quote` back-references deserialize through the same Quote contract instead of staying raw maps"
    - "Reusable Quote fixtures exist for top-level quote payloads, line-item payloads, and expanded downstream references"
  artifacts:
    - path: "lib/lattice_stripe/quote.ex"
      provides: "Quote struct contract and parser baseline"
      exports: ["from_map/1"]
    - path: "lib/lattice_stripe/quote/line_item.ex"
      provides: "Typed Quote line-item contract"
      exports: ["from_map/1"]
    - path: "lib/lattice_stripe/object_types.ex"
      provides: "quote and quote_line_item dispatch entries"
      contains: "\"quote\""
    - path: "lib/lattice_stripe/invoice.ex"
      provides: "expanded `quote` back-reference parsing"
      contains: "quote:"
    - path: "test/support/fixtures/quote.ex"
      provides: "Fixture helpers for quote resources and line items"
      contains: "quote_json"
  key_links:
    - from: "lib/lattice_stripe/quote.ex"
      to: "lib/lattice_stripe/quote/line_item.ex"
      via: "embedded and paginated line-item parsing"
      pattern: "LineItem\\.from_map"
    - from: "lib/lattice_stripe/quote.ex"
      to: "lib/lattice_stripe/object_types.ex"
      via: "expandable customer/invoice/subscription deserialization"
      pattern: "ObjectTypes\\.maybe_deserialize"
    - from: "lib/lattice_stripe/invoice.ex"
      to: "lib/lattice_stripe/quote.ex"
      via: "expanded invoice quote back-reference"
      pattern: "Quote\\.from_map|ObjectTypes\\.maybe_deserialize"
---

<objective>
Create the parser contracts, nested structs, object registry updates, Invoice back-reference handling, Billing ExDoc placement, and reusable fixtures for Quote.

Purpose: This plan locks the Quote data model and fixture infrastructure before any public request surface work begins. It establishes the selective-typing boundary that Plan 02 relies on for every public API and test.
Output: Parser-focused `quote.ex`, nested struct modules, registry/docs updates, and fixture helpers.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-quote/36-CONTEXT.md
@.planning/phases/36-quote/36-RESEARCH.md
@.planning/phases/36-quote/36-PATTERNS.md
@.planning/phases/36-quote/36-VALIDATION.md
@.planning/threads/lattice-stripe-vs-accrue-scope-boundary.md

<interfaces>
From `lib/lattice_stripe/object_types.ex`:
```elixir
@spec maybe_deserialize(map() | String.t() | nil) :: struct() | map() | String.t() | nil
def maybe_deserialize(%{"object" => object_type} = map)
```

From `lib/lattice_stripe/invoice.ex`:
```elixir
@spec finalize(Client.t(), String.t(), map(), keyword()) :: {:ok, t()} | {:error, Error.t()}
def finalize(%Client{} = client, id, params \\ %{}, opts \\ []) when is_binary(id)
```

From `lib/lattice_stripe/credit_note.ex`:
```elixir
defp do_list_line_items(%Client{} = client, path, params, opts)
defp do_stream_line_items(%Client{} = client, path, params, opts)
```

From `lib/lattice_stripe/client.ex`:
```elixir
@spec download(t(), String.t(), keyword()) :: {:ok, Response.t()} | {:error, Error.t()}
def download(%__MODULE__{} = client, path, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Quote nested structs, line-item contract, and reusable fixtures</name>
  <files>
    lib/lattice_stripe/quote/line_item.ex
    lib/lattice_stripe/quote/computed.ex
    lib/lattice_stripe/quote/status_transitions.ex
    test/support/fixtures/quote.ex
  </files>
  <read_first>
    lib/lattice_stripe/credit_note/line_item.ex
    lib/lattice_stripe/invoice/status_transitions.ex
    lib/lattice_stripe/invoice/automatic_tax.ex
    test/support/fixtures/credit_note.ex
    .planning/phases/36-quote/36-CONTEXT.md
    .planning/phases/36-quote/36-PATTERNS.md
  </read_first>
  <behavior>
    - `Quote.LineItem.from_map/1` is forward-compatible, preserves unknown fields in `extra`, and stays usable for both endpoint payloads and embedded partial snapshots.
    - `Quote.Computed.from_map/1` models only the stable bounded top-level branches and leaves deeper totals/line-item payloads raw.
    - `Quote.StatusTransitions.from_map/1` mirrors the repo's established timestamp-struct pattern.
    - Quote fixtures cover draft/open/accepted shapes, paginated line-item payloads, computed upfront line-item payloads, and expanded downstream references without inventing workflow helpers.
  </behavior>
  <action>
Create `lib/lattice_stripe/quote/line_item.ex` as `LatticeStripe.Quote.LineItem` using the same struct/from_map pattern as `CreditNote.LineItem`, but for Quote line-item fields. The contract must:

- keep `object: "quote_line_item"`
- include the stable Stripe line-item fields surfaced by Quote line-item payloads
- keep polymorphic nested branches such as taxes, price details, period details, discounts, and pricing maps raw
- preserve unknown keys in `extra`

Create `lib/lattice_stripe/quote/computed.ex` as `LatticeStripe.Quote.Computed` with a deliberately bounded struct. Model only the top-level computed branches that are stable and developer-facing; keep deeper pricing/totals branches raw maps or lists and preserve `extra`.

Create `lib/lattice_stripe/quote/status_transitions.ex` as `LatticeStripe.Quote.StatusTransitions`, mirroring the small timestamp-struct pattern already used by `Invoice.StatusTransitions`.

Create `test/support/fixtures/quote.ex` as `LatticeStripe.Test.Fixtures.Quote` with mergeable helpers:

- `quote_json/1` for a draft quote baseline
- `open_quote_json/1`, `accepted_quote_json/1`, and `canceled_quote_json/1` convenience wrappers when they materially reduce test noise
- `quote_line_item_json/1` for the standard `/line_items` surface
- `quote_computed_upfront_line_item_json/1` for the `/computed_upfront_line_items` surface

Fixture payloads must include:

- `computed`
- `status_transitions`
- expandable `customer`, `invoice`, `subscription`, and `subscription_schedule` fields in both string-ID and expanded-object shapes
- raw `subscription_data`, `invoice_settings`, and `from_quote` branches

Do not add any public Quote request functions in this task.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/quote/line_item.ex` contains `defmodule LatticeStripe.Quote.LineItem do`
    - `lib/lattice_stripe/quote/line_item.ex` contains `object: "quote_line_item"`
    - `lib/lattice_stripe/quote/computed.ex` contains `defmodule LatticeStripe.Quote.Computed do`
    - `lib/lattice_stripe/quote/status_transitions.ex` contains `defmodule LatticeStripe.Quote.StatusTransitions do`
    - `test/support/fixtures/quote.ex` contains `def quote_json(overrides \\\\ %{}) do`
    - `test/support/fixtures/quote.ex` contains `def quote_line_item_json(overrides \\\\ %{}) do`
  </acceptance_criteria>
  <done>Quote nested parser contracts and fixture helpers exist with the selective typing boundary locked for later public API work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Quote parser baseline, registry updates, Invoice back-reference fix, and Billing docs grouping</name>
  <files>
    lib/lattice_stripe/quote.ex
    lib/lattice_stripe/object_types.ex
    lib/lattice_stripe/invoice.ex
    mix.exs
    test/lattice_stripe/object_types_test.exs
  </files>
  <read_first>
    lib/lattice_stripe/quote.ex
    lib/lattice_stripe/object_types.ex
    lib/lattice_stripe/invoice.ex
    mix.exs
    test/lattice_stripe/object_types_test.exs
    lib/lattice_stripe/credit_note.ex
    .planning/phases/36-quote/36-CONTEXT.md
    .planning/phases/36-quote/36-PATTERNS.md
  </read_first>
  <behavior>
    - `Quote.from_map/1` selectively types `computed`, `status_transitions`, and line-item payloads while leaving volatile branches raw.
    - Expandable top-level references use the standard `is_map/1` guard + `ObjectTypes.maybe_deserialize/1` pattern.
    - `Invoice.from_map/1` correctly deserializes expanded quote back-references.
    - Billing ExDoc grouping includes Quote and its nested modules alongside Invoice and CreditNote.
  </behavior>
  <action>
Create the parser-focused baseline for `lib/lattice_stripe/quote.ex` without the public CRUDL/action functions yet.

Implement:

- `defmodule LatticeStripe.Quote`
- aliases for `Client`, `List`, `ObjectTypes`, `Quote.Computed`, `Quote.LineItem`, and `Quote.StatusTransitions` as needed for the parser contract
- `@known_fields`, `defstruct`, and `@type t` aligned with the phase decisions
- `from_map(nil)` and `from_map(map)` with explicit field assignment
- private helpers for:
  - expandable-field parsing
  - embedded line-item list parsing
  - enum atomization where the field set is stable enough to justify it

Honor the locked boundary directly:

- `computed` -> `Computed.from_map/1`
- `status_transitions` -> `StatusTransitions.from_map/1`
- `line_items` and other list payloads may map to `%LatticeStripe.List{data: [%Quote.LineItem{}, ...]}` when Stripe returned a real list object
- `subscription_data`, `invoice_settings`, and `from_quote` remain raw maps
- `automatic_tax` remains raw unless execution finds an already-established bounded struct that can be reused verbatim

Update `lib/lattice_stripe/object_types.ex` by adding:

- `"quote" => LatticeStripe.Quote`
- `"quote_line_item" => LatticeStripe.Quote.LineItem`

Update `lib/lattice_stripe/invoice.ex` so the `quote` field follows the repo's expandable-object pattern instead of remaining string-only when expanded.

Update `mix.exs` Billing ExDoc grouping by inserting:

- `LatticeStripe.Quote`
- `LatticeStripe.Quote.LineItem`
- `LatticeStripe.Quote.Computed`
- `LatticeStripe.Quote.StatusTransitions`

Extend `test/lattice_stripe/object_types_test.exs` with dispatch assertions for `quote` and `quote_line_item`, plus one assertion that an expanded invoice `quote` comes back typed.

Do not add `create/3`, `retrieve/3`, `pdf/3`, or lifecycle verbs in this task; Plan 02 owns the public request surface.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lattice_stripe/quote.ex` contains `defmodule LatticeStripe.Quote do`
    - `lib/lattice_stripe/quote.ex` contains `def from_map(nil), do: nil`
    - `lib/lattice_stripe/object_types.ex` contains `"quote" => LatticeStripe.Quote`
    - `lib/lattice_stripe/object_types.ex` contains `"quote_line_item" => LatticeStripe.Quote.LineItem`
    - `lib/lattice_stripe/invoice.ex` contains logic that parses expanded `quote` values via the standard object-deserialization path
    - `test/lattice_stripe/object_types_test.exs` contains `quote_line_item`
  </acceptance_criteria>
  <done>Quote parser baseline, object dispatch, Invoice back-reference behavior, and docs grouping are all in place for the public API plan.</done>
</task>

</tasks>

<verification>
  <step>Run `mix test test/lattice_stripe/object_types_test.exs --warnings-as-errors` after each task.</step>
  <step>Confirm `mix.exs` Billing docs grouping includes the new Quote modules exactly once.</step>
  <step>Manually inspect `Quote.from_map/1` and `Invoice.from_map/1` to confirm no expandable field calls `ObjectTypes.maybe_deserialize/1` without an `is_map/1` guard.</step>
</verification>

<success_criteria>
  <criterion>`Quote.from_map/1` exists and the Quote nested modules compile as a coherent parser contract.</criterion>
  <criterion>`ObjectTypes` and `Invoice` can both produce typed Quote structs from expanded payloads.</criterion>
  <criterion>Fixture helpers exist for all Quote payload families required by Plan 02 tests.</criterion>
</success_criteria>
