---
phase: 40-mandate-setupattempt-integration-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - .planning/phases/35-mandate-setupattempt/35-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "`35-VERIFICATION.md` exists in a closed verifier state backed by fresh AUTH-scoped commands per D-05, D-06, and D-08"
    - "AUTH-01 explicitly cites the new Mandate integration proof and AUTH-02 cites the current SetupAttempt unit/integration proof"
    - "The verifier states `stripe-mock` limits plainly and does not overclaim real Stripe lifecycle coverage per D-09"
    - "AUTH traceability rows in `.planning/REQUIREMENTS.md` no longer show `Pending` and reflect only this requirement family per D-13 and D-15"
    - "Phase 40 closes the audit gap without broadening into Quote, DX, roadmap reconciliation, or STATE updates"
  artifacts:
    - path: ".planning/phases/35-mandate-setupattempt/35-VERIFICATION.md"
      provides: "Closed milestone-ready verification report for AUTH-01 and AUTH-02"
      contains: "AUTH-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Current AUTH traceability state"
      contains: "AUTH-01"
  key_links:
    - from: ".planning/phases/35-mandate-setupattempt/35-VERIFICATION.md"
      to: "test/integration/mandate_integration_test.exs"
      via: "fresh Mandate retrieve integration proof"
      pattern: "mandate_integration_test"
    - from: ".planning/phases/35-mandate-setupattempt/35-VERIFICATION.md"
      to: "test/integration/setup_attempt_integration_test.exs"
      via: "fresh SetupAttempt integration proof"
      pattern: "setup_attempt_integration_test"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 40"
      via: "AUTH traceability closure"
      pattern: "Phase 40"
---

<objective>
Convert the fresh AUTH runtime proof into the closed verifier artifact and requirement traceability needed for milestone acceptance.

Purpose: The milestone audit names two missing closure artifacts for the auth family: `35-VERIFICATION.md` and current AUTH traceability. This plan closes both using the exact evidence produced or refreshed in Plan 01.
Output: A closed `35-VERIFICATION.md` plus updated AUTH-01/AUTH-02 rows in `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-v1.3-MILESTONE-AUDIT.md
@.planning/phases/40-mandate-setupattempt-integration-closure/40-CONTEXT.md
@.planning/phases/40-mandate-setupattempt-integration-closure/40-PATTERNS.md
@.planning/phases/35-mandate-setupattempt/35-VALIDATION.md
@.planning/phases/35-mandate-setupattempt/35-01-SUMMARY.md
@.planning/phases/35-mandate-setupattempt/35-02-SUMMARY.md
@lib/lattice_stripe/mandate.ex
@lib/lattice_stripe/setup_attempt.ex
@test/lattice_stripe/mandate_test.exs
@test/lattice_stripe/setup_attempt_test.exs
@test/integration/mandate_integration_test.exs
@test/integration/setup_attempt_integration_test.exs

<interfaces>
Fresh evidence this plan must cite:
```text
mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors
mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors
mix test test/integration/mandate_integration_test.exs --include integration
mix test test/integration/setup_attempt_integration_test.exs --include integration
```

Current traceability rows:
```text
AUTH-01 | Phase 40 | Pending
AUTH-02 | Phase 40 | Pending
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a closed Phase 35 verification report grounded in current AUTH evidence</name>
  <files>
    .planning/phases/35-mandate-setupattempt/35-VERIFICATION.md
  </files>
  <read_first>
    .planning/v1.3-v1.3-MILESTONE-AUDIT.md
    .planning/phases/35-mandate-setupattempt/35-VALIDATION.md
    .planning/phases/35-mandate-setupattempt/35-01-SUMMARY.md
    .planning/phases/35-mandate-setupattempt/35-02-SUMMARY.md
    test/lattice_stripe/mandate_test.exs
    test/lattice_stripe/setup_attempt_test.exs
    test/integration/mandate_integration_test.exs
    test/integration/setup_attempt_integration_test.exs
    .planning/phases/39-credit-note-verification-closure/39-VERIFICATION.md
  </read_first>
  <behavior>
    - Per D-06, prove exactly the audit gaps: fresh Mandate integration evidence, current SetupAttempt evidence, and a closed verifier file.
    - Per D-09, state `stripe-mock` limits explicitly in the report.
    - Per D-13 and D-14, keep the artifact tightly scoped to AUTH closure and directly implied proof.
  </behavior>
  <action>
Create `.planning/phases/35-mandate-setupattempt/35-VERIFICATION.md` in the repo's current closed-verifier style.

Required structure:

- frontmatter with phase identifier, verification timestamp/date, `status: closed`, score, overrides metadata, and `re_verification`
- phase goal and closed-status summary
- observable truths
- artifact verification
- behavioral spot-checks
- requirement coverage table for `AUTH-01` and `AUTH-02`
- gaps summary that explicitly names the prior audit gaps now being closed

Evidence requirements:

- cite `35-01-SUMMARY.md` for parser/object-registration/fixture completion
- cite `35-02-SUMMARY.md` for the shipped retrieve/list API surface and Phase 35 unit coverage
- cite `test/lattice_stripe/mandate_test.exs` and `test/lattice_stripe/setup_attempt_test.exs` as deterministic unit proof
- cite `test/integration/mandate_integration_test.exs` for AUTH-01 runtime proof per D-01 and D-02
- cite `test/integration/setup_attempt_integration_test.exs` for AUTH-02 runtime proof
- record the four exact fresh AUTH commands from Plan 01 verbatim, together with their observed results, in the verification evidence section:
  - `mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors`
  - `mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors`
  - `mix test test/integration/mandate_integration_test.exs --include integration`
  - `mix test test/integration/setup_attempt_integration_test.exs --include integration`
- explicitly note that `stripe-mock` proves request routing, endpoint shape, and typed decode sanity rather than full persisted Stripe state, per D-09
- explicitly state that the named audit gaps being closed are: missing `35-VERIFICATION.md`, missing Mandate integration coverage, and stale/current AUTH evidence closure for milestone acceptance

Do not write the verifier as a paperwork-only summary detached from the exact fresh commands.
  </action>
  <verify>
    <automated>rg -n "status: closed|AUTH-01|AUTH-02|mix test test/lattice_stripe/mandate_test\\.exs --warnings-as-errors|mix test test/lattice_stripe/setup_attempt_test\\.exs --warnings-as-errors|mix test test/integration/mandate_integration_test\\.exs --include integration|mix test test/integration/setup_attempt_integration_test\\.exs --include integration|pass|passed|35-01-SUMMARY|35-02-SUMMARY|stripe-mock|missing 35-VERIFICATION\\.md|missing Mandate integration coverage|stale/current AUTH evidence" .planning/phases/35-mandate-setupattempt/35-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/35-mandate-setupattempt/35-VERIFICATION.md` exists
    - the frontmatter contains `status: closed`
    - the file contains `AUTH-01` and `AUTH-02`
    - the file references both `35-01-SUMMARY.md` and `35-02-SUMMARY.md`
    - the file references both `test/integration/mandate_integration_test.exs` and `test/integration/setup_attempt_integration_test.exs`
    - the file records all four exact AUTH commands and their results rather than paraphrasing them
    - the file includes an explicit note that `stripe-mock` covers routing/endpoint/decode sanity only, not full Stripe lifecycle semantics
    - the file explicitly names the prior audit gaps it is closing: missing `35-VERIFICATION.md`, missing Mandate integration coverage, and stale/current AUTH evidence closure
  </acceptance_criteria>
  <done>Phase 35 has a milestone-ready verifier artifact that directly answers the audit's missing AUTH evidence gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile AUTH traceability rows to the closed verification state</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/35-mandate-setupattempt/35-VERIFICATION.md
    .planning/ROADMAP.md
    .planning/phases/40-mandate-setupattempt-integration-closure/40-CONTEXT.md
  </read_first>
  <behavior>
    - Per D-13, update the tightest required planning surface first: the AUTH traceability rows.
    - Per D-15, do not fold adjacent planning-truth cleanup into this phase.
    - Match the file's established terminal status vocabulary.
  </behavior>
  <action>
Update only the AUTH-specific planning truth that this phase is responsible for:

- flip both top-level AUTH checklist entries from `[ ]` to `[x]`
- update the `AUTH-01` and `AUTH-02` traceability rows in `.planning/REQUIREMENTS.md` from `Pending` to the repo's established closed-state vocabulary, which should be `Verified` if that is what surrounding closed families use

Use the repo's existing closed-state vocabulary already used for FILE, DISP, and CRDN rows. If the surrounding rows use `Verified`, use `Verified` here as well.

This task is not complete unless both AUTH checklist rows and both AUTH traceability rows reflect the closed verifier truth established by Task 1.

Do not modify:

- QUOT rows
- DX rows
- milestone counts
- `.planning/STATE.md`
- any roadmap scope language beyond what this plan directly closes
  </action>
  <verify>
    <automated>if rg -n "^- \\[ \\] \\*\\*AUTH-01\\*\\*|^- \\[ \\] \\*\\*AUTH-02\\*\\*|\\| AUTH-01 \\|.*\\| Pending \\||\\| AUTH-02 \\|.*\\| Pending \\|" .planning/REQUIREMENTS.md; then exit 1; else rg -n "^- \\[x\\] \\*\\*AUTH-01\\*\\*|^- \\[x\\] \\*\\*AUTH-02\\*\\*|\\| AUTH-01 \\|.*\\| Verified \\||\\| AUTH-02 \\|.*\\| Verified \\|" .planning/REQUIREMENTS.md; fi</automated>
  </verify>
  <acceptance_criteria>
    - the top-level `AUTH-01` checklist entry is `- [x] **AUTH-01**`
    - the top-level `AUTH-02` checklist entry is `- [x] **AUTH-02**`
    - the `AUTH-01` traceability row shows `Verified`
    - the `AUTH-02` traceability row shows `Verified`
    - no `AUTH-01` or `AUTH-02` `Pending` state remains anywhere in `.planning/REQUIREMENTS.md`
    - no non-AUTH traceability row is changed
  </acceptance_criteria>
  <done>Requirement traceability now reflects that Phase 40 closed the auth verification gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh scoped test runs -> verifier artifact | A closed report is only credible if it cites current commands and bounded claims |
| Historical Phase 35 summaries -> current milestone acceptance | Summaries prove shipped implementation but not current closure state by themselves |
| `.planning/REQUIREMENTS.md` -> future roadmap/audit workflows | Incorrect AUTH row statuses can misroute later verification and planning work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | R | `35-VERIFICATION.md` credibility | mitigate | Require exact fresh AUTH commands, exact summary references, and explicit `stripe-mock` scope notes in the verifier artifact. |
| T-40-06 | T | AUTH traceability rows | mitigate | Update only AUTH-01 and AUTH-02, using the repo's existing terminal status vocabulary. |
| T-40-07 | E | Scope escalation during docs closure | mitigate | Forbid Quote, DX, roadmap reconciliation, and STATE changes from this plan. |
</threat_model>

<verification>
- `rg -n "status:|AUTH-0[12]" .planning/phases/35-mandate-setupattempt/35-VERIFICATION.md .planning/REQUIREMENTS.md`
- confirm `35-VERIFICATION.md` cites `mandate_integration_test` and `setup_attempt_integration_test`
</verification>

<success_criteria>
- `35-VERIFICATION.md` exists in a closed state with fresh AUTH evidence.
- `AUTH-01` and `AUTH-02` are traceably closed in `.planning/REQUIREMENTS.md`.
- The report is explicit about `stripe-mock` limits and does not overclaim semantics.
- Phase 40 closes the audit gap without becoming a general planning-reconciliation phase.
</success_criteria>

<output>
After completion, create `.planning/phases/40-mandate-setupattempt-integration-closure/40-02-SUMMARY.md`
</output>
