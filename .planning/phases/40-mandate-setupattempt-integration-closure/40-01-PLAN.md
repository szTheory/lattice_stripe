---
phase: 40-mandate-setupattempt-integration-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/mandate_integration_test.exs
  - test/lattice_stripe/mandate_test.exs
  - test/integration/setup_attempt_integration_test.exs
  - test/support/fixtures/mandate.ex
  - lib/lattice_stripe/mandate.ex
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Fresh AUTH-scoped runtime evidence exists from the current closure window rather than only historical Phase 35 summaries"
    - "A dedicated Mandate integration suite proves `Mandate.retrieve/3` over the real Finch -> stripe-mock HTTP path and typed `%LatticeStripe.Mandate{}` decode per D-01 and D-02"
    - "SetupAttempt proof remains current through fresh targeted reruns without broadening the shipped API surface per D-04 and D-06"
    - "Any edits stay inside narrow evidence-repair scope only, with parser-depth semantics left in unit tests per D-03 and D-10"
    - "All proof language stays explicit that `stripe-mock` validates routing and decode shape, not real Stripe lifecycle semantics, per D-09"
  artifacts:
    - path: "test/integration/mandate_integration_test.exs"
      provides: "Fresh AUTH-01 integration proof for retrieve-only Mandate"
      contains: "defmodule LatticeStripe.MandateIntegrationTest"
    - path: "test/integration/setup_attempt_integration_test.exs"
      provides: "Fresh AUTH-02 integration proof for setup-attempt list/stream route sanity"
      contains: "defmodule LatticeStripe.SetupAttemptIntegrationTest"
    - path: "test/lattice_stripe/mandate_test.exs"
      provides: "Deterministic parser-depth and docs coverage that complements the new integration proof"
      contains: "describe \"from_map/1\""
  key_links:
    - from: "test/integration/mandate_integration_test.exs"
      to: "lib/lattice_stripe/mandate.ex"
      via: "retrieve route-sanity and typed top-level decode"
      pattern: "Mandate\\.retrieve"
    - from: "test/lattice_stripe/mandate_test.exs"
      to: "test/support/fixtures/mandate.ex"
      via: "unit coverage keeps enum atomization and nested customer-acceptance assertions deterministic"
      pattern: "mandate_json"
    - from: "test/integration/setup_attempt_integration_test.exs"
      to: "lib/lattice_stripe/setup_attempt.ex"
      via: "fresh list/stream proof for the required setup_intent-scoped endpoint"
      pattern: "SetupAttempt\\.(list|stream!)"
---

<objective>
Produce fresh AUTH runtime proof for the shipped Phase 35 authorization resources, filling the one missing code-evidence gap called out by the milestone audit: Mandate integration coverage.

Purpose: Phase 40 is a verification-closure phase. The executor must create fresh, targeted AUTH evidence per D-05 through D-09, not reopen Mandate or SetupAttempt design.
Output: A dedicated Mandate integration suite, fresh targeted AUTH reruns, and only the smallest supporting repair needed to keep the proof truthful.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-v1.3-MILESTONE-AUDIT.md
@.planning/phases/40-mandate-setupattempt-integration-closure/40-CONTEXT.md
@.planning/phases/40-mandate-setupattempt-integration-closure/40-PATTERNS.md
@.planning/phases/35-mandate-setupattempt/35-CONTEXT.md
@.planning/phases/35-mandate-setupattempt/35-RESEARCH.md
@.planning/phases/35-mandate-setupattempt/35-VALIDATION.md
@.planning/phases/35-mandate-setupattempt/35-01-SUMMARY.md
@.planning/phases/35-mandate-setupattempt/35-02-SUMMARY.md
@lib/lattice_stripe/mandate.ex
@lib/lattice_stripe/setup_attempt.ex
@test/lattice_stripe/mandate_test.exs
@test/lattice_stripe/setup_attempt_test.exs
@test/integration/setup_attempt_integration_test.exs
@test/integration/account_integration_test.exs
@test/support/fixtures/mandate.ex

<interfaces>
From `lib/lattice_stripe/mandate.ex`:
```elixir
@spec retrieve(Client.t(), String.t(), keyword()) :: {:ok, t()} | {:error, Error.t()}
@spec retrieve!(Client.t(), String.t(), keyword()) :: t()
@spec from_map(map() | nil) :: t() | nil
```

From `lib/lattice_stripe/setup_attempt.ex`:
```elixir
@spec list(Client.t(), map(), keyword()) :: {:ok, Response.t()} | {:error, Error.t()}
@spec stream!(Client.t(), map(), keyword()) :: Enumerable.t()
```

Required fresh proof commands per D-05, D-06, and D-08:
```bash
mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors
mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors
mix test test/integration/mandate_integration_test.exs --include integration
mix test test/integration/setup_attempt_integration_test.exs --include integration
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal Mandate integration proof for AUTH-01</name>
  <files>
    test/integration/mandate_integration_test.exs
    lib/lattice_stripe/mandate.ex
    test/lattice_stripe/mandate_test.exs
    test/support/fixtures/mandate.ex
  </files>
  <read_first>
    test/integration/account_integration_test.exs
    test/integration/setup_attempt_integration_test.exs
    lib/lattice_stripe/mandate.ex
    test/lattice_stripe/mandate_test.exs
    test/support/fixtures/mandate.ex
    .planning/phases/40-mandate-setupattempt-integration-closure/40-CONTEXT.md
    .planning/v1.3-v1.3-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - Per D-01 and D-02, the new proof is minimal and shape-first: real HTTP path plus typed top-level `%LatticeStripe.Mandate{}` decode.
    - Per D-03, keep enum atomization, nested `customer_acceptance`, expanded payment method, and `extra` assertions in unit tests, not in brittle integration assertions.
    - Per D-10 and D-11, allow only tiny supporting repairs that are directly required to make the fresh proof truthful.
  </behavior>
  <action>
Create `test/integration/mandate_integration_test.exs` as a dedicated integration module following the repo's established pattern from `test/integration/account_integration_test.exs` and `test/integration/setup_attempt_integration_test.exs`.

Required module shape:

- `defmodule LatticeStripe.MandateIntegrationTest`
- `use ExUnit.Case, async: false`
- `import LatticeStripe.TestHelpers`
- `@moduletag :integration`
- `alias LatticeStripe.Mandate`
- `setup_all` with the `:gen_tcp.connect(~c"localhost", 12_111, [], 1000)` guard and `start_supervised!({Finch, name: LatticeStripe.IntegrationFinch})`
- `setup` returning `client: test_integration_client()`

Required test scenario:

- call `Mandate.retrieve(client, "mandate_test1234567890abc")`
- assert `{:ok, %Mandate{id: id}}` or equivalent typed-shape success using a stable mock ID shape
- keep assertions narrow: typed top-level struct, binary/string ID shape, and no deep semantic lifecycle assumptions

If `stripe-mock` behavior requires a different acceptable test ID or a tiny wording/doc alignment in `lib/lattice_stripe/mandate.ex`, `test/lattice_stripe/mandate_test.exs`, or `test/support/fixtures/mandate.ex`, make only that smallest proof-supporting repair.

Explicitly document inside the integration module that the test proves request routing and typed decoding sanity under `stripe-mock`, not rich real-Stripe mandate semantics, per D-09.
  </action>
  <verify>
    <automated>mix test test/integration/mandate_integration_test.exs --include integration</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/mandate_integration_test.exs` exists
    - the file contains `defmodule LatticeStripe.MandateIntegrationTest`
    - the file contains `@moduletag :integration`
    - the file contains `Mandate.retrieve(`
    - the file contains an inline note or moduledoc line that `stripe-mock` proves routing/shape, not full mandate semantics
    - `mix test test/integration/mandate_integration_test.exs --include integration` exits 0 with `stripe-mock` running
  </acceptance_criteria>
  <done>AUTH-01 has fresh integration proof that closes the explicit audit gap without broadening Mandate semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh targeted AUTH unit and SetupAttempt integration evidence without scope drift</name>
  <files>
    test/lattice_stripe/mandate_test.exs
    test/lattice_stripe/setup_attempt_test.exs
    test/integration/setup_attempt_integration_test.exs
    lib/lattice_stripe/mandate.ex
  </files>
  <read_first>
    test/lattice_stripe/mandate_test.exs
    test/lattice_stripe/setup_attempt_test.exs
    test/integration/setup_attempt_integration_test.exs
    lib/lattice_stripe/setup_attempt.ex
    lib/lattice_stripe/mandate.ex
    .planning/phases/35-mandate-setupattempt/35-02-SUMMARY.md
    .planning/phases/40-mandate-setupattempt-integration-closure/40-CONTEXT.md
  </read_first>
  <behavior>
    - Per D-05 through D-08, use fresh targeted reruns as the proof posture for both AUTH requirements.
    - Per D-04, do not merge Mandate and SetupAttempt into an aesthetic combined integration module; SetupAttempt already has the needed proof surface.
    - Per D-12, if a substantive shipped-behavior bug appears, stop at evidence capture and route it forward instead of silently expanding the phase.
  </behavior>
  <action>
Run the fresh AUTH-scoped commands individually:

- `mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors`
- `mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors`
- `mix test test/integration/setup_attempt_integration_test.exs --include integration`

If all three pass, keep code unchanged and record the exact command set/results for Plan 02's verifier.

If a proof-supporting issue appears, make only the smallest alignment change directly required for truthful AUTH evidence, such as:

- clarifying a test comment that overclaims `stripe-mock`
- tightening an assertion to match actual retrieve/list/stream shape guarantees
- correcting a read-only diagnostic wording mismatch in `lib/lattice_stripe/mandate.ex`

Do not:

- add new public functions or aliases
- redesign parser depth or nested structs
- modify unrelated resources
- require `mix test` or `mix ci` as mandatory closure evidence, per D-07
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors && mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors && mix test test/integration/setup_attempt_integration_test.exs --include integration</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors` exits 0
    - `mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors` exits 0
    - `mix test test/integration/setup_attempt_integration_test.exs --include integration` exits 0 with `stripe-mock` running
    - no new Mandate or SetupAttempt public API entrypoint is introduced
    - SetupAttempt integration proof remains a separate module from the new Mandate integration proof
  </acceptance_criteria>
  <done>AUTH-01 and AUTH-02 both have fresh targeted proof, and any required repair stayed inside the narrow closure boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `stripe-mock` integration run -> milestone acceptance | Integration evidence can be over-claimed if the suite pretends to prove real Stripe mandate semantics |
| Unit parser assertions -> integration smoke assertions | Parser-depth guarantees belong in deterministic unit tests, while integration tests only prove transport/decode sanity |
| Narrow evidence repair -> public auth resource surface | Verification fixes must not smuggle in new API behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | R | Mandate integration evidence | mitigate | Add a dedicated `Mandate.retrieve/3` integration suite and rerun it fresh so AUTH-01 no longer relies on summaries alone. |
| T-40-02 | R | SetupAttempt closure evidence | mitigate | Re-run focused unit and integration commands for AUTH-02 instead of inheriting stale results. |
| T-40-03 | S | `stripe-mock` proof scope | mitigate | Require explicit wording in the integration module that the suite proves route/decode sanity, not full backend semantics. |
| T-40-04 | T | Scope expansion during verification repair | mitigate | Limit edits to the named auth files and forbid new public APIs, parser redesign, or adjacent resource work. |
</threat_model>

<verification>
- `mix test test/integration/mandate_integration_test.exs --include integration`
- `mix test test/lattice_stripe/mandate_test.exs --warnings-as-errors`
- `mix test test/lattice_stripe/setup_attempt_test.exs --warnings-as-errors`
- `mix test test/integration/setup_attempt_integration_test.exs --include integration`
</verification>

<success_criteria>
- A dedicated Mandate integration suite exists and passes under `stripe-mock`.
- Fresh AUTH-scoped unit and integration commands exist for both requirements.
- All claims remain honest about `stripe-mock` limitations.
- Plan 02 can build a closed verifier from current proof without reopening the shipped auth API design.
</success_criteria>

<output>
After completion, create `.planning/phases/40-mandate-setupattempt-integration-closure/40-01-SUMMARY.md`
</output>
