---
phase: 48-thin-event-adoption-surface-guide-integration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/webhook/plug.ex
  - test/lattice_stripe/docs_truth_test.exs
autonomous: true
requirements: [GUIDE-03]
must_haves:
  truths:
    - "Adopters reading the rendered `LatticeStripe.Webhook.Plug` HexDocs page see the `tolerance: 0` testing-only escape hatch in the Configuration Options block (closes Phase 47 deferred WR-04 per D-03 3E)."
    - "Drift in the `@moduledoc` `tolerance: 0` testing-only mention fails CI via a new docs-truth grep test (D-03 3E)."
  artifacts:
    - path: "lib/lattice_stripe/webhook/plug.ex"
      provides: "@moduledoc Configuration Options :tolerance line extended with the WR-04 testing-only language"
      contains: "testing only"
    - path: "test/lattice_stripe/docs_truth_test.exs"
      provides: "New test block enforcing the @moduledoc tolerance: 0 testing-only co-location"
      contains: "Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics"
  key_links:
    - from: "test/lattice_stripe/docs_truth_test.exs"
      to: "lib/lattice_stripe/webhook/plug.ex"
      via: "File.read! + regex ~r/@moduledoc.*tolerance.*0.*testing only/s"
      pattern: "@moduledoc.*tolerance.*0.*testing only"
---

<objective>
Close Phase 47 deferred WR-04 in lockstep with the rest of the Phase 48 docs-truth surface: extend the `LatticeStripe.Webhook.Plug` `@moduledoc` "Configuration Options" block at `:116` so the `:tolerance` line mentions the `tolerance: 0` testing-only escape hatch, and add a matching docs-truth grep test analogous to the existing WEBFIX-01 CHANGELOG grep.

Purpose: HexDocs renders the Plug `@moduledoc` as the landing page for the webhook Plug. Adopters reading that page first do NOT currently see the WEBFIX-01 `tolerance: 0` semantics — they only show up in the NimbleOptions schema `doc:` string (`plug.ex:145`) and the `check_tolerance/2` inline comment. WR-04 surfaces the contract in the prose adopters actually read. The docs-truth grep test ensures that any future "fix it to be stricter" PR that silently drops the `@moduledoc` line fails CI.

Output:
- `lib/lattice_stripe/webhook/plug.ex` — `@moduledoc` `:tolerance` line extended with a 2-line continuation (testing-only language + cross-reference to `check_tolerance/2` inline comment + v1.5 CHANGELOG WEBFIX-01 entry).
- `test/lattice_stripe/docs_truth_test.exs` — one new `test "..."` block enforcing the grep lock.

Scope discipline: This plan does NOT touch any other surface. The Plug schema `doc:` string at `:145`, the `check_tolerance/2` inline comment at `:639-646`, and the CHANGELOG WEBFIX-01 entries are Phase 47-shipped and already locked by docs-truth regression. This is the one remaining public surface that didn't get the WEBFIX-01 message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-VERIFICATION.md
@.planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-REVIEW.md
@lib/lattice_stripe/webhook/plug.ex
@test/lattice_stripe/docs_truth_test.exs

<interfaces>
<!-- Key contracts the executor needs. The plug `@moduledoc` Configuration Options block sits at lines 108-116 of plug.ex; the file is 286 lines. The exact replacement string is below — pulled from CONTEXT.md D-03 sub-decision 3E + RESEARCH.md §Webhook.Plug @moduledoc Extension + PATTERNS.md "Replacement pattern". -->

Current `:tolerance` line in `lib/lattice_stripe/webhook/plug.ex` (~line 116):
```
    - `:tolerance` — Maximum age of the webhook timestamp in seconds (default: 300).
```

Required replacement (3-line block, two-line continuation, sentence-case continuation, backticks around literal `0`):
```
    - `:tolerance` — Maximum age of the webhook timestamp in seconds (default: 300).
      Set `0` to disable the staleness check (testing only — see the inline comment
      on `LatticeStripe.Webhook.check_tolerance/2` and the v1.5 CHANGELOG WEBFIX-01 entry).
```

Existing analog test in `test/lattice_stripe/docs_truth_test.exs:175-186` (the WEBFIX-01 CHANGELOG grep):
```elixir
test "CHANGELOG.md documents WEBFIX-01 reconciliation under v1.5" do
  # WEBFIX-01 / Phase 47 D-03 regression-prevention contract: a future
  # "fix it to be stricter" PR that silently drops the CHANGELOG entry
  # MUST fail this grep test. The inline source comment + this test +
  # the function-boundary test + the Plug-boundary test together
  # triangulate the decision so the drift cannot silently come back.
  changelog = File.read!("CHANGELOG.md")

  assert changelog =~ "WEBFIX-01"
  assert changelog =~ ~r/##\s*\[?1\.5/
end
```

New test (3E grep lock) — `~r/.../s` dotall flag is load-bearing because `@moduledoc` triple-quote strings span newlines:
```elixir
test "Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics" do
  # WR-04 closure (Phase 47 deferred → Phase 48 D-03 3E): the Plug
  # @moduledoc Configuration Options section must surface the tolerance: 0
  # testing-only escape hatch. HexDocs renders this @moduledoc as the
  # landing page for the Plug; without this lock, drift here would silently
  # stop showing the contract on the page adopters actually read first.
  source = File.read!("lib/lattice_stripe/webhook/plug.ex")
  assert source =~ ~r/@moduledoc.*tolerance.*0.*testing only/s
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `Webhook.Plug` `@moduledoc` `:tolerance` line with WR-04 testing-only language</name>
  <files>lib/lattice_stripe/webhook/plug.ex</files>
  <read_first>
    - lib/lattice_stripe/webhook/plug.ex (full file — confirm exact current line position for the `:tolerance` row inside `@moduledoc`; the file is 286 lines so a single Read call suffices; do NOT re-read)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-03 sub-decision 3E — exact replacement text)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Webhook.Plug @moduledoc Extension" — full target-line analysis)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"lib/lattice_stripe/webhook/plug.ex (MOD — one-line `@moduledoc` extension)" — style + cross-ref convention)
    - .planning/phases/47-thin-event-sdk-surface-webhook-reconciliation/47-REVIEW.md (§WR-04 lines 136-153 — the original fix specification)
  </read_first>
  <action>
    In the existing `@moduledoc` "Configuration Options" block (currently around `plug.ex:108-116`), locate the `:tolerance` row (verbatim current text: `- \`:tolerance\` — Maximum age of the webhook timestamp in seconds (default: 300).`). Use the Edit tool to replace that single line with the locked 3-line block from CONTEXT.md D-03 sub-decision 3E:

    Line 1 stays the same. Add two continuation lines at the same indentation depth (matching the existing two-line continuation style used on the `:at` description per PATTERNS.md). The continuation MUST contain all three required substrings in this order so the grep regex `~r/@moduledoc.*tolerance.*0.*testing only/s` matches: `tolerance`, `0`, `testing only`. Use backticks around the literal `0` per the existing prose convention. Cross-reference both the inline comment on `LatticeStripe.Webhook.check_tolerance/2` AND the v1.5 CHANGELOG WEBFIX-01 entry so adopters following the trail find all four reconciliation surfaces. Do NOT modify any other line in the `@moduledoc` block.

    Per D-03 3E (literal replacement, copy verbatim from `<interfaces>` block above):
    ```
    - `:tolerance` — Maximum age of the webhook timestamp in seconds (default: 300).
      Set `0` to disable the staleness check (testing only — see the inline comment
      on `LatticeStripe.Webhook.check_tolerance/2` and the v1.5 CHANGELOG WEBFIX-01 entry).
    ```

    Do NOT change the NimbleOptions schema `doc:` string at `:145` — Phase 47 already shipped its `tolerance: 0` mention and it is locked by the Plug end-to-end test in `plug_test.exs`. This edit is the `@moduledoc` surface ONLY.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && grep -c "testing only" lib/lattice_stripe/webhook/plug.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "testing only" lib/lattice_stripe/webhook/plug.ex` returns at least 1 (currently 0)
    - `mix compile --warnings-as-errors` exits 0 with no new warnings
    - The string `Set \`0\` to disable the staleness check (testing only` appears in `lib/lattice_stripe/webhook/plug.ex`
    - The string `LatticeStripe.Webhook.check_tolerance/2` appears in `lib/lattice_stripe/webhook/plug.ex` (cross-reference to inline comment surface)
    - The string `WEBFIX-01` appears in the @moduledoc region of `lib/lattice_stripe/webhook/plug.ex` (cross-reference to CHANGELOG surface)
    - The `@moduledoc` region read top-to-bottom (lines ~80-120) shows `tolerance`, `0`, and `testing only` all present together (the dotall regex `~r/@moduledoc.*tolerance.*0.*testing only/s` matches when applied via `File.read!` + `=~`)
    - The NimbleOptions schema `doc:` line at `plug.ex:145` is unchanged from Phase 47 — no edits outside the `@moduledoc` block
  </acceptance_criteria>
  <done>
    - `lib/lattice_stripe/webhook/plug.ex` `@moduledoc` `:tolerance` row now spans 3 lines and contains `tolerance`, `0`, `testing only`, `check_tolerance/2`, `WEBFIX-01` substrings co-located within the `@moduledoc` block
    - Compile clean, no new Credo warnings
    - File line count delta is +2 (two new continuation lines)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add docs-truth grep test locking `Webhook.Plug` `@moduledoc` `tolerance: 0` co-location</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs (full file — 186 lines; existing 8-test pattern; analog at lines 175-186 is the WEBFIX-01 CHANGELOG grep that is the template for this new test)
    - lib/lattice_stripe/webhook/plug.ex (the file whose source the new test reads via `File.read!`)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-03 sub-decision 3E)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Webhook.Plug @moduledoc Extension" — matching docs-truth grep specification)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"test/lattice_stripe/docs_truth_test.exs (MOD — 4 patterns 3A/3B/3C/3D + 3E)" — regex flavor + comment block convention)
  </read_first>
  <action>
    Append a new `test "..."` block at the END of `test/lattice_stripe/docs_truth_test.exs` (after the existing WEBFIX-01 CHANGELOG grep block at lines 175-186). Do NOT add a `describe` wrapper — the file convention is flat `test "..."` blocks. Do NOT use `async: false` — the module-level `use ExUnit.Case, async: true` (line 1) is load-bearing and this file-read test is async-safe.

    Test name (exact): `"Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics"`.

    Body pattern (mirror the analog block convention at `:175-186` — multi-line block comment naming the regression-prevention contract, then `File.read!` + `assert source =~ ~r/.../s`):

    1. Multi-line block comment (3-4 lines) above the test that:
       - Names the contract: WR-04 closure (Phase 47 deferred → Phase 48 D-03 3E)
       - States why the lock matters: HexDocs renders this `@moduledoc` as the Plug landing page; drift here silently stops showing the contract on the page adopters read first
       - References the four-surface triangulation (inline comment + schema doc + CHANGELOG + this @moduledoc) so future maintainers see the full safety net

    2. `source = File.read!("lib/lattice_stripe/webhook/plug.ex")` (matches the file-read convention used by the existing WEBFIX-01 test on `CHANGELOG.md`).

    3. `assert source =~ ~r/@moduledoc.*tolerance.*0.*testing only/s` — the `s` flag (dotall, `.` matches newline) is LOAD-BEARING because `@moduledoc` triple-quote strings span newlines. PATTERNS.md notes the alternative of three separate `assert source =~ "..."` calls — REJECT that alternative; the regex form enforces co-location (all three substrings inside the same `@moduledoc` block, not scattered through the file). This regex form is specified in CONTEXT.md D-03 3E AND RESEARCH.md §"Webhook.Plug @moduledoc Extension" AND PATTERNS.md.

    Do NOT modify any of the existing 8 test blocks. Do NOT change the module attributes. Do NOT change the `async: true` setting.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --only "test:Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics" 2>&1 | tail -10 || mix test test/lattice_stripe/docs_truth_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/docs_truth_test.exs` exits 0 with 9 tests passing (up from 8; the new test passes because Task 1 already landed the @moduledoc edit)
    - The test block contains the literal string `Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics` as the test name
    - The test body contains `File.read!("lib/lattice_stripe/webhook/plug.ex")`
    - The test body contains the regex literal `~r/@moduledoc.*tolerance.*0.*testing only/s` (the `s` flag is present)
    - The new block is appended AFTER the existing WEBFIX-01 CHANGELOG grep (line ordering: existing WEBFIX-01 grep → new 3E grep)
    - No existing test was modified — `git diff test/lattice_stripe/docs_truth_test.exs` shows only additions
    - Module still declares `async: true` at line 1
    - If Task 1's @moduledoc edit were reverted, this test would fail (proof: temporarily revert Task 1 edit locally, re-run `mix test test/lattice_stripe/docs_truth_test.exs`, the new test must turn RED; restore Task 1 edit and re-run, the new test turns GREEN)
  </acceptance_criteria>
  <done>
    - `test/lattice_stripe/docs_truth_test.exs` now has 9 test blocks (was 8); only addition, no modifications
    - New test passes against Task 1's @moduledoc edit
    - The regex `~r/@moduledoc.*tolerance.*0.*testing only/s` is the matching pattern (NOT three separate substring asserts)
    - The block-comment above the test names WR-04 + D-03 3E + the four-surface triangulation, matching the documentation style of the WEBFIX-01 analog at `:175-186`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HexDocs reader → Plug `@moduledoc` | Adopters render `mix docs` HTML and read the Plug landing page; drift here teaches adopters incorrect operational posture |
| Maintainer source edit → docs-truth grep | A future PR that silently drops the WR-04 line must fail CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Information Disclosure | `Webhook.Plug` `@moduledoc` Configuration Options block | mitigate | Document `tolerance: 0` as "testing only" so adopters cannot accidentally deploy a Plug config that silently disables the signature-staleness check in production. Without this surface mention, adopters read the schema-doc one-liner without context; with WR-04 closure, the prose on the page they actually land on names the constraint explicitly. |
| T-48-02 | Tampering | Future PR drops the `@moduledoc` testing-only mention | mitigate | Docs-truth grep test (`~r/@moduledoc.*tolerance.*0.*testing only/s`) fails CI on drift; the comment block above the test names the regression-prevention contract so a future maintainer cannot delete the test without acknowledging the surface it protects |
| T-48-03 | Repudiation | "We never told adopters `tolerance: 0` was testing-only" | mitigate | The four-surface triangulation (inline `check_tolerance/2` comment + Plug schema `doc:` + CHANGELOG WEBFIX-01 + `@moduledoc` WR-04 closure) means at least 4 docs-truth tests must all be silenced simultaneously to drop the contract, making accidental regression infeasible |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0
- `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` exits 0 with 9 tests, 0 failures
- `grep -c "testing only" lib/lattice_stripe/webhook/plug.ex` returns at least 1
- `grep -nE '(@moduledoc|tolerance|testing only)' lib/lattice_stripe/webhook/plug.ex | head -20` shows the three substrings present in the `@moduledoc` region (lines ~80-120)
- Full suite still green: `mix test --warnings-as-errors` exits 0 with 178 tests, 0 failures (was 177 in Phase 47 — +1 from the new docs-truth block)
</verification>

<success_criteria>
- WR-04 closed: `lib/lattice_stripe/webhook/plug.ex` `@moduledoc` Configuration Options block surfaces `tolerance: 0` testing-only language with cross-references to `check_tolerance/2` inline comment AND v1.5 CHANGELOG WEBFIX-01 entry
- Docs-truth grep test added: `test/lattice_stripe/docs_truth_test.exs` has a 9th test block (`"Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics"`) using `~r/@moduledoc.*tolerance.*0.*testing only/s` to enforce co-location
- Full suite green: 178 tests, 0 failures
- No modifications outside the @moduledoc block (NimbleOptions schema `doc:` string at `:145` untouched)
- D-03 sub-decision 3E satisfied; Phase 47 deferred WR-04 closed per the `addressed_in: "Phase 48"` directive in `47-VERIFICATION.md`
</success_criteria>

<output>
Create `.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-01-SUMMARY.md` when done. Include:
- Confirmed `@moduledoc` line range edited (e.g., `plug.ex:116-118` after the +2 line addition)
- Line range of the new docs-truth test block (e.g., `docs_truth_test.exs:188-200`)
- Full suite test count delta (177 → 178)
- Confirmation that `mix ci` is green
</output>
