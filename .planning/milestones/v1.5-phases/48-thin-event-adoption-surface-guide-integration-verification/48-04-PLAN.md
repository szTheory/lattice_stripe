---
phase: 48-thin-event-adoption-surface-guide-integration-verification
plan: 04
type: execute
wave: 2
depends_on: ["48-01", "48-02", "48-03"]
files_modified:
  - test/lattice_stripe/docs_truth_test.exs
autonomous: true
requirements: [GUIDE-03, VERIFY-03]
must_haves:
  truths:
    - "Drift in any locked phrase of `guides/webhooks-thin-events.md` (function names, error atoms, rate-limit, idempotency anchor, Connect anchor, truth anchor, canonical surface name, verification-vs-payload-shape phrasing) fails CI (D-03 sub-decision 3A)."
    - "The new guide's install snippet `~> 1.5` is locked as the v1.5 canary; flipping the cross-cutting `~> 1.3` → `~> 1.5` across README/getting-started/cheatsheet naturally enforces lockstep via the existing tests at `docs_truth_test.exs:54+` (D-03 sub-decision 3B)."
    - "The ExDoc `extras` + `Operations & DX` placement of the new guide is locked in the existing `\"exdoc keeps the primary public truth surfaces published\"` test (D-03 sub-decision 3C)."
    - "The cross-link graph (forward links from new guide; reverse links from `webhooks.md`, README, JTBD) is grep-locked (D-03 sub-decision 3D)."
  artifacts:
    - path: "test/lattice_stripe/docs_truth_test.exs"
      provides: "3 new test blocks (3A, 3B, 3D) + 1 in-place extension to the existing ExDoc placement test (3C)"
      contains: "webhooks-thin-events guide locks the thin-event adopter contract"
  key_links:
    - from: "test/lattice_stripe/docs_truth_test.exs `webhooks-thin-events guide locks ...`"
      to: "guides/webhooks-thin-events.md"
      via: "File.read! + series of `assert guide =~ \"substring\"` assertions"
      pattern: "File.read!.*webhooks-thin-events.md"
    - from: "test/lattice_stripe/docs_truth_test.exs `webhooks-thin-events guide is the v1.5 install-line canary`"
      to: "guides/webhooks-thin-events.md install snippet"
      via: "assert guide =~ ~> 1.5"
      pattern: "lattice_stripe.*1\\.5"
    - from: "test/lattice_stripe/docs_truth_test.exs `exdoc keeps...` (existing test, extended)"
      to: "mix.exs `extras` + `Operations & DX` group"
      via: "in-place assertion additions"
      pattern: "guides/webhooks-thin-events.md.*in extras"
    - from: "test/lattice_stripe/docs_truth_test.exs `webhooks-thin-events guide is cross-linked...`"
      to: "guides/webhooks.md / README.md / guides/user-flows-and-jtbd.md"
      via: "File.read! + assert source =~ \"webhooks-thin-events.md\""
      pattern: "webhooks-thin-events.md"
---

<objective>
Extend `test/lattice_stripe/docs_truth_test.exs` with the D-03 sub-decisions 3A, 3B, 3C, and 3D regression net for `guides/webhooks-thin-events.md` and the discovery wiring Plan 03 just landed. This plan is the load-bearing validation layer for GUIDE-03 — every promise the new guide makes is grep-locked here so drift fails CI.

Purpose: VERIFICATION.md Phase 48 will assert "Drift in code samples must fail CI." That contract is satisfied by this plan. Without these tests, the guide content (Plan 02) and discovery wiring (Plan 03) are unverified; any future PR could silently break either surface and ship.

Output:
- `test/lattice_stripe/docs_truth_test.exs` — three new `test "..."` blocks (3A guide-content lock, 3B install-line canary, 3D cross-link graph lock) appended after the existing WEBFIX-01 test, PLUS one in-place extension to the existing `"exdoc keeps the primary public truth surfaces published"` test (3C placement assertions).

The 3E Plug `@moduledoc` grep test is OWNED BY PLAN 01 (not duplicated here); this plan covers 3A + 3B + 3C + 3D only. After Plan 01 + this plan both land, the docs_truth_test.exs file has the full D-03 sub-decisions 3A through 3E surface covered (4 new tests + 1 in-place extension = 5 total additions across both plans).

**Wave 2 dependency:** This plan depends on Plans 02 AND 03 because:
- 3A grep asserts content in `guides/webhooks-thin-events.md` (Plan 02 created)
- 3B grep asserts the `~> 1.5` install snippet in that guide (Plan 02 added)
- 3C extension asserts `mix.exs` placement (Plan 02 added the entries)
- 3D grep asserts reverse links from `webhooks.md` + README + JTBD (Plan 03 added)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md
@.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md
@test/lattice_stripe/docs_truth_test.exs
@guides/webhooks-thin-events.md
@guides/webhooks.md
@guides/user-flows-and-jtbd.md
@README.md
@mix.exs

<interfaces>
<!-- 3A — new guide content locks (per RESEARCH.md §"Phase 48 extensions per D-03 sub-decisions" → "3A — new guide content locks") -->

Test block 3A:
```elixir
test "webhooks-thin-events guide locks the thin-event adopter contract" do
  guide = File.read!("guides/webhooks-thin-events.md")

  # Function names — the helper surface the guide teaches
  assert guide =~ "parse_event_notification"
  assert guide =~ "fetch_event"
  assert guide =~ "fetch_related_object"

  # Verify-error atoms — locks the verification-vs-payload-shape failure boundary
  assert guide =~ ":no_matching_signature"
  assert guide =~ ":timestamp_expired"
  # Typed-error footguns specific to thin-event helpers
  assert guide =~ ":no_related_object"
  assert guide =~ ":unknown_object_type"

  # Rate-limit phrasing — both substrings required (REQUIREMENTS.md GUIDE-03)
  assert guide =~ "100 req/s"
  assert guide =~ "90/s"

  # Idempotency anchor (GUIDE-03)
  assert guide =~ "event.id"

  # Connect routing anchor (GUIDE-03)
  assert guide =~ "event.context"

  # Canonical truth anchor (Phase 44 D-14)
  assert guide =~ "Webhooks confirm"

  # Canonical surface name
  assert guide =~ "/v2/events"

  # Verification-vs-payload-shape failure boundary phrasing (GUIDE-03)
  assert guide =~ "verification"
  assert guide =~ "payload shape"
end
```

<!-- 3B — install-line canary (B2) -->

Test block 3B:
```elixir
test "webhooks-thin-events guide is the v1.5 install-line canary" do
  # B2 canary architecture (CONTEXT.md D-03 sub-decision 3B): this guide
  # is the ONLY 1.5-only doc until release prep flips README/getting-started/
  # cheatsheet. Existing tests at lines 54, 61, 165 still assert `~> 1.3`
  # for the other docs — when v1.5 is released and someone does the cross-
  # cutting lockstep flip, those tests fail first, naturally enforcing the
  # rule without coupling the install-line refresh to Phase 48.
  guide = File.read!("guides/webhooks-thin-events.md")
  assert guide =~ "{:lattice_stripe, \"~> 1.5\"}"
end
```

<!-- 3C — ExDoc placement (in-place extension to existing test at docs_truth_test.exs:8-40) -->

The existing test "exdoc keeps the primary public truth surfaces published" at lines 8-40 reads `docs = docs_config()`, extracts `extras = docs[:extras]` and `groups = docs[:groups_for_extras] |> Map.new()`, and asserts membership. ADD two new assertions inside that existing test (NOT a new test block):
```elixir
# D-03 sub-decision 3C — new v1.5 trust rail extension to Operations & DX
assert "guides/webhooks-thin-events.md" in extras
assert "guides/webhooks-thin-events.md" in groups["Operations & DX"]
```
Insertion point: after the existing assertions for `"guides/webhooks.md" in groups["Operations & DX"]` (same group; symmetric ordering keeps the sidebar story coherent at the assertion level).

<!-- 3D — cross-link graph lock (new sibling block per RESEARCH.md recommendation to split rather than fold into flagship-guides) -->

Test block 3D:
```elixir
test "webhooks-thin-events guide is cross-linked from README/JTBD/webhooks.md" do
  # D-03 sub-decision 3D cross-link graph: the new v1.5 guide must be
  # reachable from README hardening-ops route, JTBD Start Here Runtime
  # route + Job 7 Read next, AND linked back from the parent webhooks.md
  # guide. Forward edges from the new guide (webhooks.md / testing.md /
  # error-handling.md) are locked here in lockstep. Drift in any of these
  # surfaces fails CI and is the canonical signal that a discovery wire
  # snapped silently.

  # Forward links FROM the new guide
  thin = File.read!("guides/webhooks-thin-events.md")
  assert thin =~ "webhooks.md"
  assert thin =~ "testing.md"
  assert thin =~ "error-handling.md"

  # Reverse link from parent webhook guide
  webhooks = File.read!("guides/webhooks.md")
  assert webhooks =~ "webhooks-thin-events.md"
  assert webhooks =~ "thin event"  # locks the new "Thin events (/v2/events)" closing section in webhooks.md

  # README discovery route
  readme = File.read!("README.md")
  assert readme =~ "webhooks-thin-events.md"

  # JTBD discovery ladder (Start Here Runtime route + Job 7 Read next)
  jtbd = File.read!("guides/user-flows-and-jtbd.md")
  assert jtbd =~ "webhooks-thin-events.md"
end
```

<!-- Existing test file convention (verified against source) -->

`test/lattice_stripe/docs_truth_test.exs` is 186 lines, `use ExUnit.Case, async: true` at line 1, flat `test "..."` blocks (no `describe` wrapper — established convention). Existing test order (per PATTERNS.md): ExDoc placement (lines 8-40) → README (42-56) → getting-started (58-71) → JTBD/recipes (73-100) → flagship-guides (102-160) → cheatsheet 1.3 (162-166) → CHANGELOG 1.3 (168-173) → CHANGELOG 1.5 WEBFIX-01 (175-186).

Insertion ordering for new blocks (per PATTERNS.md §"New-block placement convention"):
1. The 3C edit is IN-PLACE inside the existing ExDoc-placement test (lines 8-40); no new block.
2. The 3A `webhooks-thin-events guide locks the thin-event adopter contract` block is appended AFTER the existing WEBFIX-01 CHANGELOG block (at ~line 186).
3. The 3B `webhooks-thin-events guide is the v1.5 install-line canary` block is appended AFTER 3A.
4. The 3D `webhooks-thin-events guide is cross-linked from README/JTBD/webhooks.md` block is appended AFTER 3B.

Plan 01 (Wave 1) appends the 3E `Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics` block separately. By the end of Wave 2, the file has 8 existing + 4 new = 12 test blocks. (3C is in-place, not new.)

If Plan 01's 3E test landed first (Wave 1) and this plan is run in Wave 2, the 3E test is already there at the end of the file. This plan's blocks (3A + 3B + 3D) should be inserted BEFORE the 3E block to keep the ordering: existing WEBFIX-01 → 3A → 3B → 3D → 3E (Plan 01). Alternative ordering (3E → 3A → 3B → 3D) is also acceptable — `ExUnit` doesn't depend on test ordering. The recommended ordering is to keep all D-03 sub-decisions adjacent for readability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend existing `exdoc keeps the primary public truth surfaces published` test with 3C placement assertions (in-place)</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs (full file — 186 lines; locate the existing `"exdoc keeps the primary public truth surfaces published"` test at lines 8-40 and read it end-to-end before editing)
    - mix.exs (full file — confirms the new guide is in `extras` and `Operations & DX` group per Plan 02's edits; the test reads `mix.exs` indirectly via `docs_config()`)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-03 sub-decision 3C — extras + groups[Operations & DX] both required)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"ExDoc `groups_for_extras` Placement Strategy" → "Docs-truth locks (per CONTEXT.md D-03 sub-decision 3C)")
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"ExDoc placement extension pattern" — D-03 3C edit: extend in-place, do NOT add a new test)
  </read_first>
  <action>
    Edit the EXISTING test block `"exdoc keeps the primary public truth surfaces published"` (located at approximately `test/lattice_stripe/docs_truth_test.exs:8-40`). Do NOT add a new `test "..."` block. Do NOT remove or reorder any existing assertions inside this block.

    Inside the existing test body, find the existing assertion `assert "guides/webhooks.md" in groups["Operations & DX"]` (the test asserts membership of `webhooks.md` in the Operations & DX group — Phase 44 D-06 lock). Immediately after that line, insert TWO new assertions:

    ```elixir
    # D-03 sub-decision 3C — new v1.5 trust rail extension to Operations & DX
    assert "guides/webhooks-thin-events.md" in extras
    assert "guides/webhooks-thin-events.md" in groups["Operations & DX"]
    ```

    Add the single-line comment above the assertions naming the sub-decision (`D-03 sub-decision 3C`) so future maintainers see the contract.

    Do NOT modify any other test block. Do NOT touch the module attributes (`async: true` stays). The `extras` and `groups` local variables already exist in the existing block body (they are set near the top of the test from `docs = docs_config()` — verify in the Read step).
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --only "test:exdoc keeps the primary public truth surfaces published" 2>&1 | tail -10 || mix test test/lattice_stripe/docs_truth_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The existing test `"exdoc keeps the primary public truth surfaces published"` still exists with its original name
    - That test now contains both new assertions: `"guides/webhooks-thin-events.md" in extras` AND `"guides/webhooks-thin-events.md" in groups["Operations & DX"]`
    - The test passes when `mix test` runs (because Plan 02 already added both entries to `mix.exs`)
    - No new top-level `test "..."` block was created for 3C — the extension is in-place per PATTERNS.md
    - `git diff test/lattice_stripe/docs_truth_test.exs` for this task shows ONLY additions inside the existing block (no deletions or reorderings)
    - All other test blocks in the file are byte-identical to their prior content
  </acceptance_criteria>
  <done>
    - The existing ExDoc-placement test passes with 2 new assertions appended in-place
    - No new test block for 3C
    - Other tests untouched
  </done>
</task>

<task type="auto">
  <name>Task 2: Append 3A + 3B + 3D test blocks to `docs_truth_test.exs`</name>
  <files>test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - test/lattice_stripe/docs_truth_test.exs (re-read after Task 1's in-place edit; locate the END of the file and the existing WEBFIX-01 CHANGELOG test at ~lines 175-186; if Plan 01's 3E test has already landed at the end, plan placement BEFORE that block per `<interfaces>` ordering recommendation)
    - guides/webhooks-thin-events.md (the file whose content the 3A + 3B assertions read via `File.read!`)
    - guides/webhooks.md (Plan 03 added closing `Thin events` section — the 3D reverse-link assertion reads this)
    - README.md (Plan 03 added hardening-ops route link — the 3D assertion reads this)
    - guides/user-flows-and-jtbd.md (Plan 03 added Start Here + Job 7 links — the 3D assertion reads this)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-CONTEXT.md (D-03 sub-decisions 3A + 3B + 3D — exact substring list)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-RESEARCH.md (§"Phase 48 extensions per D-03 sub-decisions" — verbatim test bodies for 3A, 3B, 3D)
    - .planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-PATTERNS.md (§"New-block placement convention" — ordering recommendation)
  </read_first>
  <action>
    Append THREE new `test "..."` blocks at the END of `test/lattice_stripe/docs_truth_test.exs`, in this order:

    1. `test "webhooks-thin-events guide locks the thin-event adopter contract" do ... end` — VERBATIM from the `<interfaces>` block above. Contains 16 `assert guide =~ "..."` assertions for: 3 function names + 4 error atoms + 2 rate-limit substrings + 4 anchor substrings (`event.id`, `event.context`, `Webhooks confirm`, `/v2/events`) + 2 verification-vs-payload-shape substrings + 1 file-read.

    2. `test "webhooks-thin-events guide is the v1.5 install-line canary" do ... end` — VERBATIM from the `<interfaces>` block above. Contains the block-comment about B2 canary architecture + 1 `assert guide =~ "{:lattice_stripe, \"~> 1.5\"}"` assertion.

    3. `test "webhooks-thin-events guide is cross-linked from README/JTBD/webhooks.md" do ... end` — VERBATIM from the `<interfaces>` block above. Contains the block-comment about D-03 sub-decision 3D + 4 `File.read!` calls (`thin`, `webhooks`, `readme`, `jtbd`) + 8 substring assertions (3 forward links from the new guide + 2 reverse from webhooks.md + 1 from README + 1 from JTBD; note: JTBD has 2 occurrences of `webhooks-thin-events.md` per Plan 03 acceptance criteria — the single substring grep matches both).

    **PLACEMENT decisions:**
    - If Plan 01's 3E `Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics` test has ALREADY landed (which it should have during Wave 1), the new blocks (3A + 3B + 3D) should be inserted BEFORE the 3E block — i.e., between the existing WEBFIX-01 CHANGELOG block at `:175-186` and the Plan 01 3E block at the end of the file. This keeps the D-03 sub-decisions adjacent (existing → 3A → 3B → 3D → 3E).
    - If Plan 01's 3E test has NOT yet landed (Wave 1 raced and Plan 01 ran after this plan), append at the literal end of file; Plan 01 will re-organize on its own pass.
    - Either ordering is acceptable to ExUnit — the recommendation is for human readability only.

    **CRITICAL — copy the test bodies VERBATIM from the `<interfaces>` block.** The 3A test's assertion list is exhaustive; missing even one substring fails to lock the guide content. Use the exact regex flavors and quoting shown.

    Do NOT add a `describe` wrapper — the file uses flat `test "..."` blocks per established convention.
    Do NOT touch existing blocks (the Task 1 in-place ExDoc extension is the only existing-block edit; all other 8 existing blocks must be byte-identical).
    Do NOT change the module-level `async: true` declaration.

    All 4 new tests (3 from this task + the 3C in-place from Task 1) are async-safe because they only do `File.read!` and string/regex matching.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors 2>&1 | tail -15 && grep -c '^  test "' test/lattice_stripe/docs_truth_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` exits 0 with at least 11 tests passing (8 existing + 3 new from this plan; if Plan 01's 3E test has also landed, 12 total)
    - `grep -c '^  test "' test/lattice_stripe/docs_truth_test.exs` returns 11 or 12 (depending on whether Plan 01's 3E test has landed; specifically: 8 existing + 3 from this task + optionally 1 from Plan 01 = 11 or 12)
    - The 3A test block contains exactly 15 `assert guide =~ "..."` assertion lines (3 function names + 4 error atoms + 2 rate-limit + 4 anchors + 2 verification-vs-payload-shape) plus 1 `File.read!` line = 16 total lines — verify by visual inspection of the diff
    - The 3B test asserts the exact string `{:lattice_stripe, "~> 1.5"}` (with backslash-escaped quotes inside the Elixir string)
    - The 3D test reads 4 distinct files (`guides/webhooks-thin-events.md`, `guides/webhooks.md`, `README.md`, `guides/user-flows-and-jtbd.md`) and contains exactly 8 substring assertions
    - The new test names are exactly: `"webhooks-thin-events guide locks the thin-event adopter contract"`, `"webhooks-thin-events guide is the v1.5 install-line canary"`, `"webhooks-thin-events guide is cross-linked from README/JTBD/webhooks.md"`
    - The full test suite remains green: `mix test --warnings-as-errors` exits 0 (with 178 + 3 new = 181 tests if Plan 01 landed first; 177 + 4 new = 181 same total)
    - If any locked substring is missing from `guides/webhooks-thin-events.md` (Plan 02 output), 3A FAILS RED — that is the correct enforcement (Plan 02 must satisfy every substring); if 3A fails RED, the executor should investigate whether Plan 02's guide is missing a substring rather than weaken the test
  </acceptance_criteria>
  <done>
    - 3 new test blocks appended at the end of the file (or before Plan 01's 3E if it landed)
    - All 3 new tests PASS against the guide content (Plan 02) and discovery wiring (Plan 03)
    - Full suite green at 181 tests (or 180 if Plan 01's 3E hasn't landed; the planner cross-coordinates via wave dependencies)
    - File still uses `async: true` at module level
    - No existing test block modified beyond Task 1's in-place ExDoc extension
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future PR → docs-truth grep | Drift in load-bearing guide phrases, install line, ExDoc placement, or cross-link graph MUST fail CI |
| ExUnit test runner → 4 source files | The tests read 4 separate files; concurrent edits to any of them are caught |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-11 | Tampering | Future PR silently drops idempotency-on-event.id phrasing → adopters dedup on resource state → replay/double-process | mitigate | 3A grep asserts `event.id` in guide body; any PR that removes the substring fails CI |
| T-48-12 | Tampering | Future PR silently drops rate-limit guidance → adopters exceed 100 req/s ceiling | mitigate | 3A grep asserts BOTH `100 req/s` AND `90/s`; either substring missing fails CI |
| T-48-13 | Tampering | Future PR silently disables `:timestamp_expired` documentation → adopters use `tolerance: 0` in production → signature replay attacks | mitigate | 3A grep asserts `:timestamp_expired`; Plan 01's 3E grep asserts the Plug `@moduledoc` `testing only` clause; four-surface triangulation per Plan 01 threat T-48-03 |
| T-48-14 | Tampering | Future PR silently flips guide install line to a fake version → adopters install wrong version | mitigate | 3B grep asserts `{:lattice_stripe, "~> 1.5"}` — guarantees the canary stays at 1.5 until release-prep lockstep |
| T-48-15 | Tampering | Future PR silently removes the new guide from `mix.exs` `extras` or `Operations & DX` → guide vanishes from HexDocs | mitigate | 3C in-place assertions inside the existing ExDoc-placement test enforce presence in both `extras` and `groups["Operations & DX"]` |
| T-48-16 | Tampering | Future PR silently removes one of the 4 discovery wires (README hardening-ops + Webhooks bullet, webhooks.md closing section, JTBD Start Here, JTBD Job 7 Read next) → guide becomes invisible | mitigate | 3D grep asserts presence in webhooks.md (twice: `thin event` + `webhooks-thin-events.md`), README, JTBD — 4 distinct File.read! + substring assertion paths |
</threat_model>

<verification>
- `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors` exits 0 with 11 tests passing (8 existing + 3 new from this plan); if Plan 01 has landed, 12 tests passing
- `grep -c '^  test "' test/lattice_stripe/docs_truth_test.exs` returns 11 or 12
- `git diff test/lattice_stripe/docs_truth_test.exs` shows additions only — no modifications to existing blocks beyond Task 1's in-place 3C extension
- Full test suite: `mix test --warnings-as-errors` exits 0 at 181 tests total
- Each new test, run individually, passes: `mix test test/lattice_stripe/docs_truth_test.exs --only test:"webhooks-thin-events guide locks the thin-event adopter contract"` etc.
- If Plan 02's guide is missing ANY of the 16 locked substrings, 3A reports a specific failing `assert guide =~ "..."` line, signaling exactly which substring is missing
</verification>

<success_criteria>
- D-03 sub-decisions 3A + 3B + 3C + 3D all locked via docs-truth grep tests:
  - 3A: new guide content locks — 16 substring assertions in `"webhooks-thin-events guide locks the thin-event adopter contract"`
  - 3B: install-line canary — 1 assertion in `"webhooks-thin-events guide is the v1.5 install-line canary"`
  - 3C: ExDoc placement — 2 in-place assertions appended to existing `"exdoc keeps the primary public truth surfaces published"` test
  - 3D: cross-link graph — 8 substring assertions across 4 files in `"webhooks-thin-events guide is cross-linked from README/JTBD/webhooks.md"`
- Plan 01's 3E `Webhook.Plug @moduledoc documents tolerance: 0 testing-only semantics` test is unmodified by this plan; if landed first (Wave 1), it sits at the end of the file
- Full suite green: 181 tests (or 180 if Plan 01's 3E hasn't landed; total = 8 existing + Plan 01's 3E + this plan's 3 new = 12, OR 8 + 3 = 11 if Plan 01 races to be the last)
- VERIFY-03 advanced: "Docs-truth regression suite extended so `webhooks-thin-events.md` install/handler snippets stay enforceable" is satisfied; "Drift in code samples must fail CI" contract is locked
- ROADMAP Phase 48 Success Criterion #5 satisfied
</success_criteria>

<output>
Create `.planning/phases/48-thin-event-adoption-surface-guide-integration-verification/48-04-SUMMARY.md` when done. Include:
- Line ranges of the 3 new test blocks (3A, 3B, 3D)
- The in-place edit inside the existing ExDoc placement test (3C)
- Final test count in `docs_truth_test.exs` (expected: 11 or 12 depending on Plan 01 wave-race ordering)
- Confirmation `mix test --warnings-as-errors` is green at 181 total tests (assuming Plans 01 + 02 + 03 + 05 have also landed by the time this plan ships)
- Confirmation that the 3D test reads all 4 expected files and that each file contains the expected `webhooks-thin-events.md` references
</output>
