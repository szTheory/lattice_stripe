---
phase: 43-public-truth-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - test/lattice_stripe/docs_truth_test.exs
  - mix.exs
  - README.md
  - guides/getting-started.md
  - guides/cheatsheet.cheatmd
  - CHANGELOG.md
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "Docs-truth regression checks fail when first-run onboarding install/version snippets drift from the shipped package line."
    - "The tests cover the actual high-visibility onboarding surfaces adopters hit first, not README alone."
    - "Coverage stays repo-local and deterministic; no live Hex, HexDocs, or network dependence is introduced."
  artifacts:
    - path: "test/lattice_stripe/docs_truth_test.exs"
      provides: "public docs-truth regression contract"
      contains: "guides/getting-started.md"
    - path: "mix.exs"
      provides: "ExDoc main/extras metadata used by the regression contract"
      contains: "main: \"getting-started\""
  key_links:
    - from: "mix.exs"
      to: "test/lattice_stripe/docs_truth_test.exs"
      via: "tests assert that the onboarding surfaces remain published and aligned"
      pattern: "getting-started|cheatsheet|CHANGELOG"
---

<objective>
Expand docs-truth regression coverage so public onboarding/version drift fails fast instead of slipping past a README-only test.

Purpose: After plan 01 reconciles the public surfaces, this plan locks the truth in with targeted ExUnit assertions over the actual onboarding entry points.
Output: Updated `test/lattice_stripe/docs_truth_test.exs` with coverage for first-run onboarding/version/package truth and any small supporting metadata adjustments needed to keep the contract accurate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-public-truth-baseline/43-RESEARCH.md
@.planning/phases/43-public-truth-baseline/43-PATTERNS.md
@.planning/phases/43-public-truth-baseline/43-VALIDATION.md
@test/lattice_stripe/docs_truth_test.exs
@README.md
@guides/getting-started.md
@guides/cheatsheet.cheatmd
@CHANGELOG.md
@mix.exs

<interfaces>
Current blind spot:
```elixir
test "readme points users to recipes and the published 1.3 line" do
  readme = File.read!("README.md")

  assert readme =~ "recipes.html"
  assert readme =~ "{:lattice_stripe, \"~> 1.3\"}"
end
```

This passes even when `guides/getting-started.md` still contains `{:lattice_stripe, "~> 1.2"}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs-truth tests to cover the real onboarding surfaces</name>
  <files>
    test/lattice_stripe/docs_truth_test.exs
  </files>
  <read_first>
    test/lattice_stripe/docs_truth_test.exs
    README.md
    guides/getting-started.md
    guides/cheatsheet.cheatmd
    CHANGELOG.md
    mix.exs
    .planning/phases/43-public-truth-baseline/43-RESEARCH.md
    .planning/phases/43-public-truth-baseline/43-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - the test suite reads `guides/getting-started.md` and fails if it contains `~> 1.2`.
    - the test suite asserts `{:lattice_stripe, "~> 1.3"}` on the onboarding surfaces that matter for first-run install truth.
    - the test suite remains small, deterministic, and file-content based.
    - assertions focus on durable public truth, not large fragile wording blocks.
  </acceptance_criteria>
  <action>Expand `test/lattice_stripe/docs_truth_test.exs` so it checks the actual high-visibility onboarding surfaces: README, Getting Started, and cheatsheet at minimum. Reuse the existing `File.read!/1` assertion style and keep the assertions focused on stable truth snippets such as package line, docs publication anchors, and obvious stale wording. Do not add a new parsing tool, custom Mix task, or network-based validation layer.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The regression suite now fails when onboarding/version drift reappears on the real public entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Guard docs metadata that makes the onboarding surfaces publicly reachable</name>
  <files>
    test/lattice_stripe/docs_truth_test.exs
    mix.exs
  </files>
  <read_first>
    test/lattice_stripe/docs_truth_test.exs
    mix.exs
    .planning/phases/43-public-truth-baseline/43-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - tests assert ExDoc still uses `getting-started` as the main page.
    - tests assert the primary public truth surfaces needed by this phase remain published in docs extras as applicable.
    - no network or shell-out based checks are introduced.
    - changes stay inside Phase 43 scope rather than expanding into Phase 44 docs-graph redesign.
  </acceptance_criteria>
  <action>Add or tighten metadata assertions so the regression contract covers not just content but also publication of the primary onboarding surfaces. At minimum, guard that ExDoc still points to `getting-started` as the main page, and preserve assertions that the relevant extras remain part of the public docs set where needed. Keep this narrow: protect the truth baseline, not the entire future navigation graph.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors && rg -n 'getting-started|cheatsheet|CHANGELOG|recipes' test/lattice_stripe/docs_truth_test.exs mix.exs</automated>
  </verify>
  <done>The docs-truth contract now protects both the public onboarding content and the metadata that exposes it to adopters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs files -> regression tests | Tests must encode durable truth facts, not brittle prose, or they will create noise instead of protection |
| docs metadata -> onboarding visibility | If `getting-started` or other primary surfaces stop being published, content-only assertions are insufficient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | T | `docs_truth_test.exs` coverage | mitigate | Add direct assertions for Getting Started and cheatsheet package-line truth so `~> 1.2` style drift fails immediately |
| T-43-04 | T | ExDoc metadata | mitigate | Assert `main: "getting-started"` and the key published extras needed for the truth baseline |
</threat_model>

<verification>
- `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors`
- `mix test test/lattice_stripe/docs_truth_test.exs --warnings-as-errors && rg -n 'getting-started|cheatsheet|CHANGELOG|recipes' test/lattice_stripe/docs_truth_test.exs mix.exs`
</verification>

<success_criteria>
- Docs-truth tests cover the actual onboarding surfaces that can drift in public.
- A stale install snippet like `~> 1.2` in Getting Started would now fail CI locally.
- The regression contract remains lightweight, deterministic, and phase-scoped.
</success_criteria>

<output>
After completion, create `.planning/phases/43-public-truth-baseline/43-02-SUMMARY.md`
</output>
