# v1.12 Cross-Phase Integration Check

**Scope:** Phases 74–78; cross-phase wiring and complete adopter/release flows. This is a static trace of the checked-out source and planning evidence; phase-summary verification commands were not rerun.

## Integration Check Complete

### Wiring Summary

**Connected:** 8 summarized phase-to-phase connections verified across the drift evidence, typed resource decoding, Phoenix adopter harness, CI gate, and release/closeout chain.
**Orphaned:** 0 milestone-scoped exports or API endpoints. This is a headless Elixir SDK: it has public resource functions and no application HTTP routes. Phase 75 resource APIs are called through the host app's transport tests and are not orphaned.
**Missing:** 0 BLOCKER connections. Two edge-state gaps are classified WARNING below.

### API Coverage

The project exposes SDK functions rather than server API routes. Expected consumers are present: Invoice and Refund retrieval are exercised through `PhoenixAdopter.MockTransport`; Checkout.Session creation is called by the Phoenix controller; MeterEventSummary streaming, MeterEvent creation, Balance retrieval, and Webhook.Plug dispatch are called in adopter profile tests. Release scripts consume the adopter project in both local path-dependency and exact Hex-version modes.

### Auth Protection

Not applicable: this package and test host have no user sessions or protected application routes. Webhook authenticity is the relevant inbound trust boundary; the mounted Plug verifies a signed raw body before dispatch and rejects a modified body.

### E2E Flows

**Complete:** 4 integration flows verified from source and recorded evidence.

1. **Drift triage → promoted typed fields:** the Phase 75 evidence refresh establishes exact drift inventory membership plus pinned GA snapshot paths for Invoice and Refund; `Invoice.retrieve/3` and `Refund.retrieve/3` pass responses through production decoders; typed values coexist with unknown fields in `extra`; API-version floors and unchanged default are documented and locked.
2. **Phoenix subscription Checkout → webhook:** `PhoenixAdopter.Endpoint` is supervised with the SDK Finch child; `/checkout` calls `Checkout.Session.create/2` through the injected transport and renders the typed Session result; `/webhooks/stripe` verifies the signature and dispatches an Event. Tampered body is rejected before handler dispatch.
3. **Adopter edge profiles:** B2B invoice retrieval checks explicit Stripe-Version and typed decoding, and a structured SDK error; usage profile traverses two cursor-linked pages and separately checks meter-event identifier versus HTTP idempotency header; Connect profile checks typed Balance responses and per-request Stripe-Account isolation/suppression.
4. **Release and repository closeout:** Phase 78 candidate preflight ties the additive API surface and unchanged Stripe API default to the 2.3.0 proposal; protected merge requires exact-head `ci-gate`; release workflow publishes, and exact-SHA evidence joins tag/GitHub Release/main ancestry/CI, Hex checksum, versioned HexDocs, and published-Hex Phoenix smoke. Separate checkers and final evidence cover PR dispositions and clean synchronized main/worktrees.

**Broken:** 0 flows have an absent cross-phase link.

### Wiring Map

| Provider | Consumer | Verified link | Status |
|---|---|---|---|
| Phase 74 candidate/source triage | Phase 75 field eligibility | `75-EVIDENCE.md` refresh recovers exact additions and pinned GA pointers for Invoice and Refund; explicitly leaves nested Dispute/Billie paths ineligible. | WIRED |
| Phase 75 Invoice decoder/API contract | Phase 77 B2B adopter profile | B2B profile sets `2026-05-27.dahlia`, invokes `Invoice.retrieve/3`, asserts typed amount and transport header. | WIRED |
| Phase 76 Phoenix host and MockTransport | Phase 77 profiles | Profile tests import the shared test host/transport and run as the nested app suite. | WIRED |
| Phase 76 host Checkout and webhook pipeline | Phase 78 published-package smoke | Isolated Hex mode resolves the exact package version and runs `core_flow_test.exs`, exercising Checkout and webhook against that package. | WIRED |
| Phase 77 adopter suite | CI required gate | `ci.yml` resolves nested lock and runs `mix test --warnings-as-errors`; `ci-gate` depends on adopter and checks its success. | WIRED |
| Phase 75 additive API/version evidence | Phase 78 release candidate preflight | Candidate checker requires the four Invoice/Refund field additions and unchanged Stripe API default before 2.3.0 merge. | WIRED |
| Phase 77 + release evidence | Phase 78 publish verification | CI gate is bound to release SHA; release evidence verifies package checksum, docs version, and Hex-backed Phoenix smoke. | WIRED |
| Phase 78 PR/worktree checkers | Final closeout evidence | PR disposition ledger/checker and read-only worktree gate feed the final closeout record. | WIRED |

### Detailed Findings

#### Orphaned Exports

None found among the milestone's key exports. The promoted Invoice/Refund fields are produced by the retrieval decoders, and the adopter profiles call those public APIs. Public SDK surface beyond the milestone's key exports was not exhaustively enumerated because it is outside this cross-phase scope.

#### Missing Connections

None. All expected connections above resolve end-to-end; no BLOCKER found.

#### Warnings

1. **Checkout host error branch lacks an end-to-end assertion (WARNING; ADOPT-02).** `PhoenixAdopter.CheckoutController.create/2` maps SDK errors to HTTP 502 and `checkout_unavailable` (`test_apps/phoenix_adopter/lib/phoenix_adopter.ex:49-50`), but `core_flow_test.exs` covers Checkout success only. The B2B profile proves an SDK structured error, not the host controller's failure response. The core flow is wired; the HTTP error presentation branch is unproven.
2. **Usage pagination covers a populated two-page stream only (WARNING; ADOPT-04).** `usage_reconciliation_profile_test.exs` verifies cursor/filter continuity and typed results over two pages, but not an empty first page or a transport error on a later page. The required pagination path is wired and the successful flow completes; these edge responses are not demonstrated by the adopter profile.

#### Broken Flows

None.

#### Unprotected Routes

None applicable. `/checkout` is a test-only synthetic host route with no authenticated user concept. The webhook route is protected by signature verification, with a valid signature and modified-body rejection both covered.

### Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| DRIFT-01 | Phase 74 stable-source triage → Phase 75 exact inventory refresh and candidate gate | WIRED | Phase 75 refresh supersedes Phase 74's initial no-inventory/deferred conclusion for the Invoice and Refund fields; non-selected nested candidates stay deferred. |
| DRIFT-02 | Phase 75 Invoice/Refund fields → retrieval decoders → Phase 77 B2B profile; unknown keys → `extra` | WIRED | — |
| DRIFT-03 | Phase 75 focused decoder tests/API snapshot → Phase 77 usage of public typed response behavior | WIRED | — |
| DRIFT-04 | Phase 75 minimum-version docs and unchanged default → Phase 78 candidate check and published version contract | WIRED | — |
| ADOPT-01 | Phase 76 checked-out path dependency + host supervision → Phase 77 nested CI job | WIRED | — |
| ADOPT-02 | Phase 76 Phoenix Checkout → typed Session → signed webhook Event; Phase 78 repeats core flow from Hex | WIRED | Checkout controller 502 branch is not asserted (WARNING 1). |
| ADOPT-03 | Phase 77 B2B, usage, and Connect profiles consume SDK contracts in shared host | WIRED | — |
| ADOPT-04 | Phase 77 error, pagination, idempotency, tenant-context, and webhook proofs → required adopter CI | WIRED | Later-page/empty pagination edge states are not covered (WARNING 2). |
| ADOPT-05 | Phase 77 nested deterministic synthetic suite → `ci-gate` → Phase 78 exact-SHA verification | WIRED | — |
| REL-01 | Phase 75 additive API/default evidence → Phase 78 candidate preflight/protected merge → 2.3.0 release | WIRED | — |
| REL-02 | Phase 78 release workflow → Hex package + GitHub Release + versioned HexDocs + exact Hex adopter smoke | WIRED | — |
| CLOSE-01 | Phase 77 required lanes → `ci-gate` on release SHA → release evidence | WIRED | — |
| CLOSE-02 | Phase 78 PR inventory/disposition checker → refreshed empty inventory in final closeout | WIRED | Self-contained maintainer requirement; no earlier product-phase consumer. |
| CLOSE-03 | Phase 78 worktree checker → primary main/origin synchronization and final cleanliness record | WIRED | Self-contained maintainer requirement; no earlier product-phase consumer. |

**Requirements with no cross-phase wiring:** None lack their intended connection. CLOSE-02 and CLOSE-03 are intentionally self-contained repository-governance requirements, but their checker outputs are consumed by Phase 78 closeout. There are no requirements missing from the traceability table.

### Evidence Notes

- Phase 75 provenance: `.planning/phases/75-typed-contract-updates/75-EVIDENCE.md` records the recovered drift output, immutable OpenAPI commit and hashes, exact property pointers, and candidate exclusions. This resolves the apparent mismatch with the earlier Phase 74 partial inventory.
- Adopter wiring: `test_apps/phoenix_adopter/lib/phoenix_adopter.ex`, `test_apps/phoenix_adopter/test/{core_flow_test,b2b_invoice_profile_test,usage_reconciliation_profile_test,connect_context_profile_test}.exs`, and `.github/workflows/ci.yml`.
- Release/closeout wiring and final evidence: `scripts/maintainer/{release_candidate_check,release_evidence_check,pr_closeout_check,worktree_closeout_check}.sh`, `.github/workflows/{release,release-pr-automerge}.yml`, and `78-CLOSEOUT.md`.
- Recorded final closeout claims exact release SHA `1e83a99029f19c24c97549752adf8f57cabc7dd0` separately from the later current-main SHA `7f0eb4ba75da2e256f4e05bdd10cf64551dd154e`; this separation is correctly maintained by the evidence checker flow.
