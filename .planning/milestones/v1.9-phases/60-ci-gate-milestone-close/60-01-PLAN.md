---
phase: 60-ci-gate-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CI-01
must_haves:
  truths:
    - "Guide-only and markdown-only PRs trigger full CI including docs_truth via mix test."
    - "paths-ignore lists only .planning/** on both push and pull_request triggers."
    - "CONTRIBUTING.md no longer claims CI is skipped for docs-only changes."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI runs on guides/** and **.md changes"
      contains: ".planning/**"
    - path: "CONTRIBUTING.md"
      provides: "accurate contributor CI expectations"
      min_version: "docs_truth"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "test/lattice_stripe/docs_truth_test.exs"
      via: "test job runs mix test"
      pattern: "mix test"
---

<objective>
Narrow `.github/workflows/ci.yml` `paths-ignore` so guide and markdown PRs run full CI (CI-01).

Purpose: Make Phase 59 docs_truth locks enforceable on the highest-risk edit surface — copy-paste guide examples.
Output: ci.yml with `.planning/**` only ignored; CONTRIBUTING.md L83 fixed; STATE.md CI-01 blocker cleared.
</objective>

<execution_context>
@/Users/jon/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/60-ci-gate-milestone-close/60-CONTEXT.md
@.planning/phases/60-ci-gate-milestone-close/60-RESEARCH.md
@.planning/phases/60-ci-gate-milestone-close/60-VALIDATION.md
@.github/workflows/ci.yml
@CONTRIBUTING.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow paths-ignore in ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml
    .planning/phases/60-ci-gate-milestone-close/60-CONTEXT.md
    .planning/phases/60-ci-gate-milestone-close/60-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` `on.push.paths-ignore` contains exactly one entry: `.planning/**`
    - `.github/workflows/ci.yml` `on.pull_request.paths-ignore` contains exactly one entry: `.planning/**`
    - `.github/workflows/ci.yml` does NOT contain `**.md` in any paths-ignore block
    - `.github/workflows/ci.yml` does NOT contain `guides/**` in any paths-ignore block
    - No other lines in ci.yml are modified (jobs, permissions, matrix, services unchanged)
    - Commit message body references `60-CONTEXT.md` D-01 approval and CI-01 rationale
  </acceptance_criteria>
  <action>
In `.github/workflows/ci.yml`, replace both `paths-ignore` blocks (lines 6–9 and 11–14) from:

```yaml
    paths-ignore:
      - "**.md"
      - ".planning/**"
      - "guides/**"
```

to:

```yaml
    paths-ignore:
      - ".planning/**"
```

Apply identically under `on.push` and `on.pull_request`. Do NOT add jobs, change permissions, or modify any other workflow content.

Commit with subject:
`ci: narrow paths-ignore so guide/md PRs run docs_truth (CI-01)`

Body must include:
- Reference to maintainer approval in `.planning/phases/60-ci-gate-milestone-close/60-CONTEXT.md` D-01
- Rationale: docs_truth runs in test job; guide/md edits are highest-risk copy-paste surface
  </action>
  <verify>
    <automated>rg -A2 'paths-ignore' .github/workflows/ci.yml | rg -c '\.planning/\*\*' | test "$(rg -A2 'paths-ignore' .github/workflows/ci.yml | rg -c '\.planning/\*\*')" -eq 2; ! rg 'guides/\*\*|\*\*\.md' .github/workflows/ci.yml</automated>
  </verify>
  <done>CI triggers on guide and markdown changes; planning-only edits still skip CI.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONTRIBUTING.md CI note</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    CONTRIBUTING.md
    .planning/phases/60-ci-gate-milestone-close/60-CONTEXT.md
    .github/workflows/ci.yml
  </read_first>
  <acceptance_criteria>
    - `CONTRIBUTING.md` does NOT contain the phrase `CI is skipped for documentation-only`
    - `CONTRIBUTING.md` does NOT contain `maintainer bypass of CI status checks`
    - `CONTRIBUTING.md` mentions that changes to guides, README, or other `.md` files run full CI including `docs_truth_test.exs` (or `docs_truth`)
    - Line ~83 area updated (former stale note replaced)
  </acceptance_criteria>
  <action>
Replace CONTRIBUTING.md line 83:

**From:**
`**Note:** Docs-only PRs (changes only to `.md`, `.planning/`, or `guides/` files) may require maintainer bypass of CI status checks since CI is skipped for documentation-only changes.`

**To:**
`**Note:** Changes to guides, `README.md`, or other `.md` files (outside `.planning/`) run the full CI suite, including `test/lattice_stripe/docs_truth_test.exs`. Planning-only edits under `.planning/` still skip CI.`

Keep surrounding PR workflow steps unchanged.
  </action>
  <verify>
    <automated>rg -n 'docs_truth' CONTRIBUTING.md && ! rg -n 'CI is skipped for documentation-only|maintainer bypass' CONTRIBUTING.md</automated>
  </verify>
  <done>Contributor docs match post-CI-01 workflow behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Clear CI-01 blocker in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    .planning/STATE.md
    .planning/phases/60-ci-gate-milestone-close/60-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `.planning/STATE.md` Blockers/Concerns section does NOT list CI-01 as open
    - `.planning/STATE.md` Accumulated Context notes CI-01 satisfied (or resolved) as of Phase 60 Plan 1
    - `.planning/STATE.md` Pending Todos does NOT include "Await explicit approval before CI workflow edit"
    - Current Position reflects Phase 60 Plan 1 complete (or in progress toward Plan 2)
  </acceptance_criteria>
  <action>
Update `.planning/STATE.md`:

1. Remove or resolve CI-01 from `### Blockers/Concerns` — replace with note that CI-01 shipped in Phase 60 Plan 1 (2026-05-27).
2. Update `### Decisions` bullet "CI-01 still open" → "CI-01 resolved — paths-ignore narrowed to `.planning/**` only (Phase 60)."
3. Remove "Await explicit approval before CI workflow edit" from `### Pending Todos`.
4. Set `Current Position` Plan to `01 complete` or `02 next` as appropriate.

Do NOT set `status: maintenance` here — that is Plan 2 milestone close scope.
  </action>
  <verify>
    <automated>! rg -n 'CI-01 still open|requires explicit workflow approval|Await explicit approval before CI' .planning/STATE.md</automated>
  </verify>
  <done>STATE.md reflects CI-01 no longer blocking execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML -> CI behavior | Incorrect paths-ignore leaves docs unguarded — stale copy-paste patterns ship |
| workflow scope -> supply chain | CONTEXT D-02 limits edit to paths-ignore only — no new actions or permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | T | ci.yml paths-ignore | mitigate | Remove `**.md` and `guides/**`; keep `.planning/**` only per D-04 |
| T-60-02 | I | CONTRIBUTING.md | mitigate | Align contributor expectations with actual CI behavior post-change |
</threat_model>

<verification>
- `rg -A2 'paths-ignore' .github/workflows/ci.yml` shows only `.planning/**` twice
- `! rg 'guides/\*\*|\*\*\.md' .github/workflows/ci.yml`
- `rg -n 'docs_truth' CONTRIBUTING.md`
- `mix test test/lattice_stripe/docs_truth_test.exs` exits 0 (sanity — no test changes expected)
</verification>

<success_criteria>
- CI-01: Guide-only and markdown-only PRs run docs_truth via existing test job.
- Commit documents D-01 approval per D-03.
- No JTBD-MAP or milestone-close edits in this plan (D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/60-ci-gate-milestone-close/60-01-SUMMARY.md`
</output>
