---
phase: 65-webhook-objecttypes-testing-fixtures
plan: 04
type: execute
wave: 3
depends_on: ["65-02"]
files_modified:
  - lib/lattice_stripe/object_types.ex
  - test/lattice_stripe/object_types_test.exs
autonomous: true
requirements: [OBJ-01]

must_haves:
  truths:
    - "ObjectTypes.maybe_deserialize/1 returns %LatticeStripe.Entitlements.ActiveEntitlementSummary{customer: \"cus_ABC123customer\"} for the summary fixture, and the result has no :id key."
    - "ObjectTypes.maybe_deserialize/1 returns %LatticeStripe.Billing.MeterEvent{event_name: \"api_call\"} for the meter-event fixture — asserted on event_name, never on .object, because the struct has no :object field."
    - "ObjectTypes.maybe_deserialize/1 returns %LatticeStripe.Billing.MeterEventSummary{} for the meter-event-summary fixture, including its float aggregated_value."
    - "fetch_module/1 returns {:ok, Module} for all four Phase 65 keys."
    - "OBJ-01/adjacency edge: each new key is distinct from all 48 pre-existing @object_map keys — none of the four resolved before this phase (all returned :error, measured at plan time), so no row shadows or is shadowed by an existing one. A duplicate literal key would also be caught at compile time, since a repeated map key warns and mix compile --warnings-as-errors is a gate step."
    - "OBJ-01/empty edge: maybe_deserialize/1 returns nil for nil, the input unchanged for a binary, %{} for an empty map, and the input map unchanged when it carries no \"object\" key — the last case is what permanently routes the meter_error_report data payload past the registry."
    - "OBJ-01/encoding edge: registry lookup is exact byte equality on the wire string via Map.fetch/2 — no case folding, no Unicode normalization, no trimming. A case-variant or whitespace-padded key returns :error."
    - statement: "OBJ-01/ordering edge: dispatch is a single-key Map.fetch/2 against an unordered Elixir map, so no output ordering is observable and the placement of the new rows in the source literal cannot affect resolution."
      verification: backstop
    - "The two pre-existing locks written by Phase 64 Plan 04 remain present and green: the meter_error_report absence refute and its positive round-trip twin."
    - "No test in test/lattice_stripe/webhook/ regresses from the four new fetch_module/1 rows."
  artifacts:
    - lib/lattice_stripe/object_types.ex
    - test/lattice_stripe/object_types_test.exs
  key_links:
    - "@object_map → maybe_deserialize/1's Map.fetch dispatch (the typed-decode path)"
    - "@object_map → fetch_module/1 → Webhook.fetch_related_object/3's fail-fast HTTP gate (the SECOND consumer; each new row flips a branch there)"
    - "billing.meter_event row → %MeterEvent{} now reachable from maybe_deserialize/1 → its custom defimpl Inspect is what keeps :payload out of adopter logs"
  prohibitions:
    - statement: "MUST NOT add an :id field to LatticeStripe.Entitlements.ActiveEntitlementSummary — Stripe's object genuinely has no id, and adding one misrepresents the wire shape to every adopter who reads the struct."
      status: active
      verification: "the summary dispatch test asserts refute Map.has_key?(result, :id), and active_entitlement_summary.ex must be byte-identical after this plan"
    - statement: "MUST NOT remove or weaken the custom defimpl Inspect on LatticeStripe.Billing.MeterEvent that hides :payload — registering billing.meter_event is what routes this struct into adopter logs, and payload carries the customer-mapping key and the metered value."
      status: active
      verification: "a regression assertion that inspect/1 of a deserialized %MeterEvent{} omits the payload values, and meter_event.ex must be byte-identical after this plan"
---

<objective>
Complete OBJ-01 by adding the remaining three `@object_map` rows and proving all four dispatch,
including the phase's two distinctive edge cases: the summary that has no `:id` and the meter event
that has no `:object`.

Purpose: 65-01 proved the mechanism on one key. This plan expands it to the full set. The rows
themselves are three lines; the value is entirely in the assertions, because two of the four target
structs deviate from the standard shape in ways that make the obvious assertion raise `KeyError`.

Output: a registry that types all four Phase 65 objects, with the deliberate absence of a fifth
key still locked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-RESEARCH.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-PATTERNS.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-01-SUMMARY.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-02-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section in `65-01-PLAN.md` for the full phase-wide list.
This plan contributes the three remaining `@object_map` keys:
`entitlements.active_entitlement_summary`, `billing.meter_event`, and
`billing.meter_event_summary`. Registry size after this plan: **52** (48 measured at plan time,
plus 1 from 65-01, plus 3 here).

<tasks>

<task type="auto">
  <name>Task 1: Add the three remaining registry rows and their positive dispatch assertions</name>

  <read_first>
    - `lib/lattice_stripe/object_types.ex` — the `@object_map` literal as extended by 65-01, and the three-clause `maybe_deserialize/1` at the bottom of the file.
    - `lib/lattice_stripe/entitlements/active_entitlement_summary.ex` — confirm `@known_fields` is `~w(object customer entitlements livemode)` and read the two in-source comments (near `:69` and `:80-82`) forbidding an `:id` field. Read only; do not edit.
    - `lib/lattice_stripe/billing/meter_event.ex` — confirm `defstruct` has no `:object`, no `:id`, no `:extra`, and no `@known_fields`, and note the custom `defimpl Inspect` hiding `:payload`. Read only; do not edit.
    - `lib/lattice_stripe/billing/meter_event_summary.ex` — confirm the `~w[...]` square-bracket `@known_fields` sigil and its explanatory comment. Read only; do not edit.
    - `test/lattice_stripe/object_types_test.exs` — the whole file. Specifically: `describe "maybe_deserialize/1"` and the tracer test 65-01 added to it; `describe "fetch_module/1"` and its "resolves all five Tax family object types" batch shape; and the alias line at the top, as rewired by 65-02.
    - `65-02-SUMMARY.md` — the final public meter fixture module names chosen at the Q1 checkpoint.
  </read_first>

  <files>
    lib/lattice_stripe/object_types.ex,
    test/lattice_stripe/object_types_test.exs
  </files>

  <action>
Add exactly three keys to the `@object_map` literal in `lib/lattice_stripe/object_types.ex`:
`"entitlements.active_entitlement_summary"` mapped to
`LatticeStripe.Entitlements.ActiveEntitlementSummary`, `"billing.meter_event"` mapped to
`LatticeStripe.Billing.MeterEvent`, and `"billing.meter_event_summary"` mapped to
`LatticeStripe.Billing.MeterEventSummary`. Copy each key string from the corresponding module's own
`defstruct` `object:` default rather than retyping it — the key must match the wire string byte for
byte, and `MeterEvent` is the exception with no such default, so take that one from
`65-RESEARCH.md` § The Four-vs-Five Resolution. Place the two `billing.meter*` rows adjacent to the
existing `"billing.meter"` row and the summary row next to the `entitlements.active_entitlement`
row 65-01 added. The map is only roughly alphabetical through `"transfer_reversal"` and then
appends by family; `mix format` will not reorder map keys.

**Register exactly three. Do not add a `"billing.meter_error_report"` row.** That payload is v2
thin-event `data` and carries no `"object"` key at all, so `maybe_deserialize/1`'s
`%{"object" => object_type}` head can never match it — the row would be a dead key that the next
contributor assumes works (Phase 64 F-13 / D-14, one-way). It is decoded explicitly through
`Billing.MeterErrorReport.from_event/1`.

Extend the **existing** `describe "maybe_deserialize/1"` block — do not add a new `describe` — with
three positive dispatch tests, each fed from the public fixture surface:

- **Summary:** feed `Testing.Fixtures.Entitlements.active_entitlement_summary_json()` and pattern-match
  `%LatticeStripe.Entitlements.ActiveEntitlementSummary{customer: "cus_ABC123customer"}`. Then
  `refute Map.has_key?(result, :id)`. Do **not** assert on an `:id` field — the struct genuinely has
  none, by deliberate design matching Stripe's object (Phase 63 F-02), and the registry row
  tolerating that is a headline success criterion. Carry a one-line comment recording why the
  `refute` is there, so a future reader does not "fix" the struct.
- **Meter event:** feed the public meter-event fixture and pattern-match
  `%LatticeStripe.Billing.MeterEvent{event_name: "api_call"}`. Assert on `event_name`, **never** on
  `.object` — that struct has no `:object` field and `result.object` raises `KeyError`. Carry a
  comment recording that hazard.
- **Meter event summary:** feed the public meter-event-summary fixture and pattern-match
  `%LatticeStripe.Billing.MeterEventSummary{}` including its float `aggregated_value`, so the
  numeric type is pinned and not silently coerced.

Extend the **existing** `describe "fetch_module/1"` block with one batch test in the shape of the
existing "resolves all five Tax family object types" test, asserting `{:ok, Module}` for all four
Phase 65 keys. Name it for four, not five — the fifth key is deliberately absent and the name
should not imply otherwise.

Add the OBJ-01 encoding assertion in the same `fetch_module/1` block: a case-variant of a real key
(for example the same string with a capitalized first letter) and a whitespace-padded variant each
return `:error`. Registry lookup is exact byte equality through `Map.fetch/2` — no case folding, no
Unicode normalization, no trimming — and that is worth pinning, because a near-miss key is a
silently dead row rather than a loud failure.

Do **not** add a `map_size(ObjectTypes.object_map()) == 52` count assertion. It is brittle against
Phase 66's forthcoming `product.feature` row, and 65-RESEARCH.md § Pitfall 7 recommends against it
explicitly; the per-key `fetch_module/1` assertions cover the same ground without the future false
failure.

Do not modify `active_entitlement_summary.ex`, `meter_event.ex`, or `meter_event_summary.ex`. In
particular, do not add `@known_fields` to `meter_event.ex` — that is open question Q3, resolved as
**no**: `from_map/1` does not consume `@known_fields`, so a decorative attribute would read as
load-bearing to the next contributor. The consequence is accepted drift-report noise in the
scheduled `drift.yml` workflow, which never gates a PR.
  </action>

  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs &amp;&amp; mix test &amp;&amp; mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix credo --strict</automated>
  </verify>

  <acceptance_criteria>
    - `mix run -e 'for k <- ["entitlements.active_entitlement","entitlements.active_entitlement_summary","billing.meter_event","billing.meter_event_summary"], do: IO.inspect(LatticeStripe.ObjectTypes.fetch_module(k))'` prints `{:ok, Module}` four times, none `:error`
    - `mix run -e 'IO.puts(map_size(LatticeStripe.ObjectTypes.object_map()))'` prints `52`
    - `mix run -e 'IO.inspect(LatticeStripe.ObjectTypes.fetch_module("billing.meter_error_report"))'` prints `:error`
    - `grep -c 'billing.meter_error_report' lib/lattice_stripe/object_types.ex` returns 0
    - `test/lattice_stripe/object_types_test.exs` contains `refute Map.has_key?(result, :id)` within the summary dispatch test
    - `grep -c 'result.object' test/lattice_stripe/object_types_test.exs` returns 0 (asserting `.object` on a `%MeterEvent{}` raises `KeyError`)
    - `grep -c 'describe "maybe_deserialize/1"' test/lattice_stripe/object_types_test.exs` returns 1 (extended, not duplicated)
    - `grep -c 'describe "fetch_module/1"' test/lattice_stripe/object_types_test.exs` returns 1 (extended, not duplicated)
    - `grep -c 'map_size' test/lattice_stripe/object_types_test.exs` returns 0
    - `git diff --exit-code lib/lattice_stripe/entitlements/active_entitlement_summary.ex lib/lattice_stripe/billing/meter_event.ex lib/lattice_stripe/billing/meter_event_summary.ex` shows no changes
    - Mutation check: removing any one of the three new rows makes exactly its own dispatch test and the four-key batch test fail, and nothing else. Restore afterward.
    - `mix test` exits 0, count >= 2310. A lone `client_test.exs:912` or `batch_test.exs:72` failure is a known pre-existing flake — re-run once.
  </acceptance_criteria>

  <done>
    All four Phase 65 wire strings resolve through `fetch_module/1` and deserialize through
    `maybe_deserialize/1`; the no-`id` summary and the no-`object` meter event are each asserted on
    a field they actually have; the fifth key remains absent. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the two pre-existing locks and the webhook fail-fast gate did not regress</name>

  <read_first>
    - `test/lattice_stripe/object_types_test.exs` lines 175-235 — the two tests Phase 64 Plan 04 already wrote: the meter-error-report round-trip at roughly `:181-191` and the absence `refute` at roughly `:217-227`. Read them before writing anything.
    - `lib/lattice_stripe/webhook.ex` lines 429-456 — `fetch_related_object/3` and the `ObjectTypes.fetch_module/1` branch that gates its HTTP request.
    - `test/lattice_stripe/webhook/fetch_test.exs` lines 190-230 and 295-315 — the existing `unknown_object_type` fail-fast assertions.
    - `lib/lattice_stripe/billing/meter_event.ex` lines 110-150 — the custom `defimpl Inspect` and which fields it exposes.
  </read_first>

  <files>
    test/lattice_stripe/object_types_test.exs
  </files>

  <action>
**Verify, do not re-author.** Phase 64 Plan 04 already wrote both meter-error-report locks. Confirm
each is present and green after the three new rows landed:

- the absence lock — `refute Map.has_key?(ObjectTypes.object_map(), "billing.meter_error_report")`
  plus `assert ObjectTypes.fetch_module("billing.meter_error_report") == :error`, inside
  `describe "fetch_module/1"`, carrying an eight-line comment that names Phase 65 and OBJ-01
- the positive twin — the real `MeterErrorReport` fixture fed through `maybe_deserialize/1` with
  `assert result == data` and `refute is_struct(result)`, inside `describe "maybe_deserialize/1"`

Writing a second copy of either produces a duplicate `describe` pair and is the specific failure
65-RESEARCH.md § Pitfall 7 warns about. If either is missing or red, that is a regression to
investigate and report, not to paper over with a fresh test.

**Run the webhook regression check.** `@object_map` is dual-purpose: `fetch_module/1` is also the
fail-fast gate in `Webhook.fetch_related_object/3`, where an unknown type short-circuits to
`{:error, {:unknown_object_type, type}}` with zero HTTP requests (Phase 47 D-05). Each row added
this phase flips that branch for its type — `fetch_related_object/3` will now issue
`GET related_object.url` for all four. Run the full `test/lattice_stripe/webhook/` suite and
confirm nothing regressed. Verified at plan time: no test there asserts `unknown_object_type` on
any Phase 65 string — the only asserted unknown type is `"v2.core.account"`, which is unaffected —
so this is expected to be green on the first run. If a failure does appear, it is a real coupling
regression, not a flake.

Do **not** "fix" the coupling. It is intentional and documented. Note in the summary that
`entitlements.active_entitlement_summary` in particular is not individually retrievable — it has
no `id` and no single-object URL — so were Stripe ever to deliver it as a v2 `related_object`, the
GET would 404 rather than returning `{:error, {:unknown_object_type, _}}`. Stripe delivers
entitlement summaries as v1 snapshot events, so this path is inert today (Assumption A4).

**Add one regression assertion for the `MeterEvent` Inspect masking (T-65-02).** Registering
`billing.meter_event` is exactly what lets `maybe_deserialize/1` return a `%MeterEvent{}` into
adopter logs and crash dumps, and its `payload` carries the customer-mapping key and the metered
value. Assert that `inspect/1` of a deserialized `%MeterEvent{}` does not contain the payload's
values while still showing `event_name`, so the existing masking cannot be silently removed by a
later refactor. Place it in the existing `describe "maybe_deserialize/1"` block.
  </action>

  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs test/lattice_stripe/webhook/ &amp;&amp; mix test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'billing.meter_error_report' test/lattice_stripe/object_types_test.exs` returns exactly the pre-plan count plus 0 — no new occurrence was authored
    - `grep -c 'refute Map.has_key?(ObjectTypes.object_map()' test/lattice_stripe/object_types_test.exs` returns 1 (not 2 — the lock was verified, not duplicated)
    - `grep -c 'refute is_struct(result)' test/lattice_stripe/object_types_test.exs` returns 1
    - `mix test test/lattice_stripe/webhook/` exits 0
    - `mix test test/lattice_stripe/object_types_test.exs` exits 0
    - The new Inspect assertion fails if the `defimpl Inspect` block is removed from `lib/lattice_stripe/billing/meter_event.ex` — verify by temporarily deleting it, observing the failure, and restoring
    - `git diff --exit-code lib/lattice_stripe/billing/meter_event.ex` shows no change
    - `mix test` exits 0, count >= 2311
  </acceptance_criteria>

  <done>
    Both Phase 64 locks are confirmed present and green with no duplicates; the webhook fail-fast
    suite is green against the widened registry; the `MeterEvent` payload masking is now protected
    by an assertion. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe → `Webhook.construct_event/4` → `maybe_deserialize/1` | HMAC-verified but attacker-influenced webhook JSON crosses into typed decoding. This plan widens which payloads get typed by three object types. |
| Stripe v2 thin event → `Webhook.fetch_related_object/3` | The registry gates whether an outbound HTTP request is made at all. |
| `%MeterEvent{}` → adopter logs / crash dumps | `payload` carries the customer-mapping key and the metered value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-01 | Tampering | `@object_map` gaining a dead `billing.meter_error_report` row | medium | mitigate | The row is unimplementable by construction (no `"object"` key in the payload). Absence stays locked by the Phase 64 `refute` at `object_types_test.exs:217-227`, verified in Task 2 and asserted by a `grep -c` acceptance criterion on `object_types.ex`. |
| T-65-02 | Information Disclosure | `LatticeStripe.Billing.MeterEvent` custom `defimpl Inspect` | high | mitigate | Registering `billing.meter_event` is what routes this struct into adopter-visible output, so this plan adds the behavioural regression assertion that `inspect/1` omits payload values, plus a `git diff --exit-code` criterion on the module. Mutation-checked by temporarily deleting the `defimpl`. |
| T-65-04 | Spoofing / SSRF-adjacent | `Webhook.fetch_related_object/3` via `ObjectTypes.fetch_module/1` | low | accept | All four rows flip the fail-fast branch to "issue `GET related_object.url`". The url originates in Stripe's own signed `related_object`, never in user input, and `fetch_module/1` still gates unknown types. Regression-checked across `test/lattice_stripe/webhook/`. Intentional; documented in Task 2 rather than "fixed" (Phase 47 D-05, Pitfall 1). |
| T-65-05 | Denial of Service | `from_map/1` on untrusted webhook JSON for the three new types | low | accept | No atom-exhaustion vector: no `String.to_atom/1` anywhere on this path. `ActiveEntitlementSummary` and `MeterEventSummary` use string `@known_fields` allowlists; `MeterEvent` reads explicit string keys and drops the rest. Phase 64 D-18 already declined to atomize error codes. |
| T-65-SC | Tampering | package-manager installs | low | accept | Zero packages installed; no `mix.exs` `deps/0` change. |
</threat_model>

<verification>
The five-step differential phase gate from `65-VALIDATION.md` (NOT `mix ci`, which is RED at clean
HEAD on 38 pre-existing warnings):

1. `mix format --check-formatted && mix compile --warnings-as-errors` — green
2. `mix credo --strict` — green
3. `mix test` — green, count >= 2305 (never fewer); a lone `client_test.exs:912` or
   `batch_test.exs:72` failure is a known pre-existing flake, re-run once
4. `mix docs` exits 0 and `mix docs 2>&1 | grep -c 'warning:'` returns <= 38
5. `mix docs 2>&1 | grep -ciE 'entitlement|meter|testing|fixture'` returns 0 — unconditional

Plus the targeted regression lane: `mix test test/lattice_stripe/webhook/`.
</verification>

<success_criteria>
- All four Phase 65 keys resolve; `map_size` is 52
- `billing.meter_error_report` is still absent and still locked, with no duplicated test
- The no-`id` summary and the no-`object` meter event each assert on a field they actually have
- The `MeterEvent` payload masking is protected by a mutation-checked assertion
- `test/lattice_stripe/webhook/` is green against the widened registry
</success_criteria>

<output>
Create `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-04-SUMMARY.md` when done.
Record the measured `map_size` (expected 52) and confirm both Phase 64 locks were verified rather
than re-authored.
</output>
