---
phase: 61-default-finch-pool-optional-application
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - guides/getting-started.md
  - guides/client-configuration.md
  - CHANGELOG.md
autonomous: true
requirements: [DX-01]

must_haves:
  truths:
    - "getting-started.md and client-configuration.md state a default LatticeStripe.Finch pool now exists and `:finch` is optional (you may still bring your own) (D-04, Scope Fence)"
    - "The docs tell existing BYO-Finch consumers they can disable the default pool via `config :lattice_stripe, start_default_finch: false` (D-05, Pitfall 3)"
    - "CHANGELOG records the additive, backwards-compatible default-pool change (D-04)"
  artifacts:
    - guides/getting-started.md
    - guides/client-configuration.md
    - CHANGELOG.md
  key_links:
    - "docs_truth_test.exs:695 asserts the getting-started guide still mentions \"Finch\" or \"supervision\" — edits must preserve that word"
    - "mix docs --warnings-as-errors must stay green after the guide edits (SC-5)"
---

<objective>
Update the getting-started and client-configuration guides plus the CHANGELOG so adopters learn a default Finch pool now ships, `:finch` is optional, existing wiring still works, and BYO-supervision users can opt out (D-04, D-05; Scope Fence "docs/README/getting-started note that a default pool now exists").

Purpose: The stale "Both `api_key` and `finch` are required" prose is the last place the closed footgun still surfaces to readers. This corrects it and documents the opt-out so upgrading BYO-Finch consumers don't silently run a duplicate idle pool (Pitfall 3).
Output: revised `guides/getting-started.md`, `guides/client-configuration.md`, and a CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-default-finch-pool-optional-application/61-CONTEXT.md
@.planning/phases/61-default-finch-pool-optional-application/61-RESEARCH.md
@guides/getting-started.md
@guides/client-configuration.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the guides — default pool exists, :finch is optional, opt-out documented</name>
  <files>guides/getting-started.md, guides/client-configuration.md</files>
  <read_first>
    - guides/getting-started.md — line 80 reads "Both `api_key` and `finch` are required. Everything else has sensible defaults."; the "Setting Up Finch" section (lines 31-65) tells readers to start a pool manually; the troubleshooting note (lines 213-215) about "no pool found".
    - guides/client-configuration.md — line 14 "Every client requires exactly two options"; the `## Required Options` block (lines 12-31).
    - test/lattice_stripe/docs_truth_test.exs — line 695 asserts the getting-started guide still contains "Finch" or "supervision"; keep that word present. Scan for any other assertion referencing "required" prose before editing.
    - .planning/phases/61-default-finch-pool-optional-application/61-RESEARCH.md — "State of the Art" (stale-prose list) and "Common Pitfalls #3" (duplicate-pool guidance to surface in docs).
  </read_first>
  <action>
    In `guides/getting-started.md`: change line 80 so it no longer says `finch` is required — state that only `api_key` is required and that `:finch` now defaults to the auto-started `LatticeStripe.Finch` pool, with everything else having sensible defaults (D-02, D-04). In the "Setting Up Finch" section, add a short note that a default `LatticeStripe.Finch` pool starts automatically so manual Finch setup is now optional, while keeping the existing manual/BYO instructions valid for users who want their own named pool (the word "Finch" and the supervision guidance must remain — docs_truth_test.exs:695). Add the opt-out line: BYO-supervision users can set `config :lattice_stripe, start_default_finch: false` to prevent the default pool (D-05, Pitfall 3).

    In `guides/client-configuration.md`: change line 14 / the `## Required Options` block so `api_key` is the only required option and `:finch` is documented as optional-with-default (`LatticeStripe.Finch`), still overridable by passing your own pool name (D-02, D-04). Add the same `start_default_finch: false` opt-out note.

    Keep all existing `finch: MyApp.Finch` examples working (they remain valid — explicit override). Do NOT touch any other guide, code, or the Stripe resource surface. Prose only.
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors && mix test test/lattice_stripe/docs_truth_test.exs</automated>
  </verify>
  <done>Neither guide states that `finch` is required; both document the default `LatticeStripe.Finch` pool, the optional `:finch` override, and the `start_default_finch: false` opt-out. `mix docs --warnings-as-errors` is clean and `docs_truth_test.exs` passes.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entry for the additive default-pool change</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md — top of file (lines 1-20); note the Release-Please-managed format (`## [x.y.z]` sections with Conventional-Commit-derived bullets). This project targets a Hex 1.8.0 minor bump for v1.10 (additive).
    - .planning/phases/61-default-finch-pool-optional-application/61-CONTEXT.md — D-04 ("document it as such in the CHANGELOG") and the Success Criteria.
  </read_first>
  <action>
    Add a CHANGELOG entry (Features section, phrased as an additive/backwards-compatible change per D-04) recording: LatticeStripe now ships an optional `LatticeStripe.Application` that starts a default `LatticeStripe.Finch` pool; `:finch` is no longer required (defaults to `LatticeStripe.Finch`); existing callers that pass `:finch` or start their own pool are unaffected; BYO-supervision users can disable the default with `config :lattice_stripe, start_default_finch: false`. Follow the existing CHANGELOG formatting/section conventions (do not invent a version number or timestamp Release Please will manage — match how prior unreleased/feature entries are recorded in this file). Do NOT edit `@version` in `mix.exs` (deferred to release tooling per project convention).
  </action>
  <verify>
    <automated>grep -in 'start_default_finch\|default.*Finch pool\|LatticeStripe.Finch' CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG documents the additive default-pool feature and the `start_default_finch: false` opt-out, in the file's existing format; no `mix.exs` version bump.</done>
</task>

</tasks>

<verification>
- `mix docs --warnings-as-errors` clean (SC-5 docs gate).
- `mix test test/lattice_stripe/docs_truth_test.exs` green (guide edits preserve required assertions).
- `grep -in 'start_default_finch' guides/getting-started.md guides/client-configuration.md CHANGELOG.md` finds the opt-out documented in all three surfaces.
- `mix ci` (phase gate, run at phase close) green.
</verification>

<success_criteria>
- Both guides state the default pool exists and `:finch` is optional; neither says `finch` is required.
- The opt-out (`start_default_finch: false`) is documented for BYO-supervision users (D-05, Pitfall 3).
- CHANGELOG records the additive, backwards-compatible change (D-04).
- `mix docs --warnings-as-errors` and `docs_truth_test.exs` stay green.
</success_criteria>

<output>
Create `.planning/phases/61-default-finch-pool-optional-application/61-02-SUMMARY.md` when done.
</output>
