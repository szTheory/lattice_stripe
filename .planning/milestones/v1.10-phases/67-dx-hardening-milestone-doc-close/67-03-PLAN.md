---
phase: 67-dx-hardening-milestone-doc-close
plan: "03"
type: execute
wave: 2
depends_on: [67-01, 67-02]
files_modified:
  - lib/lattice_stripe/webhook/cache_body_reader.ex
  - lib/lattice_stripe/webhook/plug.ex
  - guides/webhooks.md
  - guides/api_stability.md
  - test/lattice_stripe/docs_truth_test.exs
  - priv/api/current.txt
autonomous: false
requirements: [DX-03]
must_haves:
  truths:
    - "D-08: CacheBodyReader is conditionally public only when Plug is available; HexDocs presents Plug-before-Parsers as canonical and the body-reader route as an advanced non-multipart alternative with PII/memory/no-logging cautions."
    - "D-09: CacheBodyReader is removed from the internal stability exclusion, resolves to the Webhooks ExDoc group, and its module/function public surface is intentionally captured by api_surface_lock_test.exs."
  assumptions:
    - requirement: DX-03
      category: unclassified
      statement: "Fallback edge probe could not classify DX-03; publication assumes the existing optional-Plug guard and docs totality/API lock jointly cover conditional availability. This remains flagged for verifier review."
      status: unresolved
      flagged: true
  artifacts:
    - path: lib/lattice_stripe/webhook/cache_body_reader.ex
      provides: "Public conditional module documentation and public read_body/2 contract"
      exports: ["read_body/2"]
    - path: guides/webhooks.md
      provides: "Canonical versus advanced Phoenix integration guidance and retention cautions"
    - path: guides/api_stability.md
      provides: "Semver classification with CacheBodyReader removed from the internal list"
    - path: priv/api/current.txt
      provides: "Reviewed public module/function snapshot entries"
    - path: test/lattice_stripe/docs_truth_test.exs
      provides: "Conditional publication, grouping, and bounded guide contract"
  key_links:
    - from: lib/lattice_stripe/webhook/cache_body_reader.ex
      to: priv/api/current.txt
      via: "public moduledoc makes the module and read_body/2 visible to ApiSurface"
      pattern: "LatticeStripe.Webhook.CacheBodyReader"
    - from: mix.exs
      to: test/lattice_stripe/docs_truth_test.exs
      via: "existing Webhooks regex grouping resolves the newly public module without a new group"
      pattern: "Webhook|Event|EventNotification"
  prohibitions:
    - requirement: DX-03
      category: safety
      statement: "Published guidance must not present global raw-body retention or multipart requests as the canonical or supported use of CacheBodyReader."
      status: unverified
      flagged: true
---

<objective>
Publish the corrected body reader as a deliberate, conditionally available semver contract with safe Phoenix guidance.

Purpose: Make the advanced raw-body route discoverable without displacing the safer endpoint-ordering default or hiding retention costs.
Output: Public moduledoc, bounded guide/API-stability truth, ExDoc grouping proof, and reviewed API lock per D-08 and D-09.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-RESEARCH.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-PATTERNS.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-01-SUMMARY.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-02-SUMMARY.md

<interfaces>
The module remains wrapped by `if Code.ensure_loaded?(Plug)`. `mix.exs` already groups `LatticeStripe.Webhook.*` under Webhooks via a first-match regex, and `DocsTruthTest.docs_group_of/1` reproduces ExDoc matching semantics. `mix lattice_stripe.api_surface --update` is the only accepted writer for `priv/api/current.txt`.
</interfaces>
</context>

## Artifacts this phase produces

- Newly public module `LatticeStripe.Webhook.CacheBodyReader`.
- Newly semver-locked existing function `LatticeStripe.Webhook.CacheBodyReader.read_body/2`.
- New `priv/api/current.txt` entries for the module and public function/type surface generated by the repository task.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the already-locked CacheBodyReader public contract</name>
  <read_first>.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-02-SUMMARY.md, priv/api/current.txt</read_first>
  <decision>Confirm that execution will publish only the corrected conditional CacheBodyReader.read_body/2 contract already approved in D-07/D-09.</decision>
  <context>This checkpoint records the prior approval required before the semver lock changes; it does not reopen naming, availability, storage, or return-shape decisions.</context>
  <options>
    <option id="confirm-locked-reader">
      <name>Confirm locked reader</name>
      <pros>Publishes exactly the corrected conditional helper the user approved.</pros>
      <cons>The next release makes the module and read_body/2 contract semver-covered.</cons>
    </option>
  </options>
  <action>Read D-07 and D-09 and confirm that the downstream tasks publish only read_body/2, retain the optional-Plug guard and fixed private key, and refresh the API lock through the repository task.</action>
  <verify><automated>rg -n 'D-07:|D-09:' .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</automated></verify>
  <acceptance_criteria>The confirmation records the locked prior approval and does not reopen or modify D-07/D-09.</acceptance_criteria>
  <resume-signal>Select confirm-locked-reader to execute the already-approved contract.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Document the conditional public contract and safe advanced route</name>
  <reversibility rating="one-way">D-08/D-09 make the module and read_body/2 semver-covered at the next release; the user approved this exact conditional public contract during discussion.</reversibility>
  <read_first>lib/lattice_stripe/webhook/cache_body_reader.ex, lib/lattice_stripe/webhook/plug.ex, guides/webhooks.md, guides/api_stability.md, mix.exs, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md, .planning/phases/67-dx-hardening-milestone-doc-close/67-02-SUMMARY.md</read_first>
  <files>lib/lattice_stripe/webhook/cache_body_reader.ex, lib/lattice_stripe/webhook/plug.ex, guides/webhooks.md, guides/api_stability.md</files>
  <action>Replace the hidden moduledoc with public documentation that states conditional Plug availability, exact complete-body state only after terminal `:ok`, the fixed private key, and current-chunk tuple behavior. In the Webhook.Plug moduledoc and `guides/webhooks.md`, keep Plug-before-Parsers as canonical and present CacheBodyReader only for architectures where ordering cannot change per D-08. State that parser-level use retains another connection-lifetime copy, may contain PII, must not be logged wholesale, and is not used for multipart parsing. Remove only CacheBodyReader from `guides/api_stability.md`'s internal list per D-09. Add no configuration, storage, spooling, multipart, or alternate webhook API.</action>
  <verify>
    <automated>mix docs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>HexDocs builds with zero warnings; both source and guide name conditional availability, terminal exact-body semantics, the canonical ordering, advanced status, PII/memory/no-log cautions, and multipart exclusion; the stability guide no longer calls the module internal.</acceptance_criteria>
  <done>D-08's public documentation and D-09's stability classification are complete without expanding the helper's scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock Webhooks grouping and intentionally refresh the public API snapshot</name>
  <read_first>test/lattice_stripe/docs_truth_test.exs, test/lattice_stripe/api_surface_lock_test.exs, mix.exs, guides/api_stability.md, priv/api/current.txt, CONTRIBUTING.md, lib/lattice_stripe/webhook/cache_body_reader.ex, .planning/phases/66-product-feature-attachment/66-04-SUMMARY.md</read_first>
  <files>test/lattice_stripe/docs_truth_test.exs, priv/api/current.txt</files>
  <behavior>
    - `docs_group_of(LatticeStripe.Webhook.CacheBodyReader)` equals `:Webhooks` and the module publishes real docs when Plug is present.
    - The bounded advanced-guide section contains the fixed key, exact terminal body invariant, advanced positioning, and safety limits.
    - API surface diff contains only the approved CacheBodyReader module/function additions plus Error additions already landed by 67-01; regeneration is idempotent.
  </behavior>
  <action>Add named DocsTruthTest assertions for CacheBodyReader's public docs, Webhooks placement, and the bounded advanced-guide section per D-08/D-09. Run `mix lattice_stripe.api_surface` before regeneration and review every addition against the approved Error and CacheBodyReader artifacts. Then run `mix lattice_stripe.api_surface --update`; do not hand-edit the lock. Run the lock test and idempotency check after regeneration. Reject any unrelated public symbol, struct field, protocol implementation, or internal-module change rather than accepting a broad snapshot diff.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/api_surface_lock_test.exs test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors &amp;&amp; mix lattice_stripe.api_surface --check</automated>
  </verify>
  <acceptance_criteria>The module is mechanically grouped and documented, the multi-chunk proof remains green, and the committed API lock admits exactly the D-01/D-02/D-04/D-09 additions with no unrelated surface.</acceptance_criteria>
  <done>DX-03 is discoverable and semver-covered through the repository's standing locks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix endpoint configuration → all parsed requests | A globally configured body reader may retain unrelated request bodies and increase allocation pressure. |
| Public docs → adopter architecture | Ambiguous guidance can cause unsafe global retention or a false multipart guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-03 | Information Disclosure / Denial of Service | `guides/webhooks.md`, CacheBodyReader moduledoc | high | mitigate | Keep endpoint ordering canonical; label parser use advanced; state extra-copy lifetime, PII/no-log, route-scoping, and multipart limits. |
| T-67-02 | Tampering | public `read_body/2` contract | high | mitigate | Tie public docs and semver lock to Plan 67-02's exact-byte integration test. |

## ASVS L1 Control Mapping

| ASVS category | Applicability | L1 control and task mapping | Executable/docs evidence |
|---|---|---|---|
| V2 Authentication | not applicable | No authentication or identity mechanism changes. | Scope confirmed by the Task 2/3 file lists. |
| V3 Session Management | not applicable | Connection-lifetime raw-body retention is not session state. | Scope confirmed by the Task 2/3 file lists. |
| V4 Access Control | not applicable | Publication and documentation add no authorization decision. | Scope confirmed by the Task 2/3 file lists. |
| V5 Input Validation | not applicable | The public helper preserves opaque bytes and must not parse or normalize signed input. | `mix test test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors`. |
| V6 Cryptographic Integrity | applicable | Tasks 2 and 3 bind the public contract and semver lock to 67-02's exact-byte HMAC input invariant per D-08/D-09. | `mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/api_surface_lock_test.exs test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors`; `mix lattice_stripe.api_surface --check`. |

</threat_model>

<verification>
- `mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/api_surface_lock_test.exs test/lattice_stripe/webhook/plug_test.exs --warnings-as-errors`
- `mix docs --warnings-as-errors`
- `mix lattice_stripe.api_surface --check`
</verification>

<success_criteria>
- D-08 and D-09 are covered by public docs, bounded docs-truth tests, ExDoc totality, and the API lock.
- The optional Plug guard is unchanged and public availability is described honestly.
- No global-retention, configurable-storage, or multipart promise enters the public contract.
</success_criteria>

<output>
Create `.planning/phases/67-dx-hardening-milestone-doc-close/67-03-SUMMARY.md` when done.
</output>
