---
phase: 67-dx-hardening-milestone-doc-close
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/error.ex
  - lib/lattice_stripe/client.ex
  - test/lattice_stripe/error_test.exs
  - test/lattice_stripe/client_test.exs
  - guides/error-handling.md
autonomous: false
requirements: [DX-02]
must_haves:
  truths:
    - "D-01/D-02: Error responses expose ordered, duplicate-preserving headers and case-insensitive all-value lookup; test/lattice_stripe/error_test.exs names and proves the contract."
    - "D-03: retry_after is the first valid trimmed non-negative decimal-seconds Retry-After value, is uncapped, and is nil for missing, malformed, negative, suffixed, and HTTP-date values; error_test.exs proves each case."
    - "D-04/D-05: decoded JSON, non-JSON, download, connection, retry-context, and exhausted-retry paths expose metadata from the correct final response; client_test.exs proves the paths without changing retry policy."
    - "Fallback edge DX-02/idempotency: repeated Error.from_response/4 calls on the same inputs return equal errors and preserve the caller-owned header list; error_test.exs proves this explicitly."
    - "Fallback edge DX-02/concurrency: parallel Error.from_response/4 calls are argument-pure and return identical values without shared state; error_test.exs proves this with Task.async_stream/3."
  artifacts:
    - path: lib/lattice_stripe/error.ex
      provides: "Public Error metadata fields, lookup helper, strict parser, and compatible response constructors"
      exports: ["get_header/2", "from_response/3", "from_response/4"]
    - path: lib/lattice_stripe/client.ex
      provides: "Response-header propagation into every HTTP error construction path"
    - path: test/lattice_stripe/error_test.exs
      provides: "Deterministic parser, lookup, idempotency, and concurrency proofs"
    - path: test/lattice_stripe/client_test.exs
      provides: "Transport-to-public-error and terminal-attempt proofs"
    - path: guides/error-handling.md
      provides: "Safe Retry-After consumer guidance"
  key_links:
    - from: lib/lattice_stripe/client.ex
      to: lib/lattice_stripe/error.ex
      via: "decoded and non-JSON response builders pass resp_headers to Error.from_response/4 or an equivalent shared metadata constructor"
      pattern: "Error\\.from_response\\(status, decoded, request_id, resp_headers\\)"
    - from: lib/lattice_stripe/error.ex
      to: guides/error-handling.md
      via: "guide examples consume Error.retry_after and Error.get_header/2 while retaining adopter-owned scheduling policy"
      pattern: "retry_after|get_header"
  prohibitions:
    - requirement: DX-02
      category: safety
      statement: "The exposed response evidence must not silently become SDK-controlled scheduling, blocking request-process sleeps, queueing, or a global rate limiter."
      status: unverified
      flagged: true
---

<objective>
Deliver the end-to-end DX-02 tracer: a consumer receives faithful final-response headers and a strict, uncapped decimal-seconds `retry_after` on every HTTP error path.

Purpose: Let adopters honor Stripe response evidence without coupling the public error contract to the SDK's internal retry scheduling.
Output: Additive `LatticeStripe.Error` API, complete Client propagation, executable behavior locks, and safe consumer guidance per D-01 through D-05.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-RESEARCH.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-PATTERNS.md
@.planning/phases/67-dx-hardening-milestone-doc-close/67-VALIDATION.md

<interfaces>
`LatticeStripe.Response.get_header/2` is the exact semantic analog: it lowercases only for comparison and returns every matching value in tuple-list order.

`LatticeStripe.Client.decode_response/6` sends decoded failures through `build_decoded_response/6` and undecodable bodies through `build_non_json_error/4`; normal requests and downloads share this seam. `maybe_retry/5` already gives the strategy `resp_headers` and returns the final attempt's `error`.

New public contracts fixed by D-01 through D-04:
- `LatticeStripe.Error.headers :: [{String.t(), String.t()}]`, default `[]`
- `LatticeStripe.Error.retry_after :: non_neg_integer() | nil`, default `nil`
- `LatticeStripe.Error.get_header(error, name) :: [String.t()]`
- `LatticeStripe.Error.from_response(status, decoded_body, request_id, headers) :: Error.t()`
- `LatticeStripe.Error.from_response/3` delegates with `[]`
</interfaces>
</context>

## Artifacts this phase produces

- New public struct field `LatticeStripe.Error.headers`.
- New public struct field `LatticeStripe.Error.retry_after`.
- New public function `LatticeStripe.Error.get_header/2`.
- New public function `LatticeStripe.Error.from_response/4`.
- Compatibility-preserved public function `LatticeStripe.Error.from_response/3` delegating with empty headers.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the already-locked Error public contract</name>
  <read_first>.planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md, priv/api/current.txt</read_first>
  <decision>Confirm that execution will implement the exact Error fields and functions already approved in D-01/D-02/D-04.</decision>
  <context>This is a machine-recognized record of the prior approval for a one-way semver contract, not a new design choice and not permission to alter the locked shape.</context>
  <options>
    <option id="confirm-locked-contract">
      <name>Confirm locked contract</name>
      <pros>Implements the user-approved headers, retry_after, get_header/2, and from_response/4 surface exactly.</pros>
      <cons>The next release makes this additive public shape semver-covered.</cons>
    </option>
  </options>
  <action>Read D-01, D-02, and D-04 and confirm that the downstream task names the same fields, defaults, functions, and compatibility delegate without adding alternatives.</action>
  <verify><automated>rg -n 'D-01:|D-02:|D-04:' .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</automated></verify>
  <acceptance_criteria>The confirmation records the locked prior approval and does not reopen or modify D-01/D-02/D-04.</acceptance_criteria>
  <resume-signal>Select confirm-locked-contract to execute the already-approved contract.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Trace one final 429 response from transport into the public Error contract</name>
  <reversibility rating="one-way">D-01, D-02, and D-04 add semver-covered Error fields and functions; the user approved this exact public shape during phase discussion.</reversibility>
  <read_first>lib/lattice_stripe/error.ex, lib/lattice_stripe/client.ex, lib/lattice_stripe/response.ex, lib/lattice_stripe/retry_strategy.ex, test/lattice_stripe/error_test.exs, test/lattice_stripe/client_test.exs, test/lattice_stripe/retry_strategy_test.exs, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>lib/lattice_stripe/error.ex, lib/lattice_stripe/client.ex, test/lattice_stripe/error_test.exs, test/lattice_stripe/client_test.exs</files>
  <behavior>
    - A mocked 429 with ordered mixed-case duplicate headers returns `%Error{headers: exact_headers, retry_after: 60}` and `Error.get_header(error, "retry-after")` returns all matching values in transport order per D-01/D-02.
    - The retry strategy sees the same response headers as the public error; after two attempts with distinct headers, the returned error contains only the final attempt's headers and convenience value per D-05.
    - `Error.from_response/3` remains callable and yields `headers: []`, `retry_after: nil`; connection errors keep the same defaults per D-04.
  </behavior>
  <action>Write the failing focused Error and Client tests first. Extend `defexception`, field documentation, and `t()` with `headers: []` and `retry_after: nil` per D-01. Add public `get_header/2` with the exact ordered, duplicate-preserving, case-insensitive semantics of `Response.get_header/2` per D-02. Make `from_response/4` the metadata-building constructor and retain `/3` as an empty-header delegate per D-04. In `Client`, pass the transport response list into decoded errors and populate the same metadata for non-JSON errors; keep connection errors on struct defaults. Prove a terminal retry returns final-attempt evidence and that strategy context receives the same list per D-05. Do not call the retry strategy's capped parser or alter eligibility, backoff, delay, telemetry, or retry count.</action>
  <verify>
    <automated>mix test test/lattice_stripe/error_test.exs test/lattice_stripe/client_test.exs test/lattice_stripe/retry_strategy_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>`Error.from_response/4` and an actual `Client.request/2` 429 path return exact ordered headers, all duplicate lookup values, an uncapped decimal-seconds convenience value, and final-attempt metadata; `/3`, connection behavior, and existing retry tests remain green.</acceptance_criteria>
  <done>DX-02 works through the real transport-to-error path and is protected by named ExUnit behavior tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand strict parsing, repeatability, parallel purity, and safe adopter guidance</name>
  <read_first>lib/lattice_stripe/error.ex, test/lattice_stripe/error_test.exs, guides/error-handling.md, lib/lattice_stripe/retry_strategy.ex, .planning/phases/67-dx-hardening-milestone-doc-close/67-CONTEXT.md</read_first>
  <files>lib/lattice_stripe/error.ex, test/lattice_stripe/error_test.exs, guides/error-handling.md</files>
  <behavior>
    - The first case-insensitive matching header whose trimmed value parses as `{seconds, ""}` with `seconds >= 0` wins; whitespace and zero are valid, while negative, suffix, malformed, and HTTP-date values yield nil unless a later matching value is valid per D-03.
    - Repeating the constructor with the same list and running it concurrently returns equal structs without changing the input list, resolving the DX-02 idempotency and concurrency probes.
    - Guidance distinguishes the uncapped public fact from the internal five-second scheduling cap and tells Phoenix callers to schedule delayed background work without logging headers or raw bodies wholesale per D-05.
  </behavior>
  <action>Add the complete D-03 parsing matrix, including first-valid duplicate behavior, exact unbounded seconds, idempotent repeated calls, and parallel calls through `Task.async_stream/3`. Keep the parser pure and local to the Error contract. Update the Error struct example and retry guidance with `headers`, `retry_after`, and `get_header/2`; explicitly separate the public uncapped value from existing internal retry behavior and direct Phoenix adopters to delayed background jobs per D-05. Limit examples to response metadata and request identifiers; do not normalize, deduplicate, or encourage wholesale logging.</action>
  <verify>
    <automated>mix test test/lattice_stripe/error_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>All D-03 boundary values have named tests; repeated and parallel construction are deterministic; the guide documents safe, non-blocking consumer handling without changing SDK retry policy.</acceptance_criteria>
  <done>The public convenience is strict and policy-neutral, and both fallback edge probes have executable acceptance predicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe HTTP response → public Error | Untrusted response headers and bodies become adopter-visible application data. |
| Public Error → Phoenix consumer | Adopters may log sensitive metadata or block request processes while honoring delay evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-01 | Information Disclosure / Tampering | `Error.from_response/4`, `Client.decode_response/6` | high | mitigate | Propagate response-only tuples verbatim, parse only strict decimal seconds, preserve raw malformed evidence, and add guide cautions against wholesale logging. |
| T-67-04 | Denial of Service | `guides/error-handling.md` Phoenix guidance | medium | mitigate | Tell consumers to enqueue delayed background work; add no blocking sleep, queue, scheduler, or global limiter to the SDK per D-05. |
| T-67-05 | Tampering | retry context ↔ terminal public error | medium | mitigate | Mox test asserts both consumers receive the same attempt headers and terminal errors use only the final attempt. |

## ASVS L1 Control Mapping

| ASVS category | Applicability | L1 control and task mapping | Executable/docs evidence |
|---|---|---|---|
| V2 Authentication | not applicable | No authentication mechanism, credential verifier, or identity boundary changes in this plan. | Scope confirmed by the Task 2/3 file lists. |
| V3 Session Management | not applicable | This headless library adds no session, cookie, or token lifecycle. | Scope confirmed by the Task 2/3 file lists. |
| V4 Access Control | not applicable | No authorization decision or protected resource is introduced. | Scope confirmed by the Task 2/3 file lists. |
| V5 Input Validation | applicable | Tasks 2 and 3 enforce strict trimmed non-negative decimal-seconds parsing and retain malformed input only as raw response evidence per D-03. | `mix test test/lattice_stripe/error_test.exs test/lattice_stripe/client_test.exs test/lattice_stripe/retry_strategy_test.exs --warnings-as-errors`; bounded safety prose in `guides/error-handling.md`. |
| V6 Cryptographic Integrity | not applicable | This plan does not construct or verify webhook signatures and does not change cryptographic operations. | Webhook byte integrity is owned by 67-02/67-03. |

</threat_model>

<verification>
- `mix test test/lattice_stripe/error_test.exs test/lattice_stripe/client_test.exs test/lattice_stripe/retry_strategy_test.exs --warnings-as-errors`
- `mix format --check-formatted`
</verification>

<success_criteria>
- DX-02 is reachable through the real Client response path for JSON, non-JSON, download, connection, and exhausted-retry cases.
- D-01 through D-05 are each enforced by a named behavior test or a bounded guide assertion.
- No retry-policy, scheduler, queue, blocking-sleep, or global-limiter behavior changes.
</success_criteria>

<output>
Create `.planning/phases/67-dx-hardening-milestone-doc-close/67-01-SUMMARY.md` when done.
</output>
