---
phase: 64-meter-event-summary-reads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/billing/meter_event_summary.ex
  - mix.exs
  - test/support/fixtures/metering.ex
  - test/lattice_stripe/billing/meter_event_summary_test.exs
autonomous: false
requirements: [MTR-01]

must_haves:
  truths:
    - "LatticeStripe.Billing.MeterEventSummary.list/4 issues GET /v1/billing/meters/{meter_id}/event_summaries and returns {:ok, %LatticeStripe.Response{}} whose data is a %LatticeStripe.List{} of %MeterEventSummary{} structs (MTR-01, D-05)"
    - "customer, start_time, end_time and value_grouping_window all reach the wire as query params (MTR-01, D-05)"
    - "from_map/1 populates all seven wire fields id, object, aggregated_value, start_time, end_time, meter, livemode; aggregated_value stays a float and is never rounded or coerced to an integer (F-05)"
    - "The struct has no :customer key — the customer association is an input, never an output (F-02)"
    - "MTR-01/empty edge: from_map(nil) returns nil; from_map(%MeterEventSummary{}) returns the same struct unchanged (D-07); a list response with an empty data array decodes to an empty list, not nil"
    - "MTR-01/empty edge: list/4 raises ArgumentError before any network call when meter_id is nil or the empty string (D-09), and when customer, start_time or end_time is absent (D-08) — first failure wins, in the order meter_id, customer, start_time, end_time"
    - "MTR-01/ordering edge: the decoded data list preserves Stripe's wire order exactly; the library never re-sorts or de-duplicates summaries"
    - "The module is registered in the mix.exs groups_for_modules \"Billing Metering\" list, so ExDoc publishes it (D-04)"
  artifacts:
    - lib/lattice_stripe/billing/meter_event_summary.ex
    - test/lattice_stripe/billing/meter_event_summary_test.exs
    - "test/support/fixtures/metering.ex — new nested MeterEventSummary fixture module"
  key_links:
    - "MeterEventSummary.list/4 -> Client.request/2 -> Resource.unwrap_list(&from_map/1)"
    - "mix.exs groups_for_modules \"Billing Metering\" -> LatticeStripe.Billing.MeterEventSummary — a module absent from the group is silently dropped from HexDocs"
    - "private path/1 -> the single interpolated path string that list/4 and (in 64-03) stream!/4 both read, so they cannot diverge"
  prohibitions:
    - "No new metering write surfaces — all four already ship (ROADMAP build constraint, SEED-005 §2.1)"
    - "No Billing.Meter.event_summaries/3,4 convenience delegator (D-04, stripe-java#1852)"
    - "No request-owning module at depth 3 — MeterEventSummary is flat at depth 2 (D-01)"
    - "No retrieve/3 on MeterEventSummary — no GET /{summary_id} exists (F-04)"
    - "No bespoke starting_after pagination loop in this module — LatticeStripe.List owns the cursor state machine (Phase 63 D-05)"
    - "No edit to lib/lattice_stripe/object_types.ex — that file is Phase 65 only"
    - "No new or bumped dependency — mix.lock must stay byte-identical"
    - "Do not regress SEED-005 §6 stability contracts: nil stripe_account omits the header; per-request opts override per-client; api_version default 2026-03-25.dahlia; Client.new!/1 takes a keyword list"
---

<objective>
Prove the whole metering read path end to end with one thin, production-quality vertical slice:
a real `GET /v1/billing/meters/:id/event_summaries` call that flows through guard validation,
`%Request{}` construction, the transport, `Resource.unwrap_list/2`, and `from_map/1` into a typed
`%LatticeStripe.Billing.MeterEventSummary{}` — published in HexDocs and proven by a Mox-at-Transport test.

Purpose: `MeterEventSummary` is the phase's flagship module and its name is a one-way door (D-01).
Proving the full stack on one path before expanding catches an architectural mistake after one commit
instead of after six. Every later plan in this phase expands outward from this slice.

Output: `lib/lattice_stripe/billing/meter_event_summary.ex` with `list/2..4`, `from_map/1`, private
`path/1` and private `validate_id!/2`; a `MeterEventSummary` fixture; ExDoc registration; a passing test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-meter-event-summary-reads/64-CONTEXT.md
@.planning/phases/64-meter-event-summary-reads/64-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces:

**Modules:** `LatticeStripe.Billing.MeterEventSummary`

**Public functions:** `list/2`, `list/3`, `list/4` (arities 2 and 3 exist via default args), `from_map/1`

**Private functions:** `path/1`, `validate_id!/2`

**Struct fields:** `:id`, `:aggregated_value`, `:start_time`, `:end_time`, `:meter`, `:livemode`,
`object: "billing.meter_event_summary"`, `extra: %{}` — and deliberately **no** `:customer` (F-02)

**Module attributes:** `@known_fields` (square-bracket sigil form per D-19/D-20)

**New file paths:** `lib/lattice_stripe/billing/meter_event_summary.ex`,
`test/lattice_stripe/billing/meter_event_summary_test.exs`

**New symbols in existing files:** `LatticeStripe.Test.Fixtures.Metering.MeterEventSummary.basic/1` and
`.list_response/1` in `test/support/fixtures/metering.ex`; one entry in the `mix.exs`
`groups_for_modules` `"Billing Metering"` list

For the phase-wide artifact inventory see `64-09-PLAN.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way module name before any file is created</name>
  <decision>
    Publish the metering read surface as FLAT depth-2 modules named after the wire `object` string —
    `LatticeStripe.Billing.MeterEventSummary` and `LatticeStripe.Billing.MeterErrorReport` — overruling
    the literal text of MTR-01, MTR-02 and ROADMAP success criteria 1 and 2, which all say
    `Billing.Meter.EventSummary`.
  </decision>
  <context>
    This is the phase's only one-way door (CONTEXT D-01). The atom
    `Elixir.LatticeStripe.Billing.MeterEventSummary` becomes a published semver contract the moment
    v1.10 tags on Hex. Renaming after release is a major version bump; a `defdelegate` shim is possible
    but permanently doubles the public surface and appears twice in every HexDocs search.

    The evidence for flat, from D-01: zero of the ~50 request-owning modules in this repo sit at depth 3
    (census `grep -rl "%Request{" lib/` returns 37 at depth 1, 13 at depth 2, 0 at depth 3); within
    `"Billing Metering"` specifically, depth 3 *means* value object; both existing parent-scoped child
    resources (`TransferReversal`, `ExternalAccount`) are flat and wire-named; Stripe's own codegen
    directive is `{class_name: "MeterEventSummary", in_package: "Billing"}`; and the wire↔module transform
    Phase 65 depends on is already 43/43 outside the `LineItem` family.

    The case against, stated fairly: MTR-01/02 and SC#1/#2 say nested, in writing.

    The `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` amendments D-02 requires have ALREADY been
    applied during the context commit — verified at HEAD: REQUIREMENTS.md MTR-01/02 read
    `LatticeStripe.Billing.MeterEventSummary`, MTR-03 reads `reason.error_types`, ROADMAP SC#1/#2/#3 match,
    and Phase 65's OBJ-01 already excludes `billing.meter_error_report`. No artifact edit remains.
  </context>
  <options>
    <option id="flat">
      <name>Flat depth-2, wire-named (D-01, recommended)</name>
      <pros>Matches all ~50 request-owning modules, both parent-scoped precedents, Stripe's codegen directive, all six official SDKs and the Elixir peer stripity_stripe. Preserves the wire↔module mental map Phase 65's ObjectTypes work depends on.</pros>
      <cons>Diverges from the literal written text of two requirements and two success criteria (already amended in the planning artifacts).</cons>
    </option>
    <option id="nested">
      <name>Nested Billing.Meter.EventSummary (the literal requirement text)</name>
      <pros>Matches MTR-01/MTR-02 and SC#1/#2 verbatim as originally written.</pros>
      <cons>Would be the first depth-3 module in the repo to own a %Request{}, actively inverting the established rule that depth 3 means value object. Contradicts the ROADMAP's own build constraint, which points at the two flat templates.</cons>
    </option>
  </options>
  <action>
    Present the decision above and halt until the operator selects. On `flat`, proceed to Task 2 unchanged.
    On `nested`, STOP the plan and report back rather than improvising: the nested choice invalidates the
    module paths in every plan of this phase, the `mix.exs` group entries, the ExDoc placement assertions in
    64-09, and the already-applied REQUIREMENTS and ROADMAP amendments — all of which must be replanned
    rather than patched task by task. Record the selection and the operator's stated reasoning in the plan
    summary either way, because this is the phase's only one-way door and the record is what a future
    contributor will look for when they wonder why the module is not nested.
  </action>
  <resume-signal>Select: flat, or nested</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "read one page of usage summaries" — one path only</name>
  <files>lib/lattice_stripe/billing/meter_event_summary.ex, mix.exs, test/support/fixtures/metering.ex, test/lattice_stripe/billing/meter_event_summary_test.exs</files>
  <read_first>
    - `lib/lattice_stripe/transfer_reversal.ex` — READ IN FULL. The primary template: parent-scoped, flat, wire-named. Lines 63-66 are the correct square-bracket `@known_fields` form; 68-80 the defstruct with wire-object default; 227-244 the `list/4` transliteration target; 246-250 the bang twin.
    - `lib/lattice_stripe/entitlements/active_entitlement.ex` lines 70-118 and 205-240 — the narrower alias form, the `@list_path` single-source-of-truth idiom (73-75), the guard-first ordering, and the `from_map/1` clause ordering with its load-bearing comment at 213. NOTE line 79 uses the parenthesised sigil form, which is the `drift.ex:210` miscount bug — do NOT copy that line's bracket style.
    - `lib/lattice_stripe/external_account.ex` lines 267-278 — the private `validate_id!/2` shape to copy (its message is generic and is NOT copied).
    - `lib/lattice_stripe/billing/meter.ex` lines 90-112 — the verbatim `Resource.require_param!/3` message lock, including the article change on `an event_name`.
    - `lib/lattice_stripe/resource.ex` lines 110-125 — `require_param!/3` checks `Map.has_key?/2` only: key presence, not emptiness, string keys only.
    - `lib/lattice_stripe/list.ex` lines 1-60 and 240-300 — `from_json/3`, `last_item_id/1` cursor derivation on RAW maps, `build_next_page_request/1`.
    - `test/support/fixtures/metering.ex` — READ IN FULL (136 lines). This file ALREADY EXISTS with nested `Meter`, `MeterEvent`, `MeterEventAdjustment`, `MeterEventStreamSession` modules. You are ADDING a nested module, not creating the file.
    - `test/support/fixtures/entitlements.ex` lines 1-27 — the 6-line `PROMOTION TARGET (Phase 65 / OBJ-02)` header comment and the `overrides \\ %{}` / `Map.merge(overrides)` convention.
    - `test/support/test_helpers.ex` lines 45-70 — `list_json/3` signature `(items, url \\ "/v1/objects", has_more \\ false)`.
    - `test/lattice_stripe/entitlements/active_entitlement_test.exs` — the Mox-at-Transport request-assertion idiom for a list call.
    - `mix.exs` lines 179-190 — the `"Billing Metering"` group; note the ordering rule that each parent is immediately followed by its own depth-3 value objects.
    - `.planning/phases/64-meter-event-summary-reads/64-PATTERNS.md` — all Pattern Assignments for this module, plus the four Hint Corrections.
  </read_first>
  <behavior>
    Write these tests FIRST in `test/lattice_stripe/billing/meter_event_summary_test.exs`; they must fail
    before the module exists, then pass.

    - Given a Mox transport returning a one-item list body, `list(client, "mtr_123", %{"customer" => "cus_1", "start_time" => 1_753_620_000, "end_time" => 1_753_706_400})` requests path `/v1/billing/meters/mtr_123/event_summaries`.
    - The same call places `customer`, `start_time` and `end_time` on the wire as query params, and `value_grouping_window` too when supplied.
    - The returned value is `{:ok, %LatticeStripe.Response{}}` whose `data` is a `%LatticeStripe.List{}` whose `data` is a list of `%MeterEventSummary{}`.
    - `from_map/1` on the fixture map yields `id == "mtrusg_123"`, `object == "billing.meter_event_summary"`, `meter == "mtr_123"`, `livemode == false`, and `aggregated_value` that satisfies `is_float/1`.
    - `from_map(nil)` returns `nil`. `from_map(%MeterEventSummary{})` returns that exact struct (D-07).
    - An unrecognised wire key lands in `:extra` and is not dropped.
    - MTR-01/empty edge: a list body whose `"data"` is `[]` decodes to a `%LatticeStripe.List{}` with `data: []` — an empty list, never `nil`.
    - MTR-01/ordering edge: a three-item body decodes to three structs whose `id`s appear in the same order the wire supplied them.
    - `refute Map.has_key?(%MeterEventSummary{}, :customer)` — F-02 encoded as design, not accident.
    - `list(client, nil, params)` and `list(client, "", params)` each raise `ArgumentError` with message `LatticeStripe.Billing.MeterEventSummary.list/4 requires a non-empty meter id` (D-09).
    - With a valid meter id but `params` missing `customer`, raises `ArgumentError` `LatticeStripe.Billing.MeterEventSummary.list/4 requires a customer param`; missing `start_time` raises `... requires a start_time param`; missing `end_time` raises `... requires an end_time param` — note the article `an` on the third (D-08).
    - First-failure ordering: params missing all three raise the `customer` message; a nil meter id with params missing all three raises the meter-id message.
    - All five raises happen with no transport expectation set, proving they fire pre-network.
  </behavior>
  <action>
    Create `LatticeStripe.Billing.MeterEventSummary` at `lib/lattice_stripe/billing/meter_event_summary.ex`,
    flat at depth 2, per the D-01 decision confirmed in Task 1.

    Alias exactly `LatticeStripe.{Client, Request, Resource}` — the narrow `active_entitlement.ex:70-71`
    form. This module must not reference `ObjectTypes`.

    Declare `@known_fields` with the seven wire field names `id object aggregated_value start_time end_time
    meter livemode`, using the square-bracket word-sigil form shown at `transfer_reversal.ex:63-66`. D-20
    records that the parenthesised form makes `drift.ex:210` parse the attribute as empty and emit a
    spurious full-module drift entry; 64-02 widens that regex, but this file uses brackets regardless.

    Declare the struct with `:id`, `:aggregated_value`, `:start_time`, `:end_time`, `:meter`, `:livemode`,
    then `object: "billing.meter_event_summary"` and `extra: %{}`. Add a source comment stating there is
    deliberately no `:customer` field because Stripe does not return one (F-02), so the omission is not
    "fixed" by a later contributor. Add a `@type t` typespec — documentation only, no Dialyzer in this project.

    Add a private `path/1` returning `"/v1/billing/meters/#{meter_id}/event_summaries"`. This is the
    interpolated analogue of the `@list_path` single-source idiom at `active_entitlement.ex:73-75`; carry a
    comment saying `list/4` and the streaming variant both read it so they physically cannot diverge.

    Implement `def list(client, meter_id, params \\ %{}, opts \\ [])` as a defaults header plus one
    implementation clause. Keep `params \\ %{}` — D-06 explicitly scopes Phase 63's D-14 no-default rule to
    `create/3` only, and the just-shipped `ActiveEntitlement.list/3` keeps the default despite a required
    `customer`. The body's statements, in this exact order and all before `%Request{}` is constructed:
    `validate_id!(meter_id, "meter id")`; then three `Resource.require_param!/3` calls for `"customer"`,
    `"start_time"`, `"end_time"` with the D-08 messages verbatim —
    `LatticeStripe.Billing.MeterEventSummary.list/4 requires a customer param`,
    `... requires a start_time param`, `... requires an end_time param`. The third takes the article `an`,
    mirroring `Meter.create/3`'s existing `requires an event_name param`. Then build
    `%Request{method: :get, path: path(meter_id), params: params, opts: opts}`, pipe through
    `Client.request(client, &1)` and `Resource.unwrap_list(&from_map/1)`.

    GUARD-04, the window-alignment guard, is deliberately NOT wired here — plan 64-05 adds
    `Billing.Guards.check_summary_window!/2` and inserts the call between the `require_param!` block and
    `%Request{}`, which is exactly where `meter.ex:110` places its own guard call.

    Add the private `validate_id!/2` in a `# Private helpers` section, copying the SHAPE at
    `external_account.ex:271-278`: an `is_binary(value) and value != ""` clause returning `:ok`, and a
    catch-all clause that raises. Do NOT copy its message — that one is generic and carries no arity. Use
    `LatticeStripe.Billing.MeterEventSummary.list/4 requires a non-empty meter id` so all four ArgumentErrors
    from `list/4` share one grammar (D-09). stripe-go ships this exact bug on this exact endpoint: a nil id
    becomes `""`, producing a double-slash path and a hintless 404.

    Implement `from_map/1` with three clauses in this order: `from_map(nil), do: nil`; then
    `from_map(%__MODULE__{} = summary), do: summary`; then `from_map(map) when is_map(map)`. Carry the
    load-bearing comment from `active_entitlement.ex:213` stating the struct clause MUST precede the
    `is_map/1` clause because a struct is a map. In the map clause use `Map.split(map, @known_fields)` and
    assign every field from `known`, defaulting `object` to `"billing.meter_event_summary"`, and put the
    remainder in `extra`. Do not coerce `aggregated_value` — it arrives as a JSON number and stays a float
    (F-05); note in a comment the asymmetry that reads return a float while writes take a decimal string.

    Write a first-pass `@moduledoc` covering the wire object, the `mtrusg_` id prefix and the endpoint. The
    full moduledoc treatment (the F-02/F-08/F-09 warning admonition, the truncation section, the
    deliberate-absence section) lands in 64-03 — do not attempt it here.

    In `test/support/fixtures/metering.ex`, which ALREADY EXISTS with four nested fixture modules, add a
    nested `defmodule MeterEventSummary` following the existing in-file convention (`@moduledoc false`, a
    `@doc`, `def basic(overrides \\ %{})` returning a string-keyed wire map piped into `Map.merge(overrides)`).
    Seed it with `"id" => "mtrusg_123"`, `"object" => "billing.meter_event_summary"`,
    `"aggregated_value" => 42.5` (a float, per F-05), `"start_time" => 1_753_620_000`,
    `"end_time" => 1_753_706_400`, `"meter" => "mtr_123"`, `"livemode" => false`. Add a
    `def list_response(items \\ [basic()])` mirroring the existing `Meter.list_response/1`. Also add the
    6-line `PROMOTION TARGET (Phase 65 / OBJ-02)` header comment above `defmodule
    LatticeStripe.Test.Fixtures.Metering`, cloned verbatim from `test/support/fixtures/entitlements.ex`
    lines 1-6 with the file and module names swapped — that file has the header and this one does not.

    In `mix.exs`, append `LatticeStripe.Billing.MeterEventSummary` to the `"Billing Metering"`
    `groups_for_modules` list, after `LatticeStripe.Billing.MeterEventStream.Session`. The family's order is
    its lifecycle — define, write, read, diagnose — so the read module follows all the write modules. Add
    only this one module; 64-04 appends the four `MeterErrorReport` modules after it. Do not create a new
    group: fifteen entries is comfortably under the 22 that made a separate group necessary elsewhere (D-04).
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/billing/meter_event_summary_test.exs && mix format --check-formatted && mix compile --warnings-as-errors && mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/billing/meter_event_summary_test.exs` exits 0.
    - `mix test` exits 0 with a reported test count of at least 2188 (the verified clean-HEAD baseline).
    - `mix format --check-formatted` exits 0 and `mix compile --warnings-as-errors` exits 0.
    - `mix credo --strict` exits 0.
    - `lib/lattice_stripe/billing/meter_event_summary.ex` contains `def list(client, meter_id, params \\ %{}, opts \\ [])`.
    - `lib/lattice_stripe/billing/meter_event_summary.ex` contains the literal string `/v1/billing/meters/#{meter_id}/event_summaries`.
    - `grep -v '^\s*#' lib/lattice_stripe/billing/meter_event_summary.ex | grep -c 'defstruct'` returns 1.
    - `grep -c 'LatticeStripe.Billing.MeterEventSummary' mix.exs` returns at least 1.
    - In IEx or a test, `LatticeStripe.Billing.MeterEventSummary.__struct__() |> Map.has_key?(:customer)` is `false`.
    - `LatticeStripe.Billing.MeterEventSummary.from_map(nil)` returns `nil`.
    - `function_exported?(LatticeStripe.Billing.MeterEventSummary, :list, 4)` is `true` after `Code.ensure_loaded/1`.
    - `git diff --stat mix.lock` reports no change.
    - `git diff --stat lib/lattice_stripe/object_types.ex` reports no change.
  </acceptance_criteria>
  <done>
    A caller with a valid client can read one page of meter event summaries end to end and receive typed
    structs; all five pre-network guards raise before any transport call; the module is published in the
    HexDocs "Billing Metering" group; and the slice is committed.
  </done>
  <reversibility rating="one-way">
    The module name `LatticeStripe.Billing.MeterEventSummary` becomes a published semver contract at the
    v1.10 Hex tag. Gated by the Task 1 decision checkpoint. Every other choice in this task (guard messages,
    struct field set, fixture shape) is reversible before release.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `MeterEventSummary.list/4` | Adopter-supplied `meter_id` is interpolated directly into the request path |
| `Client` → Stripe API | Authenticated outbound HTTPS; `stripe-account` header selects the tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | `path/1` interpolation of `meter_id` | medium | mitigate | Private `validate_id!/2` rejects `nil`, `""` and non-binaries before `%Request{}` is built (D-09). Observed real-world form: stripe-go's `FormatURLPath` turns a nil id into `""`, producing a double-slash path and a hintless 404. |
| T-64-07 | Denial of Service | unbounded result set from a wide window | medium | mitigate | `list/4` returns one page bounded by Stripe's `limit` (1-100, default 10). The streaming variant and its memory guidance land in 64-03. |
| T-64-SC | Tampering | npm/pip/cargo/hex installs | high | accept | This phase installs zero packages; `mix.lock` is asserted byte-identical in the acceptance criteria. RESEARCH.md's Package Legitimacy Audit table is empty by construction, so no `[ASSUMED]`/`[SUS]` verification checkpoint is required. |

ASVS level 1, blocking on `high`. No `high` threat in this plan is left un-mitigated: T-64-SC is accepted
because there is no candidate package set.
</threat_model>

<verification>
1. `mix test test/lattice_stripe/billing/meter_event_summary_test.exs` — green.
2. `mix test` — green, count >= 2188.
3. `mix format --check-formatted && mix compile --warnings-as-errors && mix credo --strict` — all green.
4. `git diff --stat mix.lock` — empty.
</verification>

<success_criteria>
- `MeterEventSummary.list/4` performs a real, typed, single-page read against a mocked transport.
- All four `ArgumentError` messages match D-08/D-09 verbatim, including the article `an` on `end_time`.
- The struct carries exactly seven wire fields plus `extra`, and no `:customer`.
- The module appears in `mix.exs` `groups_for_modules` under `"Billing Metering"`.
- `mix.lock` unchanged; `object_types.ex` unchanged.
</success_criteria>

<output>
Create `.planning/phases/64-meter-event-summary-reads/64-01-SUMMARY.md` when done.
</output>
