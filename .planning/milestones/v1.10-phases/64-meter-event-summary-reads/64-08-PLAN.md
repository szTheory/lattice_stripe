---
phase: 64-meter-event-summary-reads
plan: 08
type: execute
wave: 5
depends_on: ["64-04", "64-07"]
files_modified:
  - guides/metering-runtime-and-reconciliation.md
  - guides/scope.md
  - lib/lattice_stripe/billing/meter_event.ex
autonomous: true
requirements: [MTR-03, MTR-04]

must_haves:
  truths:
    - "MTR-03: guides/metering-runtime-and-reconciliation.md's error-report handler is rewritten to the working shape — an event notification, a fetch call, MeterErrorReport.from_event/1 — replacing a snippet that is wrong three independent ways (F-12, N-03 row 7)"
    - "That guide no longer reads an id off the error-report payload, because the payload has none (D-17)"
    - "MTR-04: guides/scope.md gains a Deferred by design bullet stating that on the generally available API you cannot read usage back grouped by a custom dimension, with the two workarounds named (D-27)"
    - "MTR-04: the payload bullet in Billing.MeterEvent.create/3's @doc is amended from closed-set language to state that arbitrary custom dimension keys are accepted alongside the customer-mapping key and the value (D-24.3)"
    - "That @doc amendment is one corrected sentence plus a pointer to the guide, not a duplicate of the guide's full treatment (D-25)"
    - "The scope.md bullet describes only what the API cannot do; it does not publish this milestone's internal build constraints (D-27)"
    - "guides/scope.md's adopter-docs link to the top-level readme resolves, so this plan's own docs gate — zero mix docs warnings naming either guide it writes — is reachable. Fixed at its cause per D-29's fix-at-cause doctrine (STATE [63-07]) rather than by widening the gate"
  artifacts:
    - "guides/metering-runtime-and-reconciliation.md — rewritten error-report handler"
    - "guides/scope.md — one Deferred by design bullet"
    - "lib/lattice_stripe/billing/meter_event.ex — amended payload bullet in the create/3 @doc"
  key_links:
    - "the rewritten runtime-guide handler -> Webhook.fetch_event/3 -> MeterErrorReport.from_event/1"
    - "MeterEvent.create/3 @doc payload bullet -> guides/metering.md payload contract section — the @doc is the miscommunicating artifact, so fixing only the guide would leave the cause in place"
  prohibitions:
    - "Do not publish the internal do-not-add-metering-writes build constraint in scope.md — all four write surfaces already ship and nothing is deferred, so publishing it would misrepresent a completed surface as a limitation (D-27)"
    - "Do not reproduce the guide's full payload treatment in the @doc — the placement is deliberately asymmetric: full treatment in the guide, one corrected sentence plus a pointer in the @doc (D-25)"
    - "Do not add a new docs-truth prose grep — the budget this phase is zero (D-26)"
    - "Do not claim whether Stripe's parser accepts exponent notation (O-01)"
    - "No new metering write surfaces; no Billing.Meter.event_summaries/3,4"
    - "No new or bumped dependency; no edit to lib/lattice_stripe/object_types.ex"
    - "Do not regress SEED-005 §6 stability contracts"
---

<objective>
Fix the miscommunicating artifacts outside `guides/metering.md`: the second guide that teaches a webhook
handler which cannot work, the scope page that omits the phase's most valuable limitation, and the
function documentation whose closed-set phrasing is the original cause of the downstream adopter dropping
its payload.

Purpose: the placement here is deliberately asymmetric. The full treatment of the payload contract lives
in the guide, but the `@doc` on the write function is itself one of the artifacts that taught the wrong
thing — so fixing only the guide would leave the cause in place. This is targeted repair of specific
misleading text, not blanket duplication.

Output: three small, surgical edits across three files.

Ordering: this plan runs strictly after 64-07. Both of its tasks cross-reference sections of
`guides/metering.md` that 64-07 creates, and naming a heading that does not yet exist would produce exactly
the ExDoc warning class this plan's own gate fails on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/64-meter-event-summary-reads/64-CONTEXT.md
@.planning/phases/64-meter-event-summary-reads/64-RESEARCH.md
@.planning/phases/64-meter-event-summary-reads/64-04-SUMMARY.md
@.planning/phases/64-meter-event-summary-reads/64-07-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:

**Documentation changes:** a rewritten handler example in
`guides/metering-runtime-and-reconciliation.md`; one new bullet in `guides/scope.md` under
`## Deferred by design`; an amended `payload` bullet in the `@doc` of
`LatticeStripe.Billing.MeterEvent.create/3`.

**New file paths:** none. **New modules, functions or struct fields:** none.

For the phase-wide artifact inventory see `64-09-PLAN.md`.
</artifacts_this_phase_produces>

<tasks>

<!-- The negative greps below scope the `mix docs` WARNING STREAM, not any file this plan writes.
     Each literal is the grep's own regex, so its appearance in action prose cannot self-invalidate
     the gate — the escape hatch's stated case. -->
<!-- planner-discipline-allow: meter_event.ex -->
<!-- planner-discipline-allow: scope.md -->
<!-- planner-discipline-allow: metering-runtime -->

<task type="auto">
  <name>Task 1: Rewrite the second unworkable handler and add the dimension-read scope limit</name>
  <files>guides/metering-runtime-and-reconciliation.md, guides/scope.md</files>
  <read_first>
    - `guides/metering-runtime-and-reconciliation.md` lines 100-140 — the handler example and the paragraph beneath it about correlation metadata, which stays and becomes more accurate once the snippet is fixed.
    - `guides/scope.md` lines 27-45 — the `## Deferred by design` section, its bullet register (bold lead-in, then the reason), and the fact that this file is already covered by a docs-truth assertion, so its structure must not change.
    - `guides/scope.md` lines 68-71 — the `## Public documentation` section. Its adopter-docs sentence links the first of the two named surfaces with a relative parent-directory path, and that one link is the sole cause of the two baseline ExDoc warnings this task's gate counts. Confirm the shape before editing.
    - `mix.exs` lines 23-59 — the ExDoc `extras` list, to see *why* that link warns: the changelog is listed there and the top-level readme is not, and ExDoc resolves a relative documentation link from one extra only against that list. Read only — this task must not edit `mix.exs`.
    - `lib/lattice_stripe/billing/meter_error_report.ex` and its three sub-modules — as 64-04 left them. Constructor and field names in the snippet must match shipped source exactly.
    - `lib/lattice_stripe/webhook.ex` lines 320-360 — the fetch function's real arities and return shape.
    - `.planning/phases/64-meter-event-summary-reads/64-CONTEXT.md` D-27 — the four independent verifications of the dimension-read limit, and the explicit note on what must NOT go in scope.md.
    - `guides/metering.md` — as 64-07 left it, so the cross-references you add point at sections that exist under the names they now have.
  </read_first>
  <action>
    In `guides/metering-runtime-and-reconciliation.md`, replace the error-report handler example. The
    current one is wrong three ways at once: it pattern-matches the v1 event struct for what is a v2 thin
    event, it reaches for an object key inside the event data that a thin-event payload does not have, and it
    then reads an identifier off the resulting value even though this payload carries no id field at all.

    Write the replacement in the working shape: match an event notification for the error-report type, call
    the webhook fetch function to obtain the versioned event — because the payload is a fetched attribute and
    the delivered webhook body does not carry it — then call the error report's event constructor and enqueue
    from typed fields. Since this guide's framing is operator and repair workflows rather than the full
    reconciliation walkthrough, keep the snippet short: fetch, decode, enqueue on the meter id and the
    validation window. Cross-reference `guides/metering.md`'s reconciliation section for the full treatment
    rather than duplicating it.

    Carry a comment on the fetch line stating the step is mandatory. Both shipped guides had this same
    defect; a reader who has seen it once in the other guide must not be able to conclude from this one that
    it was optional there.

    The paragraph beneath the snippet, about correlation metadata paying for itself, stays — but it now has
    something real to point at. Extend it by one sentence naming the request identifier on each sample error
    as that correlation key, and noting that it is the HTTP idempotency key of the failing write, so a caller
    who lets the library generate one has nothing to correlate against.

    In `guides/scope.md`, add one bullet under `## Deferred by design`, matching the existing bullets' shape:
    a bold lead-in naming the thing, then the reason. The content: on the generally available API you cannot
    read usage back grouped by a custom dimension. Dimensions are write-only today — Stripe stores them and
    offers no way to group by them. Verified four ways: the summary object has no dimensions field, a
    group-by parameter is rejected, declaring dimension keys at meter creation is rejected, and the canonical
    meter-configuration documentation does not mention dimensions at all. Dimension grouping exists only in
    preview surfaces. Name the two workarounds: one meter per dimension value, or your own store. Point the
    reader at the payload-contract section of the metering guide.

    Do NOT add this milestone's internal build fence — the one forbidding additional metering ingestion
    endpoints. That is an internal planning constraint, not a scope limitation: all four ingestion endpoints
    already ship, nothing is deferred, and publishing it would misrepresent a completed capability as a gap.
    This distinction is explicit in D-27 and getting it wrong would make the scope page actively misleading.

    Still in `guides/scope.md`, repair the broken adopter-docs link under `## Public documentation`. The
    sentence naming the two adopter-facing documentation surfaces links the first of them with a relative
    parent-directory path, and ExDoc cannot resolve it: a relative documentation link from one extra is
    resolved only against the `extras` list, and the top-level readme is not in that list (the changelog is,
    which is why changelog-style links elsewhere in the guides do not warn). Replace that one link target with
    the absolute canonical URL `https://github.com/szTheory/lattice_stripe#readme`, leaving the link text and
    the rest of the sentence alone. The proof that this is the correct shape sits on the same line: the second
    link in that sentence is already an absolute URL and raises no warning.

    This repair belongs to this task because it is the cause of the only warnings this task's own gate counts,
    and D-29's doctrine (STATE `[63-07]`) is to answer a warning by fixing its cause rather than by widening
    the tolerance. Without it the gate below is unreachable no matter how well the rest of the task is done.
    Keep the repair to that one link:

    - Do NOT make the same repair in `guides/getting-started.md`, which carries an identical broken link. That
      file is not in this plan's `files_modified`, no gate in this phase counts it, and editing it would break
      the file-ownership rule the wave schedule depends on. Leave it as pre-existing debt for a follow-up.
    - Do NOT clear the warning by adding the readme to the ExDoc `extras` list. That would also work, but it
      edits `mix.exs` — a file two earlier plans own — and it publishes a new HexDocs page needing a group
      assignment. That is a structural documentation change, not a link repair.

    Check whether `guides/scope.md` is covered by an existing docs-truth assertion before editing, and keep
    the assertions that file already has green — a structural change to the section could break one. The
    assertions on this file check prose content and its presence in the extras list, not link targets.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs && DOCS=$(mix docs 2>&1) && test "$(printf '%s\n' "$DOCS" | grep -cE 'scope\.md|metering-runtime')" -eq 0</automated>
  </verify>
  <precondition>
    `guides/metering.md` must already carry the sections 64-07 creates — the reading-usage-back section, the
    payload-contract section and the corrected reconciliation section — because this task's cross-references
    point at them by their shipped heading names. Assert before starting:
    `grep -q '## Reading usage back' guides/metering.md`. If it is absent, 64-07 has not landed; halt rather
    than inventing heading names, because a cross-reference to a heading that does not exist raises an ExDoc
    warning this task's own gate will then fail on.
  </precondition>
  <acceptance_criteria>
    - `mix test test/lattice_stripe/docs_truth_test.exs` exits 0.
    - `mix docs` exits 0; `DOCS=$(mix docs 2>&1) && test "$(printf '%s\n' "$DOCS" | grep -cE 'scope\.md|metering-runtime')" -eq 0` exits 0. Note the shape: `grep -c` exits 1 when it counts zero, so a trailing `grep -c` would both invert this check and mask a failing test run ahead of it. This is the only docs threshold in this task, and it is scoped to the two files the task writes — mirroring VALIDATION step 5, whose scoped substring check is absolute. The phase-wide differential count against the recorded baseline is VALIDATION step 4, owned by `64-10`; do not restate it here as a planning-time constant, which by wave 5 is already stale (64-09 lands its minus-two clearing in wave 4).
    - `grep -cF 'https://github.com/szTheory/lattice_stripe#readme' guides/scope.md` returns 1 — the broken relative link was repaired at its cause, which is what makes the zero-match gate above reachable at all.
    - `grep -c 'fetch_event' guides/metering-runtime-and-reconciliation.md` returns at least 1.
    - `grep -c 'from_event' guides/metering-runtime-and-reconciliation.md` returns at least 1.
    - `grep -c 'MeterErrorReport' guides/metering-runtime-and-reconciliation.md` returns at least 1.
    - `grep -cF 'error_report["id"]' guides/metering-runtime-and-reconciliation.md` returns 0.
    - `grep -cF 'data["object"]' guides/metering-runtime-and-reconciliation.md` returns 0.
    - `grep -ci 'dimension' guides/scope.md` returns at least 1.
    - `grep -ci 'write surface' guides/scope.md` returns 0 — the internal build fence was not published.
    - `mix test` exits 0 with a reported count of at least 2188.
  </acceptance_criteria>
  <done>
    Neither shipped guide teaches a webhook handler that cannot work, and the scope page states the one
    limitation that saves a Phase 64 adopter a wasted week — a limitation no other Stripe SDK's docs state.
  </done>
  <reversibility rating="reversible">
    Documentation.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Amend the payload bullet in MeterEvent.create/3's @doc</name>
  <files>lib/lattice_stripe/billing/meter_event.ex</files>
  <read_first>
    - `lib/lattice_stripe/billing/meter_event.ex` lines 1-90 — READ IN FULL through `create/3`. The `payload` bullet in the `@doc` around lines 26-30 uses closed-set phrasing describing the payload as the customer-mapping key plus the numeric value, which reads as an exhaustive list.
    - `guides/metering.md` — as 64-07 left it, so the pointer you add names the payload-contract section by its actual heading.
    - `test/lattice_stripe/form_encoder_test.exs` — as 64-02 left it. The amended sentence must match what those assertions prove and claim nothing more.
    - `.planning/phases/64-meter-event-summary-reads/64-CONTEXT.md` D-24.3 and D-25 — the amendment target and the asymmetric-placement rule.
    - `.planning/STATE.md` note `[63-07]` — the phase-gate lesson that a docs autolink to a hidden function raises an ExDoc warning; keep any reference you add pointing at a public target.
  </read_first>
  <action>
    Amend the `payload` bullet in the `@doc` for `create/3` in `lib/lattice_stripe/billing/meter_event.ex`.

    The current phrasing describes the payload as the customer-mapping key plus the numeric value. That reads
    as an exhaustive list, and a reader building a metering integration reasonably concludes those are the
    only two keys allowed. It is the artifact that most directly caused the downstream adopter to drop its
    own dimension keys — its gap brief says it did so partly because the contract was unclear.

    Rewrite the bullet to state that the payload carries the customer-mapping key and the value key that the
    meter's configuration names, plus any number of additional custom dimension keys of your choosing, which
    Stripe stores and this library passes through without filtering. Add one sentence on the decimal rule:
    pass decimal values as strings, because a float goes through the default stringifier and can reach the
    wire in scientific notation. Then point at the payload-contract section of the metering guide for the
    full treatment — the coercion table, the flat-only rule, cardinality, and the fact that dimensions cannot
    be read back grouped.

    Keep it to a corrected sentence or two plus the pointer. The placement is deliberately asymmetric (D-25):
    the guide holds the full treatment and the `@doc` holds the correction. Do not duplicate the table or the
    four rules here. The reason this earns a second location at all is not redundancy for its own sake — it
    is that the `@doc` is itself one of the artifacts that taught the wrong thing, so repairing only the
    guide would leave the cause in place.

    Do not claim anything about whether Stripe's parser accepts exponent notation; that is unresolved.

    Make sure any reference you add resolves to a public target. Phase 63's gate had to unwind several
    autolinks that named hidden helpers, and each one cost an ExDoc warning the differential gate would
    catch.
  </action>
  <verify>
    <automated>mix test test/lattice_stripe/billing/meter_event_test.exs && DOCS=$(mix docs 2>&1) && test "$(printf '%s\n' "$DOCS" | grep -c 'meter_event.ex')" -eq 0</automated>
  </verify>
  <precondition>
    `guides/metering.md` must already carry 64-07's payload-contract section, because the pointer this task
    adds to the `@doc` names that section by its shipped heading. Assert before starting with a grep for that
    heading; if absent, 64-07 has not landed — halt rather than guessing the name, since an autolink to a
    non-existent target is exactly the ExDoc warning class Phase 63 had to unwind at gate time.
  </precondition>
  <acceptance_criteria>
    - `mix docs` exits 0; `DOCS=$(mix docs 2>&1) && test "$(printf '%s\n' "$DOCS" | grep -c 'meter_event.ex')" -eq 0` exits 0. `grep -c` exits 1 on a zero count and must never terminate a verification pipeline.
    - `mix docs 2>&1 | grep -ci 'warning'` returns a count no greater than 42.
    - The `create/3` doc string retrieved via `Code.fetch_docs(LatticeStripe.Billing.MeterEvent)` contains the substring `dimension`.
    - The same doc string contains `string` in the context of decimal values.
    - `mix test test/lattice_stripe/billing/meter_event_test.exs` exits 0.
    - `mix test` exits 0 with a reported count of at least 2188.
    - `mix format --check-formatted`, `mix compile --warnings-as-errors` and `mix credo --strict` all exit 0.
    - `git diff --numstat lib/lattice_stripe/billing/meter_event.ex` shows changes confined to documentation lines — no function body was modified.
  </acceptance_criteria>
  <done>
    A developer reading the write function's documentation learns that custom dimension keys are welcome and
    that decimals go as strings, without having to find the guide first — and is pointed at the guide for
    everything else.
  </done>
  <reversibility rating="reversible">
    Documentation inside a source file; no behavior change.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe webhook → the handler this guide teaches | The second shipped guide's snippet currently trusts payload data delivered in the webhook body |
| documentation → adopter implementation | Closed-set phrasing in a `@doc` becomes a dropped feature in every integration built from it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-08 | Spoofing / Tampering | the runtime guide's handler reading `data` from the webhook body | high | mitigate | Task 1 rewrites it to fetch the versioned event over an authenticated channel first. Both shipped guides carried this defect; fixing only one would let a reader conclude the fetch is optional. |
| T-64-17 | Repudiation | scope documentation omitting a hard API limitation | medium | mitigate | Task 1 adds the dimension-read limit to the canonical deferred-scope page, verified four independent ways. |
| T-64-15 | Tampering | closed-set `@doc` phrasing causing dropped dimensions and float-mangled values | high | mitigate | Task 2 replaces the phrasing and adds the decimal rule; both claims are backed by assertions added in 64-02. |
| T-64-SC | Tampering | package-manager installs | high | accept | Zero packages installed. |

ASVS level 1, blocking on `high`. T-64-08 and T-64-15 are mitigated in this plan; T-64-SC is accepted with
rationale.
</threat_model>

<verification>
1. `mix test test/lattice_stripe/docs_truth_test.exs` — green.
2. `mix docs` — exits 0, warning count <= 42, zero warnings naming `scope.md`, `metering-runtime-and-reconciliation.md` or `meter_event.ex`.
3. `mix test` — green, count >= 2188.
4. `mix format --check-formatted && mix compile --warnings-as-errors && mix credo --strict` — green.
5. Manual: the rewritten handler snippet compiles against the shipped modules — copy it into a scratch script and confirm.
</verification>

<success_criteria>
- Neither shipped guide teaches a handler that reads event data from the webhook body.
- `scope.md` states the dimension-read limit and does not publish the internal write-surface fence.
- `MeterEvent.create/3`'s payload bullet names custom dimensions and the decimal-string rule, and points at the guide.
- No function body in `meter_event.ex` was modified.
</success_criteria>

<output>
Create `.planning/phases/64-meter-event-summary-reads/64-08-SUMMARY.md` when done.
</output>
