---
phase: 66-product-feature-attachment
plan: 04
type: execute
wave: 3
depends_on: ["66-02", "66-03"]
files_modified:
  - lib/lattice_stripe/product/feature.ex
  - lib/lattice_stripe/product.ex
  - mix.exs
  - priv/api/current.txt
autonomous: true
requirements: [PROD-01, PROD-02]

must_haves:
  truths:
    - "D-01 / D-02 / D-07: `api surface matches the committed lock` proves the exact public Product.Feature module, struct/type, verbs, and defaulted arities are the only semver additions."
    - "D-03 / D-08 / D-15 / D-20: `mix docs` proves warning-free cross-linked Product.Feature, Entitlements.Feature, and Product moduledocs that distinguish feat_ definitions, prodft_ attachments, and marketing display copy."
    - "D-20: the existing docs totality test plus the Plan-05 named grouping assertion proves Product.Feature appears in Entitlements before Billing's broader Product regex can capture it."
    - "D-24: the API lock and focused Product.Feature tests jointly prove the published callable surface and behavior rather than relying on human inspection."
  artifacts:
    - path: lib/lattice_stripe/product/feature.ex
      provides: "Complete public moduledoc/@doc/@spec contract"
    - path: lib/lattice_stripe/product.ex
      provides: "Marketing-vs-attachment moduledoc cross-link only"
    - path: mix.exs
      provides: "Exact Product.Feature placement in Entitlements ExDoc group"
    - path: priv/api/current.txt
      provides: "Semver surface snapshot"
  key_links:
    - "mix.exs first-match ExDoc group -> exact Product.Feature matcher before Billing regex"
    - "public compiled BEAM surface -> mix lattice_stripe.api_surface -> priv/api/current.txt -> api_surface_lock_test"
  prohibitions:
    - statement: "No public aliases, Product.MarketingFeature, custom Inspect, or Product runtime-shape change may enter the API snapshot (D-03, D-08, D-09)."
      status: active
      verification: "test/lattice_stripe/api_surface_lock_test.exs plus Product.Feature structural surface and Product raw-map tests"
---

<objective>
Publish the accepted attachment API as a discoverable, semver-locked HexDocs surface without changing Product runtime decoding.

Purpose: The two Feature nouns must be immediately distinguishable in search/sidebar/docs, and the accepted public contract must be mechanically reviewable.
Output: cross-linked moduledocs, Entitlements ExDoc placement, and updated API surface lock.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/66-product-feature-attachment/66-CONTEXT.md
@.planning/phases/66-product-feature-attachment/66-RESEARCH.md
@.planning/phases/66-product-feature-attachment/66-PATTERNS.md
@.planning/phases/66-product-feature-attachment/66-02-SUMMARY.md
@.planning/phases/66-product-feature-attachment/66-03-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide inventory is enumerated in `66-01-PLAN.md`. This plan creates no additional runtime symbols: it publishes documentation/spec metadata on the existing `LatticeStripe.Product.Feature` surface, adds an exact ExDoc grouping entry in `mix.exs`, adds a Product moduledoc cross-link, and records all public additions in `priv/api/current.txt`.

<tasks>

<task type="auto">
  <name>Task 1: Cross-link the two Feature nouns and place the attachment in Entitlements</name>
  <files>lib/lattice_stripe/product/feature.ex, lib/lattice_stripe/product.ex, mix.exs</files>
  <read_first>
    - `lib/lattice_stripe/product/feature.ex` — final public surface from Plans 01-02; every public function needs @doc and @spec.
    - `lib/lattice_stripe/entitlements/feature.ex` — existing definition-vs-attachment relationship section to cross-link symmetrically.
    - `lib/lattice_stripe/product.ex` — current moduledoc and raw marketing fields; edit documentation only.
    - `mix.exs` docs/0 — first-match-wins groups_for_modules ordering and current Entitlements/Billing regexes.
    - `test/lattice_stripe/docs_truth_test.exs` — totality guard that will detect an ungrouped public module.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-01, D-03, D-08, D-15, and D-20.
  </read_first>
  <action>Finish concise public docs/specs for every locked function, teaching canonical code verbs but attach/remove domain prose per D-03. Cross-link Product.Feature and Entitlements.Feature, showing prod_, feat_, and prodft_ identity and direct nested decoding per D-08/D-15. Update Product's moduledoc only to distinguish raw pricing-table features/marketing_features from access-bearing attachments and direct catalog readers to Product.Feature.list/4 or stream!/4. In mix.exs, place an exact Product.Feature matcher in Entitlements before Billing's broad Product regex so only this nested module moves groups. Do not change Product fields/from_map, create a custom Inspect, or broaden the Entitlements regex to all Product modules.</action>
  <verify>
    <automated>MIX_ENV=prod mix compile &amp;&amp; mix docs</automated>
  </verify>
  <acceptance_criteria>
    - Production compile and docs exit 0.
    - Every public Product.Feature function has @doc/@spec.
    - Product.Feature is matched by Entitlements before Billing; Product remains in Billing.
    - Product runtime tests remain green and `git diff lib/lattice_stripe/product.ex` contains documentation-only changes.
  </acceptance_criteria>
  <done>HexDocs resolves the two Feature nouns and marketing distinction without changing runtime behavior (D-01, D-03, D-08, D-15, D-20).</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and review the public API surface lock</name>
  <reversibility rating="one-way">The D-01/D-02/D-07 public module, functions, arities, type, and struct shape become breaking to remove after the Hex tag; these exact choices were accepted in 66-CONTEXT.md.</reversibility>
  <files>priv/api/current.txt</files>
  <read_first>
    - `test/lattice_stripe/api_surface_lock_test.exs` — lock semantics and diff diagnostics.
    - `lib/mix/tasks/lattice_stripe.api_surface.ex` — supported snapshot command and check mode.
    - `priv/api/current.txt` — current Product and Entitlements entries and ordering.
    - `lib/lattice_stripe/product/feature.ex` — final public module to compare against D-02/D-07.
  </read_first>
  <action>Run the project API surface generator and review the diff against D-01/D-02/D-07. Accept only Product.Feature's module, t/0/struct fields, canonical verbs, defaulted arities, and from_map/1. If any alias, non-bang stream, extra field, callback/protocol, or Product-field type/runtime change appears, fix the source rather than blessing it. Then run both generator check mode and the standing API lock test.</action>
  <verify>
    <automated>mix lattice_stripe.api_surface &amp;&amp; mix lattice_stripe.api_surface --check &amp;&amp; mix test test/lattice_stripe/api_surface_lock_test.exs test/lattice_stripe/product/feature_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - API surface check and lock test pass.
    - Snapshot diff contains only the accepted Product.Feature additions.
    - Structural absence tests remain green.
  </acceptance_criteria>
  <done>The exact accepted Product.Feature surface is mechanically locked for the release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public source → HexDocs/API snapshot → downstream adopters | Documentation, sidebar placement, and exported symbols become the durable integration contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-02 | Elevation of Privilege | misleading Product marketing/attachment docs | high | mitigate | Cross-linked moduledocs explicitly direct access catalog reads to Product.Feature list/stream and preserve local authorization boundary for Plan 05. |
| T-66-03 | Tampering | feat_ vs prodft_ deletion identity in published docs | high | mitigate | Both module docs show explicit prod_ parent plus prodft_ attachment for item operations; focused tests enforce request identity. |
| T-66-04 | Tampering | accidental public-surface expansion | high | mitigate | Generated API snapshot is reviewed against D-02/D-07 and checked by api_surface_lock_test. |
| T-66-SC | Tampering | package-manager installs | high | accept | No install or dependency change. |
</threat_model>

<verification>
Production compile, docs build, API generator check, API lock test, focused feature test, and Product regression test pass.
</verification>

<success_criteria>
- Product.Feature is warning-free, cross-linked, and grouped under Entitlements.
- Product remains in Billing and its runtime decoding is unchanged.
- The generated API snapshot contains exactly the accepted additions.
</success_criteria>

<output>
Create `.planning/phases/66-product-feature-attachment/66-04-SUMMARY.md` when done.
</output>
