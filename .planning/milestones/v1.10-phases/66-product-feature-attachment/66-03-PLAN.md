---
phase: 66-product-feature-attachment
plan: 03
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - lib/lattice_stripe/object_types.ex
  - test/lattice_stripe/object_types_test.exs
  - test/lattice_stripe/product_test.exs
autonomous: true
requirements: [PROD-01, PROD-02]

must_haves:
  truths:
    - "D-09 / D-10 / D-26 / PROD-02: the named test `legacy and current Product marketing fields remain raw and independent` proves Product.features and Product.marketing_features retain separate string-keyed map lists with no copying or attachment decoding."
    - "D-11 / D-27: the named tests `product_feature resolves and deserializes exactly` and `product.feature is rejected byte-for-byte` prove the one exact registry row and typo rejection without a map-size lock."
    - "D-08 / D-26 / D-27: maybe_deserialize/1 returns %Product.Feature{} whose nested entitlement_feature is %Entitlements.Feature{} and whose future top-level field remains in extra."
    - "PROD-02/unclassified edge: Product retrieve/list/stream/search behavior remains governed by the unchanged Product.from_map/1 raw assignments; focused regression inputs prove old-only, current-only, and both-present payloads independently."
  artifacts:
    - path: lib/lattice_stripe/object_types.ex
      provides: "Exact product_feature dispatch row"
    - path: test/lattice_stripe/object_types_test.exs
      provides: "Exact-key positive typing and typo rejection"
    - path: test/lattice_stripe/product_test.exs
      provides: "Semver-safe raw legacy/current marketing-field regression"
  key_links:
    - "ObjectTypes @object_map exact key -> Product.Feature.from_map/1 -> nested Entitlements.Feature.from_map/1"
    - "Product.from_map/1 features and marketing_features assignments -> unchanged raw consumer-visible runtime shape"
  prohibitions:
    - statement: "Product.features and Product.marketing_features must not be typed, copied, normalized, or conflated with entitlement attachments during 1.x (D-09, D-10)."
      status: active
      verification: "test/lattice_stripe/product_test.exs — `legacy and current Product marketing fields remain raw and independent`"
    - statement: "No Product.MarketingFeature module and no product.feature registry alias may ship (D-09, D-11)."
      status: active
      verification: "product compatibility structural assertion plus object_types exact-byte rejection test"
---

<objective>
Integrate exact wire dispatch while locking the semver-safe correction that Product marketing fields stay raw and independent.

Purpose: Typed attachments belong to the dedicated endpoint; legacy/current pricing-table copy must not silently change runtime shape in a minor release.
Output: one exact ObjectTypes row plus Product and registry regression evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-product-feature-attachment/66-CONTEXT.md
@.planning/phases/66-product-feature-attachment/66-RESEARCH.md
@.planning/phases/66-product-feature-attachment/66-PATTERNS.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-06-SUMMARY.md
@.planning/phases/66-product-feature-attachment/66-01-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide inventory is enumerated in `66-01-PLAN.md`. This plan adds only the private registry literal entry `"product_feature" => LatticeStripe.Product.Feature` and new ExUnit test cases in existing `LatticeStripe.ObjectTypesTest` and `LatticeStripe.ProductTest`; it creates no new public module, Product field, alias, or fixture.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock raw independent Product marketing compatibility</name>
  <reversibility rating="one-way">D-09 preserves the already-published 1.x runtime map shape; changing it in this minor would break Access and map-pattern consumers.</reversibility>
  <files>test/lattice_stripe/product_test.exs</files>
  <read_first>
    - `lib/lattice_stripe/product.ex` — @known_fields, t/0, defstruct, and from_map/1 assignments for features and marketing_features; read only in this plan.
    - `test/lattice_stripe/product_test.exs` — existing from_map and module-surface conventions.
    - `guides/api_stability.md` — additive-minor struct/runtime stability contract.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-09, D-10, and D-26.
  </read_first>
  <behavior>
    - An old-version payload populates only features with the exact original string-keyed maps.
    - A current payload populates only marketing_features with the exact original string-keyed maps.
    - A payload containing both keeps two different lists independently; neither contains %Product.Feature{} and neither is copied into the other.
  </behavior>
  <action>Extend Product.from_map/1 tests only; do not edit Product runtime decoding. Use distinct legacy/current maps so equality cannot pass via accidental copying. Assert raw string-key access and exact list identity for old-only, current-only, and both-present payloads, with the absent sibling remaining nil. Refute Product.Feature structs in either list and structurally refute a `LatticeStripe.Product.MarketingFeature` module. This is the executable D-09/D-10/D-26 interpretation satisfying PROD-02 without a breaking conversion.</action>
  <verify>
    <automated>mix test test/lattice_stripe/product_test.exs &amp;&amp; git diff --quiet lib/lattice_stripe/product.ex</automated>
  </verify>
  <acceptance_criteria>
    - Product compatibility tests pass for old-only, current-only, and both-present payloads.
    - `git diff --quiet lib/lattice_stripe/product.ex` exits 0 in this plan.
    - No Product.MarketingFeature symbol exists.
  </acceptance_criteria>
  <done>PROD-02 is satisfied through the D-09/D-10/D-26 semver-safe interpretation with executable raw-shape evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register and prove the exact product_feature discriminator</name>
  <files>lib/lattice_stripe/object_types.ex, test/lattice_stripe/object_types_test.exs</files>
  <read_first>
    - `lib/lattice_stripe/object_types.ex` — exact Map.fetch registry and maybe_deserialize clauses.
    - `test/lattice_stripe/object_types_test.exs` — existing direct-key family assertions, unknown-key behavior, and Phase 65 no-map-size doctrine.
    - `lib/lattice_stripe/product/feature.ex` — Plan 01's decoder fields and nested decode contract.
    - `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-06-SUMMARY.md` — inherited exact-key doctrine and 52-row informational baseline; do not convert it into a count assertion.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-08, D-11, and D-27.
  </read_first>
  <behavior>
    - fetch_module/1 resolves only the exact wire bytes to Product.Feature.
    - maybe_deserialize/1 returns typed outer and nested structs and preserves a future field.
    - The dotted typo returns :error from fetch_module/1 and remains a raw map through maybe_deserialize/1.
  </behavior>
  <action>Add exactly one D-11 key mapping the exact Stripe discriminator to Product.Feature. Extend existing test blocks with direct fetch and typed maybe_deserialize assertions, including nested Entitlements.Feature and extra preservation. Add explicit D-27 dotted-typo rejection for both fetch_module and maybe_deserialize. Do not normalize, trim, case-fold, add a second spelling, or assert map size.</action>
  <verify>
    <automated>mix test test/lattice_stripe/object_types_test.exs test/lattice_stripe/product_test.exs &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Exact-key fetch and typed maybe_deserialize tests pass.
    - Dotted typo is rejected/left raw.
    - No map-size assertion is added; all pre-existing ObjectTypes and webhook tests remain green.
  </acceptance_criteria>
  <done>The exact product_feature wire object is typed and the false dotted spelling is executable rejected (D-11, D-27).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe Product JSON → existing Product.from_map/1 | Legacy/current API versions may populate different marketing-copy fields. |
| Stripe object discriminator → ObjectTypes dispatch | Exact external bytes select a public decoder and related-object behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-02 | Elevation of Privilege | marketing copy mistaken for access catalog | high | mitigate | D-09/D-10/D-26 tests prove both Product fields remain raw/independent and separate from Product.Feature. |
| T-66-04 | Tampering | typo dispatch or dropped future fields | high | mitigate | D-11/D-27 exact-key acceptance/rejection plus Product.Feature.extra behavioral assertion. |
| T-66-SC | Tampering | package-manager installs | high | accept | No dependency changes or installs. |
</threat_model>

<verification>
Run Product and ObjectTypes focused suites, then `mix test test/lattice_stripe/webhook/` to cover the registry's second consumer.
</verification>

<success_criteria>
- Raw legacy/current Product maps remain independent and unchanged.
- Exactly one product_feature dispatch row exists and the dotted typo is rejected.
- No map-size lock, normalization layer, or Product.MarketingFeature module is introduced.
</success_criteria>

<output>
Create `.planning/phases/66-product-feature-attachment/66-03-SUMMARY.md` when done.
</output>
