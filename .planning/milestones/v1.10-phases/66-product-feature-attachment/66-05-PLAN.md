---
phase: 66-product-feature-attachment
plan: 05
type: execute
wave: 4
depends_on: ["66-04"]
files_modified:
  - guides/entitlements.md
  - guides/user-flows-and-jtbd.md
  - test/lattice_stripe/docs_truth_test.exs
  - .planning/phases/66-product-feature-attachment/66-VALIDATION.md
autonomous: true
requirements: [PROD-01, PROD-02]

must_haves:
  truths:
    - "D-16 / D-17 / D-18 / D-19: the named docs-truth test `entitlements guide teaches the complete catalog to local access journey` proves the existing guide covers stable definition, attach, complete inspect, delete, webhook reconciliation, and local fail-closed authorization with visible feat_/prod_/prodft_/ent_ inputs and outputs."
    - "D-19 / D-22: the same named test proves catalog readers use Product.Feature.list/4 or stream!/4 and the permanent no-entitled?/webhook/local-snapshot/fail-closed boundary remains present."
    - "D-20: the named test `Product.Feature is discoverable beside Entitlements.Feature` proves ExDoc grouping and symmetric source cross-links."
    - "D-21: the named test `JTBD routes entitlement catalog and access across canonical guides` proves a concise Entitlements -> Subscriptions/Checkout -> Webhooks -> Testing route without duplicating the API guide."
    - "D-23: `mix docs` plus semantic-anchor tests prove the headless ExDoc/JTBD presentation uses established accessible headings, runnable examples, warning callouts, links, and monospace diagram with no UI artifact."
    - "D-28: the differential phase gate records at least 2,332 passing tests, at most 38 ExDoc warnings, and zero new warnings naming Product.Feature or the Phase 66 guides; mix ci is informational and not the sole gate."
  artifacts:
    - path: guides/entitlements.md
      provides: "Canonical product-feature catalog-to-access journey"
    - path: guides/user-flows-and-jtbd.md
      provides: "Concise entitlement catalog/access routing entry"
    - path: test/lattice_stripe/docs_truth_test.exs
      provides: "Semantic guide, grouping, cross-link, and boundary locks"
    - path: .planning/phases/66-product-feature-attachment/66-VALIDATION.md
      provides: "Completed Nyquist evidence record"
  key_links:
    - "Entitlements guide catalog examples -> Product.Feature create/retrieve/list/stream!/delete shipped signatures"
    - "entitlement-summary webhook -> full canonical customer reconciliation -> persisted local snapshot -> fail-closed local gate"
    - "JTBD route -> Entitlements, Subscriptions/Checkout, Webhooks, and Testing guides"
  prohibitions:
    - statement: "No Product.features catalog instruction, request-time entitled? helper, Checkout-as-proof claim, new guide, fifth flagship recipe, application UI, or reconciliation framework may ship (D-16, D-18, D-19, D-21, D-22, D-23)."
      status: active
      verification: "test/lattice_stripe/docs_truth_test.exs semantic positive-boundary tests plus focused Product raw-map and module-surface tests"
---

<objective>
Close the canonical documentation journey and execute the differential phase gate against the inherited clean baseline.

Purpose: Developers must discover the correct catalog surface and preserve the webhook-driven, local fail-closed authorization boundary.
Output: repaired Entitlements guide, concise JTBD route, semantic docs locks, and completed validation evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-product-feature-attachment/66-CONTEXT.md
@.planning/phases/66-product-feature-attachment/66-RESEARCH.md
@.planning/phases/66-product-feature-attachment/66-VALIDATION.md
@.planning/phases/65-webhook-objecttypes-testing-fixtures/65-06-SUMMARY.md
@.planning/phases/66-product-feature-attachment/66-04-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide inventory is enumerated in `66-01-PLAN.md`. This plan adds no runtime symbols. It replaces the Phase-65/66 placeholders in `guides/entitlements.md`, adds one routing entry in `guides/user-flows-and-jtbd.md`, adds named tests inside existing `LatticeStripe.DocsTruthTest`, and finalizes the existing `66-VALIDATION.md` evidence table.

<tasks>

<task type="auto">
  <name>Task 1: Teach and lock the catalog-to-access journey</name>
  <files>guides/entitlements.md, guides/user-flows-and-jtbd.md, test/lattice_stripe/docs_truth_test.exs</files>
  <read_first>
    - `guides/entitlements.md` — entire canonical guide, especially Scope boundary, Mental model, Attaching features, Testing, and Webhooks placeholders.
    - `guides/user-flows-and-jtbd.md` — Start Here By Situation routing-list convention.
    - `test/lattice_stripe/docs_truth_test.exs` — docs_group_of/1 helper, totality guard, and existing Entitlements semantic assertions.
    - `lib/lattice_stripe/product/feature.ex` — final signatures and identity language; examples must compile conceptually against this surface.
    - `.planning/phases/63-stripe-native-entitlements/63-CONTEXT.md` — permanent no-entitled?/canonical reconciliation decisions inherited by D-22.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-16 through D-23 and D-28.
  </read_first>
  <action>Replace every stale Phase 65/66 placeholder in the existing Entitlements guide in one pass. Teach D-17's progressive journey with visible feat_ definition + prod_ Product -> prodft_ attachment -> possible ent_ entitlement -> summary webhook -> full canonical customer refetch -> persisted local snapshot -> local fail-closed gate. Cover Product.Feature create/retrieve/list/stream!/delete with canonical code verbs and attach/remove teaching language; show both IDs for retrieve/delete. State that marketing_features and legacy features are pricing-table display maps and direct authoritative catalog readers only to Product.Feature.list/4 or stream!/4. Update Testing/Webhooks placeholders to shipped Phase 65 truth. Preserve and strengthen the no-entitled?, reconciliation, staleness, and Checkout-not-proof boundaries. Add one concise JTBD route linking Entitlements through Subscriptions/Checkout, Webhooks, and Testing. Extend the existing docs-truth semantic block with named tests for durable anchors, grouping/cross-links, and routing; assert concepts and symbols rather than paragraphs.</action>
  <verify>
    <automated>mix test test/lattice_stripe/docs_truth_test.exs test/lattice_stripe/product/feature_test.exs test/lattice_stripe/product_test.exs &amp;&amp; mix docs</automated>
  </verify>
  <acceptance_criteria>
    - Named docs-truth tests cover every D-28 semantic anchor and D-20 grouping/cross-link.
    - Guide has no Phase 65/66 placeholder and never directs catalog reads through Product.features.
    - JTBD contains one concise route, not a duplicate guide or new flagship recipe.
    - Docs build exits 0 with no warning naming Product.Feature or either changed guide.
  </acceptance_criteria>
  <done>The D-16..D-23 guide/JTBD journey is shipped and protected by named semantic tests.</done>
</task>

<task type="auto">
  <name>Task 2: Run the inherited differential gate and finalize validation evidence</name>
  <files>.planning/phases/66-product-feature-attachment/66-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/66-product-feature-attachment/66-VALIDATION.md` — task map and exact sampling contract to mark only from observed evidence.
    - `.planning/phases/65-webhook-objecttypes-testing-fixtures/65-06-SUMMARY.md` — inherited 2,332-test and 38-warning measurements plus known flakes.
    - `.planning/STATE.md` — clean-HEAD warning baseline and why mix ci is inherited-red/not the sole gate.
    - `.github/workflows/ci.yml` — actual compile/test/docs/integration lanes; do not invent a replacement gate.
  </read_first>
  <action>Run the focused Phase 66 command, format, warnings-as-errors compile, strict Credo, full test suite, production compile, API lock test, and docs build. Capture rather than recall the final test count and docs warning count. Require at least 2,332 tests and at most 38 warnings, and reject docs output naming `LatticeStripe.Product.Feature`, `guides/entitlements.md`, or `guides/user-flows-and-jtbd.md`. A known inherited client_test or batch_test flake may be rerun once and must be recorded honestly; do not weaken the floor or warning ceiling. Treat mix ci only as informational because clean HEAD is already red on docs warnings. Before changing any validation flag, reconcile exactly the nine actual task rows `66-01-01`, `66-01-02`, `66-02-01`, `66-03-01`, `66-03-02`, `66-04-01`, `66-04-02`, `66-05-01`, and `66-05-02` against their PLAN.md task names, waves, requirements, threat references, test types, commands, file state, and observed evidence. Record the measured test total and warning total in 66-VALIDATION.md, then set status/nyquist/Wave 0 flags only when all nine rows are green and every Wave 0 file exists.</action>
  <verify>
    <automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix credo --strict &amp;&amp; test_log=$(mktemp "${TMPDIR:-/tmp}/phase66-test.XXXXXX") &amp;&amp; docs_log=$(mktemp "${TMPDIR:-/tmp}/phase66-docs.XXXXXX") &amp;&amp; trap 'rm -f "$test_log" "$docs_log"' 0 &amp;&amp; mix test &gt;"$test_log" 2&gt;&amp;1; test_status=$?; cat "$test_log"; [ "$test_status" -eq 0 ] || exit "$test_status"; test_count=$(sed -n 's/^[[:space:]]*\([0-9][0-9,]*\) tests*,.*$/\1/p' "$test_log" | tail -n 1 | tr -d ','); case "$test_count" in ""|*[!0-9]*) echo "Could not parse mix test count" &gt;&amp;2; exit 1;; esac; [ "$test_count" -ge 2332 ] || { echo "Test floor failed: $test_count &lt; 2332" &gt;&amp;2; exit 1; }; mix docs &gt;"$docs_log" 2&gt;&amp;1; docs_status=$?; cat "$docs_log"; [ "$docs_status" -eq 0 ] || exit "$docs_status"; warning_count=$(grep -c 'warning:' "$docs_log" || true); case "$warning_count" in ""|*[!0-9]*) echo "Could not parse docs warning count" &gt;&amp;2; exit 1;; esac; [ "$warning_count" -le 38 ] || { echo "Docs warning ceiling failed: $warning_count &gt; 38" &gt;&amp;2; exit 1; }; if grep -E 'LatticeStripe\.Product\.Feature|guides/(entitlements|user-flows-and-jtbd)\.md' "$docs_log"; then echo "Phase 66 name found in docs warnings/output" &gt;&amp;2; exit 1; fi; printf 'D-28 measured: %s tests; %s docs warnings\n' "$test_count" "$warning_count"; MIX_ENV=prod mix compile &amp;&amp; mix lattice_stripe.api_surface --check</automated>
  </verify>
  <acceptance_criteria>
    - The failure-propagating gate parses a non-empty numeric full-suite total of at least 2,332 from a successful `mix test` run and records the observed total in 66-VALIDATION.md; zero non-flake failures remain.
    - The same gate requires `mix docs` exit 0, parses and records a numeric warning count no greater than 38, and rejects output naming `LatticeStripe.Product.Feature`, `guides/entitlements.md`, or `guides/user-flows-and-jtbd.md`.
    - API lock, production compile, format, compile warnings, and Credo all pass.
    - 66-VALIDATION.md contains exactly the nine reconciled task IDs from Plans 01–05, no pending task row, and is marked validated/nyquist compliant/Wave 0 complete only from captured passing evidence.
  </acceptance_criteria>
  <done>D-28's differential gate passes and every Phase 66 behavior has recorded executable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library guide → adopter catalog/reconciliation/authorization design | Documentation can cause a caller to use partial or non-access data as authorization truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-02 | Elevation of Privilege | partial catalog or marketing copy used for access | high | mitigate | Named docs-truth test requires complete stream guidance, webhook reconciliation, persisted snapshot, and local fail-closed gate. |
| T-66-03 | Tampering | feat_ definition confused with prodft_ deletion target | high | mitigate | Guide examples show feat_, prod_, and prodft_ together and item operations use explicit prodft_; semantic test locks identity anchors. |
| T-66-04 | Tampering | future fields/public docs drift | medium | mitigate | extra behavior is tested; docs truth and API lock are included in the final differential gate. |
| T-66-SC | Tampering | package-manager installs | high | accept | Phase installs no package and modifies no manifest/lock dependency set. |
</threat_model>

<verification>
Focused Phase 66 tests, full suite floor, docs warning ceiling/differential, format, strict compile/Credo, production compile, and API lock all pass with measured results recorded in 66-VALIDATION.md.
</verification>

<success_criteria>
- The canonical guide and JTBD route match every locked documentation/boundary decision.
- Every D-28 semantic anchor has a named executable docs-truth assertion.
- At least 2,332 tests pass, ExDoc warnings do not exceed 38, and no Phase 66 warning is introduced.
</success_criteria>

<output>
Create `.planning/phases/66-product-feature-attachment/66-05-SUMMARY.md` when done, including exact test and warning measurements.
</output>
