---
phase: 66-product-feature-attachment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/product/feature.ex
  - test/lattice_stripe/product/feature_test.exs
autonomous: true
requirements: [PROD-01]

must_haves:
  truths:
    - "PROD-01 / D-01 / D-04 / D-05: the named test `create/4 posts to the scoped product collection and returns a typed attachment` proves a string-keyed entitlement_feature map is sent to /v1/products/prod_.../features and decoded as %LatticeStripe.Product.Feature{}."
    - "D-02 / D-03: the named test `exports exactly the canonical attachment surface and defaulted arities` proves create/retrieve/list/stream!/delete, their ordinary bang twins, and from_map/1 exist, while attach/detach/remove and a non-bang stream do not."
    - "D-07 / D-08 / D-14: the named tests under `Product.Feature.from_map/1` prove exact fields/defaults, nil safety, idempotence, nested Entitlements.Feature decoding, typed deletion, and unknown top-level extra preservation."
    - "D-05 / D-06 / D-15 / D-24: the named test `delete addresses one prodft attachment directly and never resolves a feat definition` proves explicit prod_ and prodft_ identity with one DELETE and no list/filter/delete lookup."
    - "PROD-01/empty edge: the named test `list returns a typed empty page` proves an empty Stripe page stays an empty %LatticeStripe.List{}, not nil or an error."
    - "PROD-01/ordering edge: the named test `list preserves attachment wire order` proves list/4 neither sorts nor deduplicates equal-feature attachments."
    - "PROD-01/idempotency edge: the named test `two create attempts preserve the caller idempotency key` proves identical retries carry the same key on both requests."
    - "PROD-01/concurrency edge: the named delete identity test proves one caller-selected prodft_ resource is deleted in one request, so concurrent catalog changes cannot retarget an SDK-invented lookup sequence."
  artifacts:
    - path: lib/lattice_stripe/product/feature.ex
      provides: "Typed parent-scoped Product Feature attachment resource"
    - path: test/lattice_stripe/product/feature_test.exs
      provides: "Mox request, decode, guard, error, identity, and public-surface proof"
  key_links:
    - "Product.Feature canonical verbs -> %Request{} scoped paths -> Client.request/2 -> Resource.unwrap_singular/2 or unwrap_list/2 -> Product.Feature.from_map/1"
    - "Product.Feature.from_map/1 -> Entitlements.Feature.from_map/1 for the direct nested object"
  prohibitions:
    - statement: "No attach, detach, remove, delete-by-feat, or non-bang stream public API may ship (D-03, D-06)."
      status: active
      verification: "test/lattice_stripe/product/feature_test.exs — `exports exactly the canonical attachment surface and defaulted arities`"
    - statement: "Create params remain a map with no empty-map default; no positional feature-id replacement may ship (D-04)."
      status: active
      verification: "test/lattice_stripe/product/feature_test.exs — structural arity and missing entitlement_feature guard tests"
---

<objective>
Ship the production Product Feature attachment resource, beginning with one real create path and then expanding to the complete locked verb/decode/guard surface.

Purpose: Developers need a typed, explicit bridge between a Product and an Entitlements Feature without confusing feat_ definitions with prodft_ attachments.
Output: `LatticeStripe.Product.Feature` and focused Mox/ExUnit proof for PROD-01.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-product-feature-attachment/66-CONTEXT.md
@.planning/phases/66-product-feature-attachment/66-RESEARCH.md
@.planning/phases/66-product-feature-attachment/66-PATTERNS.md
@.planning/phases/66-product-feature-attachment/66-VALIDATION.md
</context>

## Artifacts this phase produces

Phase-wide new production symbol/file inventory: `lib/lattice_stripe/product/feature.ex`; public module `LatticeStripe.Product.Feature`; public struct/type `t/0`; public `create/3,4`, `create!/3,4`, `retrieve/3,4`, `retrieve!/3,4`, `list/2,3,4`, `list!/2,3,4`, `stream!/2,3,4`, `delete/3,4`, `delete!/3,4`, and `from_map/1`; private `collection_path/1`, `item_path/2`, `validate_product_id!/2`, and `validate_product_feature_id!/2`. New proof files/modules: `test/lattice_stripe/product/feature_test.exs` / `LatticeStripe.Product.FeatureTest`, and `test/lattice_stripe/product/feature_stream_test.exs` / `LatticeStripe.Product.FeatureStreamTest`. Existing files later modified by this phase: `lib/lattice_stripe/object_types.ex`, `lib/lattice_stripe/product.ex` (moduledoc only), `mix.exs`, `priv/api/current.txt`, `test/lattice_stripe/product_test.exs`, `test/lattice_stripe/object_types_test.exs`, `test/lattice_stripe/docs_truth_test.exs`, `guides/entitlements.md`, and `guides/user-flows-and-jtbd.md`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end create one Product Feature attachment</name>
  <reversibility rating="one-way">D-01's public module name and attachment noun become a published semver contract, already explicitly accepted in 66-CONTEXT.md.</reversibility>
  <files>lib/lattice_stripe/product/feature.ex, test/lattice_stripe/product/feature_test.exs</files>
  <read_first>
    - `lib/lattice_stripe/entitlements/feature.ex` — required-param message shape, bang wrapper, idempotent decoder, nested definition contract, and public docs style.
    - `lib/lattice_stripe/transfer_reversal.ex` — parent-scoped collection/item path construction and eager parent-id guards.
    - `lib/lattice_stripe/resource.ex` — require_param!/3, unwrap_singular/2, and unwrap_bang!/1 contracts.
    - `test/lattice_stripe/entitlements/feature_test.exs` — Mox request/body/options, no-expectation guard, decoder, and bang-test patterns.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-01, D-04, D-05, D-07, D-08, D-14, and D-24.
  </read_first>
  <behavior>
    - A failing test first specifies POST `/v1/products/prod_123/features`, string-keyed `%{"entitlement_feature" => "feat_123"}`, caller opts, and a typed attachment response.
    - Missing `"entitlement_feature"` and nil/empty Product IDs raise ArgumentError before Mox transport.
    - from_map/1 returns nil for nil, returns an existing struct unchanged, defaults object/deleted, directly nests %Entitlements.Feature{}, and retains future fields in extra.
  </behavior>
  <action>Create the D-01 public module with D-07's exact fields `id`, `object`, `livemode`, `entitlement_feature`, `deleted`, and `extra`; default `object` to `"product_feature"`, `deleted` to false, and `extra` to `%{}`. Implement the permanent create path per D-04/D-05 with no params default, the required string key guard before request construction, caller opts unchanged, and singular typed decode. Implement D-08 from_map/1 with nil and struct clauses before the map clause, direct `Entitlements.Feature.from_map/1` nesting, and Map.split-based extra preservation. Add create!/3,4 only through Resource.unwrap_bang!/1. Start with the failing behavioral tests, then implement until green; this is production code that remains in the final module.</action>
  <verify>
    <automated>mix test test/lattice_stripe/product/feature_test.exs --only tracer &amp;&amp; MIX_ENV=prod mix compile</automated>
  </verify>
  <acceptance_criteria>
    - A `@tag :tracer` create test sends POST to the exact Product-scoped collection path, includes entitlement_feature, preserves a per-request option, and returns `%Product.Feature{entitlement_feature: %Entitlements.Feature{}}`.
    - Missing required key and nil/empty Product IDs raise before any transport expectation is installed.
    - Decoder tests prove nil, idempotence, exact defaults, typed nesting, and unknown extra.
  </acceptance_criteria>
  <done>The typed create happy path works end to end and its invalid inputs fail before transport (D-01, D-04, D-05, D-07, D-08, D-14, D-24).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand to retrieve, list, delete, bang twins, and exact surface</name>
  <reversibility rating="costly">D-02's published verbs and defaulted arities require a deprecation cycle or major release to remove.</reversibility>
  <files>lib/lattice_stripe/product/feature.ex, test/lattice_stripe/product/feature_test.exs</files>
  <read_first>
    - `lib/lattice_stripe/transfer_reversal.ex` — retrieve/list/stream parent+child signatures and eager guard clauses.
    - `lib/lattice_stripe/tax_id.ex` — typed deleted response and ordinary bang-twin shapes.
    - `test/lattice_stripe/entitlements/feature_test.exs` — typed list envelope, empty/order tests, tuple/bang errors, and structural exported/absent verb locks.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-02, D-03, D-05, D-06, D-12, D-14, D-15, and D-24.
  </read_first>
  <behavior>
    - Retrieve GETs the scoped prodft_ item; list GETs the collection with params/options; delete DELETEs the same scoped item and decodes deleted true.
    - Every nil/empty required ID raises synchronously before transport for every verb and bang twin; delete also rejects a feat_-prefixed definition ID before transport.
    - Empty list remains typed/empty; multiple attachments retain wire order.
    - Repeated create calls preserve a supplied idempotency key.
    - Exact defaulted arities exist and all forbidden aliases/conveniences are structurally absent.
  </behavior>
  <action>Complete D-02's exact public surface with default arguments exposing only the locked arities. Use collection_path/1 and item_path/2 so every call remains Product-scoped. Per D-05/D-06/D-15, retrieve/delete accept explicit parent and attachment IDs and issue direct item requests; reject nil/empty IDs everywhere and specifically reject a feat_-prefixed definition passed to delete before transport. Do not add lookup/filter/delete behavior. Implement list via Resource.unwrap_list/2, delete via unwrap_singular/2 returning the typed deleted attachment, and ordinary bang twins via unwrap_bang!/1. Add explicit structural assertions for required exports and forbidden attach/detach/remove/non-bang-stream/delete-by-feature shapes. Cover tuple errors, bang raises, options, empty results, stable ordering, stable idempotency headers, and single-request deletion.</action>
  <verify>
    <automated>mix test test/lattice_stripe/product/feature_test.exs &amp;&amp; mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix credo --strict</automated>
  </verify>
  <acceptance_criteria>
    - POST collection, GET item, GET collection, and DELETE item method/path assertions pass.
    - Delete returns `%Product.Feature{id: "prodft_...", object: "product_feature", deleted: true}` with omitted live fields nil, and a feat_-prefixed target raises before transport.
    - Public-surface test asserts every required/defaulted arity and refutes every forbidden alias/convenience arity.
    - Empty, ordering, idempotency, and direct-delete concurrency edge tests pass.
  </acceptance_criteria>
  <done>Developers can create, retrieve, list, and delete typed attachments with the exact D-02 surface and D-03/D-06 identity boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller maps/IDs → Product.Feature → Stripe request | Untrusted Product IDs, attachment IDs, params, and opts become request paths/body/headers. |
| Stripe JSON → Product.Feature.from_map/1 | Externally versioned response data becomes a public typed struct. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-03 | Tampering | delete identity and scoped item path | high | mitigate | D-05/D-06 direct prod_ + prodft_ API, eager ID guards, one-request delete test, and structural absence of a feat_-lookup convenience. |
| T-66-04 | Tampering | future Product Feature response fields | medium | mitigate | D-07/D-08 Map.split decoder preserves all unknown top-level fields in extra; named decoder test supplies a future field. |
| T-66-SC | Tampering | package-manager installs | high | accept | Research requires no package install; tasks modify no dependency manifest or lockfile. |
</threat_model>

<verification>
`mix test test/lattice_stripe/product/feature_test.exs`, production compile, format, warnings-as-errors compile, and strict Credo all pass.
</verification>

<success_criteria>
- The complete canonical typed attachment surface exists with exact defaulted arities.
- All request, guard, decoder, tuple/bang, identity, empty, ordering, idempotency, and concurrency behaviors have named executable tests.
- No alias, convenience deletion, dependency, or unrelated Product-field change is introduced.
</success_criteria>

<output>
Create `.planning/phases/66-product-feature-attachment/66-01-SUMMARY.md` when done.
</output>
