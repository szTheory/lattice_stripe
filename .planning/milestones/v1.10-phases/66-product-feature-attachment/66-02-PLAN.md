---
phase: 66-product-feature-attachment
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - lib/lattice_stripe/product/feature.ex
  - test/lattice_stripe/product/feature_stream_test.exs
autonomous: true
requirements: [PROD-01]

must_haves:
  truths:
    - "D-12 / D-13 / D-25: the named test `page 2 preserves Product scope filters options cursor order and type` proves stream!/4 delegates to List.stream!/2 and retains prod_ scope, limit/expand params, Connect header, prodft_ cursor, wire order, and typed output."
    - "D-13 / D-25: the named test `early termination does not fetch page 2` proves Stream.take/2 causes exactly one transport call."
    - "D-13 / D-25: the named test `later page error raises instead of returning a partial catalog` proves page-two failure is loud."
    - "D-12 / PROD-01/ordering: one-page list and complete stream preserve Stripe order without sorting, deduplication, or reconciliation policy."
  artifacts:
    - path: lib/lattice_stripe/product/feature.ex
      provides: "stream!/2,3,4 delegating the cursor state machine"
    - path: test/lattice_stripe/product/feature_stream_test.exs
      provides: "Product/Connect scoped multi-page, laziness, order/type, and failure proof"
  key_links:
    - "Product.Feature.stream!/4 -> LatticeStripe.List.stream!/2 -> List.build_next_page_request/1 -> page-two Mox request"
    - "raw page data -> List.from_json/3 cursor capture -> downstream Stream.map(Product.Feature.from_map/1)"
  prohibitions:
    - statement: "No custom cursor loop or page-one-as-complete catalog path may be introduced (D-13)."
      status: active
      verification: "test/lattice_stripe/product/feature_stream_test.exs — multi-page and later-page error tests; lib/lattice_stripe/product/feature.ex uses List.stream!/2"
---

<objective>
Prove complete attachment enumeration across the pagination boundary, including Product and Connect scope, laziness, and failure semantics.

Purpose: A correct page-one method is insufficient for an authoritative catalog; the highest-risk failures occur only on page two.
Output: the final stream!/4 implementation and dedicated Mox pagination suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/66-product-feature-attachment/66-CONTEXT.md
@.planning/phases/66-product-feature-attachment/66-RESEARCH.md
@.planning/phases/66-product-feature-attachment/66-PATTERNS.md
@.planning/phases/66-product-feature-attachment/66-01-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide inventory is enumerated in `66-01-PLAN.md`. This plan creates `test/lattice_stripe/product/feature_stream_test.exs`, test module `LatticeStripe.Product.FeatureStreamTest`, and its private page helpers `list_response/2`, `error_response/1`, and `product_feature/2`; it completes public `Product.Feature.stream!/2,3,4` without any non-bang twin.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove complete Product-scoped lazy pagination</name>
  <files>lib/lattice_stripe/product/feature.ex, test/lattice_stripe/product/feature_stream_test.exs</files>
  <read_first>
    - `lib/lattice_stripe/product/feature.ex` — Plan 01's final collection path, validators, decoder, and stream signature.
    - `lib/lattice_stripe/list.ex` — read from from_json/3 through build_next_page_request/1; raw cursor derivation, base params, response url, opts carry, and idempotency-key removal are load-bearing.
    - `test/lattice_stripe/entitlements/active_entitlement_stream_test.exs` — complete hand-authored page seam, early termination, Connect header, and later-page error patterns.
    - `.planning/phases/66-product-feature-attachment/66-CONTEXT.md` — D-12, D-13, and D-25.
  </read_first>
  <behavior>
    - Page two uses the last raw prodft_ ID from page one as starting_after.
    - Product-scoped response URL, caller limit/expand filters, and stripe-account option survive onto page two; page-two GET drops any idempotency key.
    - Items from all pages are typed and emitted in wire order.
    - Stream.take(1) makes only one request; an empty first page yields an empty stream; a page-two error raises LatticeStripe.Error.
  </behavior>
  <action>Ensure D-13's stream!/4 validates the Product ID before constructing the lazy stream, builds the collection GET request, delegates exactly once to `LatticeStripe.List.stream!/2`, and types only after the shared list machinery has captured raw cursor IDs. Create the dedicated stream suite using hand-authored raw JSON pages and exact Mox expectation counts. Assert page two's decoded query values rather than ambiguous substrings: starting_after is the last page-one prodft_ ID; limit and expand persist; the request path remains under the original prod_ Product; the stripe-account header persists; the idempotency header is removed. Assert typed wire order, one-request early termination, one-request empty input, and LatticeStripe.Error on page two. Do not edit List itself or use stripe-mock for cursor claims.</action>
  <verify>
    <automated>mix test test/lattice_stripe/product/feature_stream_test.exs &amp;&amp; git diff --quiet lib/lattice_stripe/list.ex &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Dedicated suite passes with named tests for cursor/scope/filters/options/order/type, early termination, empty first page, and later-page error.
    - `git diff --quiet lib/lattice_stripe/list.ex` exits 0.
    - The page-two scope/options test fails if base params or forwarded opts are removed, and the cursor test fails if typing moves ahead of raw ID derivation; temporary mutations are restored and recorded in the summary.
  </acceptance_criteria>
  <done>Product.Feature.stream!/4 completely enumerates attachments with D-12/D-13/D-25 scope, ordering, laziness, and loud failure guarantees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| List page one → library-built page two request | The caller no longer constructs later requests; Product scope, filters, and Connect opts must survive reconstruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-01 | Information Disclosure | page-two Product path or stripe-account header | high | mitigate | Named Mox test asserts exact prod_ path and account header on page two. |
| T-66-02 | Elevation of Privilege | partial catalog consumed as authorization truth | high | mitigate | Complete stream delegates to List.stream!/2, later-page errors raise, and early/complete behavior is separately tested; docs boundary lands in Plan 05. |
| T-66-04 | Tampering | cursor derived after typing or from wrong item | high | mitigate | Named test asserts the last raw page-one prodft_ ID becomes page-two starting_after and is mutation-checked. |
| T-66-SC | Tampering | package-manager installs | high | accept | No package installation or manifest change occurs. |
</threat_model>

<verification>
Run the dedicated pagination suite, assert List is unmodified, then run `mix test test/lattice_stripe/product/feature_test.exs test/lattice_stripe/product/feature_stream_test.exs`.
</verification>

<success_criteria>
- Complete enumeration retains Product/Connect scope and all caller filters.
- Cursor, call count, laziness, empty input, order/type, and later-page failure are executable named evidence.
- The shared List state machine remains byte-identical.
</success_criteria>

<output>
Create `.planning/phases/66-product-feature-attachment/66-02-SUMMARY.md` when done.
</output>
