---
phase: 04-customers-paymentintents
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/lattice_stripe/payment_intent.ex
  - test/lattice_stripe/payment_intent_test.exs
autonomous: true
requirements:
  - PINT-01
  - PINT-02
  - PINT-03
  - PINT-04
  - PINT-05
  - PINT-06
  - PINT-07

must_haves:
  truths:
    - "Developer can create a PaymentIntent with amount and currency and receive a typed %PaymentIntent{} struct"
    - "Developer can retrieve, update, confirm, capture, and cancel a PaymentIntent by ID"
    - "Developer can list PaymentIntents with filters and receive %Response{data: %List{}} with typed %PaymentIntent{} items"
    - "Developer can stream all PaymentIntents lazily with stream!/3"
    - "Custom Inspect hides client_secret and shows id, object, amount, currency, status"
  artifacts:
    - path: "lib/lattice_stripe/payment_intent.ex"
      provides: "PaymentIntent struct, CRUD, confirm, capture, cancel, list, stream, bang variants, from_map/1, custom Inspect"
      min_lines: 180
    - path: "test/lattice_stripe/payment_intent_test.exs"
      provides: "Mox-based tests for all PaymentIntent operations"
      min_lines: 120
  key_links:
    - from: "lib/lattice_stripe/payment_intent.ex"
      to: "lib/lattice_stripe/client.ex"
      via: "Client.request/2 called for every operation"
      pattern: "Client\\.request\\(client"
    - from: "lib/lattice_stripe/payment_intent.ex"
      to: "lib/lattice_stripe/list.ex"
      via: "List.stream!/2 wrapped for auto-pagination"
      pattern: "List\\.stream!\\(client"
    - from: "lib/lattice_stripe/payment_intent.ex"
      to: "lib/lattice_stripe/request.ex"
      via: "%Request{} structs built for each operation"
      pattern: "%Request\\{method:"
---

<objective>
Create the LatticeStripe.PaymentIntent resource module — the most important Stripe resource for SaaS apps — following the pattern established by Customer in Plan 01.

Purpose: PaymentIntent adds action verbs (confirm, capture, cancel) to the CRUD+list pattern, completing the resource module validation. This is the resource developers will use most often.
Output: `lib/lattice_stripe/payment_intent.ex` with struct, CRUD, action verbs, list, stream, bang variants + comprehensive Mox tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-customers-paymentintents/04-CONTEXT.md
@.planning/phases/04-customers-paymentintents/04-RESEARCH.md
@.planning/phases/04-customers-paymentintents/04-01-SUMMARY.md

@lib/lattice_stripe/customer.ex
@lib/lattice_stripe/client.ex
@lib/lattice_stripe/request.ex
@lib/lattice_stripe/response.ex
@lib/lattice_stripe/list.ex
@lib/lattice_stripe/error.ex
@test/lattice_stripe/customer_test.exs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/lattice_stripe/request.ex:
```elixir
defstruct [:method, :path, params: %{}, opts: []]
```

From lib/lattice_stripe/response.ex:
```elixir
defstruct [:data, :status, :request_id, headers: []]
```

From lib/lattice_stripe/list.ex:
```elixir
@spec stream!(Client.t(), Request.t()) :: Enumerable.t()
def stream!(%Client{} = client, %Request{} = req)
```

From lib/lattice_stripe/error.ex:
```elixir
defexception [:type, :code, :message, :status, :request_id, :param, :decline_code, :charge, :doc_url, :raw_body]
```

From lib/lattice_stripe/client.ex:
```elixir
def request(%__MODULE__{} = client, %Request{} = req)  # {:ok, %Response{}} | {:error, %Error{}}
def request!(%__MODULE__{} = client, %Request{} = req)  # %Response{} | raises Error
```

From lib/lattice_stripe/customer.ex (Plan 01 output — follow this pattern exactly):
```elixir
# Pattern to copy: unwrap_singular, unwrap_list, unwrap_bang!, from_map
defp unwrap_singular({:ok, %Response{data: data}}), do: {:ok, from_map(data)}
defp unwrap_singular({:error, %Error{}} = error), do: error

defp unwrap_list({:ok, %Response{data: %List{} = list} = resp}) do
  typed = %{list | data: Enum.map(list.data, &from_map/1)}
  {:ok, %{resp | data: typed}}
end
defp unwrap_list({:error, %Error{}} = error), do: error

defp unwrap_bang!({:ok, result}), do: result
defp unwrap_bang!({:error, %Error{} = error}), do: raise(error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaymentIntent struct, CRUD, action verbs, list, stream, and tests</name>
  <files>lib/lattice_stripe/payment_intent.ex, test/lattice_stripe/payment_intent_test.exs</files>
  <read_first>
    - lib/lattice_stripe/customer.ex (THE pattern to follow — copy structure, adapt for PaymentIntent)
    - test/lattice_stripe/customer_test.exs (test pattern to follow — copy structure, adapt for PaymentIntent)
    - lib/lattice_stripe/client.ex (Client.request/2 signature)
    - lib/lattice_stripe/request.ex (Request struct fields)
    - lib/lattice_stripe/response.ex (Response struct and Inspect pattern)
    - lib/lattice_stripe/list.ex (List.stream!/2 signature)
    - lib/lattice_stripe/error.ex (Error struct for pattern matching)
    - test/test_helper.exs (MockTransport definition)
  </read_first>
  <behavior>
    - from_map/1 maps all ~35 known PaymentIntent fields to struct fields, unknown fields go to `extra`
    - from_map/1 defaults: object to "payment_intent", extra to %{}
    - Inspect shows id, object, amount, currency, status; hides client_secret, receipt_email, shipping
    - create/3 sends POST /v1/payment_intents with params (amount+currency required by Stripe), returns {:ok, %PaymentIntent{}}
    - retrieve/3 sends GET /v1/payment_intents/:id, returns typed struct
    - update/4 sends POST /v1/payment_intents/:id with params, returns typed struct
    - confirm/4 sends POST /v1/payment_intents/:id/confirm with optional params, returns typed struct
    - capture/4 sends POST /v1/payment_intents/:id/capture with optional params, returns typed struct
    - cancel/4 sends POST /v1/payment_intents/:id/cancel with optional params, returns typed struct
    - list/3 sends GET /v1/payment_intents, returns %Response{data: %List{}} with typed items
    - stream!/3 yields individual %PaymentIntent{} structs from lazy pagination
    - bang variants raise Error on error response
    - error passthrough: {:error, %Error{}} passed through unchanged
    - NO delete function (D-11: PaymentIntent has no delete endpoint)
  </behavior>
  <action>
Create `lib/lattice_stripe/payment_intent.ex` following the Customer module pattern exactly (per D-01). Key differences from Customer:

**Module: `LatticeStripe.PaymentIntent`**

1. `@moduledoc` describing PaymentIntent resource. Include usage examples for create (with amount+currency), confirm, capture, cancel.

2. Aliases: `LatticeStripe.{Client, Error, List, Request, Response}`

3. `@known_fields ~w[id object amount amount_capturable amount_details amount_received application application_fee_amount automatic_payment_methods canceled_at cancellation_reason capture_method client_secret confirmation_method created currency customer customer_account description excluded_payment_method_types hooks last_payment_error latest_charge livemode metadata next_action on_behalf_of payment_method payment_method_configuration_details payment_method_options payment_method_types processing receipt_email review setup_future_usage shipping source statement_descriptor statement_descriptor_suffix status transfer_data transfer_group]`

4. `defstruct` — all fields as nil defaults, plus:
   - `object: "payment_intent"` (default)
   - `extra: %{}` (default)
   - NO `deleted` field (PaymentIntent cannot be deleted per D-11)

5. `@type t :: %__MODULE__{...}` typespec for documentation.

6. Public API functions:
   - `create(client, params \\ %{}, opts \\ [])` — POST `/v1/payment_intents`, unwrap_singular
   - `retrieve(client, id, opts \\ [])` — GET `/v1/payment_intents/#{id}`, unwrap_singular, guard `when is_binary(id)`
   - `update(client, id, params, opts \\ [])` — POST `/v1/payment_intents/#{id}`, unwrap_singular, guard `when is_binary(id)`
   - `confirm(client, id, params \\ %{}, opts \\ [])` — POST `/v1/payment_intents/#{id}/confirm`, unwrap_singular, guard `when is_binary(id)`
   - `capture(client, id, params \\ %{}, opts \\ [])` — POST `/v1/payment_intents/#{id}/capture`, unwrap_singular, guard `when is_binary(id)`
   - `cancel(client, id, params \\ %{}, opts \\ [])` — POST `/v1/payment_intents/#{id}/cancel`, unwrap_singular, guard `when is_binary(id)`
   - `list(client, params \\ %{}, opts \\ [])` — GET `/v1/payment_intents`, unwrap_list
   - `stream!(client, params \\ %{}, opts \\ [])` — builds Request, pipes `List.stream!(client, req) |> Stream.map(&from_map/1)`
   - NO `delete` function, NO `search` function, NO `search_stream!` (PaymentIntents do not have delete or search endpoints)

7. Bang variants: `create!/3`, `retrieve!/3`, `update!/4`, `confirm!/4`, `capture!/4`, `cancel!/4`, `list!/3` — each calls non-bang and pipes through `unwrap_bang!/1`.

8. Private helpers — copy exactly from Customer module:
   - `unwrap_singular/1`, `unwrap_list/1`, `unwrap_bang!/1` — identical logic
   - `from_map(map)` — maps all ~35 known fields, `extra: Map.drop(map, @known_fields)`

9. `defimpl Inspect, for: LatticeStripe.PaymentIntent` — show id, object, amount, currency, status. Hide client_secret (CRITICAL security — see Pitfall 1), receipt_email, shipping.

**Test file: `test/lattice_stripe/payment_intent_test.exs`**

Module: `LatticeStripe.PaymentIntentTest`, `use ExUnit.Case, async: true`, `import Mox`, `setup :verify_on_exit!`

Test helper functions (same pattern as CustomerTest):
- `test_client/0` — same as Customer
- `payment_intent_json/1` — defaults: `"id" => "pi_test123"`, `"object" => "payment_intent"`, `"amount" => 2000`, `"currency" => "usd"`, `"status" => "requires_payment_method"`, `"client_secret" => "pi_test123_secret_abc"`, `"livemode" => false`, `"created" => 1_700_000_000`, `"metadata" => %{}`
- `ok_response/1` — `{:ok, %{status: 200, headers: [{"request-id", "req_test"}], body: Jason.encode!(body)}}`
- `error_response/0` — 400 error response
- `list_json/1` — list wrapper

Test cases:
- `describe "create/3"`: assert POST /v1/payment_intents, returns `{:ok, %PaymentIntent{id: "pi_test123", amount: 2000, currency: "usd"}}`
- `describe "retrieve/3"`: assert GET /v1/payment_intents/pi_test123
- `describe "update/4"`: assert POST /v1/payment_intents/pi_test123
- `describe "confirm/4"`: assert POST /v1/payment_intents/pi_test123/confirm, test with empty params (Pitfall 6)
- `describe "capture/4"`: assert POST /v1/payment_intents/pi_test123/capture
- `describe "cancel/4"`: assert POST /v1/payment_intents/pi_test123/cancel
- `describe "list/3"`: assert GET /v1/payment_intents, typed items in List
- `describe "create!/3"`: test raises Error on error response
- `describe "from_map/1"`: test unknown fields go to extra, status field preserved
- `describe "Inspect"`: test inspect output contains id and amount but NOT client_secret
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lattice_stripe && mix test test/lattice_stripe/payment_intent_test.exs --trace</automated>
  </verify>
  <acceptance_criteria>
    - lib/lattice_stripe/payment_intent.ex contains `defmodule LatticeStripe.PaymentIntent do`
    - lib/lattice_stripe/payment_intent.ex contains `defstruct` with `object: "payment_intent"` and `extra: %{}`
    - lib/lattice_stripe/payment_intent.ex contains `@known_fields ~w[id object amount`
    - lib/lattice_stripe/payment_intent.ex contains `def create(%Client{} = client, params \\ %{}, opts \\ [])`
    - lib/lattice_stripe/payment_intent.ex contains `def retrieve(%Client{} = client, id, opts \\ []) when is_binary(id)`
    - lib/lattice_stripe/payment_intent.ex contains `def update(%Client{} = client, id, params, opts \\ []) when is_binary(id)`
    - lib/lattice_stripe/payment_intent.ex contains `def confirm(%Client{} = client, id, params \\ %{}, opts \\ []) when is_binary(id)`
    - lib/lattice_stripe/payment_intent.ex contains `def capture(%Client{} = client, id, params \\ %{}, opts \\ []) when is_binary(id)`
    - lib/lattice_stripe/payment_intent.ex contains `def cancel(%Client{} = client, id, params \\ %{}, opts \\ []) when is_binary(id)`
    - lib/lattice_stripe/payment_intent.ex contains `def list(%Client{} = client, params \\ %{}, opts \\ [])`
    - lib/lattice_stripe/payment_intent.ex contains `def stream!(%Client{} = client, params \\ %{}, opts \\ [])`
    - lib/lattice_stripe/payment_intent.ex contains `defp from_map(map) when is_map(map)`
    - lib/lattice_stripe/payment_intent.ex contains `extra: Map.drop(map, @known_fields)`
    - lib/lattice_stripe/payment_intent.ex contains `defimpl Inspect, for: LatticeStripe.PaymentIntent`
    - lib/lattice_stripe/payment_intent.ex does NOT contain `def delete`
    - lib/lattice_stripe/payment_intent.ex does NOT contain `def search`
    - lib/lattice_stripe/payment_intent.ex does NOT contain `Jason.Encoder`
    - test/lattice_stripe/payment_intent_test.exs contains `describe "confirm/4"`
    - test/lattice_stripe/payment_intent_test.exs contains `describe "capture/4"`
    - test/lattice_stripe/payment_intent_test.exs contains `describe "cancel/4"`
    - `mix test test/lattice_stripe/payment_intent_test.exs` exits 0
    - `mix test` exits 0 (no regressions including Customer tests from Plan 01)
  </acceptance_criteria>
  <done>
    PaymentIntent module implements all 7 PINT requirements. CRUD, confirm, capture, cancel, list, stream, and bang variants work.
    Tests pass for all operations. No delete or search (per D-11 and Stripe API).
    No regressions in Customer tests or existing test suite.
    client_secret hidden in Inspect output.
  </done>
</task>

</tasks>

<verification>
- `mix test test/lattice_stripe/payment_intent_test.exs --trace` — all PaymentIntent tests pass
- `mix test test/lattice_stripe/customer_test.exs --trace` — Customer tests still pass
- `mix test` — full suite passes, no regressions
- `mix compile --warnings-as-errors` — no warnings
- `mix format --check-formatted` — code formatted
</verification>

<success_criteria>
- LatticeStripe.PaymentIntent module exists with struct, CRUD, confirm/capture/cancel, list, stream, bang variants
- All 7 PINT requirements (PINT-01 through PINT-07) have passing tests
- No delete function (per D-11)
- Custom Inspect hides client_secret, no Jason.Encoder
- Pattern matches Customer module structure (per D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-customers-paymentintents/04-02-SUMMARY.md`
</output>
