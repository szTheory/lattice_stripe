---
phase: 75-typed-contract-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lattice_stripe/invoice.ex
  - test/lattice_stripe/invoice_test.exs
  - guides/invoices.md
  - CHANGELOG.md
  - priv/api/current.txt
autonomous: true
requirements: [DRIFT-02, DRIFT-03, DRIFT-04]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DRIFT-02: A schema-derived invoice response returned through Invoice.retrieve/3 exposes amount_paid_off_stripe as an integer and retains an unrelated response key in extra; the named retrieval test proves this."
    - "DRIFT-03: Invoice.from_map/1 yields nil when the optional field is absent or null, preserves the existing amount_paid meaning, and the focused tests plus API surface lock show an additive change only."
    - "DRIFT-04: Invoice module docs, the invoices guide, and the changelog state the 2026-05-27.dahlia minimum, API-request-only availability, and unchanged 2026-03-25.dahlia default; ExDoc and version-prose checks prove the documentation builds."
  artifacts:
    - path: lib/lattice_stripe/invoice.ex
      provides: "Optional public invoice field, integer typespec, decoder assignment, and field contract"
    - path: test/lattice_stripe/invoice_test.exs
      provides: "Schema-derived retrieval and decoder regression proof"
    - path: guides/invoices.md
      provides: "Off-Stripe payment reconciliation guidance"
    - path: CHANGELOG.md
      provides: "Unreleased adopter-facing contract note"
    - path: priv/api/current.txt
      provides: "Reviewed additive public surface snapshot"
  key_links:
    - from: "Invoice.retrieve/3 transport response"
      to: "Invoice.from_map/1"
      via: "Existing Resource response unwrapping, asserted by the named retrieval test"
    - from: "@known_fields amount_paid_off_stripe"
      to: "Invoice.extra"
      via: "Map.split keeps the selected key typed and unrelated keys available"
    - from: ".planning/phases/75-typed-contract-updates/75-EVIDENCE.md"
      to: "invoice decoder, fixture note, and adopter docs"
      via: "Pinned GA JSON Pointer, 2026-05-27.dahlia minimum, and schema-derived provenance (D-01, D-06)"
  assumptions:
    - id: EDGE-75-DRIFT-02
      status: flagged
      statement: "Spec-less edge probe left DRIFT-02 unclassified and unresolved; the plan uses D-04/D-05's optional-field and extra cases without claiming live Stripe response realism."
    - id: EDGE-75-DRIFT-03
      status: flagged
      statement: "Spec-less edge probe left DRIFT-03 unclassified and unresolved; focused compatibility checks cover the known public contract while any new incompatibility must be surfaced."
    - id: EDGE-75-DRIFT-04
      status: flagged
      statement: "Spec-less edge probe left DRIFT-04 concurrency unresolved; this plan owns its shared files in wave 1 and a later plan must edit them in a later wave after re-reading current content."
---

<objective>
Make the selected Invoice off-Stripe payment amount available as typed data through the public retrieval path, with schema-derived decoding proof and honest version guidance.

Purpose: Give reconciliation code the actual off-Stripe amount in smallest currency units while preserving existing Invoice values and unknown-field access (DRIFT-02, DRIFT-03, DRIFT-04; D-01–D-07).
Output: Updated Invoice struct/decoder/docs, focused tests, the invoices guide, changelog note, and reviewed API surface lock.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-typed-contract-updates/75-CONTEXT.md
@.planning/phases/75-typed-contract-updates/75-EVIDENCE.md
@.planning/phases/75-typed-contract-updates/75-PATTERNS.md
@.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
@.agents/skills/lattice-verification-policy/SKILL.md
@guides/api_stability.md

The dated 75-EVIDENCE.md refresh supersedes the earlier no-eligible-field conclusion in 75-RESEARCH.md and the conditional status of 75-VALIDATION.md. It establishes exact inventory membership and both immutable GA JSON pointers for the selected Invoice field. The 2026-03-25.dahlia package pin remains unchanged (D-02). These are schema-derived decoder examples, never captured live Stripe responses (D-05, D-06). The spec-less edge probe's DRIFT-02, DRIFT-03, and DRIFT-04 rows remain flagged in must_haves.assumptions.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Decode and explain one off-Stripe Invoice payment through retrieval</name>
  <files>lib/lattice_stripe/invoice.ex, test/lattice_stripe/invoice_test.exs, guides/invoices.md, CHANGELOG.md</files>
  <read_first>.planning/phases/75-typed-contract-updates/75-EVIDENCE.md exact Invoice row and provenance; lib/lattice_stripe/invoice.ex known fields, struct, typespec, from_map/1, and module docs; test/lattice_stripe/invoice_test.exs from_map/1 and retrieve/3 groups; guides/invoices.md; CHANGELOG.md top section; lib/lattice_stripe.ex API pin.</read_first>
  <behavior>
    - A mocked Invoice.retrieve/3 response at a client override of 2026-05-27.dahlia returns %Invoice{amount_paid_off_stripe: 700}; the transport request carries that version header, and an unrelated future key remains in extra.
    - Invoice.from_map/1 maps the integer value and yields nil for both omitted and explicit null forms; existing amount_paid and unrelated extra values remain unchanged.
  </behavior>
  <action>Write the failing retrieval and decoder assertions first, then implement the production path. Per D-01, use only Invoice.amount_paid_off_stripe qualified in 75-EVIDENCE.md, whose pinned GA pointer is /components/schemas/invoice/properties/amount_paid_off_stripe at Stripe OpenAPI commit c8faccbde66b784ea916d3c28f5790d7a9c9aee2. In Invoice, add amount_paid_off_stripe to @known_fields, defstruct, @type t as integer() | nil, and from_map/1 using the existing string-key pass-through pattern (D-03, D-04). Leave amount_paid, extra, request options, and LatticeStripe.api_version/0 behavior intact. In the test, mark the payload schema-derived with the commit, pointer, and 2026-05-27.dahlia minimum; use the existing invoice_json override and MockTransport retrieval pattern, assert the requested Stripe-Version header, typed amount, and unrelated extra key. Add direct omission/null assertions; do not claim a live Stripe capture or event-payload support (D-05). Add concise Invoice module/typedoc prose and a reconciliation paragraph in guides/invoices.md: integer smallest-currency-unit amount, retrieval/API-request response only, minimum 2026-05-27.dahlia, and explicit client-version opt-in because the default stays 2026-03-25.dahlia (D-02, D-06). Add an unreleased changelog note without rewriting published release sections. Keep the guide focused on the adopter's reconciliation job (D-06, D-07).</action>
  <reversibility rating="costly">The optional public field and integer representation become a compatibility commitment when the next Hex release publishes them; D-03 already fixes the additive shape.</reversibility>
  <verify>
    <automated>mix test test/lattice_stripe/invoice_test.exs</automated>
    <fails_when>The focused suite exits nonzero, or the new retrieval/omission/null/extra assertions fail.</fails_when>
  </verify>
  <acceptance_criteria>
    - Invoice.retrieve/3 returns the selected integer field through the existing response pipeline at the explicit later API version, with the unrelated response key still in extra.
    - Absent and explicit null values decode to nil; existing amount_paid is unchanged.
    - The test payload is labeled schema-derived with pinned GA commit, exact pointer, and minimum API version.
    - Public docs and the changelog accurately scope the field to API request responses and the later minimum; the package default pin remains 2026-03-25.dahlia.
  </acceptance_criteria>
  <done>The named Invoice retrieval and decoder tests pass, and an adopter can discover the typed reconciliation field and its version boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Review and lock the additive Invoice public surface</name>
  <files>priv/api/current.txt</files>
  <read_first>guides/api_stability.md public struct and release meanings; test/lattice_stripe/api_surface_lock_test.exs; lib/mix/tasks/lattice_stripe.api_surface.ex --update contract; current priv/api/current.txt; Task 1 Invoice source diff.</read_first>
  <action>Run mix lattice_stripe.api_surface --update locally after the Invoice path passes, then inspect the diff before accepting it. Per D-03 and D-07, retain only the expected additive Invoice field surface entry and any directly required type signature entry; a removed/renamed line or altered existing representation is a compatibility failure requiring replanning, not a snapshot refresh. Record the minor-release implication from guides/api_stability.md in the plan summary without manually bumping the release manifest (D-06). Confirm the package API-version constant is still 2026-03-25.dahlia (D-02). The public snapshot is a shared file: before writing, re-read its current content and reconcile any intervening edit; do not overwrite a concurrent change (EDGE-75-DRIFT-04).</action>
  <verify>
    <automated>mix lattice_stripe.api_surface --check</automated>
    <fails_when>The command exits nonzero or reports a public surface mismatch.</fails_when>
    <automated>mix lattice_stripe.version_prose --check</automated>
    <fails_when>The command exits nonzero or reports a published-version prose mismatch.</fails_when>
    <automated>mix docs --warnings-as-errors</automated>
    <fails_when>ExDoc exits nonzero or emits a warning treated as an error.</fails_when>
  </verify>
  <acceptance_criteria>
    - The API lock diff contains only reviewed additions for Invoice; there are no removals or changed existing public entries.
    - The default API version constant remains 2026-03-25.dahlia.
    - API surface, version prose, and warning-free docs checks pass.
  </acceptance_criteria>
  <done>The Invoice addition has a reviewable compatibility diff and builds as truthful public documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe response map → Invoice.from_map/1 | External JSON keys and values enter a public struct; only the selected compile-time key moves from extra to a field. |

## STRIDE Threat Register (ASVS 1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-75-01 | Tampering | Invoice.from_map/1 | medium | mitigate | Keep Map.split and extra, avoid dynamic atom creation, and assert integer, omission, null, and unknown-key cases in the focused test. |
| T-75-02 | Information disclosure | Invoice docs | low | accept | The guide uses synthetic IDs and a schema-derived amount; no credentials or live customer data enter fixtures. |
</threat_model>

## Artifacts this phase produces

This plan produces the typed Invoice field, its retrieval and decoder proof, the off-Stripe reconciliation guide, a changelog note, and an additive API lock entry. Plan 75-02 produces the remaining Refund attribution fields and final phase gate.

<verification>
Run the focused Invoice test after Task 1 and the surface, prose, and ExDoc checks after Task 2. Each runnable command above names its failure signal. Review the API lock diff against the compatibility guide before accepting it; the final full mix ci gate belongs to Plan 75-02.
</verification>

<success_criteria>
An adopter who explicitly requests the supported later Stripe version can retrieve an Invoice and read amount_paid_off_stripe as an integer; unknown fields remain in extra, existing fields and the default API pin keep their behavior, and public docs state the precise version and response scope.
</success_criteria>

<output>
Create .planning/phases/75-typed-contract-updates/75-01-SUMMARY.md when done, with named executable evidence and schema-derived fixture provenance.
</output>
