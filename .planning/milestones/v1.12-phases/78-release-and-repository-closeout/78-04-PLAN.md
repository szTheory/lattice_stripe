---
phase: 78-release-and-repository-closeout
plan: 04
type: execute
wave: 2
depends_on: [78-02]
files_modified:
  - scripts/maintainer/worktree_closeout_check.sh
  - scripts/maintainer/test_worktree_closeout_check.sh
  - docs/maintainer-release.md
autonomous: true
requirements: [CLOSE-03]
estimate: {tokens: 15000, raw_tokens: 15000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A named read-only checker inventories the primary and every linked worktree, including untracked state (test_worktree_closeout_check.sh)."
    - "A dirty, uninspectable, or unowned worktree blocks closure and remains untouched; the primary tree must be release main synchronized to origin/main at final closeout."
  artifacts:
    - scripts/maintainer/worktree_closeout_check.sh
    - scripts/maintainer/test_worktree_closeout_check.sh
  key_links:
    - "The final closeout command uses git worktree list --porcelain -z and per-path porcelain status rather than the current-tree-only hygiene result."
---

<objective>
Make repository cleanliness an exhaustive, non-destructive, repeatable closeout gate, per D-06.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/ROADMAP.md
@.planning/phases/78-release-and-repository-closeout/78-CONTEXT.md
@.planning/phases/78-release-and-repository-closeout/78-PATTERNS.md
@scripts/maintainer/repo_hygiene_check.sh</context>
<tasks>
<task type="auto">
  <name>Probe every Git worktree and the release-main relation</name>
  <read_first>scripts/maintainer/repo_hygiene_check.sh; .planning/phases/78-release-and-repository-closeout/78-RESEARCH.md</read_first>
  <files>scripts/maintainer/worktree_closeout_check.sh, scripts/maintainer/test_worktree_closeout_check.sh</files>
  <action>Per D-06, parse NUL-delimited `git worktree list --porcelain -z` records without whitespace splitting; identify the primary path and each linked path, including detached/locked cases. For each path run `git -C "$path" status --porcelain=v1 -z --untracked-files=all`; report path, branch/SHA, dirty entries and inspection errors. Require an explicit owner association for every linked worktree in the closeout record; missing ownership blocks even if clean. At final mode require primary branch main, release SHA at main, and local main synchronized to freshly fetched origin/main. The checker performs read-only Git queries; fetch is a separate explicit prerequisite, and no status failure triggers cleanup. Add fixtures for clean, tracked dirty, untracked, path with spaces, missing/locked/uninspectable and command failure.</action>
  <verify><automated>bash scripts/maintainer/test_worktree_closeout_check.sh</automated><fails_when>Any dirty or uninspectable fixture passes, a whitespace path is misparsed, or a clean inventory fails.</fails_when></verify>
  <acceptance_criteria>The script reports all inventoried paths and fails closed on any unknown state or release-main mismatch.</acceptance_criteria>
  <done>The primary and linked trees can be checked without changing their contents.</done>
</task>
<task type="auto">
  <name>Document the owner-safe worktree closeout procedure</name>
  <read_first>docs/maintainer-release.md; scripts/maintainer/worktree_closeout_check.sh; .planning/RELEASE-TRAIN.md</read_first>
  <files>docs/maintainer-release.md</files>
  <action>Per D-06, add the exact final probe command, required owner mapping format, origin/main refresh prerequisite, and response to dirty/unowned worktrees: identify the owner, preserve the tree, and ask that owner to resolve its changes through ordinary commits or explicit disposal. Explain that a prior local checkout is currently ahead of origin/main and that final synchronization is a separate release step; avoid implying that research-time counts remain current.</action>
  <verify><automated>bash scripts/maintainer/test_worktree_closeout_check.sh</automated><fails_when>Documented command/owner format does not match the implemented probe or fixture suite fails.</fails_when></verify>
  <acceptance_criteria>A maintainer can run the full inventory and act on each blocker without destructive cleanup.</acceptance_criteria>
  <done>Closeout instructions make the machine gate and owner boundary explicit.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shared Git workspace to milestone closeout | Linked worktrees may contain another agent's or user's uncommitted work. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-09 | Tampering | worktree closeout | high | mitigate | Read-only status, owner association, and blocker on dirty/unknown paths; no implicit cleanup. |
| T-78-10 | Repudiation | release main state | medium | mitigate | Record refreshed origin/main, local main, and release SHA relation in final evidence. |
</threat_model>
## Artifacts this phase produces
- Worktree inventory checker, fixtures, and maintainer closeout instructions.
<verification>Run the deterministic fixtures and inspect the live inventory without changing any tree.</verification>
<success_criteria>All worktree paths are inspected and any dirty/unknown state is a named blocker.</success_criteria>
<output>Create `.planning/phases/78-release-and-repository-closeout/78-04-SUMMARY.md`.</output>
