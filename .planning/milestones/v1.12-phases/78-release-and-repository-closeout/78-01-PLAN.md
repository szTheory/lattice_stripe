---
phase: 78-release-and-repository-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test_apps/phoenix_adopter/mix.exs
  - test_apps/phoenix_adopter/README.md
  - scripts/maintainer/published_hex_adopter_smoke.sh
  - scripts/maintainer/release_evidence_check.sh
  - scripts/maintainer/test_release_evidence_check.sh
  - .github/workflows/release.yml
  - .github/workflows/publish-hex.yml
autonomous: true
requirements: [REL-02, CLOSE-01]
estimate: {tokens: 29000, raw_tokens: 29000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A named published-Hex smoke proves an exact registry version is installed and the synthetic Checkout/webhook host flow passes without Stripe credentials (published_hex_adopter_smoke.sh)."
    - "A named release-evidence check joins main ancestry, immutable tag and GitHub Release SHA, exact-SHA successful ci-gate, Hex version/checksum, and versioned HexDocs (release_evidence_check.sh)."
    - "Wrong SHA, stale CI, missing docs, wrong Hex source/version, or mismatched checksum fails the fixture test (test_release_evidence_check.sh)."
  artifacts:
    - scripts/maintainer/published_hex_adopter_smoke.sh
    - scripts/maintainer/release_evidence_check.sh
    - scripts/maintainer/test_release_evidence_check.sh
  key_links:
    - "Release workflow publication invokes release_evidence_check.sh with the resolved release SHA and version after registry propagation."
    - "The Hex smoke resolves lattice_stripe from Hex in isolated Mix state, while regular adopter CI retains the path dependency."
---

<objective>
Make a published package independently provable from an immutable release commit. D-02 and D-03 require the adopter's actual Hex bytes, release metadata, docs, and CI to agree. Output: a reusable release verifier and release-boundary workflow call.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-release-and-repository-closeout/78-CONTEXT.md
@.planning/phases/78-release-and-repository-closeout/78-RESEARCH.md
@.planning/phases/78-release-and-repository-closeout/78-PATTERNS.md
@.planning/phases/78-release-and-repository-closeout/78-VALIDATION.md
@.github/workflows/release.yml
@.github/workflows/publish-hex.yml
@test_apps/phoenix_adopter/mix.exs
@test_apps/phoenix_adopter/test/core_flow_test.exs
</context>

<tasks>

<task type="tracer">
  <name>Trace one published Hex release through registry, docs, and Phoenix consumption</name>
  <reversibility rating="reversible">The verifier and test-only dependency selector can be changed without altering the published SDK contract.</reversibility>
  <read_first>test_apps/phoenix_adopter/mix.exs; test_apps/phoenix_adopter/test/core_flow_test.exs; test_apps/phoenix_adopter/README.md; .planning/phases/78-release-and-repository-closeout/78-RESEARCH.md</read_first>
  <files>test_apps/phoenix_adopter/mix.exs, test_apps/phoenix_adopter/README.md, scripts/maintainer/published_hex_adopter_smoke.sh, scripts/maintainer/release_evidence_check.sh</files>
  <action>Per D-02, add an explicit release-only Mix dependency selector that requires a requested exact version and uses a Hex tuple; retain the existing path tuple as the default for PR CI. The smoke script must run in an isolated temporary lock/deps/build context, assert Mix resolved lattice_stripe from Hex at exactly that version, compile, and run the existing synthetic core-flow test with no Stripe credential or live request. The evidence script accepts a full SHA and exact SemVer, checks that the SHA is the tag's peeled commit and on main, verifies the matching GitHub Release and newest completed ci-gate on that SHA, checks public Hex release metadata and versioned HexDocs, then invokes the Hex smoke. Before implementing checksum comparison, inspect the documented Hex release response and a known 2.2.2 artifact to establish whether the authoritative checksum covers outer tarball or package contents; compare like representations and report both values. Use bounded registry/docs retries and fail closed on unknown/auth/parse errors. Document one maintainer command and explicit published-source assertion.</action>
  <verify>
    <automated>scripts/maintainer/release_evidence_check.sh --version 2.2.2 --sha "$(git rev-list -n 1 v2.2.2)"</automated>
    <fails_when>Any required SHA, CI, package version/checksum, versioned docs, or Hex-source assertion differs or cannot be read; the script exits nonzero with the failing component.</fails_when>
  </verify>
  <acceptance_criteria>Known published 2.2.2 can be checked end to end from its immutable tag SHA, and the smoke's Mix metadata shows a Hex dependency at 2.2.2.</acceptance_criteria>
  <done>A real release can be reconciled through the same command without switching to local path bytes.</done>
</task>

<task type="auto">
  <name>Guard negative release cases and run the verifier at the publication boundary</name>
  <read_first>.github/workflows/release.yml; .github/workflows/publish-hex.yml; scripts/maintainer/release_evidence_check.sh; .planning/phases/78-release-and-repository-closeout/78-VALIDATION.md</read_first>
  <files>scripts/maintainer/test_release_evidence_check.sh, .github/workflows/release.yml, .github/workflows/publish-hex.yml</files>
  <action>Per D-02 and D-03, add deterministic command/API fixtures for wrong tag SHA, stale or absent ci-gate, wrong Hex version/source, checksum mismatch, and absent versioned docs; prove each returns a named blocker. Invoke the same verifier after routine publication and after non-dry-run manual recovery, passing the resolved SHA/version; preserve docs-only recovery's separate source-SHA provenance and existing dry-run/idempotency checks. Keep publish credentials scoped only to the publishing step, and keep the cold adopter smoke secret-free. Avoid a new recurring PR CI lane because published bytes exist only at release time.</action>
  <verify>
    <automated>bash scripts/maintainer/test_release_evidence_check.sh</automated>
    <fails_when>A required negative fixture passes, a normal fixture fails, or workflow wiring omits the resolved immutable SHA and exact version.</fails_when>
  </verify>
  <acceptance_criteria>Routine and recovery release jobs call the verifier only after public publication, and fixture failures identify the failed release fact.</acceptance_criteria>
  <done>Published-package and exact-SHA checks are automatic at the release boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub/Hex/HexDocs to release evidence | Remote metadata may be missing, stale, or mismatched. |
| Published Hex package to test host | Mix must fetch registry bytes rather than the local checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-01 | Tampering | tag/check/Hex chain | high | mitigate | Require one full tag SHA, exact-SHA ci-gate, matching release target, and authoritative checksum parity. |
| T-78-02 | Spoofing | adopter dependency source | medium | mitigate | Assert Mix dependency metadata names Hex registry and exact version in isolated state. |
| T-78-03 | Information disclosure | Hex publish credential | high | mitigate | Keep HEX_API_KEY only on publish steps; post-publish smoke uses no secret. |
</threat_model>

## Artifacts this phase produces

- A reusable post-publish verifier, a Hex-sourced Phoenix smoke, and deterministic failure fixtures.

<verification>Run the fixture test, known 2.2.2 release check, actionlint/CI, and the existing path-mode adopter command: `cd test_apps/phoenix_adopter && mix deps.get --check-locked && mix test --warnings-as-errors`.</verification>
<success_criteria>A real published release has one passing SHA-bound machine report, including an actual Hex dependency resolution and synthetic host flow.</success_criteria>
<output>Create `.planning/phases/78-release-and-repository-closeout/78-01-SUMMARY.md`.</output>
