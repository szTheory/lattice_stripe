---
phase: 78-release-and-repository-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/maintainer/release_candidate_check.sh
  - scripts/maintainer/test_release_candidate_check.sh
  - .github/workflows/release-pr-automerge.yml
  - docs/maintainer-release.md
autonomous: true
requirements: [REL-01, CLOSE-01]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "release_candidate_check.sh proves the final API-lock delta is additive, the default Stripe API pin is unchanged, and Release Please proposes the SemVer-consistent 2.3.0 version (test_release_candidate_check.sh)."
    - "Release PR Auto-Merge cannot bypass protection and reports the current PR head/check/merge blocker when protected merge fails (actionlint and workflow fixture review)."
  artifacts:
    - scripts/maintainer/release_candidate_check.sh
    - scripts/maintainer/test_release_candidate_check.sh
    - .github/workflows/release-pr-automerge.yml
  key_links:
    - "Release candidate check reads the generated PR, not a hand-edited manifest."
    - "Protected merge follows ci-gate on the exact current PR head and retains explicit CI/release dispatch after merge."
---

<objective>
Make the 2.3.0 compatibility claim executable and make routine release merging honor repository protection. D-01, D-03, and D-04 govern this plan.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/78-release-and-repository-closeout/78-CONTEXT.md
@.planning/phases/78-release-and-repository-closeout/78-RESEARCH.md
@.planning/phases/75-typed-contract-updates/75-VERIFICATION.md
@.planning/phases/75-typed-contract-updates/75-EVIDENCE.md
@.github/workflows/release-pr-automerge.yml
@guides/api_stability.md
@docs/maintainer-release.md
</context>

<tasks>

<task type="auto">
  <name>Check the Release Please proposal against final compatibility evidence</name>
  <reversibility rating="costly">Publishing a wrong SemVer or changed default API pin would require a corrective public release and adopter guidance.</reversibility>
  <read_first>priv/api/current.txt; test/lattice_stripe/api_surface_lock_test.exs; test/lattice_stripe/version_prose_test.exs; mix.exs; lib/lattice_stripe.ex; .release-please-manifest.json; release-please-config.json; .planning/phases/75-typed-contract-updates/75-VERIFICATION.md</read_first>
  <files>scripts/maintainer/release_candidate_check.sh, scripts/maintainer/test_release_candidate_check.sh</files>
  <action>Per D-01 and D-02, inspect the final public API lock against the v2.2.2 baseline, require only the four reviewed optional-field additions unless a new compatibility decision is recorded, and rerun the API-surface and version-prose locks. Read the actual open Release Please PR's proposed mix.exs/manifest/changelog at its head; require matching 2.3.0 for the additive candidate, with no manual version override. Assert `LatticeStripe.api_version/0` remains `2026-03-25.dahlia`. On unexpected diff or proposal, fail with the precise field/version and require investigation before merge. Add fixture cases for a removed API row, changed pin, absent PR, stale PR head, and wrong version.</action>
  <verify>
    <automated>bash scripts/maintainer/test_release_candidate_check.sh && mix test test/lattice_stripe/api_surface_lock_test.exs test/lattice_stripe/version_prose_test.exs</automated>
    <fails_when>A negative fixture passes, the proposal differs from the source diff, the pin moves, or a standing compatibility lock fails.</fails_when>
  </verify>
  <acceptance_criteria>The generated release proposal is accepted only when source diff, version, changelog and default API pin agree with Phase 75 evidence.</acceptance_criteria>
  <done>A maintainer can run one command and see an actionable compatibility verdict on the current Release Please head.</done>
</task>

<task type="auto">
  <name>Preserve branch protection during Release Please auto-merge and recovery</name>
  <read_first>.github/workflows/release-pr-automerge.yml; .github/workflows/release.yml; .github/workflows/publish-hex.yml; docs/maintainer-release.md</read_first>
  <files>.github/workflows/release-pr-automerge.yml, docs/maintainer-release.md</files>
  <action>Per D-01, D-03 and D-04, run the trusted-main candidate preflight against the current Release Please PR before merge, and remove the admin override path. After a failed normal protected merge, query the current PR head, mergeability/review state, required check conclusions and conversation state available through GitHub, then fail closed with PR number, full head SHA and unmet rule or an explicit unknown diagnostic. Recheck the head and latest ci-gate before each retry; no success result may come from another head. Keep the existing GITHUB_TOKEN event and explicit dispatch path; retain release and recovery job token scopes. Update the maintainer guide to describe the minor release and the specific protected-merge recovery action, not blanket human UAT.</action>
  <verify>
    <automated>bash scripts/maintainer/test_release_candidate_check.sh && ./scripts/maintainer/repo_hygiene_check.sh --ci</automated>
    <fails_when>The workflow still contains a protection bypass, allows a stale PR head, or the repository-owned hygiene checks report a blocker.</fails_when>
  </verify>
  <acceptance_criteria>A failed protected merge leaves the PR open and identifies the actual blocking state; the successful path still dispatches CI and Release.</acceptance_criteria>
  <done>Routine release cannot bypass required ci-gate or resolved conversations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release Please PR to protected main | Generated version and PR head are untrusted until compatibility and CI checks pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-04 | Elevation of privilege | auto-merge workflow | high | mitigate | Remove administrator merge path; use protected normal merge only. |
| T-78-05 | Tampering | version/API proposal | high | mitigate | Compare final lock diff, pin, and generated PR head/version before merge. |
| T-78-06 | Repudiation | failed merge | medium | mitigate | Emit PR/head/check/merge-state diagnostic and retain the open PR. |
</threat_model>

## Artifacts this phase produces

- A source-backed candidate preflight, negative fixtures, protected merge workflow, and maintainer recovery guidance.

<verification>Run the candidate fixtures, focused API/version tests, hygiene CI mode, and the existing Actions actionlint lane.</verification>
<success_criteria>The current generated release proposal has a reproducible compatibility verdict and can only merge under normal branch protection.</success_criteria>
<output>Create `.planning/phases/78-release-and-repository-closeout/78-02-SUMMARY.md`.</output>
