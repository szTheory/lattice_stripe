---
phase: 74-versioned-stripe-drift-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
autonomous: false
requirements:
  - DRIFT-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "When a successful current inventory or dated exact-path inventory is available, every path in it for an already-supported resource has an evidence-backed disposition; when neither is available, the artifact states PARTIAL / NOT EXHAUSTIVE, records the failed refresh and missing exact-path artifact, and leaves inventory membership unconfirmed without claiming complete coverage."
    - "Every selected or deferred candidate has an exact path, stable versioned Stripe evidence, explicit status, exact GA OpenAPI snapshot identity or unconfirmed disposition, adopter job, operational and semantic rationale, type/decode implications, and reopening evidence when deferred; ranking does not use counts, recency, a fixed score, or a resource quota."
    - "The default API version decision is recorded against inspected compatibility evidence and remains 2026-03-25.dahlia in this phase; any later move requires the separate D-10 compatibility review."
    - "The decision artifact is saved atomically, and a repeated or parallel writer detects intervening edits before replacement instead of silently overwriting them."
  artifacts:
    - path: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      provides: "Human-reviewable Phase 74 selection, deferral, source-provenance, and API-pin decision record"
  key_links:
    - from: .planning/threads/v1-12-next-milestone-assessment.md and any successful current drift output
      to: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      via: "Exact-path inventory and supported-resource intersection when a successful current output or dated exact-path inventory exists; otherwise an explicit partial-coverage record with unconfirmed membership (D-01). A dated count-only summary is not an exact-path inventory."
    - from: Official dated Stripe changelog or explicitly versioned API reference and the exact official GA /latest OpenAPI snapshot
      to: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      via: "Stable API-version classification, exact field-path corroboration, and recorded snapshot identity; preview and unconfirmed findings cannot be selected (D-02, D-03)."
    - from: .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md
      to: Phase 75 selected-field implementation
      via: "Each selected row supplies the version floor, response scope, and type/decode implications while preserving extra and open-enum behavior (D-03, D-09, D-11)."
  assumptions:
    - id: EDGE-74-01
      status: flagged
      statement: "Same-directory atomic rename and a pre-write content-hash check are available for the Markdown artifact; if either cannot be used, retain the last complete file and stop for safe recovery."
---

<objective>
Create the sole Phase 74 decision artifact: a concise, reviewable triage of evidence-qualified changes in already-supported Stripe resources, with a bounded selection and explicit deferrals.

Purpose: Separate stable, versioned Stripe contract changes from preview changes and OpenAPI-shape noise, then recommend Phase 75 work by adopter value and operational consequence. Keep the package default API version at 2026-03-25.dahlia through this phase and document the evidence required for a separately reviewed future move.
Output: `.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md`; no source code, tests, generator, validator, new resource family, issue-per-field workflow, or API-pin migration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-versioned-stripe-drift-triage/74-CONTEXT.md
@.planning/phases/74-versioned-stripe-drift-triage/74-RESEARCH.md
@.planning/phases/74-versioned-stripe-drift-triage/74-PATTERNS.md
@.planning/phases/74-versioned-stripe-drift-triage/74-VALIDATION.md
@.planning/phases/74-versioned-stripe-drift-triage/74-COVERAGE.md
@.planning/threads/v1-12-next-milestone-assessment.md
@.planning/JTBD-MAP.md
@guides/api_stability.md
@lib/lattice_stripe.ex

Research limitation carried forward: the local `mix lattice_stripe.check_drift` attempt failed before running because Mix could not open its PubSub TCP socket, and the researcher could not inspect the exact 4.31 MB GA OpenAPI snapshot. The dated milestone note reports 104 additions across 23 modules but does not contain their 104 exact paths; current phase/repository evidence includes no separate dated exact-path inventory. Treat that count-only note as context, not an inventory. At execution, retry the current drift task and check for any newly available dated exact-path artifact. If both sources are unavailable, record `Inventory coverage: PARTIAL / NOT EXHAUSTIVE`, the refresh failure, and the missing exact-path artifact; make no complete-ledger or exhaustive-coverage claim. Keep the research leads (`Invoice.amount_paid_off_stripe`, the exact Refund paths in the cited changelog, `Dispute.payment_method_details.card.network`, Invoice Item frozen fields, and Billie support) explicitly marked as inventory-membership-unconfirmed and deferred until an exact inventory permits intersection. For any lead whose inventory membership is confirmed, verify exact paths in the identified GA snapshot and decoder/type semantics before selection. Any missing or ambiguous source evidence remains unconfirmed/deferred.

Scope and policy: address DRIFT-01 only. The artifact records whether candidate accounting is complete or partial and does not present the requirement as fully covered under the partial-inventory branch. Carry D-01–D-11 from CONTEXT.md into the artifact and actions. The spec-less edge probe surfaced one unresolved concurrency question; it is preserved in `must_haves.assumptions` as EDGE-74-01 and addressed by the atomic-save/edit-detection truth. The phase coverage declaration says there is no external API integration: this is public-source review, not Stripe API integration work.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one lead through the available inventory and evidence gates</name>
  <files>.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</files>
  <read_first>
  <precondition>The target triage file is absent, or its existing content has been read and its current hash recorded. If it changed after inspection or an existing decision cannot be reconciled, stop and preserve it for explicit merge rather than replacing it.</precondition>
  <action>Start `74-TRIAGE.md` with the review date, scope, current pin, evidence method, the four statuses `current stable`, `later stable`, `preview-only`, and `unconfirmed/schema noise`, a separate inventory-coverage field, and a compact candidate row schema. Retry `mix lattice_stripe.check_drift` and inspect the milestone assessment plus repository artifacts for a dated exact-path inventory; record the exact source path and retrieval/run result. Use a successful current exact-path output or an available dated exact-path list to trace one candidate belonging to an already-supported resource. A 104-addition count without its paths is not sufficient to assert membership. If neither exact-path source is available, record `PARTIAL / NOT EXHAUSTIVE`, the drift failure and missing dated exact-path artifact, and create one research-lead row with `inventory membership: unconfirmed` and disposition `deferred`; do not describe that row as an inventory intersection or the ledger as complete (D-01). For a membership-confirmed candidate, follow the path end to end: cite its exact dated stable source, compare its minimum API version with `2026-03-25.dahlia`, and inspect the exact field path in the official GA `/latest` OpenAPI JSON. Record the source URL, immutable commit/ref or snapshot identity, retrieval date, JSON path, and SHA-256; if unavailable or ambiguous, mark GA corroboration `unconfirmed` and defer it (D-02, D-03). Inspect the resource decoder and response scope; record optionality, nullability, ID/object shape, units, nested path, and event-versus-retrieval scope as applicable. Preserve unknown response keys in `extra`, keep enum-like upstream values open, and treat any runtime probe only as corroboration after stable-source qualification (D-04, D-11). Add the adopter job, operational/semantic rationale, disposition, and specific reopening evidence (D-05–D-09). This traced row proves the evidence path for the next ledger task while preserving the partial-inventory branch. Save by writing a complete temporary file in the same directory and atomically renaming it into place. Immediately before replacement, compare the current target hash with the hash captured on read; if another writer changed it, stop and reconcile rather than clobbering it.</action>
  <verify>
    <automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
    <fails_when>Exit status is non-zero because of whitespace errors; this command proves whitespace hygiene only. Inspect the new file directly because untracked content is outside the ordinary worktree diff until staged.</fails_when>
    <human-check>Trace the row from a successful or dated exact-path inventory to its supported resource, cited Stripe source, and exact GA snapshot. If no exact-path inventory is available, confirm the failure and missing artifact are recorded, inventory coverage is PARTIAL / NOT EXHAUSTIVE, and row membership is unconfirmed/deferred. Verify version/status, pin applicability, type/decode note, adopter rationale, and reopening evidence against the available primary sources.</human-check>
  </verify>
  <acceptance_criteria>
    - `74-TRIAGE.md` exists and contains one fully populated candidate row or research-lead row, plus evidence method, status definitions, and inventory-coverage state.
    - If an exact-path inventory is available, the traced candidate's membership and already-supported resource are confirmed; otherwise its membership is explicitly unconfirmed/deferred and the artifact states PARTIAL / NOT EXHAUSTIVE with the failed refresh and missing exact-path artifact recorded.
    - GA corroboration is recorded only with exact snapshot identity and field path; unavailable or ambiguous source access results in an unconfirmed deferral and reopening evidence.
    - The file replacement uses same-directory temp plus atomic rename and detects a changed target hash before replacement (EDGE-74-01).
  </acceptance_criteria>
  <done>The artifact has one fully traced row whose inventory membership and source qualification match available evidence, and the write path preserves complete prior content on interruption or concurrent edits.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the bounded evidence ledger and API-pin rationale</name>
  <files>.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</files>
  <read_first>
    .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md, .planning/phases/74-versioned-stripe-drift-triage/74-CONTEXT.md, .planning/phases/74-versioned-stripe-drift-triage/74-RESEARCH.md, .planning/phases/74-versioned-stripe-drift-triage/74-PATTERNS.md, .planning/threads/v1-12-next-milestone-assessment.md, .planning/JTBD-MAP.md, and each relevant resource decoder after exact candidate paths are established.</read_first>
  <action>Build the candidate ledger from the best exact evidence actually available. When a successful current output or dated exact-path inventory exists, enumerate every exact path in that input whose resource is already supported, intersect by full path, and give every in-scope path a disposition; preserve exact source location and inventory date/run identity. Check each named research lead—`Invoice.amount_paid_off_stripe`, the exact Refund paths in its cited changelog, `Dispute.payment_method_details.card.network`, Invoice Item frozen fields, and Billie support—against the exact paths rather than inferring membership from a resource-family mention. When neither exact-path source exists, keep inventory coverage `PARTIAL / NOT EXHAUSTIVE`, record the current refresh failure and the checked locations where the dated exact-path artifact was unavailable, include the research leads as membership-unconfirmed/deferred rows, and identify the successful current output or dated exact-path list needed to reopen each. Do not claim a complete ledger or exhaustive candidate coverage in this branch (D-01). For every candidate row, verify the full field path, dated stable source and explicit minimum API version, current-pin applicability, and exact official GA snapshot identity plus JSON path, or explicitly mark the evidence unconfirmed; preview-only or unconfirmed findings remain deferred. Inspect each row's resource decoder and response scope, including type, nullability, ID/object shape, units, nested path, and event-versus-retrieval limits where applicable. Preserve unknown response keys in `extra` and keep enum-like Stripe values open (D-02–D-04, D-11). Record per-row adopter job, operational consequence, semantic rationale, type/decode implications, disposition, and specific reopening evidence for deferrals (D-05, D-06, D-09). Explain prioritization by adopter work, operational consequence, and coherent additions, without count, recency, numeric-score, or quota ranking (D-05–D-07). Keep the artifact concise, human-reviewable, and limited to the Markdown decision record (D-08). Add an API-version section naming the source of `2026-03-25.dahlia`, inspected compatibility evidence, candidate version floors/applicability, and gaps against the full D-10 review. Record the pin as `unchanged` for Phase 74 and document that any later move requires a separate review covering the intervening changelog, request semantics, response decoding, webhook event/version behavior, supported adopter flows, and package SemVer impact. If evidence for any part is absent, name it as a gap; this task does not migrate the pin (D-10). Before replacement, compare the current file hash to the inspected hash and stop for reconciliation on an intervening change; save the complete revision through a same-directory temporary file and atomic rename.</action>
  <verify>
    <automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
    <fails_when>Exit status is non-zero because of whitespace errors; this command proves whitespace hygiene only. Inspect the entire artifact directly for content and accidental truncation.</fails_when>
    <human-check>Review each candidate row against its exact path, inventory-membership state, stable versioned source, API version/status, GA snapshot provenance or explicit unconfirmed state, pin applicability, adopter job, rationale, type/decode implications, disposition, and reopening evidence. If inventory coverage is partial, verify that candidates with unconfirmed membership remain deferred and no complete-coverage claim appears. Check the D-10 pin rationale and evidence gaps.</human-check>
  </verify>
  <acceptance_criteria>
    - Every considered in-scope candidate has a disposition and all D-09 evidence; every deferral names both a reason and specific reopening evidence.
    - With an exact-path inventory, every path in that input whose resource is already supported is accounted for as selected, deferred, preview-only, or unconfirmed, including low-priority and unresolved paths.
    - Without a successful current inventory and without a dated exact-path artifact, the ledger is explicitly PARTIAL / NOT EXHAUSTIVE; the refresh failure and missing artifact are recorded, research leads are membership-unconfirmed/deferred, and no complete-ledger or exhaustive-coverage claim is made.
    - Each named research lead is checked by exact path against any available inventory; if no path list is available, its membership remains unconfirmed. No field is treated as inventory-backed or GA-confirmed by assumption.
    - Every selected candidate has stable dated/versioned provenance, confirmed inventory membership, and exact GA snapshot corroboration; unavailable or ambiguous evidence is labeled unconfirmed/deferred.
    - The selected set is bounded through named adopter work and operational rationale, not candidate count, recency, fixed score, or quota (D-05–D-07).
    - The artifact contains no implementation, package, generator, validator, added-resource-family, or per-field-issue work (D-08 and phase boundary).
    - The API-pin section records `2026-03-25.dahlia`, candidate version applicability, the compatibility evidence inspected, and any missing elements of D-10; the Phase 74 decision remains `unchanged`, and any later move requires a separate review.
  </acceptance_criteria>
  <done>The record gives a complete, exact-path ledger only when an exact-path inventory is available; otherwise it carries an explicit partial/non-exhaustive boundary and unconfirmed/deferred memberships. All considered rows and the unchanged-pin rationale have candidate-level evidence and reopening criteria sufficient for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Approve the source-by-source triage review</name>
  <what-built>The completed `74-TRIAGE.md` selection, deferral, source-provenance, and API-pin decision record.</what-built>
  <how-to-verify>
    Review D-01–D-11 against the completed artifact. For each selected and deferred candidate, inspect its exact path, inventory-membership state, dated stable Stripe source, API version, and GA/preview/status classification. Confirm selected paths have exact GA snapshot identity and field-path evidence; absent or ambiguous corroboration must remain unconfirmed/deferred. If both exact-path inventory sources are unavailable, confirm the failed refresh, missing dated artifact, PARTIAL / NOT EXHAUSTIVE coverage label, and unconfirmed/deferred membership for each research lead; the artifact must not imply complete candidate accounting. Check current-pin versus later-version applicability, adopter job, type/decode rationale, and reopening evidence for every deferral. Confirm the pin remains `2026-03-25.dahlia` and the D-10 review gaps are accurately named. Confirm `extra` and open-enum compatibility, documentation-only scope, and that `git diff --check` is described only as whitespace validation.
  </how-to-verify>
  <resume-signal>Type `approved` only after every review criterion passes, including the partial-coverage safeguards when applicable, or describe the specific rows or claims that need correction so the artifact can be revised before approval.</resume-signal>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL | Distinguish stable/versioned contract changes from preview and OpenAPI noise, then select by adopter value and operational consequence | Tasks 1–3 | COVERED |
| REQ | DRIFT-01 | Tasks 1–3; frontmatter requirements | COVERED |
| RESEARCH | Retry current drift and inspect for dated exact-path list; if neither exists, record failure and missing artifact as PARTIAL / NOT EXHAUSTIVE without complete-coverage claim | Tasks 1–2 | COVERED |
| RESEARCH | Exact 4.31 MB GA OpenAPI snapshot was inaccessible; exact candidate path plus snapshot identity required, otherwise unconfirmed/deferred | Tasks 1–3 | COVERED |
| RESEARCH | Later-stable leads: Invoice amount paid off Stripe; exact Refund paths; Dispute card network; Invoice Item frozen fields; Billie support | Task 2, exact-path inventory state and candidate-level evidence recorded; membership remains unconfirmed when no path list exists | COVERED |
| RESEARCH | Hand-written decoders, `extra`, open enum values, event/retrieval scope; no package dependency | Tasks 1–3 | COVERED |
| CONTEXT | D-01–D-04: discovery/evidence/status/probe boundaries | Tasks 1–2 | COVERED |
| CONTEXT | D-05–D-07: adopter and operational prioritization; no count, recency, score, or quota ranking | Task 2 | COVERED |
| CONTEXT | D-08–D-09: concise Markdown artifact and row-level evidence/deferrals | Tasks 1–2 | COVERED |
| CONTEXT | D-10: unchanged default in Phase 74; any later move requires separate compatibility review | Tasks 2–3 | COVERED |
| CONTEXT | D-11: preserve `extra` and open enum semantics | Tasks 1–3 | COVERED |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Stripe public changelog/reference/OpenAPI → triage record | External contract claims enter a maintainer decision record; GA versus preview and snapshot identity must remain explicit. |
| Repeated or parallel local writer → triage file | A second run may update the same `.planning` artifact; partial writes or silent replacement could lose a decision. |
| Runtime security | Not applicable: no application endpoint, secret, authentication, authorization, session, input, or cryptographic behavior is changed; no live Stripe credentials are required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-74-01 | Tampering | Source status and candidate qualification | high | mitigate | Require a dated/versioned official source, exact inventory path, and exact official GA snapshot path/identity before selection; inaccessible evidence is explicitly unconfirmed/deferred. |
| T-74-02 | Tampering | `.planning` decision artifact | medium | mitigate | Write complete same-directory temporary content, compare the current hash immediately before atomic rename, and stop for reconciliation on concurrent changes. |
| T-74-03 | Repudiation | Candidate provenance rows | medium | mitigate | Store exact URLs, API-version/status, snapshot revision/hash, path, review date, and per-row rationale in the artifact. |
| T-74-04 | Information disclosure | Public-source review | low | mitigate | Use only public Stripe sources; do not put secrets in the record or require live credentials/probes. |
| T-74-05 | Elevation of privilege | Runtime package | low | accept | No runtime or authorization behavior is in scope; no package is installed and no privilege path changes. |
</threat_model>

<verification>
`git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md` checks whitespace only in the uncommitted artifact, including its initial creation. No code tests are added or run. Human source-by-source review is mandatory for every selected and deferred row, exact candidate-inventory membership, GA provenance/status, type/decode implications, reopening evidence, and the unchanged API pin. If both exact-path inventory sources are unavailable, the record must say PARTIAL / NOT EXHAUSTIVE and mark membership unconfirmed/deferred; if any source claim cannot be resolved, that row remains unconfirmed/deferred. Whitespace hygiene does not establish Stripe status or adopter value.

<automated>git add --intent-to-add .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md && git diff --check HEAD -- .planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md</automated>
<fails_when>Any non-zero exit because of whitespace errors; `git diff --check` does not detect conflict markers, source status, or semantic value, so the untracked artifact content must be opened and reviewed directly.</fails_when>
<human-check>Review every row against its primary Stripe source and the identified exact GA snapshot. Verify the four status classes and current-pin applicability, the semantic/type rationale and adopter job, every deferral's reopening evidence, the pin decision, and D-01–D-11. Confirm the file is safely updated and the phase remains documentation-only.</human-check>
</verification>

<success_criteria>
- Inventory coverage is labeled complete only when grounded in a successful current exact-path output or a dated exact-path inventory; if neither exists, the artifact records the failed refresh and missing input, is explicitly PARTIAL / NOT EXHAUSTIVE, and makes no complete-ledger or exhaustive-coverage claim.
- Every promoted candidate has confirmed exact-path inventory membership, stable versioned Stripe provenance, and exact official GA snapshot corroboration; unresolved membership or source evidence remains unconfirmed/deferred.
- All selected/deferred rows contain required adopter, semantic, type/decode, and disposition detail; deferrals identify reopening evidence.
- Unconfirmed and preview-only evidence is not promoted; `extra` and open-enum semantics are preserved in the Phase 75 recommendations.
- The recorded default remains `2026-03-25.dahlia` in Phase 74; any later move requires D-10's separate compatibility review, and this plan performs no pin migration.
- Human source-by-source review is complete; `git diff --check` is represented only as whitespace validation and not as proof of inventory coverage, Stripe status, or adopter value.
</success_criteria>

<output>
Create `.planning/phases/74-versioned-stripe-drift-triage/74-TRIAGE.md` when done.
</output>
