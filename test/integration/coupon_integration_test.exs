defmodule LatticeStripe.Integration.CouponTest do
  use ExUnit.Case, async: false

  @moduletag :integration
  @moduletag :wave0_stub

  # stripe-mock integration scaffold — populated by Plan 12-05.
  test "wave 0 stub" do
    assert true
  end
end
