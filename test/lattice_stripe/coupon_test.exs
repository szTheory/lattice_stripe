defmodule LatticeStripe.CouponTest do
  use ExUnit.Case, async: true

  @moduletag :wave0_stub

  # Placeholder so `mix test` discovers this file.
  # Real tests land in Plan 12-05.
  test "wave 0 stub" do
    assert true
  end
end
