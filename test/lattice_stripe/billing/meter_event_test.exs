defmodule LatticeStripe.Billing.MeterEventTest do
  use ExUnit.Case, async: true
  # Wave 0 skeleton — filled in by Plan 20-04
  @moduletag :pending

  test "wave 0 placeholder" do
    assert true
  end
end
