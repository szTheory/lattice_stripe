defmodule LatticeStripe.Billing.MeterIntegrationTest do
  use ExUnit.Case, async: true
  # Wave 0 skeleton — filled in by Plan 20-03
  @moduletag :pending
  @moduletag :integration

  test "wave 0 placeholder" do
    assert true
  end
end
