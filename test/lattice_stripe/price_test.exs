defmodule LatticeStripe.PriceTest do
  use ExUnit.Case, async: true

  @moduletag :wave0_stub

  # Placeholder so `mix test` discovers this file.
  # Real tests land in Plan 12-04.
  test "wave 0 stub" do
    assert true
  end
end
