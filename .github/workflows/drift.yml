name: Stripe API Drift Check

on:
  schedule:
    - cron: '0 9 * * 1'  # Mondays at 9:00 UTC
  workflow_dispatch:       # Allow manual trigger from Actions tab

permissions:
  contents: read
  issues: write

jobs:
  drift-check:
    name: Stripe API Drift Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - uses: erlef/setup-beam@v1
        with:
          elixir-version: '1.19'
          otp-version: '28'

      - name: Cache deps
        uses: actions/cache@v5
        with:
          path: deps
          key: ${{ runner.os }}-1.19-28-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-1.19-28-mix-

      - name: Cache _build
        uses: actions/cache@v5
        with:
          path: _build
          key: ${{ runner.os }}-1.19-28-build-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-1.19-28-build-

      - name: Install dependencies
        run: mix deps.get

      - name: Compile
        run: mix compile

      - name: Ensure drift issue labels exist
        run: |
          gh label create "stripe-drift" \
            --color "#e4e669" \
            --description "Stripe OpenAPI spec fields not yet in @known_fields" \
            --force
          gh label create "maintenance" \
            --color "#0e8a16" \
            --description "Sustaining maintenance — not release-blocking" \
            --force
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run drift check
        id: drift
        continue-on-error: true
        run: |
          set +e
          mix lattice_stripe.check_drift --summary 2>&1 | tee /tmp/drift_summary.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"

      - name: Create or update drift issue
        if: steps.drift.outputs.exit_code == '1'
        run: |
          EXISTING=$(gh issue list \
            --label "stripe-drift" \
            --state open \
            --json number \
            --jq '.[0].number // empty')

          SUMMARY=$(cat /tmp/drift_summary.txt)
          TITLE="Stripe API drift tracker (maintenance)"

          if [ -n "$EXISTING" ]; then
            gh issue comment "$EXISTING" --body "$SUMMARY"
          else
            gh issue create \
              --title "$TITLE" \
              --body "$SUMMARY" \
              --label "stripe-drift" \
              --label "maintenance"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
